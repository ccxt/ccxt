// ----------------------------------------------------------------------------

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code
// EDIT THE CORRESPONDENT .ts FILE INSTEAD

import assert from 'assert';
import { AuthenticationError } from '../../../base/errors.js';
import ccxt from '../../../../ccxt.js';
// native ts test, intentionally not transpiled - pins the single-flight
// authentication logic from https://github.com/ccxt/ccxt/issues/29393 on
// ccxt.pro.hashkey. the logic is inlined directly into authenticate (), so
// there is no helper method to unit-test: this file is the only guard, and no
// build/lint gate sees it - dropping the in-progress early-return or the
// flight settlement still compiles and only surfaces as duplicate listenKey
// fetches (plus duplicate keepalive timers) against a live venue
function sleep(ms) {
    return new Promise((resolve) => setTimeout(resolve, ms));
}
function flightClient(exchange) {
    // the flights live on a never-dialed client keyed 'authenticationFlights'
    // - see the inlined single-flight block in hashkey#authenticate
    const clients = exchange.clients;
    if (!('authenticationFlights' in clients)) {
        return undefined;
    }
    return clients['authenticationFlights'];
}
function flightCount(exchange) {
    // the flight IS the future: registered by the leader in client.futures
    // before the first await and dropped by client.resolve () /
    // client.reject () when it settles
    const client = flightClient(exchange);
    if (client === undefined) {
        return 0;
    }
    return Object.keys(client.futures).length;
}
function parkedRejectionCount(exchange) {
    // client.reject () parks the error in rejections when no future is
    // registered - a parked rejection would poison the FIRST waiter of the
    // next retry flight, so this must stay empty too
    const client = flightClient(exchange);
    if (client === undefined) {
        return 0;
    }
    return Object.keys(client.rejections).length;
}
function assertNoFlightResidue(exchange, label) {
    assert(flightCount(exchange) === 0, label + ': a settled flight must leave no future behind (got ' + flightCount(exchange).toString() + ')');
    assert(parkedRejectionCount(exchange) === 0, label + ': a settled flight must park no rejection (got ' + parkedRejectionCount(exchange).toString() + ')');
}
function makeStubbedHashkey(state) {
    const exchange = new ccxt.pro.hashkey({
        'apiKey': 'test-api-key',
        'secret': 'test-secret',
    });
    // stub the listenKey endpoint: count fetches, hold the response open long
    // enough for concurrent callers to pile onto the flight
    exchange.privatePostApiV1UserDataStream = async () => {
        state.fetches = state.fetches + 1;
        await sleep(50); // the race window
        return { 'listenKey': 'HASHKEY-KEY-' + state.fetches.toString() };
    };
    // neutralize the keepalive scheduler and count the schedules - hashkey's
    // distinctive symptom is that every racing caller also arms its own
    // keepAliveListenKey timer, which then self-reschedules forever
    exchange.delay = () => {
        state.delays = state.delays + 1;
    };
    return exchange;
}
async function testHashkeyAuthenticateSingleFlight() {
    // N concurrent authenticate () calls on a cold instance must produce
    // exactly ONE fetch, cache the leader key, and arm exactly ONE keepalive
    const state = { 'fetches': 0, 'delays': 0 };
    const exchange = makeStubbedHashkey(state);
    const keys = await Promise.all([
        exchange.authenticate(),
        exchange.authenticate(),
        exchange.authenticate(),
        exchange.authenticate(),
    ]);
    assert(state.fetches === 1, 'concurrent hashkey authenticates must elect exactly one leader (got ' + state.fetches.toString() + ' fetches)');
    assert(state.delays === 1, 'only the leader may schedule a keepAliveListenKey timer (got ' + state.delays.toString() + ' schedules)');
    assert(exchange.options['listenKey'] === 'HASHKEY-KEY-1', 'the leader listenKey must be cached');
    // every caller must receive the leader key: the waiters return the cached
    // credential, not undefined - getPrivateUrl () builds .../ws/<listenKey>
    // from this return value, so an undefined leaks straight into the ws url
    for (let i = 0; i < keys.length; i++) {
        assert(keys[i] === 'HASHKEY-KEY-1', 'every caller must receive the leader listenKey (caller ' + i.toString() + ' got ' + String(keys[i]) + ')');
    }
    assertNoFlightResidue(exchange, 'resolved flight');
    // a warm call returns the cached key without fetching or re-arming
    const warm = await exchange.authenticate();
    assert(warm === 'HASHKEY-KEY-1', 'a warm authenticate must return the cached listenKey');
    assert(state.fetches === 1, 'a warm authenticate must not fetch');
    assert(state.delays === 1, 'a warm authenticate must not schedule another keepalive');
}
async function testHashkeyAuthenticateEmptyKeyRejection() {
    // a hollow 200 must reject the flight BEFORE any cache write: on master
    // hashkey caches undefined silently, so getPrivateUrl () yields
    // .../ws/undefined and the early-return gate never re-mints
    const state = { 'fetches': 0, 'delays': 0 };
    const exchange = makeStubbedHashkey(state);
    exchange.privatePostApiV1UserDataStream = async () => {
        state.fetches = state.fetches + 1;
        await sleep(10);
        return {}; // hollow response, no listenKey
    };
    const outcomes = await Promise.allSettled([
        exchange.authenticate(),
        exchange.authenticate(),
    ]);
    assert(state.fetches === 1, 'concurrent hashkey authenticates must elect exactly one leader');
    // allSettled keeps input order but leader election does not: assert BOTH
    // entries reject with the typed error, so a waiter that swallows the
    // flight rejection and returns cannot pass silently
    assert(outcomes[0].status === 'rejected' && outcomes[1].status === 'rejected', 'both the leader and the waiter must throw on an empty listenKey');
    assert(outcomes[0].reason instanceof AuthenticationError, 'the hashkey leader must reject with AuthenticationError');
    assert(outcomes[1].reason instanceof AuthenticationError, 'the hashkey waiter must observe the same AuthenticationError');
    assert(exchange.safeString(exchange.options, 'listenKey') === undefined, 'an empty listenKey must never be cached');
    assert(state.delays === 0, 'a failed flight must not arm a keepalive timer');
    assertNoFlightResidue(exchange, 'rejected flight');
    // recovery: a good response re-leads and caches. a rejection parked under
    // the flight hash would be re-thrown here by client.future ()
    exchange.privatePostApiV1UserDataStream = async () => {
        state.fetches = state.fetches + 1;
        return { 'listenKey': 'HASHKEY-KEY-RETRY' };
    };
    const retried = await exchange.authenticate();
    assert(retried === 'HASHKEY-KEY-RETRY', 'a retry after a rejected flight must return the fresh listenKey');
    assert(exchange.options['listenKey'] === 'HASHKEY-KEY-RETRY', 'a retry after a rejected flight must re-lead and cache');
    assertNoFlightResidue(exchange, 'retried flight');
}
async function testHashkeyAuthenticateSoloLeaderRejection() {
    // an alone leader - the fetch fails before any waiter arrives - must
    // throw to its own caller, clear the slot, and NOT produce an unhandled
    // promise rejection (which would kill the process on the tick below).
    // the trailing `await future` is what attaches that handler
    const state = { 'fetches': 0, 'delays': 0 };
    const exchange = makeStubbedHashkey(state);
    exchange.privatePostApiV1UserDataStream = async () => {
        state.fetches = state.fetches + 1;
        throw new AuthenticationError('hashkey solo leader failure');
    };
    let soloError = undefined;
    try {
        await exchange.authenticate();
    }
    catch (e) {
        soloError = e;
    }
    assert(soloError instanceof AuthenticationError, 'a solo leader must throw its own error');
    // give the microtask queue a tick - an unhandled rejection fires here
    await sleep(20);
    assertNoFlightResidue(exchange, 'solo rejected flight');
    // the next caller becomes a fresh leader
    exchange.privatePostApiV1UserDataStream = async () => {
        state.fetches = state.fetches + 1;
        return { 'listenKey': 'HASHKEY-KEY-SOLO-RETRY' };
    };
    const retried = await exchange.authenticate();
    assert(retried === 'HASHKEY-KEY-SOLO-RETRY', 'the caller after a solo rejection must re-lead');
    assert(exchange.options['listenKey'] === 'HASHKEY-KEY-SOLO-RETRY', 'the caller after a solo rejection must cache the fresh key');
    assert(state.delays === 1, 'exactly one keepalive must be armed after the recovery');
    assertNoFlightResidue(exchange, 'solo retried flight');
}
async function testHashkeyAuthenticateKeepAliveClearRemint() {
    // keepAliveListenKey () clears options['listenKey'] when the refresh call
    // fails - the single-flight gate must not block that recovery: the next
    // cold caller has to re-lead and mint a fresh key
    const state = { 'fetches': 0, 'delays': 0 };
    const exchange = makeStubbedHashkey(state);
    await exchange.authenticate();
    assert(exchange.options['listenKey'] === 'HASHKEY-KEY-1', 'the first authenticate must cache a key');
    // simulate the keepalive failure path: the cached key is dropped
    exchange.options['listenKey'] = undefined;
    const reminted = await exchange.authenticate();
    assert(reminted === 'HASHKEY-KEY-2', 'a cleared listenKey must be re-minted by the next caller');
    assert(state.fetches === 2, 'a cleared listenKey must trigger exactly one new fetch');
    assertNoFlightResidue(exchange, 'reminted flight');
}
async function testWsSingleFlightWiringHashkey() {
    await testHashkeyAuthenticateSingleFlight();
    await testHashkeyAuthenticateEmptyKeyRejection();
    await testHashkeyAuthenticateSoloLeaderRejection();
    await testHashkeyAuthenticateKeepAliveClearRemint();
}
export default testWsSingleFlightWiringHashkey;
