// ----------------------------------------------------------------------------

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code
// EDIT THE CORRESPONDENT .ts FILE INSTEAD

import bit24 from './bit24';
/**
 *
 */
async function main() {
    // // Create an exchange instance (using Binance as an example)
    const exchange = new bit24();
    console.log('Exchange:', exchange.name);
    // Load markets
    const markets = await exchange.loadMarkets();
    console.log('Total markets:', Object.keys(markets).length);
    // eslint-disable-next-line no-restricted-syntax
    for (const market of Object.keys(markets).slice(0, 2)) {
        const ticker = await exchange.fetchTicker(market);
        console.log(market, ticker['last']);
    }
    console.log('First 5 market symbols:', Object.keys(markets).slice(0, 5));
}
main().catch(console.error);
