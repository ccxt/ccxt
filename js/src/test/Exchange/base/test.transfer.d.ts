import { Exchange } from "../../../../ccxt.js";
declare function testTransfer(exchange: Exchange, skippedProperties: object, method: string, entry: object, requestedCode: string): void;
export default testTransfer;
