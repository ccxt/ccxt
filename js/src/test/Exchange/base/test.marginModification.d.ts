import { Exchange } from "../../../../ccxt";
declare function testMarginModification(exchange: Exchange, skippedProperties: object, method: string, entry: object): void;
export default testMarginModification;
