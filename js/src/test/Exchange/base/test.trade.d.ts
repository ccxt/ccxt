import { Exchange } from "../../../../ccxt.js";
declare function testTrade(exchange: Exchange, skippedProperties: object, method: string, entry: object, symbol: string, now: number): void;
export default testTrade;
