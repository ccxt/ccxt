import { Exchange } from "../../../../ccxt";
declare function testBorrowRate(exchange: Exchange, skippedProperties: object, method: string, entry: object, requestedCode: string): void;
export default testBorrowRate;
