package io.github.ccxt.exchanges.pro;
import io.github.ccxt.base.Precise;
import io.github.ccxt.errors.*;
import io.github.ccxt.Helpers;
import io.github.ccxt.ws.*;
import io.github.ccxt.Client;

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

public class AftermathCore extends io.github.ccxt.exchanges.Aftermath
{
   public AftermathCore () {
       super();
   }

   public AftermathCore (Object options) {
       super(options);
   }

    public Object describe()
    {
        return this.deepExtend(super.describe(), new java.util.HashMap<String, Object>() {{
            put( "has", new java.util.HashMap<String, Object>() {{
                put( "ws", true );
                put( "watchBalance", false );
                put( "watchMyTrades", false );
                put( "watchOHLCV", false );
                put( "watchOrderBook", true );
                put( "watchOrders", false );
                put( "watchTicker", false );
                put( "watchTickers", false );
                put( "watchBidsAsks", false );
                put( "watchTrades", true );
                put( "watchTradesForSymbols", false );
                put( "watchPositions", true );
            }} );
            put( "urls", new java.util.HashMap<String, Object>() {{
                put( "api", new java.util.HashMap<String, Object>() {{
                    put( "ws", new java.util.HashMap<String, Object>() {{
                        put( "swap", "wss://aftermath.finance/iperps-api/ccxt/stream" );
                    }} );
                }} );
                put( "test", new java.util.HashMap<String, Object>() {{
                    put( "ws", new java.util.HashMap<String, Object>() {{
                        put( "swap", "wss://testnet.aftermath.finance/iperps-api/ccxt/stream" );
                    }} );
                }} );
            }} );
            put( "options", new java.util.HashMap<String, Object>() {{
                put( "tradesLimit", 1000 );
                put( "ordersLimit", 1000 );
                put( "watchPositions", new java.util.HashMap<String, Object>() {{
                    put( "fetchPositionsSnapshot", true );
                    put( "awaitPositionsSnapshot", true );
                }} );
            }} );
            put( "streaming", new java.util.HashMap<String, Object>() {{
                put( "keepAlive", 59000 );
            }} );
            put( "exceptions", new java.util.HashMap<String, Object>() {{}} );
        }});
    }

    public java.util.concurrent.CompletableFuture<Object> watchPublic(Object suffix, Object messageHash, Object message)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Object url = Helpers.add(Helpers.add(Helpers.GetValue(Helpers.GetValue(Helpers.GetValue(this.urls, "api"), "ws"), "swap"), "/"), suffix);
            return (this.watch(url, messageHash, this.json(message), messageHash, message)).join();
        });

    }

    public java.util.concurrent.CompletableFuture<Object> watchPublicMultiple(Object suffix, Object messageHashes, Object message)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Object url = Helpers.add(Helpers.add(Helpers.GetValue(Helpers.GetValue(Helpers.GetValue(this.urls, "api"), "ws"), "swap"), "/"), suffix);
            return (this.watchMultiple(url, messageHashes, this.json(message), messageHashes, message)).join();
        });

    }

    /**
     * @method
     * @name aftermath#watchTrades
     * @description watches information on multiple trades made in a market
     * @see https://testnet.aftermath.finance/docs/#/CCXT/service%3A%3Ahandlers%3A%3Accxt%3A%3Astream%3A%3Atrades
     * @param {string} symbol unified market symbol of the market trades were made in
     * @param {int} [since] the earliest time in ms to fetch trades for
     * @param {int} [limit] the maximum number of trade structures to retrieve
     * @param {object} [params] extra parameters specific to the exchange API endpoint
     * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=trade-structure}
     */
    public java.util.concurrent.CompletableFuture<Object> watchTrades(Object symbol2, Object... optionalArgs)
    {
        final Object symbol3 = symbol2;
        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {
            Object symbol = symbol3;
            Object since = Helpers.getArg(optionalArgs, 0, null);
            Object limit = Helpers.getArg(optionalArgs, 1, null);
            Object parameters = Helpers.getArg(optionalArgs, 2, new java.util.HashMap<String, Object>() {{}});
            (this.loadMarkets()).join();
            Object market = this.market(symbol);
            symbol = Helpers.GetValue(market, "symbol");
            Object topic = Helpers.add(Helpers.GetValue(market, "id"), "@trade");
            Object request = new java.util.HashMap<String, Object>() {{
                put( "chId", Helpers.GetValue(market, "id") );
            }};
            Object message = this.extend(request, parameters);
            Object trades = (this.watchPublic("trades", topic, message)).join();
            if (Helpers.isTrue(this.newUpdates))
            {
                limit = Helpers.callDynamically(trades, "getLimit", new Object[]{Helpers.GetValue(market, "symbol"), limit});
            }
            return this.filterBySymbolSinceLimit(trades, symbol, since, limit, true);
        });

    }

    public void handleTrade(Client client, Object message)
    {
        //
        // {
        //     "amount": 0.1,
        //     "cost": 0.1,
        //     "datetime": "string",
        //     "fee": null,
        //     "id": "string",
        //     "order": "string",
        //     "price": 0.1,
        //     "side": null,
        //     "symbol": "string",
        //     "takerOrMaker": null,
        //     "timestamp": null,
        //     "type": "string"
        // }
        //
        Object trade = this.parseTrade(message);
        Object symbol = Helpers.GetValue(trade, "symbol");
        Object market = this.market(symbol);
        if (!Helpers.isTrue((Helpers.inOp(this.trades, symbol))))
        {
            Object limit = this.safeInteger(this.options, "tradesLimit", 1000);
            var stored = new ArrayCache(((Number)limit).intValue());
            Helpers.addElementToObject(this.trades, symbol, stored);
        }
        Object messageHash = Helpers.add(Helpers.GetValue(market, "id"), "@trade");
        Object trades = Helpers.GetValue(this.trades, symbol);
        Helpers.callDynamically(trades, "append", new Object[]{trade});
        Helpers.addElementToObject(this.trades, symbol, trades);
        client.resolve(trades, messageHash);
    }

    /**
     * @method
     * @name aftermath#watchOrderBook
     * @see https://testnet.aftermath.finance/docs/#/CCXT/service%3A%3Ahandlers%3A%3Accxt%3A%3Astream%3A%3Aorderbook
     * @description watches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
     * @param {string} symbol unified symbol of the market to fetch the order book for
     * @param {int} [limit] the maximum amount of order book entries to return.
     * @param {object} [params] extra parameters specific to the exchange API endpoint
     * @returns {object} A dictionary of [order book structures]{@link https://docs.ccxt.com/?id=order-book-structure}
     */
    public java.util.concurrent.CompletableFuture<Object> watchOrderBook(Object symbol2, Object... optionalArgs)
    {
        final Object symbol3 = symbol2;
        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {
            Object symbol = symbol3;
            Object limit = Helpers.getArg(optionalArgs, 0, null);
            Object parameters = Helpers.getArg(optionalArgs, 1, new java.util.HashMap<String, Object>() {{}});
            (this.loadMarkets()).join();
            Object market = this.market(symbol);
            symbol = Helpers.GetValue(market, "symbol");
            Object topic = Helpers.add(Helpers.GetValue(market, "id"), "@orderbook");
            Object request = new java.util.HashMap<String, Object>() {{
                put( "chId", Helpers.GetValue(market, "id") );
            }};
            Object message = this.extend(request, parameters);
            Object orderbook = (this.watchPublic("orderbook", topic, message)).join();
            return Helpers.callDynamically(orderbook, "limit", new Object[]{});
        });

    }

    public void handleOrderBook(Client client, Object message)
    {
        //
        // {
        //     "asks": [
        //       []
        //     ],
        //     "bids": [
        //       []
        //     ],
        //     "datetime": "string",
        //     "nonce": 9007199254740991,
        //     "symbol": "string",
        //     "timestamp": 9007199254740991
        // }
        //
        Object symbol = this.safeString(message, "symbol");
        Object market = this.market(symbol);
        Object topic = Helpers.add(Helpers.GetValue(market, "id"), "@orderbook");
        if (!Helpers.isTrue((Helpers.inOp(this.orderbooks, symbol))))
        {
            Object defaultLimit = this.safeInteger(this.options, "watchOrderBookLimit", 1000);
            Object subscription = Helpers.GetValue(client.subscriptions, topic);
            Object limit = this.safeInteger(subscription, "limit", defaultLimit);
            Helpers.addElementToObject(this.orderbooks, symbol, this.orderBook(new java.util.HashMap<String, Object>() {{}}, limit));
            Helpers.addElementToObject(subscription, "limit", limit);
            this.spawn(() -> { try { this.fetchOrderBookSnapshot(client, message, subscription); } catch(Exception _e) { throw new RuntimeException(_e); } });
        } else
        {
            Object orderbook = Helpers.GetValue(this.orderbooks, symbol);
            Object prevNonce = this.safeInteger(orderbook, "nonce");
            Object nonce = this.safeInteger(message, "nonce");
            if (Helpers.isTrue(Helpers.isEqual(nonce, (Helpers.add(prevNonce, 1)))))
            {
                this.handleOrderBookMessage(client, message, orderbook);
                client.resolve(orderbook, topic);
            }
        }
    }

    public java.util.concurrent.CompletableFuture<Object> fetchOrderBookSnapshot(Client client, Object message, Object subscription)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Object symbol = this.safeString(message, "symbol");
            Object market = this.market(symbol);
            Object messageHash = Helpers.add(Helpers.GetValue(market, "id"), "@orderbook");
            try
            {
                Object defaultLimit = this.safeInteger(this.options, "watchOrderBookLimit", 1000);
                Object limit = this.safeInteger(subscription, "limit", defaultLimit);
                Object parameters = this.safeDict(subscription, "params");
                Object snapshot = (this.fetchRestOrderBookSafe(symbol, limit, parameters)).join();
                if (Helpers.isTrue(Helpers.isEqual(this.safeValue(this.orderbooks, symbol), null)))
                {
                    // if the orderbook is dropped before the snapshot is received
                    return null;
                }
                Object orderbook = Helpers.GetValue(this.orderbooks, symbol);
                Helpers.callDynamically(orderbook, "reset", new Object[]{snapshot});
                Helpers.addElementToObject(this.orderbooks, symbol, orderbook);
                client.resolve(orderbook, messageHash);
            } catch(Exception e)
            {
                ((java.util.Map<String,Object>)client.subscriptions).remove((String)messageHash);
                client.reject(e, messageHash);
            }
            return null;
        });

    }

    public Object handleOrderBookMessage(Client client, Object message, Object orderbook)
    {
        this.handleDeltas(Helpers.GetValue(orderbook, "asks"), this.safeValue(message, "asks", new java.util.ArrayList<Object>(java.util.Arrays.asList())));
        this.handleDeltas(Helpers.GetValue(orderbook, "bids"), this.safeValue(message, "bids", new java.util.ArrayList<Object>(java.util.Arrays.asList())));
        Object timestamp = this.safeInteger(message, "timestamp");
        Object nonce = this.safeInteger(message, "nonce");
        Helpers.addElementToObject(orderbook, "timestamp", timestamp);
        Helpers.addElementToObject(orderbook, "datetime", this.iso8601(timestamp));
        Helpers.addElementToObject(orderbook, "nonce", nonce);
        return orderbook;
    }

    public void handleDelta(Object bookside, Object delta)
    {
        Object price = this.safeFloat2(delta, "price", 0);
        Object amount = this.safeFloat2(delta, "quantity", 1);
        Helpers.callDynamically(bookside, "store", new Object[]{price, amount});
    }

    public void handleDeltas(Object bookside, Object deltas)
    {
        for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(deltas)); i++)
        {
            this.handleDelta(bookside, Helpers.GetValue(deltas, i));
        }
    }

    /**
     * @method
     * @name aftermath#watchPositions
     * @see https://testnet.aftermath.finance/docs/#/CCXT/service%3A%3Ahandlers%3A%3Accxt%3A%3Astream%3A%3Apositions
     * @description watch all open positions
     * @param {string[]|undefined} symbols list of unified market symbols
     * @param {int} [since] the earliest time in ms to fetch positions for
     * @param {int} [limit] the maximum number of position structures to retrieve
     * @param {object} params extra parameters specific to the exchange API endpoint
     * @param {int} [params.accountNumber] account number to query orders for, required
     * @returns {object[]} a list of [position structure]{@link https://docs.ccxt.com/en/latest/manual.html#position-structure}
     */
    public java.util.concurrent.CompletableFuture<Object> watchPositions(Object... optionalArgs)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Object symbols = Helpers.getArg(optionalArgs, 0, null);
            Object since = Helpers.getArg(optionalArgs, 1, null);
            Object limit = Helpers.getArg(optionalArgs, 2, null);
            Object parameters = Helpers.getArg(optionalArgs, 3, new java.util.HashMap<String, Object>() {{}});
            (this.loadMarkets()).join();
            Object messageHashes = new java.util.ArrayList<Object>(java.util.Arrays.asList());
            symbols = this.marketSymbols(symbols);
            if (!Helpers.isTrue(this.isEmpty(symbols)))
            {
                for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(symbols)); i++)
                {
                    Object symbol = Helpers.GetValue(symbols, i);
                    ((java.util.List<Object>)messageHashes).add(Helpers.add("positions::", symbol));
                }
            } else
            {
                ((java.util.List<Object>)messageHashes).add("positions");
            }
            Object accountNumber = this.safeNumber(parameters, "accountNumber");
            Object request = new java.util.HashMap<String, Object>() {{
                put( "accountNumber", accountNumber );
            }};
            Object message = this.extend(request, parameters);
            Object suffix = "positions";
            Object url = Helpers.add(Helpers.add(Helpers.GetValue(Helpers.GetValue(Helpers.GetValue(this.urls, "api"), "ws"), "swap"), "/"), suffix);
            Client client = this.client(url);
            this.setPositionsCache(client, symbols, parameters);
            Object fetchPositionsSnapshot = this.handleOption("watchPositions", "fetchPositionsSnapshot", true);
            Object awaitPositionsSnapshot = this.handleOption("watchPositions", "awaitPositionsSnapshot", true);
            if (Helpers.isTrue(Helpers.isTrue(Helpers.isTrue(fetchPositionsSnapshot) && Helpers.isTrue(awaitPositionsSnapshot)) && Helpers.isTrue(Helpers.isEqual(this.positions, null))))
            {
                Object snapshot = client.future("fetchPositionsSnapshot").getFuture().join();
                return this.filterBySymbolsSinceLimit(snapshot, symbols, since, limit, true);
            }
            Object newPositions = (this.watchPublicMultiple(suffix, messageHashes, message)).join();
            if (Helpers.isTrue(this.newUpdates))
            {
                return newPositions;
            }
            return this.filterBySymbolsSinceLimit(this.positions, symbols, since, limit, true);
        });

    }

    public void setPositionsCache(Client client, Object... optionalArgs)
    {
        Object symbols = Helpers.getArg(optionalArgs, 0, null);
        Object parameters = Helpers.getArg(optionalArgs, 1, new java.util.HashMap<String, Object>() {{}});
        Object fetchPositionsSnapshot = this.handleOption("watchPositions", "fetchPositionsSnapshot", false);
        if (Helpers.isTrue(fetchPositionsSnapshot))
        {
            Object messageHash = "fetchPositionsSnapshot";
            if (!Helpers.isTrue((Helpers.inOp(client.futures, messageHash))))
            {
                client.future((String)messageHash);
                this.spawn(() -> { try { this.loadPositionsSnapshot(client, messageHash, symbols, parameters); } catch(Exception _e) { throw new RuntimeException(_e); } });
            }
        } else
        {
            this.positions = new ArrayCache.ArrayCacheBySymbolBySide();
        }
    }

    public java.util.concurrent.CompletableFuture<Object> loadPositionsSnapshot(Client client, Object messageHash, Object symbols, Object parameters)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Object positions = (this.fetchPositions((Object)(symbols), (Object)(parameters))).join();
            this.positions = new ArrayCache.ArrayCacheBySymbolBySide();
            Object cache = this.positions;
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(positions)); i++)
            {
                Object position = Helpers.GetValue(positions, i);
                Helpers.callDynamically(cache, "append", new Object[]{position});
            }
            // don't remove the future from the .futures cache
            io.github.ccxt.ws.Future future = (io.github.ccxt.ws.Future)Helpers.GetValue(client.futures, messageHash);
            ((io.github.ccxt.ws.Future)future).resolve(cache);
            client.resolve(cache, "positions");
            return null;
        });

    }

    public void handlePositions(Client client, Object message)
    {
        //
        // {
        //     "collateral": 0.1,
        //     "contractSize": 0.1,
        //     "contracts": 0.1,
        //     "datetime": "string",
        //     "entryPrice": 0.1,
        //     "id": "0x895037c09dd1025a136b5a5789c4ea2481176adf4c3b0c3521d5d2039bdfc3ba:123",
        //     "initialMargin": 0.1,
        //     "initialMarginPercentage": 0.1,
        //     "leverage": 0.1,
        //     "liquidationPrice": 0.1,
        //     "maintenanceMargin": 0.1,
        //     "maintenanceMarginPercentage": 0.1,
        //     "marginMode": "cross",
        //     "marginRatio": 0.1,
        //     "notional": 0.1,
        //     "side": "long",
        //     "symbol": "BTC/USD:USDC",
        //     "timestamp": 9007199254740991,
        //     "unrealizedPnl": 0.1
        // }
        //
        if (Helpers.isTrue(Helpers.isEqual(this.positions, null)))
        {
            this.positions = new ArrayCache.ArrayCacheBySymbolBySide();
        }
        Object cache = this.positions;
        Object symbol = this.safeString(message, "symbol");
        Object market = this.safeMarket(symbol);
        Object position = this.parsePosition(message, market);
        Helpers.callDynamically(cache, "append", new Object[]{position});
        Object messageHash = Helpers.add("positions::", Helpers.GetValue(market, "symbol"));
        client.resolve(position, messageHash);
        client.resolve(new java.util.ArrayList<Object>(java.util.Arrays.asList(position)), "positions");
    }

    public Object handleErrorMessage(Client client, Object message)
    {
        //
        // User error: Expected Message::Text from client, got Ping(b\"\")
        //
        if (Helpers.isTrue((message instanceof String)))
        {
            if (Helpers.isTrue(Helpers.isGreaterThanOrEqual(Helpers.getIndexOf(message, "error"), 0)))
            {
                try
                {
                    Object feedback = Helpers.add(Helpers.add(this.id, " "), message);
                    this.throwExactlyMatchedException(Helpers.GetValue(this.exceptions, "exact"), message, feedback);
                    throw new ExchangeError((String)message) ;
                } catch(Exception error)
                {
                    if (Helpers.isTrue(Helpers.isInstance(error, AuthenticationError.class)))
                    {
                        Object messageHash = "authenticated";
                        client.reject(error, messageHash);
                        if (Helpers.isTrue(Helpers.inOp(client.subscriptions, messageHash)))
                        {
                            ((java.util.Map<String,Object>)client.subscriptions).remove((String)messageHash);
                        }
                    } else
                    {
                        client.reject(error);
                    }
                    return true;
                }
            }
        }
        return false;
    }

    public void handleMessage(Client client, Object message)
    {
        if (Helpers.isTrue(this.handleErrorMessage(client, message)))
        {
            return;
        }
        // const methods: Dict = {
        //     'trade': "handleTrade",
        // };
        if (Helpers.isTrue(Helpers.inOp(message, "asks")))
        {
            this.handleOrderBook(client, message);
        } else if (Helpers.isTrue(Helpers.inOp(message, "notional")))
        {
            this.handlePositions(client, message);
        } else
        {
            this.handleTrade(client, message);
        }
    }
}
