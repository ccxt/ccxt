// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

package io.github.ccxt.types;

import java.util.Map;

public final class Limits {
    public MinMax amount;
    public MinMax cost;
    public MinMax leverage;
    public MinMax price;
    public MinMax market;

    @SuppressWarnings("unchecked")
    public Limits(Object raw) {
        Map<String, Object> data = TypeHelper.toMap(raw);
        Object amountRaw = TypeHelper.safeValue(data, "amount");
        this.amount = amountRaw instanceof Map<?, ?> ? new MinMax(amountRaw) : null;
        Object costRaw = TypeHelper.safeValue(data, "cost");
        this.cost = costRaw instanceof Map<?, ?> ? new MinMax(costRaw) : null;
        Object leverageRaw = TypeHelper.safeValue(data, "leverage");
        this.leverage = leverageRaw instanceof Map<?, ?> ? new MinMax(leverageRaw) : null;
        Object priceRaw = TypeHelper.safeValue(data, "price");
        this.price = priceRaw instanceof Map<?, ?> ? new MinMax(priceRaw) : null;
        Object marketRaw = TypeHelper.safeValue(data, "market");
        this.market = marketRaw instanceof Map<?, ?> ? new MinMax(marketRaw) : null;
    }
}
