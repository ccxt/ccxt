package tests.exchange;
import tests.BaseTest;
import io.github.ccxt.Helpers;
import io.github.ccxt.Exchange;
import io.github.ccxt.errors.*;
import io.github.ccxt.base.Precise;


// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code


public class TestSharedMethods extends BaseTest {
    public static Object logTemplate(Exchange exchange, Object method, Object entry)
    {
        // there are cases when exchange is undefined (eg. base tests)
        Object id = ((Helpers.isTrue((!Helpers.isEqual(exchange, null))))) ? exchange.id : "undefined";
        Object methodString = ((Helpers.isTrue((!Helpers.isEqual(method, null))))) ? method : "undefined";
        Object entryString = ((Helpers.isTrue((Helpers.isTrue(!Helpers.isEqual(exchange, null)) && Helpers.isTrue(!Helpers.isEqual(entry, null)))))) ? exchange.json(entry) : "";
        return Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(" <<< ", id), " "), methodString), " ::: "), entryString), " >>> ");
    }
    public static Object isTemporaryFailure(Object e)
    {
        return Helpers.isTrue((Helpers.isInstance(e, OperationFailed.class))) && Helpers.isTrue((!Helpers.isTrue((Helpers.isInstance(e, OnMaintenance.class)))));
    }
    public static Object stringValue(Object value)
    {
        Object stringVal = null;
        if (Helpers.isTrue((value instanceof String)))
        {
            stringVal = value;
        } else if (Helpers.isTrue(Helpers.isEqual(value, null)))
        {
            stringVal = "undefined";
        } else
        {
            stringVal = String.valueOf(value);
        }
        return stringVal;
    }
    public static Object AssertType(Exchange exchange, Object skippedProperties, Object entry, Object key, Object format)
    {
        if (Helpers.isTrue(Helpers.inOp(skippedProperties, key)))
        {
            return null;
        }
        // because "typeof" string is not transpilable without === 'name', we list them manually at this moment
        Object entryKeyVal = exchange.safeValue(entry, key);
        Object formatKeyVal = exchange.safeValue(format, key);
        Object same_string = Helpers.isTrue(((entryKeyVal instanceof String))) && Helpers.isTrue(((formatKeyVal instanceof String)));
        Object same_numeric = Helpers.isTrue(((entryKeyVal instanceof Long || entryKeyVal instanceof Integer || entryKeyVal instanceof Float || entryKeyVal instanceof Double))) && Helpers.isTrue(((formatKeyVal instanceof Long || formatKeyVal instanceof Integer || formatKeyVal instanceof Float || formatKeyVal instanceof Double)));
        Object same_boolean = Helpers.isTrue((Helpers.isTrue((Helpers.isEqual(entryKeyVal, true))) || Helpers.isTrue((Helpers.isEqual(entryKeyVal, false))))) && Helpers.isTrue((Helpers.isTrue((Helpers.isEqual(formatKeyVal, true))) || Helpers.isTrue((Helpers.isEqual(formatKeyVal, false)))));
        Object same_array = Helpers.isTrue(Helpers.isArray(entryKeyVal)) && Helpers.isTrue(Helpers.isArray(formatKeyVal));
        Object same_object = Helpers.isTrue(exchange.isDictionary(entryKeyVal)) && Helpers.isTrue(exchange.isDictionary(formatKeyVal));
        Object result = Helpers.isTrue(Helpers.isTrue(Helpers.isTrue(Helpers.isTrue(Helpers.isTrue((Helpers.isEqual(entryKeyVal, null))) || Helpers.isTrue(same_string)) || Helpers.isTrue(same_numeric)) || Helpers.isTrue(same_boolean)) || Helpers.isTrue(same_array)) || Helpers.isTrue(same_object);
        return result;
    }
    public static void AssertStructure(Exchange exchange, Object skippedProperties, Object method, Object entry, Object format, Object... optionalArgs)
    {
        Object emptyAllowedFor = Helpers.getArg(optionalArgs, 0, null);
        Object deep = Helpers.getArg(optionalArgs, 1, false);
        Object logText = logTemplate(exchange, method, entry);
        Assert(!Helpers.isEqual(entry, null), Helpers.add("item is null/undefined", logText));
        // get all expected & predefined keys for this specific item and ensure thos ekeys exist in parsed structure
        Object allowEmptySkips = exchange.safeList(skippedProperties, "allowNull", new java.util.ArrayList<Object>(java.util.Arrays.asList()));
        if (Helpers.isTrue(!Helpers.isEqual(emptyAllowedFor, null)))
        {
            emptyAllowedFor = concat(emptyAllowedFor, allowEmptySkips);
        }
        if (Helpers.isTrue(Helpers.isArray(format)))
        {
            Assert(Helpers.isArray(entry), Helpers.add("entry is not an array", logText));
            Object realLength = Helpers.getArrayLength(entry);
            Object expectedLength = Helpers.getArrayLength(format);
            Assert(Helpers.isEqual(realLength, expectedLength), Helpers.add(Helpers.add("entry length is not equal to expected length of ", String.valueOf(expectedLength)), logText));
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(format)); i++)
            {
                Object emptyAllowedForThisKey = Helpers.isTrue((Helpers.isEqual(emptyAllowedFor, null))) || Helpers.isTrue(exchange.inArray(i, emptyAllowedFor));
                Object value = Helpers.GetValue(entry, i);
                if (Helpers.isTrue(Helpers.inOp(skippedProperties, i)))
                {
                    continue;
                }
                // check when:
                // - it's not inside "allowe empty values" list
                // - it's not undefined
                if (Helpers.isTrue(Helpers.isTrue(emptyAllowedForThisKey) && Helpers.isTrue((Helpers.isEqual(value, null)))))
                {
                    continue;
                }
                Assert(!Helpers.isEqual(value, null), Helpers.add(Helpers.add(String.valueOf(i), " index is expected to have a value"), logText));
                // because of other langs, this is needed for arrays
                Object typeAssertion = AssertType(exchange, skippedProperties, entry, i, format);
                Assert(typeAssertion, Helpers.add(Helpers.add(String.valueOf(i), " index does not have an expected type "), logText));
            }
        } else
        {
            Assert(exchange.isDictionary(entry), Helpers.add("entry is not a dict", logText));
            Object keys = Helpers.objectKeys(format);
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(keys)); i++)
            {
                Object key = Helpers.GetValue(keys, i);
                if (Helpers.isTrue(Helpers.inOp(skippedProperties, key)))
                {
                    continue;
                }
                Assert(Helpers.inOp(entry, key), Helpers.add(Helpers.add(Helpers.add("\"", stringValue(key)), "\" key is missing from structure"), logText));
                if (Helpers.isTrue(Helpers.inOp(skippedProperties, key)))
                {
                    continue;
                }
                Object emptyAllowedForThisKey = Helpers.isTrue((Helpers.isEqual(emptyAllowedFor, null))) || Helpers.isTrue(exchange.inArray(key, emptyAllowedFor));
                Object value = Helpers.GetValue(entry, key);
                // check when:
                // - it's not inside "allowe empty values" list
                // - it's not undefined
                if (Helpers.isTrue(Helpers.isTrue(emptyAllowedForThisKey) && Helpers.isTrue((Helpers.isEqual(value, null)))))
                {
                    continue;
                }
                // if it was in needed keys, then it should have value.
                Assert(!Helpers.isEqual(value, null), Helpers.add(Helpers.add(Helpers.add("\"", stringValue(key)), "\" key is expected to have a value"), logText));
                // add exclusion for info key, as it can be any type
                if (Helpers.isTrue(!Helpers.isEqual(key, "info")))
                {
                    Object typeAssertion = AssertType(exchange, skippedProperties, entry, key, format);
                    Assert(typeAssertion, Helpers.add(Helpers.add(Helpers.add("\"", stringValue(key)), "\" key is neither undefined, neither of expected type"), logText));
                    if (Helpers.isTrue(deep))
                    {
                        if (Helpers.isTrue(Helpers.isTrue(exchange.isDictionary(value)) || Helpers.isTrue(Helpers.isArray(value))))
                        {
                            AssertStructure(exchange, skippedProperties, method, value, Helpers.GetValue(format, key), emptyAllowedFor, deep);
                        }
                    }
                }
            }
        }
    }
    public static void AssertTimestamp(Exchange exchange, Object skippedProperties, Object method, Object entry, Object... optionalArgs)
    {
        Object nowToCheck = Helpers.getArg(optionalArgs, 0, null);
        Object keyNameOrIndex = Helpers.getArg(optionalArgs, 1, "timestamp");
        Object allowNull = Helpers.getArg(optionalArgs, 2, true);
        Object logText = logTemplate(exchange, method, entry);
        Object skipValue = exchange.safeValue(skippedProperties, keyNameOrIndex);
        if (Helpers.isTrue(!Helpers.isEqual(skipValue, null)))
        {
            return;  // skipped
        }
        Object isDateTimeObject = (keyNameOrIndex instanceof String);
        if (Helpers.isTrue(isDateTimeObject))
        {
            Assert((Helpers.inOp(entry, keyNameOrIndex)), Helpers.add(Helpers.add(Helpers.add("timestamp key \"", keyNameOrIndex), "\" is missing from structure"), logText));
        } else
        {
            // if index was provided (mostly from fetchOHLCV) then we check if it exists, as mandatory
            Assert(!Helpers.isTrue((Helpers.isEqual(Helpers.GetValue(entry, keyNameOrIndex), null))), Helpers.add(Helpers.add(Helpers.add("timestamp index ", stringValue(keyNameOrIndex)), " is undefined"), logText));
        }
        Object ts = Helpers.GetValue(entry, keyNameOrIndex);
        Assert(Helpers.isTrue(!Helpers.isEqual(ts, null)) || Helpers.isTrue(allowNull), Helpers.add("timestamp is null", logText));
        if (Helpers.isTrue(!Helpers.isEqual(ts, null)))
        {
            Assert((ts instanceof Long || ts instanceof Integer || ts instanceof Float || ts instanceof Double), Helpers.add("timestamp is not numeric", logText));
            Assert(((ts instanceof Integer) || (ts instanceof Long)), Helpers.add("timestamp should be an integer", logText));
            Object minTs = 1230940800000L; // 03 Jan 2009 - first block
            Object maxTs = 2147483648000L; // 19 Jan 2038 - max int
            Assert(Helpers.isGreaterThan(ts, minTs), Helpers.add(Helpers.add(Helpers.add("timestamp is impossible to be before ", String.valueOf(minTs)), " (03.01.2009)"), logText)); // 03 Jan 2009 - first block
            Assert(Helpers.isLessThan(ts, maxTs), Helpers.add(Helpers.add(Helpers.add("timestamp more than ", String.valueOf(maxTs)), " (19.01.2038)"), logText)); // 19 Jan 2038 - int32 overflows // 7258118400000  -> Jan 1 2200
            if (Helpers.isTrue(!Helpers.isEqual(nowToCheck, null)))
            {
                Object maxMsOffset = 60000; // 1 min
                Assert(Helpers.isLessThan(ts, Helpers.add(nowToCheck, maxMsOffset)), Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add("returned item timestamp (", exchange.iso8601(ts)), ") is ahead of the current time ("), exchange.iso8601(nowToCheck)), ")"), logText));
            }
        }
    }
    public static void AssertTimestampAndDatetime(Exchange exchange, Object skippedProperties, Object method, Object entry, Object... optionalArgs)
    {
        Object nowToCheck = Helpers.getArg(optionalArgs, 0, null);
        Object keyNameOrIndex = Helpers.getArg(optionalArgs, 1, "timestamp");
        Object allowNull = Helpers.getArg(optionalArgs, 2, true);
        Object logText = logTemplate(exchange, method, entry);
        Object skipValue = exchange.safeValue(skippedProperties, keyNameOrIndex);
        if (Helpers.isTrue(!Helpers.isEqual(skipValue, null)))
        {
            return;
        }
        AssertTimestamp(exchange, skippedProperties, method, entry, nowToCheck, keyNameOrIndex);
        Object isDateTimeObject = (keyNameOrIndex instanceof String);
        // only in case if the entry is a dictionary, thus it must have 'timestamp' & 'datetime' string keys
        if (Helpers.isTrue(isDateTimeObject))
        {
            // we also test 'datetime' here because it's certain sibling of 'timestamp'
            Assert((Helpers.inOp(entry, "datetime")), Helpers.add("\"datetime\" key is missing from structure", logText));
            Object dt = Helpers.GetValue(entry, "datetime");
            Assert(Helpers.isTrue(!Helpers.isEqual(dt, null)) || Helpers.isTrue(allowNull), Helpers.add("timestamp is null", logText));
            if (Helpers.isTrue(!Helpers.isEqual(dt, null)))
            {
                Assert((dt instanceof String), Helpers.add("\"datetime\" key does not have a string value", logText));
                // there are exceptional cases, like getting microsecond-targeted string '2022-08-08T22:03:19.014680Z', so parsed unified timestamp, which carries only 13 digits (millisecond precision) can not be stringified back to microsecond accuracy, causing the bellow Assertion to fail
                //    Assert (dt === exchange.iso8601 (entry['timestamp']))
                // so, we have to compare with millisecond accururacy
                Object dtParsed = exchange.parse8601(dt);
                Object tsMs = Helpers.GetValue(entry, "timestamp");
                Object diff = Helpers.mathAbs(Double.parseDouble(Helpers.toString(Helpers.subtract(dtParsed, tsMs))));
                if (Helpers.isTrue(Helpers.isGreaterThanOrEqual(diff, 500)))
                {
                    Object dtParsedString = exchange.iso8601(dtParsed);
                    Object dtEntryString = exchange.iso8601(tsMs);
                    Assert(false, Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add("datetime is not iso8601 of timestamp:", dtParsedString), "(string) != "), dtEntryString), "(from ts)"), logText));
                }
            }
        }
    }
    public static void AssertCurrencyCode(Exchange exchange, Object skippedProperties, Object method, Object entry, Object actualCode, Object... optionalArgs)
    {
        Object expectedCode = Helpers.getArg(optionalArgs, 0, null);
        Object allowNull = Helpers.getArg(optionalArgs, 1, true);
        if (Helpers.isTrue(Helpers.isTrue((Helpers.inOp(skippedProperties, "currency"))) || Helpers.isTrue((Helpers.inOp(skippedProperties, "currencyIdAndCode")))))
        {
            return;
        }
        Object logText = logTemplate(exchange, method, entry);
        Assert(Helpers.isTrue(!Helpers.isEqual(actualCode, null)) || Helpers.isTrue(allowNull), Helpers.add("currency code is null", logText));
        if (Helpers.isTrue(!Helpers.isEqual(actualCode, null)))
        {
            Assert((actualCode instanceof String), Helpers.add("currency code should be either undefined or a string", logText));
            Assert((Helpers.inOp(exchange.currencies, actualCode)), Helpers.add(Helpers.add(Helpers.add("currency code (\"", actualCode), "\") should be present in exchange.currencies"), logText));
            if (Helpers.isTrue(!Helpers.isEqual(expectedCode, null)))
            {
                Assert(Helpers.isEqual(actualCode, expectedCode), Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add("currency code in response (\"", stringValue(actualCode)), "\") should be equal to expected code (\""), stringValue(expectedCode)), "\")"), logText));
            }
        }
    }
    public static void AssertValidCurrencyIdAndCode(Exchange exchange, Object skippedProperties, Object method, Object entry, Object currencyId, Object currencyCode, Object... optionalArgs)
    {
        // this is exclusive exceptional key name to be used in `skip-tests.json`, to skip check for currency id and code
        Object allowNull = Helpers.getArg(optionalArgs, 0, true);
        if (Helpers.isTrue(Helpers.isTrue((Helpers.inOp(skippedProperties, "currency"))) || Helpers.isTrue((Helpers.inOp(skippedProperties, "currencyIdAndCode")))))
        {
            return;
        }
        Object logText = logTemplate(exchange, method, entry);
        Object undefinedValues = Helpers.isTrue(Helpers.isEqual(currencyId, null)) && Helpers.isTrue(Helpers.isEqual(currencyCode, null));
        Object definedValues = Helpers.isTrue(!Helpers.isEqual(currencyId, null)) && Helpers.isTrue(!Helpers.isEqual(currencyCode, null));
        Assert(Helpers.isTrue(undefinedValues) || Helpers.isTrue(definedValues), Helpers.add("currencyId and currencyCode should be either both defined or both undefined", logText));
        Assert(Helpers.isTrue(definedValues) || Helpers.isTrue(allowNull), Helpers.add("currency code and id is not defined", logText));
        if (Helpers.isTrue(definedValues))
        {
            // check by code
            Object currencyByCode = exchange.currency(currencyCode);
            Assert(Helpers.isEqual(Helpers.GetValue(currencyByCode, "id"), currencyId), Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add("currencyId \"", stringValue(currencyId)), "\" does not match currency id from instance: \""), stringValue(Helpers.GetValue(currencyByCode, "id"))), "\""), logText));
            // check by id
            Object currencyById = exchange.safeCurrency(currencyId);
            Assert(Helpers.isEqual(Helpers.GetValue(currencyById, "code"), currencyCode), Helpers.add(Helpers.add(Helpers.add(Helpers.add("currencyCode ", stringValue(currencyCode)), " does not match currency of id: "), stringValue(currencyId)), logText));
        }
    }
    public static void AssertSymbol(Exchange exchange, Object skippedProperties, Object method, Object entry, Object key, Object... optionalArgs)
    {
        Object expectedSymbol = Helpers.getArg(optionalArgs, 0, null);
        Object allowNull = Helpers.getArg(optionalArgs, 1, true);
        if (Helpers.isTrue(Helpers.inOp(skippedProperties, key)))
        {
            return;
        }
        Object logText = logTemplate(exchange, method, entry);
        Object actualSymbol = exchange.safeString(entry, key);
        if (Helpers.isTrue(!Helpers.isEqual(actualSymbol, null)))
        {
            Assert((actualSymbol instanceof String), Helpers.add("symbol should be either undefined or a string", logText));
        }
        if (Helpers.isTrue(!Helpers.isEqual(expectedSymbol, null)))
        {
            Assert(Helpers.isEqual(actualSymbol, expectedSymbol), Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add("symbol in response (\"", stringValue(actualSymbol)), "\") should be equal to expected symbol (\""), stringValue(expectedSymbol)), "\")"), logText));
        }
        Object definedValues = Helpers.isTrue(!Helpers.isEqual(actualSymbol, null)) && Helpers.isTrue(!Helpers.isEqual(expectedSymbol, null));
        Assert(Helpers.isTrue(definedValues) || Helpers.isTrue(allowNull), Helpers.add("symbols are not defined", logText));
    }
    public static void AssertSymbolInMarkets(Exchange exchange, Object skippedProperties, Object method, Object symbol)
    {
        Object logText = logTemplate(exchange, method, new java.util.HashMap<String, Object>() {{}});
        Assert((Helpers.inOp(exchange.markets, symbol)), Helpers.add("symbol should be present in exchange.symbols", logText));
    }
    public static void AssertGreater(Exchange exchange, Object skippedProperties, Object method, Object entry, Object key, Object compareTo, Object... optionalArgs)
    {
        Object allowNull = Helpers.getArg(optionalArgs, 0, true);
        if (Helpers.isTrue(Helpers.inOp(skippedProperties, key)))
        {
            return;
        }
        Object logText = logTemplate(exchange, method, entry);
        Object value = exchange.safeString(entry, key);
        Assert(Helpers.isTrue(!Helpers.isEqual(value, null)) || Helpers.isTrue(allowNull), Helpers.add("value is null", logText));
        if (Helpers.isTrue(!Helpers.isEqual(value, null)))
        {
            Assert(Precise.stringGt(value, compareTo), Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(stringValue(key), " key (with a value of "), stringValue(value)), ") was expected to be > "), stringValue(compareTo)), logText));
        }
    }
    public static void AssertGreaterOrEqual(Exchange exchange, Object skippedProperties, Object method, Object entry, Object key, Object compareTo, Object... optionalArgs)
    {
        Object allowNull = Helpers.getArg(optionalArgs, 0, true);
        if (Helpers.isTrue(Helpers.inOp(skippedProperties, key)))
        {
            return;
        }
        Object logText = logTemplate(exchange, method, entry);
        Object value = exchange.safeString(entry, key);
        Assert(Helpers.isTrue(!Helpers.isEqual(value, null)) || Helpers.isTrue(allowNull), Helpers.add("value is null", logText));
        if (Helpers.isTrue(Helpers.isTrue(!Helpers.isEqual(value, null)) && Helpers.isTrue(!Helpers.isEqual(compareTo, null))))
        {
            Assert(Precise.stringGe(value, compareTo), Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(stringValue(key), " key (with a value of "), stringValue(value)), ") was expected to be >= "), stringValue(compareTo)), logText));
        }
    }
    public static void AssertLess(Exchange exchange, Object skippedProperties, Object method, Object entry, Object key, Object compareTo, Object... optionalArgs)
    {
        Object allowNull = Helpers.getArg(optionalArgs, 0, true);
        if (Helpers.isTrue(Helpers.inOp(skippedProperties, key)))
        {
            return;
        }
        Object logText = logTemplate(exchange, method, entry);
        Object value = exchange.safeString(entry, key);
        Assert(Helpers.isTrue(!Helpers.isEqual(value, null)) || Helpers.isTrue(allowNull), Helpers.add("value is null", logText));
        if (Helpers.isTrue(Helpers.isTrue(!Helpers.isEqual(value, null)) && Helpers.isTrue(!Helpers.isEqual(compareTo, null))))
        {
            Assert(Precise.stringLt(value, compareTo), Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(stringValue(key), " key (with a value of "), stringValue(value)), ") was expected to be < "), stringValue(compareTo)), logText));
        }
    }
    public static void AssertLessOrEqual(Exchange exchange, Object skippedProperties, Object method, Object entry, Object key, Object compareTo, Object... optionalArgs)
    {
        Object allowNull = Helpers.getArg(optionalArgs, 0, true);
        if (Helpers.isTrue(Helpers.inOp(skippedProperties, key)))
        {
            return;
        }
        Object logText = logTemplate(exchange, method, entry);
        Object value = exchange.safeString(entry, key);
        Assert(Helpers.isTrue(!Helpers.isEqual(value, null)) || Helpers.isTrue(allowNull), Helpers.add("value is null", logText));
        if (Helpers.isTrue(Helpers.isTrue(!Helpers.isEqual(value, null)) && Helpers.isTrue(!Helpers.isEqual(compareTo, null))))
        {
            Assert(Precise.stringLe(value, compareTo), Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(stringValue(key), " key (with a value of "), stringValue(value)), ") was expected to be <= "), stringValue(compareTo)), logText));
        }
    }
    public static void AssertEqual(Exchange exchange, Object skippedProperties, Object method, Object entry, Object key, Object compareTo, Object... optionalArgs)
    {
        Object allowNull = Helpers.getArg(optionalArgs, 0, true);
        if (Helpers.isTrue(Helpers.inOp(skippedProperties, key)))
        {
            return;
        }
        Object logText = logTemplate(exchange, method, entry);
        Object value = exchange.safeString(entry, key);
        Assert(Helpers.isTrue(!Helpers.isEqual(value, null)) || Helpers.isTrue(allowNull), Helpers.add("value is null", logText));
        if (Helpers.isTrue(Helpers.isTrue(!Helpers.isEqual(value, null)) && Helpers.isTrue(!Helpers.isEqual(compareTo, null))))
        {
            Assert(Precise.stringEq(value, compareTo), Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(stringValue(key), " key (with a value of "), stringValue(value)), ") was expected to be equal to "), stringValue(compareTo)), logText));
        }
    }
    public static void AssertNonEqual(Exchange exchange, Object skippedProperties, Object method, Object entry, Object key, Object compareTo, Object... optionalArgs)
    {
        Object allowNull = Helpers.getArg(optionalArgs, 0, true);
        if (Helpers.isTrue(Helpers.inOp(skippedProperties, key)))
        {
            return;
        }
        Object logText = logTemplate(exchange, method, entry);
        Object value = exchange.safeString(entry, key);
        Assert(Helpers.isTrue(!Helpers.isEqual(value, null)) || Helpers.isTrue(allowNull), Helpers.add("value is null", logText));
        if (Helpers.isTrue(!Helpers.isEqual(value, null)))
        {
            Assert(!Helpers.isTrue(Precise.stringEq(value, compareTo)), Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(stringValue(key), " key (with a value of "), stringValue(value)), ") was expected not to be equal to "), stringValue(compareTo)), logText));
        }
    }
    public static void AssertInArray(Exchange exchange, Object skippedProperties, Object method, Object entry, Object key, Object expectedArray, Object... optionalArgs)
    {
        Object allowNull = Helpers.getArg(optionalArgs, 0, true);
        if (Helpers.isTrue(Helpers.inOp(skippedProperties, key)))
        {
            return;
        }
        Object logText = logTemplate(exchange, method, entry);
        Object value = exchange.safeValue(entry, key);
        Assert(Helpers.isTrue(!Helpers.isEqual(value, null)) || Helpers.isTrue(allowNull), Helpers.add("value is null", logText));
        // todo: remove undefined check
        if (Helpers.isTrue(!Helpers.isEqual(value, null)))
        {
            Object stingifiedArrayValue = exchange.json(expectedArray); // don't use expectedArray.join (','), as it bugs in other languages, if values are bool, undefined or etc..
            Assert(exchange.inArray(value, expectedArray), Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add("\"", stringValue(key)), "\" key (value \""), stringValue(value)), "\") is not from the expected list : ["), stingifiedArrayValue), "]"), logText));
        }
    }
    public static void AssertFeeStructure(Exchange exchange, Object skippedProperties, Object method, Object entry, Object key, Object... optionalArgs)
    {
        Object allowNull = Helpers.getArg(optionalArgs, 0, true);
        Object logText = logTemplate(exchange, method, entry);
        Object keyString = stringValue(key);
        if (Helpers.isTrue(((key instanceof Integer) || (key instanceof Long))))
        {
            Assert(Helpers.isArray(entry), Helpers.add("fee container is expected to be an array", logText));
            Assert(Helpers.isLessThan(key, Helpers.getArrayLength(entry)), Helpers.add(Helpers.add(Helpers.add("fee key ", keyString), " was expected to be present in entry"), logText));
        } else
        {
            Assert(exchange.isDictionary(entry), Helpers.add("fee container is expected to be a dict", logText));
            Assert(Helpers.inOp(entry, key), Helpers.add(Helpers.add(Helpers.add("fee key \"", key), "\" was expected to be present in entry"), logText));
        }
        Object feeObject = exchange.safeValue(entry, key);
        Assert(Helpers.isTrue(!Helpers.isEqual(feeObject, null)) || Helpers.isTrue(allowNull), Helpers.add("fee object is null", logText));
        // todo: remove undefined check to make stricter
        if (Helpers.isTrue(!Helpers.isEqual(feeObject, null)))
        {
            Assert(Helpers.inOp(feeObject, "cost"), Helpers.add(Helpers.add(keyString, " fee object should contain \"cost\" key"), logText));
            if (Helpers.isTrue(Helpers.isEqual(Helpers.GetValue(feeObject, "cost"), null)))
            {
                return;  // todo: remove undefined check to make stricter
            }
            Assert((Helpers.GetValue(feeObject, "cost") instanceof Long || Helpers.GetValue(feeObject, "cost") instanceof Integer || Helpers.GetValue(feeObject, "cost") instanceof Float || Helpers.GetValue(feeObject, "cost") instanceof Double), Helpers.add(Helpers.add(keyString, " \"cost\" must be numeric type"), logText));
            // AssertGreaterOrEqual (exchange, skippedProperties, method, feeObject, 'cost', '0'); // fee might be negative in the case of a rebate or reward
            Assert(Helpers.inOp(feeObject, "currency"), Helpers.add(Helpers.add(Helpers.add("\"", keyString), "\" fee object should contain \"currency\" key"), logText));
            AssertCurrencyCode(exchange, skippedProperties, method, entry, Helpers.GetValue(feeObject, "currency"));
        }
    }
    public static void AssertTimestampOrder(Exchange exchange, Object method, Object codeOrSymbol, Object items, Object... optionalArgs)
    {
        Object ascending = Helpers.getArg(optionalArgs, 0, true);
        for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(items)); i++)
        {
            if (Helpers.isTrue(Helpers.isGreaterThan(i, 0)))
            {
                Object currentTs = Helpers.GetValue(Helpers.GetValue(items, Helpers.subtract(i, 1)), "timestamp");
                Object nextTs = Helpers.GetValue(Helpers.GetValue(items, i), "timestamp");
                if (Helpers.isTrue(Helpers.isTrue(!Helpers.isEqual(currentTs, null)) && Helpers.isTrue(!Helpers.isEqual(nextTs, null))))
                {
                    Object ascendingOrDescending = ((Helpers.isTrue(ascending))) ? "ascending" : "descending";
                    Object comparison = ((Helpers.isTrue(ascending))) ? (Helpers.isLessThanOrEqual(currentTs, nextTs)) : (Helpers.isGreaterThanOrEqual(currentTs, nextTs));
                    Assert(comparison, Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(exchange.id, " "), method), " "), stringValue(codeOrSymbol)), " must return a "), ascendingOrDescending), " sorted array of items by timestamp, but "), String.valueOf(currentTs)), " is opposite with its next "), String.valueOf(nextTs)), " "), exchange.json(items)));
                }
            }
        }
    }
    public static void AssertInteger(Exchange exchange, Object skippedProperties, Object method, Object entry, Object key, Object... optionalArgs)
    {
        Object allowNull = Helpers.getArg(optionalArgs, 0, true);
        if (Helpers.isTrue(Helpers.inOp(skippedProperties, key)))
        {
            return;
        }
        Object logText = logTemplate(exchange, method, entry);
        if (Helpers.isTrue(!Helpers.isEqual(entry, null)))
        {
            Object value = exchange.safeValue(entry, key);
            Assert(Helpers.isTrue(!Helpers.isEqual(value, null)) || Helpers.isTrue(allowNull), Helpers.add("value is null", logText));
            if (Helpers.isTrue(!Helpers.isEqual(value, null)))
            {
                Object isInteger = ((value instanceof Integer) || (value instanceof Long));
                Assert(isInteger, Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add("\"", stringValue(key)), "\" key (value \""), stringValue(value)), "\") is not an integer"), logText));
            }
        }
    }
    public static void checkPrecisionAccuracy(Exchange exchange, Object skippedProperties, Object method, Object entry, Object key)
    {
        if (Helpers.isTrue(Helpers.inOp(skippedProperties, key)))
        {
            return;
        }
        if (Helpers.isTrue(exchange.isTickPrecision()))
        {
            // TICK_SIZE should be above zero
            AssertGreater(exchange, skippedProperties, method, entry, key, "0");
            // the below array of integers are inexistent tick-sizes (theoretically technically possible, but not in real-world cases), so in our case, such values probably indicate an incorrectly implemented tick-sizes calculation, so we throw new RuntimeException(e)rror
            Object decimalNumbers = new java.util.ArrayList<Object>(java.util.Arrays.asList("2", "3", "4", "5", "6", "7", "8", "9", "11", "12", "13", "14", "15", "16"));
            if (Helpers.isTrue(Helpers.isTrue(Helpers.isEqual(key, "amount")) && Helpers.isTrue(Helpers.inOp(skippedProperties, "precisionAmountAbnormal"))))
            {
                return;
            }
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(decimalNumbers)); i++)
            {
                Object num = Helpers.GetValue(decimalNumbers, i);
                Object numStr = num;
                AssertNonEqual(exchange, skippedProperties, method, entry, key, numStr);
            }
        } else
        {
            // todo: significant-digits return doubles from `this.parseNumber`, so for now can't Assert against integer atm
            // AssertInteger (exchange, skippedProperties, method, entry, key); // should be integer
            AssertLessOrEqual(exchange, skippedProperties, method, entry, key, "18"); // should be under 18 decimals
            AssertGreaterOrEqual(exchange, skippedProperties, method, entry, key, "-8"); // in real-world cases, there would not be less than that
        }
    }
    public static java.util.concurrent.CompletableFuture<Object> fetchBestBidAsk(Exchange exchange, Object method, Object symbol)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

        Object logText = logTemplate(exchange, method, new java.util.HashMap<String, Object>() {{}});
        // find out best bid/ask price
        Object bestBid = null;
        Object bestAsk = null;
        Object usedMethod = null;
        if (Helpers.isTrue(Helpers.GetValue(exchange.has, "fetchOrderBook")))
        {
            usedMethod = "fetchOrderBook";
            Object orderbook = (exchange.fetchOrderBook(symbol)).join();
            Object bids = exchange.safeList(orderbook, "bids");
            Object asks = exchange.safeList(orderbook, "asks");
            Object bestBidArray = exchange.safeList(bids, 0);
            Object bestAskArray = exchange.safeList(asks, 0);
            bestBid = exchange.safeNumber(bestBidArray, 0);
            bestAsk = exchange.safeNumber(bestAskArray, 0);
        } else if (Helpers.isTrue(Helpers.GetValue(exchange.has, "fetchBidsAsks")))
        {
            usedMethod = "fetchBidsAsks";
            Object tickers = (exchange.fetchBidsAsks(new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)))).join();
            Object ticker = exchange.safeDict(tickers, symbol);
            bestBid = exchange.safeNumber(ticker, "bid");
            bestAsk = exchange.safeNumber(ticker, "ask");
        } else if (Helpers.isTrue(Helpers.GetValue(exchange.has, "fetchTicker")))
        {
            usedMethod = "fetchTicker";
            Object ticker = (exchange.fetchTicker(symbol)).join();
            bestBid = exchange.safeNumber(ticker, "bid");
            bestAsk = exchange.safeNumber(ticker, "ask");
        } else if (Helpers.isTrue(Helpers.GetValue(exchange.has, "fetchTickers")))
        {
            usedMethod = "fetchTickers";
            Object tickers = (exchange.fetchTickers(new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)))).join();
            Object ticker = exchange.safeDict(tickers, symbol);
            bestBid = exchange.safeNumber(ticker, "bid");
            bestAsk = exchange.safeNumber(ticker, "ask");
        }
        //
        Assert(Helpers.isTrue(!Helpers.isEqual(bestBid, null)) && Helpers.isTrue(!Helpers.isEqual(bestAsk, null)), Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(logText, " "), exchange.id), " could not get best bid/ask for "), symbol), " using "), usedMethod), " while testing "), method));
        return new java.util.ArrayList<Object>(java.util.Arrays.asList(bestBid, bestAsk));
        });

    }
    public static java.util.concurrent.CompletableFuture<Object> fetchOrder(Exchange exchange, Object symbol, Object orderId, Object skippedProperties)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

        Object fetchedOrder = null;
        Object originalId = orderId;
        // set 'since' to 5 minute ago for optimal results
        Object sinceTime = Helpers.subtract(exchange.milliseconds(), Helpers.multiply(Helpers.multiply(1000, 60), 5));
        // iterate
        Object methods_singular = new java.util.ArrayList<Object>(java.util.Arrays.asList("fetchOrder", "fetchOpenOrder", "fetchClosedOrder", "fetchCanceledOrder"));
        for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(methods_singular)); i++)
        {
            Object singularFetchName = Helpers.GetValue(methods_singular, i);
            if (Helpers.isTrue(Helpers.GetValue(exchange.has, singularFetchName)))
            {
                Object currentOrder = ((java.util.concurrent.CompletableFuture<Object>)Helpers.callDynamically(exchange, singularFetchName, new Object[] { originalId, symbol })).join();
                // if there is an id inside the order, it means the order was fetched successfully
                if (Helpers.isTrue(Helpers.isEqual(Helpers.GetValue(currentOrder, "id"), originalId)))
                {
                    fetchedOrder = currentOrder;
                    break;
                }
            }
        }
        //
        // search through plural methods
        if (Helpers.isTrue(Helpers.isEqual(fetchedOrder, null)))
        {
            Object methods_plural = new java.util.ArrayList<Object>(java.util.Arrays.asList("fetchOrders", "fetchOpenOrders", "fetchClosedOrders", "fetchCanceledOrders"));
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(methods_plural)); i++)
            {
                Object pluralFetchName = Helpers.GetValue(methods_plural, i);
                if (Helpers.isTrue(Helpers.GetValue(exchange.has, pluralFetchName)))
                {
                    Object orders = ((java.util.concurrent.CompletableFuture<Object>)Helpers.callDynamically(exchange, pluralFetchName, new Object[] { symbol, sinceTime })).join();
                    Object found = false;
                    for (var j = 0; Helpers.isLessThan(j, Helpers.getArrayLength(orders)); j++)
                    {
                        Object currentOrder = Helpers.GetValue(orders, j);
                        if (Helpers.isTrue(Helpers.isEqual(Helpers.GetValue(currentOrder, "id"), originalId)))
                        {
                            fetchedOrder = currentOrder;
                            found = true;
                            break;
                        }
                    }
                    if (Helpers.isTrue(found))
                    {
                        break;
                    }
                }
            }
        }
        return fetchedOrder;
        });

    }
    public static void AssertOrderState(Exchange exchange, Object skippedProperties, Object method, Object order, Object AssertedStatus, Object strictCheck)
    {
        // note, `strictCheck` is `true` only from "fetchOrder" cases
        Object logText = logTemplate(exchange, method, order);
        Object msg = Helpers.add(Helpers.add(Helpers.add("order should be ", AssertedStatus), ", but it was not Asserted"), logText);
        Object filled = exchange.safeString(order, "filled");
        Object amount = exchange.safeString(order, "amount");
        // shorthand variables
        Object statusUndefined = (Helpers.isEqual(Helpers.GetValue(order, "status"), null));
        Object statusOpen = (Helpers.isEqual(Helpers.GetValue(order, "status"), "open"));
        Object statusClosed = (Helpers.isEqual(Helpers.GetValue(order, "status"), "closed"));
        Object statusClanceled = (Helpers.isEqual(Helpers.GetValue(order, "status"), "canceled"));
        Object filledDefined = (!Helpers.isEqual(filled, null));
        Object amountDefined = (!Helpers.isEqual(amount, null));
        Object condition = null;
        //
        // ### OPEN STATUS
        //
        // if strict check, then 'status' must be 'open' and filled amount should be less then whole order amount
        Object strictOpen = Helpers.isTrue(statusOpen) && Helpers.isTrue((Helpers.isTrue(Helpers.isTrue(filledDefined) && Helpers.isTrue(amountDefined)) && Helpers.isTrue(Helpers.isLessThan(filled, amount))));
        // if non-strict check, then accept & ignore undefined values
        Object nonstrictOpen = Helpers.isTrue((Helpers.isTrue(statusOpen) || Helpers.isTrue(statusUndefined))) && Helpers.isTrue((Helpers.isTrue((!Helpers.isTrue(filledDefined) || !Helpers.isTrue(amountDefined))) || Helpers.isTrue(Precise.stringLt(filled, amount))));
        // check
        if (Helpers.isTrue(Helpers.isEqual(AssertedStatus, "open")))
        {
            condition = ((Helpers.isTrue(strictCheck))) ? strictOpen : nonstrictOpen;
            Assert(condition, msg);
            return;
        }
        //
        // ### CLOSED STATUS
        //
        // if strict check, then 'status' must be 'closed' and filled amount should be equal to the whole order amount
        Object closedStrict = Helpers.isTrue(statusClosed) && Helpers.isTrue((Helpers.isTrue(Helpers.isTrue(filledDefined) && Helpers.isTrue(amountDefined)) && Helpers.isTrue(Precise.stringEq(filled, amount))));
        // if non-strict check, then accept & ignore undefined values
        Object closedNonStrict = Helpers.isTrue((Helpers.isTrue(statusClosed) || Helpers.isTrue(statusUndefined))) && Helpers.isTrue((Helpers.isTrue((!Helpers.isTrue(filledDefined) || !Helpers.isTrue(amountDefined))) || Helpers.isTrue(Precise.stringEq(filled, amount))));
        // check
        if (Helpers.isTrue(Helpers.isEqual(AssertedStatus, "closed")))
        {
            condition = ((Helpers.isTrue(strictCheck))) ? closedStrict : closedNonStrict;
            Assert(condition, msg);
            return;
        }
        //
        // ### CANCELED STATUS
        //
        // if strict check, then 'status' must be 'canceled' and filled amount should be less then whole order amount
        Object canceledStrict = Helpers.isTrue(statusClanceled) && Helpers.isTrue((Helpers.isTrue(Helpers.isTrue(filledDefined) && Helpers.isTrue(amountDefined)) && Helpers.isTrue(Precise.stringLt(filled, amount))));
        // if non-strict check, then accept & ignore undefined values
        Object canceledNonStrict = Helpers.isTrue((Helpers.isTrue(statusClanceled) || Helpers.isTrue(statusUndefined))) && Helpers.isTrue((Helpers.isTrue((!Helpers.isTrue(filledDefined) || !Helpers.isTrue(amountDefined))) || Helpers.isTrue(Precise.stringLt(filled, amount))));
        // check
        if (Helpers.isTrue(Helpers.isEqual(AssertedStatus, "canceled")))
        {
            condition = ((Helpers.isTrue(strictCheck))) ? canceledStrict : canceledNonStrict;
            Assert(condition, msg);
            return;
        }
        //
        // ### CLOSED_or_CANCELED STATUS
        //
        if (Helpers.isTrue(Helpers.isEqual(AssertedStatus, "closed_or_canceled")))
        {
            condition = ((Helpers.isTrue(strictCheck))) ? (Helpers.isTrue(closedStrict) || Helpers.isTrue(canceledStrict)) : (Helpers.isTrue(closedNonStrict) || Helpers.isTrue(canceledNonStrict));
            Assert(condition, msg);
            return;
        }
    }
    public static Object getActiveMarkets(Exchange exchange, Object... optionalArgs)
    {
        Object includeUnknown = Helpers.getArg(optionalArgs, 0, true);
        Object filteredActive = exchange.filterBy(exchange.markets, "active", true);
        if (Helpers.isTrue(includeUnknown))
        {
            Object filteredUndefined = exchange.filterBy(exchange.markets, "active", null);
            return exchange.arrayConcat(filteredActive, filteredUndefined);
        }
        return filteredActive;
    }
    public static Object removeProxyOptions(Exchange exchange, Object skippedProperties)
    {
        Object proxyUrl = exchange.checkProxyUrlSettings();
        var httpProxyhttpsProxysocksProxyVariable = exchange.checkProxySettings();
        var httpProxy = ((java.util.List<Object>) httpProxyhttpsProxysocksProxyVariable).get(0);
        var httpsProxy = ((java.util.List<Object>) httpProxyhttpsProxysocksProxyVariable).get(1);
        var socksProxy = ((java.util.List<Object>) httpProxyhttpsProxysocksProxyVariable).get(2);
        // because of bug in transpiled, about `.proxyUrl` being transpiled into `.proxy_url`, we have to use this workaround
        exchange.setProperty(exchange, "proxyUrl", null);
        exchange.setProperty(exchange, "proxy_url", null);
        exchange.setProperty(exchange, "httpProxy", null);
        exchange.setProperty(exchange, "http_proxy", null);
        exchange.setProperty(exchange, "httpsProxy", null);
        exchange.setProperty(exchange, "https_proxy", null);
        exchange.setProperty(exchange, "socksProxy", null);
        exchange.setProperty(exchange, "socks_proxy", null);
        return new java.util.ArrayList<Object>(java.util.Arrays.asList(proxyUrl, httpProxy, httpsProxy, socksProxy));
    }
    public static void setProxyOptions(Exchange exchange, Object skippedProperties, Object proxyUrl, Object httpProxy, Object httpsProxy, Object socksProxy)
    {
        exchange.proxyUrl = proxyUrl;
        exchange.httpProxy = httpProxy;
        exchange.httpsProxy = httpsProxy;
        exchange.socksProxy = socksProxy;
    }
    public static Object concat(Object... optionalArgs)
    {
        // we use this method temporarily, because of ast-transpiler issue across langs
        Object a = Helpers.getArg(optionalArgs, 0, null);
        Object b = Helpers.getArg(optionalArgs, 1, null);
        if (Helpers.isTrue(Helpers.isEqual(a, null)))
        {
            return b;
        } else if (Helpers.isTrue(Helpers.isEqual(b, null)))
        {
            return a;
        } else
        {
            Object result = new java.util.ArrayList<Object>(java.util.Arrays.asList());
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(a)); i++)
            {
                ((java.util.List<Object>)result).add(Helpers.GetValue(a, i));
            }
            for (var j = 0; Helpers.isLessThan(j, Helpers.getArrayLength(b)); j++)
            {
                ((java.util.List<Object>)result).add(Helpers.GetValue(b, j));
            }
            return result;
        }
    }
    public static void AssertNonEmtpyArray(Exchange exchange, Object skippedProperties, Object method, Object entry, Object... optionalArgs)
    {
        Object hint = Helpers.getArg(optionalArgs, 0, null);
        Object logText = logTemplate(exchange, method, entry);
        if (Helpers.isTrue(!Helpers.isEqual(hint, null)))
        {
            logText = Helpers.add(Helpers.add(logText, " "), hint);
        }
        Assert(Helpers.isArray(entry), Helpers.add("response is expected to be an array", logText));
        if (!Helpers.isTrue((Helpers.inOp(skippedProperties, "emptyResponse"))))
        {
            return;
        }
        Assert(Helpers.isGreaterThan(Helpers.getArrayLength(entry), 0), Helpers.add(Helpers.add("response is expected to be a non-empty array", logText), " (add \"emptyResponse\" in skip-tests.json to skip this check)"));
    }
    public static void AssertRoundMinuteTimestamp(Exchange exchange, Object skippedProperties, Object method, Object entry, Object key)
    {
        if (Helpers.isTrue(Helpers.inOp(skippedProperties, key)))
        {
            return;
        }
        Object logText = logTemplate(exchange, method, entry);
        Object ts = exchange.safeString(entry, key);
        Assert(Helpers.isEqual(Precise.stringMod(ts, "60000"), "0"), Helpers.add("timestamp should be a multiple of 60 seconds (1 minute)", logText));
    }
    public static Object deepEqual(Exchange exchange, Object a, Object b)
    {
        return Helpers.isEqual(exchange.json(a), exchange.json(b));
    }
    public static void AssertDeepEqual(Exchange exchange, Object skippedProperties, Object method, Object a, Object b)
    {
        Object logText = logTemplate(exchange, method, new java.util.HashMap<String, Object>() {{}});
        Assert(deepEqual(exchange, a, b), Helpers.add(Helpers.add(Helpers.add(Helpers.add("two dicts do not match: ", exchange.json(a)), " != "), exchange.json(b)), logText));
    }
    public static Object exchangeProp(Exchange exchange, Object key, Object... optionalArgs)
    {
        Object defaultValue = Helpers.getArg(optionalArgs, 0, null);
        Object value = exchange.getProperty(exchange, String.valueOf(key));
        if (Helpers.isTrue(!Helpers.isEqual(value, null)))
        {
            return value;
        }
        // try UpperCase key also, for other langs
        Object keyUpper = exchange.capitalize(String.valueOf(key));
        return exchange.getProperty(exchange, keyUpper, defaultValue);
    }
    public static java.util.concurrent.CompletableFuture<Void> validateTickerExceptionForPercentage(Object ex, Exchange exchange, Object ticker)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

        // only skip cases of "too far price" when it's the first day of listing, otherwise rethrow abnormality
        Object eMessage = exchange.exceptionMessage(ex, false);
        if (Helpers.isTrue(Helpers.isTrue(Helpers.isGreaterThanOrEqual(Helpers.getIndexOf(eMessage, "percentage should be above"), 0)) || Helpers.isTrue(Helpers.isGreaterThanOrEqual(Helpers.getIndexOf(eMessage, "percentage should be below"), 0))))
        {
            Object symbol = Helpers.GetValue(ticker, "symbol");
            if (Helpers.isTrue(!Helpers.isEqual(symbol, null)))
            {
                // if it's not in markets, then maybe newly added symbol, so can can compromise there
                if (!Helpers.isTrue((Helpers.inOp(exchange.markets, symbol))))
                {
                    return null;
                }
                // if OHLCV supported
                if (Helpers.isTrue(!Helpers.isEqual(exchange.featureValue(symbol, "fetchOHLCV"), null)))
                {
                    Object ohlcv = (exchange.fetchOHLCV(symbol, "1d", null, 5)).join();
                    if (Helpers.isTrue(Helpers.isLessThanOrEqual(Helpers.getArrayLength(ohlcv), 1)))
                    {
                        // if only 1 day, then allow it
                        return null;
                    }
                }
            }
        }
        Assert(Helpers.isEqual(eMessage, ""), eMessage); // trigger error
            return null;
        });

    }

}