package tests.exchange;
import io.github.ccxt.Helpers;
import io.github.ccxt.Exchange;
import tests.BaseTest;
import io.github.ccxt.errors.*;

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

public class TestMain extends BaseTest
{
    public boolean idTests = false;
    public boolean requestTestsFailed = false;
    public boolean responseTestsFailed = false;
    public boolean requestTests = false;
    public boolean wsTests = false;
    public boolean responseTests = false;
    public boolean info = false;
    public boolean verbose = false;
    public boolean debug = false;
    public boolean privateTest = false;
    public boolean privateTestOnly = false;
    public boolean loadKeys = false;
    public boolean sandbox = false;
    public java.util.List<Object> onlySpecificTests = new java.util.ArrayList<Object>(java.util.Arrays.asList());
    public Object skippedSettingsForExchange = new java.util.HashMap<String, Object>() {{}};
    public Object skippedMethods = new java.util.HashMap<String, Object>() {{}};
    public Object checkedPublicTests = new java.util.HashMap<String, Object>() {{}};
    public java.util.Map<String, Object> testFiles = new java.util.HashMap<String, Object>() {{}};
    public Object publicTests = new java.util.HashMap<String, Object>() {{}};
    public String ext = "";
    public String lang = "";
    public Object proxyTestFileName = "proxies";

    public void parseCliArgsAndProps()
    {
        this.responseTests = Helpers.isTrue(getCliArgValue("--responseTests")) || Helpers.isTrue(getCliArgValue("--response"));
        this.idTests = getCliArgValue("--idTests");
        this.requestTests = Helpers.isTrue(getCliArgValue("--requestTests")) || Helpers.isTrue(getCliArgValue("--request"));
        this.info = getCliArgValue("--info");
        this.verbose = getCliArgValue("--verbose");
        this.debug = getCliArgValue("--debug");
        this.privateTest = getCliArgValue("--private");
        this.privateTestOnly = getCliArgValue("--privateOnly");
        this.sandbox = getCliArgValue("--sandbox");
        this.loadKeys = getCliArgValue("--loadKeys");
        this.wsTests = getCliArgValue("--ws");
        this.lang = getLang();
        this.ext = getExt();
    }

    public java.util.concurrent.CompletableFuture<Object> init(Object exchangeId, Object symbolArgv, Object methodArgv)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            try
            {
                (this.initInner(exchangeId, symbolArgv, methodArgv)).join();
            } catch(Exception e)
            {
                dump("[TEST_FAILURE]"); // tell run-tests.js this is failure
                throw new RuntimeException(e);
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> initInner(Object exchangeId, Object symbolArgv, Object methodArgv)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            this.parseCliArgsAndProps();
            if (Helpers.isTrue(Helpers.isTrue(this.requestTests) && Helpers.isTrue(this.responseTests)))
            {
                (this.runStaticRequestTests(exchangeId, symbolArgv)).join();
                (this.runStaticResponseTests(exchangeId, symbolArgv)).join();
                return true;
            }
            if (Helpers.isTrue(this.responseTests))
            {
                (this.runStaticResponseTests(exchangeId, symbolArgv)).join();
                return true;
            }
            if (Helpers.isTrue(this.requestTests))
            {
                (this.runStaticRequestTests(exchangeId, symbolArgv)).join(); // symbol here is the testname
                return true;
            }
            if (Helpers.isTrue(this.idTests))
            {
                (this.runBrokerIdTests()).join();
                return true;
            }
            Object newLine = "\n";
            dump(Helpers.add(Helpers.add(Helpers.add(Helpers.add(newLine, ""), newLine), ""), "[INFO] TESTING "), this.ext, new java.util.HashMap<String, Object>() {{
                put( "exchange", exchangeId );
                put( "symbol", symbolArgv );
                put( "method", methodArgv );
                put( "isWs", TestMain.this.wsTests );
                put( "useProxy", getCliArgValue("--useProxy") );
            }}, newLine);
            Object exchangeArgs = new java.util.HashMap<String, Object>() {{
                put( "verbose", TestMain.this.verbose );
                put( "debug", TestMain.this.debug );
                put( "enableRateLimit", true );
                put( "timeout", 30000 );
            }};
            Exchange exchange = initExchange(exchangeId, exchangeArgs, this.wsTests);
            if (Helpers.isTrue(exchange.alias))
            {
                dump(this.addPadding("[INFO] skipping alias", 25));
                exitScript(0);
            }
            (this.importFiles(exchange)).join();
            // ensure test files are found & filled
            Assert(Helpers.isGreaterThan(Helpers.getArrayLength(Helpers.objectKeys(this.testFiles)), 0), "Test files were not loaded");
            this.expandSettings(exchange);
            this.checkIfSpecificTestIsChosen(methodArgv);
            (this.startTest(exchange, symbolArgv)).join();
            exitScript(0); // needed to be explicitly finished for WS tests
            return true;  // required for c#
        });

    }

    public void checkIfSpecificTestIsChosen(Object methodArgv)
    {
        if (Helpers.isTrue(!Helpers.isEqual(methodArgv, null)))
        {
            Object testFileNames = Helpers.objectKeys(this.testFiles);
            Object possibleMethodNames = Helpers.split(methodArgv, ","); // i.e. `test.ts binance fetchBalance,fetchDeposits`
            if (Helpers.isTrue(Helpers.isGreaterThanOrEqual(Helpers.getArrayLength(possibleMethodNames), 1)))
            {
                for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(testFileNames)); i++)
                {
                    Object testFileName = Helpers.GetValue(testFileNames, i);
                    for (var j = 0; Helpers.isLessThan(j, Helpers.getArrayLength(possibleMethodNames)); j++)
                    {
                        Object methodName = Helpers.GetValue(possibleMethodNames, j);
                        methodName = Helpers.replace((String)methodName, (String)"()", (String)"");
                        if (Helpers.isTrue(Helpers.isEqual(testFileName, methodName)))
                        {
                            ((java.util.List<Object>)this.onlySpecificTests).add(testFileName);
                        }
                    }
                }
            }
        }
    }

    public java.util.concurrent.CompletableFuture<Object> importFiles(Exchange exchange)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Object properties = Helpers.objectKeys(exchange.has);
            ((java.util.List<Object>)properties).add("loadMarkets");
            ((java.util.List<Object>)properties).add("afterConstruct");
            if (Helpers.isTrue(isSync()))
            {
                this.testFiles = getTestFilesSync(properties, this.wsTests);
            } else
            {
                this.testFiles = (getTestFiles(properties, this.wsTests)).join();
            }
            return true;
        });

    }

    public void loadCredentialsFromEnv(Exchange exchange)
    {
        Object exchangeId = exchange.id;
        Object reqCreds = getExchangeProp(exchange, Helpers.add("re", "quiredCredentials")); // dont glue the r-e-q-u-i-r-e phrase, because leads to messed up transpilation
        Object objkeys = Helpers.objectKeys(reqCreds);
        for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(objkeys)); i++)
        {
            Object credential = Helpers.GetValue(objkeys, i);
            Object isRequired = Helpers.GetValue(reqCreds, credential);
            if (Helpers.isTrue(Helpers.isTrue(isRequired) && Helpers.isTrue(Helpers.isEqual(getExchangeProp(exchange, credential), null))))
            {
                Object fullKey = Helpers.add(Helpers.add(exchangeId, "_"), credential);
                Object credentialEnvName = ((String)fullKey).toUpperCase(); // example: KRAKEN_APIKEY
                Object envVars = getEnvVars();
                Object credentialValue = ((Helpers.isTrue((Helpers.inOp(envVars, credentialEnvName))))) ? Helpers.GetValue(envVars, credentialEnvName) : null;
                if (Helpers.isTrue(credentialValue))
                {
                    setExchangeProp(exchange, credential, credentialValue);
                }
            }
        }
    }

    public void expandSettings(Exchange exchange)
    {
        Object exchangeId = exchange.id;
        Object keysGlobal = Helpers.add(getRootDir(), "keys.json");
        Object keysLocal = Helpers.add(getRootDir(), "keys.local.json");
        Object keysGlobalExists = ioFileExists(keysGlobal);
        Object keysLocalExists = ioFileExists(keysLocal);
        Object globalSettings = new java.util.HashMap<String, Object>() {{}};
        if (Helpers.isTrue(keysGlobalExists))
        {
            globalSettings = ioFileRead(keysGlobal);
        }
        Object localSettings = new java.util.HashMap<String, Object>() {{}};
        if (Helpers.isTrue(keysLocalExists))
        {
            localSettings = ioFileRead(keysLocal);
        }
        Object allSettings = exchange.deepExtend(globalSettings, localSettings);
        Object exchangeSettings = exchange.safeValue(allSettings, exchangeId, new java.util.HashMap<String, Object>() {{}});
        if (Helpers.isTrue(exchangeSettings))
        {
            Object settingKeys = Helpers.objectKeys(exchangeSettings);
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(settingKeys)); i++)
            {
                Object key = Helpers.GetValue(settingKeys, i);
                if (Helpers.isTrue(Helpers.GetValue(exchangeSettings, key)))
                {
                    Object finalValue = null;
                    if (Helpers.isTrue(exchange.isDictionary(Helpers.GetValue(exchangeSettings, key))))
                    {
                        Object existing = getExchangeProp(exchange, key, new java.util.HashMap<String, Object>() {{}});
                        finalValue = exchange.deepExtend(existing, Helpers.GetValue(exchangeSettings, key));
                    } else
                    {
                        finalValue = Helpers.GetValue(exchangeSettings, key);
                    }
                    setExchangeProp(exchange, key, finalValue);
                }
            }
        }
        // credentials
        if (Helpers.isTrue(this.loadKeys))
        {
            this.loadCredentialsFromEnv(exchange);
        }
        // skipped tests
        Object skippedFile = Helpers.add(getRootDir(), "skip-tests.json");
        Object skippedSettings = ioFileRead(skippedFile);
        this.skippedSettingsForExchange = exchange.safeValue(skippedSettings, exchangeId, new java.util.HashMap<String, Object>() {{}});
        Object skippedSettingsForExchange = this.skippedSettingsForExchange;
        // others
        Object timeout = exchange.safeValue(skippedSettingsForExchange, "timeout");
        if (Helpers.isTrue(!Helpers.isEqual(timeout, null)))
        {
            exchange.timeout = exchange.parseToInt(timeout);
        }
        if (Helpers.isTrue(getCliArgValue("--useProxy")))
        {
            exchange.httpProxy = exchange.safeString(skippedSettingsForExchange, "httpProxy");
            exchange.httpsProxy = exchange.safeString(skippedSettingsForExchange, "httpsProxy");
            exchange.wsProxy = exchange.safeString(skippedSettingsForExchange, "wsProxy");
            exchange.wssProxy = exchange.safeString(skippedSettingsForExchange, "wssProxy");
        }
        this.skippedMethods = exchange.safeValue(skippedSettingsForExchange, "skipMethods", new java.util.HashMap<String, Object>() {{}});
        this.checkedPublicTests = new java.util.HashMap<String, Object>() {{}};
    }

    public Object addPadding(Object message, Object size)
    {
        // has to be transpilable
        Object res = "";
        Object messageLength = ((String)message).length(); // avoid php transpilation issue
        Object missingSpace = Helpers.subtract(Helpers.subtract(size, messageLength), 0); // - 0 is added just to trick transpile to treat the .length as a string for php
        if (Helpers.isTrue(Helpers.isGreaterThan(missingSpace, 0)))
        {
            for (var i = 0; Helpers.isLessThan(i, missingSpace); i++)
            {
                res = Helpers.add(res, " ");
            }
        }
        return Helpers.add(message, res);
    }

    public java.util.concurrent.CompletableFuture<Object> testMethod(Object methodName2, Exchange exchange, Object args, Object isPublic)
    {
        final Object methodName3 = methodName2;
        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {
            Object methodName = methodName3;
            // todo: temporary skip for c#
            if (Helpers.isTrue(Helpers.isTrue(Helpers.isGreaterThanOrEqual(Helpers.getIndexOf(methodName, "OrderBook"), 0)) && Helpers.isTrue(Helpers.isEqual(this.ext, "cs"))))
            {
                Helpers.addElementToObject(exchange.options, "checksum", false);
            }
            // todo: temporary skip for php
            if (Helpers.isTrue(Helpers.isTrue(Helpers.isGreaterThanOrEqual(Helpers.getIndexOf(methodName, "OrderBook"), 0)) && Helpers.isTrue(Helpers.isEqual(this.ext, "php"))))
            {
                return true;
            }
            Object skippedPropertiesForMethod = this.getSkips(exchange, methodName);
            Object isLoadMarkets = (Helpers.isEqual(methodName, "loadMarkets"));
            Object isFetchCurrencies = (Helpers.isEqual(methodName, "fetchCurrencies"));
            Object isProxyTest = (Helpers.isEqual(methodName, this.proxyTestFileName));
            Object isConstructorTest = (Helpers.isEqual(methodName, "afterConstruct"));
            Object isFeatureTest = (Helpers.isEqual(methodName, "features"));
            // if this is a private test, and the implementation was already tested in public, then no need to re-test it in private test (exception is fetchCurrencies, because our approach in base exchange)
            if (Helpers.isTrue(Helpers.isTrue(!Helpers.isTrue(isPublic) && Helpers.isTrue((Helpers.inOp(this.checkedPublicTests, methodName)))) && !Helpers.isTrue(isFetchCurrencies)))
            {
                return true;
            }
            Object skipMessage = null;
            Object supportedByExchange = Helpers.isTrue((Helpers.inOp(exchange.has, methodName))) && Helpers.isTrue(Helpers.GetValue(exchange.has, methodName));
            if (Helpers.isTrue(!Helpers.isTrue(isLoadMarkets) && Helpers.isTrue((Helpers.isTrue(Helpers.isGreaterThan(Helpers.getArrayLength(this.onlySpecificTests), 0)) && !Helpers.isTrue(exchange.inArray(methodName, this.onlySpecificTests))))))
            {
                skipMessage = "[INFO] IGNORED_TEST";
            } else if (Helpers.isTrue(Helpers.isTrue(Helpers.isTrue(Helpers.isTrue(!Helpers.isTrue(isLoadMarkets) && !Helpers.isTrue(supportedByExchange)) && !Helpers.isTrue(isProxyTest)) && !Helpers.isTrue(isFeatureTest)) && !Helpers.isTrue(isConstructorTest)))
            {
                skipMessage = "[INFO] UNSUPPORTED_TEST"; // keep it aligned with the longest message
            } else if (Helpers.isTrue((skippedPropertiesForMethod instanceof String)))
            {
                skipMessage = "[INFO] SKIPPED_TEST";
            } else if (!Helpers.isTrue((Helpers.inOp(this.testFiles, methodName))))
            {
                skipMessage = "[INFO] UNIMPLEMENTED_TEST";
            }
            // exceptionally for `loadMarkets` call, we call it before it's even checked for "skip" as we need it to be called anyway (but can skip "test.loadMarket" for it)
            if (Helpers.isTrue(isLoadMarkets))
            {
                (exchange.loadMarkets(true)).join();
            }
            Object name = exchange.id;
            if (Helpers.isTrue(skipMessage))
            {
                if (Helpers.isTrue(this.info))
                {
                    dump(this.addPadding(skipMessage, 25), name, methodName);
                }
                return true;
            }
            if (Helpers.isTrue(this.info))
            {
                Object argsStringified = Helpers.add(Helpers.add("(", exchange.json(args)), ")"); // args.join() breaks when we provide a list of symbols or multidimensional array; "args.toString()" breaks bcz of "array to string conversion"
                dump(this.addPadding("[INFO] TESTING", 25), name, methodName, argsStringified);
            }
            if (Helpers.isTrue(isSync()))
            {
                callMethodSync(this.testFiles, methodName, exchange, skippedPropertiesForMethod, args);
            } else
            {
                (callMethod(this.testFiles, methodName, exchange, skippedPropertiesForMethod, args)).join();
            }
            if (Helpers.isTrue(this.info))
            {
                dump(this.addPadding("[INFO] TESTING DONE", 25), name, methodName);
            }
            // add to the list of successed tests
            if (Helpers.isTrue(isPublic))
            {
                Helpers.addElementToObject(this.checkedPublicTests, methodName, true);
            }
            return true;
        });

    }

    public Object getSkips(Exchange exchange, Object methodName)
    {
        Object finalSkips = new java.util.HashMap<String, Object>() {{}};
        // check the exact method (i.e. `fetchTrades`) and language-specific (i.e. `fetchTrades.php`)
        Object methodNames = new java.util.ArrayList<Object>(java.util.Arrays.asList(methodName, Helpers.add(Helpers.add(methodName, "."), this.ext)));
        for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(methodNames)); i++)
        {
            Object mName = Helpers.GetValue(methodNames, i);
            if (Helpers.isTrue(Helpers.inOp(this.skippedMethods, mName)))
            {
                // if whole method is skipped, by assigning a string to it, i.e. "fetchOrders":"blabla"
                if (Helpers.isTrue((Helpers.GetValue(this.skippedMethods, mName) instanceof String)))
                {
                    return Helpers.GetValue(this.skippedMethods, mName);
                } else
                {
                    finalSkips = exchange.deepExtend(finalSkips, Helpers.GetValue(this.skippedMethods, mName));
                }
            }
        }
        // get "object-specific" skips
        Object objectSkips = new java.util.HashMap<String, Object>() {{
            put( "orderBook", new java.util.ArrayList<Object>(java.util.Arrays.asList("fetchOrderBook", "fetchOrderBooks", "fetchL2OrderBook", "watchOrderBook", "watchOrderBookForSymbols")) );
            put( "ticker", new java.util.ArrayList<Object>(java.util.Arrays.asList("fetchTicker", "fetchTickers", "watchTicker", "watchTickers")) );
            put( "trade", new java.util.ArrayList<Object>(java.util.Arrays.asList("fetchTrades", "watchTrades", "watchTradesForSymbols")) );
            put( "ohlcv", new java.util.ArrayList<Object>(java.util.Arrays.asList("fetchOHLCV", "watchOHLCV", "watchOHLCVForSymbols")) );
            put( "ledger", new java.util.ArrayList<Object>(java.util.Arrays.asList("fetchLedger", "fetchLedgerEntry")) );
            put( "depositWithdraw", new java.util.ArrayList<Object>(java.util.Arrays.asList("fetchDepositsWithdrawals", "fetchDeposits", "fetchWithdrawals")) );
            put( "depositWithdrawFee", new java.util.ArrayList<Object>(java.util.Arrays.asList("fetchDepositWithdrawFee", "fetchDepositWithdrawFees")) );
        }};
        Object objectNames = Helpers.objectKeys(objectSkips);
        for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(objectNames)); i++)
        {
            Object objectName = Helpers.GetValue(objectNames, i);
            Object objectMethods = Helpers.GetValue(objectSkips, objectName);
            if (Helpers.isTrue(exchange.inArray(methodName, objectMethods)))
            {
                // if whole object is skipped, by assigning a string to it, i.e. "orderBook":"blabla"
                if (Helpers.isTrue(Helpers.isTrue((Helpers.inOp(this.skippedMethods, objectName))) && Helpers.isTrue(((Helpers.GetValue(this.skippedMethods, objectName) instanceof String)))))
                {
                    return Helpers.GetValue(this.skippedMethods, objectName);
                }
                Object extraSkips = exchange.safeDict(this.skippedMethods, objectName, new java.util.HashMap<String, Object>() {{}});
                finalSkips = exchange.deepExtend(finalSkips, extraSkips);
            }
        }
        // extend related skips
        // - if 'timestamp' is skipped, we should do so for 'datetime' too
        // - if 'bid' is skipped, skip 'ask' too
        if (Helpers.isTrue(Helpers.isTrue((Helpers.inOp(finalSkips, "timestamp"))) && !Helpers.isTrue((Helpers.inOp(finalSkips, "datetime")))))
        {
            Helpers.addElementToObject(finalSkips, "datetime", Helpers.GetValue(finalSkips, "timestamp"));
        }
        if (Helpers.isTrue(Helpers.isTrue((Helpers.inOp(finalSkips, "bid"))) && !Helpers.isTrue((Helpers.inOp(finalSkips, "ask")))))
        {
            Helpers.addElementToObject(finalSkips, "ask", Helpers.GetValue(finalSkips, "bid"));
        }
        if (Helpers.isTrue(Helpers.isTrue((Helpers.inOp(finalSkips, "baseVolume"))) && !Helpers.isTrue((Helpers.inOp(finalSkips, "quoteVolume")))))
        {
            Helpers.addElementToObject(finalSkips, "quoteVolume", Helpers.GetValue(finalSkips, "baseVolume"));
        }
        return finalSkips;
    }

    public java.util.concurrent.CompletableFuture<Object> testSafe(Object methodName2, Exchange exchange, Object... optionalArgs)
    {
        final Object methodName3 = methodName2;
        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {
            Object methodName = methodName3;
            // `testSafe` method does not throw an exception, instead mutes it. The reason we
            // mute the thrown exceptions here is because we don't want to stop the whole
            // tests queue if any single test-method fails. Instead, they are echoed with
            // formatted message "[TEST_FAILURE] ..." and that output is then regex-matched by
            // run-tests.js, so the exceptions are still printed out to console from there.
            Object args = Helpers.getArg(optionalArgs, 0, new java.util.ArrayList<Object>(java.util.Arrays.asList()));
            Object isPublic = Helpers.getArg(optionalArgs, 1, false);
            Object maxRetries = 3;
            Object argsStringified = exchange.json(args); // args.join() breaks when we provide a list of symbols or multidimensional array; "args.toString()" breaks bcz of "array to string conversion"
            for (var i = 0; Helpers.isLessThan(i, maxRetries); i++)
            {
                try
                {
                    (this.testMethod(methodName, exchange, args, isPublic)).join();
                    return true;
                } catch(Exception ex)
                {
                    Object e = getRootException(ex);
                    Object isLoadMarkets = (Helpers.isEqual(methodName, "loadMarkets"));
                    Object isAuthError = (Helpers.isInstance(e, AuthenticationError.class));
                    Object isNotSupported = (Helpers.isInstance(e, NotSupported.class));
                    Object isOperationFailed = (Helpers.isInstance(e, OperationFailed.class)); // includes "DDoSProtection", "RateLimitExceeded", "RequestTimeout", "ExchangeNotAvailable", "OperationFailed", "InvalidNonce", ...
                    Object lastUrlMsg = ((Helpers.isTrue(this.wsTests))) ? "" : Helpers.add(Helpers.add(" (Last url: ", exchange.last_request_url), " )");
                    if (Helpers.isTrue(isOperationFailed))
                    {
                        // if last retry was gone with same `tempFailure` error, then let's eventually return false
                        if (Helpers.isTrue(Helpers.isEqual(i, Helpers.subtract(maxRetries, 1))))
                        {
                            Object isOnMaintenance = (Helpers.isInstance(e, OnMaintenance.class));
                            Object isExchangeNotAvailable = (Helpers.isInstance(e, ExchangeNotAvailable.class));
                            Object shouldFail = null;
                            Object retSuccess = null;
                            if (Helpers.isTrue(isLoadMarkets))
                            {
                                // if "loadMarkets" does not succeed, we must return "false" to caller method, to stop tests continual
                                retSuccess = false;
                                // we might not break exchange tests, if exchange is on maintenance at this moment
                                if (Helpers.isTrue(isOnMaintenance))
                                {
                                    shouldFail = false;
                                } else
                                {
                                    shouldFail = true;
                                }
                            } else
                            {
                                // for any other method tests:
                                if (Helpers.isTrue(Helpers.isTrue(isExchangeNotAvailable) && !Helpers.isTrue(isOnMaintenance)))
                                {
                                    // break exchange tests if "ExchangeNotAvailable" exception is thrown, but it's not maintenance
                                    shouldFail = true;
                                    retSuccess = false;
                                } else
                                {
                                    // in all other cases of OperationFailed, show Warning, but don't mark test as failed
                                    shouldFail = false;
                                    retSuccess = true;
                                }
                            }
                            // output the message
                            Object failType = ((Helpers.isTrue(shouldFail))) ? "[TEST_FAILURE]" : "[TEST_WARNING]";
                            dump(failType, exchange.id, methodName, argsStringified, lastUrlMsg, "Method could not be tested due to a repeated Network/Availability issues", " | ", exceptionMessage(e));
                            return retSuccess;
                        } else
                        {
                            // wait and retry again
                            // (increase wait time on every retry)
                            (exchange.sleep(Helpers.multiply((Helpers.add(i, 1)), 1000))).join();
                        }
                    } else
                    {
                        // if it's loadMarkets, then fail test, because it's mandatory for tests
                        if (Helpers.isTrue(isLoadMarkets))
                        {
                            dump("[TEST_FAILURE]", exchange.id, methodName, argsStringified, lastUrlMsg, "Exchange can not load markets", exceptionMessage(e));
                            return false;
                        }
                        // if the specific arguments to the test method throws "NotSupported" exception
                        // then let's don't fail the test
                        if (Helpers.isTrue(isNotSupported))
                        {
                            if (Helpers.isTrue(this.info))
                            {
                                dump("[INFO] NOT_SUPPORTED", exchange.id, methodName, argsStringified, lastUrlMsg, exceptionMessage(e));
                            }
                            return true;
                        }
                        // If public test faces authentication error, we don't break (see comments under `testSafe` method)
                        if (Helpers.isTrue(Helpers.isTrue(isPublic) && Helpers.isTrue(isAuthError)))
                        {
                            if (Helpers.isTrue(this.info))
                            {
                                // todo - turn into warning
                                dump("[INFO]", exchange.id, methodName, argsStringified, lastUrlMsg, "Authentication problem for public method", exceptionMessage(e));
                            }
                            return true;
                        } else
                        {
                            dump("[TEST_FAILURE]", exchange.id, methodName, argsStringified, lastUrlMsg, exceptionMessage(e));
                            return false;
                        }
                    }
                }
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> runPublicTests(Exchange exchange, Object symbols)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Object primarySymbol = Helpers.GetValue(symbols, 0);
            Object tests = new java.util.HashMap<String, Object>() {{
                put( "features", new java.util.ArrayList<Object>(java.util.Arrays.asList()) );
                put( "afterConstruct", new java.util.ArrayList<Object>(java.util.Arrays.asList()) );
                put( "fetchCurrencies", new java.util.ArrayList<Object>(java.util.Arrays.asList()) );
                put( "fetchTicker", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)) );
                put( "fetchTickers", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)) );
                put( "fetchLastPrices", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)) );
                put( "fetchOHLCV", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)) );
                put( "fetchTrades", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)) );
                put( "fetchOrderBook", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)) );
                put( "fetchOrderBooks", new java.util.ArrayList<Object>(java.util.Arrays.asList()) );
                put( "fetchBidsAsks", new java.util.ArrayList<Object>(java.util.Arrays.asList()) );
                put( "fetchStatus", new java.util.ArrayList<Object>(java.util.Arrays.asList()) );
                put( "fetchTime", new java.util.ArrayList<Object>(java.util.Arrays.asList()) );
            }};
            if (Helpers.isTrue(this.wsTests))
            {
                tests = new java.util.HashMap<String, Object>() {{
                    put( "watchOHLCV", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)) );
                    put( "watchOHLCVForSymbols", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)) );
                    put( "watchTicker", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)) );
                    put( "watchTickers", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)) );
                    put( "watchBidsAsks", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)) );
                    put( "watchOrderBook", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)) );
                    put( "watchOrderBookForSymbols", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbols)) );
                    put( "watchTrades", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)) );
                    put( "watchTradesForSymbols", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbols)) );
                }};
            }
            Object market = exchange.market(primarySymbol);
            Object isSpot = Helpers.GetValue(market, "spot");
            if (!Helpers.isTrue(this.wsTests))
            {
                if (Helpers.isTrue(isSpot))
                {
                    Helpers.addElementToObject(tests, "fetchCurrencies", new java.util.ArrayList<Object>(java.util.Arrays.asList()));
                } else
                {
                    Helpers.addElementToObject(tests, "fetchFundingRates", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)));
                    Helpers.addElementToObject(tests, "fetchFundingRate", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)));
                    Helpers.addElementToObject(tests, "fetchFundingRateHistory", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)));
                    Helpers.addElementToObject(tests, "fetchIndexOHLCV", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)));
                    Helpers.addElementToObject(tests, "fetchMarkOHLCV", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)));
                    Helpers.addElementToObject(tests, "fetchPremiumIndexOHLCV", new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol)));
                }
            }
            this.publicTests = tests;
            (this.runTests(exchange, tests, true)).join();
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> runTests(Exchange exchange, Object tests, Object isPublicTest)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Object testNames = Helpers.objectKeys(tests);
            Object promises = new java.util.ArrayList<Object>(java.util.Arrays.asList());
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(testNames)); i++)
            {
                Object testName = Helpers.GetValue(testNames, i);
                Object testArgs = Helpers.GetValue(tests, testName);
                ((java.util.List<Object>)promises).add(this.testSafe(testName, exchange, testArgs, isPublicTest));
            }
            // todo - not yet ready in other langs too
            // promises.push (testThrottle ());
            Object results = (Helpers.promiseAll(promises)).join();
            // now count which test-methods retuned `false` from "testSafe" and dump that info below
            Object failedMethods = new java.util.ArrayList<Object>(java.util.Arrays.asList());
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(testNames)); i++)
            {
                Object testName = Helpers.GetValue(testNames, i);
                Object testReturnedValue = Helpers.GetValue(results, i);
                if (!Helpers.isTrue(testReturnedValue))
                {
                    ((java.util.List<Object>)failedMethods).add(testName);
                }
            }
            Object testPrefixString = ((Helpers.isTrue(isPublicTest))) ? "PUBLIC_TESTS" : "PRIVATE_TESTS";
            if (Helpers.isTrue(Helpers.getArrayLength(failedMethods)))
            {
                Object errorsString = String.join((String)", ", (java.util.List<String>)failedMethods);
                dump("[TEST_FAILURE]", exchange.id, testPrefixString, Helpers.add("Failed methods : ", errorsString));
            }
            if (Helpers.isTrue(this.info))
            {
                dump(this.addPadding(Helpers.add(Helpers.add(Helpers.add("[INFO] END ", testPrefixString), " "), exchange.id), 25));
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> loadExchange(Exchange exchange)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Object result = (this.testSafe("loadMarkets", exchange, new java.util.ArrayList<Object>(java.util.Arrays.asList()), true)).join();
            if (!Helpers.isTrue(result))
            {
                return false;
            }
            Object exchangeSymbolsLength = Helpers.getArrayLength(exchange.symbols);
            dump("[INFO:MAIN] Exchange loaded", exchangeSymbolsLength, "symbols");
            return true;
        });

    }

    public Object getTestSymbol(Exchange exchange, Object isSpot, Object symbols)
    {
        Object symbol = null;
        Object preferredSpotSymbol = exchange.safeString(this.skippedSettingsForExchange, "preferredSpotSymbol");
        Object preferredSwapSymbol = exchange.safeString(this.skippedSettingsForExchange, "preferredSwapSymbol");
        if (Helpers.isTrue(Helpers.isTrue(isSpot) && Helpers.isTrue(preferredSpotSymbol)))
        {
            return preferredSpotSymbol;
        } else if (Helpers.isTrue(!Helpers.isTrue(isSpot) && Helpers.isTrue(preferredSwapSymbol)))
        {
            return preferredSwapSymbol;
        }
        for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(symbols)); i++)
        {
            Object s = Helpers.GetValue(symbols, i);
            Object market = exchange.safeValue(exchange.markets, s);
            if (Helpers.isTrue(!Helpers.isEqual(market, null)))
            {
                Object active = exchange.safeValue(market, "active");
                if (Helpers.isTrue(Helpers.isTrue(active) || Helpers.isTrue((Helpers.isEqual(active, null)))))
                {
                    symbol = s;
                    break;
                }
            }
        }
        return symbol;
    }

    public Object getExchangeCode(Exchange exchange, Object... optionalArgs)
    {
        Object codes = Helpers.getArg(optionalArgs, 0, null);
        if (Helpers.isTrue(Helpers.isEqual(codes, null)))
        {
            codes = new java.util.ArrayList<Object>(java.util.Arrays.asList("BTC", "ETH", "XRP", "LTC", "BCH", "EOS", "BNB", "BSV", "USDT"));
        }
        Object code = Helpers.GetValue(codes, 0);
        for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(codes)); i++)
        {
            if (Helpers.isTrue(Helpers.inOp(exchange.currencies, Helpers.GetValue(codes, i))))
            {
                return Helpers.GetValue(codes, i);
            }
        }
        return code;
    }

    public Object getMarketsFromExchange(Exchange exchange, Object... optionalArgs)
    {
        Object spot = Helpers.getArg(optionalArgs, 0, true);
        Object res = new java.util.HashMap<String, Object>() {{}};
        Object markets = exchange.markets;
        Object keys = Helpers.objectKeys(markets);
        for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(keys)); i++)
        {
            Object key = Helpers.GetValue(keys, i);
            Object market = Helpers.GetValue(markets, key);
            if (Helpers.isTrue(Helpers.isTrue(spot) && Helpers.isTrue(Helpers.GetValue(market, "spot"))))
            {
                Helpers.addElementToObject(res, Helpers.GetValue(market, "symbol"), market);
            } else if (Helpers.isTrue(!Helpers.isTrue(spot) && !Helpers.isTrue(Helpers.GetValue(market, "spot"))))
            {
                Helpers.addElementToObject(res, Helpers.GetValue(market, "symbol"), market);
            }
        }
        return res;
    }

    public Object getValidSymbol(Exchange exchange, Object... optionalArgs)
    {
        Object spot = Helpers.getArg(optionalArgs, 0, true);
        Object currentTypeMarkets = this.getMarketsFromExchange(exchange, spot);
        Object codes = new java.util.ArrayList<Object>(java.util.Arrays.asList("BTC", "ETH", "XRP", "LTC", "BNB", "DASH", "DOGE", "ETC", "TRX", "USDT", "USDC", "USD", "GUSD", "EUR", "TUSD", "CNY", "JPY", "BRL"));
        Object spotSymbols = new java.util.ArrayList<Object>(java.util.Arrays.asList("BTC/USDT", "BTC/USDC", "BTC/USD", "BTC/CNY", "BTC/EUR", "BTC/AUD", "BTC/BRL", "BTC/JPY", "ETH/USDT", "ETH/USDC", "ETH/USD", "ETH/CNY", "ETH/EUR", "ETH/AUD", "ETH/BRL", "ETH/JPY", "EUR/USDT", "EUR/USD", "EUR/USDC", "USDT/EUR", "USD/EUR", "USDC/EUR", "BTC/ETH", "ETH/BTC"));
        Object swapSymbols = new java.util.ArrayList<Object>(java.util.Arrays.asList("BTC/USDT:USDT", "BTC/USD:USDT", "BTC/USDC:USDC", "BTC/USD:USDC", "BTC/USD:USD", "ETH/USDT:USDT", "ETH/USD:USDT", "ETH/USDC:USDC", "ETH/USD:USDC", "ETH/USD:USD", "BTC/USD:BTC", "ETH/USD:ETH"));
        Object targetSymbols = ((Helpers.isTrue(spot))) ? spotSymbols : swapSymbols;
        Object symbol = this.getTestSymbol(exchange, spot, targetSymbols);
        // if symbols wasn't found from above hardcoded list, then try to locate any symbol which has our target hardcoded 'base' code
        if (Helpers.isTrue(Helpers.isEqual(symbol, null)))
        {
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(codes)); i++)
            {
                Object currentCode = Helpers.GetValue(codes, i);
                Object marketsArrayForCurrentCode = exchange.filterBy(currentTypeMarkets, "base", currentCode);
                Object indexedMkts = exchange.indexBy(marketsArrayForCurrentCode, "symbol");
                Object symbolsArrayForCurrentCode = Helpers.objectKeys(indexedMkts);
                Object symbolsLength = Helpers.getArrayLength(symbolsArrayForCurrentCode);
                if (Helpers.isTrue(symbolsLength))
                {
                    symbol = this.getTestSymbol(exchange, spot, symbolsArrayForCurrentCode);
                    break;
                }
            }
        }
        // if there wasn't found any symbol with our hardcoded 'base' code, then just try to find symbols that are 'active'
        if (Helpers.isTrue(Helpers.isEqual(symbol, null)))
        {
            Object activeMarkets = exchange.filterBy(currentTypeMarkets, "active", true);
            Object activeSymbols = new java.util.ArrayList<Object>(java.util.Arrays.asList());
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(activeMarkets)); i++)
            {
                ((java.util.List<Object>)activeSymbols).add(Helpers.GetValue(Helpers.GetValue(activeMarkets, i), "symbol"));
            }
            symbol = this.getTestSymbol(exchange, spot, activeSymbols);
        }
        if (Helpers.isTrue(Helpers.isEqual(symbol, null)))
        {
            Object values = Helpers.objectValues(currentTypeMarkets);
            Object valuesLength = Helpers.getArrayLength(values);
            if (Helpers.isTrue(Helpers.isGreaterThan(valuesLength, 0)))
            {
                Object first = Helpers.GetValue(values, 0);
                if (Helpers.isTrue(first))
                {
                    symbol = Helpers.GetValue(first, "symbol");
                }
            }
        }
        return symbol;
    }

    public java.util.concurrent.CompletableFuture<Object> testExchange(Exchange exchange, Object... optionalArgs)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Object providedSymbol = Helpers.getArg(optionalArgs, 0, null);
            Object spotSymbols = null;
            Object swapSymbols = null;
            if (Helpers.isTrue(!Helpers.isEqual(providedSymbol, null)))
            {
                Object market = exchange.market(providedSymbol);
                if (Helpers.isTrue(Helpers.GetValue(market, "spot")))
                {
                    spotSymbols = new java.util.ArrayList<Object>(java.util.Arrays.asList(providedSymbol));
                } else
                {
                    swapSymbols = new java.util.ArrayList<Object>(java.util.Arrays.asList(providedSymbol));
                }
            } else
            {
                if (Helpers.isTrue(Helpers.GetValue(exchange.has, "spot")))
                {
                    Object primarySymbol = this.getValidSymbol(exchange, true);
                    if (Helpers.isTrue(!Helpers.isEqual(primarySymbol, null)))
                    {
                        Object secondarySymbol = Helpers.replace((String)primarySymbol, (String)"BTC", (String)"ETH"); // this should work any exchange
                        spotSymbols = new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol, secondarySymbol));
                    }
                }
                if (Helpers.isTrue(Helpers.GetValue(exchange.has, "swap")))
                {
                    Object primarySymbol = this.getValidSymbol(exchange, false);
                    // some exchanges advertise has['swap']=true via describe() but
                    // the live market list contains no swap entries (e.g. bequant
                    // inherits hitbtc swap support but exposes only spot pairs).
                    // getValidSymbol returns undefined in that case — skip swap
                    // tests rather than crashing on `undefined.replace(...)`.
                    if (Helpers.isTrue(!Helpers.isEqual(primarySymbol, null)))
                    {
                        Object secondarySymbol = Helpers.replace((String)primarySymbol, (String)"BTC", (String)"ETH"); // this should work any exchange
                        swapSymbols = new java.util.ArrayList<Object>(java.util.Arrays.asList(primarySymbol, secondarySymbol));
                    }
                }
            }
            if (Helpers.isTrue(!Helpers.isEqual(spotSymbols, null)))
            {
                dump("[INFO:MAIN] Selected SPOT SYMBOL:", exchange.json(spotSymbols));
            }
            if (Helpers.isTrue(!Helpers.isEqual(swapSymbols, null)))
            {
                dump("[INFO:MAIN] Selected SWAP SYMBOL:", exchange.json(swapSymbols));
            }
            if (!Helpers.isTrue(this.privateTestOnly))
            {
                // note, spot & swap tests should run sequentially, because of conflicting `exchange.options['defaultType']` setting
                if (Helpers.isTrue(Helpers.isTrue(Helpers.GetValue(exchange.has, "spot")) && Helpers.isTrue(!Helpers.isEqual(spotSymbols, null))))
                {
                    if (Helpers.isTrue(this.info))
                    {
                        dump("[INFO] ### SPOT TESTS ###");
                    }
                    Helpers.addElementToObject(exchange.options, "defaultType", "spot");
                    (this.runPublicTests(exchange, spotSymbols)).join();
                }
                if (Helpers.isTrue(Helpers.isTrue(Helpers.GetValue(exchange.has, "swap")) && Helpers.isTrue(!Helpers.isEqual(swapSymbols, null))))
                {
                    if (Helpers.isTrue(this.info))
                    {
                        dump("[INFO] ### SWAP TESTS ###");
                    }
                    Helpers.addElementToObject(exchange.options, "defaultType", "swap");
                    (this.runPublicTests(exchange, swapSymbols)).join();
                }
            }
            if (Helpers.isTrue(Helpers.isTrue(this.privateTest) || Helpers.isTrue(this.privateTestOnly)))
            {
                if (Helpers.isTrue(Helpers.isTrue(Helpers.GetValue(exchange.has, "spot")) && Helpers.isTrue(!Helpers.isEqual(spotSymbols, null))))
                {
                    Helpers.addElementToObject(exchange.options, "defaultType", "spot");
                    (this.runPrivateTests(exchange, spotSymbols)).join();
                }
                if (Helpers.isTrue(Helpers.isTrue(Helpers.GetValue(exchange.has, "swap")) && Helpers.isTrue(!Helpers.isEqual(swapSymbols, null))))
                {
                    Helpers.addElementToObject(exchange.options, "defaultType", "swap");
                    (this.runPrivateTests(exchange, swapSymbols)).join();
                }
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> runPrivateTests(Exchange exchange, Object symbol)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            if (!Helpers.isTrue(exchange.checkRequiredCredentials(false)))
            {
                dump("[INFO] Skipping private tests", "Keys not found");
                return true;
            }
            Object code = this.getExchangeCode(exchange);
            // if (exchange.deepExtendedTest) {
            //     await test ('InvalidNonce', exchange, symbol);
            //     await test ('OrderNotFound', exchange, symbol);
            //     await test ('InvalidOrder', exchange, symbol);
            //     await test ('InsufficientFunds', exchange, symbol, balance); // danger zone - won't execute with non-empty balance
            // }
            Object tests = new java.util.HashMap<String, Object>() {{
                put( "signIn", new java.util.ArrayList<Object>(java.util.Arrays.asList()) );
                put( "fetchBalance", new java.util.ArrayList<Object>(java.util.Arrays.asList()) );
                put( "fetchAccounts", new java.util.ArrayList<Object>(java.util.Arrays.asList()) );
                put( "fetchTransactionFees", new java.util.ArrayList<Object>(java.util.Arrays.asList()) );
                put( "fetchTradingFees", new java.util.ArrayList<Object>(java.util.Arrays.asList()) );
                put( "fetchStatus", new java.util.ArrayList<Object>(java.util.Arrays.asList()) );
                put( "fetchOrders", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)) );
                put( "fetchOpenOrders", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)) );
                put( "fetchClosedOrders", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)) );
                put( "fetchMyTrades", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)) );
                put( "fetchLeverageTiers", new java.util.ArrayList<Object>(java.util.Arrays.asList(new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)))) );
                put( "fetchLedger", new java.util.ArrayList<Object>(java.util.Arrays.asList(code)) );
                put( "fetchTransactions", new java.util.ArrayList<Object>(java.util.Arrays.asList(code)) );
                put( "fetchDeposits", new java.util.ArrayList<Object>(java.util.Arrays.asList(code)) );
                put( "fetchWithdrawals", new java.util.ArrayList<Object>(java.util.Arrays.asList(code)) );
                put( "fetchBorrowInterest", new java.util.ArrayList<Object>(java.util.Arrays.asList(code, symbol)) );
                put( "cancelAllOrders", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)) );
                put( "fetchCanceledOrders", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)) );
                put( "fetchMarginModes", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)) );
                put( "fetchPosition", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)) );
                put( "fetchDeposit", new java.util.ArrayList<Object>(java.util.Arrays.asList(code)) );
                put( "createDepositAddress", new java.util.ArrayList<Object>(java.util.Arrays.asList(code)) );
                put( "fetchDepositAddress", new java.util.ArrayList<Object>(java.util.Arrays.asList(code)) );
                put( "fetchDepositAddresses", new java.util.ArrayList<Object>(java.util.Arrays.asList(code)) );
                put( "fetchDepositAddressesByNetwork", new java.util.ArrayList<Object>(java.util.Arrays.asList(code)) );
                put( "fetchBorrowRateHistory", new java.util.ArrayList<Object>(java.util.Arrays.asList(code)) );
                put( "fetchLedgerEntry", new java.util.ArrayList<Object>(java.util.Arrays.asList(code)) );
            }};
            if (Helpers.isTrue(getCliArgValue("--fundedTests")))
            {
                Helpers.addElementToObject(tests, "createOrder", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)));
            }
            if (Helpers.isTrue(this.wsTests))
            {
                tests = new java.util.HashMap<String, Object>() {{
                    put( "watchBalance", new java.util.ArrayList<Object>(java.util.Arrays.asList(code)) );
                    put( "watchMyTrades", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)) );
                    put( "watchOrders", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)) );
                    put( "watchPosition", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)) );
                    put( "watchPositions", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)) );
                }};
            }
            Object market = exchange.market(symbol);
            Object isSpot = Helpers.GetValue(market, "spot");
            if (!Helpers.isTrue(this.wsTests))
            {
                if (Helpers.isTrue(isSpot))
                {
                    Helpers.addElementToObject(tests, "fetchCurrencies", new java.util.ArrayList<Object>(java.util.Arrays.asList()));
                } else
                {
                    // derivatives only
                    Helpers.addElementToObject(tests, "fetchPositions", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol))); // this test fetches all positions for 1 symbol
                    Helpers.addElementToObject(tests, "fetchPosition", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)));
                    Helpers.addElementToObject(tests, "fetchPositionRisk", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)));
                    Helpers.addElementToObject(tests, "setPositionMode", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)));
                    Helpers.addElementToObject(tests, "setMarginMode", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)));
                    Helpers.addElementToObject(tests, "fetchOpenInterestHistory", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)));
                    Helpers.addElementToObject(tests, "fetchFundingRateHistory", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)));
                    Helpers.addElementToObject(tests, "fetchFundingHistory", new java.util.ArrayList<Object>(java.util.Arrays.asList(symbol)));
                }
            }
            // const combinedTests = exchange.deepExtend (this.publicTests, privateTests);
            (this.runTests(exchange, tests, false)).join();
            return true;  // required in c#
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testProxies(Exchange exchange)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            // these tests should be synchronously executed, because of conflicting nature of proxy settings
            Object proxyTestName = this.proxyTestFileName;
            // todo: temporary skip for sync py
            if (Helpers.isTrue(Helpers.isTrue(Helpers.isEqual(this.ext, "py")) && Helpers.isTrue(isSync())))
            {
                return true;
            }
            // try proxy several times
            Object maxRetries = 3;
            Object exception = null;
            for (var j = 0; Helpers.isLessThan(j, maxRetries); j++)
            {
                try
                {
                    (this.testMethod(proxyTestName, exchange, new java.util.ArrayList<Object>(java.util.Arrays.asList()), true)).join();
                    return true;  // if successfull, then end the test
                } catch(Exception e)
                {
                    exception = e;
                    (exchange.sleep(Helpers.multiply(j, 1000))).join();
                }
            }
            // if exception was set, then throw it
            if (Helpers.isTrue(!Helpers.isEqual(exception, null)))
            {
                Object errorMessage = Helpers.add(Helpers.add(Helpers.add("[TEST_FAILURE] Failed ", proxyTestName), " : "), exceptionMessage(exception));
                // temporary comment the below, because c# transpilation failure
                // throw new Exchange Error (errorMessage.toString ());
                dump(Helpers.add("[TEST_WARNING]", errorMessage));
            }
            return true;
        });

    }

    public void checkConstructor(Exchange exchange)
    {
        // todo: this might be moved in base tests later
        if (Helpers.isTrue(Helpers.isEqual(exchange.id, "binance")))
        {
            Assert(Helpers.isTrue(Helpers.isEqual(exchange.hostname, null)) || Helpers.isTrue(Helpers.isEqual(exchange.hostname, "")), "binance.com hostname should be empty");
            Assert(Helpers.isEqual(Helpers.GetValue(Helpers.GetValue(exchange.urls, "api"), "public"), "https://api.binance.com/api/v3"), Helpers.add("https://api.binance.com/api/v3 does not match: ", Helpers.GetValue(Helpers.GetValue(exchange.urls, "api"), "public")));
            Assert((Helpers.inOp(Helpers.GetValue(Helpers.GetValue(exchange.api, "sapi"), "get"), "lending/union/account")), Helpers.add("SAPI should contain the endpoint lending/union/account, ", jsonStringify(Helpers.GetValue(Helpers.GetValue(exchange.api, "sapi"), "get"))));
        } else if (Helpers.isTrue(Helpers.isEqual(exchange.id, "binanceus")))
        {
            Assert(Helpers.isEqual(exchange.hostname, "binance.us"), Helpers.add("binance.us hostname does not match ", exchange.hostname));
            Assert(Helpers.isEqual(Helpers.GetValue(Helpers.GetValue(exchange.urls, "api"), "public"), "https://api.binance.us/api/v3"), Helpers.add("https://api.binance.us/api/v3 does not match: ", Helpers.GetValue(Helpers.GetValue(exchange.urls, "api"), "public")));
        }
    }

    public java.util.concurrent.CompletableFuture<Object> testReturnResponseHeaders(Exchange exchange)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            if (Helpers.isTrue(!Helpers.isEqual(exchange.id, "binance")))
            {
                return false;  // this test is only for binance exchange for now
            }
            exchange.returnResponseHeaders = true;
            Object ticker = (exchange.fetchTicker("BTC/USDT")).join();
            Object info = Helpers.GetValue(ticker, "info");
            Object headers = Helpers.GetValue(info, "responseHeaders");
            Object headersKeys = Helpers.objectKeys(headers);
            Assert(Helpers.isGreaterThan(Helpers.getArrayLength(headersKeys), 0), "Response headers should not be empty");
            Object headerValues = Helpers.objectValues(headers);
            Assert(Helpers.isGreaterThan(Helpers.getArrayLength(headerValues), 0), "Response headers values should not be empty");
            exchange.returnResponseHeaders = false;
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> startTest(Exchange exchange, Object symbolArgv)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            // we do not need to test aliases
            if (Helpers.isTrue(exchange.alias))
            {
                return true;
            }
            this.checkConstructor(exchange);
            // await this.testReturnResponseHeaders (exchange);
            if (Helpers.isTrue(Helpers.isTrue(this.sandbox) || Helpers.isTrue(getExchangeProp(exchange, "sandbox"))))
            {
                exchange.setSandboxMode(true);
            }
            this.testHasProps(exchange);
            try
            {
                Object result = (this.loadExchange(exchange)).join();
                if (!Helpers.isTrue(result))
                {
                    if (!Helpers.isTrue(isSync()))
                    {
                        (close(exchange)).join();
                    }
                    return true;
                }
                // if (exchange.id === 'binance') {
                //     // we test proxies functionality just for one random exchange on each build, because proxy functionality is not exchange-specific, instead it's all done from base methods, so just one working sample would mean it works for all ccxt exchanges
                //     // await this.testProxies (exchange);
                // }
                (this.testExchange(exchange, symbolArgv)).join();
                if (!Helpers.isTrue(isSync()))
                {
                    (close(exchange)).join();
                }
            } catch(Exception e)
            {
                if (!Helpers.isTrue(isSync()))
                {
                    (close(exchange)).join();
                }
                throw new RuntimeException(e);
            }
            return true;  // required in c#
        });

    }

    public void testHasProps(Exchange exchange)
    {
        Object watchOrderBookSkips = this.getSkips(exchange, "watchOrderBook");
        Object fetchOrderBookSkips = this.getSkips(exchange, "fetchOrderBook");
        // ensure with hardcoded list of required methods
        if (Helpers.isTrue(Helpers.isTrue(Helpers.isTrue(this.wsTests) && !Helpers.isTrue(exchange.safeBool(exchange.has, "watchOrderBook", false))) && Helpers.isTrue(!(watchOrderBookSkips instanceof String))))
        {
            dump("[TEST_FAILURE] Method \"watchOrderBook\" is not set in \"has\", please check the \"has\" property of exchange");
            exitScript(1);
        } else if (Helpers.isTrue(Helpers.isTrue(!Helpers.isTrue(this.wsTests) && !Helpers.isTrue(exchange.safeBool(exchange.has, "fetchOrderBook", false))) && Helpers.isTrue(!(fetchOrderBookSkips instanceof String))))
        {
            dump("[TEST_FAILURE] Method \"fetchOrderBook\" is not set in \"has\", please check the \"has\" property of exchange");
            exitScript(1);
        }
    }

    public void AssertStaticError(Object cond, Object message, Object calculatedOutput, Object storedOutput, Object... optionalArgs)
    {
        //  -----------------------------------------------------------------------------
        //  --- Init of static tests functions------------------------------------------
        //  -----------------------------------------------------------------------------
        Object key = Helpers.getArg(optionalArgs, 0, null);
        Object calculatedString = jsonStringify(calculatedOutput);
        Object storedString = jsonStringify(storedOutput);
        Object errorMessage = message;
        if (Helpers.isTrue(!Helpers.isEqual(key, null)))
        {
            errorMessage = Helpers.add(Helpers.add("[", key), "]");
        }
        errorMessage = Helpers.add(errorMessage, Helpers.add(Helpers.add(Helpers.add(" computed: ", storedString), " stored: "), calculatedString));
        Assert(cond, errorMessage);
    }

    public Object loadMarketsFromFile(Object id)
    {
        // load markets from file
        // to make this test as fast as possible
        // and basically independent from the exchange
        // so we can run it offline
        Object filename = Helpers.add(Helpers.add(Helpers.add(getRootDir(), "./ts/src/test/static/markets/"), id), ".json");
        Object content = ioFileRead(filename);
        return content;
    }

    public Object loadCurrenciesFromFile(Object id)
    {
        Object filename = Helpers.add(Helpers.add(Helpers.add(getRootDir(), "./ts/src/test/static/currencies/"), id), ".json");
        Object content = ioFileRead(filename);
        return content;
    }

    public Object loadStaticData(Object folder, Object... optionalArgs)
    {
        Object targetExchange = Helpers.getArg(optionalArgs, 0, null);
        Object result = new java.util.HashMap<String, Object>() {{}};
        if (Helpers.isTrue(targetExchange))
        {
            // read a single exchange
            Object path = Helpers.add(Helpers.add(folder, targetExchange), ".json");
            if (!Helpers.isTrue(ioFileExists(path)))
            {
                dump(Helpers.add("[WARN] tests not found: ", path));
                return null;
            }
            Helpers.addElementToObject(result, targetExchange, ioFileRead(path));
            return result;
        }
        Object files = ioDirRead(folder);
        for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(files)); i++)
        {
            Object file = Helpers.GetValue(files, i);
            Object exchangeName = Helpers.replace((String)file, (String)".json", (String)"");
            Object content = ioFileRead(Helpers.add(folder, file));
            Helpers.addElementToObject(result, exchangeName, content);
        }
        return result;
    }

    public Object removeHostnamefromUrl(Object url)
    {
        if (Helpers.isTrue(Helpers.isEqual(url, null)))
        {
            return null;
        }
        Object urlParts = Helpers.split(url, "/");
        Object res = "";
        for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(urlParts)); i++)
        {
            if (Helpers.isTrue(Helpers.isGreaterThan(i, 2)))
            {
                Object current = Helpers.GetValue(urlParts, i);
                if (Helpers.isTrue(Helpers.isGreaterThan(Helpers.getIndexOf(current, "?"), Helpers.opNeg(1))))
                {
                    // handle urls like this: /v1/account/accounts?AccessK
                    Object currentParts = Helpers.split(current, "?");
                    res = Helpers.add(res, "/");
                    res = Helpers.add(res, Helpers.GetValue(currentParts, 0));
                    break;
                }
                res = Helpers.add(res, "/");
                res = Helpers.add(res, current);
            }
        }
        return res;
    }

    public Object urlencodedToDict(Object url)
    {
        Object result = new java.util.HashMap<String, Object>() {{}};
        Object parts = Helpers.split(url, "&");
        for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(parts)); i++)
        {
            Object part = Helpers.GetValue(parts, i);
            Object keyValue = Helpers.split(part, "=");
            Object keysLength = Helpers.getArrayLength(keyValue);
            if (Helpers.isTrue(!Helpers.isEqual(keysLength, 2)))
            {
                continue;
            }
            Object key = Helpers.GetValue(keyValue, 0);
            Object value = Helpers.GetValue(keyValue, 1);
            if (Helpers.isTrue(Helpers.isTrue((!Helpers.isEqual(value, null))) && Helpers.isTrue((Helpers.isTrue((((String)value).startsWith(((String)"[")))) || Helpers.isTrue((((String)value).startsWith(((String)"{"))))))))
            {
                // some exchanges might return something like this: timestamp=1699382693405&batchOrders=[{\"symbol\":\"LTCUSDT\",\"side\":\"BUY\",\"newClientOrderI
                value = jsonParse(value);
            }
            Helpers.addElementToObject(result, key, value);
        }
        return result;
    }

    public Object AssertNewAndStoredOutputInner(Exchange exchange, Object skipKeys, Object newOutput, Object storedOutput, Object... optionalArgs)
    {
        Object strictTypeCheck = Helpers.getArg(optionalArgs, 0, true);
        Object AssertingKey = Helpers.getArg(optionalArgs, 1, null);
        if (Helpers.isTrue(Helpers.isTrue(isNullValue(newOutput)) && Helpers.isTrue(isNullValue(storedOutput))))
        {
            return true;
        }
        if (Helpers.isTrue(!Helpers.isTrue(newOutput) && !Helpers.isTrue(storedOutput)))
        {
            return true;
        }
        // if needed convert stringified jsons to objects
        if (Helpers.isTrue(Helpers.isTrue(Helpers.isTrue(Helpers.isTrue(((storedOutput instanceof String))) && Helpers.isTrue(((newOutput instanceof String)))) && Helpers.isTrue(((String)storedOutput).startsWith(((String)"{")))) && Helpers.isTrue(((String)newOutput).startsWith(((String)"{")))))
        {
            storedOutput = jsonParse(storedOutput);
            newOutput = jsonParse(newOutput);
        }
        if (Helpers.isTrue(Helpers.isTrue(exchange.isDictionary(storedOutput)) && Helpers.isTrue(exchange.isDictionary(newOutput))))
        {
            Object storedOutputKeys = Helpers.objectKeys(storedOutput);
            Object newOutputKeys = Helpers.objectKeys(newOutput);
            Object storedKeysLength = Helpers.getArrayLength(storedOutputKeys);
            Object newKeysLength = Helpers.getArrayLength(newOutputKeys);
            this.AssertStaticError(Helpers.isEqual(storedKeysLength, newKeysLength), "output length mismatch", storedOutput, newOutput);
            // iterate over the keys
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(storedOutputKeys)); i++)
            {
                Object key = Helpers.GetValue(storedOutputKeys, i);
                if (Helpers.isTrue(exchange.inArray(key, skipKeys)))
                {
                    continue;
                }
                if (!Helpers.isTrue((exchange.inArray(key, newOutputKeys))))
                {
                    this.AssertStaticError(false, Helpers.add("output key missing: ", key), storedOutput, newOutput);
                }
                Object storedValue = Helpers.GetValue(storedOutput, key);
                Object newValue = Helpers.GetValue(newOutput, key);
                this.AssertNewAndStoredOutput(exchange, skipKeys, newValue, storedValue, strictTypeCheck, key);
            }
        } else if (Helpers.isTrue(Helpers.isTrue(Helpers.isTrue((!Helpers.isEqual(storedOutput, null))) && Helpers.isTrue(Helpers.isArray(storedOutput))) && Helpers.isTrue((Helpers.isArray(newOutput)))))
        {
            Object storedArrayLength = Helpers.getArrayLength(storedOutput);
            Object newArrayLength = Helpers.getArrayLength(newOutput);
            this.AssertStaticError(Helpers.isEqual(storedArrayLength, newArrayLength), "output length mismatch", storedOutput, newOutput);
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(storedOutput)); i++)
            {
                Object storedItem = Helpers.GetValue(storedOutput, i);
                Object newItem = Helpers.GetValue(newOutput, i);
                this.AssertNewAndStoredOutput(exchange, skipKeys, newItem, storedItem, strictTypeCheck);
            }
        } else
        {
            // built-in types like strings, numbers, booleans
            Object sanitizedNewOutput = ((Helpers.isTrue((isNullValue(newOutput))))) ? null : newOutput; // we store undefined as nulls in the json file so we need to convert it back
            Object sanitizedStoredOutput = ((Helpers.isTrue((isNullValue(storedOutput))))) ? null : storedOutput;
            Object newOutputString = ((Helpers.isTrue(sanitizedNewOutput))) ? String.valueOf(sanitizedNewOutput) : "undefined";
            Object storedOutputString = ((Helpers.isTrue(sanitizedStoredOutput))) ? String.valueOf(sanitizedStoredOutput) : "undefined";
            Object messageError = Helpers.add(Helpers.add(Helpers.add("output value mismatch:", newOutputString), " != "), storedOutputString);
            if (Helpers.isTrue(Helpers.isTrue(strictTypeCheck) && Helpers.isTrue((!Helpers.isEqual(this.lang, "C#")))))
            {
                // upon building the request we want strict type check to make sure all the types are correct
                // when comparing the response we want to allow some flexibility, because a 50.0 can be equal to 50 after saving it to the json file
                this.AssertStaticError(Helpers.isEqual(sanitizedNewOutput, sanitizedStoredOutput), messageError, storedOutput, newOutput, AssertingKey);
            } else
            {
                Object isComputedBool = ((sanitizedNewOutput instanceof Boolean));
                Object isStoredBool = ((sanitizedStoredOutput instanceof Boolean));
                Object isComputedString = ((sanitizedNewOutput instanceof String));
                Object isStoredString = ((sanitizedStoredOutput instanceof String));
                Object isComputedUndefined = (Helpers.isEqual(sanitizedNewOutput, null));
                Object isStoredUndefined = (Helpers.isEqual(sanitizedStoredOutput, null));
                Object shouldBeSame = Helpers.isTrue(Helpers.isTrue((Helpers.isEqual(isComputedBool, isStoredBool))) && Helpers.isTrue((Helpers.isEqual(isComputedString, isStoredString)))) && Helpers.isTrue((Helpers.isEqual(isComputedUndefined, isStoredUndefined)));
                this.AssertStaticError(shouldBeSame, "output type mismatch", storedOutput, newOutput, AssertingKey);
                Object isBoolean = Helpers.isTrue(isComputedBool) || Helpers.isTrue(isStoredBool);
                Object isString = Helpers.isTrue(isComputedString) || Helpers.isTrue(isStoredString);
                Object isUndefined = Helpers.isTrue(isComputedUndefined) || Helpers.isTrue(isStoredUndefined); // undefined is a perfetly valid value
                if (Helpers.isTrue(Helpers.isTrue(Helpers.isTrue(isBoolean) || Helpers.isTrue(isString)) || Helpers.isTrue(isUndefined)))
                {
                    if (Helpers.isTrue(Helpers.isTrue((Helpers.isEqual(this.lang, "C#"))) || Helpers.isTrue((Helpers.isEqual(this.lang, "GO")))))
                    {
                        // tmp c# number comparsion
                        Object isNumber = false;
                        try
                        {
                            exchange.parseToNumeric(sanitizedNewOutput);
                            isNumber = true;
                        } catch(Exception e)
                        {
                            // if we can't parse it to number, then it's not a number
                            isNumber = false;
                        }
                        if (Helpers.isTrue(isNumber))
                        {
                            this.AssertStaticError(Helpers.isEqual(exchange.parseToNumeric(sanitizedNewOutput), exchange.parseToNumeric(sanitizedStoredOutput)), messageError, storedOutput, newOutput, AssertingKey);
                            return true;
                        } else
                        {
                            this.AssertStaticError(Helpers.isEqual(convertAscii(newOutputString), convertAscii(storedOutputString)), messageError, storedOutput, newOutput, AssertingKey);
                            return true;
                        }
                    } else
                    {
                        this.AssertStaticError(Helpers.isEqual(convertAscii(newOutputString), convertAscii(storedOutputString)), messageError, storedOutput, newOutput, AssertingKey);
                        return true;
                    }
                } else
                {
                    if (Helpers.isTrue(Helpers.isEqual(this.lang, "C#")))
                    {
                        Object stringifiedNewOutput = exchange.numberToString(sanitizedNewOutput);
                        Object stringifiedStoredOutput = exchange.numberToString(sanitizedStoredOutput);
                        this.AssertStaticError(Helpers.isEqual(String.valueOf(stringifiedNewOutput), String.valueOf(stringifiedStoredOutput)), messageError, storedOutput, newOutput, AssertingKey);
                    } else
                    {
                        Object numericNewOutput = exchange.parseToNumeric(newOutputString);
                        Object numericStoredOutput = exchange.parseToNumeric(storedOutputString);
                        this.AssertStaticError(Helpers.isEqual(numericNewOutput, numericStoredOutput), messageError, storedOutput, newOutput, AssertingKey);
                    }
                }
            }
        }
        return true;  // c# requ
    }

    public Object AssertNewAndStoredOutput(Exchange exchange, Object skipKeys, Object newOutput, Object storedOutput, Object... optionalArgs)
    {
        Object strictTypeCheck = Helpers.getArg(optionalArgs, 0, true);
        Object AssertingKey = Helpers.getArg(optionalArgs, 1, null);
        Object res = true;
        try
        {
            res = this.AssertNewAndStoredOutputInner(exchange, skipKeys, newOutput, storedOutput, strictTypeCheck, AssertingKey);
        } catch(Exception e)
        {
            if (Helpers.isTrue(this.info))
            {
                Object errorMessage = Helpers.add(Helpers.add(Helpers.add(Helpers.add(this.varToString(newOutput), "(calculated)"), " != "), this.varToString(storedOutput)), "(stored)");
                dump(Helpers.add("[TEST_FAILURE_DETAIL]", errorMessage));
            }
            throw new RuntimeException(e);
        }
        return res;
    }

    public Object varToString(Object... optionalArgs)
    {
        Object obj = Helpers.getArg(optionalArgs, 0, null);
        Object newString = null;
        if (Helpers.isTrue(Helpers.isEqual(obj, null)))
        {
            newString = "undefined";
        } else if (Helpers.isTrue(isNullValue(obj)))
        {
            newString = "null";
        } else
        {
            newString = jsonStringify(obj);
        }
        return newString;
    }

    public Object AssertStaticRequestOutput(Exchange exchange, Object type, Object skipKeys, Object storedUrl, Object requestUrl, Object storedOutput, Object newOutput)
    {
        if (Helpers.isTrue(!Helpers.isEqual(storedUrl, requestUrl)))
        {
            // remove the host part from the url
            Object firstPath = this.removeHostnamefromUrl(storedUrl);
            Object secondPath = this.removeHostnamefromUrl(requestUrl);
            this.AssertStaticError(Helpers.isEqual(firstPath, secondPath), "url mismatch", firstPath, secondPath);
        }
        // body (aka storedOutput and newOutput) is not defined and information is in the url
        // example: "https://open-api.bingx.com/openApi/spot/v1/trade/order?quoteOrderQty=5&side=BUY&symbol=LTC-USDT&timestamp=1698777135343&type=MARKET&signature=d55a7e4f7f9dbe56c4004c9f3ab340869d3cb004e2f0b5b861e5fbd1762fd9a0
        if (Helpers.isTrue(Helpers.isTrue((Helpers.isEqual(storedOutput, null))) && Helpers.isTrue((Helpers.isEqual(newOutput, null)))))
        {
            if (Helpers.isTrue(Helpers.isTrue((!Helpers.isEqual(storedUrl, null))) && Helpers.isTrue((!Helpers.isEqual(requestUrl, null)))))
            {
                Object storedUrlParts = Helpers.split(storedUrl, "?");
                Object newUrlParts = Helpers.split(requestUrl, "?");
                Object storedUrlQuery = exchange.safeValue(storedUrlParts, 1);
                Object newUrlQuery = exchange.safeValue(newUrlParts, 1);
                if (Helpers.isTrue(Helpers.isTrue((Helpers.isEqual(storedUrlQuery, null))) && Helpers.isTrue((Helpers.isEqual(newUrlQuery, null)))))
                {
                    // might be a get request without any query parameters
                    // example: https://api.gateio.ws/api/v4/delivery/usdt/positions
                    return true;
                }
                Object storedUrlParams = this.urlencodedToDict(storedUrlQuery);
                Object newUrlParams = this.urlencodedToDict(newUrlQuery);
                this.AssertNewAndStoredOutput(exchange, skipKeys, newUrlParams, storedUrlParams);
                return true;
            }
        }
        if (Helpers.isTrue(Helpers.isTrue(Helpers.isTrue(Helpers.isEqual(type, "json")) && Helpers.isTrue((!Helpers.isEqual(storedOutput, null)))) && Helpers.isTrue((!Helpers.isEqual(newOutput, null)))))
        {
            if (Helpers.isTrue((storedOutput instanceof String)))
            {
                storedOutput = jsonParse(storedOutput);
            }
            if (Helpers.isTrue((newOutput instanceof String)))
            {
                newOutput = jsonParse(newOutput);
            }
        } else if (Helpers.isTrue(Helpers.isTrue(Helpers.isTrue(Helpers.isEqual(type, "urlencoded")) && Helpers.isTrue((!Helpers.isEqual(storedOutput, null)))) && Helpers.isTrue((!Helpers.isEqual(newOutput, null)))))
        {
            storedOutput = this.urlencodedToDict(storedOutput);
            newOutput = this.urlencodedToDict(newOutput);
        } else if (Helpers.isTrue(Helpers.isEqual(type, "both")))
        {
            if (Helpers.isTrue(Helpers.isTrue(((String)storedOutput).startsWith(((String)"{"))) || Helpers.isTrue(((String)storedOutput).startsWith(((String)"[")))))
            {
                storedOutput = jsonParse(storedOutput);
                newOutput = jsonParse(newOutput);
            } else
            {
                storedOutput = this.urlencodedToDict(storedOutput);
                newOutput = this.urlencodedToDict(newOutput);
            }
        }
        this.AssertNewAndStoredOutput(exchange, skipKeys, newOutput, storedOutput);
        return true;
    }

    public void AssertStaticResponseOutput(Exchange exchange, Object skipKeys, Object computedResult, Object storedResult)
    {
        this.AssertNewAndStoredOutput(exchange, skipKeys, computedResult, storedResult, false);
    }

    public Object sanitizeDataInput(Object input)
    {
        // remove nulls and replace with unefined instead
        if (Helpers.isTrue(Helpers.isEqual(input, null)))
        {
            return null;
        }
        Object newInput = new java.util.ArrayList<Object>(java.util.Arrays.asList());
        for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(input)); i++)
        {
            Object current = Helpers.GetValue(input, i);
            if (Helpers.isTrue(isNullValue(current)))
            {
                ((java.util.List<Object>)newInput).add(null);
            } else
            {
                ((java.util.List<Object>)newInput).add(current);
            }
        }
        return newInput;
    }

    public java.util.concurrent.CompletableFuture<Object> testRequestStatically(Exchange exchange, Object method, Object data, Object type, Object skipKeys)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Object output = null;
            Object requestUrl = null;
            if (Helpers.isTrue(this.info))
            {
                dump("[INFO] STATIC REQUEST TEST:", method, ":", Helpers.GetValue(data, "description"));
            }
            try
            {
                if (!Helpers.isTrue(isSync()))
                {
                    (callExchangeMethodDynamically(exchange, method, this.sanitizeDataInput(Helpers.GetValue(data, "input")))).join();
                } else
                {
                    callExchangeMethodDynamicallySync(exchange, method, this.sanitizeDataInput(Helpers.GetValue(data, "input")));
                }
            } catch(Exception e)
            {
                if (!Helpers.isTrue((Helpers.isInstance(e, InvalidProxySettings.class))))
                {
                    throw new RuntimeException(e);
                }
                output = exchange.last_request_body;
                requestUrl = exchange.last_request_url;
            }
            try
            {
                Object callOutput = exchange.safeValue(data, "output");
                this.AssertStaticRequestOutput(exchange, type, skipKeys, Helpers.GetValue(data, "url"), requestUrl, callOutput, output);
            } catch(Exception e)
            {
                this.requestTestsFailed = true;
                Object errorMessage = Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add("[", this.lang), "][STATIC_REQUEST]"), "["), exchange.id), "]"), "["), method), "]"), "["), Helpers.GetValue(data, "description")), "]"), exceptionMessage(e));
                dump(Helpers.add("[TEST_FAILURE]", errorMessage));
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testResponseStatically(Exchange exchange, Object method, Object skipKeys, Object data)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Object expectedResult = exchange.safeValue(data, "parsedResponse");
            var mockedExchange = setFetchResponse(exchange, Helpers.GetValue(data, "httpResponse"));
            if (Helpers.isTrue(this.info))
            {
                dump("[INFO] STATIC RESPONSE TEST:", method, ":", Helpers.GetValue(data, "description"));
            }
            try
            {
                if (!Helpers.isTrue(isSync()))
                {
                    Object unifiedResult = (callExchangeMethodDynamically(exchange, method, this.sanitizeDataInput(Helpers.GetValue(data, "input")))).join();
                    this.AssertStaticResponseOutput(mockedExchange, skipKeys, unifiedResult, expectedResult);
                } else
                {
                    Object unifiedResultSync = callExchangeMethodDynamicallySync(exchange, method, this.sanitizeDataInput(Helpers.GetValue(data, "input")));
                    this.AssertStaticResponseOutput(mockedExchange, skipKeys, unifiedResultSync, expectedResult);
                }
            } catch(Exception e)
            {
                this.responseTestsFailed = true;
                Object errorMessage = Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add("[", this.lang), "][STATIC_RESPONSE]"), "["), exchange.id), "]"), "["), method), "]"), "["), Helpers.GetValue(data, "description")), "]"), exceptionMessage(e));
                dump(Helpers.add("[TEST_FAILURE]", errorMessage));
            }
            setFetchResponse(exchange, null); // reset state
            return true;
        });

    }

    public Exchange initOfflineExchange(Object exchangeName)
    {
        Object markets = this.loadMarketsFromFile(exchangeName);
        Object currencies = this.loadCurrenciesFromFile(exchangeName);
        Object wasmExecPath = null;
        Object libraryPath = null;
        // const wasmExecPath = getRootDir () + '/src/test/static/binaries/wasm_exec.js';
        // const ligherWasmPath = getRootDir () + 'ts/src/test/static/binaries/lighter.wasm';
        // const binaryPath = getRootDir () + '/ts/src/test/static/binaries/lighter-signer-linux-amd64.so';
        // const librarypath = (this.lang === 'JS') ? ligherWasmPath : binaryPath;
        Object basePath = Helpers.add(getRootDir(), "ts/src/test/static/binaries/");
        if (Helpers.isTrue(Helpers.isEqual(exchangeName, "lighter")))
        {
            if (Helpers.isTrue(Helpers.isEqual(this.lang, "JS")))
            {
                wasmExecPath = Helpers.add(basePath, "wasm_exec.js");
                libraryPath = Helpers.add(basePath, "lighter.wasm");
            } else
            {
                if (Helpers.isTrue(isWindows()))
                {
                    libraryPath = Helpers.add(basePath, "lighter-signer-windows-amd64.dll");
                } else if (Helpers.isTrue(isLinux()))
                {
                    if (Helpers.isTrue(isAmd64()))
                    {
                        libraryPath = Helpers.add(basePath, "lighter-signer-linux-amd64.so");
                    } else
                    {
                        libraryPath = Helpers.add(basePath, "lighter-signer-linux-arm64.so");
                    }
                } else
                {
                    if (Helpers.isTrue(isAmd64()))
                    {
                        libraryPath = Helpers.add(basePath, "lighter-signer-darwin-x86.dylib");
                    } else
                    {
                        libraryPath = Helpers.add(basePath, "lighter-signer-darwin-arm64.dylib");
                    }
                }
            }
        }
        final Object finalLibraryPath = libraryPath;
        final Object finalWasmExecPath = wasmExecPath;
        Object options = new java.util.HashMap<String, Object>() {{
            put( "markets", markets );
            put( "currencies", currencies );
            put( "enableRateLimit", false );
            put( "rateLimit", 1 );
            put( "httpProxy", "http://fake:8080" );
            put( "httpsProxy", "http://fake:8080" );
            put( "apiKey", "key" );
            put( "secret", "secretsecret" );
            put( "password", "password" );
            put( "walletAddress", "wallet" );
            put( "privateKey", "0xff3bdd43534543d421f05aec535965b5050ad6ac15345435345435453495e771" );
            put( "uid", "uid" );
            put( "token", "token" );
            put( "login", "login" );
            put( "accountId", "12345" );
            put( "accounts", new java.util.ArrayList<Object>(java.util.Arrays.asList(new java.util.HashMap<String, Object>() {{
    put( "id", "myAccount" );
    put( "code", "USDT" );
}}, new java.util.HashMap<String, Object>() {{
    put( "id", "myAccount" );
    put( "code", "USDC" );
}})) );
            put( "options", new java.util.HashMap<String, Object>() {{
                put( "enableUnifiedAccount", true );
                put( "enableUnifiedMargin", false );
                put( "accessToken", "token" );
                put( "expires", 999999999999999L );
                put( "leverageBrackets", new java.util.HashMap<String, Object>() {{}} );
                put( "libraryPath", finalLibraryPath );
                put( "wasmExecPath", finalWasmExecPath );
            }} );
        }};
        if (Helpers.isTrue(Helpers.isEqual(exchangeName, "grvt")))
        {
            Helpers.addElementToObject(options, "apiKey", "");
            Helpers.addElementToObject(options, "secret", "");
        }
        Exchange exchange = initExchange(exchangeName, options);
        exchange.currencies = currencies;
        // not working in python if assigned  in the config dict
        return exchange;
    }

    public java.util.concurrent.CompletableFuture<Object> testExchangeRequestStatically(Object exchangeName, Object exchangeData, Object... optionalArgs)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            // instantiate the exchange and make sure that we sink the requests to avoid an actual request
            Object testName = Helpers.getArg(optionalArgs, 0, null);
            Exchange exchange = this.initOfflineExchange(exchangeName);
            Object globalOptions = exchange.safeDict(exchangeData, "options", new java.util.HashMap<String, Object>() {{}});
            // read apiKey/secret from the test file
            Object apiKey = exchange.safeString(exchangeData, "apiKey");
            if (!Helpers.isTrue(exchange.isEmptyString(apiKey)))
            {
                // c# to string requirement
                exchange.apiKey = String.valueOf(apiKey);
            }
            Object secret = exchange.safeString(exchangeData, "secret");
            if (!Helpers.isTrue(exchange.isEmptyString(secret)))
            {
                // c# to string requirement
                exchange.secret = String.valueOf(secret);
            }
            Object privateKey = exchange.safeString(exchangeData, "privateKey");
            if (!Helpers.isTrue(exchange.isEmptyString(privateKey)))
            {
                // c# to string requirement
                exchange.privateKey = String.valueOf(privateKey);
            }
            Object walletAddress = exchange.safeString(exchangeData, "walletAddress");
            if (!Helpers.isTrue(exchange.isEmptyString(walletAddress)))
            {
                // c# to string requirement
                exchange.walletAddress = String.valueOf(walletAddress);
            }
            Object accounts = exchange.safeList(exchangeData, "accounts");
            if (Helpers.isTrue(accounts))
            {
                exchange.accounts = accounts;
            }
            // exchange.options = exchange.deepExtend (exchange.options, globalOptions); // custom options to be used in the tests
            exchange.extendExchangeOptions(globalOptions);
            Object methods = exchange.safeValue(exchangeData, "methods", new java.util.HashMap<String, Object>() {{}});
            Object methodsNames = Helpers.objectKeys(methods);
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(methodsNames)); i++)
            {
                Object method = Helpers.GetValue(methodsNames, i);
                Object results = Helpers.GetValue(methods, method);
                for (var j = 0; Helpers.isLessThan(j, Helpers.getArrayLength(results)); j++)
                {
                    Object result = Helpers.GetValue(results, j);
                    Object oldExchangeOptions = exchange.options; // snapshot options;
                    Object testExchangeOptions = exchange.safeValue(result, "options", new java.util.HashMap<String, Object>() {{}});
                    // exchange.options = exchange.deepExtend (oldExchangeOptions, testExchangeOptions); // custom options to be used in the tests
                    exchange.extendExchangeOptions(exchange.deepExtend(oldExchangeOptions, testExchangeOptions));
                    Object description = exchange.safeValue(result, "description");
                    if (Helpers.isTrue(Helpers.isTrue((!Helpers.isEqual(testName, null))) && Helpers.isTrue((!Helpers.isEqual(testName, description)))))
                    {
                        continue;
                    }
                    Object isDisabled = exchange.safeBool(result, "disabled", false);
                    if (Helpers.isTrue(isDisabled))
                    {
                        continue;
                    }
                    Object disabledString = exchange.safeString(result, "disabled", "");
                    if (Helpers.isTrue(!Helpers.isEqual(disabledString, "")))
                    {
                        continue;
                    }
                    Object isDisabledCSharp = exchange.safeBool(result, "disabledCS", false);
                    if (Helpers.isTrue(Helpers.isTrue(isDisabledCSharp) && Helpers.isTrue((Helpers.isEqual(this.lang, "C#")))))
                    {
                        continue;
                    }
                    Object isDisabledGo = exchange.safeBool(result, "disabledGO", false);
                    if (Helpers.isTrue(Helpers.isTrue(isDisabledGo) && Helpers.isTrue((Helpers.isEqual(this.lang, "GO")))))
                    {
                        continue;
                    }
                    Object isDisabledJava = exchange.safeBool(result, "disabledJava", false);
                    if (Helpers.isTrue(Helpers.isTrue(isDisabledJava) && Helpers.isTrue((Helpers.isEqual(this.lang, "java")))))
                    {
                        continue;
                    }
                    Object type = exchange.safeString(exchangeData, "outputType");
                    Object skipKeys = exchange.safeValue(exchangeData, "skipKeys", new java.util.ArrayList<Object>(java.util.Arrays.asList()));
                    (this.testRequestStatically(exchange, method, result, type, skipKeys)).join();
                    // reset options
                    exchange.options = exchange.convertToSafeDictionary(exchange.deepExtend(oldExchangeOptions, new java.util.HashMap<String, Object>() {{}}));
                }
            }
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;  // in c# methods that will be used with promiseAll need to return something
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testExchangeResponseStatically(Object exchangeName, Object exchangeData, Object... optionalArgs)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Object testName = Helpers.getArg(optionalArgs, 0, null);
            Exchange exchange = this.initOfflineExchange(exchangeName);
            // read apiKey/secret from the test file
            Object apiKey = exchange.safeString(exchangeData, "apiKey");
            if (!Helpers.isTrue(exchange.isEmptyString(apiKey)))
            {
                // c# to string requirement
                exchange.apiKey = String.valueOf(apiKey);
            }
            Object secret = exchange.safeString(exchangeData, "secret");
            if (!Helpers.isTrue(exchange.isEmptyString(secret)))
            {
                // c# to string requirement
                exchange.secret = String.valueOf(secret);
            }
            Object privateKey = exchange.safeString(exchangeData, "privateKey");
            if (!Helpers.isTrue(exchange.isEmptyString(privateKey)))
            {
                // c# to string requirement
                exchange.privateKey = String.valueOf(privateKey);
            }
            Object walletAddress = exchange.safeString(exchangeData, "walletAddress");
            if (!Helpers.isTrue(exchange.isEmptyString(walletAddress)))
            {
                // c# to string requirement
                exchange.walletAddress = String.valueOf(walletAddress);
            }
            Object methods = exchange.safeValue(exchangeData, "methods", new java.util.HashMap<String, Object>() {{}});
            Object options = exchange.safeValue(exchangeData, "options", new java.util.HashMap<String, Object>() {{}});
            // exchange.options = exchange.deepExtend (exchange.options, options); // custom options to be used in the tests
            exchange.extendExchangeOptions(options);
            Object methodsNames = Helpers.objectKeys(methods);
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(methodsNames)); i++)
            {
                Object method = Helpers.GetValue(methodsNames, i);
                Object results = Helpers.GetValue(methods, method);
                for (var j = 0; Helpers.isLessThan(j, Helpers.getArrayLength(results)); j++)
                {
                    Object result = Helpers.GetValue(results, j);
                    Object description = exchange.safeValue(result, "description");
                    Object oldExchangeOptions = exchange.options; // snapshot options;
                    Object testExchangeOptions = exchange.safeValue(result, "options", new java.util.HashMap<String, Object>() {{}});
                    // exchange.options = exchange.deepExtend (oldExchangeOptions, testExchangeOptions); // custom options to be used in the tests
                    exchange.extendExchangeOptions(exchange.deepExtend(oldExchangeOptions, testExchangeOptions));
                    Object isDisabled = exchange.safeBool(result, "disabled", false);
                    if (Helpers.isTrue(isDisabled))
                    {
                        continue;
                    }
                    Object isDisabledCSharp = exchange.safeBool(result, "disabledCS", false);
                    if (Helpers.isTrue(Helpers.isTrue(isDisabledCSharp) && Helpers.isTrue((Helpers.isEqual(this.lang, "C#")))))
                    {
                        continue;
                    }
                    Object isDisabledPHP = exchange.safeBool(result, "disabledPHP", false);
                    if (Helpers.isTrue(Helpers.isTrue(isDisabledPHP) && Helpers.isTrue((Helpers.isEqual(this.lang, "PHP")))))
                    {
                        continue;
                    }
                    if (Helpers.isTrue(Helpers.isTrue((!Helpers.isEqual(testName, null))) && Helpers.isTrue((!Helpers.isEqual(testName, description)))))
                    {
                        continue;
                    }
                    Object isDisabledGO = exchange.safeBool(result, "disabledGO", false);
                    if (Helpers.isTrue(Helpers.isTrue(isDisabledGO) && Helpers.isTrue((Helpers.isEqual(this.lang, "GO")))))
                    {
                        continue;
                    }
                    Object isDisabledJava = exchange.safeBool(result, "disabledJava", false);
                    if (Helpers.isTrue(Helpers.isTrue(isDisabledJava) && Helpers.isTrue((Helpers.isEqual(this.lang, "java")))))
                    {
                        continue;
                    }
                    Object skipKeys = exchange.safeValue(exchangeData, "skipKeys", new java.util.ArrayList<Object>(java.util.Arrays.asList()));
                    (this.testResponseStatically(exchange, method, skipKeys, result)).join();
                    // reset options
                    // exchange.options = exchange.deepExtend (oldExchangeOptions, {});
                    exchange.extendExchangeOptions(exchange.deepExtend(oldExchangeOptions, new java.util.HashMap<String, Object>() {{}}));
                }
            }
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;  // in c# methods that will be used with promiseAll need to return something
        });

    }

    public Object getNumberOfTestsFromExchange(Exchange exchange, Object exchangeData, Object... optionalArgs)
    {
        Object testName = Helpers.getArg(optionalArgs, 0, null);
        if (Helpers.isTrue(!Helpers.isEqual(testName, null)))
        {
            return 1;
        }
        Object sum = 0;
        Object methods = Helpers.GetValue(exchangeData, "methods");
        Object methodsNames = Helpers.objectKeys(methods);
        for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(methodsNames)); i++)
        {
            Object method = Helpers.GetValue(methodsNames, i);
            Object results = Helpers.GetValue(methods, method);
            Object resultsLength = Helpers.getArrayLength(results);
            sum = exchange.sum(sum, resultsLength);
        }
        return sum;
    }

    public Object checkIfExchangeIsDisabled(Object exchangeName, Object exchangeData)
    {
        Exchange exchange = initExchange("Exchange", new java.util.HashMap<String, Object>() {{}});
        Object isDisabledPy = exchange.safeBool(exchangeData, "disabledPy", false);
        if (Helpers.isTrue(Helpers.isTrue(isDisabledPy) && Helpers.isTrue((Helpers.isEqual(this.lang, "PY")))))
        {
            dump(Helpers.add(Helpers.add("[TEST_WARNING] Exchange ", exchangeName), " is disabled in python"));
            return true;
        }
        Object isDisabledPHP = exchange.safeBool(exchangeData, "disabledPHP", false);
        if (Helpers.isTrue(Helpers.isTrue(isDisabledPHP) && Helpers.isTrue((Helpers.isEqual(this.lang, "PHP")))))
        {
            dump(Helpers.add(Helpers.add("[TEST_WARNING] Exchange ", exchangeName), " is disabled in php"));
            return true;
        }
        Object isDisabledCSharp = exchange.safeBool(exchangeData, "disabledCS", false);
        if (Helpers.isTrue(Helpers.isTrue(isDisabledCSharp) && Helpers.isTrue((Helpers.isEqual(this.lang, "C#")))))
        {
            dump(Helpers.add(Helpers.add("[TEST_WARNING] Exchange ", exchangeName), " is disabled in c#"));
            return true;
        }
        Object isDisabledGO = exchange.safeBool(exchangeData, "disabledGO", false);
        if (Helpers.isTrue(Helpers.isTrue(isDisabledGO) && Helpers.isTrue((Helpers.isEqual(this.lang, "GO")))))
        {
            dump(Helpers.add(Helpers.add("[TEST_WARNING] Exchange ", exchangeName), " is disabled in go"));
            return true;
        }
        Object isDisabledJava = exchange.safeBool(exchangeData, "disabledJava", false);
        if (Helpers.isTrue(Helpers.isTrue(isDisabledJava) && Helpers.isTrue((Helpers.isEqual(this.lang, "java")))))
        {
            dump(Helpers.add(Helpers.add("[TEST_WARNING] Exchange ", exchangeName), " is disabled in java"));
            return true;
        }
        return false;
    }

    public java.util.concurrent.CompletableFuture<Object> runStaticRequestTests(Object... optionalArgs)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Object targetExchange = Helpers.getArg(optionalArgs, 0, null);
            Object testName = Helpers.getArg(optionalArgs, 1, null);
            (this.runStaticTests("request", targetExchange, testName)).join();
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> runStaticTests(Object type2, Object... optionalArgs)
    {
        final Object type3 = type2;
        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {
            Object type = type3;
            Object targetExchange = Helpers.getArg(optionalArgs, 0, null);
            Object testName = Helpers.getArg(optionalArgs, 1, null);
            Object folder = Helpers.add(Helpers.add(Helpers.add(getRootDir(), "./ts/src/test/static/"), type), "/");
            Object staticData = this.loadStaticData(folder, targetExchange);
            if (Helpers.isTrue(Helpers.isEqual(staticData, null)))
            {
                return true;
            }
            Object exchanges = Helpers.objectKeys(staticData);
            Exchange exchange = initExchange("Exchange", new java.util.HashMap<String, Object>() {{}}); // tmp to do the calculations until we have the ast-transpiler transpiling this code
            Object promises = new java.util.ArrayList<Object>(java.util.Arrays.asList());
            Object sum = 0;
            if (Helpers.isTrue(targetExchange))
            {
                dump(Helpers.add("[INFO:MAIN] Exchange to test: ", targetExchange));
            }
            if (Helpers.isTrue(testName))
            {
                dump(Helpers.add("[INFO:MAIN] Testing only: ", testName));
            }
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(exchanges)); i++)
            {
                Object exchangeName = Helpers.GetValue(exchanges, i);
                Object exchangeData = Helpers.GetValue(staticData, exchangeName);
                Object disabled = this.checkIfExchangeIsDisabled(exchangeName, exchangeData);
                if (Helpers.isTrue(disabled))
                {
                    continue;
                }
                Object numberOfTests = this.getNumberOfTestsFromExchange(exchange, exchangeData, testName);
                sum = exchange.sum(sum, numberOfTests);
                if (Helpers.isTrue(Helpers.isEqual(type, "request")))
                {
                    ((java.util.List<Object>)promises).add(this.testExchangeRequestStatically(exchangeName, exchangeData, testName));
                } else
                {
                    ((java.util.List<Object>)promises).add(this.testExchangeResponseStatically(exchangeName, exchangeData, testName));
                }
            }
            try
            {
                (Helpers.promiseAll(promises)).join();
            } catch(Exception e)
            {
                if (Helpers.isTrue(Helpers.isEqual(type, "request")))
                {
                    this.requestTestsFailed = true;
                } else
                {
                    this.responseTestsFailed = true;
                }
                Object errorMessage = Helpers.add(Helpers.add(Helpers.add("[", this.lang), "][STATIC_REQUEST]"), exceptionMessage(e));
                dump(Helpers.add("[TEST_FAILURE]", errorMessage));
            }
            if (Helpers.isTrue(Helpers.isTrue(this.requestTestsFailed) || Helpers.isTrue(this.responseTestsFailed)))
            {
                exitScript(1);
            } else
            {
                Object prefix = ((Helpers.isTrue((isSync())))) ? "[SYNC]" : "";
                Object successMessage = Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add(Helpers.add("[", this.lang), "]"), prefix), "[TEST_SUCCESS] "), String.valueOf(sum)), " static "), type), " tests passed.");
                dump(Helpers.add("[INFO]", successMessage));
            }
            return true;  // required in c#
        });

    }

    public java.util.concurrent.CompletableFuture<Object> runStaticResponseTests(Object... optionalArgs)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            //  -----------------------------------------------------------------------------
            //  --- Init of mockResponses tests functions------------------------------------
            //  -----------------------------------------------------------------------------
            Object exchangeName = Helpers.getArg(optionalArgs, 0, null);
            Object test = Helpers.getArg(optionalArgs, 1, null);
            (this.runStaticTests("response", exchangeName, test)).join();
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> runBrokerIdTests()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            //  -----------------------------------------------------------------------------
            //  --- Init of brokerId tests functions-----------------------------------------
            //  -----------------------------------------------------------------------------
            Object promises = new java.util.ArrayList<Object>(java.util.Arrays.asList(this.testBinance(), this.testOkx(), this.testCryptocom(), this.testBybit(), this.testKucoin(), this.testKucoinfutures(), this.testBitget(), this.testMexc(), this.testHtx(), this.testWoo(), this.testBitmart(), this.testCoinex(), this.testBingx(), this.testPhemex(), this.testBlofin(), this.testCoinbaseinternational(), this.testCoinbaseAdvanced(), this.testWoofiPro(), this.testXT(), this.testParadex(), this.testHashkey(), this.testCryptomus(), this.testDerive(), this.testModeTrade(), this.testBackpack(), this.testToobit(), this.testWeex()));
            (Helpers.promiseAll(promises)).join();
            Object successMessage = Helpers.add(Helpers.add("[", this.lang), "][TEST_SUCCESS] brokerId tests passed.");
            dump(Helpers.add("[INFO]", successMessage));
            exitScript(0);
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testBinance()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("binance");
            Object spotId = "x-TKT5PX2F";
            Object swapId = "x-cvBPrNm9";
            Object inverseSwapId = "x-xcKtGhcu";
            Object spotOrderRequest = null;
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                spotOrderRequest = this.urlencodedToDict(exchange.last_request_body);
            }
            Object clientOrderId = Helpers.GetValue(spotOrderRequest, "newClientOrderId");
            Object spotIdString = String.valueOf(spotId);
            Assert(((String)clientOrderId).startsWith(((String)spotIdString)), Helpers.add(Helpers.add(Helpers.add("binance - spot clientOrderId: ", clientOrderId), " does not start with spotId"), spotIdString));
            Object swapOrderRequest = null;
            try
            {
                (exchange.createOrder("BTC/USDT:USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                swapOrderRequest = this.urlencodedToDict(exchange.last_request_body);
            }
            Object swapInverseOrderRequest = null;
            try
            {
                (exchange.createOrder("BTC/USD:BTC", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                swapInverseOrderRequest = this.urlencodedToDict(exchange.last_request_body);
            }
            // linear swap
            Object clientOrderIdSwap = Helpers.GetValue(swapOrderRequest, "newClientOrderId");
            Object swapIdString = String.valueOf(swapId);
            Assert(((String)clientOrderIdSwap).startsWith(((String)swapIdString)), Helpers.add(Helpers.add(Helpers.add("binance - swap clientOrderId: ", clientOrderIdSwap), " does not start with swapId"), swapIdString));
            // inverse swap
            Object clientOrderIdInverse = Helpers.GetValue(swapInverseOrderRequest, "newClientOrderId");
            Assert(((String)clientOrderIdInverse).startsWith(((String)inverseSwapId)), Helpers.add(Helpers.add(Helpers.add("binance - swap clientOrderIdInverse: ", clientOrderIdInverse), " does not start with swapId"), inverseSwapId));
            // linear swap conditional order
            Object swapAlgoOrderRequest = null;
            try
            {
                (exchange.createOrder("BTC/USDT:USDT", "limit", "buy", 0.002, 102000, new java.util.HashMap<String, Object>() {{
                    put( "triggerPrice", 101000 );
                }})).join();
                Object checkOrderRequest = this.urlencodedToDict(exchange.last_request_body);
                Object algoOrderIdDefined = (!Helpers.isEqual(Helpers.GetValue(checkOrderRequest, "algoOrderId"), null));
                Assert(algoOrderIdDefined, "binance - swap clientOrderId needs to be sent as algoOrderId but algoOrderId is not defined");
                Object clientAlgoIdSwap = Helpers.GetValue(swapAlgoOrderRequest, "clientAlgoId");
                Object swapAlgoIdString = String.valueOf(swapId);
                Assert(((String)clientAlgoIdSwap).startsWith(((String)swapAlgoIdString)), Helpers.add(Helpers.add(Helpers.add("binance - swap clientOrderId: ", clientAlgoIdSwap), " does not start with swapId"), swapAlgoIdString));
            } catch(Exception e)
            {
                swapAlgoOrderRequest = this.urlencodedToDict(exchange.last_request_body);
            }
            Object createOrdersRequest = null;
            try
            {
                Object orders = new java.util.ArrayList<Object>(java.util.Arrays.asList(new java.util.HashMap<String, Object>() {{
        put( "symbol", "BTC/USDT:USDT" );
        put( "type", "limit" );
        put( "side", "sell" );
        put( "amount", 1 );
        put( "price", 100000 );
    }}, new java.util.HashMap<String, Object>() {{
        put( "symbol", "BTC/USDT:USDT" );
        put( "type", "market" );
        put( "side", "buy" );
        put( "amount", 1 );
    }}));
                (exchange.createOrders(orders)).join();
            } catch(Exception e)
            {
                createOrdersRequest = this.urlencodedToDict(exchange.last_request_body);
            }
            Object batchOrders = Helpers.GetValue(createOrdersRequest, "batchOrders");
            for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(batchOrders)); i++)
            {
                Object current = Helpers.GetValue(batchOrders, i);
                Object currentClientOrderId = Helpers.GetValue(current, "newClientOrderId");
                Assert(((String)currentClientOrderId).startsWith(((String)swapIdString)), Helpers.add(Helpers.add(Helpers.add("binance createOrders - clientOrderId: ", currentClientOrderId), " does not start with swapId"), swapIdString));
            }
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testOkx()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("okx");
            Object id = "6b9ad766b55dBCDE";
            Object spotOrderRequest = null;
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                spotOrderRequest = jsonParse(exchange.last_request_body);
            }
            Object clientOrderId = Helpers.GetValue(Helpers.GetValue(spotOrderRequest, 0), "clOrdId"); // returns order inside array
            Object idString = String.valueOf(id);
            Assert(((String)clientOrderId).startsWith(((String)idString)), Helpers.add(Helpers.add(Helpers.add("okx - spot clientOrderId: ", clientOrderId), " does not start with id: "), idString));
            Object spotTag = Helpers.GetValue(Helpers.GetValue(spotOrderRequest, 0), "tag");
            Assert(Helpers.isEqual(spotTag, id), Helpers.add(Helpers.add(Helpers.add("okx - id: ", id), " different from spot tag: "), spotTag));
            Object swapOrderRequest = null;
            try
            {
                (exchange.createOrder("BTC/USDT:USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                swapOrderRequest = jsonParse(exchange.last_request_body);
            }
            Object clientOrderIdSwap = Helpers.GetValue(Helpers.GetValue(swapOrderRequest, 0), "clOrdId");
            Assert(((String)clientOrderIdSwap).startsWith(((String)idString)), Helpers.add(Helpers.add(Helpers.add("okx - swap clientOrderId: ", clientOrderIdSwap), " does not start with id: "), idString));
            Object swapTag = Helpers.GetValue(Helpers.GetValue(swapOrderRequest, 0), "tag");
            Assert(Helpers.isEqual(swapTag, id), Helpers.add(Helpers.add(Helpers.add("okx - id: ", id), " different from swap tag: "), swapTag));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testCryptocom()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("cryptocom");
            Object id = "CCXT";
            (exchange.loadMarkets()).join();
            Object request = null;
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                request = jsonParse(exchange.last_request_body);
            }
            Object brokerId = Helpers.GetValue(Helpers.GetValue(request, "params"), "broker_id");
            Assert(Helpers.isEqual(brokerId, id), Helpers.add(Helpers.add(Helpers.add("cryptocom - id: ", id), " different from  broker_id: "), brokerId));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testBybit()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("bybit");
            Object reqHeaders = null;
            Object id = "CCXT";
            Assert(Helpers.isEqual(Helpers.GetValue(exchange.options, "brokerId"), id), "id not in options");
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                // we expect an error here, we're only interested in the headers
                reqHeaders = exchange.last_request_headers;
            }
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "Referer"), id), Helpers.add(Helpers.add("bybit - id: ", id), " not in headers."));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testKucoin()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("kucoin");
            Helpers.addElementToObject(exchange.options, "uta", false); // prevents fetching account mode inside createOrder
            Object reqHeaders = null;
            Object spotId = Helpers.GetValue(Helpers.GetValue(Helpers.GetValue(exchange.options, "partner"), "spot"), "id");
            Object spotKey = Helpers.GetValue(Helpers.GetValue(Helpers.GetValue(exchange.options, "partner"), "spot"), "key");
            Assert(Helpers.isEqual(spotId, "ccxt"), Helpers.add(Helpers.add("kucoin - id: ", spotId), " not in options"));
            Assert(Helpers.isEqual(spotKey, "9e58cc35-5b5e-4133-92ec-166e3f077cb8"), Helpers.add(Helpers.add("kucoin - key: ", spotKey), " not in options."));
            Object futureId = Helpers.GetValue(Helpers.GetValue(Helpers.GetValue(exchange.options, "partner"), "future"), "id");
            Object futureKey = Helpers.GetValue(Helpers.GetValue(Helpers.GetValue(exchange.options, "partner"), "future"), "key");
            Assert(Helpers.isEqual(futureId, "ccxtfutures"), Helpers.add(Helpers.add("kucoin - id: ", futureId), " not in options."));
            Assert(Helpers.isEqual(futureKey, "1b327198-f30c-4f14-a0ac-918871282f15"), Helpers.add(Helpers.add("kucoin - key: ", futureKey), " not in options."));
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                // we expect an error here, we're only interested in the headers
                reqHeaders = exchange.last_request_headers;
            }
            Object id = "ccxt";
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "KC-API-PARTNER"), id), Helpers.add(Helpers.add("kucoin - id: ", id), " not in headers for spot orders."));
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000, new java.util.HashMap<String, Object>() {{
                    put( "uta", true );
                }})).join();
            } catch(Exception e)
            {
                reqHeaders = exchange.last_request_headers;
            }
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "KC-API-PARTNER"), id), Helpers.add(Helpers.add("kucoin - id: ", id), " not in headers for spot uta orders."));
            id = "ccxtfutures";
            try
            {
                (exchange.createOrder("BTC/USDT:USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                reqHeaders = exchange.last_request_headers;
            }
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "KC-API-PARTNER"), id), Helpers.add(Helpers.add("kucoin - id: ", id), " not in headers for swap orders."));
            try
            {
                (exchange.createOrder("BTC/USDT:USDT", "limit", "buy", 1, 20000, new java.util.HashMap<String, Object>() {{
                    put( "uta", true );
                }})).join();
            } catch(Exception e)
            {
                reqHeaders = exchange.last_request_headers;
            }
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "KC-API-PARTNER"), id), Helpers.add(Helpers.add("kucoin - id: ", id), " not in headers for swap uta orders."));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testKucoinfutures()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("kucoinfutures");
            Object reqHeaders = null;
            Object id = "ccxtfutures";
            Object futureId = Helpers.GetValue(Helpers.GetValue(Helpers.GetValue(exchange.options, "partner"), "future"), "id");
            Object futureKey = Helpers.GetValue(Helpers.GetValue(Helpers.GetValue(exchange.options, "partner"), "future"), "key");
            Assert(Helpers.isEqual(futureId, id), Helpers.add(Helpers.add("kucoinfutures - id: ", futureId), " not in options."));
            Assert(Helpers.isEqual(futureKey, "1b327198-f30c-4f14-a0ac-918871282f15"), Helpers.add(Helpers.add("kucoinfutures - key: ", futureKey), " not in options."));
            try
            {
                Helpers.addElementToObject(exchange.options, "uta", false);
                (exchange.createOrder("BTC/USDT:USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                reqHeaders = exchange.last_request_headers;
            }
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "KC-API-PARTNER"), id), Helpers.add(Helpers.add("kucoinfutures - id: ", id), " not in headers."));
            try
            {
                Helpers.addElementToObject(exchange.options, "uta", true);
                (exchange.createOrder("BTC/USDT:USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                reqHeaders = exchange.last_request_headers;
            }
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "KC-API-PARTNER"), id), Helpers.add(Helpers.add("kucoinfutures - id: ", id), " not in headers for uta orders."));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testBitget()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("bitget");
            Object reqHeaders = null;
            Object id = "p4sve";
            Assert(Helpers.isEqual(Helpers.GetValue(exchange.options, "broker"), id), Helpers.add(Helpers.add("bitget - id: ", id), " not in options"));
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                reqHeaders = exchange.last_request_headers;
            }
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "X-CHANNEL-API-CODE"), id), Helpers.add(Helpers.add("bitget - id: ", id), " not in headers."));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testMexc()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("mexc");
            Object reqHeaders = null;
            Object id = "CCXT";
            Assert(Helpers.isEqual(Helpers.GetValue(exchange.options, "broker"), id), Helpers.add(Helpers.add("mexc - id: ", id), " not in options"));
            (exchange.loadMarkets()).join();
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                reqHeaders = exchange.last_request_headers;
            }
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "source"), id), Helpers.add(Helpers.add("mexc - id: ", id), " not in headers."));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testHtx()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("htx");
            // spot test
            Object id = "AA03022abc";
            Object spotOrderRequest = null;
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                spotOrderRequest = jsonParse(exchange.last_request_body);
            }
            Object clientOrderId = Helpers.GetValue(spotOrderRequest, "client-order-id");
            Object idString = String.valueOf(id);
            Assert(((String)clientOrderId).startsWith(((String)idString)), Helpers.add(Helpers.add(Helpers.add("htx - spot clientOrderId ", clientOrderId), " does not start with id: "), idString));
            // swap test
            Object swapOrderRequest = null;
            try
            {
                (exchange.createOrder("BTC/USDT:USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                swapOrderRequest = jsonParse(exchange.last_request_body);
            }
            Object swapInverseOrderRequest = null;
            try
            {
                (exchange.createOrder("BTC/USD:BTC", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                swapInverseOrderRequest = jsonParse(exchange.last_request_body);
            }
            Object clientOrderIdSwap = Helpers.GetValue(swapOrderRequest, "channel_code");
            Assert(((String)clientOrderIdSwap).startsWith(((String)idString)), Helpers.add(Helpers.add(Helpers.add("htx - swap channel_code ", clientOrderIdSwap), " does not start with id: "), idString));
            Object clientOrderIdInverse = Helpers.GetValue(swapInverseOrderRequest, "channel_code");
            Assert(((String)clientOrderIdInverse).startsWith(((String)idString)), Helpers.add(Helpers.add(Helpers.add("htx - swap inverse channel_code ", clientOrderIdInverse), " does not start with id: "), idString));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testWoo()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("woo");
            // spot test
            Object id = "bc830de7-50f3-460b-9ee0-f430f83f9dad";
            Object spotOrderRequest = null;
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                spotOrderRequest = jsonParse(exchange.last_request_body);
            }
            Object brokerId = Helpers.GetValue(spotOrderRequest, "broker_id");
            Object idString = String.valueOf(id);
            Assert(((String)brokerId).startsWith(((String)idString)), Helpers.add(Helpers.add(Helpers.add("woo - broker_id: ", brokerId), " does not start with id: "), idString));
            // swap test
            Object stopOrderRequest = null;
            try
            {
                (exchange.createOrder("BTC/USDT:USDT", "limit", "buy", 1, 20000, new java.util.HashMap<String, Object>() {{
                    put( "stopPrice", 30000 );
                }})).join();
            } catch(Exception e)
            {
                stopOrderRequest = jsonParse(exchange.last_request_body);
            }
            Object clientOrderIdStop = Helpers.GetValue(stopOrderRequest, "brokerId");
            Assert(((String)clientOrderIdStop).startsWith(((String)idString)), Helpers.add(Helpers.add(Helpers.add("woo - brokerId: ", clientOrderIdStop), " does not start with id: "), idString));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testBitmart()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("bitmart");
            Object reqHeaders = null;
            Object id = "CCXTxBitmart000";
            Assert(Helpers.isEqual(Helpers.GetValue(exchange.options, "brokerId"), id), Helpers.add(Helpers.add("bitmart - id: ", id), " not in options"));
            (exchange.loadMarkets()).join();
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                reqHeaders = exchange.last_request_headers;
            }
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "X-BM-BROKER-ID"), id), Helpers.add(Helpers.add("bitmart - id: ", id), " not in headers"));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testCoinex()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("coinex");
            Object id = "x-167673045";
            Assert(Helpers.isEqual(Helpers.GetValue(exchange.options, "brokerId"), id), Helpers.add(Helpers.add("coinex - id: ", id), " not in options"));
            Object spotOrderRequest = null;
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                spotOrderRequest = jsonParse(exchange.last_request_body);
            }
            Object clientOrderId = Helpers.GetValue(spotOrderRequest, "client_id");
            Object idString = String.valueOf(id);
            Assert(((String)clientOrderId).startsWith(((String)idString)), Helpers.add(Helpers.add(Helpers.add("coinex - clientOrderId: ", clientOrderId), " does not start with id: "), idString));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testBingx()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("bingx");
            Object reqHeaders = null;
            Object id = "CCXT";
            Assert(Helpers.isEqual(Helpers.GetValue(exchange.options, "broker"), id), Helpers.add(Helpers.add("bingx - id: ", id), " not in options"));
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                // we expect an error here, we're only interested in the headers
                reqHeaders = exchange.last_request_headers;
            }
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "X-SOURCE-KEY"), id), Helpers.add(Helpers.add("bingx - id: ", id), " not in headers."));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testPhemex()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("phemex");
            Object id = "CCXT123456";
            Object request = null;
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                request = jsonParse(exchange.last_request_body);
            }
            Object clientOrderId = Helpers.GetValue(request, "clOrdID");
            Object idString = String.valueOf(id);
            Assert(((String)clientOrderId).startsWith(((String)idString)), Helpers.add(Helpers.add(Helpers.add("phemex - clOrdID: ", clientOrderId), " does not start with id: "), idString));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testBlofin()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("blofin");
            Object id = "ec6dd3a7dd982d0b";
            Object request = null;
            try
            {
                (exchange.createOrder("LTC/USDT:USDT", "market", "buy", 1)).join();
            } catch(Exception e)
            {
                request = jsonParse(exchange.last_request_body);
            }
            Object brokerId = Helpers.GetValue(request, "brokerId");
            Object idString = String.valueOf(id);
            Assert(((String)brokerId).startsWith(((String)idString)), Helpers.add(Helpers.add(Helpers.add("blofin - brokerId: ", brokerId), " does not start with id: "), idString));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    // async testHyperliquid () {
    //     const exchange = this.initOfflineExchange ('hyperliquid');
    //     const id = '1';
    //     let request = undefined;
    //     try {
    //         await exchange.createOrder ('SOL/USDC:USDC', 'limit', 'buy', 1, 100);
    //     } catch (e) {
    //         request = jsonParse (exchange.last_request_body);
    //     }
    //     const brokerId = (request['action']['brokerCode']).toString ();
    //     Assert (brokerId === id, 'hyperliquid - brokerId: ' + brokerId + ' does not start with id: ' + id);
    //     if (!isSync ()) {
    //         await close (exchange);
    //     }
    //     return true;
    // }
    public java.util.concurrent.CompletableFuture<Object> testCoinbaseinternational()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("coinbaseinternational");
            Helpers.addElementToObject(exchange.options, "portfolio", "random");
            Object id = "nfqkvdjp";
            Assert(Helpers.isEqual(Helpers.GetValue(exchange.options, "brokerId"), id), "id not in options");
            Object request = null;
            try
            {
                (exchange.createOrder("BTC/USDC:USDC", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                request = jsonParse(exchange.last_request_body);
            }
            Object clientOrderId = Helpers.GetValue(request, "client_order_id");
            Assert(((String)clientOrderId).startsWith(((String)String.valueOf(id))), "clientOrderId does not start with id");
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testCoinbaseAdvanced()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("coinbase");
            Object id = "ccxt";
            Assert(Helpers.isEqual(Helpers.GetValue(exchange.options, "brokerId"), id), "id not in options");
            Object request = null;
            try
            {
                (exchange.createOrder("BTC/USDC", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                request = jsonParse(exchange.last_request_body);
            }
            Object clientOrderId = Helpers.GetValue(request, "client_order_id");
            Assert(((String)clientOrderId).startsWith(((String)String.valueOf(id))), "clientOrderId does not start with id");
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testWoofiPro()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            if (Helpers.isTrue(Helpers.isEqual(this.lang, "java")))
            {
                return false;
            }
            Exchange exchange = this.initOfflineExchange("woofipro");
            exchange.secret = "secretsecretsecretsecretsecretsecretsecrets";
            Object id = "CCXT";
            (exchange.loadMarkets()).join();
            Object request = null;
            try
            {
                (exchange.createOrder("BTC/USDC:USDC", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                request = jsonParse(exchange.last_request_body);
            }
            Object brokerId = Helpers.GetValue(request, "order_tag");
            Assert(Helpers.isEqual(brokerId, id), Helpers.add(Helpers.add(Helpers.add("woofipro - id: ", id), " different from  broker_id: "), brokerId));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testXT()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("xt");
            Object id = "CCXT";
            Object spotOrderRequest = null;
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                spotOrderRequest = jsonParse(exchange.last_request_body);
            }
            Object spotMedia = Helpers.GetValue(spotOrderRequest, "media");
            Assert(Helpers.isEqual(spotMedia, id), Helpers.add(Helpers.add(Helpers.add("xt - id: ", id), " different from swap tag: "), spotMedia));
            Object swapOrderRequest = null;
            try
            {
                (exchange.createOrder("BTC/USDT:USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                swapOrderRequest = jsonParse(exchange.last_request_body);
            }
            Object swapMedia = Helpers.GetValue(swapOrderRequest, "clientMedia");
            Assert(Helpers.isEqual(swapMedia, id), Helpers.add(Helpers.add(Helpers.add("xt - id: ", id), " different from swap tag: "), swapMedia));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testParadex()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            if (Helpers.isTrue(Helpers.isEqual(this.lang, "java")))
            {
                return false;
            }
            Exchange exchange = this.initOfflineExchange("paradex");
            exchange.walletAddress = "0xc751489d24a33172541ea451bc253d7a9e98c781";
            exchange.privateKey = "c33b1eb4b53108bf52e10f636d8c1236c04c33a712357ba3543ab45f48a5cb0b";
            Helpers.addElementToObject(exchange.options, "authToken", "token");
            Helpers.addElementToObject(exchange.options, "systemConfig", new java.util.HashMap<String, Object>() {{
        put( "starknet_gateway_url", "https://potc-testnet-sepolia.starknet.io" );
        put( "starknet_fullnode_rpc_url", "https://pathfinder.api.testnet.paradex.trade/rpc/v0_7" );
        put( "starknet_chain_id", "PRIVATE_SN_POTC_SEPOLIA" );
        put( "block_explorer_url", "https://voyager.testnet.paradex.trade/" );
        put( "paraclear_address", "0x286003f7c7bfc3f94e8f0af48b48302e7aee2fb13c23b141479ba00832ef2c6" );
        put( "paraclear_decimals", 8 );
        put( "paraclear_account_proxy_hash", "0x3530cc4759d78042f1b543bf797f5f3d647cde0388c33734cf91b7f7b9314a9" );
        put( "paraclear_account_hash", "0x41cb0280ebadaa75f996d8d92c6f265f6d040bb3ba442e5f86a554f1765244e" );
        put( "oracle_address", "0x2c6a867917ef858d6b193a0ff9e62b46d0dc760366920d631715d58baeaca1f" );
        put( "bridged_tokens", new java.util.ArrayList<Object>(java.util.Arrays.asList(new java.util.HashMap<String, Object>() {{
        put( "name", "TEST USDC" );
        put( "symbol", "USDC" );
        put( "decimals", 6 );
        put( "l1_token_address", "0x29A873159D5e14AcBd63913D4A7E2df04570c666" );
        put( "l1_bridge_address", "0x8586e05adc0C35aa11609023d4Ae6075Cb813b4C" );
        put( "l2_token_address", "0x6f373b346561036d98ea10fb3e60d2f459c872b1933b50b21fe6ef4fda3b75e" );
        put( "l2_bridge_address", "0x46e9237f5408b5f899e72125dd69bd55485a287aaf24663d3ebe00d237fc7ef" );
    }})) );
        put( "l1_core_contract_address", "0x582CC5d9b509391232cd544cDF9da036e55833Af" );
        put( "l1_operator_address", "0x11bACdFbBcd3Febe5e8CEAa75E0Ef6444d9B45FB" );
        put( "l1_chain_id", "11155111" );
        put( "liquidation_fee", "0.2" );
    }});
            Object reqHeaders = null;
            Object id = "CCXT";
            Assert(Helpers.isEqual(Helpers.GetValue(exchange.options, "broker"), id), Helpers.add(Helpers.add("paradex - id: ", id), " not in options"));
            (exchange.loadMarkets()).join();
            try
            {
                (exchange.createOrder("BTC/USD:USDC", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                reqHeaders = exchange.last_request_headers;
            }
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "PARADEX-PARTNER"), id), Helpers.add(Helpers.add("paradex - id: ", id), " not in headers"));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testHashkey()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("hashkey");
            Object reqHeaders = null;
            Object id = "10000700011";
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                // we expect an error here, we're only interested in the headers
                reqHeaders = exchange.last_request_headers;
            }
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "INPUT-SOURCE"), id), Helpers.add(Helpers.add("hashkey - id: ", id), " not in headers."));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testCryptomus()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("cryptomus");
            Object request = null;
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "sell", 1, 20000)).join();
            } catch(Exception e)
            {
                request = jsonParse(exchange.last_request_body);
            }
            Object tag = "ccxt";
            Assert(Helpers.isEqual(Helpers.GetValue(request, "tag"), tag), Helpers.add(Helpers.add("cryptomus - tag: ", tag), " not in request."));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testDerive()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            if (Helpers.isTrue(Helpers.isEqual(this.lang, "java")))
            {
                return false;
            }
            Exchange exchange = this.initOfflineExchange("derive");
            Object id = "0x0ad42b8e602c2d3d475ae52d678cf63d84ab2749";
            Assert(Helpers.isEqual(Helpers.GetValue(exchange.options, "id"), id), Helpers.add(Helpers.add("derive - id: ", id), " not in options"));
            Object request = null;
            try
            {
                Object parameters = new java.util.HashMap<String, Object>() {{
                    put( "subaccount_id", 1234 );
                    put( "max_fee", 10 );
                    put( "deriveWalletAddress", "0x0ad42b8e602c2d3d475ae52d678cf63d84ab2749" );
                }};
                exchange.walletAddress = "0x0ad42b8e602c2d3d475ae52d678cf63d84ab2749";
                exchange.privateKey = "0x7b77bb7b20e92bbb85f2a22b330b896959229a5790e35f2f290922de3fb22ad5";
                (exchange.createOrder("LBTC/USDC", "limit", "sell", 0.01, 3000, parameters)).join();
            } catch(Exception e)
            {
                request = jsonParse(exchange.last_request_body);
            }
            Assert(Helpers.isEqual(Helpers.GetValue(request, "referral_code"), id), Helpers.add(Helpers.add("derive - referral_code: ", id), " not in request."));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testModeTrade()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            if (Helpers.isTrue(Helpers.isEqual(this.lang, "java")))
            {
                return false;
            }
            Exchange exchange = this.initOfflineExchange("modetrade");
            exchange.secret = "secretsecretsecretsecretsecretsecretsecrets";
            Object id = "CCXTMODE";
            (exchange.loadMarkets()).join();
            Object request = null;
            try
            {
                (exchange.createOrder("BTC/USDC:USDC", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                request = jsonParse(exchange.last_request_body);
            }
            Object brokerId = Helpers.GetValue(request, "order_tag");
            Assert(Helpers.isEqual(brokerId, id), Helpers.add(Helpers.add(Helpers.add("modetrade - id: ", id), " different from  broker_id: "), brokerId));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testBackpack()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("backpack");
            exchange.apiKey = "Jcj3vxDMAIrx0G5YYfydzS/le/owoQ+VSS164zC1RXo=";
            exchange.secret = "sRkC124Iazob0QYvaFj9dm63MXEVY48lDNt+/GVDVAU=";
            Object reqHeaders = null;
            Object id = "1400";
            try
            {
                (exchange.createOrder("ETH/USDC", "limit", "buy", 1, 5000)).join();
            } catch(Exception e)
            {
                // we expect an error here, we're only interested in the headers
                reqHeaders = exchange.last_request_headers;
            }
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "X-Broker-Id"), id), Helpers.add(Helpers.add("backpack - id: ", id), " not in headers."));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testToobit()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("toobit");
            Object reqHeaders = null;
            Object id = "177321641268789";
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                // we expect an error here, we're only interested in the headers
                reqHeaders = exchange.last_request_headers;
            }
            Assert(Helpers.isEqual(Helpers.GetValue(reqHeaders, "X-BB-API-PLATFORM"), id), Helpers.add(Helpers.add("toobit - id: ", id), " not in headers."));
            if (!Helpers.isTrue(isSync()))
            {
                (close(exchange)).join();
            }
            return true;
        });

    }

    public java.util.concurrent.CompletableFuture<Object> testWeex()
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

            Exchange exchange = this.initOfflineExchange("weex");
            Object id = "b-WEEX111125";
            Assert(Helpers.isEqual(Helpers.GetValue(exchange.options, "partner"), id), Helpers.add(Helpers.add("weex - id: ", id), " not in options"));
            Object request = null;
            try
            {
                (exchange.createOrder("BTC/USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                request = jsonParse(exchange.last_request_body);
            }
            Object clientOrderId = Helpers.GetValue(request, "newClientOrderId");
            Assert(((String)clientOrderId).startsWith(((String)id)), Helpers.add(Helpers.add(Helpers.add("weex - newClientOrderId: ", clientOrderId), " for spot order does not start with id: "), id));
            try
            {
                (exchange.createOrder("BTC/USDT:USDT", "limit", "buy", 1, 20000)).join();
            } catch(Exception e)
            {
                request = jsonParse(exchange.last_request_body);
            }
            clientOrderId = Helpers.GetValue(request, "newClientOrderId");
            Assert(((String)clientOrderId).startsWith(((String)id)), Helpers.add(Helpers.add(Helpers.add("weex - newClientOrderId: ", clientOrderId), " for swap order does not start with id: "), id));
            return null;
        });

    }
}
