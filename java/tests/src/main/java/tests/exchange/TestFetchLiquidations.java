package tests.exchange;
import tests.BaseTest;
import io.github.ccxt.Helpers;
import io.github.ccxt.Exchange;
import io.github.ccxt.BaseExchange;
import io.github.ccxt.errors.*;
import java.util.List;
import java.util.Map;
import java.util.concurrent.CompletableFuture;


// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code


public class TestFetchLiquidations extends BaseTest {
    public CompletableFuture<Object> testFetchLiquidations(BaseExchange exchange, Object skippedProperties, Object code)
    {

        return BaseExchange.supplyAsync(() -> {

        String method = "fetchLiquidations";
        if (java.util.Objects.equals(((Map<String, Object>)exchange.has).get("fetchLiquidations"), null) || java.util.Objects.equals(((Map<String, Object>)exchange.has).get("fetchLiquidations"), false))
        {
            return true;
        }
        Object items = ((CompletableFuture<Object>)Helpers.callDynamically(exchange, "fetchLiquidations", new Object[]{code})).join();
        Assert((items instanceof List), ((((((exchange.id + " ") + method) + " ") + code) + " must return an array. ") + exchange.json(items)));
        // const now = exchange.milliseconds ();
        for (var i = 0; i < ((List<?>)items).size(); i++)
        {
            TestLiquidation.testLiquidation(exchange, skippedProperties, method, (items == null || i < 0 || i >= ((List<?>)items).size() ? null : ((List<?>)items).get(i)), code);
        }
        TestSharedMethods.AssertTimestampOrder(exchange, method, code, items);
        return true;
        });

    }

}