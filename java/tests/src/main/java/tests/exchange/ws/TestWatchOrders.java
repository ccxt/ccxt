package tests.exchange.ws;
import tests.BaseTest;
import io.github.ccxt.Helpers;
import io.github.ccxt.Exchange;
import io.github.ccxt.errors.*;
import tests.exchange.*;


// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code


public class TestWatchOrders extends BaseTest {
    public java.util.concurrent.CompletableFuture<Object> testWatchOrders(Exchange exchange, Object skippedProperties, Object symbol)
    {

        return java.util.concurrent.CompletableFuture.supplyAsync(() -> {

        Object method = "watchOrders";
        Object now = exchange.milliseconds();
        Object ends = Helpers.add(now, 15000);
        while (Helpers.isLessThan(now, ends))
        {
            Object response = null;
            Object success = true;
            try
            {
                response = (exchange.watchOrders(symbol)).join();
                if (Helpers.isTrue(Helpers.isEqual(response, null)))
                {
                    throw new RuntimeException((String)Helpers.add(exchange.id, " watch returned undefined response")) ;
                }
            } catch(Exception e)
            {
                if (!Helpers.isTrue(TestSharedMethods.isTemporaryFailure(e)))
                {
                    throw (e instanceof RuntimeException ? (RuntimeException)e : new RuntimeException(e));
                }
                now = exchange.milliseconds();
                // continue;
                success = false;
            }
            if (Helpers.isTrue(Helpers.isEqual(success, true)))
            {
                if (Helpers.isTrue(Helpers.isEqual(response, null)))
                {
                    throw new RuntimeException((String)Helpers.add(exchange.id, " watch returned undefined response")) ;
                }
                TestSharedMethods.AssertNonEmtpyArray(exchange, skippedProperties, method, response, symbol);
                now = exchange.milliseconds();
                for (var i = 0; Helpers.isLessThan(i, Helpers.getArrayLength(response)); i++)
                {
                    TestOrder.testOrder(exchange, skippedProperties, method, Helpers.GetValue(response, i), symbol, now);
                }
                TestSharedMethods.AssertTimestampOrder(exchange, method, symbol, response);
            }
        }
        return true;
        });

    }

}