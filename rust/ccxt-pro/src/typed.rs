// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code
//
// Aggregator: re-exports every per-exchange typed wrapper at the crate
// root so users can write `use ccxt::Binance;` instead of the full
// `use ccxt::exchanges::binance_typed::Binance;` path, and declares the
// `TypedExchange` / `TypedExchangeExt` traits — the unified interface every
// wrapper implements (each wrapper's `impl` lives in `<id>_typed.rs`).
//
// Generated by `build/generateRustWrappers.ts`.

#![allow(unused_imports, unused_variables, clippy::all)]

use crate::Value;
use crate::types::*;

/// Object-safe core of the unified interface. Every typed wrapper
/// implements it (just `call_raw`); the ergonomic typed methods live on
/// [`TypedExchangeExt`]. `Box<dyn TypedExchange>` works.
#[async_trait::async_trait]
pub trait TypedExchange: Send {
    /// Dynamic dispatch — the ONE method each wrapper implements. Routes
    /// `method(args)` to the underlying Core, surfacing its panic-based
    /// errors as `Result`.
    async fn call_raw(&mut self, method: &str, args: Vec<Value>) -> crate::Result<Value>;

    /// Loads and caches markets (untyped setup for symbol resolution).
    async fn load_markets(&mut self, reload: bool) -> Value {
        self.call_raw("load_markets", vec![Value::Bool(reload)]).await.unwrap_or(Value::Null)
    }
}

/// The typed unified methods (`fetch_ticker` → `Result<Ticker>`, …),
/// blanket-implemented for every `TypedExchange`. Bring it into scope to
/// call them: generic (`fn f<E: TypedExchange>(ex: &mut E)`), on a concrete
/// wrapper, or on `&mut dyn TypedExchange`. A method the exchange does not
/// implement resolves to a `NotSupported` error at runtime.
pub trait TypedExchangeExt: TypedExchange {
    /// Typed `watchLiquidations`.
    fn watch_liquidations<'a>(&'a mut self, symbol: &str, since: Option<i64>, limit: Option<i64>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Vec<Liquidation>>> + Send + 'a {
        let __args: Vec<Value> = vec![Value::Str(symbol.to_string()), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_liquidations", __args).await?;
            Ok(vec_from_value(&v, Liquidation::from_value))
        }
    }
    /// Typed `watchLiquidationsForSymbols`.
    fn watch_liquidations_for_symbols<'a>(&'a mut self, symbols: Vec<String>, since: Option<i64>, limit: Option<i64>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Vec<Liquidation>>> + Send + 'a {
        let __args: Vec<Value> = vec![Value::Arr(std::sync::Arc::new(symbols.into_iter().map(Value::Str).collect())), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_liquidations_for_symbols", __args).await?;
            Ok(vec_from_value(&v, Liquidation::from_value))
        }
    }
    /// Typed `watchMyLiquidations`.
    fn watch_my_liquidations<'a>(&'a mut self, symbol: &str, since: Option<i64>, limit: Option<i64>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Vec<Liquidation>>> + Send + 'a {
        let __args: Vec<Value> = vec![Value::Str(symbol.to_string()), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_my_liquidations", __args).await?;
            Ok(vec_from_value(&v, Liquidation::from_value))
        }
    }
    /// Typed `watchMyLiquidationsForSymbols`.
    fn watch_my_liquidations_for_symbols<'a>(&'a mut self, symbols: Vec<String>, since: Option<i64>, limit: Option<i64>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Vec<Liquidation>>> + Send + 'a {
        let __args: Vec<Value> = vec![Value::Arr(std::sync::Arc::new(symbols.into_iter().map(Value::Str).collect())), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_my_liquidations_for_symbols", __args).await?;
            Ok(vec_from_value(&v, Liquidation::from_value))
        }
    }
    /// Typed `watchFundingRate`.
    fn watch_funding_rate<'a>(&'a mut self, symbol: &str, params: Value) -> impl ::std::future::Future<Output = crate::Result<FundingRate>> + Send + 'a {
        let __args: Vec<Value> = vec![Value::Str(symbol.to_string()), params];
        async move {
            let v = self.call_raw("watch_funding_rate", __args).await?;
            Ok(FundingRate::from_value(v))
        }
    }
    /// Typed `watchFundingRates`.
    fn watch_funding_rates<'a>(&'a mut self, symbols: Option<Vec<String>>, params: Value) -> impl ::std::future::Future<Output = crate::Result<FundingRates>> + Send + 'a {
        let __args: Vec<Value> = vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params];
        async move {
            let v = self.call_raw("watch_funding_rates", __args).await?;
            Ok(dict_from_value(&v, FundingRate::from_value))
        }
    }
    /// Typed `watchFundingRatesForSymbols`.
    fn watch_funding_rates_for_symbols<'a>(&'a mut self, symbols: Vec<String>, params: Value) -> impl ::std::future::Future<Output = crate::Result<FundingRates>> + Send + 'a {
        let __args: Vec<Value> = vec![Value::Arr(std::sync::Arc::new(symbols.into_iter().map(Value::Str).collect())), params];
        async move {
            let v = self.call_raw("watch_funding_rates_for_symbols", __args).await?;
            Ok(dict_from_value(&v, FundingRate::from_value))
        }
    }
    /// Typed `watchBalance`.
    fn watch_balance<'a>(&'a mut self, params: Value) -> impl ::std::future::Future<Output = crate::Result<Balances>> + Send + 'a {
        let __args: Vec<Value> = vec![params];
        async move {
            let v = self.call_raw("watch_balance", __args).await?;
            Ok(Balances::from_value(v))
        }
    }
    /// Typed `watchPosition`.
    fn watch_position<'a>(&'a mut self, symbol: Option<&str>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Position>> + Send + 'a {
        let __args: Vec<Value> = vec![symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_position", __args).await?;
            Ok(Position::from_value(v))
        }
    }
    /// Typed `watchMyTradesForSymbols`.
    fn watch_my_trades_for_symbols<'a>(&'a mut self, symbols: Vec<String>, since: Option<i64>, limit: Option<i64>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Vec<Trade>>> + Send + 'a {
        let __args: Vec<Value> = vec![Value::Arr(std::sync::Arc::new(symbols.into_iter().map(Value::Str).collect())), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_my_trades_for_symbols", __args).await?;
            Ok(vec_from_value(&v, Trade::from_value))
        }
    }
    /// Typed `watchTradesForSymbols`.
    fn watch_trades_for_symbols<'a>(&'a mut self, symbols: Vec<String>, since: Option<i64>, limit: Option<i64>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Vec<Trade>>> + Send + 'a {
        let __args: Vec<Value> = vec![Value::Arr(std::sync::Arc::new(symbols.into_iter().map(Value::Str).collect())), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_trades_for_symbols", __args).await?;
            Ok(vec_from_value(&v, Trade::from_value))
        }
    }
    /// Typed `watchBidsAsks`.
    fn watch_bids_asks<'a>(&'a mut self, symbols: Option<Vec<String>>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Tickers>> + Send + 'a {
        let __args: Vec<Value> = vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params];
        async move {
            let v = self.call_raw("watch_bids_asks", __args).await?;
            Ok(dict_from_value(&v, Ticker::from_value))
        }
    }
    /// Typed `watchMarkPrice`.
    fn watch_mark_price<'a>(&'a mut self, symbol: &str, params: Value) -> impl ::std::future::Future<Output = crate::Result<Ticker>> + Send + 'a {
        let __args: Vec<Value> = vec![Value::Str(symbol.to_string()), params];
        async move {
            let v = self.call_raw("watch_mark_price", __args).await?;
            Ok(Ticker::from_value(v))
        }
    }
    /// Typed `watchMarkPrices`.
    fn watch_mark_prices<'a>(&'a mut self, symbols: Option<Vec<String>>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Tickers>> + Send + 'a {
        let __args: Vec<Value> = vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params];
        async move {
            let v = self.call_raw("watch_mark_prices", __args).await?;
            Ok(dict_from_value(&v, Ticker::from_value))
        }
    }
    /// Typed `watchOrderBookForSymbols`.
    fn watch_order_book_for_symbols<'a>(&'a mut self, symbols: Vec<String>, limit: Option<i64>, params: Value) -> impl ::std::future::Future<Output = crate::Result<OrderBook>> + Send + 'a {
        let __args: Vec<Value> = vec![Value::Arr(std::sync::Arc::new(symbols.into_iter().map(Value::Str).collect())), limit.map(Value::Int).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_order_book_for_symbols", __args).await?;
            Ok(OrderBook::from_value(v))
        }
    }
    /// Typed `watchOrdersForSymbols`.
    fn watch_orders_for_symbols<'a>(&'a mut self, symbols: Vec<String>, since: Option<i64>, limit: Option<i64>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Vec<Order>>> + Send + 'a {
        let __args: Vec<Value> = vec![Value::Arr(std::sync::Arc::new(symbols.into_iter().map(Value::Str).collect())), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_orders_for_symbols", __args).await?;
            Ok(vec_from_value(&v, Order::from_value))
        }
    }
    /// Typed `watchTrades`.
    fn watch_trades<'a>(&'a mut self, symbol: &str, since: Option<i64>, limit: Option<i64>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Vec<Trade>>> + Send + 'a {
        let __args: Vec<Value> = vec![Value::Str(symbol.to_string()), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_trades", __args).await?;
            Ok(vec_from_value(&v, Trade::from_value))
        }
    }
    /// Typed `watchOrderBook`.
    fn watch_order_book<'a>(&'a mut self, symbol: &str, limit: Option<i64>, params: Value) -> impl ::std::future::Future<Output = crate::Result<OrderBook>> + Send + 'a {
        let __args: Vec<Value> = vec![Value::Str(symbol.to_string()), limit.map(Value::Int).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_order_book", __args).await?;
            Ok(OrderBook::from_value(v))
        }
    }
    /// Typed `watchPositions`.
    fn watch_positions<'a>(&'a mut self, symbols: Option<Vec<String>>, since: Option<i64>, limit: Option<i64>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Vec<Position>>> + Send + 'a {
        let __args: Vec<Value> = vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_positions", __args).await?;
            Ok(vec_from_value(&v, Position::from_value))
        }
    }
    /// Typed `watchPositionForSymbols`.
    fn watch_position_for_symbols<'a>(&'a mut self, symbols: Option<Vec<String>>, since: Option<i64>, limit: Option<i64>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Vec<Position>>> + Send + 'a {
        let __args: Vec<Value> = vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_position_for_symbols", __args).await?;
            Ok(vec_from_value(&v, Position::from_value))
        }
    }
    /// Typed `watchTicker`.
    fn watch_ticker<'a>(&'a mut self, symbol: &str, params: Value) -> impl ::std::future::Future<Output = crate::Result<Ticker>> + Send + 'a {
        let __args: Vec<Value> = vec![Value::Str(symbol.to_string()), params];
        async move {
            let v = self.call_raw("watch_ticker", __args).await?;
            Ok(Ticker::from_value(v))
        }
    }
    /// Typed `watchTickers`.
    fn watch_tickers<'a>(&'a mut self, symbols: Option<Vec<String>>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Tickers>> + Send + 'a {
        let __args: Vec<Value> = vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params];
        async move {
            let v = self.call_raw("watch_tickers", __args).await?;
            Ok(dict_from_value(&v, Ticker::from_value))
        }
    }
    /// Typed `watchOrders`.
    fn watch_orders<'a>(&'a mut self, symbol: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Vec<Order>>> + Send + 'a {
        let __args: Vec<Value> = vec![symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_orders", __args).await?;
            Ok(vec_from_value(&v, Order::from_value))
        }
    }
    /// Typed `watchMyTrades`.
    fn watch_my_trades<'a>(&'a mut self, symbol: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> impl ::std::future::Future<Output = crate::Result<Vec<Trade>>> + Send + 'a {
        let __args: Vec<Value> = vec![symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params];
        async move {
            let v = self.call_raw("watch_my_trades", __args).await?;
            Ok(vec_from_value(&v, Trade::from_value))
        }
    }
}
impl<T: TypedExchange + ?Sized> TypedExchangeExt for T {}

pub use crate::pro_typed::alpaca_typed::Alpaca;
pub use crate::pro_typed::apex_typed::Apex;
pub use crate::pro_typed::aster_typed::Aster;
pub use crate::pro_typed::backpack_typed::Backpack;
pub use crate::pro_typed::bequant_typed::Bequant;
pub use crate::pro_typed::binance_typed::Binance;
pub use crate::pro_typed::binancecoinm_typed::Binancecoinm;
pub use crate::pro_typed::binanceus_typed::Binanceus;
pub use crate::pro_typed::binanceusdm_typed::Binanceusdm;
pub use crate::pro_typed::bingx_typed::Bingx;
pub use crate::pro_typed::bitfinex_typed::Bitfinex;
pub use crate::pro_typed::bitget_typed::Bitget;
pub use crate::pro_typed::bithumb_typed::Bithumb;
pub use crate::pro_typed::bitmex_typed::Bitmex;
pub use crate::pro_typed::bitopro_typed::Bitopro;
pub use crate::pro_typed::bitrue_typed::Bitrue;
pub use crate::pro_typed::bitstamp_typed::Bitstamp;
pub use crate::pro_typed::bittrade_typed::Bittrade;
pub use crate::pro_typed::bitvavo_typed::Bitvavo;
pub use crate::pro_typed::blockchaincom_typed::Blockchaincom;
pub use crate::pro_typed::blofin_typed::Blofin;
pub use crate::pro_typed::bullish_typed::Bullish;
pub use crate::pro_typed::bybit_typed::Bybit;
pub use crate::pro_typed::bybiteu_typed::Bybiteu;
pub use crate::pro_typed::bydfi_typed::Bydfi;
pub use crate::pro_typed::cex_typed::Cex;
pub use crate::pro_typed::coinbase_typed::Coinbase;
pub use crate::pro_typed::coinbaseexchange_typed::Coinbaseexchange;
pub use crate::pro_typed::coinbaseinternational_typed::Coinbaseinternational;
pub use crate::pro_typed::coincheck_typed::Coincheck;
pub use crate::pro_typed::coinex_typed::Coinex;
pub use crate::pro_typed::coinone_typed::Coinone;
pub use crate::pro_typed::cryptocom_typed::Cryptocom;
pub use crate::pro_typed::deepcoin_typed::Deepcoin;
pub use crate::pro_typed::deribit_typed::Deribit;
pub use crate::pro_typed::derive_typed::Derive;
pub use crate::pro_typed::dydx_typed::Dydx;
pub use crate::pro_typed::extended_typed::Extended;
pub use crate::pro_typed::gate_typed::Gate;
pub use crate::pro_typed::gateeu_typed::Gateeu;
pub use crate::pro_typed::gemini_typed::Gemini;
pub use crate::pro_typed::grvt_typed::Grvt;
pub use crate::pro_typed::hashkey_typed::Hashkey;
pub use crate::pro_typed::hitbtc_typed::Hitbtc;
pub use crate::pro_typed::hollaex_typed::Hollaex;
pub use crate::pro_typed::htx_typed::Htx;
pub use crate::pro_typed::hyperliquid_typed::Hyperliquid;
pub use crate::pro_typed::independentreserve_typed::Independentreserve;
pub use crate::pro_typed::kraken_typed::Kraken;
pub use crate::pro_typed::krakenfutures_typed::Krakenfutures;
pub use crate::pro_typed::kucoin_typed::Kucoin;
pub use crate::pro_typed::kucoinfutures_typed::Kucoinfutures;
pub use crate::pro_typed::lbank_typed::Lbank;
pub use crate::pro_typed::lighter_typed::Lighter;
pub use crate::pro_typed::luno_typed::Luno;
pub use crate::pro_typed::mexc_typed::Mexc;
pub use crate::pro_typed::modetrade_typed::Modetrade;
pub use crate::pro_typed::mudrex_typed::Mudrex;
pub use crate::pro_typed::myokx_typed::Myokx;
pub use crate::pro_typed::nado_typed::Nado;
pub use crate::pro_typed::ndax_typed::Ndax;
pub use crate::pro_typed::okx_typed::Okx;
pub use crate::pro_typed::okxus_typed::Okxus;
pub use crate::pro_typed::onetrading_typed::Onetrading;
pub use crate::pro_typed::p2b_typed::P2b;
pub use crate::pro_typed::pacifica_typed::Pacifica;
pub use crate::pro_typed::paradex_typed::Paradex;
pub use crate::pro_typed::phemex_typed::Phemex;
pub use crate::pro_typed::poloniex_typed::Poloniex;
pub use crate::pro_typed::toobit_typed::Toobit;
pub use crate::pro_typed::upbit_typed::Upbit;
pub use crate::pro_typed::weex_typed::Weex;
pub use crate::pro_typed::whitebit_typed::Whitebit;
pub use crate::pro_typed::woo_typed::Woo;
pub use crate::pro_typed::woofipro_typed::Woofipro;
pub use crate::pro_typed::xt_typed::Xt;
