// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#![allow(unused, non_snake_case, clippy::all)]
use crate::Value;
use crate::get_value;
use crate::runtime::*;
// Base methods are now trait methods (review #1: static dispatch). Bring the
// traits into scope so `self.market(...)`, `self.safe_market(...)`,
// `self.load_markets(...)`, … on this Core resolve to the base defaults.
use crate::exchange_generated::ExchangeBase;
use crate::exchange::ExchangeRuntime;
use crate::pro::*;


pub struct WhitebitCore {
    pub parent: crate::exchanges::whitebit::WhitebitCore,
}

impl WhitebitCore {
    pub fn new(config: Option<crate::Value>) -> Self {
        let mut s = Self { parent: crate::exchanges::whitebit::WhitebitCore::new(config) };
        s.init();
        s
    }

    pub fn init(&mut self) {
        let described = WhitebitCore::describe(self);
        self.initialize_properties(described);
        <Self as crate::exchange_generated::ExchangeBase>::after_construct(self);
    }

    /// Compatibility no-op. The old pointer-based dispatch needed a post-move
    /// `bind()`; static trait dispatch (review #1) needs no binding, so this
    /// just exists so callers that still call it keep compiling.
    #[inline]
    pub fn bind(&mut self) {}
}

impl crate::exchange::DerivedExchange for WhitebitCore {
    fn nonce(&self, ) -> crate::Value {
        crate::exchange::DerivedExchange::nonce(&self.parent)
    }
    fn parse_ticker(&self, ticker: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_ticker(&self.parent, ticker, market)
    }
    fn parse_trade(&self, trade: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_trade(&self.parent, trade, market)
    }
    fn parse_order(&self, order: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_order(&self.parent, order, market)
    }
    fn parse_market(&self, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_market(&self.parent, market)
    }
    fn parse_ohlcv(&self, ohlcv: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_ohlcv(&self.parent, ohlcv, market)
    }
    fn parse_order_book(&self, ob: crate::Value, symbol: crate::Value, ts: crate::Value, bk: crate::Value, ak: crate::Value, pk: crate::Value, ak2: crate::Value, ck: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_order_book(&self.parent, ob, symbol, ts, bk, ak, pk, ak2, ck)
    }
    fn parse_balance(&self, response: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_balance(&self.parent, response)
    }
    fn parse_position(&self, position: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_position(&self.parent, position, market)
    }
    fn parse_funding_rate(&self, rate: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_funding_rate(&self.parent, rate, market)
    }
    fn parse_deposit(&self, tx: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_deposit(&self.parent, tx, currency)
    }
    fn parse_deposit_address(&self, depositAddress: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_deposit_address(&self.parent, depositAddress, currency)
    }
    fn parse_last_price(&self, entry: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_last_price(&self.parent, entry, market)
    }
    fn parse_withdrawal(&self, tx: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_withdrawal(&self.parent, tx, currency)
    }
    fn parse_ledger_entry(&self, entry: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_ledger_entry(&self.parent, entry, currency)
    }
    fn parse_transfer(&self, transfer: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_transfer(&self.parent, transfer, currency)
    }
    fn parse_currency(&self, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_currency(&self.parent, currency)
    }
    fn parse_bid_ask(&self, bidask: crate::Value, price_key: crate::Value, amount_key: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_bid_ask(&self.parent, bidask, price_key, amount_key, market)
    }
    fn parse_open_interest(&self, interest: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_open_interest(&self.parent, interest, market)
    }
    fn parse_liquidation(&self, liquidation: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_liquidation(&self.parent, liquidation, market)
    }
    fn parse_funding_rate_history(&self, entry: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_funding_rate_history(&self.parent, entry, market)
    }
    fn parse_margin_modification(&self, data: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_margin_modification(&self.parent, data, market)
    }
    fn parse_account(&self, account: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_account(&self.parent, account)
    }
    fn parse_my_trade(&self, trade: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_my_trade(&self.parent, trade, market)
    }
    fn parse_transaction(&self, transaction: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_transaction(&self.parent, transaction, currency)
    }
    fn parse_borrow_interest(&self, info: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_borrow_interest(&self.parent, info, market)
    }
    fn parse_adl_rank(&self, info: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_adl_rank(&self.parent, info, market)
    }
    fn parse_income(&self, info: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_income(&self.parent, info, market)
    }
    fn parse_greeks(&self, greeks: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_greeks(&self.parent, greeks, market)
    }
    fn parse_margin_mode(&self, margin_mode: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_margin_mode(&self.parent, margin_mode, market)
    }
    fn parse_conversion(&self, conversion: crate::Value, from_currency: crate::Value, to_currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_conversion(&self.parent, conversion, from_currency, to_currency)
    }
    fn parse_borrow_rate(&self, info: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_borrow_rate(&self.parent, info, currency)
    }
    fn parse_leverage(&self, leverage: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_leverage(&self.parent, leverage, market)
    }
    fn parse_market_leverage_tiers(&self, info: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_market_leverage_tiers(&self.parent, info, market)
    }
    fn parse_deposit_withdraw_fee(&self, fee: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_deposit_withdraw_fee(&self.parent, fee, currency)
    }
    fn parse_prediction_trade(&self, trade: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_prediction_trade(&self.parent, trade, market)
    }
    fn parse_prediction_order(&self, order: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_prediction_order(&self.parent, order, market)
    }
    fn parse_prediction_position(&self, position: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_prediction_position(&self.parent, position, market)
    }
    fn create_expired_option_market(&self, symbol: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::create_expired_option_market(&self.parent, symbol)
    }
    fn sign(&self, path: crate::Value, api: crate::Value, method: crate::Value, params: crate::Value, headers: crate::Value, body: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::sign(&self.parent, path, api, method, params, headers, body)
    }
    fn handle_errors(&self, code: crate::Value, reason: crate::Value, url: crate::Value, method: crate::Value, headers: crate::Value, body: crate::Value, response: crate::Value, request_headers: crate::Value, request_body: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::handle_errors(&self.parent, code, reason, url, method, headers, body, response, request_headers, request_body)
    }
}

impl crate::exchange_generated::ExchangeBase for WhitebitCore {
    fn call_dynamic<'a>(&'a mut self, method: &'a str, args: Vec<crate::Value>)
        -> std::pin::Pin<Box<dyn std::future::Future<Output = crate::Value> + Send + 'a>>
    {
        Box::pin(async move {
            match method {
                "authenticate" => self.authenticate(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "handle_authenticate" => self.handle_authenticate(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
                "handle_error_message" => self.handle_error_message(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
                "handle_message" => { self.handle_message(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)); crate::Value::Null },
                "handle_ohlcv" => self.handle_ohlcv(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
                "handle_pong" => self.handle_pong(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
                "handle_ticker" => self.handle_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
                "load_balance_snapshot" => self.load_balance_snapshot(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), args.get(3).cloned().unwrap_or(crate::Value::Null)).await,
                "parse_ws_order" => self.parse_ws_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
                "parse_ws_order_type" => self.parse_ws_order_type(args.get(0).cloned().unwrap_or(crate::Value::Null)),
                "parse_ws_trade" => self.parse_ws_trade(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
                "ping" => self.ping(args.get(0).cloned().unwrap_or(crate::Value::Null)),
                "watch_balance" => self.watch_balance(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "watch_multiple_subscription" => self.watch_multiple_subscription(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), &args.get(3..).unwrap_or(&[]).to_vec()[..]).await,
                "watch_my_trades" => self.watch_my_trades(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "watch_ohlcv" => self.watch_ohlcv(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
                "watch_order_book" => self.watch_order_book(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
                "watch_orders" => self.watch_orders(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "watch_private" => self.watch_private(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), &args.get(2..).unwrap_or(&[]).to_vec()[..]).await,
                "watch_public" => self.watch_public(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), &args.get(2..).unwrap_or(&[]).to_vec()[..]).await,
                "watch_ticker" => self.watch_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
                "watch_tickers" => self.watch_tickers(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "watch_trades" => self.watch_trades(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
                // Go-style inheritance: an un-overridden method dispatches to the parent core.
                _ => crate::exchange_generated::ExchangeBase::call_dynamic(&mut self.parent, method, args).await,
            }
        })
    }
}
impl WhitebitCore {
    /// Synchronous WS handler dispatch — routes a handler-name string (from the
    /// venue's handle_message dispatch table) to the real handler method.
    #[allow(dead_code, unreachable_patterns, clippy::all)]
    pub fn dispatch_ws_handler(&mut self, __name: &crate::Value, args: &[crate::Value]) -> crate::Value {
        let __n = match __name { crate::Value::Str(s) => s.as_str(), _ => return crate::Value::Null };
        match __n {
            "authenticate" => { crate::exchange_stubs::enqueue_spawn("authenticate", args.to_vec()); crate::Value::Null },
            "handle_authenticate" => self.handle_authenticate(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
            "handle_balance" => { self.handle_balance(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)); crate::Value::Null },
            "handle_delta" => { self.handle_delta(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)); crate::Value::Null },
            "handle_deltas" => { self.handle_deltas(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)); crate::Value::Null },
            "handle_error_message" => self.handle_error_message(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
            "handle_message" => { self.handle_message(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)); crate::Value::Null },
            "handle_my_trades" => { self.handle_my_trades(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), &args.get(2..).unwrap_or(&[]).to_vec()[..]); crate::Value::Null },
            "handle_ohlcv" => self.handle_ohlcv(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
            "handle_order" => { self.handle_order(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), &args.get(2..).unwrap_or(&[]).to_vec()[..]); crate::Value::Null },
            "handle_order_book" => { self.handle_order_book(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)); crate::Value::Null },
            "handle_pong" => self.handle_pong(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
            "handle_subscription_status" => { self.handle_subscription_status(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null)); crate::Value::Null },
            "handle_ticker" => self.handle_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
            "handle_trades" => { self.handle_trades(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)); crate::Value::Null },
            "load_balance_snapshot" => { crate::exchange_stubs::enqueue_spawn("load_balance_snapshot", args.to_vec()); crate::Value::Null },
            "parse_ws_order" => self.parse_ws_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "parse_ws_order_type" => self.parse_ws_order_type(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "parse_ws_trade" => self.parse_ws_trade(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "ping" => self.ping(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "set_balance_cache" => { self.set_balance_cache(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null)); crate::Value::Null },
            "watch_balance" => { crate::exchange_stubs::enqueue_spawn("watch_balance", args.to_vec()); crate::Value::Null },
            "watch_multiple_subscription" => { crate::exchange_stubs::enqueue_spawn("watch_multiple_subscription", args.to_vec()); crate::Value::Null },
            "watch_my_trades" => { crate::exchange_stubs::enqueue_spawn("watch_my_trades", args.to_vec()); crate::Value::Null },
            "watch_ohlcv" => { crate::exchange_stubs::enqueue_spawn("watch_ohlcv", args.to_vec()); crate::Value::Null },
            "watch_order_book" => { crate::exchange_stubs::enqueue_spawn("watch_order_book", args.to_vec()); crate::Value::Null },
            "watch_orders" => { crate::exchange_stubs::enqueue_spawn("watch_orders", args.to_vec()); crate::Value::Null },
            "watch_private" => { crate::exchange_stubs::enqueue_spawn("watch_private", args.to_vec()); crate::Value::Null },
            "watch_public" => { crate::exchange_stubs::enqueue_spawn("watch_public", args.to_vec()); crate::Value::Null },
            "watch_ticker" => { crate::exchange_stubs::enqueue_spawn("watch_ticker", args.to_vec()); crate::Value::Null },
            "watch_tickers" => { crate::exchange_stubs::enqueue_spawn("watch_tickers", args.to_vec()); crate::Value::Null },
            "watch_trades" => { crate::exchange_stubs::enqueue_spawn("watch_trades", args.to_vec()); crate::Value::Null },
            _ => crate::Value::Null,
        }
    }
}

impl std::ops::Deref for WhitebitCore {
    type Target = crate::exchange::Exchange;
    fn deref(&self) -> &crate::exchange::Exchange { std::ops::Deref::deref(&self.parent) }
}

impl std::ops::DerefMut for WhitebitCore {
    fn deref_mut(&mut self) -> &mut crate::exchange::Exchange { std::ops::DerefMut::deref_mut(&mut self.parent) }
}

impl WhitebitCore {
    pub fn describe(&self) -> Value {
        return self.deep_extend(self.parent.describe(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("has".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("ws".to_string(), Value::Bool(true));
        m.insert("watchBalance".to_string(), Value::Bool(true));
        m.insert("watchMyTrades".to_string(), Value::Bool(true));
        m.insert("watchOHLCV".to_string(), Value::Bool(true));
        m.insert("watchOrderBook".to_string(), Value::Bool(true));
        m.insert("watchOrders".to_string(), Value::Bool(true));
        m.insert("watchTicker".to_string(), Value::Bool(true));
        m.insert("watchTickers".to_string(), Value::Bool(true));
        m.insert("watchTrades".to_string(), Value::Bool(true));
        m.insert("watchTradesForSymbols".to_string(), Value::Bool(false));
    m
}));
        m.insert("urls".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("api".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("ws".to_string(), Value::Str("wss://api.whitebit.com/ws".to_string()));
    m
}));
    m
}));
        m.insert("options".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("timeframes".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("1m".to_string(), Value::Str("60".to_string()));
        m.insert("5m".to_string(), Value::Str("300".to_string()));
        m.insert("15m".to_string(), Value::Str("900".to_string()));
        m.insert("30m".to_string(), Value::Str("1800".to_string()));
        m.insert("1h".to_string(), Value::Str("3600".to_string()));
        m.insert("4h".to_string(), Value::Str("14400".to_string()));
        m.insert("8h".to_string(), Value::Str("28800".to_string()));
        m.insert("1d".to_string(), Value::Str("86400".to_string()));
        m.insert("1w".to_string(), Value::Str("604800".to_string()));
    m
}));
        m.insert("watchOrderBook".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("priceInterval".to_string(), Value::Int(0));
    m
}));
    m
}));
        m.insert("streaming".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("ping".to_string(), Value::Str("ping".to_string()).clone());
    m
}));
        m.insert("exceptions".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("ws".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("exact".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("1".to_string(), Value::Str("BadRequest".to_string()).clone());
        m.insert("2".to_string(), Value::Str("BadRequest".to_string()).clone());
        m.insert("4".to_string(), Value::Str("BadRequest".to_string()).clone());
        m.insert("6".to_string(), Value::Str("AuthenticationError".to_string()).clone());
    m
}));
    m
}));
    m
}));
    m
})]);

    Value::Null
}

/*
 * @method
 * @name whitebit#watchOHLCV
 * @description watches historical candlestick data containing the open, high, low, and close price, and the volume of a market
 * @see https://docs.whitebit.com/public/websocket/#kline
 * @param {string} symbol unified symbol of the market to fetch OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum amount of candles to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 */
    pub async fn watch_ohlcv(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut timeframe = get_arg(optional_args, 0, Value::Str("1m".to_string()));
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol.clone());
        symbol = get_value(&market, &Value::Str("symbol".to_string()));
        let mut timeframes: Value = self.safe_value_k(self.options.clone(), "timeframes", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut interval: Value = self.safe_integer(timeframes.clone(), timeframe.clone(), &[]);
        let mut marketId: Value = get_value(&market, &Value::Str("id".to_string()));
        // currently there is no way of knowing
        // the interval upon getting an update
        // so that can't be part of the message hash, and the user can only subscribe
        // to one timeframe per symbol
        let mut messageHash: Value = add(&Value::Str("candles:".to_string()), &symbol);
        let mut reqParams: Value = Value::List(vec![marketId.clone(), interval.clone()]);
        let mut method: Value = Value::Str("candles_subscribe".to_string());
        let mut ohlcv: Value = self.watch_public(messageHash.clone(), method.clone(), &[reqParams.clone(), params.clone()]).await;
        if is_true(&self.newUpdates) {
            limit = ohlcv.get_limit(symbol.clone(), limit.clone());
        }
        return self.filter_by_since_limit(ohlcv.clone(), &[since.clone(), limit.clone(), Value::Int(0), Value::Bool(true)]);

    Value::Null
}

    pub fn handle_ohlcv(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // {
        //     "method": "candles_update",
        //     "params": [
        //       [
        //         1655204760,
        //         "22374.15",
        //         "22351.34",
        //         "22374.27",
        //         "22342.52",
        //         "30.213426",
        //         "675499.29718947",
        //         "BTC_USDT"
        //       ]
        //     ],
        //     "id": null
        // }
        //
        let mut params: Value = self.safe_value_k(message.clone(), "params", &[Value::List(vec![])]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_649: bool = true;
            while { if !__for_first_649 { i = add(&i, &Value::Int(1)); } __for_first_649 = false; is_less_than(&i, &get_array_length(&params)) } {
            let mut data: Value = get_value(&params, &i);
            let mut data: Value = get_value(&params, &i);
            let mut marketId: Value = self.safe_string(data.clone(), Value::Int(7), &[]);
            let mut market: Value = self.safe_market(&[marketId.clone()]);
            let mut symbol: Value = get_value(&market, &Value::Str("symbol".to_string()));
            let mut messageHash: Value = add(&add(&Value::Str("candles".to_string()), &Value::Str(":".to_string())), &symbol);
            let mut parsed: Value = self.parse_ohlcv(data.clone(), &[market.clone()]);
            // this.ohlcvs[symbol] = this.safeValue (this.ohlcvs, symbol);
            if !is_true(&(Value::Bool(in_op(&self.ohlcvs, &symbol)))) {
                add_element_to_object(&mut self.ohlcvs, &symbol, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
            }
            // let stored = this.ohlcvs[symbol]['unknown']; // we don't know the timeframe but we need to respect the type
            if !is_true(&(Value::Bool(in_op(&get_value(&self.ohlcvs, &symbol), &Value::Str("unknown".to_string()))))) {
                let mut limit: Value = self.safe_integer_k(self.options.clone(), "OHLCVLimit", &[Value::Int(1000)]);
                let mut stored = ArrayCacheByTimestamp::new(limit.clone());
                add_element_to_object(get_value_mut(unsafe { crate::runtime::coerce_value_to_mut(&self.ohlcvs) }, &symbol), &Value::Str("unknown".to_string()), stored.clone());
            }
            let mut ohlcv: Value = get_value(&get_value(&self.ohlcvs, &symbol), &Value::Str("unknown".to_string()));
            ohlcv.append(parsed.clone());
            client.resolve(&[ohlcv.clone(), messageHash.clone()]);
        }
        }
        return message;

    Value::Null
}

/*
 * @method
 * @name whitebit#watchOrderBook
 * @description watches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
 * @see https://docs.whitebit.com/public/websocket/#market-depth
 * @param {string} symbol unified symbol of the market to fetch the order book for
 * @param {int} [limit] the maximum amount of order book entries to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order book structure]{@link https://docs.ccxt.com/?id=order-book-structure}
 */
    pub async fn watch_order_book(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut limit = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol.clone());
        if is_equal(&limit, &Value::Null) {
            limit = Value::Int(10); // max 100
        }
        let mut messageHash: Value = add(&add(&Value::Str("orderbook".to_string()), &Value::Str(":".to_string())), &get_value(&market, &Value::Str("symbol".to_string())));
        let mut method: Value = Value::Str("depth_subscribe".to_string());
        let mut options: Value = self.safe_value_k(self.options.clone(), "watchOrderBook", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut defaultPriceInterval: Value = self.safe_string_k(options.clone(), "priceInterval", &[Value::Str("0".to_string())]);
        let mut priceInterval: Value = self.safe_string_k(params.clone(), "priceInterval", &[defaultPriceInterval.clone()]);
        params = self.omit(params.clone(), Value::Str("priceInterval".to_string()), &[]);
        let mut reqParams: Value = Value::List(vec![get_value(&market, &Value::Str("id".to_string())), limit.clone(), priceInterval.clone(), Value::Bool(true)]);
        let mut orderbook: Value = self.watch_public(messageHash.clone(), method.clone(), &[reqParams.clone(), params.clone()]).await;
        return orderbook.limit();

    Value::Null
}

    pub fn handle_order_book(&mut self, mut client: Value, mut message: Value) {
        //
        // {
        //     "method":"depth_update",
        //     "params":[
        //        true,
        //        {
        //           "timestamp": 1708679568.940867,
        //           "asks":[
        //              [ "21252.45","0.01957"],
        //              ["21252.55","0.126205"],
        //              ["21252.66","0.222689"],
        //              ["21252.76","0.185358"],
        //              ["21252.87","0.210077"],
        //              ["21252.98","0.303991"],
        //              ["21253.08","0.327909"],
        //              ["21253.19","0.399007"],
        //              ["21253.3","0.427695"],
        //              ["21253.4","0.492901"]
        //           ],
        //           "bids":[
        //              ["21248.82","0.22"],
        //              ["21248.73","0.000467"],
        //              ["21248.62","0.100864"],
        //              ["21248.51","0.061436"],
        //              ["21248.42","0.091"],
        //              ["21248.41","0.126839"],
        //              ["21248.3","0.063511"],
        //              ["21248.2","0.110547"],
        //              ["21248","0.25257"],
        //              ["21247.7","1.71813"]
        //           ]
        //        },
        //        "BTC_USDT"
        //     ],
        //     "id":null
        //  }
        //
        let mut params: Value = self.safe_value_k(message.clone(), "params", &[Value::List(vec![])]);
        let mut isSnapshot: Value = self.safe_value(params.clone(), Value::Int(0), &[]);
        let mut marketId: Value = self.safe_string(params.clone(), Value::Int(2), &[]);
        let mut market: Value = self.safe_market(&[marketId.clone()]);
        let mut symbol: Value = get_value(&market, &Value::Str("symbol".to_string()));
        let mut data: Value = self.safe_value(params.clone(), Value::Int(1), &[]);
        let mut timestamp: Value = self.safe_timestamp(data.clone(), Value::Str("timestamp".to_string()), &[]);
        if !is_true(&(Value::Bool(in_op(&self.orderbooks, &symbol)))) {
            let mut ob: Value = self.order_book(&[]);
            add_element_to_object(&mut self.orderbooks, &symbol, ob.clone());
        }
        let mut orderbook: Value = get_value(&self.orderbooks, &symbol);
        add_element_to_object(&mut orderbook, &Value::Str("timestamp".to_string()), timestamp.clone());
        add_element_to_object(&mut orderbook, &Value::Str("datetime".to_string()), self.iso8601(timestamp.clone()));
        if is_equal(&isSnapshot, &Value::Bool(true)) {
            let mut snapshot: Value = self.parse_order_book(data.clone(), symbol.clone(), &[]);
            orderbook.reset(snapshot.clone());
        }  else {
            let mut asks: Value = self.safe_value_k(data.clone(), "asks", &[Value::List(vec![])]);
            let mut bids: Value = self.safe_value_k(data.clone(), "bids", &[Value::List(vec![])]);
            self.handle_deltas(get_value(&orderbook, &Value::Str("asks".to_string())), asks.clone());
            self.handle_deltas(get_value(&orderbook, &Value::Str("bids".to_string())), bids.clone());
        }
        let mut messageHash: Value = add(&add(&Value::Str("orderbook".to_string()), &Value::Str(":".to_string())), &symbol);
        client.resolve(&[orderbook.clone(), messageHash.clone()]);
}

    pub fn handle_delta(&self, mut bookside: Value, mut delta: Value) {
        let mut price: Value = self.safe_float(delta.clone(), Value::Int(0), &[]);
        let mut amount: Value = self.safe_float(delta.clone(), Value::Int(1), &[]);
        bookside.store(price.clone(), amount.clone());
}

    pub fn handle_deltas(&self, mut bookside: Value, mut deltas: Value) {
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_650: bool = true;
            while { if !__for_first_650 { i = add(&i, &Value::Int(1)); } __for_first_650 = false; is_less_than(&i, &get_array_length(&deltas)) } {
            self.handle_delta(bookside.clone(), get_value(&deltas, &i));
        }
        }
}

/*
 * @method
 * @name whitebit#watchTicker
 * @description watches a price ticker, a statistical calculation with the information calculated over the past 24 hours for a specific market
 * @see https://docs.whitebit.com/public/websocket/#market-statistics
 * @param {string} symbol unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
    pub async fn watch_ticker(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol.clone());
        symbol = get_value(&market, &Value::Str("symbol".to_string()));
        let mut method: Value = Value::Str("market_subscribe".to_string());
        let mut messageHash: Value = add(&Value::Str("ticker:".to_string()), &symbol);
        return self.watch_multiple_subscription(messageHash.clone(), method.clone(), symbol.clone(), &[Value::Bool(false), params.clone()]).await;

    Value::Null
}

/*
 * @method
 * @name whitebit#watchTickers
 * @description watches a price ticker, a statistical calculation with the information calculated over the past 24 hours for all markets of a specific list
 * @see https://docs.whitebit.com/public/websocket/#market-statistics
 * @param {string[]} [symbols] unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
    pub async fn watch_tickers(&mut self, optional_args: &[Value]) -> Value {
        let mut symbols = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        symbols = self.market_symbols(&[symbols.clone(), Value::Null, Value::Bool(false)]);
        let mut method: Value = Value::Str("market_subscribe".to_string());
        let mut url: Value = get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string()));
        let mut id: Value = self.nonce();
        let mut messageHashes: Value = Value::List(vec![]);
        let mut args: Value = Value::List(vec![]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_651: bool = true;
            while { if !__for_first_651 { i = add(&i, &Value::Int(1)); } __for_first_651 = false; is_less_than(&i, &get_array_length(&symbols)) } {
            let mut market: Value = self.market(get_value(&symbols, &i));
            append_to_array(&mut messageHashes, add(&Value::Str("ticker:".to_string()), &get_value(&market, &Value::Str("symbol".to_string()))));
            append_to_array(&mut args, get_value(&market, &Value::Str("id".to_string())));
        }
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("id".to_string(), id.clone());
                m.insert("method".to_string(), method.clone());
                m.insert("params".to_string(), args.clone());
            m
        });
        let __ws_arg_0 = self.extend(request.clone(), &[params.clone()]);
        self.watch_multiple(url.clone(), messageHashes.clone(), &[__ws_arg_0, messageHashes.clone()]).await;
        return self.filter_by_array(self.tickers.clone(), Value::Str("symbol".to_string()), &[symbols.clone()]);

    Value::Null
}

    pub fn handle_ticker(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //   {
        //       "method": "market_update",
        //       "params": [
        //         "BTC_USDT",
        //         {
        //           "close": "22293.86",
        //           "deal": "1986990019.96552952",
        //           "high": "24360.7",
        //           "last": "22293.86",
        //           "low": "20851.44",
        //           "open": "24076.12",
        //           "period": 86400,
        //           "volume": "87016.995668"
        //         }
        //       ],
        //       "id": null
        //   }
        //
        let mut tickers: Value = self.safe_value_k(message.clone(), "params", &[Value::List(vec![])]);
        let mut marketId: Value = self.safe_string(tickers.clone(), Value::Int(0), &[]);
        let mut market: Value = self.safe_market(&[marketId.clone()]);
        let mut symbol: Value = get_value(&market, &Value::Str("symbol".to_string()));
        let mut rawTicker: Value = self.safe_value(tickers.clone(), Value::Int(1), &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut messageHash: Value = add(&add(&Value::Str("ticker".to_string()), &Value::Str(":".to_string())), &symbol);
        let mut ticker: Value = self.parse_ticker(rawTicker.clone(), &[market.clone()]);
        add_element_to_object(&mut self.tickers, &symbol, ticker.clone());
        // watchTicker
        client.resolve(&[ticker.clone(), messageHash.clone()]);
        // watchTickers
        let mut messageHashes: Value = object_keys(&get_value(&client, &Value::Str("futures".to_string())));
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_652: bool = true;
            while { if !__for_first_652 { i = add(&i, &Value::Int(1)); } __for_first_652 = false; is_less_than(&i, &get_array_length(&messageHashes)) } {
            let mut currentMessageHash: Value = get_value(&messageHashes, &i);
            let mut currentMessageHash: Value = get_value(&messageHashes, &i);
            if is_greater_than_or_equal(&get_index_of(&currentMessageHash, &Value::Str("tickers".to_string())), &Value::Int(0)) && is_greater_than_or_equal(&get_index_of(&currentMessageHash, &symbol), &Value::Int(0)) {
                // Example: user calls watchTickers with ['LTC/USDT', 'ETH/USDT']
                // the associated messagehash will be: 'tickers:LTC/USDT:ETH/USDT'
                // since we only have access to a single symbol at a time
                // we have to do a reverse lookup into the tickers hashes
                // and check if the current symbol is a part of one or more
                // tickers hashes and resolve them
                // user might have multiple watchTickers promises
                // watchTickers ( ['LTC/USDT', 'ETH/USDT'] ), watchTickers ( ['ETC/USDT', 'DOGE/USDT'] )
                // and we want to make sure we resolve only the correct ones
                client.resolve(&[ticker.clone(), currentMessageHash.clone()]);
            }
        }
        }
        return message;

    Value::Null
}

/*
 * @method
 * @name whitebit#watchTrades
 * @description get the list of most recent trades for a particular symbol
 * @see https://docs.whitebit.com/public/websocket/#market-trades
 * @param {string} symbol unified symbol of the market to fetch trades for
 * @param {int} [since] timestamp in ms of the earliest trade to fetch
 * @param {int} [limit] the maximum amount of trades to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=public-trades}
 */
    pub async fn watch_trades(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut since = get_arg(optional_args, 0, Value::Null);
        let mut limit = get_arg(optional_args, 1, Value::Null);
        let mut params = get_arg(optional_args, 2, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol.clone());
        symbol = get_value(&market, &Value::Str("symbol".to_string()));
        let mut messageHash: Value = add(&add(&Value::Str("trades".to_string()), &Value::Str(":".to_string())), &symbol);
        let mut method: Value = Value::Str("trades_subscribe".to_string());
        // every time we want to subscribe to another market we have to 're-subscribe' sending it all again
        let mut trades: Value = self.watch_multiple_subscription(messageHash.clone(), method.clone(), symbol.clone(), &[Value::Bool(false), params.clone()]).await;
        if is_true(&self.newUpdates) {
            limit = trades.get_limit(symbol.clone(), limit.clone());
        }
        return self.filter_by_since_limit(trades.clone(), &[since.clone(), limit.clone(), Value::Str("timestamp".to_string()), Value::Bool(true)]);

    Value::Null
}

    pub fn handle_trades(&mut self, mut client: Value, mut message: Value) {
        //
        //    {
        //        "method":"trades_update",
        //        "params":[
        //           "BTC_USDT",
        //           [
        //              {
        //                 "id":1900632398,
        //                 "time":1656320231.404343,
        //                 "price":"21443.04",
        //                 "amount":"0.072844",
        //                 "type":"buy"
        //              },
        //              {
        //                 "id":1900632397,
        //                 "time":1656320231.400001,
        //                 "price":"21443.15",
        //                 "amount":"0.060757",
        //                 "type":"buy"
        //              }
        //           ]
        //        ]
        //    }
        //
        let mut params: Value = self.safe_value_k(message.clone(), "params", &[Value::List(vec![])]);
        let mut marketId: Value = self.safe_string(params.clone(), Value::Int(0), &[]);
        let mut market: Value = self.safe_market(&[marketId.clone()]);
        let mut symbol: Value = get_value(&market, &Value::Str("symbol".to_string()));
        let mut stored: Value = self.safe_value(self.trades.clone(), symbol.clone(), &[]);
        if is_equal(&stored, &Value::Null) {
            let mut limit: Value = self.safe_integer_k(self.options.clone(), "tradesLimit", &[Value::Int(1000)]);
            stored = ArrayCache::new(limit.clone());
            add_element_to_object(&mut self.trades, &symbol, stored.clone());
        }
        let mut data: Value = self.safe_value(params.clone(), Value::Int(1), &[Value::List(vec![])]);
        let mut parsedTrades: Value = self.parse_trades(data.clone(), &[market.clone()]);
        {
                        let mut j: Value = Value::Int(0);
            let mut __for_first_653: bool = true;
            while { if !__for_first_653 { j = add(&j, &Value::Int(1)); } __for_first_653 = false; is_less_than(&j, &get_array_length(&parsedTrades)) } {
            stored.append(get_value(&parsedTrades, &j));
        }
        }
        let mut messageHash: Value = add(&Value::Str("trades:".to_string()), &get_value(&market, &Value::Str("symbol".to_string())));
        client.resolve(&[stored.clone(), messageHash.clone()]);
}

/*
 * @method
 * @name whitebit#watchMyTrades
 * @description watches trades made by the user
 * @see https://docs.whitebit.com/private/websocket/#deals
 * @param {str} symbol unified market symbol
 * @param {int} [since] the earliest time in ms to fetch trades for
 * @param {int} [limit] the maximum number of trades structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=trade-structure}
 */
    pub async fn watch_my_trades(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&symbol, &Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(add(&self.id, &Value::Str(" watchMyTrades() requires a symbol argument".to_string()))));
        }
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        self.authenticate(&[]).await;
        let mut market: Value = self.market(symbol.clone());
        symbol = get_value(&market, &Value::Str("symbol".to_string()));
        let mut messageHash: Value = add(&Value::Str("myTrades:".to_string()), &symbol);
        let mut method: Value = Value::Str("deals_subscribe".to_string());
        let mut trades: Value = self.watch_multiple_subscription(messageHash.clone(), method.clone(), symbol.clone(), &[Value::Bool(true), params.clone()]).await;
        if is_true(&self.newUpdates) {
            limit = trades.get_limit(symbol.clone(), limit.clone());
        }
        return self.filter_by_symbol_since_limit(trades.clone(), &[symbol.clone(), since.clone(), limit.clone(), Value::Bool(true)]);

    Value::Null
}

    pub fn handle_my_trades(&mut self, mut client: Value, mut message: Value, optional_args: &[Value]) {
        let mut subscription = get_arg(optional_args, 0, Value::Null);
        //
        //   {
        //       "method": "deals_update",
        //       "params": [
        //         1894994106,
        //         1656151427.729706,
        //         "LTC_USDT",
        //         96624037337,
        //         "56.78",
        //         "0.16717",
        //         "0.0094919126",
        //         '',
        //         "2",
        //         "2",
        //         "LTC"
        //       ],
        //       "id": null
        //   }
        //
        let mut trade: Value = self.safe_value_k(message.clone(), "params", &[]);
        if is_equal(&self.myTrades, &Value::Null) {
            let mut limit: Value = self.safe_integer_k(self.options.clone(), "tradesLimit", &[Value::Int(1000)]);
            self.myTrades = ArrayCache::new(limit.clone());
        }
        let mut stored: Value = self.myTrades.clone();
        let mut parsed: Value = self.parse_ws_trade(trade.clone(), &[]);
        stored.append(parsed.clone());
        let mut symbol: Value = get_value(&parsed, &Value::Str("symbol".to_string()));
        let mut messageHash: Value = add(&Value::Str("myTrades:".to_string()), &symbol);
        client.resolve(&[stored.clone(), messageHash.clone()]);
}

    pub fn parse_ws_trade(&self, mut trade: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        //   [
        //         1894994106, // id
        //         1656151427.729706, // deal time
        //         "LTC_USDT", // symbol
        //         96624037337, // order id
        //         "56.78", // price
        //         "0.16717", // amount
        //         "0.0094919126", // fee
        //         '', // client order id
        //         "2", // side, 1 = sell, 2 = buy
        //         "2", // role, 1 = maker, 2 = taker
        //         "LTC" // fee asset
        //    ]
        //
        let mut orderId: Value = self.safe_string(trade.clone(), Value::Int(3), &[]);
        let mut timestamp: Value = self.safe_timestamp(trade.clone(), Value::Int(1), &[]);
        let mut id: Value = self.safe_string(trade.clone(), Value::Int(0), &[]);
        let mut price: Value = self.safe_string(trade.clone(), Value::Int(4), &[]);
        let mut amount: Value = self.safe_string(trade.clone(), Value::Int(5), &[]);
        let mut marketId: Value = self.safe_string(trade.clone(), Value::Int(2), &[]);
        market = self.safe_market(&[marketId.clone(), market.clone()]);
        let mut fee: Value = Value::Null;
        let mut feeCost: Value = self.safe_string(trade.clone(), Value::Int(6), &[]);
        if !is_equal(&feeCost, &Value::Null) {
            let mut feeCurrencyId: Value = self.safe_string(trade.clone(), Value::Int(10), &[]);
            let mut feeCurrencyCode: Value = ternary(is_true(&(!is_equal(&feeCurrencyId, &Value::Null))), self.safe_currency_code(feeCurrencyId.clone(), &[]), get_value(&market, &Value::Str("quote".to_string())));
            fee = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("cost".to_string(), feeCost.clone());
                    m.insert("currency".to_string(), feeCurrencyCode.clone());
                m
            });
        }
        let mut rawSide: Value = self.safe_integer(trade.clone(), Value::Int(8), &[]);
        let mut side: Value = Value::Null;
        if is_equal(&rawSide, &Value::Int(1)) {
            side = Value::Str("sell".to_string());
        }  else if is_equal(&rawSide, &Value::Int(2)) {
            side = Value::Str("buy".to_string());
        }
        let mut role: Value = self.safe_integer(trade.clone(), Value::Int(9), &[]);
        let mut takerOrMaker: Value = Value::Null;
        if is_equal(&role, &Value::Int(1)) {
            takerOrMaker = Value::Str("maker".to_string());
        }  else if is_equal(&role, &Value::Int(2)) {
            takerOrMaker = Value::Str("taker".to_string());
        }
        return self.safe_trade(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), id.clone());
        m.insert("info".to_string(), trade.clone());
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp.clone()));
        m.insert("symbol".to_string(), get_value(&market, &Value::Str("symbol".to_string())));
        m.insert("order".to_string(), orderId.clone());
        m.insert("type".to_string(), Value::Null);
        m.insert("side".to_string(), side.clone());
        m.insert("takerOrMaker".to_string(), takerOrMaker.clone());
        m.insert("price".to_string(), price.clone());
        m.insert("amount".to_string(), amount.clone());
        m.insert("cost".to_string(), Value::Null);
        m.insert("fee".to_string(), fee.clone());
    m
}), &[market.clone()]);

    Value::Null
}

/*
 * @method
 * @name whitebit#watchOrders
 * @description watches information on multiple orders made by the user
 * @see https://docs.whitebit.com/private/websocket/#orders-pending
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn watch_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&symbol, &Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(add(&self.id, &Value::Str(" watchOrders() requires a symbol argument".to_string()))));
        }
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        self.authenticate(&[]).await;
        let mut market: Value = self.market(symbol.clone());
        symbol = get_value(&market, &Value::Str("symbol".to_string()));
        let mut messageHash: Value = add(&Value::Str("orders:".to_string()), &symbol);
        let mut method: Value = Value::Str("ordersPending_subscribe".to_string());
        let mut trades: Value = self.watch_multiple_subscription(messageHash.clone(), method.clone(), symbol.clone(), &[Value::Bool(false), params.clone()]).await;
        if is_true(&self.newUpdates) {
            limit = trades.get_limit(symbol.clone(), limit.clone());
        }
        return self.filter_by_symbol_since_limit(trades.clone(), &[symbol.clone(), since.clone(), limit.clone(), Value::Bool(true)]);

    Value::Null
}

    pub fn handle_order(&mut self, mut client: Value, mut message: Value, optional_args: &[Value]) {
        let mut subscription = get_arg(optional_args, 0, Value::Null);
        //
        // {
        //     "method": "ordersPending_update",
        //     "params": [
        //       1, // 1 = new, 2 = update 3 = cancel or execute
        //       {
        //         "id": 96433622651,
        //         "market": "LTC_USDT",
        //         "type": 1,
        //         "side": 2,
        //         "ctime": 1656092215.39375,
        //         "mtime": 1656092215.39375,
        //         "price": "25",
        //         "amount": "0.202",
        //         "taker_fee": "0.001",
        //         "maker_fee": "0.001",
        //         "left": "0.202",
        //         "deal_stock": "0",
        //         "deal_money": "0",
        //         "deal_fee": "0",
        //         "client_order_id": ''
        //       }
        //     ]
        //     "id": null
        // }
        //
        let mut params: Value = self.safe_value_k(message.clone(), "params", &[Value::List(vec![])]);
        let mut data: Value = self.safe_value(params.clone(), Value::Int(1), &[]);
        if is_equal(&self.orders, &Value::Null) {
            let mut limit: Value = self.safe_integer_k(self.options.clone(), "ordersLimit", &[Value::Int(1000)]);
            self.orders = ArrayCacheBySymbolById::new(limit.clone());
        }
        let mut stored: Value = self.orders.clone();
        let mut status: Value = self.safe_integer(params.clone(), Value::Int(0), &[]);
        let __ws_arg_1 = self.extend(data.clone(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("status".to_string(), status.clone());
    m
})]);
        let mut parsed: Value = self.parse_ws_order(__ws_arg_1, &[]);
        stored.append(parsed.clone());
        let mut symbol: Value = get_value(&parsed, &Value::Str("symbol".to_string()));
        let mut messageHash: Value = add(&Value::Str("orders:".to_string()), &symbol);
        client.resolve(&[self.orders.clone(), messageHash.clone()]);
}

    pub fn parse_ws_order(&self, mut order: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        //   {
        //         "id": 96433622651,
        //         "market": "LTC_USDT",
        //         "type": 1,
        //         "side": 2, //1- sell 2-buy
        //         "ctime": 1656092215.39375,
        //         "mtime": 1656092215.39375,
        //         "price": "25",
        //         "amount": "0.202",
        //         "taker_fee": "0.001",
        //         "maker_fee": "0.001",
        //         "left": "0.202",
        //         "deal_stock": "0",
        //         "deal_money": "0",
        //         "deal_fee": "0",
        //         "activation_price": "40",
        //         "activation_condition": "lte",
        //         "client_order_id": ''
        //         "status": 1, // 1 = new, 2 = update 3 = cancel or execute
        //    }
        //
        let mut status: Value = self.safe_integer_k(order.clone(), "status", &[]);
        let mut marketId: Value = self.safe_string_k(order.clone(), "market", &[]);
        market = self.safe_market(&[marketId.clone(), market.clone()]);
        let mut id: Value = self.safe_string_k(order.clone(), "id", &[]);
        let mut clientOrderId: Value = self.omit_zero(self.safe_string_k(order.clone(), "client_order_id", &[]));
        let mut price: Value = self.safe_string_k(order.clone(), "price", &[]);
        let mut filled: Value = self.safe_string_k(order.clone(), "deal_stock", &[]);
        let mut cost: Value = self.safe_string_k(order.clone(), "deal_money", &[]);
        let mut stopPrice: Value = self.safe_string_k(order.clone(), "activation_price", &[]);
        let mut rawType: Value = self.safe_string_k(order.clone(), "type", &[]);
        let mut type_var: Value = self.parse_ws_order_type(rawType.clone());
        let mut amount: Value = Value::Null;
        let mut remaining: Value = Value::Null;
        if is_equal(&type_var, &Value::Str("market".to_string())) {
            amount = self.safe_string_k(order.clone(), "deal_stock", &[]);
            remaining = Value::Str("0".to_string());
        }  else {
            remaining = self.safe_string_k(order.clone(), "left", &[]);
            amount = self.safe_string_k(order.clone(), "amount", &[]);
        }
        let mut timestamp: Value = self.safe_timestamp(order.clone(), Value::Str("ctime".to_string()), &[]);
        let mut lastTradeTimestamp: Value = self.safe_timestamp(order.clone(), Value::Str("mtime".to_string()), &[]);
        let mut symbol: Value = get_value(&market, &Value::Str("symbol".to_string()));
        let mut rawSide: Value = self.safe_integer_k(order.clone(), "side", &[]);
        let mut side: Value = ternary(is_true(&(is_equal(&rawSide, &Value::Int(1)))), Value::Str("sell".to_string()), Value::Str("buy".to_string()));
        let mut dealFee: Value = self.safe_string_k(order.clone(), "deal_fee", &[]);
        let mut fee: Value = Value::Null;
        if !is_equal(&dealFee, &Value::Null) {
            fee = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("cost".to_string(), self.parse_number(dealFee.clone(), &[]));
                    m.insert("currency".to_string(), get_value(&market, &Value::Str("quote".to_string())));
                m
            });
        }
        let mut unifiedStatus: Value = Value::Null;
        if is_true(&(is_equal(&status, &Value::Int(1)))) || is_true(&(is_equal(&status, &Value::Int(2)))) {
            unifiedStatus = Value::Str("open".to_string());
        }  else {
            if is_true(&crate::precise::Precise::stringEquals(&remaining, &Value::Str("0".to_string()))) {
                unifiedStatus = Value::Str("closed".to_string());
            }  else {
                unifiedStatus = Value::Str("canceled".to_string());
            }
        }
        return self.safe_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), order.clone());
        m.insert("symbol".to_string(), symbol.clone());
        m.insert("id".to_string(), id.clone());
        m.insert("clientOrderId".to_string(), clientOrderId.clone());
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp.clone()));
        m.insert("lastTradeTimestamp".to_string(), lastTradeTimestamp.clone());
        m.insert("type".to_string(), type_var.clone());
        m.insert("timeInForce".to_string(), Value::Null);
        m.insert("postOnly".to_string(), Value::Null);
        m.insert("side".to_string(), side.clone());
        m.insert("price".to_string(), price.clone());
        m.insert("stopPrice".to_string(), stopPrice.clone());
        m.insert("triggerPrice".to_string(), stopPrice.clone());
        m.insert("amount".to_string(), amount.clone());
        m.insert("cost".to_string(), cost.clone());
        m.insert("average".to_string(), Value::Null);
        m.insert("filled".to_string(), filled.clone());
        m.insert("remaining".to_string(), remaining.clone());
        m.insert("status".to_string(), unifiedStatus.clone());
        m.insert("fee".to_string(), fee.clone());
        m.insert("trades".to_string(), Value::Null);
    m
}), &[market.clone()]);

    Value::Null
}

    pub fn parse_ws_order_type(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("1".to_string(), Value::Str("limit".to_string()));
                m.insert("2".to_string(), Value::Str("market".to_string()));
                m.insert("202".to_string(), Value::Str("market".to_string()));
                m.insert("3".to_string(), Value::Str("limit".to_string()));
                m.insert("4".to_string(), Value::Str("market".to_string()));
                m.insert("5".to_string(), Value::Str("limit".to_string()));
                m.insert("6".to_string(), Value::Str("market".to_string()));
                m.insert("8".to_string(), Value::Str("limit".to_string()));
                m.insert("10".to_string(), Value::Str("market".to_string()));
            m
        });
        return self.safe_string(statuses.clone(), status.clone(), &[status.clone()]);

    Value::Null
}

/*
 * @method
 * @name whitebit#watchBalance
 * @description watch balance and get the amount of funds available for trading or funds locked in orders
 * @see https://docs.whitebit.com/private/websocket/#balance-spot
 * @see https://docs.whitebit.com/private/websocket/#balance-margin
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {str} [params.type] spot or contract if not provided this.options['defaultType'] is used
 * @param {bool} [params.fetchBalanceSnapshot] whether to fetch the initial balance snapshot over REST, default is true
 * @param {bool} [params.awaitBalanceSnapshot] whether to wait for the balance snapshot before providing updates, default is true
 * @returns {object} a [balance structure]{@link https://docs.ccxt.com/?id=balance-structure}
 */
    pub async fn watch_balance(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut type_var: Value = Value::Null;
        { let __destr_tmp = self.handle_market_type_and_params(Value::Str("watchBalance".to_string()), &[Value::Null, params.clone()]); type_var = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        let mut messageHash: Value = Value::Str("wallet:".to_string());
        let mut method: Value = Value::Null;
        if is_equal(&type_var, &Value::Str("spot".to_string())) {
            method = Value::Str("balanceSpot_subscribe".to_string());
            messageHash = add(&messageHash, &Value::Str("spot".to_string()));
        }  else {
            method = Value::Str("balanceMargin_subscribe".to_string());
            messageHash = add(&messageHash, &Value::Str("margin".to_string()));
        }
        let mut url: Value = get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string()));
        let mut client: Value = self.client(&[url.clone()]);
        self.set_balance_cache(client.clone(), type_var.clone(), messageHash.clone());
        let mut fetchBalanceSnapshot: Value = Value::Null;
        let mut awaitBalanceSnapshot: Value = Value::Null;
        { let __destr_tmp = self.handle_option_and_params(params.clone(), Value::Str("watchBalance".to_string()), Value::Str("fetchBalanceSnapshot".to_string()), &[Value::Bool(true)]); fetchBalanceSnapshot = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        { let __destr_tmp = self.handle_option_and_params(params.clone(), Value::Str("watchBalance".to_string()), Value::Str("awaitBalanceSnapshot".to_string()), &[Value::Bool(true)]); awaitBalanceSnapshot = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        if is_true(&fetchBalanceSnapshot) && is_true(&awaitBalanceSnapshot) {
            crate::exchange_stubs::ws_await_flight(&client.future(&[add(&type_var, &Value::Str(":fetchBalanceSnapshot".to_string()))])).await;
        }
        return self.watch_private(messageHash.clone(), method.clone(), &[Value::List(vec![]), params.clone()]).await;

    Value::Null
}

    pub fn set_balance_cache(&mut self, mut client: Value, mut type_var: Value, mut subscriptionHash: Value) {
        if is_true(&Value::Bool(in_op(&get_value(&client, &Value::Str("subscriptions".to_string())), &subscriptionHash))) {
            return;
        }
        let mut fetchBalanceSnapshot: Value = self.handle_option(Value::Str("watchBalance".to_string()), Value::Str("fetchBalanceSnapshot".to_string()), &[Value::Bool(true)]);
        if is_equal(&fetchBalanceSnapshot, &Value::Bool(true)) {
            let mut messageHash: Value = add(&type_var, &Value::Str(":fetchBalanceSnapshot".to_string()));
            if !is_true(&(Value::Bool(in_op(&get_value(&client, &Value::Str("futures".to_string())), &messageHash)))) {
                client.future(&[messageHash.clone()]);
                self.spawn(&[Value::Str("load_balance_snapshot".to_string()).clone(), client.clone(), messageHash.clone(), type_var.clone(), subscriptionHash.clone()]);
            }
        }
}

    pub async fn load_balance_snapshot(&mut self, mut client: Value, mut messageHash: Value, mut type_var: Value, mut subscriptionHash: Value) -> Value {
        let mut response: Value = self.fetch_balance(&[Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("type".to_string(), type_var.clone());
            m
        })]).await;
        { let __t = self.extend(response.clone(), &[self.balance.clone()]); self.balance = __t; }
        // don't remove the future from the .futures cache
        if is_true(&Value::Bool(in_op(&get_value(&client, &Value::Str("futures".to_string())), &messageHash))) {
            let mut future: Value = get_value(&get_value(&client, &Value::Str("futures".to_string())), &messageHash);
            future.resolve(&[]);
            client.resolve(&[self.balance.clone(), subscriptionHash.clone()]);
        }

    Value::Null
}

    pub fn handle_balance(&mut self, mut client: Value, mut message: Value) {
        //
        // spot
        //
        //   {
        //       "method":"balanceSpot_update",
        //       "params":[
        //          {
        //             "LTC":{
        //                "available":"0.16587",
        //                "freeze":"0"
        //             },
        //             "BTC":{
        //                "available":"0.005",
        //                "freeze":"0.001"
        //             }
        //          }
        //       ],
        //       "id":null
        //   }
        //
        // margin
        //
        //   {
        //       "method":"balanceMargin_update",
        //       "params":[
        //          {
        //             "a":"USDT",         // asset
        //             "B":"0.00538073",   // total balance
        //             "b":"0",            // borrowed
        //             "av":"0.00538073",  // available without borrowing
        //             "ab":"28.43739825"  // available with borrowing
        //          }
        //       ],
        //       "id":null
        //   }
        //
        let mut method: Value = self.safe_string_k(message.clone(), "method", &[]);
        if is_equal(&method, &Value::Null) {
            return;
        }
        let mut isMargin: bool = is_greater_than_or_equal(&get_index_of(&method, &Value::Str("Margin".to_string())), &Value::Int(0));
        let mut data: Value = self.safe_list_k(message.clone(), "params", &[Value::List(vec![])]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_655: bool = true;
            while { if !__for_first_655 { i = add(&i, &Value::Int(1)); } __for_first_655 = false; is_less_than(&i, &get_array_length(&data)) } {
            let mut balanceDict: Value = self.safe_dict(data.clone(), i.clone(), &[Value::Map({
                let mut m = indexmap::IndexMap::new();
                m
            })]);
            add_element_to_object(&mut self.balance, &Value::Str("info".to_string()), balanceDict.clone());
            if is_true(&isMargin) {
                let mut currencyId: Value = self.safe_string_k(balanceDict.clone(), "a", &[]);
                let mut code: Value = self.safe_currency_code(currencyId.clone(), &[]);
                let mut account: Value = self.account();
                add_element_to_object(&mut account, &Value::Str("free".to_string()), self.safe_string_k(balanceDict.clone(), "av", &[]));
                add_element_to_object(&mut account, &Value::Str("total".to_string()), self.safe_string_k(balanceDict.clone(), "B", &[]));
                add_element_to_object(&mut account, &Value::Str("debt".to_string()), self.safe_string_k(balanceDict.clone(), "b", &[]));
                if !is_equal(&code, &Value::Null) {
                    add_element_to_object(&mut self.balance, &code, account.clone());
                }
            }  else {
                let mut keys: Value = object_keys(&balanceDict);
                {
                                        let mut j: Value = Value::Int(0);
                    let mut __for_first_654: bool = true;
                    while { if !__for_first_654 { j = add(&j, &Value::Int(1)); } __for_first_654 = false; is_less_than(&j, &get_array_length(&keys)) } {
                    let mut currencyId: Value = get_value(&keys, &j);
                    let mut currencyId: Value = get_value(&keys, &j);
                    let mut rawBalance: Value = self.safe_dict(balanceDict.clone(), currencyId.clone(), &[Value::Map({
                        let mut m = indexmap::IndexMap::new();
                        m
                    })]);
                    let mut code: Value = self.safe_currency_code(currencyId.clone(), &[]);
                    let mut account: Value = self.account();
                    add_element_to_object(&mut account, &Value::Str("free".to_string()), self.safe_string_k(rawBalance.clone(), "available", &[]));
                    add_element_to_object(&mut account, &Value::Str("used".to_string()), self.safe_string_k(rawBalance.clone(), "freeze", &[]));
                    if !is_equal(&code, &Value::Null) {
                        add_element_to_object(&mut self.balance, &code, account.clone());
                    }
                }
                }
            }
        }
        }
        { let __t = self.safe_balance(self.balance.clone()); self.balance = __t; }
        let mut messageHash: Value = Value::Str("wallet:".to_string());
        if is_greater_than_or_equal(&get_index_of(&method, &Value::Str("Spot".to_string())), &Value::Int(0)) {
            messageHash = add(&messageHash, &Value::Str("spot".to_string()));
        }  else {
            messageHash = add(&messageHash, &Value::Str("margin".to_string()));
        }
        client.resolve(&[self.balance.clone(), messageHash.clone()]);
}

    pub async fn watch_public(&mut self, mut messageHash: Value, mut method: Value, optional_args: &[Value]) -> Value {
        let mut reqParams = get_arg(optional_args, 0, Value::List(vec![]));
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut url: Value = get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string()));
        let mut id: Value = self.nonce();
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("id".to_string(), id.clone());
                m.insert("method".to_string(), method.clone());
                m.insert("params".to_string(), reqParams.clone());
            m
        });
        let mut message: Value = self.extend(request.clone(), &[params.clone()]);
        return self.watch(url.clone(), messageHash.clone(), &[message.clone(), messageHash.clone()]).await;

    Value::Null
}

    pub async fn watch_multiple_subscription(&mut self, mut messageHash: Value, mut method: Value, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut isNested = get_arg(optional_args, 0, Value::Bool(false));
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut url: Value = get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string()));
        let mut id: Value = self.nonce();
        let mut client: Value = self.safe_value(self.clients.clone(), url.clone(), &[]);
        let mut request: Value = Value::Null;
        let mut marketIds: Value = Value::List(vec![]);
        if is_equal(&client, &Value::Null) {
            let mut subscription: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                m
            });
            let mut market: Value = self.market(symbol.clone());
            let mut marketId: Value = get_value(&market, &Value::Str("id".to_string()));
            if !is_equal(&marketId, &Value::Null) {
                add_element_to_object(&mut subscription, &marketId, Value::Bool(true));
            }
            marketIds = Value::List(vec![marketId.clone()]);
            if is_true(&isNested) {
                marketIds = Value::List(vec![marketIds.clone()]);
            }
            request = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("id".to_string(), id.clone());
                    m.insert("method".to_string(), method.clone());
                    m.insert("params".to_string(), marketIds.clone());
                m
            });
            let mut message: Value = self.extend(request.clone(), &[params.clone()]);
            return self.watch(url.clone(), messageHash.clone(), &[message.clone(), method.clone(), subscription.clone()]).await;
        }  else {
            let mut subscription: Value = self.safe_value(get_value(&client, &Value::Str("subscriptions".to_string())), method.clone(), &[Value::Map({
                let mut m = indexmap::IndexMap::new();
                m
            })]);
            let mut hasSymbolSubscription: bool = true;
            let mut market: Value = self.market(symbol.clone());
            let mut marketId: Value = get_value(&market, &Value::Str("id".to_string()));
            let mut isSubscribed: Value = self.safe_bool(subscription.clone(), marketId.clone(), &[Value::Bool(false)]);
            if !is_equal(&isSubscribed, &Value::Bool(true)) {
                if !is_equal(&marketId, &Value::Null) {
                    add_element_to_object(&mut subscription, &marketId, Value::Bool(true));
                }
                hasSymbolSubscription = false;
            }
            if is_true(&hasSymbolSubscription) {
                return self.watch(url.clone(), messageHash.clone(), &[request.clone(), method.clone(), subscription.clone()]).await;
            }  else {
                // resubscribe
                let mut marketIdsNew: Value = Value::List(vec![]);
                marketIdsNew = object_keys(&subscription);
                if is_true(&isNested) {
                    marketIdsNew = Value::List(vec![marketIdsNew.clone()]);
                }
                let mut resubRequest: Value = Value::Map({
                    let mut m = indexmap::IndexMap::new();
                        m.insert("id".to_string(), id.clone());
                        m.insert("method".to_string(), method.clone());
                        m.insert("params".to_string(), marketIdsNew.clone());
                    m
                });
                if is_true(&Value::Bool(in_op(&get_value(&client, &Value::Str("subscriptions".to_string())), &method))) {
                    remove(&mut get_value(&client, &Value::Str("subscriptions".to_string())), &method);
                }
                return self.watch(url.clone(), messageHash.clone(), &[resubRequest.clone(), method.clone(), subscription.clone()]).await;
            }
        }

    Value::Null
}

    pub async fn watch_private(&mut self, mut messageHash: Value, mut method: Value, optional_args: &[Value]) -> Value {
        let mut reqParams = get_arg(optional_args, 0, Value::List(vec![]));
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.check_required_credentials(&[]);
        self.authenticate(&[]).await;
        let mut url: Value = get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string()));
        let mut id: Value = self.nonce();
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("id".to_string(), id.clone());
                m.insert("method".to_string(), method.clone());
                m.insert("params".to_string(), reqParams.clone());
            m
        });
        let mut message: Value = self.extend(request.clone(), &[params.clone()]);
        return self.watch(url.clone(), messageHash.clone(), &[message.clone(), messageHash.clone()]).await;

    Value::Null
}

    pub async fn authenticate(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.check_required_credentials(&[]);
        let mut url: Value = get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string()));
        let mut client: Value = self.client(&[url.clone()]);
        let mut subscribeHash: Value = Value::Str("authenticated".to_string());
        // handleAuthenticate () resolves the handshake future with 1, so 1 is
        // the authorized sentinel authenticate () has always returned - every
        // path below hands back that same value
        let mut authorized: Value = Value::Int(1);
        // single-flight leader election, see
        // https://github.com/ccxt/ccxt/issues/29393: the handshake is gated on
        // subscriptions['authenticated'], which watch () only registers once
        // the awaited v4PrivatePostProfileWebsocketToken () has resolved, so
        // every concurrent cold caller used to pass that gate, burn a
        // rate-limited private REST call for its own websocket_token and push
        // its own authorize frame down the shared socket. the flight is
        // registered in client.futures on the very client that carries the
        // handshake, under a key that is not one of the exchange's own
        // messageHashes, and is settled through client.resolve () /
        // client.reject () so every write to that map goes through the
        // client's own accessors
        let mut messageHash: Value = Value::Str("authenticateFlight".to_string());
        if is_true(&Value::Bool(in_op(&get_value(&client, &Value::Str("futures".to_string())), &messageHash))) {
            // a flight is already in progress - wake when the leader settles
            // it, the socket is authorized by then. the flight gate is
            // checked before the subscriptions one because watch () registers
            // subscriptions['authenticated'] immediately, long before the
            // venue acks the authorize frame
            crate::exchange_stubs::ws_await_flight(&client.future(&[messageHash.clone()])).await;
            return authorized;
        }
        let mut authenticated: Value = self.safe_value(get_value(&client, &Value::Str("subscriptions".to_string())), subscribeHash.clone(), &[]);
        if !is_equal(&authenticated, &Value::Null) {
            return authorized;
        }
        // register the flight BEFORE the first await, so a caller arriving
        // during the fetch or the authorize round-trip finds it and waits
        // instead of re-leading, and so client.reject () below always has a
        // waiter and can never park the error in client.rejections
        let mut future: Value = client.reusable_future(messageHash.clone());
        let _try_result = futures::FutureExt::catch_unwind(std::panic::AssertUnwindSafe(async {
            let mut authToken: Value = self.parent.v4_private_post_profile_websocket_token(&[]).await;
            //
            //   {
            //       "websocket_token": "$2y$10$lxCvTXig/XrcTBFY1bdFseCKQmFTDtCpEzHNVnXowGplExFxPJp9y"
            //   }
            //
            let mut token: Value = self.safe_string_k(authToken.clone(), "websocket_token", &[]);
            if is_equal(&token, &Value::Null) {
                panic!("{}", crate::exchange_errors::authentication_error(add(&self.id, &Value::Str(" authenticate() received an empty websocket_token".to_string()))));
            }
            let mut id: Value = self.nonce();
            let mut request: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("id".to_string(), id.clone());
                    m.insert("method".to_string(), Value::Str("authorize".to_string()));
                    m.insert("params".to_string(), Value::List(vec![token.clone(), Value::Str("public".to_string())]));
                m
            });
            let mut subscription: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("id".to_string(), id.clone());
                    m.insert("method".to_string(), Value::Str("handle_authenticate".to_string()).clone());
                m
            });
            self.watch(url.clone(), subscribeHash.clone(), &[request.clone(), subscribeHash.clone(), subscription.clone()]).await;
            // settle the flight and wake every waiter - resolve () also drops
            // the registry entry, so a later cold call can re-lead
            client.resolve(&[authorized.clone(), messageHash.clone()]);
         #[allow(unreachable_code)] { Value::Null }})).await;
if let Err(_try_err) = _try_result { let e: Value = panic_to_value(_try_err);
            // drop the handshake state so the next caller can retry: watch ()
            // registers subscriptions['authenticated'] before it connects and
            // parks a rejected future under the same key when the dial fails,
            // and either one left behind would make every later authenticate ()
            // replay that failure. the stale future is settled through
            // client.reject () - guarded, so it always has a waiter and the
            // error is never parked in client.rejections
            if is_true(&Value::Bool(in_op(&get_value(&client, &Value::Str("subscriptions".to_string())), &subscribeHash))) {
                remove(&mut get_value(&client, &Value::Str("subscriptions".to_string())), &subscribeHash);
            }
            if is_true(&Value::Bool(in_op(&get_value(&client, &Value::Str("futures".to_string())), &subscribeHash))) {
                client.reject(&[e.clone(), subscribeHash.clone()]);
            }
            // reject the flight - the leader and every waiter throw and the
            // next caller re-leads instead of deadlocking on a dead flight
            client.reject(&[e.clone(), messageHash.clone()]);
        }
        // rethrows the failure to the leader and attaches the handler that
        // keeps an alone-leader rejection from crashing the process
        crate::exchange_stubs::ws_await_flight(&future).await;
        return authorized;

    Value::Null
}

    pub fn handle_authenticate(&self, mut client: Value, mut message: Value) -> Value {
        //
        //     { error: null, result: { status: "success" }, id: 1656084550 }
        //
        let mut future: Value = get_value(&get_value(&client, &Value::Str("futures".to_string())), &Value::Str("authenticated".to_string()));
        future.resolve(&[Value::Int(1)]);
        return message;

    Value::Null
}

    pub fn handle_error_message(&self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "error": { code: 1, message: "invalid argument" },
        //         "result": null,
        //         "id": 1656090882
        //     }
        //
        let mut error: Value = self.safe_value_k(message.clone(), "error", &[]);
        let _try_result = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            if !is_equal(&error, &Value::Null) {
                let mut code: Value = self.safe_string_k(message.clone(), "code", &[]);
                let mut feedback: Value = add(&add(&self.id, &Value::Str(" ".to_string())), &self.json(message.clone()));
                self.throw_exactly_matched_exception(get_value(&get_value(&self.exceptions, &Value::Str("ws".to_string())), &Value::Str("exact".to_string())), code.clone(), feedback.clone());
            }
         #[allow(unreachable_code)] { Value::Null }}));
if let Err(_try_err) = _try_result { let e: Value = panic_to_value(_try_err);
            if is_true(&is_instance(&e, &Value::Str("AuthenticationError".to_string()))) {
                client.reject(&[e.clone(), Value::Str("authenticated".to_string())]);
                if is_true(&Value::Bool(in_op(&get_value(&client, &Value::Str("subscriptions".to_string())), &Value::Str("authenticated".to_string())))) {
                    remove(&mut get_value(&client, &Value::Str("subscriptions".to_string())), &Value::Str("authenticated".to_string()));
                }
                return Value::Bool(false);
            }
        }
        return Value::Bool(true);

    Value::Null
}

    pub fn handle_message(&mut self, mut client: Value, mut message: Value) {
        //
        // auth
        //    { error: null, result: { status: "success" }, id: 1656084550 }
        //
        // pong
        //    { error: null, result: "pong", id: 0 }
        //
        if !is_equal(&self.handle_error_message(client.clone(), message.clone()), &Value::Bool(true)) {
            return;
        }
        let mut result: Value = self.safe_string_k(message.clone(), "result", &[]);
        if is_equal(&result, &Value::Str("pong".to_string())) {
            self.handle_pong(client.clone(), message.clone());
            return;
        }
        let mut id: Value = self.safe_integer_k(message.clone(), "id", &[]);
        if !is_equal(&id, &Value::Null) {
            self.handle_subscription_status(client.clone(), message.clone(), id.clone());
            return;
        }
        let mut methods: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("market_update".to_string(), Value::Str("handle_ticker".to_string()).clone());
                m.insert("trades_update".to_string(), Value::Str("handle_trades".to_string()).clone());
                m.insert("depth_update".to_string(), Value::Str("handle_order_book".to_string()).clone());
                m.insert("candles_update".to_string(), Value::Str("handle_ohlcv".to_string()).clone());
                m.insert("ordersPending_update".to_string(), Value::Str("handle_order".to_string()).clone());
                m.insert("ordersExecuted_update".to_string(), Value::Str("handle_order".to_string()).clone());
                m.insert("balanceSpot_update".to_string(), Value::Str("handle_balance".to_string()).clone());
                m.insert("balanceMargin_update".to_string(), Value::Str("handle_balance".to_string()).clone());
                m.insert("deals_update".to_string(), Value::Str("handle_my_trades".to_string()).clone());
            m
        });
        let mut topic: Value = self.safe_value_k(message.clone(), "method", &[]);
        let mut method: Value = self.safe_value(methods.clone(), topic.clone(), &[]);
        if !is_equal(&method, &Value::Null) {
            self.dispatch_ws_handler(&method, &[client.clone(), message.clone()]);
        }
}

    pub fn handle_subscription_status(&mut self, mut client: Value, mut message: Value, mut id: Value) {
        // not every method stores its subscription
        // as an object so we can't do indeById here
        let mut subs: Value = get_value(&client, &Value::Str("subscriptions".to_string()));
        let mut values: Value = object_values(&subs);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_656: bool = true;
            while { if !__for_first_656 { i = add(&i, &Value::Int(1)); } __for_first_656 = false; is_less_than(&i, &get_array_length(&values)) } {
            let mut subscription: Value = get_value(&values, &i);
            let mut subscription: Value = get_value(&values, &i);
            if !is_equal(&subscription, &Value::Bool(true)) {
                let mut subId: Value = self.safe_integer_k(subscription.clone(), "id", &[]);
                if is_true(&(!is_equal(&subId, &Value::Null))) && is_true(&(is_equal(&subId, &id))) {
                    let mut method: Value = self.safe_value_k(subscription.clone(), "method", &[]);
                    if !is_equal(&method, &Value::Null) {
                        self.dispatch_ws_handler(&method, &[client.clone(), message.clone()]);
                        return;
                    }
                }
            }
        }
        }
}

    pub fn handle_pong(&self, mut client: Value, mut message: Value) -> Value {
        crate::set_value(&mut client, &Value::Str("lastPong".to_string()), self.milliseconds());
        return message;

    Value::Null
}

    pub fn ping(&self, mut client: Value) -> Value {
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), Value::Int(0));
        m.insert("method".to_string(), Value::Str("ping".to_string()));
        m.insert("params".to_string(), Value::List(vec![]));
    m
});

    Value::Null
}
}
