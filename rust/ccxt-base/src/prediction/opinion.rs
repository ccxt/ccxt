// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#![allow(unused, non_snake_case, clippy::all)]
use crate::Value;
use crate::get_value;
use crate::runtime::*;
// Base methods are now trait methods (review #1: static dispatch). Bring the
// traits into scope so `self.market(...)`, `self.safe_market(...)`,
// `self.load_markets(...)`, … on this Core resolve to the base defaults.
use crate::exchange_generated::ExchangeBase;
use crate::exchange::ExchangeRuntime;
use crate::pro::*;
use crate::prediction_exchange_generated::PredictionBase;
use crate::prediction_exchange::PredictionRuntime;


pub struct OpinionCore {
    pub exchange: crate::prediction_exchange::PredictionExchange,
}

impl OpinionCore {
    pub fn new(config: Option<crate::Value>) -> Self {
        let mut s = Self { exchange: crate::prediction_exchange::PredictionExchange::new(config) };
        s.init();
        s
    }

    /// One-shot setup: populates describe() data onto the inner Exchange and
    /// builds the implicit API table. Idempotent — calling more than once is
    /// safe.
    ///
    /// Dispatch is now fully static (review #1: pointer removal) — virtual
    /// calls resolve through the DerivedExchange/ExchangeBase trait impls
    /// on the concrete Core type, so there is no address to bind and init()
    /// needs no self-referential setup. It runs safely while self is still a
    /// movable local in new().
    pub fn init(&mut self) {
        // Populate describe()-derived fields.
        let described = OpinionCore::describe(self);
        self.api      = crate::get_value(&described, &crate::Value::Str("api".to_string()));
        self.urls     = crate::get_value(&described, &crate::Value::Str("urls".to_string()));
        self.has      = crate::get_value(&described, &crate::Value::Str("has".to_string()));
        // rateLimit drives the throttle spacing. describe() usually declares a
        // venue-specific value (binance 50ms, …); new() ran apply_config before
        // init(), so a config-supplied rateLimit is already in place. Only fall
        // back to describe()'s value when the field is still the base 2000ms
        // default, so caller config keeps precedence (review #8 — init() used
        // to drop describe().rateLimit, leaving every venue at 2000ms).
        let __described_rate_limit = crate::get_value(&described, &crate::Value::Str("rateLimit".to_string()));
        if matches!(__described_rate_limit, crate::Value::Int(_) | crate::Value::Float(_))
            && matches!(self.rateLimit, crate::Value::Int(2000)) {
            self.rateLimit = __described_rate_limit;
        }
        // Merge describe() options INTO whatever apply_config (or
        // earlier setup) already populated. Caller-supplied options
        // (e.g. portfolioMargin via fixture) take precedence, then
        // describe()'s defaults fill in any gaps.
        let __described_options = crate::get_value(&described, &crate::Value::Str("options".to_string()));
        // Capture the describe()'s explicit networksById BEFORE the merge
        // — the merge below overlays the base Exchange's options (whose
        // after_construct left an EMPTY networksById), which would
        // otherwise clobber the manual mappings like binance BSC to BEP20.
        let __described_networks_by_id = crate::get_value(&__described_options, &crate::Value::Str("networksById".to_string()));
        if let (crate::Value::Dict(_), crate::Value::Dict(_)) =
            (&self.options.clone(), &__described_options)
        {
            // Deep-extend (CCXT deepExtend): describe() defaults as the base,
            // existing (config + parent REST describe) winning on leaf conflicts,
            // nested dicts merged rather than replaced — so e.g. a pro Core's
            // flat options.timeframes unions with the parent REST's nested one
            // instead of one clobbering the other.
            self.options = crate::runtime::deep_merge_dict(
                &__described_options, &self.options.clone());
        } else if !matches!(self.options, crate::Value::Dict(_)) {
            self.options = __described_options;
        }
        // Derive options.networksById (CCXT's createNetworksByIdObject):
        // start from the auto-inverted networks (generated), then overlay
        // the describe()'s explicit networksById so manual mappings win —
        // mirrors TS extend(generated, manual). Without the manual
        // overlay, an exchange whose networks defines both BSC to BSC and
        // BEP20 to BSC would invert to BSC to BSC (wrong) by iteration
        // order instead of the intended BSC to BEP20.
        if let crate::Value::Dict(opts_arc) = self.options.clone() {
            let mut opts = std::sync::Arc::try_unwrap(opts_arc).unwrap_or_else(|a| (*a).clone());
            let mut by_id: indexmap::IndexMap<String, crate::Value> = indexmap::IndexMap::new();
            if let Some(crate::Value::Dict(networks)) = opts.get("networks") {
                for (code, id) in networks.iter() {
                    if let crate::Value::Str(id_s) = id {
                        by_id.entry(id_s.clone())
                             .or_insert_with(|| crate::Value::Str(code.clone()));
                    }
                }
            }
            if let crate::Value::Dict(manual) = &__described_networks_by_id {
                for (k, v) in manual.iter() { by_id.insert(k.clone(), v.clone()); }
            }
            opts.insert("networksById".to_string(), crate::Value::Map(by_id));
            self.options = crate::Value::Map(opts);
        }
        self.hostname = crate::get_value(&described, &crate::Value::Str("hostname".to_string()));
        self.version  = crate::get_value(&described, &crate::Value::Str("version".to_string()));
        self.id       = crate::get_value(&described, &crate::Value::Str("id".to_string()));
        self.name     = crate::get_value(&described, &crate::Value::Str("name".to_string()));
        self.exceptions = crate::get_value(&described, &crate::Value::Str("exceptions".to_string()));
        // Only override the base-default requiredCredentials when the
        // exchange's describe() actually provides them (super.describe()
        // is stubbed, so most exchanges' describe() omits this).
        { let __rc = crate::get_value(&described, &crate::Value::Str("requiredCredentials".to_string())); if !matches!(__rc, crate::Value::Null) { self.requiredCredentials = __rc; } }
        // Merge describe()'s commonCurrencies over the base defaults so
        // exchange-specific aliases (bitfinex UST to USDT, onetrading
        // MIOTA to IOTA) reach commonCurrencyCode / safeCurrencyCode.
        { let __cc = crate::get_value(&described, &crate::Value::Str("commonCurrencies".to_string())); if let crate::Value::Dict(extra) = __cc { let extra = std::sync::Arc::try_unwrap(extra).unwrap_or_else(|a| (*a).clone()); if let crate::Value::Dict(base) = &mut self.commonCurrencies { let base = std::sync::Arc::make_mut(base); for (k, v) in extra { base.insert(k, v); } } else { self.commonCurrencies = crate::Value::Map(extra); } } }
        self.precisionMode = crate::get_value(&described, &crate::Value::Str("precisionMode".to_string()));
        self.timeframes = crate::get_value(&described, &crate::Value::Str("timeframes".to_string()));
        self.fees = crate::get_value(&described, &crate::Value::Str("fees".to_string()));
        // `features` carries the describe() block that drives
        // unified-method tests (e.g. `features.spot.fetchCurrencies.private`
        // tells testFetchCurrencies to skip the length check). It's set
        // after `Exchange::new` because `features_generator` (in
        // `after_construct`) bails when `features == Null` — so we
        // assign and re-run the generator here.
        self.features = crate::get_value(&described, &crate::Value::Str("features".to_string()));
        if !matches!(self.features, crate::Value::Null) {
            self.features_generator();
        }
        // Markets and currencies may have been populated already by
        // the constructor config (test runners pass them in via
        // Exchange::new(Some(config-with-markets)) — same as CCXT TS).
        // Don't reset them here.
        // Apply hardcoded describe().markets. Venues like coincheck ship a
        // static markets block and never fetchMarkets — CCXT sets this.markets
        // from describe() at construction, and its base fetchMarkets just
        // returns Object.values(this.markets). Mirror that here so loadMarkets
        // resolves without a network fetch. Guarded on a non-empty describe()
        // markets and an as-yet-unloaded self.markets, so fetch-based venues
        // (empty describe markets) and config-injected markets are untouched.
        {
            let __described_markets = crate::get_value(&described, &crate::Value::Str("markets".to_string()));
            if matches!(&__described_markets, crate::Value::Dict(mm) if !mm.is_empty())
                && matches!(self.markets, crate::Value::Null)
            {
                let __markets_list = crate::runtime::object_values(&__described_markets);
                <Self as crate::exchange_generated::ExchangeBase>::set_markets(self, __markets_list, &[crate::Value::Null]);
            }
        }
        self.build_implicit_api();
    }

    /// Compatibility no-op. The old pointer-based dispatch needed a post-move
    /// `bind()`; static trait dispatch (review #1) needs no binding, so this
    /// just exists so callers that still call it keep compiling.
    #[inline]
    pub fn bind(&mut self) {}
}

impl crate::exchange::DerivedExchange for OpinionCore {
    fn parse_ohlcv(&self, ohlcv: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on OpinionCore.
        OpinionCore::parse_ohlcv(self, ohlcv, &[market.clone()])
    }
    fn parse_balance(&self, response: crate::Value) -> crate::Value {
        // Forward to the inherent method on OpinionCore.
        OpinionCore::parse_balance(self, response)
    }
    fn parse_prediction_trade(&self, trade: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on OpinionCore.
        OpinionCore::parse_prediction_trade(self, trade, &[market.clone()])
    }
    fn parse_prediction_order(&self, order: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on OpinionCore.
        OpinionCore::parse_prediction_order(self, order, &[market.clone()])
    }
    fn parse_prediction_position(&self, position: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on OpinionCore.
        OpinionCore::parse_prediction_position(self, position, &[market.clone()])
    }
    fn sign(&self, path: crate::Value, api: crate::Value, method: crate::Value, params: crate::Value, headers: crate::Value, body: crate::Value) -> crate::Value {
        // Forward to the inherent method on OpinionCore.
        OpinionCore::sign(self, path, &[api.clone(), method.clone(), params.clone(), headers.clone(), body.clone()])
    }
    fn handle_errors(&self, code: crate::Value, reason: crate::Value, url: crate::Value, method: crate::Value, headers: crate::Value, body: crate::Value, response: crate::Value, request_headers: crate::Value, request_body: crate::Value) -> crate::Value {
        // Forward to the inherent method on OpinionCore.
        OpinionCore::handle_errors(self, code, reason, url, method, headers, body, response, request_headers, request_body)
    }
}

impl crate::exchange_generated::ExchangeBase for OpinionCore {
    fn call_dynamic<'a>(&'a mut self, method: &'a str, args: Vec<crate::Value>)
        -> std::pin::Pin<Box<dyn std::future::Future<Output = crate::Value> + Send + 'a>>
    {
        Box::pin(async move {
            match method {
                "cancel_order" => self.cancel_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
                "create_api_key" => self.create_api_key(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "create_order" => self.create_order(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), args.get(3).cloned().unwrap_or(crate::Value::Null), &args.get(4..).unwrap_or(&[]).to_vec()[..]).await,
                "delete_api_key" => self.delete_api_key(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_api_key" => self.fetch_api_key(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_balance" => self.fetch_balance(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_closed_orders" => self.fetch_closed_orders(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_event" => self.fetch_event(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_events" => self.fetch_events(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_markets" => self.fetch_markets(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_my_trades" => self.fetch_my_trades(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_ohlcv" => self.fetch_ohlcv(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_open_orders" => self.fetch_open_orders(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_order" => self.fetch_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_order_book" => self.fetch_order_book(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_orders" => self.fetch_orders(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_outcome" => self.fetch_outcome(args.get(0).cloned().unwrap_or(crate::Value::Null)).await,
                "fetch_positions" => self.fetch_positions(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_ticker" => self.fetch_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
                "fetch_tickers" => self.fetch_tickers(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "handle_errors" => self.handle_errors(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), args.get(3).cloned().unwrap_or(crate::Value::Null), args.get(4).cloned().unwrap_or(crate::Value::Null), args.get(5).cloned().unwrap_or(crate::Value::Null), args.get(6).cloned().unwrap_or(crate::Value::Null), args.get(7).cloned().unwrap_or(crate::Value::Null), args.get(8).cloned().unwrap_or(crate::Value::Null)),
                "handle_message" => { self.handle_message(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)); crate::Value::Null },
                "hash_message" => self.hash_message(args.get(0).cloned().unwrap_or(crate::Value::Null)),
                "load_api_key" => self.load_api_key().await,
                "load_multi_sign_address" => self.load_multi_sign_address().await,
                "load_quote_token" => self.load_quote_token(args.get(0).cloned().unwrap_or(crate::Value::Null)).await,
                "load_trade_market" => self.load_trade_market(args.get(0).cloned().unwrap_or(crate::Value::Null)).await,
                "opinion_order_raw_amounts" => self.opinion_order_raw_amounts(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), args.get(3).cloned().unwrap_or(crate::Value::Null), args.get(4).cloned().unwrap_or(crate::Value::Null)),
                "opinion_outcome_by_market_id_side" => self.opinion_outcome_by_market_id_side(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
                "opinion_ws_url" => self.opinion_ws_url(),
                "parse_balance" => self.parse_balance(args.get(0).cloned().unwrap_or(crate::Value::Null)),
                "parse_event" => self.parse_event(args.get(0).cloned().unwrap_or(crate::Value::Null)),
                "parse_ohlcv" => self.parse_ohlcv(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
                "parse_opinion_market" => self.parse_opinion_market(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
                "parse_order_status" => self.parse_order_status(args.get(0).cloned().unwrap_or(crate::Value::Null)),
                "parse_prediction_order" => self.parse_prediction_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
                "parse_prediction_position" => self.parse_prediction_position(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
                "parse_prediction_ticker" => self.parse_prediction_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
                "parse_prediction_trade" => self.parse_prediction_trade(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
                "parse_ws_order_status" => self.parse_ws_order_status(args.get(0).cloned().unwrap_or(crate::Value::Null)),
                "ping" => self.ping(args.get(0).cloned().unwrap_or(crate::Value::Null)),
                "seed_order_book" => self.seed_order_book(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), &args.get(2..).unwrap_or(&[]).to_vec()[..]).await,
                "set_api_credentials" => self.set_api_credentials(args.get(0).cloned().unwrap_or(crate::Value::Null)),
                "sign" => self.sign(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
                "sign_api_key_auth" => self.sign_api_key_auth(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null)),
                "sign_hash" => self.sign_hash(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
                "sign_message" => self.sign_message(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
                "sign_opinion_order" => self.sign_opinion_order(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
                "subscribe_opinion_channel" => self.subscribe_opinion_channel(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null)).await,
                "watch_my_trades" => self.watch_my_trades(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "watch_order_book" => self.watch_order_book(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
                "watch_orders" => self.watch_orders(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
                "watch_ticker" => self.watch_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
                "watch_trades" => self.watch_trades(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
                // Prediction Core: fall through to prediction base methods (then Exchange base).
                _ => self.call_dynamic_prediction_base(method, args).await,
            }
        })
    }
}

impl crate::prediction_exchange_generated::PredictionBase for OpinionCore {
    fn pred(&self) -> &crate::prediction_exchange::PredictionExchange { &self.exchange }
    fn pred_mut(&mut self) -> &mut crate::prediction_exchange::PredictionExchange { &mut self.exchange }
}

impl std::ops::Deref for OpinionCore {
    type Target = crate::exchange::Exchange;
    fn deref(&self) -> &crate::exchange::Exchange { std::ops::Deref::deref(&self.exchange) }
}

impl std::ops::DerefMut for OpinionCore {
    fn deref_mut(&mut self) -> &mut crate::exchange::Exchange { std::ops::DerefMut::deref_mut(&mut self.exchange) }
}

impl OpinionCore {
    pub fn describe(&self) -> Value {
        return self.deep_extend(self.super_describe(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), Value::Str("opinion".to_string()));
        m.insert("name".to_string(), Value::Str("Opinion".to_string()));
        m.insert("countries".to_string(), Value::List(vec![Value::Str("HK".to_string())]));
        m.insert("rateLimit".to_string(), Value::Int(67));
        m.insert("certified".to_string(), Value::Bool(false));
        m.insert("pro".to_string(), Value::Bool(true));
        m.insert("has".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("CORS".to_string(), Value::Null);
        m.insert("spot".to_string(), Value::Bool(false));
        m.insert("margin".to_string(), Value::Bool(false));
        m.insert("swap".to_string(), Value::Bool(false));
        m.insert("future".to_string(), Value::Bool(false));
        m.insert("option".to_string(), Value::Bool(false));
        m.insert("cancelOrder".to_string(), Value::Bool(true));
        m.insert("createOrder".to_string(), Value::Bool(true));
        m.insert("fetchBalance".to_string(), Value::Bool(true));
        m.insert("fetchClosedOrders".to_string(), Value::Bool(true));
        m.insert("fetchEvent".to_string(), Value::Bool(true));
        m.insert("fetchEvents".to_string(), Value::Bool(true));
        m.insert("fetchMarkets".to_string(), Value::Bool(true));
        m.insert("fetchMyTrades".to_string(), Value::Bool(true));
        m.insert("fetchOHLCV".to_string(), Value::Bool(true));
        m.insert("fetchOpenOrders".to_string(), Value::Bool(true));
        m.insert("fetchOrder".to_string(), Value::Bool(true));
        m.insert("fetchOrderBook".to_string(), Value::Bool(true));
        m.insert("fetchOrders".to_string(), Value::Bool(true));
        m.insert("fetchPositions".to_string(), Value::Bool(true));
        m.insert("fetchTicker".to_string(), Value::Bool(true));
        m.insert("fetchTickers".to_string(), Value::Bool(true));
        m.insert("prediction".to_string(), Value::Bool(true));
        m.insert("watchMyTrades".to_string(), Value::Bool(true));
        m.insert("watchOrderBook".to_string(), Value::Bool(true));
        m.insert("watchOrders".to_string(), Value::Bool(true));
        m.insert("watchTicker".to_string(), Value::Bool(true));
        m.insert("watchTrades".to_string(), Value::Bool(true));
    m
}));
        m.insert("timeframes".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("1h".to_string(), Value::Str("1h".to_string()));
        m.insert("1d".to_string(), Value::Str("1d".to_string()));
    m
}));
        m.insert("urls".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("logo".to_string(), Value::Str("https://github.com/user-attachments/assets/9905d9d4-3eb3-48d2-bdb3-551a9ddc7559".to_string()));
        m.insert("api".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("opinion".to_string(), Value::Str("https://openapi.opinion.trade/openapi".to_string()));
        m.insert("ws".to_string(), Value::Str("wss://ws.opinion.trade".to_string()));
    m
}));
        m.insert("www".to_string(), Value::Str("https://opinion.trade".to_string()));
        m.insert("doc".to_string(), Value::List(vec![Value::Str("https://docs.opinion.trade".to_string())]));
    m
}));
        m.insert("api".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("opinion".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("public".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("get".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("market".to_string(), Value::Int(1));
        m.insert("market/{marketId}".to_string(), Value::Int(1));
        m.insert("market/categorical/{marketId}".to_string(), Value::Int(1));
        m.insert("market/slug/{slug}".to_string(), Value::Int(1));
        m.insert("label".to_string(), Value::Int(1));
        m.insert("token/latest-price".to_string(), Value::Int(1));
        m.insert("token/orderbook".to_string(), Value::Int(1));
        m.insert("token/price-history".to_string(), Value::Int(1));
        m.insert("quoteToken".to_string(), Value::Int(1));
    m
}));
    m
}));
        m.insert("private".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("get".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("order".to_string(), Value::Int(1));
        m.insert("order/{orderId}".to_string(), Value::Int(1));
        m.insert("positions/user/{walletAddress}".to_string(), Value::Int(1));
        m.insert("trade/user/{walletAddress}".to_string(), Value::Int(1));
        m.insert("auth/api-key".to_string(), Value::Int(1));
        m.insert("user/auth".to_string(), Value::Int(1));
        m.insert("user/balance".to_string(), Value::Int(1));
    m
}));
        m.insert("post".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("auth/api-key".to_string(), Value::Int(1));
        m.insert("order".to_string(), Value::Int(1));
        m.insert("order/cancel".to_string(), Value::Int(1));
    m
}));
        m.insert("delete".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("auth/api-key".to_string(), Value::Int(1));
    m
}));
    m
}));
    m
}));
    m
}));
        m.insert("requiredCredentials".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("apiKey".to_string(), Value::Bool(true));
        m.insert("secret".to_string(), Value::Bool(false));
        m.insert("walletAddress".to_string(), Value::Bool(true));
        m.insert("privateKey".to_string(), Value::Bool(true));
    m
}));
        m.insert("fees".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("trading".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("tierBased".to_string(), Value::Bool(false));
        m.insert("percentage".to_string(), Value::Bool(true));
        m.insert("maker".to_string(), negate(&Value::Float(0.02)));
        m.insert("taker".to_string(), Value::Float(0.04));
    m
}));
    m
}));
        m.insert("exceptions".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("exact".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("10014".to_string(), Value::Str("AccountNotEnabled".to_string()).clone());
        m.insert("10403".to_string(), Value::Str("PermissionDenied".to_string()).clone());
        m.insert("11001".to_string(), Value::Str("AuthenticationError".to_string()).clone());
        m.insert("11002".to_string(), Value::Str("AuthenticationError".to_string()).clone());
        m.insert("10605".to_string(), Value::Str("InvalidOrder".to_string()).clone());
        m.insert("10610".to_string(), Value::Str("InvalidOrder".to_string()).clone());
        m.insert("11004".to_string(), Value::Str("AuthenticationError".to_string()).clone());
        m.insert("11005".to_string(), Value::Str("AuthenticationError".to_string()).clone());
        m.insert("11009".to_string(), Value::Str("AuthenticationError".to_string()).clone());
        m.insert("11010".to_string(), Value::Str("AuthenticationError".to_string()).clone());
        m.insert("11011".to_string(), Value::Str("AuthenticationError".to_string()).clone());
        m.insert("11012".to_string(), Value::Str("AuthenticationError".to_string()).clone());
        m.insert("11013".to_string(), Value::Str("AuthenticationError".to_string()).clone());
    m
}));
        m.insert("broad".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("Insufficient token balance".to_string(), Value::Str("InsufficientFunds".to_string()).clone());
        m.insert("below the minimum required value".to_string(), Value::Str("InvalidOrder".to_string()).clone());
        m.insert("must be the current multi-signature wallet".to_string(), Value::Str("InvalidOrder".to_string()).clone());
    m
}));
    m
}));
        m.insert("streaming".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("keepAlive".to_string(), Value::Int(25000));
    m
}));
        m.insert("options".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("tradesLimit".to_string(), Value::Int(1000));
        m.insert("ordersLimit".to_string(), Value::Int(1000));
        m.insert("myTradesLimit".to_string(), Value::Int(1000));
        m.insert("eventScopeParams".to_string(), Value::List(vec![Value::Str("labelId".to_string())]));
        m.insert("defaultFetchEventsLimit".to_string(), Value::Int(20));
        m.insert("maxFetchEventsResults".to_string(), Value::Int(100));
        m.insert("maxEventsPages".to_string(), Value::Int(50));
        m.insert("marketsPageLimit".to_string(), Value::Int(20));
        m.insert("maxMarketsPages".to_string(), Value::Int(50));
    m
}));
    m
})]);

    Value::Null
}

/*
 * @method
 * @name opinion#fetchMarkets
 * @description fetches every kind of opinion market
 * categorical parents double as our unified "events" and are cached into this.events as a side effect
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/market
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.limit] max number of markets to collect (defaults to options.marketsPageLimit * options.maxMarketsPages, 1000)
 * @returns {object[]} an array of objects representing market data
 */
    pub async fn fetch_markets(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut rest: Value = self.omit(params.clone(), Value::List(vec![Value::Str("limit".to_string())]), &[]);
        let mut userLimit: Value = self.safe_integer_k(params.clone(), "limit", &[]);
        let mut pageLimit: Value = self.safe_integer_k(self.options.clone(), "marketsPageLimit", &[Value::Int(20)]);
        let mut maxPages: Value = self.safe_integer_k(self.options.clone(), "maxMarketsPages", &[Value::Int(50)]);
        let mut flatMarkets: Value = Value::List(vec![]);
        // seen-guard keyed by the event handle; the events themselves go through setEvents below
        // so the cache gets the base indexing (id + handle + slug) instead of a raw assignment
        let mut seenEvents: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut eventsList: Value = Value::List(vec![]);
        let mut page: Value = Value::Int(1);
        let mut fetchedRawCount: Value = Value::Int(0);
        while is_true(&Value::Bool(true)) {
            let mut request: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("marketType".to_string(), Value::Int(2));
                    m.insert("limit".to_string(), pageLimit.clone());
                    m.insert("page".to_string(), page.clone());
                m
            });
            let __ws_arg_0 = self.extend(request.clone(), &[rest.clone()]);
            let mut response: Value = self.opinion_public_get_market(&[__ws_arg_0]).await;
            let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            let mut rawMarkets: Value = self.safe_list_k(result.clone(), "list", &[Value::List(vec![])]);
            let mut rawMarketsLength: Value = get_array_length(&rawMarkets);
            fetchedRawCount = self.sum(&[fetchedRawCount.clone(), rawMarketsLength.clone()]);
            // categorical parents expand into several flatMarkets entries each, so the raw,
            // unflattened row count in 'total' must be compared against fetchedRawCount, not
            // flatMarkets.length - otherwise expansion makes the comparison meaningless
            let mut total: Value = self.safe_integer_k(result.clone(), "total", &[]);
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_1309: bool = true;
                while { if !__for_first_1309 { i = add(&i, &Value::Int(1)); } __for_first_1309 = false; is_less_than(&i, &rawMarketsLength) } {
                let mut raw: Value = get_value(&rawMarkets, &i);
                let mut raw: Value = get_value(&rawMarkets, &i);
                let mut marketType: Value = self.safe_integer_k(raw.clone(), "marketType", &[]);
                if is_equal(&marketType, &Value::Int(1)) {
                    let mut event: Value = self.parse_event(raw.clone());
                    let mut childMarkets: Value = get_value(&event, &Value::Str("markets".to_string()));
                    let mut childMarketsLength: Value = get_array_length(&childMarkets);
                    {
                                                let mut ci: Value = Value::Int(0);
                        let mut __for_first_1308: bool = true;
                        while { if !__for_first_1308 { ci = add(&ci, &Value::Int(1)); } __for_first_1308 = false; is_less_than(&ci, &childMarketsLength) } {
                        append_to_array(&mut flatMarkets, get_value(&childMarkets, &ci));
                    }
                    }
                    let mut eventKey: Value = self.safe_string_k(event.clone(), "event", &[]);
                    if is_true(&(!is_equal(&eventKey, &Value::Null))) && is_true(&(!is_equal(&eventKey, &Value::Str("".to_string())))) && !is_true(&(Value::Bool(in_op(&seenEvents, &eventKey)))) {
                        add_element_to_object(&mut seenEvents, &eventKey, Value::Bool(true));
                        append_to_array(&mut eventsList, event.clone());
                    }
                }  else {
                    append_to_array(&mut flatMarkets, self.parse_opinion_market(raw.clone(), &[]));
                }
            }
            }
            let mut collectedLength: Value = get_array_length(&flatMarkets);
            if is_true(&(is_less_than(&rawMarketsLength, &pageLimit))) || is_true(&(is_greater_than_or_equal(&page, &maxPages))) || is_true(&(is_true(&(!is_equal(&total, &Value::Null))) && is_true(&(is_greater_than_or_equal(&fetchedRawCount, &total))))) || is_true(&(is_true(&(!is_equal(&userLimit, &Value::Null))) && is_true(&(is_greater_than_or_equal(&collectedLength, &userLimit))))) {
                break;
            }
            page = self.sum(&[page.clone(), Value::Int(1)]);
        }
        self.set_events(eventsList.clone());
        let mut flatMarketsLength: Value = get_array_length(&flatMarkets);
        if is_true(&(!is_equal(&userLimit, &Value::Null))) && is_true(&(is_greater_than(&flatMarketsLength, &userLimit))) {
            return self.array_slice(flatMarkets.clone(), Value::Int(0), &[userLimit.clone()]);
        }
        return flatMarkets;

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#fetchOutcome
 * @description resolves a single outcome; a bare numeric token id carries no search text for
 * the base's fetchEvents-driven resolution (opinion has no per-id lookup endpoint, unlike
 * kalshi/polymarket), so bulk-warm the outcome cache via loadOutcomes() first for id-form input
 * @param {string} outcomeSymbol the outcome handle or token id
 * @returns {object} the outcome cache
 */
    pub async fn fetch_outcome(&mut self, mut outcomeSymbol: Value) -> Value {
        if is_equal(&self.outcome_search_query(outcomeSymbol.clone()), &Value::Null) {
            self.load_outcomes(&[]).await;
            if is_true(&self.has_outcome(outcomeSymbol.clone())) {
                return self.safe_outcome(outcomeSymbol.clone(), &[]);
            }
        }
        return self.super_fetch_outcome(outcomeSymbol.clone()).await;

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#parseOpinionMarket
 * @description converts a single raw opinion market into one ccxt market with yes/no outcomes
 * @param {object} raw the raw opinion market object
 * @param {string} [eventSlug] the slug of the parent event
 * @returns {object} a [market structure](https://docs.ccxt.com/#/?id=market-structure)
 */
    pub fn parse_opinion_market(&self, mut raw: Value, optional_args: &[Value]) -> Value {
        let mut eventSlug = get_arg(optional_args, 0, Value::Null);
        // {
        //     "chainId": "56",
        //     "conditionId": "469db44df1309dac7cf9fcaa142562f3c89719d47277e095d021c1561166539a",
        //     "createdAt": 1778750719,
        //     "cutoffAt": 0,
        //     "isResolvableByAI": true,
        //     "marketId": 16565,
        //     "marketTitle": "10–15s",
        //     "noLabel": "No",
        //     "noTokenId": "48641131337815600205538397357674754469573755318952418769815647071528778171134",
        //     "questionId": "63907f471c045499f69b5ea157bdea1942f9d53b6e5e767ae69f4d148aeeefae",
        //     "quoteToken": "0x55d398326f99059fF775485246999027B3197955",
        //     "rebate": {
        //         "maker": 0.5
        //     },
        //     "resolvedAt": 1779342061,
        //     "resultTokenId": "107063188116504514729209026208703521982564071792212276771696073817845504321279",
        //     "rules": "",
        //     "slug": "how-long-will-trump-and-xi-shake-hands-when-they-meet-10-15s",
        //     "status": 4,
        //     "statusEnum": "Resolved",
        //     "volume": "50",
        //     "yesLabel": "Yes",
        //     "yesTokenId": "107063188116504514729209026208703521982564071792212276771696073817845504321279"
        // }
        let mut marketId: Value = self.safe_string_k(raw.clone(), "marketId", &[]);
        let mut slug: Value = self.safe_string_k(raw.clone(), "slug", &[]);
        let mut effectiveEventSlug: Value = eventSlug.clone();
        if is_true(&(!is_equal(&eventSlug, &Value::Null))) && is_true(&(!is_equal(&slug, &Value::Null))) && is_true(&(is_equal(&get_index_of(&slug, &eventSlug), &Value::Int(0)))) {
            effectiveEventSlug = Value::Null;
        }
        let mut marketSymbol: Value = self.slug_to_market_symbol(effectiveEventSlug.clone(), slug.clone());
        let mut statusEnum: Value = self.safe_string_k(raw.clone(), "statusEnum", &[]);
        let mut active: Value = Value::Bool(is_equal(&statusEnum, &Value::Str("Activated".to_string())));
        let mut resolved: Value = Value::Bool(is_equal(&statusEnum, &Value::Str("Resolved".to_string())));
        let mut resultTokenId: Value = self.safe_string_k(raw.clone(), "resultTokenId", &[]);
        let mut hasResult: Value = Value::Bool(is_true(&resolved) && is_true(&(!is_equal(&resultTokenId, &Value::Null))) && is_true(&(!is_equal(&resultTokenId, &Value::Str("".to_string())))));
        let mut outcomeLabels: Value = Value::List(vec![self.safe_string_k(raw.clone(), "yesLabel", &[Value::Str("YES".to_string())]), self.safe_string_k(raw.clone(), "noLabel", &[Value::Str("NO".to_string())])]);
        let mut outcomeTokenIds: Value = Value::List(vec![self.safe_string_k(raw.clone(), "yesTokenId", &[]), self.safe_string_k(raw.clone(), "noTokenId", &[])]);
        let mut outcomes: Value = Value::List(vec![]);
        let mut resolvedOutcome: Value = Value::Null;
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1310: bool = true;
            while { if !__for_first_1310 { i = add(&i, &Value::Int(1)); } __for_first_1310 = false; is_less_than(&i, &get_array_length(&outcomeLabels)) } {
            let mut label: Value = get_value(&outcomeLabels, &i);
            let mut label: Value = get_value(&outcomeLabels, &i);
            let mut tokenId: Value = get_value(&outcomeTokenIds, &i);
            let mut tokenId: Value = get_value(&outcomeTokenIds, &i);
            let mut outcomeHandle: Value = self.slug_to_outcome_symbol(effectiveEventSlug.clone(), slug.clone(), label.clone());
            let mut winner: Value = Value::Null;
            let mut settleFraction: Value = Value::Null;
            if is_true(&hasResult) {
                winner = Value::Bool(is_equal(&tokenId, &resultTokenId));
                settleFraction = ternary(is_true(&winner), Value::Int(1), Value::Int(0));
                if is_true(&winner) {
                    resolvedOutcome = outcomeHandle.clone();
                }
            }
            append_to_array(&mut outcomes, Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("id".to_string(), tokenId.clone());
                    m.insert("outcomeId".to_string(), tokenId.clone());
                    m.insert("outcome".to_string(), outcomeHandle.clone());
                    m.insert("market".to_string(), marketSymbol.clone());
                    m.insert("label".to_string(), label.clone());
                    m.insert("active".to_string(), active.clone());
                    m.insert("winner".to_string(), winner.clone());
                    m.insert("settleFraction".to_string(), settleFraction.clone());
                    m.insert("info".to_string(), raw.clone());
                m
            }));
        }
        }
        let mut marketResolvedOutcome: Value = resolvedOutcome.clone();
        // the venue sends cutoffAt 0 for markets without a scheduled cutoff - map it to
        // undefined instead of the epoch, same for the event-level end date
        let mut expiryTimestamp: Value = Value::Null;
        if !is_equal(&self.safe_integer_k(raw.clone(), "cutoffAt", &[Value::Int(0)]), &Value::Int(0)) {
            expiryTimestamp = self.safe_timestamp(raw.clone(), Value::Str("cutoffAt".to_string()), &[]);
        }
        let mut created: Value = self.safe_timestamp(raw.clone(), Value::Str("createdAt".to_string()), &[]);
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), marketId.clone());
        m.insert("market".to_string(), marketSymbol.clone());
        m.insert("base".to_string(), Value::Str("USDT".to_string()));
        m.insert("quote".to_string(), Value::Str("USDT".to_string()));
        m.insert("settle".to_string(), Value::Null);
        m.insert("baseId".to_string(), marketId.clone());
        m.insert("quoteId".to_string(), Value::Str("USDT".to_string()));
        m.insert("settleId".to_string(), Value::Null);
        m.insert("type".to_string(), Value::Str("prediction".to_string()));
        m.insert("marketType".to_string(), Value::Str("binary".to_string()));
        m.insert("executionModel".to_string(), Value::Str("clob".to_string()));
        m.insert("spot".to_string(), Value::Bool(false));
        m.insert("margin".to_string(), Value::Bool(false));
        m.insert("swap".to_string(), Value::Bool(false));
        m.insert("future".to_string(), Value::Bool(false));
        m.insert("option".to_string(), Value::Bool(false));
        m.insert("prediction".to_string(), Value::Bool(true));
        m.insert("active".to_string(), active.clone());
        m.insert("resolved".to_string(), resolved.clone());
        m.insert("resolvedOutcome".to_string(), marketResolvedOutcome.clone());
        m.insert("contract".to_string(), Value::Bool(false));
        m.insert("linear".to_string(), Value::Null);
        m.insert("inverse".to_string(), Value::Null);
        m.insert("contractSize".to_string(), Value::Null);
        m.insert("expiry".to_string(), expiryTimestamp.clone());
        m.insert("expiryDatetime".to_string(), self.iso8601(expiryTimestamp.clone()));
        m.insert("strike".to_string(), Value::Null);
        m.insert("optionType".to_string(), Value::Null);
        m.insert("taker".to_string(), get_value(&get_value(&self.fees, &Value::Str("trading".to_string())), &Value::Str("taker".to_string())));
        m.insert("maker".to_string(), get_value(&get_value(&self.fees, &Value::Str("trading".to_string())), &Value::Str("maker".to_string())));
        m.insert("percentage".to_string(), Value::Bool(true));
        m.insert("tierBased".to_string(), Value::Bool(false));
        m.insert("feeSide".to_string(), Value::Str("get".to_string()));
        m.insert("precision".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("amount".to_string(), Value::Null);
        m.insert("price".to_string(), Value::Null);
    m
}));
        m.insert("limits".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("leverage".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Int(1));
        m.insert("max".to_string(), Value::Int(1));
    m
}));
        m.insert("amount".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Null);
        m.insert("max".to_string(), Value::Null);
    m
}));
        m.insert("price".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Int(0));
        m.insert("max".to_string(), Value::Int(1));
    m
}));
        m.insert("cost".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Null);
        m.insert("max".to_string(), Value::Null);
    m
}));
    m
}));
        m.insert("outcomes".to_string(), outcomes.clone());
        m.insert("info".to_string(), raw.clone());
        m.insert("created".to_string(), created.clone());
    m
});

    Value::Null
}

/*
 * @method
 * @name opinion#fetchEvents
 * @description fetches Opinion's categorical markets - scope required via query/queries/tags/eventId/slug/labelId
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/market
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.labelId] filter by opinion category label id
 * @param {int} [params.limit] max number of events to fetch (paginated server-side; defaults to options.maxFetchEventsResults, 100)
 * @returns {object[]} an array of event structures
 */
    pub async fn fetch_events(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.require_event_query(&[params.clone()]);
        let mut queries: Value = self.parse_search_queries(&[params.clone()]);
        let mut eventId: Value = self.safe_string_k(params.clone(), "eventId", &[]);
        let mut slug: Value = self.safe_string_k(params.clone(), "slug", &[]);
        if is_true(&(!is_equal(&eventId, &Value::Null))) || is_true(&(!is_equal(&slug, &Value::Null))) {
            let mut singleRest: Value = self.omit(params.clone(), Value::List(vec![Value::Str("eventId".to_string()), Value::Str("slug".to_string()), Value::Str("query".to_string()), Value::Str("queries".to_string()), Value::Str("tags".to_string()), Value::Str("status".to_string()), Value::Str("sort".to_string()), Value::Str("searchIn".to_string()), Value::Str("limit".to_string())]), &[]);
            let mut singleResponse: Value = Value::Null;
            if !is_equal(&slug, &Value::Null) {
                let __ws_arg_1 = self.extend(Value::Map({
                    let mut m = indexmap::IndexMap::new();
                        m.insert("slug".to_string(), slug.clone());
                    m
                }), &[singleRest.clone()]);
                singleResponse = self.opinion_public_get_market_slug_slug(&[__ws_arg_1]).await;
            }  else {
                let __ws_arg_2 = self.extend(Value::Map({
                    let mut m = indexmap::IndexMap::new();
                        m.insert("marketId".to_string(), eventId.clone());
                    m
                }), &[singleRest.clone()]);
                singleResponse = self.opinion_public_get_market_categorical_market_id(&[__ws_arg_2]).await;
            }
            let mut singleResult: Value = self.safe_dict_k(singleResponse.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            let mut singleData: Value = self.safe_dict_k(singleResult.clone(), "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            let mut single: Value = self.parse_event(singleData.clone());
            self.index_event_outcomes(single.clone());
            return self.apply_event_fetch_params(Value::List(vec![single.clone()]), &[params.clone(), queries.clone()]);
        }
        let mut rest: Value = self.omit(params.clone(), Value::List(vec![Value::Str("query".to_string()), Value::Str("queries".to_string()), Value::Str("tags".to_string()), Value::Str("status".to_string()), Value::Str("sort".to_string()), Value::Str("searchIn".to_string()), Value::Str("limit".to_string())]), &[]);
        let mut pageLimit: Value = self.safe_integer_k(self.options.clone(), "defaultFetchEventsLimit", &[Value::Int(20)]);
        let mut userLimit: Value = self.safe_integer_k(params.clone(), "limit", &[]);
        // bound how many events are actually FETCHED: the user limit when given, otherwise
        // options.maxFetchEventsResults - the scope filters keep the listing narrow, but a broad
        // label can still hold more than one page
        let mut fetchCap: Value = self.safe_integer_k(self.options.clone(), "maxFetchEventsResults", &[Value::Int(100)]);
        if !is_equal(&userLimit, &Value::Null) {
            fetchCap = userLimit.clone();
        }
        let mut maxPages: Value = self.safe_integer_k(self.options.clone(), "maxEventsPages", &[Value::Int(50)]);
        let mut rawEvents: Value = Value::List(vec![]);
        let mut page: Value = Value::Int(1);
        let mut fetchedRawCount: Value = Value::Int(0);
        while is_true(&Value::Bool(true)) {
            let mut reqLimit: Value = pageLimit.clone();
            let mut remaining: Value = subtract(&fetchCap, &fetchedRawCount);
            if is_less_than(&remaining, &reqLimit) {
                reqLimit = remaining.clone();
            }
            if is_less_than_or_equal(&reqLimit, &Value::Int(0)) {
                break;
            }
            let mut request: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("marketType".to_string(), Value::Int(1));
                    m.insert("limit".to_string(), reqLimit.clone());
                    m.insert("page".to_string(), page.clone());
                m
            });
            let __ws_arg_3 = self.extend(request.clone(), &[rest.clone()]);
            let mut response: Value = self.opinion_public_get_market(&[__ws_arg_3]).await;
            let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            let mut pageEvents: Value = self.safe_list_k(result.clone(), "list", &[Value::List(vec![])]);
            let mut pageEventsLength: Value = get_array_length(&pageEvents);
            fetchedRawCount = self.sum(&[fetchedRawCount.clone(), pageEventsLength.clone()]);
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_1311: bool = true;
                while { if !__for_first_1311 { i = add(&i, &Value::Int(1)); } __for_first_1311 = false; is_less_than(&i, &pageEventsLength) } {
                append_to_array(&mut rawEvents, get_value(&pageEvents, &i));
            }
            }
            let mut total: Value = self.safe_integer_k(result.clone(), "total", &[]);
            if is_true(&(is_less_than(&pageEventsLength, &reqLimit))) || is_true(&(is_greater_than_or_equal(&page, &maxPages))) || is_true(&(is_true(&(!is_equal(&total, &Value::Null))) && is_true(&(is_greater_than_or_equal(&fetchedRawCount, &total))))) || is_true(&(is_greater_than_or_equal(&fetchedRawCount, &fetchCap))) {
                break;
            }
            page = self.sum(&[page.clone(), Value::Int(1)]);
        }
        let mut rawEventsLength: Value = get_array_length(&rawEvents);
        let mut parsedEvents: Value = Value::List(vec![]);
        if is_equal(&self.markets, &Value::Null) {
            { let __t = self.create_safe_dictionary(&[]); self.markets = __t; }
        }
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1313: bool = true;
            while { if !__for_first_1313 { i = add(&i, &Value::Int(1)); } __for_first_1313 = false; is_less_than(&i, &rawEventsLength) } {
            let mut event: Value = self.parse_event(get_value(&rawEvents, &i));
            append_to_array(&mut parsedEvents, event.clone());
            // register the parsed markets so populateOutcomes can index their outcomes
            let mut eventMarkets: Value = self.safe_list_k(event.clone(), "markets", &[Value::List(vec![])]);
            let mut eventMarketsLength: Value = get_array_length(&eventMarkets);
            {
                                let mut mi: Value = Value::Int(0);
                let mut __for_first_1312: bool = true;
                while { if !__for_first_1312 { mi = add(&mi, &Value::Int(1)); } __for_first_1312 = false; is_less_than(&mi, &eventMarketsLength) } {
                let mut m: Value = get_value(&eventMarkets, &mi);
                let mut m: Value = get_value(&eventMarkets, &mi);
                add_element_to_object(&mut self.markets, &get_value(&m, &Value::Str("market".to_string())), m.clone());
            }
            }
        }
        }
        self.populate_outcomes();
        return self.apply_event_fetch_params(parsedEvents.clone(), &[params.clone(), queries.clone()]);

    Value::Null
}

/*
 * @method
 * @name opinion#fetchEvent
 * @description fetches a single prediction-market event by its market id, or slug
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/market
 * @param {string} id the numeric marketId, or the market slug
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [prediction event structure](https://docs.ccxt.com/#/?id=prediction-event-structure)
 */
    pub async fn fetch_event(&mut self, mut id: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut isSlug: Value = Value::Bool(is_greater_than_or_equal(&get_index_of(&id, &Value::Str("-".to_string())), &Value::Int(0)));
        let mut response: Value = Value::Null;
        if is_true(&isSlug) {
            let __ws_arg_4 = self.extend(Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("slug".to_string(), id.clone());
                m
            }), &[params.clone()]);
            response = self.opinion_public_get_market_slug_slug(&[__ws_arg_4]).await;
        }  else {
            let __ws_arg_5 = self.extend(Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("marketId".to_string(), id.clone());
                m
            }), &[params.clone()]);
            response = self.opinion_public_get_market_categorical_market_id(&[__ws_arg_5]).await;
        }
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut data: Value = self.safe_dict_k(result.clone(), "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut event: Value = self.parse_event(data.clone());
        self.index_event_outcomes(event.clone());
        return event;

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#parseEvent
 * @description parses a raw opinion categorical market (with nested childMarkets) into the unified event shape
 * @param {object} rawEvent the raw opinion categorical market object
 * @returns {object} an event structure
 */
    pub fn parse_event(&self, mut rawEvent: Value) -> Value {
        // {
        //     "chainId": "56",
        //     "childMarkets": [
        //         {
        //             "chainId": "56",
        //             "conditionId": "469db44df1309dac7cf9fcaa142562f3c89719d47277e095d021c1561166539a",
        //             "createdAt": 1778750719,
        //             "cutoffAt": 0,
        //             "isResolvableByAI": true,
        //             "marketId": 16565,
        //             "marketTitle": "10–15s",
        //             "noLabel": "No",
        //             "noTokenId": "48641131337815600205538397357674754469573755318952418769815647071528778171134",
        //             "questionId": "63907f471c045499f69b5ea157bdea1942f9d53b6e5e767ae69f4d148aeeefae",
        //             "quoteToken": "0x55d398326f99059fF775485246999027B3197955",
        //             "rebate": {
        //                 "maker": 0.5
        //             },
        //             "resolvedAt": 1779342061,
        //             "resultTokenId": "107063188116504514729209026208703521982564071792212276771696073817845504321279",
        //             "rules": "",
        //             "slug": "how-long-will-trump-and-xi-shake-hands-when-they-meet-10-15s",
        //             "status": 4,
        //             "statusEnum": "Resolved",
        //             "volume": "50",
        //             "yesLabel": "Yes",
        //             "yesTokenId": "107063188116504514729209026208703521982564071792212276771696073817845504321279"
        //         },
        //         {
        //             "chainId": "56",
        //             "conditionId": "c4054e23e1afc96c93bbc05378414f8286b2f3e3107bb80944ba8a7581428812",
        //             "createdAt": 1778750720,
        //             "cutoffAt": 0,
        //             "isResolvableByAI": true,
        //             "marketId": 16566,
        //             "marketTitle": "15s+",
        //             "noLabel": "No",
        //             "noTokenId": "47010463515333704767569479601138504130354011266006912230152057656074556410652",
        //             "questionId": "3c78ac485d772b8241fd185f8b45ef9d7df9690d99ea59ed58927ade2f50b182",
        //             "quoteToken": "0x55d398326f99059fF775485246999027B3197955",
        //             "rebate": {
        //                 "maker": 0.5
        //             },
        //             "resolvedAt": 1779342061,
        //             "resultTokenId": "47010463515333704767569479601138504130354011266006912230152057656074556410652",
        //             "rules": "",
        //             "slug": "how-long-will-trump-and-xi-shake-hands-when-they-meet-15s-plus",
        //             "status": 4,
        //             "statusEnum": "Resolved",
        //             "volume": "50",
        //             "yesLabel": "Yes",
        //             "yesTokenId": "4219962057210945760892475434030882254029819241100485445656470801524365584613"
        //         }
        //     ],
        //     "conditionId": "",
        //     "coverUrl": "",
        //     "createdAt": 1778750719,
        //     "cutoffAt": 1778803200,
        //     "isResolvableByAI": false,
        //     "labelIds": [
        //         1
        //     ],
        //     "labels": [
        //         "Politics"
        //     ],
        //     "marketId": 788,
        //     "marketTitle": "How long will Trump and Xi shake hands when they meet?",
        //     "marketType": 1,
        //     "noLabel": "",
        //     "noTokenId": "",
        //     "questionId": "63907f471c045499f69b5ea157bdea1942f9d53b6e5e767ae69f4d148aeeefae",
        //     "quoteToken": "0x55d398326f99059fF775485246999027B3197955",
        //     "rebate": {
        //         "maker": 0.5
        //     },
        //     "resolvedAt": 0,
        //     "resultTokenId": "",
        //     "rules": "This market resolves based on the length of the longest qualifying handshake between Donald Trump and Xi Jinping on May 14, 2026 (Beijing local time), the day of their bilateral meeting at the Great Hall of the People.\nBaseline already established: Video footage from the welcome ceremony confirms a qualifying handshake within the 10–15s range. The remaining question is whether any additional handshake on the same day produces a longer measured duration.\nOutcomes:\n\n10–15s — resolves YES if no qualifying handshake on May 14, 2026 exceeds 15 seconds.\n15s+ — resolves YES if any qualifying handshake on May 14, 2026 is measured at more than 15 seconds.\n\nMeasurement: Duration is measured from the exact moment hands make initial physical contact until the exact moment either party breaks contact. Where multiple video sources exist, the highest-resolution footage is used; where measurements differ, the consensus reading across major media is applied. A duration falling exactly on the 15s boundary resolves to 15s+.\nQualifying handshake: voluntary, intentional, in-person; direct hand-to-hand contact (gloves permitted); clearly visible on video from start to finish. Fist bumps, hugs, waves, back pats, and other non-handshake contact are excluded from the measured duration even if they occur during the same greeting.\nResolution window: All qualifying handshakes occurring on May 14, 2026 in Beijing local time are eligible, including those at arrival, bilateral sessions, signing ceremonies, state dinners, and departure. Handshakes on any other date do not count.\nResolution source: video footage from May 14, 2026.",
        //     "slug": "how-long-will-trump-and-xi-shake-hands-when-they-meet",
        //     "status": 1,
        //     "statusEnum": "Created",
        //     "thumbnailUrl": "https://images.opinion.trade/0xf988d66bd9c46b69d33e2703f7264d3c2267136c/0xdf583bdff183f389d7c5d9c5d099127cabda824afd05d66059b8027ca775763d",
        //     "volume": "100.00",
        //     "volume24h": "100.00",
        //     "volume7d": "100.00",
        //     "yesLabel": "",
        //     "yesTokenId": ""
        // }
        let mut eventId: Value = self.safe_string_k(rawEvent.clone(), "marketId", &[]);
        let mut slug: Value = self.safe_string_k(rawEvent.clone(), "slug", &[]);
        let mut title: Value = self.safe_string_k(rawEvent.clone(), "marketTitle", &[]);
        let mut eventHandle: Value = ternary(is_true(&(!is_equal(&title, &Value::Null))), self.shorten_slug(title.clone()), self.shorten_slug(slug.clone()));
        let mut rawChildren: Value = self.safe_list_k(rawEvent.clone(), "childMarkets", &[Value::List(vec![])]);
        let mut rawChildrenLength: Value = get_array_length(&rawChildren);
        let mut marketsList: Value = Value::List(vec![]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1314: bool = true;
            while { if !__for_first_1314 { i = add(&i, &Value::Int(1)); } __for_first_1314 = false; is_less_than(&i, &rawChildrenLength) } {
            append_to_array(&mut marketsList, self.parse_opinion_market(get_value(&rawChildren, &i), &[slug.clone()]));
        }
        }
        let mut statusEnum: Value = self.safe_string_k(rawEvent.clone(), "statusEnum", &[]);
        let mut active: Value = Value::Bool(is_equal(&statusEnum, &Value::Str("Activated".to_string())));
        let mut resolved: Value = Value::Bool(is_equal(&statusEnum, &Value::Str("Resolved".to_string())));
        let mut end: Value = Value::Null;
        if !is_equal(&self.safe_integer_k(rawEvent.clone(), "cutoffAt", &[Value::Int(0)]), &Value::Int(0)) {
            end = self.safe_timestamp(rawEvent.clone(), Value::Str("cutoffAt".to_string()), &[]);
        }
        let mut created: Value = self.safe_timestamp(rawEvent.clone(), Value::Str("createdAt".to_string()), &[]);
        let mut labels: Value = self.safe_list_k(rawEvent.clone(), "labels", &[Value::List(vec![])]);
        let __ws_arg_6 = self.safe_string_k(rawEvent.clone(), "rules", &[]);
        let __ws_arg_7 = self.safe_string(labels.clone(), Value::Int(0), &[]);
        let __ws_arg_8 = self.safe_number_k(rawEvent.clone(), "volume", &[]);
        let __ws_arg_9 = self.iso8601(created.clone());
        let __ws_arg_10 = self.iso8601(end.clone());
        let __ws_arg_11 = self.safe_string2(rawEvent.clone(), Value::Str("coverUrl".to_string()), Value::Str("thumbnailUrl".to_string()), &[]);
        return self.extend(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), eventId.clone());
        m.insert("event".to_string(), eventHandle.clone());
        m.insert("title".to_string(), title.clone());
        m.insert("description".to_string(), __ws_arg_6);
        m.insert("slug".to_string(), slug.clone());
        m.insert("category".to_string(), __ws_arg_7);
        m.insert("tags".to_string(), labels.clone());
        m.insert("markets".to_string(), marketsList.clone());
        m.insert("active".to_string(), active.clone());
        m.insert("resolved".to_string(), resolved.clone());
        m.insert("volume".to_string(), __ws_arg_8);
        m.insert("created".to_string(), created.clone());
        m.insert("createdDatetime".to_string(), __ws_arg_9);
        m.insert("end".to_string(), end.clone());
        m.insert("endDatetime".to_string(), __ws_arg_10);
        m.insert("image".to_string(), __ws_arg_11);
        m.insert("info".to_string(), rawEvent.clone());
    m
}), &[]);

    Value::Null
}

/*
 * @method
 * @name opinion#fetchTicker
 * @description fetches the latest trade price and top of book for a single outcome token
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/token
 * @param {string} outcome unified outcome or outcome token id
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [prediction ticker structure](https://docs.ccxt.com/#/?id=prediction-ticker-structure)
 */
    pub async fn fetch_ticker(&mut self, mut outcome: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut outcomeObj: Value = self.load_outcome(outcome.clone(), &[]).await;
        let mut tokenId: Value = get_value(&outcomeObj, &Value::Str("outcomeId".to_string()));
        let __ws_arg_12 = self.extend(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("token_id".to_string(), tokenId.clone());
    m
}), &[params.clone()]);
        let mut promises: Value = Value::List(vec![self.opinion_public_get_token_latest_price(&[__ws_arg_12]).await, self.opinion_public_get_token_orderbook(&[self.extend(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("token_id".to_string(), tokenId.clone());
    m
}), &[params.clone()])]).await]);
        let mut priceResponsebookResponseVariable = promise_all(&promises).await;
        let mut priceResponse: Value = get_value(&priceResponsebookResponseVariable, &Value::Int(0));
        let mut bookResponse: Value = get_value(&priceResponsebookResponseVariable, &Value::Int(1));
        let mut response: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("price".to_string(), priceResponse.clone());
                m.insert("book".to_string(), bookResponse.clone());
            m
        });
        return self.parse_prediction_ticker(response.clone(), &[outcomeObj.clone()]);

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#parsePredictionTicker
 * @description parses a raw opinion latest-price + orderbook pair into a unified ticker object
 * @param {object} ticker a { price, book } dict of the two raw responses
 * @param {object} [market] the outcome object the ticker belongs to
 * @returns {object} a [prediction ticker structure](https://docs.ccxt.com/#/?id=prediction-ticker-structure)
 */
    pub fn parse_prediction_ticker(&self, mut ticker: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        //     {
        //         "price": {
        //             "errmsg": "",
        //             "errno": 0,
        //             "result": { "price": "0.002", "side": "buy-limit", "size": "205.03", "timestamp": 1766844546000, "tokenId": "..." }
        //         },
        //         "book": {
        //             "errmsg": "",
        //             "errno": 0,
        //             "result": { "asks": [ { "price": "0.999", "size": "5500" } ], "bids": [], "market": "...", "timestamp": ..., "tokenId": "..." }
        //         }
        //     }
        //
        let mut marketAny: Value = market.clone();
        let mut priceResponse: Value = self.safe_dict_k(ticker.clone(), "price", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut priceResult: Value = self.safe_dict_k(priceResponse.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut bookResponse: Value = self.safe_dict_k(ticker.clone(), "book", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut bookResult: Value = self.safe_dict_k(bookResponse.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut bids: Value = self.safe_list_k(bookResult.clone(), "bids", &[Value::List(vec![])]);
        let mut asks: Value = self.safe_list_k(bookResult.clone(), "asks", &[Value::List(vec![])]);
        let mut bestBid: Value = self.safe_dict(bids.clone(), Value::Int(0), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut bestAsk: Value = self.safe_dict(asks.clone(), Value::Int(0), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut last: Value = self.safe_number_k(priceResult.clone(), "price", &[]);
        let mut timestamp: Value = self.safe_integer_k(priceResult.clone(), "timestamp", &[self.milliseconds()]);
        return self.safe_prediction_ticker(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("outcome".to_string(), self.safe_string_k(marketAny.clone(), "outcome", &[]));
        m.insert("outcomeId".to_string(), self.safe_string2(marketAny.clone(), Value::Str("outcomeId".to_string()), Value::Str("id".to_string()), &[]));
        m.insert("label".to_string(), self.safe_string_k(marketAny.clone(), "label", &[]));
        m.insert("market".to_string(), self.safe_string2(marketAny.clone(), Value::Str("market".to_string()), Value::Str("outcome".to_string()), &[]));
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp.clone()));
        m.insert("high".to_string(), Value::Null);
        m.insert("low".to_string(), Value::Null);
        m.insert("bid".to_string(), self.safe_number_k(bestBid.clone(), "price", &[]));
        m.insert("bidVolume".to_string(), self.safe_number_k(bestBid.clone(), "size", &[]));
        m.insert("ask".to_string(), self.safe_number_k(bestAsk.clone(), "price", &[]));
        m.insert("askVolume".to_string(), self.safe_number_k(bestAsk.clone(), "size", &[]));
        m.insert("open".to_string(), Value::Null);
        m.insert("close".to_string(), last.clone());
        m.insert("last".to_string(), last.clone());
        m.insert("change".to_string(), Value::Null);
        m.insert("percentage".to_string(), Value::Null);
        m.insert("average".to_string(), Value::Null);
        m.insert("baseVolume".to_string(), Value::Null);
        m.insert("quoteVolume".to_string(), Value::Null);
        m.insert("info".to_string(), ticker.clone());
    m
}), &[market.clone()]);

    Value::Null
}

/*
 * @method
 * @name opinion#fetchTickers
 * @description fetches tickers for multiple outcome tokens - opinion has no all-tickers endpoint, each token needs its own latest-price + orderbook request
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/token
 * @param {string[]} outcomes unified outcomes or outcome token ids - required, opinion has no all-tickers endpoint
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a dictionary of [prediction ticker structures](https://docs.ccxt.com/#/?id=prediction-ticker-structure) indexed by outcome
 */
    pub async fn fetch_tickers(&mut self, optional_args: &[Value]) -> Value {
        let mut outcomes = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&outcomes, &Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(add(&self.id, &Value::Str(" fetchTickers() requires an outcomes argument — the venue has no all-tickers endpoint; pass the outcome handles or token ids to fetch (discover them via fetchEvents ())".to_string()))));
        }
        self.load_outcomes(&[outcomes.clone()]).await;
        let mut outcomesLength: Value = get_array_length(&outcomes);
        let mut promises: Value = Value::List(vec![]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1315: bool = true;
            while { if !__for_first_1315 { i = add(&i, &Value::Int(1)); } __for_first_1315 = false; is_less_than(&i, &outcomesLength) } {
            let mut outcomeObj: Value = self.outcome(get_value(&outcomes, &i));
            let mut tokenId: Value = get_value(&outcomeObj, &Value::Str("outcomeId".to_string()));
            let __ws_arg_13 = self.extend(Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("token_id".to_string(), tokenId.clone());
                m
            }), &[params.clone()]);
            append_to_array(&mut promises, self.opinion_public_get_token_latest_price(&[__ws_arg_13]).await);
            let __ws_arg_14 = self.extend(Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("token_id".to_string(), tokenId.clone());
                m
            }), &[params.clone()]);
            append_to_array(&mut promises, self.opinion_public_get_token_orderbook(&[__ws_arg_14]).await);
        }
        }
        let mut responses: Value = promise_all(&promises).await;
        let mut result: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1316: bool = true;
            while { if !__for_first_1316 { i = add(&i, &Value::Int(1)); } __for_first_1316 = false; is_less_than(&i, &outcomesLength) } {
            let mut outcomeObj: Value = self.outcome(get_value(&outcomes, &i));
            let mut priceIndex: Value = multiply(&i, &Value::Int(2));
            let mut priceResponse: Value = get_value(&responses, &priceIndex);
            let mut priceResponse: Value = get_value(&responses, &priceIndex);
            let mut bookResponse: Value = get_value(&responses, &self.sum(&[priceIndex.clone(), Value::Int(1)]));
            let mut response: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("price".to_string(), priceResponse.clone());
                    m.insert("book".to_string(), bookResponse.clone());
                m
            });
            let mut ticker: Value = self.parse_prediction_ticker(response.clone(), &[outcomeObj.clone()]);
            let mut symbolKey: Value = self.safe_string_k(ticker.clone(), "outcome", &[]);
            if !is_equal(&symbolKey, &Value::Null) {
                add_element_to_object(&mut result, &symbolKey, ticker.clone());
            }
        }
        }
        return result;

    Value::Null
}

/*
 * @method
 * @name opinion#fetchOrderBook
 * @description fetches the order book for a single outcome token
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/token
 * @param {string} outcome unified outcome or outcome token id
 * @param {int} [limit] not used by opinion fetchOrderBook
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [prediction order book structure](https://docs.ccxt.com/#/?id=prediction-order-book-structure)
 */
    pub async fn fetch_order_book(&mut self, mut outcome: Value, optional_args: &[Value]) -> Value {
        let mut limit = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut outcomeObj: Value = self.load_outcome(outcome.clone(), &[]).await;
        let mut tokenId: Value = get_value(&outcomeObj, &Value::Str("outcomeId".to_string()));
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("token_id".to_string(), tokenId.clone());
            m
        });
        let __ws_arg_15 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.opinion_public_get_token_orderbook(&[__ws_arg_15]).await;
        //
        //     {
        //         "errmsg": "",
        //         "errno": 0,
        //         "result": {
        //             "asks": [
        //                 { "price": "0.999", "size": "5500" }
        //             ],
        //             "bids": [],
        //             "market": "ff7d2d935d0cce2922ea05a363e5a87439e1f8f86f01dacf7238d4c4cc542f6c",
        //             "timestamp": 1785488076901,
        //             "tokenId": "56915117085756475550546730127709511264652860289185956800398231821503615918119"
        //         }
        //     }
        //
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut timestamp: Value = self.safe_integer_k(result.clone(), "timestamp", &[]);
        let mut orderbook: Value = self.parse_order_book(result.clone(), self.safe_outcome_symbol(outcome.clone(), &[outcomeObj.clone()]), &[timestamp.clone(), Value::Str("bids".to_string()), Value::Str("asks".to_string()), Value::Str("price".to_string()), Value::Str("size".to_string())]);
        return self.safe_prediction_order_book(orderbook.clone(), &[outcomeObj.clone()]);

    Value::Null
}

/*
 * @method
 * @name opinion#fetchOHLCV
 * @description fetches historical candlestick data for an outcome token
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/token
 * @param {string} outcome unified outcome or outcome token id
 * @param {string} timeframe the length of time each candle represents - only '1h' and '1d' are supported live
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum number of candles to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int[][]} a list of candles ordered as timestamp, open, high, low, close, volume
 */
    pub async fn fetch_ohlcv(&mut self, mut outcome: Value, optional_args: &[Value]) -> Value {
        let mut timeframe = get_arg(optional_args, 0, Value::Str("1d".to_string()));
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if !is_true(&(Value::Bool(in_op(&self.timeframes, &timeframe)))) {
            let mut supportedKeys: Value = object_keys(&self.timeframes);
            panic!("{}", crate::exchange_errors::bad_request(add(&add(&add(&add(&self.id, &Value::Str(" fetchOHLCV() unsupported timeframe ".to_string())), &timeframe), &Value::Str(", supported timeframes are ".to_string())), &join(&supportedKeys, &Value::Str(", ".to_string())))));
        }
        let mut outcomeObj: Value = self.load_outcome(outcome.clone(), &[]).await;
        let mut tokenId: Value = get_value(&outcomeObj, &Value::Str("outcomeId".to_string()));
        let mut interval: Value = self.safe_string(self.timeframes.clone(), timeframe.clone(), &[]);
        let __ws_arg_16 = self.extend(Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("token_id".to_string(), tokenId.clone());
                m.insert("interval".to_string(), interval.clone());
            m
        }), &[params.clone()]);
        let mut response: Value = self.opinion_public_get_token_price_history(&[__ws_arg_16]).await;
        //
        //     {
        //         "errmsg": "",
        //         "errno": 0,
        //         "result": {
        //             "history": [
        //                 { "p": "0.001", "t": 1785495600 }
        //             ]
        //         }
        //     }
        //
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut history: Value = self.safe_list_k(result.clone(), "history", &[Value::List(vec![])]);
        let mut candles: Value = Value::List(vec![]);
        let mut historyLength: Value = get_array_length(&history);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1317: bool = true;
            while { if !__for_first_1317 { i = add(&i, &Value::Int(1)); } __for_first_1317 = false; is_less_than(&i, &historyLength) } {
            let mut point: Value = get_value(&history, &i);
            let mut point: Value = get_value(&history, &i);
            let mut price: Value = self.safe_number_k(point.clone(), "p", &[]);
            let mut timestamp: Value = self.safe_timestamp(point.clone(), Value::Str("t".to_string()), &[]);
            if is_true(&(!is_equal(&price, &Value::Null))) && is_true(&(!is_equal(&timestamp, &Value::Null))) {
                append_to_array(&mut candles, Value::List(vec![timestamp.clone(), price.clone(), price.clone(), price.clone(), price.clone(), Value::Null]));
            }
        }
        }
        let mut sorted: Value = self.sort_by(candles.clone(), Value::Int(0), &[]);
        return self.filter_by_since_limit(sorted.clone(), &[since.clone(), limit.clone(), Value::Int(0)]);

    Value::Null
}

/*
 * @method
 * @name opinion#parseOHLCV
 * @description parses a single opinion price-history point into a unified OHLCV candle
 * @param {object} ohlcv the raw { p, t } point
 * @param {object} [market] the outcome object the candle belongs to
 * @returns {int[]} a candle ordered as timestamp, open, high, low, close, volume
 */
    pub fn parse_ohlcv(&self, mut ohlcv: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        // Unused: fetchOHLCV maps { p, t } points directly.
        //
        //     { "p": "0.001", "t": 1785495600 }
        //
        let mut price: Value = self.safe_number_k(ohlcv.clone(), "p", &[]);
        return Value::List(vec![self.safe_timestamp(ohlcv.clone(), Value::Str("t".to_string()), &[]), price.clone(), price.clone(), price.clone(), price.clone(), Value::Null]);

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#loadQuoteToken
 * @description fetches and caches quote-token metadata needed to sign orders
 * @param {string} quoteTokenAddress the on-chain quote-token contract address, read from a 'quoteToken' field
 * @returns {object} the matching quote-token entry
 */
    pub async fn load_quote_token(&mut self, mut quoteTokenAddress: Value) -> Value {
        if is_equal(&quoteTokenAddress, &Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(add(&self.id, &Value::Str(" loadQuoteToken() requires a quoteTokenAddress".to_string()))));
        }
        let mut cacheKey: Value = to_lower(&quoteTokenAddress);
        let mut cached: Value = self.safe_dict_k(self.options.clone(), "quoteTokens", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut existing: Value = self.safe_dict(cached.clone(), cacheKey.clone(), &[]);
        if !is_equal(&existing, &Value::Null) {
            return existing;
        }
        let mut response: Value = self.opinion_public_get_quote_token(&[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]).await;
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut list: Value = self.safe_list_k(result.clone(), "list", &[Value::List(vec![])]);
        let mut listLength: Value = get_array_length(&list);
        let mut quoteTokens: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1318: bool = true;
            while { if !__for_first_1318 { i = add(&i, &Value::Int(1)); } __for_first_1318 = false; is_less_than(&i, &listLength) } {
            let mut entry: Value = get_value(&list, &i);
            let mut entry: Value = get_value(&list, &i);
            let mut address: Value = self.safe_string_lower(entry.clone(), Value::Str("quoteTokenAddress".to_string()), &[]);
            if !is_equal(&address, &Value::Null) {
                add_element_to_object(&mut quoteTokens, &address, entry.clone());
            }
        }
        }
        add_element_to_object(&mut self.options, &Value::Str("quoteTokens".to_string()), quoteTokens.clone());
        let mut quoteToken: Value = self.safe_dict(quoteTokens.clone(), cacheKey.clone(), &[]);
        if is_equal(&quoteToken, &Value::Null) {
            panic!("{}", crate::exchange_errors::exchange_error(add(&add(&self.id, &Value::Str(" loadQuoteToken() could not find quote token ".to_string())), &quoteTokenAddress)));
        }
        return quoteToken;

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#loadMultiSignAddress
 * @description fetches and caches the per-wallet multi-signature address that owns order assets
 * @returns {string} the multi-sig wallet address for this.walletAddress on chain 56, or this.walletAddress itself if none exists yet
 */
    pub async fn load_multi_sign_address(&mut self) -> Value {
        let mut cached: Value = self.safe_string_k(self.options.clone(), "multiSignAddress", &[]);
        if !is_equal(&cached, &Value::Null) {
            return cached;
        }
        let mut response: Value = self.opinion_private_get_user_auth(&[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]).await;
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut walletUsers: Value = self.safe_dict_k(result.clone(), "walletUsers", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut multiSignAddress: Value = self.safe_string_k(walletUsers.clone(), "56", &[self.walletAddress.clone()]);
        add_element_to_object(&mut self.options, &Value::Str("multiSignAddress".to_string()), multiSignAddress.clone());
        return multiSignAddress;

    Value::Null
}

    pub fn sign_opinion_order(&self, mut order: Value, mut exchangeAddress: Value) -> Value {
        let mut domain: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("name".to_string(), Value::Str("OPINION CTF Exchange".to_string()));
                m.insert("version".to_string(), Value::Str("1".to_string()));
                m.insert("chainId".to_string(), Value::Int(56));
                m.insert("verifyingContract".to_string(), exchangeAddress.clone());
            m
        });
        let mut messageTypes: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("Order".to_string(), Value::List(vec![Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("salt".to_string()));
        m.insert("type".to_string(), Value::Str("uint256".to_string()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("maker".to_string()));
        m.insert("type".to_string(), Value::Str("address".to_string()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("signer".to_string()));
        m.insert("type".to_string(), Value::Str("address".to_string()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("taker".to_string()));
        m.insert("type".to_string(), Value::Str("address".to_string()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("tokenId".to_string()));
        m.insert("type".to_string(), Value::Str("uint256".to_string()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("makerAmount".to_string()));
        m.insert("type".to_string(), Value::Str("uint256".to_string()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("takerAmount".to_string()));
        m.insert("type".to_string(), Value::Str("uint256".to_string()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("expiration".to_string()));
        m.insert("type".to_string(), Value::Str("uint256".to_string()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("nonce".to_string()));
        m.insert("type".to_string(), Value::Str("uint256".to_string()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("feeRateBps".to_string()));
        m.insert("type".to_string(), Value::Str("uint256".to_string()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("side".to_string()));
        m.insert("type".to_string(), Value::Str("uint8".to_string()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("signatureType".to_string()));
        m.insert("type".to_string(), Value::Str("uint8".to_string()));
    m
})]));
            m
        });
        let mut encoded: Value = self.eth_encode_structured_data(domain.clone(), messageTypes.clone(), order.clone());
        let mut sig: Value = self.sign_message(encoded.clone(), self.privateKey.clone());
        return add(&add(&add(&Value::Str("0x".to_string()), &self.remove0x_prefix(get_value(&sig, &Value::Str("r".to_string())))), &self.remove0x_prefix(get_value(&sig, &Value::Str("s".to_string())))), &self.int_to_base16(get_value(&sig, &Value::Str("v".to_string())), &[]));

    Value::Null
}

    pub fn opinion_order_raw_amounts(&self, mut isMarket: Value, mut side: Value, mut amount: Value, mut price: Value, mut decimals: Value) -> Value {
        let mut decimalsStr: Value = Value::Str("1".to_string());
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1319: bool = true;
            while { if !__for_first_1319 { i = add(&i, &Value::Int(1)); } __for_first_1319 = false; is_less_than(&i, &decimals) } {
            decimalsStr = add(&decimalsStr, &Value::Str("0".to_string()));
        }
        }
        let mut amountStr: Value = self.number_to_string(amount.clone());
        if is_true(&isMarket) && is_true(&(is_equal(&side, &Value::Str("BUY".to_string())))) {
            let mut marketMakerAmountWei: Value = self.decimal_to_precision(crate::precise::Precise::stringMul(&amountStr, &decimalsStr), Value::Int(crate::runtime::TRUNCATE), Value::Int(0), &[Value::Int(crate::runtime::DECIMAL_PLACES)]);
            return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("makerAmount".to_string(), marketMakerAmountWei.clone());
        m.insert("takerAmount".to_string(), Value::Str("0".to_string()));
    m
});
        }
        let mut priceStr: Value = self.decimal_to_precision(self.number_to_string(price.clone()), Value::Int(crate::runtime::ROUND), Value::Int(6), &[Value::Int(crate::runtime::DECIMAL_PLACES)]);
        let mut priceParts: Value = split(&priceStr, &Value::Str(".".to_string()));
        let mut priceInt: Value = self.safe_string(priceParts.clone(), Value::Int(0), &[Value::Str("0".to_string())]);
        let mut priceFrac: Value = self.safe_string(priceParts.clone(), Value::Int(1), &[Value::Str("".to_string())]);
        let mut priceDenom: Value = Value::Str("1000000".to_string());
        let mut priceNum: Value = crate::precise::Precise::stringAdd(&crate::precise::Precise::stringMul(&priceInt, &priceDenom), &pad_end(&priceFrac, &Value::Int(6), &Value::Str("0".to_string())));
        if is_equal(&priceNum, &Value::Str("0".to_string())) {
            panic!("{}", crate::exchange_errors::invalid_order(add(&add(&self.id, &Value::Str(" createOrder() invalid price ".to_string())), &priceStr)));
        }
        let mut makerRaw: Value = amountStr.clone();
        if is_equal(&side, &Value::Str("BUY".to_string())) {
            makerRaw = crate::precise::Precise::stringMul(&amountStr, &priceStr);
        }
        let mut makerAmountWei: Value = self.decimal_to_precision(crate::precise::Precise::stringMul(&makerRaw, &decimalsStr), Value::Int(crate::runtime::TRUNCATE), Value::Int(0), &[Value::Int(crate::runtime::DECIMAL_PLACES)]);
        let mut makerAmount: Value = Value::Null;
        let mut takerAmount: Value = Value::Null;
        if is_equal(&side, &Value::Str("BUY".to_string())) {
            let mut k: Value = crate::precise::Precise::stringDivPrec(&makerAmountWei, &priceNum, &Value::Int(0));
            makerAmount = crate::precise::Precise::stringMul(&k, &priceNum);
            takerAmount = crate::precise::Precise::stringMul(&k, &priceDenom);
        }  else {
            let mut k: Value = crate::precise::Precise::stringDivPrec(&makerAmountWei, &priceDenom, &Value::Int(0));
            makerAmount = crate::precise::Precise::stringMul(&k, &priceDenom);
            takerAmount = crate::precise::Precise::stringMul(&k, &priceNum);
        }
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("makerAmount".to_string(), makerAmount.clone());
        m.insert("takerAmount".to_string(), takerAmount.clone());
    m
});

    Value::Null
}

/*
 * @method
 * @name opinion#createOrder
 * @description places a limit or market order on the CLOB for the given outcome token
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/order
 * @param {string} outcome unified outcome or outcome token id
 * @param {string} type 'market' or 'limit'
 * @param {string} side 'buy' or 'sell'
 * @param {float} amount for limit orders, the number of outcome shares to trade; for market orders, the quote (USDT) to spend on a BUY or the shares to sell on a SELL
 * @param {float} [price] the price per outcome token between 0 and 1; required for limit orders and market SELL orders (where it acts as the reference / worst acceptable price for the taker amount); ignored for market BUY orders (amount is already the quote to spend)
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {bool} [params.postOnly] limit orders only - reject the order if it would cross the spread
 * @returns {object} a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
 */
    pub async fn create_order(&mut self, mut outcome: Value, mut type_var: Value, mut side: Value, mut amount: Value, optional_args: &[Value]) -> Value {
        let mut price = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_api_key().await;
        self.check_required_credentials(&[]);
        let mut outcomeObj: Value = self.load_outcome(outcome.clone(), &[]).await;
        let mut tokenId: Value = get_value(&outcomeObj, &Value::Str("outcomeId".to_string()));
        let mut isMarket: Value = Value::Bool(is_equal(&type_var, &Value::Str("market".to_string())));
        let mut sideStr: Value = to_upper(&side);
        if is_equal(&price, &Value::Null) {
            if !is_true(&isMarket) {
                panic!("{}", crate::exchange_errors::arguments_required(add(&self.id, &Value::Str(" createOrder() requires a price for limit orders".to_string()))));
            }
            if is_equal(&sideStr, &Value::Str("SELL".to_string())) {
                panic!("{}", crate::exchange_errors::arguments_required(add(&self.id, &Value::Str(" createOrder() requires a price for market sell orders".to_string()))));
            }
        }
        let mut marketOrderPrice: Value = Value::Str("0".to_string());
        if is_true(&isMarket) && is_true(&(is_equal(&sideStr, &Value::Str("SELL".to_string())))) {
            marketOrderPrice = self.number_to_string(price.clone());
        }
        let mut info: Value = self.safe_dict_k(outcomeObj.clone(), "info", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut topicId: Value = self.safe_integer_k(info.clone(), "marketId", &[]);
        let mut quoteTokenAddress: Value = self.safe_string_k(info.clone(), "quoteToken", &[]);
        let mut quoteToken: Value = self.load_quote_token(quoteTokenAddress.clone()).await;
        let mut exchangeAddress: Value = self.safe_string_k(quoteToken.clone(), "ctfExchangeAddress", &[Value::Str("".to_string())]);
        let mut decimals: Value = self.safe_integer_k(quoteToken.clone(), "decimal", &[Value::Int(18)]);
        let mut amounts: Value = self.opinion_order_raw_amounts(isMarket.clone(), sideStr.clone(), amount.clone(), price.clone(), decimals.clone());
        let mut makerAmount: Value = self.safe_string_k(amounts.clone(), "makerAmount", &[]);
        let mut takerAmount: Value = self.safe_string_k(amounts.clone(), "takerAmount", &[]);
        let mut sideInt: Value = ternary(is_true(&(is_equal(&sideStr, &Value::Str("BUY".to_string())))), Value::Int(0), Value::Int(1));
        let mut salt: Value = self.number_to_string(self.milliseconds());
        let mut postOnly: Value = self.safe_bool_k(params.clone(), "postOnly", &[Value::Bool(false)]);
        let mut rest: Value = self.omit(params.clone(), Value::List(vec![Value::Str("postOnly".to_string())]), &[]);
        let mut maker: Value = self.load_multi_sign_address().await;
        // Ethereum addresses are case-insensitive - a checksummed multiSignAddress compared
        // against a differently-cased walletAddress with strict equality would pick the wrong
        // signatureType (0 EOA vs 2 Gnosis Safe) and break order signing/validation
        let mut makerLower: Value = to_lower(&maker);
        let mut walletAddressLower: Value = to_lower(&self.walletAddress);
        let mut signatureType: Value = ternary(is_true(&(is_equal(&makerLower, &walletAddressLower))), Value::Int(0), Value::Int(2));
        let mut order: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("salt".to_string(), salt.clone());
                m.insert("maker".to_string(), maker.clone());
                m.insert("signer".to_string(), self.walletAddress.clone());
                m.insert("taker".to_string(), Value::Str("0x0000000000000000000000000000000000000000".to_string()));
                m.insert("tokenId".to_string(), tokenId.clone());
                m.insert("makerAmount".to_string(), makerAmount.clone());
                m.insert("takerAmount".to_string(), takerAmount.clone());
                m.insert("expiration".to_string(), Value::Str("0".to_string()));
                m.insert("nonce".to_string(), Value::Str("0".to_string()));
                m.insert("feeRateBps".to_string(), Value::Str("0".to_string()));
                m.insert("side".to_string(), sideInt.clone());
                m.insert("signatureType".to_string(), signatureType.clone());
            m
        });
        let mut signature: Value = self.sign_opinion_order(order.clone(), exchangeAddress.clone());
        let mut signatureNo0x: Value = self.remove0x_prefix(signature.clone());
        let __ws_arg_17 = self.number_to_string(price.clone());
        let __ws_arg_18 = self.seconds();
        let mut orderBody: Value = self.extend(Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("salt".to_string(), salt.clone());
                m.insert("maker".to_string(), maker.clone());
                m.insert("signer".to_string(), self.walletAddress.clone());
                m.insert("taker".to_string(), Value::Str("0x0000000000000000000000000000000000000000".to_string()));
                m.insert("tokenId".to_string(), tokenId.clone());
                m.insert("makerAmount".to_string(), makerAmount.clone());
                m.insert("takerAmount".to_string(), takerAmount.clone());
                m.insert("expiration".to_string(), Value::Str("0".to_string()));
                m.insert("nonce".to_string(), Value::Str("0".to_string()));
                m.insert("feeRateBps".to_string(), Value::Str("0".to_string()));
                m.insert("side".to_string(), to_string_val(&sideInt));
                m.insert("signatureType".to_string(), to_string_val(&signatureType));
                m.insert("signature".to_string(), signature.clone());
                m.insert("sign".to_string(), slice(&signatureNo0x, &Value::Int(0), &Value::Int(64)));
                m.insert("contractAddress".to_string(), Value::Str("".to_string()));
                m.insert("currencyAddress".to_string(), quoteTokenAddress.clone());
                m.insert("topicId".to_string(), topicId.clone());
                m.insert("price".to_string(), ternary(is_true(&isMarket), marketOrderPrice.clone(), __ws_arg_17));
                m.insert("tradingMethod".to_string(), ternary(is_true(&isMarket), Value::Int(1), Value::Int(2)));
                m.insert("timestamp".to_string(), __ws_arg_18);
                m.insert("safeRate".to_string(), Value::Str("0".to_string()));
                m.insert("orderExpTime".to_string(), Value::Str("0".to_string()));
                m.insert("postOnly".to_string(), postOnly.clone());
            m
        }), &[rest.clone()]);
        let mut response: Value = self.opinion_private_post_order(&[orderBody.clone()]).await;
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut orderData: Value = self.safe_dict_k(result.clone(), "orderData", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        return self.parse_prediction_order(orderData.clone(), &[outcomeObj.clone()]);

    Value::Null
}

/*
 * @method
 * @name opinion#cancelOrder
 * @description cancels a single open order by id
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/order
 * @param {string} id the order id
 * @param {string} [outcome] not used by opinion cancelOrder
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
 */
    pub async fn cancel_order(&mut self, mut id: Value, optional_args: &[Value]) -> Value {
        let mut outcome = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_api_key().await;
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("orderId".to_string(), id.clone());
            m
        });
        let __ws_arg_19 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.opinion_private_post_order_cancel(&[__ws_arg_19]).await;
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut canceled: Value = self.safe_bool_k(result.clone(), "result", &[Value::Bool(false)]);
        // a false result does NOT mean the order is still open — it may already be filled,
        // already cancelled, or unknown; don't invent a status the venue didn't report.
        // error responses with an errno never reach this line, handleErrors throws on them
        let mut status: Value = ternary(is_true(&(canceled)), Value::Str("canceled".to_string()), Value::Null);
        return self.safe_prediction_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), id.clone());
        m.insert("status".to_string(), status.clone());
        m.insert("info".to_string(), response.clone());
    m
}), &[]);

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#parseOrderStatus
 * @description maps an opinion order statusEnum string to the unified status vocabulary
 * @param {string} status the raw opinion order statusEnum
 * @returns {string} a unified order status
 */
    pub fn parse_order_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("Pending".to_string(), Value::Str("open".to_string()));
                m.insert("Finished".to_string(), Value::Str("closed".to_string()));
                m.insert("Canceled".to_string(), Value::Str("canceled".to_string()));
                m.insert("Expired".to_string(), Value::Str("canceled".to_string()));
                m.insert("Failed".to_string(), Value::Str("canceled".to_string()));
            m
        });
        return self.safe_string(statuses.clone(), status.clone(), &[status.clone()]);

    Value::Null
}

/*
 * @method
 * @name opinion#parsePredictionOrder
 * @description parses a raw opinion order object into a unified prediction order structure
 * @param {object} order the raw opinion OrderData object
 * @param {object} [market] the outcome object the order belongs to
 * @returns {object} a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
 */
    pub fn parse_prediction_order(&self, mut order: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        //     {
        //         "orderId": "...",
        //         "marketId": 1094,
        //         "side": 0,
        //         "sideEnum": "Buy",
        //         "tradingMethod": 2,
        //         "tradingMethodEnum": "Limit",
        //         "price": "0.01",
        //         "orderShares": "5",
        //         "orderAmount": "0.05",
        //         "filledShares": "0",
        //         "filledAmount": "0",
        //         "profit": "0",
        //         "status": 1,
        //         "statusEnum": "Pending",
        //         "createdAt": 1785500000,
        //         "expiresAt": 0,
        //         "postOnly": false
        //     }
        //
        let mut id: Value = self.safe_string_k(order.clone(), "orderId", &[]);
        let mut marketAny: Value = market.clone();
        let mut statusEnum: Value = self.safe_string_k(order.clone(), "statusEnum", &[]);
        let mut status: Value = self.parse_order_status(statusEnum.clone());
        let mut sideEnum: Value = self.safe_string_lower(order.clone(), Value::Str("sideEnum".to_string()), &[]);
        let mut tradingMethodEnum: Value = self.safe_string_lower(order.clone(), Value::Str("tradingMethodEnum".to_string()), &[]);
        let mut timestamp: Value = self.safe_timestamp(order.clone(), Value::Str("createdAt".to_string()), &[]);
        return self.safe_prediction_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), id.clone());
        m.insert("clientOrderId".to_string(), Value::Null);
        m.insert("info".to_string(), order.clone());
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp.clone()));
        m.insert("lastTradeTimestamp".to_string(), Value::Null);
        m.insert("status".to_string(), status.clone());
        m.insert("outcome".to_string(), self.safe_string_k(marketAny.clone(), "outcome", &[]));
        m.insert("outcomeId".to_string(), self.safe_string2(marketAny.clone(), Value::Str("outcomeId".to_string()), Value::Str("id".to_string()), &[]));
        m.insert("label".to_string(), self.safe_string_k(marketAny.clone(), "label", &[]));
        m.insert("market".to_string(), self.safe_string2(marketAny.clone(), Value::Str("market".to_string()), Value::Str("outcome".to_string()), &[]));
        m.insert("type".to_string(), tradingMethodEnum.clone());
        m.insert("side".to_string(), sideEnum.clone());
        m.insert("price".to_string(), self.safe_number_k(order.clone(), "price", &[]));
        m.insert("amount".to_string(), self.safe_number_k(order.clone(), "orderShares", &[]));
        m.insert("cost".to_string(), self.safe_number_k(order.clone(), "filledAmount", &[]));
        m.insert("filled".to_string(), self.safe_number_k(order.clone(), "filledShares", &[]));
        m.insert("fee".to_string(), Value::Null);
        m.insert("trades".to_string(), Value::List(vec![]));
    m
}), &[market.clone()]);

    Value::Null
}

/*
 * @method
 * @name opinion#fetchOrders
 * @description fetches all of the authenticated user's orders
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/order
 * @param {string} [outcome] filter by unified outcome or outcome token id
 * @param {int} [since] timestamp in ms of the earliest order to fetch
 * @param {int} [limit] the maximum number of orders to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
 */
    pub async fn fetch_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut outcome = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_api_key().await;
        let mut outcomeObj: Value = Value::Null;
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        if !is_equal(&outcome, &Value::Null) {
            outcomeObj = self.load_outcome(outcome.clone(), &[]).await;
            let mut info: Value = self.safe_dict_k(outcomeObj.clone(), "info", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            add_element_to_object(&mut request, &Value::Str("marketId".to_string()), self.safe_integer_k(info.clone(), "marketId", &[]));
        }
        let __ws_arg_20 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.opinion_private_get_order(&[__ws_arg_20]).await;
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut orders: Value = self.safe_list_k(result.clone(), "list", &[Value::List(vec![])]);
        return self.parse_prediction_orders(orders.clone(), &[outcomeObj.clone(), since.clone(), limit.clone()]);

    Value::Null
}

/*
 * @method
 * @name opinion#fetchOrder
 * @description fetches a single order by id
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/order
 * @param {string} id the order id
 * @param {string} [outcome] unified outcome or outcome token id
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
 */
    pub async fn fetch_order(&mut self, mut id: Value, optional_args: &[Value]) -> Value {
        let mut outcome = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_api_key().await;
        let mut outcomeObj: Value = Value::Null;
        if !is_equal(&outcome, &Value::Null) {
            outcomeObj = self.load_outcome(outcome.clone(), &[]).await;
        }
        let __ws_arg_21 = self.extend(Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("orderId".to_string(), id.clone());
            m
        }), &[params.clone()]);
        let mut response: Value = self.opinion_private_get_order_order_id(&[__ws_arg_21]).await;
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut orderData: Value = self.safe_dict_k(result.clone(), "orderData", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        return self.parse_prediction_order(orderData.clone(), &[outcomeObj.clone()]);

    Value::Null
}

/*
 * @method
 * @name opinion#fetchOpenOrders
 * @description fetches the authenticated user's open orders
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/order
 * @param {string} [outcome] filter by unified outcome or outcome token id
 * @param {int} [since] timestamp in ms of the earliest order to fetch
 * @param {int} [limit] the maximum number of orders to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
 */
    pub async fn fetch_open_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut outcome = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        // 1 = pending - open status
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("status".to_string(), Value::Str("1".to_string()));
            m
        });
        let __ws_arg_22 = self.extend(request.clone(), &[params.clone()]);
        return self.fetch_orders(&[outcome.clone(), since.clone(), limit.clone(), __ws_arg_22]).await;

    Value::Null
}

/*
 * @method
 * @name opinion#fetchClosedOrders
 * @description fetches the authenticated user's closed orders
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/order
 * @param {string} [outcome] filter by unified outcome or outcome token id
 * @param {int} [since] timestamp in ms of the earliest order to fetch
 * @param {int} [limit] the maximum number of orders to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
 */
    pub async fn fetch_closed_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut outcome = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        // 2 = filled, 3 = canceled, 4 = expired, 5 = failed
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("status".to_string(), Value::Str("2,3,4,5".to_string()));
            m
        });
        let __ws_arg_23 = self.extend(request.clone(), &[params.clone()]);
        return self.fetch_orders(&[outcome.clone(), since.clone(), limit.clone(), __ws_arg_23]).await;

    Value::Null
}

/*
 * @method
 * @name opinion#fetchMyTrades
 * @description fetches the authenticated user's trades
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/trade
 * @param {string} [outcome] filter by unified outcome or outcome token id
 * @param {int} [since] timestamp in ms of the earliest trade to fetch
 * @param {int} [limit] the maximum number of trades to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [prediction trade structures](https://docs.ccxt.com/#/?id=prediction-trade-structure)
 */
    pub async fn fetch_my_trades(&mut self, optional_args: &[Value]) -> Value {
        let mut outcome = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.walletAddress, &Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(add(&self.id, &Value::Str(" fetchMyTrades() requires a walletAddress".to_string()))));
        }
        self.load_api_key().await;
        let mut outcomeObj: Value = Value::Null;
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("walletAddress".to_string(), self.walletAddress.clone());
            m
        });
        if !is_equal(&outcome, &Value::Null) {
            outcomeObj = self.load_outcome(outcome.clone(), &[]).await;
            let mut info: Value = self.safe_dict_k(outcomeObj.clone(), "info", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            add_element_to_object(&mut request, &Value::Str("marketId".to_string()), self.safe_integer_k(info.clone(), "marketId", &[]));
        }
        let __ws_arg_24 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.opinion_private_get_trade_user_wallet_address(&[__ws_arg_24]).await;
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut trades: Value = self.safe_list_k(result.clone(), "list", &[Value::List(vec![])]);
        let mut tradesLength: Value = get_array_length(&trades);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1320: bool = true;
            while { if !__for_first_1320 { i = add(&i, &Value::Int(1)); } __for_first_1320 = false; is_less_than(&i, &tradesLength) } {
            let mut trade: Value = get_value(&trades, &i);
            let mut trade: Value = get_value(&trades, &i);
            let mut tokenId: Value = self.safe_string_k(trade.clone(), "tokenId", &[]);
            let mut marketId: Value = self.safe_integer_k(trade.clone(), "marketId", &[]);
            if is_true(&(is_equal(&tokenId, &Value::Null))) && is_true(&(!is_equal(&marketId, &Value::Null))) {
                let mut tradeMarket: Value = self.load_trade_market(marketId.clone()).await;
                let mut info: Value = self.safe_dict_k(tradeMarket.clone(), "info", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
                let mut isYes: Value = Value::Bool(is_equal(&self.safe_string_lower(trade.clone(), Value::Str("outcomeSideEnum".to_string()), &[]), &Value::Str("yes".to_string())));
                add_element_to_object(&mut trade, &Value::Str("tokenId".to_string()), ternary(is_true(&isYes), self.safe_string_k(info.clone(), "yesTokenId", &[]), self.safe_string_k(info.clone(), "noTokenId", &[])));
            }
        }
        }
        return self.parse_prediction_trades(trades.clone(), &[outcomeObj.clone(), since.clone(), limit.clone()]);

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#loadTradeMarket
 * @description fetches and caches a single market by its numeric marketId, and indexes its outcomes
 * @param {int} marketId the numeric market id
 * @returns {object} the parsed market
 */
    pub async fn load_trade_market(&mut self, mut marketId: Value) -> Value {
        if is_equal(&marketId, &Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(add(&self.id, &Value::Str(" loadTradeMarket() requires a marketId".to_string()))));
        }
        let mut cacheKey: Value = Value::Str("tradeMarketsById".to_string());
        let mut cached: Value = self.safe_dict(self.options.clone(), cacheKey.clone(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut idStr: Value = to_string_val(&marketId);
        let mut existing: Value = self.safe_dict(cached.clone(), idStr.clone(), &[]);
        if !is_equal(&existing, &Value::Null) {
            return existing;
        }
        let mut response: Value = self.opinion_public_get_market_market_id(&[Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("marketId".to_string(), marketId.clone());
            m
        })]).await;
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut data: Value = self.safe_dict_k(result.clone(), "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut market: Value = self.parse_opinion_market(data.clone(), &[]);
        if is_equal(&market, &Value::Null) {
            panic!("{}", crate::exchange_errors::exchange_error(add(&add(&self.id, &Value::Str(" loadTradeMarket() could not parse market ".to_string())), &idStr)));
        }
        if is_equal(&self.markets, &Value::Null) {
            { let __t = self.create_safe_dictionary(&[]); self.markets = __t; }
        }
        let mut marketHandle: Value = self.safe_string_k(market.clone(), "market", &[Value::Str("".to_string())]);
        add_element_to_object(&mut self.markets, &marketHandle, market.clone());
        self.index_market_outcomes(market.clone());
        add_element_to_object(&mut cached, &idStr, market.clone());
        add_element_to_object(&mut self.options, &cacheKey, cached.clone());
        return market;

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#parsePredictionTrade
 * @description parses a raw opinion trade object into a unified trade object
 * @param {object} trade the raw opinion TradeData object
 * @param {object} [market] the outcome object the trade belongs to
 * @returns {object} a [prediction trade structure](https://docs.ccxt.com/#/?id=prediction-trade-structure)
 */
    pub fn parse_prediction_trade(&self, mut trade: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        let mut tokenId: Value = self.safe_string_k(trade.clone(), "tokenId", &[]);
        let mut outcomeObj: Value = self.safe_outcome(tokenId.clone(), &[market.clone()]);
        let mut timestamp: Value = self.safe_timestamp(trade.clone(), Value::Str("createdAt".to_string()), &[]);
        let mut side: Value = self.safe_string_lower(trade.clone(), Value::Str("side".to_string()), &[]);
        return self.safe_prediction_trade(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), self.safe_string_k(trade.clone(), "txHash", &[]));
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("side".to_string(), side.clone());
        m.insert("price".to_string(), self.safe_number_k(trade.clone(), "price", &[]));
        m.insert("amount".to_string(), self.safe_number_k(trade.clone(), "shares", &[]));
        m.insert("cost".to_string(), self.safe_number_k(trade.clone(), "amount", &[]));
        m.insert("fee".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), self.safe_number_k(trade.clone(), "fee", &[]));
        m.insert("currency".to_string(), Value::Str("USDT".to_string()));
    m
}));
        m.insert("outcome".to_string(), self.safe_string_k(outcomeObj.clone(), "outcome", &[]));
        m.insert("outcomeId".to_string(), self.safe_string2(outcomeObj.clone(), Value::Str("outcomeId".to_string()), Value::Str("id".to_string()), &[]));
        m.insert("label".to_string(), self.safe_string_k(outcomeObj.clone(), "label", &[]));
        m.insert("market".to_string(), self.safe_string2(outcomeObj.clone(), Value::Str("market".to_string()), Value::Str("outcome".to_string()), &[]));
    m
}), &[]);

    Value::Null
}

/*
 * @method
 * @name opinion#fetchBalance
 * @description fetches the authenticated user's quote-token balances
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/quote-token
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [balance structure](https://docs.ccxt.com/#/?id=balance-structure)
 */
    pub async fn fetch_balance(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_api_key().await;
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("chain_id".to_string(), Value::Str("56".to_string()));
            m
        });
        let __ws_arg_25 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.opinion_private_get_user_balance(&[__ws_arg_25]).await;
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut rawBalances: Value = self.safe_list_k(result.clone(), "balances", &[Value::List(vec![])]);
        let mut rawBalancesLength: Value = get_array_length(&rawBalances);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1321: bool = true;
            while { if !__for_first_1321 { i = add(&i, &Value::Int(1)); } __for_first_1321 = false; is_less_than(&i, &rawBalancesLength) } {
            let mut rawBalance: Value = get_value(&rawBalances, &i);
            let mut rawBalance: Value = get_value(&rawBalances, &i);
            let mut quoteTokenAddress: Value = self.safe_string_k(rawBalance.clone(), "quoteToken", &[]);
            let mut quoteToken: Value = self.load_quote_token(quoteTokenAddress.clone()).await;
            add_element_to_object(&mut rawBalance, &Value::Str("symbol".to_string()), self.safe_string_k(quoteToken.clone(), "symbol", &[Value::Str("USDT".to_string())]));
        }
        }
        return self.parse_balance(response.clone());

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#parseBalance
 * @description parses an opinion user-balance response into a unified balances object
 * @param {object} response the raw user-balance response
 * @returns {object} a [balance structure](https://docs.ccxt.com/#/?id=balance-structure)
 */
    pub fn parse_balance(&self, mut response: Value) -> Value {
        let mut result: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("info".to_string(), response.clone());
            m
        });
        let mut data: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut balances: Value = self.safe_list_k(data.clone(), "balances", &[Value::List(vec![])]);
        let mut balancesLength: Value = get_array_length(&balances);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1322: bool = true;
            while { if !__for_first_1322 { i = add(&i, &Value::Int(1)); } __for_first_1322 = false; is_less_than(&i, &balancesLength) } {
            let mut balance: Value = get_value(&balances, &i);
            let mut balance: Value = get_value(&balances, &i);
            let mut code: Value = self.safe_string_k(balance.clone(), "symbol", &[Value::Str("USDT".to_string())]);
            add_element_to_object(&mut result, &code, Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("free".to_string(), self.safe_number_k(balance.clone(), "availableBalance", &[]));
        m.insert("used".to_string(), self.safe_number_k(balance.clone(), "frozenBalance", &[]));
        m.insert("total".to_string(), self.safe_number_k(balance.clone(), "totalBalance", &[]));
    m
}));
        }
        }
        return self.safe_balance(result.clone());

    Value::Null
}

/*
 * @method
 * @name opinion#fetchPositions
 * @description fetches the authenticated user's open positions
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/position
 * @param {string[]} [outcomes] filter by unified outcomes or outcome token ids
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [prediction position structures](https://docs.ccxt.com/#/?id=prediction-position-structure)
 */
    pub async fn fetch_positions(&mut self, optional_args: &[Value]) -> Value {
        let mut outcomes = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.walletAddress, &Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(add(&self.id, &Value::Str(" fetchPositions() requires a walletAddress".to_string()))));
        }
        self.load_api_key().await;
        let mut outcomesLength: Value = Value::Int(0);
        if !is_equal(&outcomes, &Value::Null) {
            outcomesLength = get_array_length(&outcomes);
            self.load_outcomes(&[outcomes.clone()]).await;
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("walletAddress".to_string(), self.walletAddress.clone());
            m
        });
        let __ws_arg_26 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.opinion_private_get_positions_user_wallet_address(&[__ws_arg_26]).await;
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut positions: Value = self.safe_list_k(result.clone(), "list", &[Value::List(vec![])]);
        let mut parsed: Value = self.parse_prediction_positions(positions.clone(), &[]);
        if is_equal(&outcomesLength, &Value::Int(0)) {
            return parsed;
        }
        let mut wantedTokenIds: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        // copy to a plain list so the strict null checks see one shape
        let mut outcomesList: Value = ternary(is_true(&(is_equal(&outcomes, &Value::Null))), Value::List(vec![]), outcomes.clone());
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1323: bool = true;
            while { if !__for_first_1323 { i = add(&i, &Value::Int(1)); } __for_first_1323 = false; is_less_than(&i, &get_array_length(&outcomesList)) } {
            let mut outcomeObj: Value = self.outcome(get_value(&outcomesList, &i));
            let mut tokenId: Value = self.safe_string_k(outcomeObj.clone(), "outcomeId", &[]);
            if !is_equal(&tokenId, &Value::Null) {
                add_element_to_object(&mut wantedTokenIds, &tokenId, Value::Bool(true));
            }
        }
        }
        let mut filtered: Value = Value::List(vec![]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1324: bool = true;
            while { if !__for_first_1324 { i = add(&i, &Value::Int(1)); } __for_first_1324 = false; is_less_than(&i, &get_array_length(&parsed)) } {
            let mut position: Value = get_value(&parsed, &i);
            let mut position: Value = get_value(&parsed, &i);
            let mut info: Value = self.safe_dict_k(position.clone(), "info", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            let mut tokenId: Value = self.safe_string_k(info.clone(), "tokenId", &[]);
            if is_true(&(!is_equal(&tokenId, &Value::Null))) && is_true(&(Value::Bool(in_op(&wantedTokenIds, &tokenId)))) {
                append_to_array(&mut filtered, position.clone());
            }
        }
        }
        return filtered;

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#parsePredictionPosition
 * @description parses a raw opinion position object into a unified position object
 * @param {object} position the raw opinion PositionData object
 * @param {object} [market] the outcome object the position belongs to
 * @returns {object} a [prediction position structure](https://docs.ccxt.com/#/?id=prediction-position-structure)
 */
    pub fn parse_prediction_position(&self, mut position: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        let mut tokenId: Value = self.safe_string_k(position.clone(), "tokenId", &[]);
        let mut outcomeObj: Value = self.safe_outcome(tokenId.clone(), &[market.clone()]);
        let mut outcomeSideEnum: Value = self.safe_string_lower(position.clone(), Value::Str("outcomeSideEnum".to_string()), &[]);
        return self.safe_prediction_position(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("contracts".to_string(), self.safe_number_k(position.clone(), "sharesOwned", &[]));
        m.insert("side".to_string(), outcomeSideEnum.clone());
        m.insert("unrealizedPnl".to_string(), self.safe_number_k(position.clone(), "unrealizedPnl", &[]));
        m.insert("entryPrice".to_string(), self.safe_number_k(position.clone(), "avgEntryPrice", &[]));
        m.insert("outcome".to_string(), self.safe_string_k(outcomeObj.clone(), "outcome", &[]));
        m.insert("outcomeId".to_string(), self.safe_string2(outcomeObj.clone(), Value::Str("outcomeId".to_string()), Value::Str("id".to_string()), &[]));
        m.insert("label".to_string(), self.safe_string_k(outcomeObj.clone(), "label", &[]));
        m.insert("market".to_string(), self.safe_string2(outcomeObj.clone(), Value::Str("market".to_string()), Value::Str("outcome".to_string()), &[]));
        m.insert("info".to_string(), position.clone());
    m
}));

    Value::Null
}

    pub fn hash_message(&self, mut message: Value) -> Value {
        return add(&Value::Str("0x".to_string()), &self.hash(message.clone(), Value::Str("keccak".to_string()), &[Value::Str("hex".to_string())]));

    Value::Null
}

    pub fn sign_hash(&self, mut hash: Value, mut privateKey: Value) -> Value {
        let mut signature: Value = ecdsa(slice(&hash, &negate(&Value::Int(64)), &Value::Null), slice(&privateKey, &negate(&Value::Int(64)), &Value::Null), Value::Str("secp256k1".to_string()), Value::Null);
        // assign before padStart so the PHP str_pad regex matches
        let mut rRaw: Value = get_value(&signature, &Value::Str("r".to_string()));
        let mut sRaw: Value = get_value(&signature, &Value::Str("s".to_string()));
        let mut r: Value = pad_start(&rRaw, &Value::Int(64), &Value::Str("0".to_string()));
        let mut s: Value = pad_start(&sRaw, &Value::Int(64), &Value::Str("0".to_string()));
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("r".to_string(), add(&Value::Str("0x".to_string()), &r));
        m.insert("s".to_string(), add(&Value::Str("0x".to_string()), &s));
        m.insert("v".to_string(), self.sum(&[Value::Int(27), get_value(&signature, &Value::Str("v".to_string()))]));
    m
});

    Value::Null
}

    pub fn sign_message(&self, mut message: Value, mut privateKey: Value) -> Value {
        return self.sign_hash(self.hash_message(message.clone()), slice(&privateKey, &negate(&Value::Int(64)), &Value::Null));

    Value::Null
}

    pub fn sign_api_key_auth(&self, mut walletAddress: Value, mut action: Value, mut timestamp: Value) -> Value {
        // EIP-712 signature used to create/get/delete an API key (wallet-authenticated key management)
        let mut domain: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("name".to_string(), Value::Str("Opinion OpenAPI".to_string()));
                m.insert("version".to_string(), Value::Str("1".to_string()));
                m.insert("chainId".to_string(), Value::Int(56));
            m
        });
        let mut messageTypes: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("OpinionApiKeyAuth".to_string(), Value::List(vec![Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("walletAddress".to_string()));
        m.insert("type".to_string(), Value::Str("address".to_string()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("action".to_string()));
        m.insert("type".to_string(), Value::Str("string".to_string()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("timestamp".to_string()));
        m.insert("type".to_string(), Value::Str("string".to_string()));
    m
})]));
            m
        });
        let mut messageData: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("walletAddress".to_string(), walletAddress.clone());
                m.insert("action".to_string(), action.clone());
                m.insert("timestamp".to_string(), timestamp.clone());
            m
        });
        let mut encoded: Value = self.eth_encode_structured_data(domain.clone(), messageTypes.clone(), messageData.clone());
        let mut sig: Value = self.sign_message(encoded.clone(), self.privateKey.clone());
        return add(&add(&add(&Value::Str("0x".to_string()), &self.remove0x_prefix(get_value(&sig, &Value::Str("r".to_string())))), &self.remove0x_prefix(get_value(&sig, &Value::Str("s".to_string())))), &self.int_to_base16(get_value(&sig, &Value::Str("v".to_string())), &[]));

    Value::Null
}

/*
 * @method
 * @name opinion#createApiKey
 * @description self-service creation of an Open API key linked to this.walletAddress via
 * an EIP-712-signed request - there is no "generate key" button in the Opinion GUI, this is
 * the only documented way to obtain a wallet-linked key
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/authentication
 * @param {object} [params] extra parameters
 * @returns {object} the api credentials { apiKey, walletAddress }
 */
    pub async fn create_api_key(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut response: Value = self.opinion_private_post_auth_api_key(&[params.clone()]).await;
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        return self.set_api_credentials(result.clone());

    Value::Null
}

/*
 * @method
 * @name opinion#fetchApiKey
 * @description fetches the currently active Open API key for this.walletAddress
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/authentication
 * @param {object} [params] extra parameters
 * @returns {object} the api credentials { apiKey, walletAddress }
 */
    pub async fn fetch_api_key(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut response: Value = self.opinion_private_get_auth_api_key(&[params.clone()]).await;
        let mut result: Value = self.safe_dict_k(response.clone(), "result", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        return self.set_api_credentials(result.clone());

    Value::Null
}

/*
 * @method
 * @name opinion#deleteApiKey
 * @description revokes the Open API key for this.walletAddress
 * @see https://docs.opinion.trade/developer-guide/opinion-open-api/authentication
 * @param {object} [params] extra parameters
 * @returns {object} raw response, result.deleted confirms revocation
 */
    pub async fn delete_api_key(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut response: Value = self.opinion_private_delete_auth_api_key(&[params.clone()]).await;
        add_element_to_object(&mut self.options, &Value::Str("apiKey".to_string()), Value::Null);
        // sign() prefers this.apiKey over options['apiKey'] - clear it too, or a directly-set
        // exchange.apiKey would keep being used for private calls after the key is revoked.
        // an empty string, not undefined: the strict base types the credential as string, and
        // sign() treats an empty key as absent
        self.apiKey = Value::Str("".to_string());
        return response;

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#loadApiKey
 * @description ensures an apiKey is available before a private call - reuses a directly-set key, otherwise
 * self-issues one from the walletAddress/privateKey via fetchApiKey(), falling back to createApiKey() when
 * the wallet has no key yet; freshly created keys can take ~15 seconds to activate venue-side
 * @returns {string} the apiKey
 */
    pub async fn load_api_key(&mut self) -> Value {
        let mut hasDirectApiKey: Value = Value::Bool(!is_true(&self.is_empty_string(self.apiKey.clone())));
        if is_true(&hasDirectApiKey) {
            return self.apiKey.clone();
        }
        let mut optionsKey: Value = self.safe_string_k(self.options.clone(), "apiKey", &[]);
        if !is_equal(&optionsKey, &Value::Null) {
            return optionsKey;
        }
        if is_true(&(is_equal(&self.walletAddress, &Value::Null))) || is_true(&(is_equal(&self.privateKey, &Value::Null))) {
            panic!("{}", crate::exchange_errors::authentication_error(add(&self.id, &Value::Str(" private endpoints require an apiKey, or a walletAddress and privateKey to self-issue one".to_string()))));
        }
        let mut creds: Value = Value::Null;
        let _try_result = futures::FutureExt::catch_unwind(std::panic::AssertUnwindSafe(async {
            creds = self.fetch_api_key(&[]).await;
         #[allow(unreachable_code)] { Value::Null }})).await;
if let Err(_try_err) = _try_result { let e: Value = panic_to_value(_try_err);
            // no key exists for this wallet yet (11010) - self-issue one; any other
            // failure (unregistered wallet, disabled issuance) surfaces from the create call
            creds = self.create_api_key(&[]).await;
        }
        return self.safe_string_k(creds.clone(), "apiKey", &[]);

    Value::Null
}

    pub fn set_api_credentials(&mut self, mut response: Value) -> Value {
        //
        //     { "apiKey": "...", "walletAddress": "..." }
        //
        let mut creds: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("apiKey".to_string(), self.safe_string_k(response.clone(), "apiKey", &[]));
                m.insert("walletAddress".to_string(), self.safe_string_k(response.clone(), "walletAddress", &[]));
            m
        });
        add_element_to_object(&mut self.options.clone(), &Value::Str("apiKey".to_string()), get_value(&creds, &Value::Str("apiKey".to_string())));
        // checkRequiredCredentials() (called by createOrder()) checks this.apiKey, not
        // options['apiKey'] - keep both in sync, same as deleteApiKey() clearing both
        self.apiKey = get_value(&creds, &Value::Str("apiKey".to_string()));
        return creds;

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#opinionWsUrl
 * @description builds the websocket url - the venue authenticates the whole connection with the apiKey passed as a query parameter, for public and private channels alike
 * @returns {string} the websocket url
 */
    pub fn opinion_ws_url(&self) -> Value {
        let mut hasDirectApiKey: Value = Value::Bool(!is_true(&self.is_empty_string(self.apiKey.clone())));
        let mut apiKey: Value = ternary(is_true(&(hasDirectApiKey)), self.apiKey.clone(), self.safe_string_k(self.options.clone(), "apiKey", &[]));
        if is_equal(&apiKey, &Value::Null) {
            panic!("{}", crate::exchange_errors::authentication_error(add(&self.id, &Value::Str(" websocket requires an apiKey - set it directly or call createApiKey()/fetchApiKey() first".to_string()))));
        }
        let mut wsUrl: Value = self.safe_string(get_value(&self.urls, &Value::Str("api".to_string())), Value::Str("ws".to_string()), &[Value::Str("".to_string())]);
        return add(&add(&wsUrl, &Value::Str("?apikey=".to_string())), &apiKey);

    Value::Null
}

    pub fn ping(&self, mut client: Value) -> Value {
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("action".to_string(), Value::Str("HEARTBEAT".to_string()));
    m
});

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#subscribeOpinionChannel
 * @description subscribes to one venue channel scoped by the binary marketId and waits on the given message hash
 * @param {string} messageHash the internal hash the awaited payload resolves on
 * @param {string} channel the venue channel name (e.g. 'market.depth.diff')
 * @param {int} marketId the numeric binary market id
 * @returns {any} the first resolved payload
 */
    pub async fn subscribe_opinion_channel(&mut self, mut messageHash: Value, mut channel: Value, mut marketId: Value) -> Value {
        self.load_api_key().await;
        let mut url: Value = self.opinion_ws_url();
        let mut subscriptionKey: Value = add(&add(&channel, &Value::Str(":".to_string())), &self.number_to_string(marketId.clone()));
        let mut subscribeMsg: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("action".to_string(), Value::Str("SUBSCRIBE".to_string()));
                m.insert("channel".to_string(), channel.clone());
                m.insert("marketId".to_string(), marketId.clone());
            m
        });
        return self.watch(url.clone(), messageHash.clone(), &[subscribeMsg.clone(), subscriptionKey.clone()]).await;

    Value::Null
}

    pub fn handle_message(&mut self, mut client: Value, mut message: Value) {
        // every data payload carries its channel name in msgType; frames without one -
        // subscribe acks and heartbeat echoes - carry nothing to route
        let mut msgType: Value = self.safe_string_k(message.clone(), "msgType", &[]);
        if is_equal(&msgType, &Value::Null) {
            return;
        }
        if is_equal(&msgType, &Value::Str("market.depth.diff".to_string())) {
            self.handle_order_book(client.clone(), message.clone());
        }  else if is_equal(&msgType, &Value::Str("market.last.price".to_string())) {
            self.handle_ticker(client.clone(), message.clone());
        }  else if is_equal(&msgType, &Value::Str("market.last.trade".to_string())) {
            self.handle_trades(client.clone(), message.clone());
        }  else if is_equal(&msgType, &Value::Str("trade.order.update".to_string())) {
            self.handle_order(client.clone(), message.clone());
        }  else if is_equal(&msgType, &Value::Str("trade.record.new".to_string())) {
            self.handle_my_trade(client.clone(), message.clone());
        }
}

/*
 * @ignore
 * @method
 * @name opinion#opinionOutcomeByMarketIdSide
 * @description resolves a cached outcome object from the numeric marketId + outcomeSide (1 yes / 2 no) the user channels report instead of a tokenId - cache-only, returns undefined on a cold cache
 * @param {int} marketId the numeric binary market id
 * @param {int} outcomeSide 1 for the yes token, 2 for the no token
 * @returns {object} the outcome object, or undefined
 */
    pub fn opinion_outcome_by_market_id_side(&self, mut marketId: Value, mut outcomeSide: Value) -> Value {
        if is_true(&(is_equal(&marketId, &Value::Null))) || is_true(&(is_equal(&self.markets, &Value::Null))) {
            return Value::Null;
        }
        let mut marketKeys: Value = object_keys(&self.markets);
        let mut marketKeysLength: Value = get_array_length(&marketKeys);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1325: bool = true;
            while { if !__for_first_1325 { i = add(&i, &Value::Int(1)); } __for_first_1325 = false; is_less_than(&i, &marketKeysLength) } {
            let mut market: Value = get_value(&self.markets, &get_value(&marketKeys, &i));
            let mut info: Value = self.safe_dict_k(market.clone(), "info", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            if is_equal(&self.safe_integer_k(info.clone(), "marketId", &[]), &marketId) {
                let mut outcomes: Value = self.safe_list_k(market.clone(), "outcomes", &[Value::List(vec![])]);
                let mut index: Value = ternary(is_true(&(is_equal(&outcomeSide, &Value::Int(2)))), Value::Int(1), Value::Int(0));
                return self.safe_dict(outcomes.clone(), index.clone(), &[]);
            }
        }
        }
        return Value::Null;

    Value::Null
}

/*
 * @method
 * @name opinion#watchOrderBook
 * @description streams the order book of an outcome token; the channel is delta-only so the live book is seeded from the REST snapshot
 * @see https://docs.opinion.trade/developer-guide/opinion-websocket/market-channels
 * @param {string} outcome unified outcome or outcome token id
 * @param {int} [limit] the maximum number of order book entries to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [prediction order book structure](https://docs.ccxt.com/#/?id=prediction-order-book-structure)
 */
    pub async fn watch_order_book(&mut self, mut outcome: Value, optional_args: &[Value]) -> Value {
        let mut limit = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut outcomeObj: Value = self.load_outcome(outcome.clone(), &[]).await;
        let mut info: Value = self.safe_dict_k(outcomeObj.clone(), "info", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut marketId: Value = self.safe_integer_k(info.clone(), "marketId", &[]);
        let mut sym: Value = self.safe_outcome_symbol(outcome.clone(), &[outcomeObj.clone()]);
        let mut channel: Value = Value::Str("market.depth.diff".to_string());
        let mut messageHash: Value = add(&Value::Str("orderbook::".to_string()), &sym);
        self.load_api_key().await;
        let mut url: Value = self.opinion_ws_url();
        let mut client: Value = self.client(&[url.clone()]);
        let mut subscriptionKey: Value = add(&add(&channel, &Value::Str(":".to_string())), &self.number_to_string(marketId.clone()));
        let mut isNewSubscription: Value = Value::Bool(is_equal(&self.safe_value(get_value(&client, &Value::Str("subscriptions".to_string())), subscriptionKey.clone(), &[]), &Value::Null));
        if is_true(&isNewSubscription) {
            self.seed_order_book(outcome.clone(), sym.clone(), &[limit.clone()]).await;
        }
        let mut subscribeMsg: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("action".to_string(), Value::Str("SUBSCRIBE".to_string()));
                m.insert("channel".to_string(), channel.clone());
                m.insert("marketId".to_string(), marketId.clone());
            m
        });
        let mut future: Value = self.watch(url.clone(), messageHash.clone(), &[subscribeMsg.clone(), subscriptionKey.clone()]).await;
        if is_true(&isNewSubscription) {
            // return the freshly-seeded book immediately instead of blocking until the next delta
            client.resolve(&[self.safe_value(self.orderbooks.clone(), sym.clone(), &[]), messageHash.clone()]);
        }
        let mut orderbook: Value = get_value(&future, &Value::Str("await".to_string()));
        return orderbook.limit();

    Value::Null
}

    pub async fn seed_order_book(&mut self, mut outcome: Value, mut sym: Value, optional_args: &[Value]) -> Value {
        let mut limit = get_arg(optional_args, 0, Value::Null);
        // the depth channel streams single-level deltas only, so seed the live book from the REST snapshot
        let mut snapshot: Value = self.fetch_order_book(outcome.clone(), &[limit.clone()]).await;
        let mut orderbook: Value = self.order_book(&[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        orderbook.reset(snapshot.clone());
        add_element_to_object(&mut self.orderbooks, &sym, orderbook.clone());

    Value::Null
}

    pub fn handle_order_book(&self, mut client: Value, mut message: Value) {
        //
        //     {
        //         "marketId": 2764,
        //         "tokenId": "19120407572139442221452465677574895365338028945317996490376653704877573103648",
        //         "outcomeSide": 1,
        //         "side": "bids",
        //         "price": "0.2",
        //         "size": "50",
        //         "msgType": "market.depth.diff"
        //     }
        //
        let mut tokenId: Value = self.safe_string_k(message.clone(), "tokenId", &[]);
        let mut outcomeObj: Value = self.safe_dict(self.exchange.outcomes_by_id.clone(), tokenId.clone(), &[]);
        let mut sym: Value = self.safe_string_k(outcomeObj.clone(), "outcome", &[]);
        if is_equal(&sym, &Value::Null) {
            return;
        }
        if is_equal(&self.safe_value(self.orderbooks.clone(), sym.clone(), &[]), &Value::Null) {
            return;
        }
        let mut orderbook: Value = get_value(&self.orderbooks, &sym);
        let mut sideStr: Value = self.safe_string_k(message.clone(), "side", &[]);
        let mut bookSide: Value = ternary(is_true(&(is_equal(&sideStr, &Value::Str("bids".to_string())))), get_value(&orderbook, &Value::Str("bids".to_string())), get_value(&orderbook, &Value::Str("asks".to_string())));
        let mut price: Value = self.safe_number_k(message.clone(), "price", &[]);
        let mut size: Value = self.safe_number_k(message.clone(), "size", &[]);
        bookSide.store_array(Value::List(vec![price.clone(), size.clone()]));
        let mut now: Value = self.milliseconds();
        add_element_to_object(&mut orderbook, &Value::Str("timestamp".to_string()), now.clone());
        add_element_to_object(&mut orderbook, &Value::Str("datetime".to_string()), self.iso8601(now.clone()));
        client.resolve(&[orderbook.clone(), add(&Value::Str("orderbook::".to_string()), &sym)]);
}

/*
 * @method
 * @name opinion#watchTicker
 * @description streams last-price updates of an outcome token
 * @see https://docs.opinion.trade/developer-guide/opinion-websocket/market-channels
 * @param {string} outcome unified outcome or outcome token id
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [prediction ticker structure](https://docs.ccxt.com/#/?id=prediction-ticker-structure)
 */
    pub async fn watch_ticker(&mut self, mut outcome: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut outcomeObj: Value = self.load_outcome(outcome.clone(), &[]).await;
        let mut info: Value = self.safe_dict_k(outcomeObj.clone(), "info", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut marketId: Value = self.safe_integer_k(info.clone(), "marketId", &[]);
        let mut sym: Value = self.safe_outcome_symbol(outcome.clone(), &[outcomeObj.clone()]);
        let mut messageHash: Value = add(&Value::Str("ticker::".to_string()), &sym);
        return self.subscribe_opinion_channel(messageHash.clone(), Value::Str("market.last.price".to_string()), marketId.clone()).await;

    Value::Null
}

    pub fn handle_ticker(&self, mut client: Value, mut message: Value) {
        //
        //     {
        //         "tokenId": "19120407572139442221452465677574895365338028945317996490376653704877573103648",
        //         "outcomeSide": 1,
        //         "price": "0.85",
        //         "marketId": 2764,
        //         "msgType": "market.last.price"
        //     }
        //
        let mut tokenId: Value = self.safe_string_k(message.clone(), "tokenId", &[]);
        let mut outcomeObj: Value = self.safe_dict(self.exchange.outcomes_by_id.clone(), tokenId.clone(), &[]);
        let mut sym: Value = self.safe_string_k(outcomeObj.clone(), "outcome", &[]);
        if is_equal(&sym, &Value::Null) {
            return;
        }
        let mut now: Value = self.milliseconds();
        let mut last: Value = self.safe_number_k(message.clone(), "price", &[]);
        let mut ticker: Value = self.safe_prediction_ticker(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("outcome".to_string(), sym.clone());
        m.insert("outcomeId".to_string(), tokenId.clone());
        m.insert("label".to_string(), self.safe_string_k(outcomeObj.clone(), "label", &[]));
        m.insert("market".to_string(), self.safe_string_k(outcomeObj.clone(), "market", &[]));
        m.insert("timestamp".to_string(), now.clone());
        m.insert("datetime".to_string(), self.iso8601(now.clone()));
        m.insert("close".to_string(), last.clone());
        m.insert("last".to_string(), last.clone());
        m.insert("info".to_string(), message.clone());
    m
}), &[outcomeObj.clone()]);
        add_element_to_object(&mut self.tickers.clone(), &sym, ticker.clone());
        client.resolve(&[ticker.clone(), add(&Value::Str("ticker::".to_string()), &sym)]);
}

/*
 * @method
 * @name opinion#watchTrades
 * @description streams public trades of an outcome token
 * @see https://docs.opinion.trade/developer-guide/opinion-websocket/market-channels
 * @param {string} outcome unified outcome or outcome token id
 * @param {int} [since] timestamp in ms of the earliest trade to return
 * @param {int} [limit] the maximum number of trades to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [prediction trade structures](https://docs.ccxt.com/#/?id=prediction-trade-structure)
 */
    pub async fn watch_trades(&mut self, mut outcome: Value, optional_args: &[Value]) -> Value {
        let mut since = get_arg(optional_args, 0, Value::Null);
        let mut limit = get_arg(optional_args, 1, Value::Null);
        let mut params = get_arg(optional_args, 2, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut outcomeObj: Value = self.load_outcome(outcome.clone(), &[]).await;
        let mut info: Value = self.safe_dict_k(outcomeObj.clone(), "info", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut marketId: Value = self.safe_integer_k(info.clone(), "marketId", &[]);
        let mut sym: Value = self.safe_outcome_symbol(outcome.clone(), &[outcomeObj.clone()]);
        let mut messageHash: Value = add(&Value::Str("trades::".to_string()), &sym);
        let mut trades: Value = self.subscribe_opinion_channel(messageHash.clone(), Value::Str("market.last.trade".to_string()), marketId.clone()).await;
        return self.filter_by_since_limit(trades.clone(), &[since.clone(), limit.clone(), Value::Str("timestamp".to_string()), Value::Bool(true)]);

    Value::Null
}

    pub fn handle_trades(&mut self, mut client: Value, mut message: Value) {
        //
        //     {
        //         "tokenId": "19120407572139442221452465677574895365338028945317996490376653704877573103648",
        //         "side": "Buy",
        //         "outcomeSide": 1,
        //         "price": "0.85",
        //         "shares": "10",
        //         "amount": "8.5",
        //         "marketId": 2764,
        //         "msgType": "market.last.trade"
        //     }
        //
        let mut tokenId: Value = self.safe_string_k(message.clone(), "tokenId", &[]);
        let mut outcomeObj: Value = self.safe_dict(self.exchange.outcomes_by_id.clone(), tokenId.clone(), &[]);
        let mut sym: Value = self.safe_string_k(outcomeObj.clone(), "outcome", &[]);
        if is_equal(&sym, &Value::Null) {
            return;
        }
        let mut now: Value = self.milliseconds();
        let mut trade: Value = self.safe_prediction_trade(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), Value::Null);
        m.insert("info".to_string(), message.clone());
        m.insert("timestamp".to_string(), now.clone());
        m.insert("datetime".to_string(), self.iso8601(now.clone()));
        m.insert("outcome".to_string(), sym.clone());
        m.insert("outcomeId".to_string(), tokenId.clone());
        m.insert("label".to_string(), self.safe_string_k(outcomeObj.clone(), "label", &[]));
        m.insert("market".to_string(), self.safe_string_k(outcomeObj.clone(), "market", &[]));
        m.insert("order".to_string(), Value::Null);
        m.insert("type".to_string(), Value::Null);
        m.insert("side".to_string(), self.safe_string_lower(message.clone(), Value::Str("side".to_string()), &[]));
        m.insert("takerOrMaker".to_string(), Value::Str("taker".to_string()));
        m.insert("price".to_string(), self.safe_number_k(message.clone(), "price", &[]));
        m.insert("amount".to_string(), self.safe_number_k(message.clone(), "shares", &[]));
        m.insert("cost".to_string(), self.safe_number_k(message.clone(), "amount", &[]));
        m.insert("fee".to_string(), Value::Null);
    m
}), &[outcomeObj.clone()]);
        if is_equal(&self.trades, &Value::Null) {
            { let __t = self.create_safe_dictionary(&[]); self.trades = __t; }
        }
        if is_equal(&self.safe_value(self.trades.clone(), sym.clone(), &[]), &Value::Null) {
            let mut tradesLimit: Value = self.safe_integer_k(self.options.clone(), "tradesLimit", &[Value::Int(1000)]);
            add_element_to_object(&mut self.trades.clone(), &sym, ArrayCache::new(tradesLimit.clone()));
        }
        let mut stored: Value = get_value(&self.trades, &sym);
        stored.append(trade.clone());
        client.resolve(&[stored.clone(), add(&Value::Str("trades::".to_string()), &sym)]);
}

/*
 * @method
 * @name opinion#watchOrders
 * @description streams the authenticated user's order updates of one market - the venue channel is per-market, so the outcome argument is required
 * @see https://docs.opinion.trade/developer-guide/opinion-websocket/user-channels
 * @param {string} outcome unified outcome or outcome token id whose market to watch
 * @param {int} [since] timestamp in ms of the earliest order to return
 * @param {int} [limit] the maximum number of orders to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
 */
    pub async fn watch_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut outcome = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&outcome, &Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(add(&self.id, &Value::Str(" watchOrders() requires an outcome (the order update channel is per-market)".to_string()))));
        }
        let mut outcomeObj: Value = self.load_outcome(outcome.clone(), &[]).await;
        let mut info: Value = self.safe_dict_k(outcomeObj.clone(), "info", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut marketId: Value = self.safe_integer_k(info.clone(), "marketId", &[]);
        let mut messageHash: Value = Value::Str("orders".to_string());
        let mut orders: Value = self.subscribe_opinion_channel(messageHash.clone(), Value::Str("trade.order.update".to_string()), marketId.clone()).await;
        let mut sym: Value = self.safe_outcome_symbol(outcome.clone(), &[outcomeObj.clone()]);
        return self.filter_by_value_since_limit(orders.clone(), Value::Str("outcome".to_string()), &[sym.clone(), since.clone(), limit.clone(), Value::Str("timestamp".to_string()), Value::Bool(true)]);

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#parseWsOrderStatus
 * @description maps the numeric order status of the websocket order channel onto the unified vocabulary
 * @param {int} status the numeric order status
 * @returns {string} a unified order status, or undefined
 */
    pub fn parse_ws_order_status(&self, mut status: Value) -> Value {
        // per the venue docs: 1 pending, 2 finished, 3 canceled, 4 expired, 5 failed
        if is_equal(&status, &Value::Int(1)) {
            return Value::Str("open".to_string());
        }
        if is_equal(&status, &Value::Int(2)) {
            return Value::Str("closed".to_string());
        }
        if is_equal(&status, &Value::Int(3)) {
            return Value::Str("canceled".to_string());
        }
        if is_equal(&status, &Value::Int(4)) {
            return Value::Str("expired".to_string());
        }
        if is_equal(&status, &Value::Int(5)) {
            return Value::Str("rejected".to_string());
        }
        return Value::Null;

    Value::Null
}

    pub fn handle_order(&mut self, mut client: Value, mut message: Value) {
        //
        //     {
        //         "orderUpdateType": "orderConfirm",
        //         "marketId": 2770,
        //         "rootMarketId": 122,
        //         "orderId": "a11ee07e-e22f-11f0-9714-0a58a9feac02",
        //         "side": 1,
        //         "outcomeSide": 1,
        //         "price": "0.150000000000000000",
        //         "shares": "66.66",
        //         "amount": "9.999000000000000000",
        //         "status": 1,
        //         "tradingMethod": 2,
        //         "quoteToken": "0x55d398326f99059fF775485246999027B3197955",
        //         "createdAt": 1766735464,
        //         "expiresAt": 0,
        //         "chainId": "56",
        //         "filledShares": "10.000000000000000000",
        //         "filledAmount": "1.500000000000000000",
        //         "msgType": "trade.order.update"
        //     }
        //
        let mut marketId: Value = self.safe_integer_k(message.clone(), "marketId", &[]);
        let mut outcomeSide: Value = self.safe_integer_k(message.clone(), "outcomeSide", &[]);
        let mut outcomeObj: Value = self.opinion_outcome_by_market_id_side(marketId.clone(), outcomeSide.clone());
        let mut timestamp: Value = self.safe_timestamp(message.clone(), Value::Str("createdAt".to_string()), &[]);
        // unlike the REST order body (0 buy / 1 sell), the websocket channel uses 1 buy / 2 sell
        // per the docs and confirmed live
        let mut sideInt: Value = self.safe_integer_k(message.clone(), "side", &[]);
        let mut side: Value = ternary(is_true(&(is_equal(&sideInt, &Value::Int(1)))), Value::Str("buy".to_string()), Value::Str("sell".to_string()));
        let mut tradingMethod: Value = self.safe_integer_k(message.clone(), "tradingMethod", &[]);
        let mut type_var: Value = ternary(is_true(&(is_equal(&tradingMethod, &Value::Int(1)))), Value::Str("market".to_string()), Value::Str("limit".to_string()));
        let mut order: Value = self.safe_prediction_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), self.safe_string_k(message.clone(), "orderId", &[]));
        m.insert("clientOrderId".to_string(), Value::Null);
        m.insert("info".to_string(), message.clone());
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp.clone()));
        m.insert("lastTradeTimestamp".to_string(), Value::Null);
        m.insert("status".to_string(), self.parse_ws_order_status(self.safe_integer_k(message.clone(), "status", &[])));
        m.insert("outcome".to_string(), self.safe_string_k(outcomeObj.clone(), "outcome", &[]));
        m.insert("outcomeId".to_string(), self.safe_string_k(outcomeObj.clone(), "outcomeId", &[]));
        m.insert("label".to_string(), self.safe_string_k(outcomeObj.clone(), "label", &[]));
        m.insert("market".to_string(), self.safe_string_k(outcomeObj.clone(), "market", &[]));
        m.insert("type".to_string(), type_var.clone());
        m.insert("side".to_string(), side.clone());
        m.insert("price".to_string(), self.safe_number_k(message.clone(), "price", &[]));
        m.insert("amount".to_string(), self.safe_number_k(message.clone(), "shares", &[]));
        m.insert("cost".to_string(), self.safe_number_k(message.clone(), "filledAmount", &[]));
        m.insert("filled".to_string(), self.safe_number_k(message.clone(), "filledShares", &[]));
        m.insert("fee".to_string(), Value::Null);
        m.insert("trades".to_string(), Value::List(vec![]));
    m
}), &[outcomeObj.clone()]);
        if is_equal(&self.orders, &Value::Null) {
            let mut limit: Value = self.safe_integer_k(self.options.clone(), "ordersLimit", &[Value::Int(1000)]);
            self.orders = ArrayCacheByOutcomeById::new(limit.clone());
        }
        let mut stored: Value = self.orders.clone();
        stored.append(order.clone());
        client.resolve(&[stored.clone(), Value::Str("orders".to_string())]);
}

/*
 * @method
 * @name opinion#watchMyTrades
 * @description streams the authenticated user's executed trades of one market - the venue channel is per-market, so the outcome argument is required
 * @see https://docs.opinion.trade/developer-guide/opinion-websocket/user-channels
 * @param {string} outcome unified outcome or outcome token id whose market to watch
 * @param {int} [since] timestamp in ms of the earliest trade to return
 * @param {int} [limit] the maximum number of trades to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [prediction trade structures](https://docs.ccxt.com/#/?id=prediction-trade-structure)
 */
    pub async fn watch_my_trades(&mut self, optional_args: &[Value]) -> Value {
        let mut outcome = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&outcome, &Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(add(&self.id, &Value::Str(" watchMyTrades() requires an outcome (the trade record channel is per-market)".to_string()))));
        }
        let mut outcomeObj: Value = self.load_outcome(outcome.clone(), &[]).await;
        let mut info: Value = self.safe_dict_k(outcomeObj.clone(), "info", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut marketId: Value = self.safe_integer_k(info.clone(), "marketId", &[]);
        let mut messageHash: Value = Value::Str("myTrades".to_string());
        let mut trades: Value = self.subscribe_opinion_channel(messageHash.clone(), Value::Str("trade.record.new".to_string()), marketId.clone()).await;
        let mut sym: Value = self.safe_outcome_symbol(outcome.clone(), &[outcomeObj.clone()]);
        return self.filter_by_value_since_limit(trades.clone(), Value::Str("outcome".to_string()), &[sym.clone(), since.clone(), limit.clone(), Value::Str("timestamp".to_string()), Value::Bool(true)]);

    Value::Null
}

    pub fn handle_my_trade(&mut self, mut client: Value, mut message: Value) {
        //
        //     {
        //         "orderId": "3c7af25f-e21f-11f0-9714-0a58a9feac02",
        //         "tradeNo": "e1403840-e22f-11f0-83af-0a58a9feac02",
        //         "marketId": 2770,
        //         "rootMarketId": 122,
        //         "txHash": "0x272c...4195",
        //         "side": "Buy",
        //         "outcomeSide": 2,
        //         "price": "0.100000000000000000",
        //         "shares": "9.44444",
        //         "amount": "0.944444",
        //         "profit": "0.000000000000000000",
        //         "status": 2,
        //         "quoteToken": "0x55d398326f99059fF775485246999027B3197955",
        //         "fee": "0.000000000000000000",
        //         "chainId": "56",
        //         "createdAt": 1766735571,
        //         "msgType": "trade.record.new"
        //     }
        //
        let mut marketId: Value = self.safe_integer_k(message.clone(), "marketId", &[]);
        let mut outcomeSide: Value = self.safe_integer_k(message.clone(), "outcomeSide", &[]);
        let mut outcomeObj: Value = self.opinion_outcome_by_market_id_side(marketId.clone(), outcomeSide.clone());
        let mut sym: Value = self.safe_string_k(outcomeObj.clone(), "outcome", &[]);
        let mut timestamp: Value = self.safe_timestamp(message.clone(), Value::Str("createdAt".to_string()), &[]);
        let mut trade: Value = self.safe_prediction_trade(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), self.safe_string_k(message.clone(), "tradeNo", &[]));
        m.insert("info".to_string(), message.clone());
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp.clone()));
        m.insert("outcome".to_string(), sym.clone());
        m.insert("outcomeId".to_string(), self.safe_string_k(outcomeObj.clone(), "outcomeId", &[]));
        m.insert("label".to_string(), self.safe_string_k(outcomeObj.clone(), "label", &[]));
        m.insert("market".to_string(), self.safe_string_k(outcomeObj.clone(), "market", &[]));
        m.insert("order".to_string(), self.safe_string_k(message.clone(), "orderId", &[]));
        m.insert("type".to_string(), Value::Null);
        m.insert("side".to_string(), self.safe_string_lower(message.clone(), Value::Str("side".to_string()), &[]));
        m.insert("takerOrMaker".to_string(), Value::Null);
        m.insert("price".to_string(), self.safe_number_k(message.clone(), "price", &[]));
        m.insert("amount".to_string(), self.safe_number_k(message.clone(), "shares", &[]));
        m.insert("cost".to_string(), self.safe_number_k(message.clone(), "amount", &[]));
        m.insert("fee".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), self.safe_number_k(message.clone(), "fee", &[]));
        m.insert("currency".to_string(), Value::Str("USDT".to_string()));
    m
}));
    m
}), &[outcomeObj.clone()]);
        if is_equal(&self.myTrades, &Value::Null) {
            let mut myTradesLimit: Value = self.safe_integer_k(self.options.clone(), "myTradesLimit", &[Value::Int(1000)]);
            self.myTrades = ArrayCacheByOutcomeById::new(myTradesLimit.clone());
        }
        let mut stored: Value = self.myTrades.clone();
        stored.append(trade.clone());
        client.resolve(&[stored.clone(), Value::Str("myTrades".to_string())]);
}

    pub fn handle_errors(&self, mut code: Value, mut reason: Value, mut url: Value, mut method: Value, mut headers: Value, mut body: Value, mut response: Value, mut requestHeaders: Value, mut requestBody: Value) -> Value {
        if is_equal(&response, &Value::Null) {
            return Value::Null;
        }
        let mut errno: Value = self.safe_integer_k(response.clone(), "errno", &[]);
        if is_true(&(!is_equal(&errno, &Value::Null))) && is_true(&(!is_equal(&errno, &Value::Int(0)))) {
            let mut errmsg: Value = self.safe_string_k(response.clone(), "errmsg", &[Value::Str("".to_string())]);
            let mut feedback: Value = add(&add(&self.id, &Value::Str(" ".to_string())), &body);
            self.throw_exactly_matched_exception(get_value(&self.exceptions, &Value::Str("exact".to_string())), self.number_to_string(errno.clone()), feedback.clone());
            self.throw_broadly_matched_exception(get_value(&self.exceptions, &Value::Str("broad".to_string())), errmsg.clone(), feedback.clone());
            panic!("{}", crate::exchange_errors::exchange_error(feedback));
        }
        return Value::Null;

    Value::Null
}

/*
 * @ignore
 * @method
 * @name opinion#sign
 * @description builds the request url and attaches the apikey/EIP-712 authentication headers for private endpoints
 * @param {string} path the endpoint path
 * @param {string|string[]} api the api group and access level
 * @param {string} method the http method
 * @param {object} params the request parameters
 * @param {object} [headers] request headers
 * @param {string} [body] the request body
 * @returns {object} a dict with url, method, body and headers
 */
    pub fn sign(&self, mut path: Value, optional_args: &[Value]) -> Value {
        let mut api = get_arg(optional_args, 0, Value::Str("opinion".to_string()));
        let mut method = get_arg(optional_args, 1, Value::Str("GET".to_string()));
        let mut params = get_arg(optional_args, 2, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut headers = get_arg(optional_args, 3, Value::Null);
        let mut body = get_arg(optional_args, 4, Value::Null);
        let mut apiGroup: Value = ternary(is_string(&api), api.clone(), get_value(&api, &Value::Int(0)));
        let mut access: Value = ternary(is_string(&api), Value::Str("public".to_string()), get_value(&api, &Value::Int(1)));
        let mut baseUrls: Value = get_value(&self.urls, &Value::Str("api".to_string()));
        let mut baseUrl: Value = self.safe_string(baseUrls.clone(), apiGroup.clone(), &[get_value(&baseUrls, &Value::Str("opinion".to_string()))]);
        let mut url: Value = add(&add(&baseUrl, &Value::Str("/".to_string())), &self.implode_params(path.clone(), params.clone()));
        let mut query: Value = self.omit(params.clone(), self.extract_params(path.clone()), &[]);
        let mut existingHeaders: Value = ternary(is_true(&(!is_equal(&headers, &Value::Null))), headers.clone(), Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        headers = self.extend(Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("Accept".to_string(), Value::Str("application/json".to_string()));
                m.insert("Content-Type".to_string(), Value::Str("application/json".to_string()));
            m
        }), &[existingHeaders.clone()]);
        if is_equal(&access, &Value::Str("private".to_string())) {
            if is_equal(&path, &Value::Str("auth/api-key".to_string())) {
                // wallet-signature scheme: no apiKey involved, the signature itself is the credential
                if is_true(&(is_equal(&self.walletAddress, &Value::Null))) || is_true(&(is_equal(&self.privateKey, &Value::Null))) {
                    panic!("{}", crate::exchange_errors::arguments_required(add(&add(&add(&self.id, &Value::Str(" ".to_string())), &path), &Value::Str(" requires a walletAddress and privateKey".to_string()))));
                }
                let mut actionByMethod: Value = Value::Map({
                    let mut m = indexmap::IndexMap::new();
                        m.insert("POST".to_string(), Value::Str("create".to_string()));
                        m.insert("GET".to_string(), Value::Str("get".to_string()));
                        m.insert("DELETE".to_string(), Value::Str("delete".to_string()));
                    m
                });
                let mut action: Value = self.safe_string(actionByMethod.clone(), method.clone(), &[Value::Str("get".to_string())]);
                let mut timestamp: Value = self.number_to_string(self.seconds());
                add_element_to_object(&mut headers, &Value::Str("OPINION_ADDRESS".to_string()), self.walletAddress.clone());
                add_element_to_object(&mut headers, &Value::Str("OPINION_SIGNATURE".to_string()), self.sign_api_key_auth(self.walletAddress.clone(), action.clone(), timestamp.clone()));
                add_element_to_object(&mut headers, &Value::Str("OPINION_TIMESTAMP".to_string()), timestamp.clone());
            }  else {
                // an empty this.apiKey counts as absent - deleteApiKey clears it to '' (the
                // strict base types the credential as string, undefined can not be assigned)
                let mut hasDirectApiKey: Value = Value::Bool(!is_true(&self.is_empty_string(self.apiKey.clone())));
                let mut apiKey: Value = ternary(is_true(&(hasDirectApiKey)), self.apiKey.clone(), self.safe_string_k(self.options.clone(), "apiKey", &[]));
                if is_equal(&apiKey, &Value::Null) {
                    panic!("{}", crate::exchange_errors::authentication_error(add(&add(&add(&self.id, &Value::Str(" ".to_string())), &path), &Value::Str(" requires an apiKey - set it directly or call createApiKey()/fetchApiKey() first".to_string()))));
                }
                add_element_to_object(&mut headers, &Value::Str("apikey".to_string()), apiKey.clone());
            }
        }
        if is_equal(&method, &Value::Str("GET".to_string())) {
            if is_true(&get_array_length(&object_keys(&query))) {
                url = add(&url, &add(&Value::Str("?".to_string()), &self.urlencode(query.clone(), &[])));
            }
        }  else {
            body = self.json(query.clone());
        }
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("url".to_string(), url.clone());
        m.insert("method".to_string(), method.clone());
        m.insert("body".to_string(), body.clone());
        m.insert("headers".to_string(), headers.clone());
    m
});

    Value::Null
}
}
