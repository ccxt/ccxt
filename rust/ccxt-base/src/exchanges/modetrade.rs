// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#![allow(unused, non_snake_case, clippy::all)]
use crate::Value;
use crate::get_value;
use crate::runtime::*;
// Base methods are now trait methods (review #1: static dispatch). Bring the
// traits into scope so `self.market(...)`, `self.safe_market(...)`,
// `self.load_markets(...)`, … on this Core resolve to the base defaults.
use crate::exchange_generated::ExchangeBase;
use crate::exchange::ExchangeRuntime;
// Dynamic `this[method](...)` re-entries are emitted as
// `self.call_dynamic_checked(...)` (blanket-impl'd on every Core) so an
// unresolvable name raises NotSupported instead of yielding a silent Null.
use crate::exchange::CallDynamicChecked;


pub struct ModetradeCore {
    pub exchange: crate::exchange::Exchange,
}

impl ModetradeCore {
    pub fn new(config: Option<crate::Value>) -> Self {
        let mut s = Self { exchange: crate::exchange::Exchange::new(config) };
        s.init();
        s
    }

    pub fn init(&mut self) {
        let described = ModetradeCore::describe(self);
        self.initialize_properties(described);
        <Self as crate::exchange_generated::ExchangeBase>::after_construct(self);
    }

    /// Compatibility no-op. The old pointer-based dispatch needed a post-move
    /// `bind()`; static trait dispatch (review #1) needs no binding, so this
    /// just exists so callers that still call it keep compiling.
    #[inline]
    pub fn bind(&mut self) {}
}

impl crate::exchange::DerivedExchange for ModetradeCore {
    fn nonce(&self, ) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::nonce(self, )
    }
    fn parse_trade(&self, trade: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::parse_trade(self, trade, &[market])
    }
    fn parse_order(&self, order: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::parse_order(self, order, &[market])
    }
    fn parse_market(&self, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::parse_market(self, market)
    }
    fn parse_ohlcv(&self, ohlcv: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::parse_ohlcv(self, ohlcv, &[market])
    }
    fn parse_balance(&self, response: crate::Value) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::parse_balance(self, response)
    }
    fn parse_position(&self, position: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::parse_position(self, position, &[market])
    }
    fn parse_funding_rate(&self, rate: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::parse_funding_rate(self, rate, &[market])
    }
    fn parse_ledger_entry(&self, entry: crate::Value, currency: crate::Value) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::parse_ledger_entry(self, entry, &[currency])
    }
    fn parse_currency(&self, currency: crate::Value) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::parse_currency(self, currency)
    }
    fn parse_transaction(&self, transaction: crate::Value, currency: crate::Value) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::parse_transaction(self, transaction, &[currency])
    }
    fn parse_income(&self, info: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::parse_income(self, info, &[market])
    }
    fn parse_leverage(&self, leverage: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::parse_leverage(self, leverage, &[market])
    }
    fn sign(&self, path: crate::Value, api: crate::Value, method: crate::Value, params: crate::Value, headers: crate::Value, body: crate::Value) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::sign(self, path, &[api, method, params, headers, body])
    }
    fn handle_errors(&self, code: crate::Value, reason: crate::Value, url: crate::Value, method: crate::Value, headers: crate::Value, body: crate::Value, response: crate::Value, request_headers: crate::Value, request_body: crate::Value) -> crate::Value {
        // Forward to the inherent method on ModetradeCore.
        ModetradeCore::handle_errors(self, code, reason, url, method, headers, body, response, request_headers, request_body)
    }
}

impl crate::exchange_generated::ExchangeBase for ModetradeCore {
    fn call_dynamic<'a>(&'a mut self, method: &'a str, args: Vec<crate::Value>)
        -> std::pin::Pin<Box<dyn std::future::Future<Output = crate::Value> + Send + 'a>>
    {
        Box::pin(async move {
            match method {
                "cancel_all_orders" => self.cancel_all_orders(&args[..]).await,
                "cancel_order" => self.cancel_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "cancel_orders" => self.cancel_orders(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "create_order" => self.create_order(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), args.get(3).cloned().unwrap_or(crate::Value::Null), &args[4.min(args.len())..]).await,
                "create_order_request" => self.create_order_request(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), args.get(3).cloned().unwrap_or(crate::Value::Null), &args[4.min(args.len())..]),
                "create_orders" => self.create_orders(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "edit_order" => self.edit_order(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), args.get(3).cloned().unwrap_or(crate::Value::Null), &args[4.min(args.len())..]).await,
                "fetch_balance" => self.fetch_balance(&args[..]).await,
                "fetch_closed_orders" => self.fetch_closed_orders(&args[..]).await,
                "fetch_currencies" => self.fetch_currencies(&args[..]).await,
                "fetch_deposits" => self.fetch_deposits(&args[..]).await,
                "fetch_deposits_withdrawals" => self.fetch_deposits_withdrawals(&args[..]).await,
                "fetch_funding_history" => self.fetch_funding_history(&args[..]).await,
                "fetch_funding_interval" => self.fetch_funding_interval(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "fetch_funding_rate" => self.fetch_funding_rate(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "fetch_funding_rate_history" => self.fetch_funding_rate_history(&args[..]).await,
                "fetch_funding_rates" => self.fetch_funding_rates(&args[..]).await,
                "fetch_ledger" => self.fetch_ledger(&args[..]).await,
                "fetch_leverage" => self.fetch_leverage(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "fetch_markets" => self.fetch_markets(&args[..]).await,
                "fetch_my_trades" => self.fetch_my_trades(&args[..]).await,
                "fetch_ohlcv" => self.fetch_ohlcv(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "fetch_open_orders" => self.fetch_open_orders(&args[..]).await,
                "fetch_order" => self.fetch_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "fetch_order_book" => self.fetch_order_book(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "fetch_order_trades" => self.fetch_order_trades(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "fetch_orders" => self.fetch_orders(&args[..]).await,
                "fetch_position" => self.fetch_position(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "fetch_positions" => self.fetch_positions(&args[..]).await,
                "fetch_status" => self.fetch_status(&args[..]).await,
                "fetch_time" => self.fetch_time(&args[..]).await,
                "fetch_trades" => self.fetch_trades(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "fetch_trading_fees" => self.fetch_trading_fees(&args[..]).await,
                "fetch_withdrawals" => self.fetch_withdrawals(&args[..]).await,
                "get_asset_history_rows" => self.get_asset_history_rows(&args[..]).await,
                "get_withdraw_nonce" => self.get_withdraw_nonce(&args[..]).await,
                "handle_errors" => self.handle_errors(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), args.get(3).cloned().unwrap_or(crate::Value::Null), args.get(4).cloned().unwrap_or(crate::Value::Null), args.get(5).cloned().unwrap_or(crate::Value::Null), args.get(6).cloned().unwrap_or(crate::Value::Null), args.get(7).cloned().unwrap_or(crate::Value::Null), args.get(8).cloned().unwrap_or(crate::Value::Null)),
                "nonce" => self.nonce(),
                "parse_balance" => self.parse_balance(args.get(0).cloned().unwrap_or(crate::Value::Null)),
                "parse_currency" => self.parse_currency(args.get(0).cloned().unwrap_or(crate::Value::Null)),
                "parse_funding_rate" => self.parse_funding_rate(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "parse_income" => self.parse_income(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "parse_ledger_entry" => self.parse_ledger_entry(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "parse_leverage" => self.parse_leverage(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "parse_market" => self.parse_market(args.get(0).cloned().unwrap_or(crate::Value::Null)),
                "parse_ohlcv" => self.parse_ohlcv(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "parse_order" => self.parse_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "parse_position" => self.parse_position(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "parse_token_and_fee_temp" => self.parse_token_and_fee_temp(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null)),
                "parse_trade" => self.parse_trade(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "parse_transaction" => self.parse_transaction(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "set_leverage" => self.set_leverage(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "sign" => self.sign(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "withdraw" => self.withdraw(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), &args[3.min(args.len())..]).await,
                // Fall through to the base-only methods (cancelOrderWithClientOrderId, …).
                _ => self.call_dynamic_base(method, args).await,
            }
        })
    }
}

impl std::ops::Deref for ModetradeCore {
    type Target = crate::exchange::Exchange;
    fn deref(&self) -> &crate::exchange::Exchange { &self.exchange }
}

impl std::ops::DerefMut for ModetradeCore {
    fn deref_mut(&mut self) -> &mut crate::exchange::Exchange { &mut self.exchange }
}

impl ModetradeCore {
    pub fn describe(&self) -> Value {
        return self.deep_extend(self.super_describe(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), Value::Str("modetrade".into()));
        m.insert("name".to_string(), Value::Str("Mode Trade".into()));
        m.insert("countries".to_string(), Value::from(vec![Value::Str("KY".into())]));
        m.insert("rateLimit".to_string(), Value::Int(100));
        m.insert("version".to_string(), Value::Str("v1".into()));
        m.insert("certified".to_string(), Value::Bool(false));
        m.insert("pro".to_string(), Value::Bool(true));
        m.insert("dex".to_string(), Value::Bool(true));
        m.insert("has".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("CORS".to_string(), Value::Null);
        m.insert("spot".to_string(), Value::Bool(false));
        m.insert("margin".to_string(), Value::Bool(false));
        m.insert("swap".to_string(), Value::Bool(true));
        m.insert("future".to_string(), Value::Bool(false));
        m.insert("option".to_string(), Value::Bool(false));
        m.insert("addMargin".to_string(), Value::Bool(false));
        m.insert("cancelAllOrders".to_string(), Value::Bool(true));
        m.insert("cancelOrder".to_string(), Value::Bool(true));
        m.insert("cancelOrders".to_string(), Value::Bool(true));
        m.insert("cancelWithdraw".to_string(), Value::Bool(false));
        m.insert("closeAllPositions".to_string(), Value::Bool(false));
        m.insert("closePosition".to_string(), Value::Bool(false));
        m.insert("createConvertTrade".to_string(), Value::Bool(false));
        m.insert("createDepositAddress".to_string(), Value::Bool(false));
        m.insert("createMarketBuyOrderWithCost".to_string(), Value::Bool(false));
        m.insert("createMarketOrder".to_string(), Value::Bool(true));
        m.insert("createMarketOrderWithCost".to_string(), Value::Bool(false));
        m.insert("createMarketSellOrderWithCost".to_string(), Value::Bool(false));
        m.insert("createOrder".to_string(), Value::Bool(true));
        m.insert("createOrders".to_string(), Value::Bool(true));
        m.insert("createOrderWithTakeProfitAndStopLoss".to_string(), Value::Bool(true));
        m.insert("createReduceOnlyOrder".to_string(), Value::Bool(true));
        m.insert("createStopLimitOrder".to_string(), Value::Bool(true));
        m.insert("createStopLossOrder".to_string(), Value::Bool(true));
        m.insert("createStopMarketOrder".to_string(), Value::Bool(true));
        m.insert("createStopOrder".to_string(), Value::Bool(true));
        m.insert("createTakeProfitOrder".to_string(), Value::Bool(true));
        m.insert("createTrailingAmountOrder".to_string(), Value::Bool(false));
        m.insert("createTrailingPercentOrder".to_string(), Value::Bool(false));
        m.insert("createTriggerOrder".to_string(), Value::Bool(true));
        m.insert("editOrder".to_string(), Value::Bool(true));
        m.insert("fetchAccounts".to_string(), Value::Bool(false));
        m.insert("fetchBalance".to_string(), Value::Bool(true));
        m.insert("fetchCanceledOrders".to_string(), Value::Bool(false));
        m.insert("fetchClosedOrder".to_string(), Value::Bool(false));
        m.insert("fetchClosedOrders".to_string(), Value::Bool(true));
        m.insert("fetchConvertCurrencies".to_string(), Value::Bool(false));
        m.insert("fetchConvertQuote".to_string(), Value::Bool(false));
        m.insert("fetchCurrencies".to_string(), Value::Bool(true));
        m.insert("fetchDepositAddress".to_string(), Value::Bool(false));
        m.insert("fetchDeposits".to_string(), Value::Bool(true));
        m.insert("fetchDepositsWithdrawals".to_string(), Value::Bool(true));
        m.insert("fetchFundingHistory".to_string(), Value::Bool(true));
        m.insert("fetchFundingInterval".to_string(), Value::Bool(true));
        m.insert("fetchFundingIntervals".to_string(), Value::Bool(false));
        m.insert("fetchFundingRate".to_string(), Value::Bool(true));
        m.insert("fetchFundingRateHistory".to_string(), Value::Bool(true));
        m.insert("fetchFundingRates".to_string(), Value::Bool(true));
        m.insert("fetchIndexOHLCV".to_string(), Value::Bool(false));
        m.insert("fetchLedger".to_string(), Value::Bool(true));
        m.insert("fetchLeverage".to_string(), Value::Bool(true));
        m.insert("fetchMarginAdjustmentHistory".to_string(), Value::Bool(false));
        m.insert("fetchMarginMode".to_string(), Value::Bool(false));
        m.insert("fetchMarkets".to_string(), Value::Bool(true));
        m.insert("fetchMarkOHLCV".to_string(), Value::Bool(false));
        m.insert("fetchMyTrades".to_string(), Value::Bool(true));
        m.insert("fetchOHLCV".to_string(), Value::Bool(true));
        m.insert("fetchOpenInterestHistory".to_string(), Value::Bool(false));
        m.insert("fetchOpenOrder".to_string(), Value::Bool(false));
        m.insert("fetchOpenOrders".to_string(), Value::Bool(true));
        m.insert("fetchOrder".to_string(), Value::Bool(true));
        m.insert("fetchOrderBook".to_string(), Value::Bool(true));
        m.insert("fetchOrders".to_string(), Value::Bool(true));
        m.insert("fetchOrderTrades".to_string(), Value::Bool(true));
        m.insert("fetchPosition".to_string(), Value::Bool(true));
        m.insert("fetchPositionMode".to_string(), Value::Bool(false));
        m.insert("fetchPositions".to_string(), Value::Bool(true));
        m.insert("fetchPremiumIndexOHLCV".to_string(), Value::Bool(false));
        m.insert("fetchStatus".to_string(), Value::Bool(true));
        m.insert("fetchTicker".to_string(), Value::Bool(false));
        m.insert("fetchTickers".to_string(), Value::Bool(false));
        m.insert("fetchTime".to_string(), Value::Bool(true));
        m.insert("fetchTrades".to_string(), Value::Bool(true));
        m.insert("fetchTradingFee".to_string(), Value::Bool(false));
        m.insert("fetchTradingFees".to_string(), Value::Bool(true));
        m.insert("fetchTransactions".to_string(), Value::Str("emulated".into()));
        m.insert("fetchTransfers".to_string(), Value::Bool(false));
        m.insert("fetchWithdrawals".to_string(), Value::Bool(true));
        m.insert("reduceMargin".to_string(), Value::Bool(false));
        m.insert("setLeverage".to_string(), Value::Bool(true));
        m.insert("setMargin".to_string(), Value::Bool(false));
        m.insert("setPositionMode".to_string(), Value::Bool(false));
        m.insert("transfer".to_string(), Value::Bool(false));
        m.insert("withdraw".to_string(), Value::Bool(true));
    m
}));
        m.insert("timeframes".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("1m".to_string(), Value::Str("1m".into()));
        m.insert("5m".to_string(), Value::Str("5m".into()));
        m.insert("15m".to_string(), Value::Str("15m".into()));
        m.insert("30m".to_string(), Value::Str("30m".into()));
        m.insert("1h".to_string(), Value::Str("1h".into()));
        m.insert("4h".to_string(), Value::Str("4h".into()));
        m.insert("12h".to_string(), Value::Str("12h".into()));
        m.insert("1d".to_string(), Value::Str("1d".into()));
        m.insert("1w".to_string(), Value::Str("1w".into()));
        m.insert("1M".to_string(), Value::Str("1mon".into()));
        m.insert("1y".to_string(), Value::Str("1y".into()));
    m
}));
        m.insert("urls".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("logo".to_string(), Value::Str("https://github.com/user-attachments/assets/bbde7d00-6e40-404f-8f34-8fb15893eb24".into()));
        m.insert("api".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("public".to_string(), Value::Str("https://api-evm.orderly.org".into()));
        m.insert("private".to_string(), Value::Str("https://api-evm.orderly.org".into()));
    m
}));
        m.insert("test".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("public".to_string(), Value::Str("https://testnet-api-evm.orderly.org".into()));
        m.insert("private".to_string(), Value::Str("https://testnet-api-evm.orderly.org".into()));
    m
}));
        m.insert("www".to_string(), Value::Str("https://trade.mode.network".into()));
        m.insert("doc".to_string(), Value::from(vec![Value::Str("https://orderly.network/docs/build-on-omnichain/building-on-omnichain".into())]));
        m.insert("referral".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("url".to_string(), Value::Str("https://trade.mode.network?ref=MODETRADE".into()));
        m.insert("discount".to_string(), Value::Float(0.2));
    m
}));
    m
}));
        m.insert("api".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("v1".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("public".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("get".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("public/volume/stats".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/broker/name".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/chain_info/{broker_id}".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/system_info".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/vault_balance".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/insurancefund".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/chain_info".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("faucet/usdc".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/account".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("get_account".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("registration_nonce".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("get_orderly_key".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/liquidation".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/liquidated_positions".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/config".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/campaign/ranking".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("public/campaign/stats".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("public/campaign/user".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("public/campaign/stats/details".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("public/campaigns".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("public/points/leaderboard".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("client/points".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/points/epoch".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/points/epoch_dates".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/referral/check_ref_code".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/referral/verify_ref_code".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/admin_info".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/info".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/referee_info".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/referee_rebate_summary".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/referee_history".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/referral_history".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/rebate_summary".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("client/distribution_history".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("tv/config".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("tv/history".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("tv/symbol_info".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("tv/kline_history".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(20));
    m
}));
        m.insert("public/funding_rate_history".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/funding_rate/{symbol}".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Float(0.33));
    m
}));
        m.insert("public/funding_rates".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/info".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/info/{symbol}".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/market_trades".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/token".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/futures".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/futures/{symbol}".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
    m
}));
        m.insert("post".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("register_account".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("public/query".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
    m
}));
    m
}));
        m.insert("private".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("get".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("client/key_info".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(6));
    m
}));
        m.insert("client/orderly_key_ip_restriction".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(6));
    m
}));
        m.insert("order/{oid}".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("client/order/{client_order_id}".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("algo/order/{oid}".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("algo/client/order/{client_order_id}".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("orders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("algo/orders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("trade/{tid}".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("trades".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("order/{oid}/trades".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("client/liquidator_liquidations".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("liquidations".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("asset/history".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(60));
    m
}));
        m.insert("client/holding".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("withdraw_nonce".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("settle_nonce".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("pnl_settlement/history".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("internal_transfer_history".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("volume/user/daily".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(60));
    m
}));
        m.insert("volume/user/stats".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(60));
    m
}));
        m.insert("client/statistics".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(60));
    m
}));
        m.insert("client/info".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(60));
    m
}));
        m.insert("client/statistics/daily".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(60));
    m
}));
        m.insert("positions".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Float(3.33));
    m
}));
        m.insert("position/{symbol}".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Float(3.33));
    m
}));
        m.insert("funding_fee/history".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(30));
    m
}));
        m.insert("notification/inbox/notifications".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(60));
    m
}));
        m.insert("notification/inbox/unread".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(60));
    m
}));
        m.insert("volume/broker/daily".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(60));
    m
}));
        m.insert("broker/fee_rate/default".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("broker/user_info".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("broker/daily_fee_revenue".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("orderbook/{symbol}".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("kline".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("client/leverages".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("client/margin_modes".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/multi_level/admin".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("referral/multi_level/admin/info".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/multi_level/admin/referee_list".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/multi_level/admin/summary".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/multi_level/max_rebate_rate".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("referral/multi_level/rebate_info".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("referral/multi_level/referee_list".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/multi_level/statistics".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/multi_level/volume_prerequisite".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
    m
}));
        m.insert("post".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("orderly_key".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("client/set_orderly_key_ip_restriction".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(6));
    m
}));
        m.insert("client/reset_orderly_key_ip_restriction".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(6));
    m
}));
        m.insert("order".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("batch-order".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("algo/order".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("liquidation".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("claim_insurance_fund".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("withdraw_request".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("settle_pnl".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("internal_transfer".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("notification/inbox/mark_read".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(60));
    m
}));
        m.insert("notification/inbox/mark_read_all".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(60));
    m
}));
        m.insert("client/leverage".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(120));
    m
}));
        m.insert("client/leverages".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(120));
    m
}));
        m.insert("client/margin_mode".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("position_margin".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("client/maintenance_config".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(60));
    m
}));
        m.insert("delegate_signer".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("delegate_orderly_key".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("delegate_settle_pnl".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("delegate_withdraw_request".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("broker/fee_rate/set".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("broker/fee_rate/set_default".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("broker/fee_rate/default".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("referral/create".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("referral/update".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("referral/bind".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("referral/edit_split".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("referral/edit_referee_description".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/multi_level/admin".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("referral/multi_level/admin/update".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("referral/multi_level/admin/create/affiliate".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("referral/multi_level/admin/reset/affiliate".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("referral/multi_level/admin/update/affiliate".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("referral/multi_level/claim_code".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("referral/multi_level/rebate_rate/set_default".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
        m.insert("referral/multi_level/rebate_rate/update".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(10));
    m
}));
    m
}));
        m.insert("put".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("order".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("algo/order".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
    m
}));
        m.insert("delete".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("order".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("algo/order".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("client/order".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("algo/client/order".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("algo/orders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("orders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("batch-order".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
        m.insert("client/batch-order".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), Value::Int(1));
    m
}));
    m
}));
    m
}));
    m
}));
    m
}));
        m.insert("requiredCredentials".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("apiKey".to_string(), Value::Bool(true));
        m.insert("secret".to_string(), Value::Bool(true));
        m.insert("accountId".to_string(), Value::Bool(true));
        m.insert("privateKey".to_string(), Value::Bool(false));
    m
}));
        m.insert("fees".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("trading".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("tierBased".to_string(), Value::Bool(true));
        m.insert("percentage".to_string(), Value::Bool(true));
        m.insert("maker".to_string(), self.parse_number(Value::Str("0.0002".into()), &[]));
        m.insert("taker".to_string(), self.parse_number(Value::Str("0.0005".into()), &[]));
    m
}));
    m
}));
        m.insert("options".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("sandboxMode".to_string(), Value::Bool(false));
        m.insert("brokerId".to_string(), Value::Str("CCXTMODE".into()));
        m.insert("verifyingContractAddress".to_string(), Value::Str("0x6F7a338F2aA472838dEFD3283eB360d4Dff5D203".into()));
    m
}));
        m.insert("features".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("default".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("sandbox".to_string(), Value::Bool(true));
        m.insert("createOrder".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("triggerPrice".to_string(), Value::Bool(true));
        m.insert("triggerPriceType".to_string(), Value::Null);
        m.insert("triggerDirection".to_string(), Value::Bool(false));
        m.insert("stopLossPrice".to_string(), Value::Bool(false));
        m.insert("takeProfitPrice".to_string(), Value::Bool(false));
        m.insert("attachedStopLossTakeProfit".to_string(), Value::Null);
        m.insert("timeInForce".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("IOC".to_string(), Value::Bool(true));
        m.insert("FOK".to_string(), Value::Bool(true));
        m.insert("PO".to_string(), Value::Bool(true));
        m.insert("GTD".to_string(), Value::Bool(false));
    m
}));
        m.insert("hedged".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("leverage".to_string(), Value::Bool(false));
        m.insert("marketBuyByCost".to_string(), Value::Bool(false));
        m.insert("marketBuyRequiresPrice".to_string(), Value::Bool(false));
        m.insert("selfTradePrevention".to_string(), Value::Bool(false));
        m.insert("iceberg".to_string(), Value::Bool(false));
    m
}));
        m.insert("createOrders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("max".to_string(), Value::Int(10));
    m
}));
        m.insert("fetchMyTrades".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(500));
        m.insert("daysBack".to_string(), Value::Null);
        m.insert("untilDays".to_string(), Value::Int(100000));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
        m.insert("fetchOrder".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("trigger".to_string(), Value::Bool(true));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
        m.insert("fetchOpenOrders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(500));
        m.insert("trigger".to_string(), Value::Bool(true));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
        m.insert("fetchOrders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(500));
        m.insert("daysBack".to_string(), Value::Null);
        m.insert("untilDays".to_string(), Value::Int(100000));
        m.insert("trigger".to_string(), Value::Bool(true));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
        m.insert("fetchClosedOrders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(500));
        m.insert("daysBack".to_string(), Value::Null);
        m.insert("daysBackCanceled".to_string(), Value::Null);
        m.insert("untilDays".to_string(), Value::Int(100000));
        m.insert("trigger".to_string(), Value::Bool(true));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
        m.insert("fetchOHLCV".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("limit".to_string(), Value::Int(1000));
    m
}));
    m
}));
        m.insert("spot".to_string(), Value::Null);
        m.insert("forDerivatives".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("extends".to_string(), Value::Str("default".into()));
        m.insert("createOrder".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("triggerPriceType".to_string(), Value::Null);
        m.insert("attachedStopLossTakeProfit".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("triggerPriceType".to_string(), Value::Null);
        m.insert("price".to_string(), Value::Bool(false));
    m
}));
    m
}));
    m
}));
        m.insert("swap".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("linear".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("extends".to_string(), Value::Str("forDerivatives".into()));
    m
}));
        m.insert("inverse".to_string(), Value::Null);
    m
}));
        m.insert("future".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("linear".to_string(), Value::Null);
        m.insert("inverse".to_string(), Value::Null);
    m
}));
    m
}));
        m.insert("commonCurrencies".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        m.insert("exceptions".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("exact".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("-1000".to_string(), Value::Str("ExchangeError".into()).clone());
        m.insert("-1001".to_string(), Value::Str("AuthenticationError".into()).clone());
        m.insert("-1002".to_string(), Value::Str("AuthenticationError".into()).clone());
        m.insert("-1003".to_string(), Value::Str("RateLimitExceeded".into()).clone());
        m.insert("-1004".to_string(), Value::Str("BadRequest".into()).clone());
        m.insert("-1005".to_string(), Value::Str("BadRequest".into()).clone());
        m.insert("-1006".to_string(), Value::Str("InvalidOrder".into()).clone());
        m.insert("-1007".to_string(), Value::Str("BadRequest".into()).clone());
        m.insert("-1008".to_string(), Value::Str("InvalidOrder".into()).clone());
        m.insert("-1009".to_string(), Value::Str("InsufficientFunds".into()).clone());
        m.insert("-1011".to_string(), Value::Str("NetworkError".into()).clone());
        m.insert("-1012".to_string(), Value::Str("BadRequest".into()).clone());
        m.insert("-1101".to_string(), Value::Str("InsufficientFunds".into()).clone());
        m.insert("-1102".to_string(), Value::Str("InvalidOrder".into()).clone());
        m.insert("-1103".to_string(), Value::Str("InvalidOrder".into()).clone());
        m.insert("-1104".to_string(), Value::Str("InvalidOrder".into()).clone());
        m.insert("-1105".to_string(), Value::Str("InvalidOrder".into()).clone());
        m.insert("-1201".to_string(), Value::Str("BadRequest".into()).clone());
        m.insert("-1202".to_string(), Value::Str("BadRequest".into()).clone());
        m.insert("29".to_string(), Value::Str("BadRequest".into()).clone());
        m.insert("9".to_string(), Value::Str("AuthenticationError".into()).clone());
        m.insert("3".to_string(), Value::Str("AuthenticationError".into()).clone());
        m.insert("2".to_string(), Value::Str("BadRequest".into()).clone());
        m.insert("15".to_string(), Value::Str("BadRequest".into()).clone());
    m
}));
        m.insert("broad".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
    m
}));
        m.insert("precisionMode".to_string(), Value::Int(crate::runtime::TICK_SIZE));
    m
})]);

    Value::Null
}

    pub fn set_sandbox_mode(&mut self, mut enable: Value) {
        self.super_set_sandbox_mode(enable.clone());
        if let Value::Dict(__d) = &mut self.options { std::sync::Arc::make_mut(__d).insert("sandboxMode".to_string(), enable); }
}

/*
 * @method
 * @name modetrade#fetchStatus
 * @description the latest known information on the availability of the exchange API
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/public/get-system-maintenance-status
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [status structure]{@link https://docs.ccxt.com/?id=exchange-status-structure}
 */
    pub async fn fetch_status(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut response: Value = self.v1_public_get_public_system_info(&[params]).await;
        //
        //     {
        //         "success": true,
        //         "data": {
        //             "status": 0,
        //             "msg": "System is functioning properly."
        //         },
        //         "timestamp": "1709274106602"
        //     }
        //
        let mut data: Value = self.safe_dict_k(response.clone(), "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut status: Value = self.safe_string_k(data, "status", &[]);
        if (status == Value::Null) {
            status = Value::Str("error".into());
        }  else if (status.as_str() == Some("0")) {
            status = Value::Str("ok".into());
        }  else {
            status = Value::Str("maintenance".into());
        }
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("status".to_string(), status);
        m.insert("updated".to_string(), Value::Null);
        m.insert("eta".to_string(), Value::Null);
        m.insert("url".to_string(), Value::Null);
        m.insert("info".to_string(), response);
    m
});

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchTime
 * @description fetches the current integer timestamp in milliseconds from the exchange server
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/public/get-system-maintenance-status
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int} the current integer timestamp in milliseconds from the exchange server
 */
    pub async fn fetch_time(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut response: Value = self.v1_public_get_public_system_info(&[params]).await;
        return self.safe_integer_k(response, "timestamp", &[]);

    Value::Null
}

    pub fn parse_market(&self, mut market: Value) -> Value {
        //
        //   {
        //     "symbol": "PERP_BTC_USDC",
        //     "quote_min": 123,
        //     "quote_max": 100000,
        //     "quote_tick": 0.1,
        //     "base_min": 0.00001,
        //     "base_max": 20,
        //     "base_tick": 0.00001,
        //     "min_notional": 1,
        //     "price_range": 0.02,
        //     "price_scope": 0.4,
        //     "std_liquidation_fee": 0.03,
        //     "liquidator_fee": 0.015,
        //     "claim_insurance_fund_discount": 0.0075,
        //     "funding_period": 8,
        //     "cap_funding": 0.000375,
        //     "floor_funding": -0.000375,
        //     "interest_rate": 0.0001,
        //     "created_time": 1684140107326,
        //     "updated_time": 1685345968053,
        //     "base_mmr": 0.05,
        //     "base_imr": 0.1,
        //     "imr_factor": 0.0002512,
        //     "liquidation_tier": "1"
        //   }
        //
        let mut marketId: Value = self.safe_string_k(market.clone(), "symbol", &[Value::Str("".into())]);
        let mut parts: Value = split(&marketId, &Value::Str("_".into()));
        let mut marketType: Value = Value::Str("swap".into());
        let mut baseId: Value = self.safe_string(parts.clone(), Value::Int(1), &[]);
        let mut quoteId: Value = self.safe_string(parts.clone(), Value::Int(2), &[]);
        let mut base: Value = self.safe_currency_code(baseId.clone(), &[]);
        let mut quote: Value = self.safe_currency_code(quoteId.clone(), &[]);
        let mut settleId: Value = self.safe_string(parts, Value::Int(2), &[]);
        let mut settle: Value = self.safe_currency_code(settleId.clone(), &[]);
        let mut symbol: Value = Value::Str(format!("{}{}", Value::Str(format!("{}{}", Value::Str(format!("{}{}", Value::Str(format!("{}{}", base, Value::Str("/".into())).into()), quote).into()), Value::Str(":".into())).into()), settle).into());
        return self.safe_market_structure(&[Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), marketId);
        m.insert("symbol".to_string(), symbol);
        m.insert("base".to_string(), base);
        m.insert("quote".to_string(), quote);
        m.insert("settle".to_string(), settle);
        m.insert("baseId".to_string(), baseId);
        m.insert("quoteId".to_string(), quoteId);
        m.insert("settleId".to_string(), settleId);
        m.insert("type".to_string(), marketType);
        m.insert("spot".to_string(), Value::Bool(false));
        m.insert("margin".to_string(), Value::Bool(false));
        m.insert("swap".to_string(), Value::Bool(true));
        m.insert("future".to_string(), Value::Bool(false));
        m.insert("option".to_string(), Value::Bool(false));
        m.insert("active".to_string(), Value::Null);
        m.insert("contract".to_string(), Value::Bool(true));
        m.insert("linear".to_string(), Value::Bool(true));
        m.insert("inverse".to_string(), Value::Bool(false));
        m.insert("contractSize".to_string(), self.parse_number(Value::Str("1".into()), &[]));
        m.insert("expiry".to_string(), Value::Null);
        m.insert("expiryDatetime".to_string(), Value::Null);
        m.insert("strike".to_string(), Value::Null);
        m.insert("optionType".to_string(), Value::Null);
        m.insert("precision".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("amount".to_string(), self.safe_number_k(market.clone(), "base_tick", &[]));
        m.insert("price".to_string(), self.safe_number_k(market.clone(), "quote_tick", &[]));
    m
}));
        m.insert("limits".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("leverage".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Null);
        m.insert("max".to_string(), Value::Null);
    m
}));
        m.insert("amount".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), self.safe_number_k(market.clone(), "base_min", &[]));
        m.insert("max".to_string(), self.safe_number_k(market.clone(), "base_max", &[]));
    m
}));
        m.insert("price".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), self.safe_number_k(market.clone(), "quote_min", &[]));
        m.insert("max".to_string(), self.safe_number_k(market.clone(), "quote_max", &[]));
    m
}));
        m.insert("cost".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), self.safe_number_k(market.clone(), "min_notional", &[]));
        m.insert("max".to_string(), Value::Null);
    m
}));
    m
}));
        m.insert("created".to_string(), self.safe_integer_k(market.clone(), "created_time", &[]));
        m.insert("info".to_string(), market);
    m
})]);

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchMarkets
 * @description retrieves data on all markets for modetrade
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/public/get-available-symbols
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} an array of objects representing market data
 */
    pub async fn fetch_markets(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut response: Value = self.v1_public_get_public_info(&[params]).await;
        //
        //   {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //       "rows": [
        //         {
        //           "symbol": "PERP_BTC_USDC",
        //           "quote_min": 123,
        //           "quote_max": 100000,
        //           "quote_tick": 0.1,
        //           "base_min": 0.00001,
        //           "base_max": 20,
        //           "base_tick": 0.00001,
        //           "min_notional": 1,
        //           "price_range": 0.02,
        //           "price_scope": 0.4,
        //           "std_liquidation_fee": 0.03,
        //           "liquidator_fee": 0.015,
        //           "claim_insurance_fund_discount": 0.0075,
        //           "funding_period": 8,
        //           "cap_funding": 0.000375,
        //           "floor_funding": -0.000375,
        //           "interest_rate": 0.0001,
        //           "created_time": 1684140107326,
        //           "updated_time": 1685345968053,
        //           "base_mmr": 0.05,
        //           "base_imr": 0.1,
        //           "imr_factor": 0.0002512,
        //           "liquidation_tier": "1"
        //         }
        //       ]
        //     }
        //   }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut rows: Value = self.safe_list_k(data, "rows", &[Value::from(vec![])]);
        return self.parse_markets(rows);

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchCurrencies
 * @description fetches all available currencies on an exchange
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/public/get-supported-collateral-info
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an associative dictionary of currencies
 */
    pub async fn fetch_currencies(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut response: Value = self.v1_public_get_public_token(&[params]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //       "rows": [{
        //         "token": "USDC",
        //         "decimals": 6,
        //         "minimum_withdraw_amount": 0.000001,
        //         "token_hash": "0xd6aca1be9729c13d677335161321649cccae6a591554772516700f986f942eaa",
        //         "chain_details": [{
        //             "chain_id": 43113,
        //             "contract_address": "0x5d64c9cfb0197775b4b3ad9be4d3c7976e0d8dc3",
        //             "cross_chain_withdrawal_fee": 123,
        //             "decimals": 6,
        //             "withdraw_fee": 2
        //             }]
        //         }
        //       ]
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut tokenRows: Value = self.safe_list_k(data, "rows", &[Value::from(vec![])]);
        return self.parse_currencies(tokenRows);

    Value::Null
}

    pub fn parse_currency(&self, mut rawCurrency: Value) -> Value {
        let mut currencyId: Value = self.safe_string_k(rawCurrency.clone(), "token", &[]);
        let mut networks: Value = self.safe_list_k(rawCurrency.clone(), "chain_details", &[Value::from(vec![])]);
        let mut code: Value = self.safe_currency_code(currencyId.clone(), &[]);
        let mut minPrecision: Value = Value::Null;
        let mut resultingNetworks: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        {
                        let mut j: Value = Value::Int(0);
            let mut __for_first_952: bool = true;
            while { if !__for_first_952 { j = (match (&(j), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_952 = false; j.as_f64().unwrap_or(f64::NAN) < ((networks.len() as i64) as f64) } {
            let mut network: Value = networks.as_array().and_then(|__arr| match &j { Value::Int(__n) => __arr.get(*__n as usize), Value::Str(__s) => __s.parse::<usize>().ok().and_then(|__n| __arr.get(__n)), _ => None }).cloned().unwrap_or(Value::Null);
            // TODO: transform chain id to human readable name
            let mut networkId: Value = self.safe_string_k(network.clone(), "chain_id", &[Value::Str("".into())]);
            let mut precision: Value = self.parse_precision(&[self.safe_string_k(network.clone(), "decimals", &[])]);
            if (precision != Value::Null) {
                minPrecision = (if (minPrecision == Value::Null) { precision.clone() } else { crate::precise::Precise::stringMin(&precision, &minPrecision) });
            }
            add_element_to_object(&mut resultingNetworks, &networkId, Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), networkId.clone());
        m.insert("network".to_string(), networkId.clone());
        m.insert("limits".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("withdraw".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Null);
        m.insert("max".to_string(), Value::Null);
    m
}));
        m.insert("deposit".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Null);
        m.insert("max".to_string(), Value::Null);
    m
}));
    m
}));
        m.insert("active".to_string(), Value::Null);
        m.insert("deposit".to_string(), Value::Null);
        m.insert("withdraw".to_string(), Value::Null);
        m.insert("fee".to_string(), self.safe_number_k(network.clone(), "withdrawal_fee", &[]));
        m.insert("precision".to_string(), self.parse_number(precision, &[]));
        m.insert("info".to_string(), network);
    m
}));
        }
        }
        return self.safe_currency_structure(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), currencyId.clone());
        m.insert("name".to_string(), currencyId);
        m.insert("code".to_string(), code);
        m.insert("precision".to_string(), self.parse_number(minPrecision, &[]));
        m.insert("active".to_string(), Value::Null);
        m.insert("fee".to_string(), Value::Null);
        m.insert("networks".to_string(), resultingNetworks);
        m.insert("deposit".to_string(), Value::Null);
        m.insert("withdraw".to_string(), Value::Null);
        m.insert("limits".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("deposit".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Null);
        m.insert("max".to_string(), Value::Null);
    m
}));
        m.insert("withdraw".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), self.safe_number_k(rawCurrency.clone(), "minimum_withdraw_amount", &[]));
        m.insert("max".to_string(), Value::Null);
    m
}));
    m
}));
        m.insert("info".to_string(), rawCurrency);
    m
}));

    Value::Null
}

    pub fn parse_token_and_fee_temp(&self, mut item: Value, mut feeTokenKey: Value, mut feeAmountKey: Value) -> Value {
        let mut feeCost: Value = self.safe_string(item.clone(), feeAmountKey, &[]);
        let mut fee: Value = Value::Null;
        if (feeCost != Value::Null) {
            let mut feeCurrencyId: Value = self.safe_string(item, feeTokenKey, &[]);
            let mut feeCurrencyCode: Value = self.safe_currency_code(feeCurrencyId, &[]);
            fee = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("cost".to_string(), feeCost);
                    m.insert("currency".to_string(), feeCurrencyCode);
                m
            });
        }
        return fee;

    Value::Null
}

    pub fn parse_trade(&self, mut trade: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        // public/market_trades
        //
        //     {
        //         "symbol": "PERP_ETH_USDC",
        //         "side": "SELL",
        //         "executed_price": 46222.35,
        //         "executed_quantity": 0.0012,
        //         "executed_timestamp": "1683878609166"
        //     }
        //
        // fetchOrderTrades, fetchOrder
        //
        //     {
        //         "id": "99119876",
        //         "symbol": "PERP_BTC_USDC",
        //         "fee": "0.0024",
        //         "side": "BUY",
        //         "executed_timestamp": "1641481113084",
        //         "order_id": "87001234",
        //         "order_tag": "default", <-- this param only in "fetchOrderTrades"
        //         "executed_price": "1",
        //         "executed_quantity": "12",
        //         "fee_asset": "BTC",
        //         "is_maker": "1"
        //     }
        //
        let mut isFromFetchOrder: bool = matches!(&trade, Value::Dict(__d) if __d.contains_key("id"));
        let mut timestamp: Value = self.safe_integer_k(trade.clone(), "executed_timestamp", &[]);
        let mut marketId: Value = self.safe_string_k(trade.clone(), "symbol", &[]);
        market = self.safe_market(&[marketId, market.clone()]);
        let mut symbol: Value = market.as_map().and_then(|__m| __m.get("symbol")).cloned().unwrap_or(Value::Null);
        let mut price: Value = self.safe_string_k(trade.clone(), "executed_price", &[]);
        let mut amount: Value = self.safe_string_k(trade.clone(), "executed_quantity", &[]);
        let mut order_id: Value = self.safe_string_k(trade.clone(), "order_id", &[]);
        let mut fee: Value = self.parse_token_and_fee_temp(trade.clone(), Value::Str("fee_asset".into()), Value::Str("fee".into()));
        let mut feeCost: Value = self.safe_string_k(fee.clone(), "cost", &[]);
        if (feeCost != Value::Null) && (fee != Value::Null) {
            if let Value::Dict(__d) = &mut fee { std::sync::Arc::make_mut(__d).insert("cost".to_string(), feeCost); }
        }
        let mut cost: Value = crate::precise::Precise::stringMul(&price, &amount);
        let mut side: Value = self.safe_string_lower(trade.clone(), Value::Str("side".into()), &[]);
        let mut id: Value = self.safe_string_k(trade.clone(), "id", &[]);
        let mut takerOrMaker: Value = Value::Null;
        if isFromFetchOrder {
            let mut isMaker: bool = self.safe_string_k(trade.clone(), "is_maker", &[]).as_str() == Some("1");
            takerOrMaker = (if isMaker { Value::Str("maker".into()) } else { Value::Str("taker".into()) });
        }
        return self.safe_trade(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), id);
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp));
        m.insert("symbol".to_string(), symbol);
        m.insert("side".to_string(), side);
        m.insert("price".to_string(), price);
        m.insert("amount".to_string(), amount);
        m.insert("cost".to_string(), cost);
        m.insert("order".to_string(), order_id);
        m.insert("takerOrMaker".to_string(), takerOrMaker);
        m.insert("type".to_string(), Value::Null);
        m.insert("fee".to_string(), fee);
        m.insert("info".to_string(), trade);
    m
}), &[market]);

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchTrades
 * @description get the list of most recent trades for a particular symbol
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/public/get-market-trades
 * @param {string} symbol unified symbol of the market to fetch trades for
 * @param {int} [since] timestamp in ms of the earliest trade to fetch
 * @param {int} [limit] the maximum amount of trades to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {Trade[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=public-trades}
 */
    pub async fn fetch_trades(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut since = get_arg(optional_args, 0, Value::Null);
        let mut limit = get_arg(optional_args, 1, Value::Null);
        let mut params = get_arg(optional_args, 2, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
            m
        });
        if (limit != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("limit".to_string(), limit.clone()); }
        }
        let __ws_arg_0 = self.extend(request, &[params]);
        let mut response: Value = self.v1_public_get_public_market_trades(&[__ws_arg_0]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //       "rows": [{
        //         "symbol": "PERP_ETH_USDC",
        //         "side": "BUY",
        //         "executed_price": 2050,
        //         "executed_quantity": 1,
        //         "executed_timestamp": 1683878609166
        //       }]
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut rows: Value = self.safe_list_k(data, "rows", &[Value::from(vec![])]);
        return self.parse_trades(rows, &[market, since, limit]);

    Value::Null
}

    pub fn parse_funding_rate(&self, mut fundingRate: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        //         {
        //             "symbol":"PERP_AAVE_USDT",
        //             "est_funding_rate":-0.00003447,
        //             "est_funding_rate_timestamp":1653633959001,
        //             "last_funding_rate":-0.00002094,
        //             "last_funding_rate_timestamp":1653631200000,
        //             "next_funding_time":1653634800000,
        //            "sum_unitary_funding": 521.367
        //         }
        //
        let mut symbol: Value = self.safe_string_k(fundingRate.clone(), "symbol", &[]);
        market = (if (symbol == Value::Null) { market.clone() } else { self.market(symbol) });
        let mut nextFundingTimestamp: Value = self.safe_integer_k(fundingRate.clone(), "next_funding_time", &[]);
        let mut estFundingRateTimestamp: Value = self.safe_integer_k(fundingRate.clone(), "est_funding_rate_timestamp", &[]);
        let mut lastFundingRateTimestamp: Value = self.safe_integer_k(fundingRate.clone(), "last_funding_rate_timestamp", &[]);
        let mut fundingTimeString: Value = self.safe_string_k(fundingRate.clone(), "last_funding_rate_timestamp", &[]);
        let mut nextFundingTimeString: Value = self.safe_string_k(fundingRate.clone(), "next_funding_time", &[]);
        let mut millisecondsInterval: Value = crate::precise::Precise::stringSub(&nextFundingTimeString, &fundingTimeString);
        let mut fundingSymbol: Value = (if (market != Value::Null) { market.as_map().and_then(|__m| __m.get("symbol")).cloned().unwrap_or(Value::Null) } else { Value::Null });
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), fundingRate.clone());
        m.insert("symbol".to_string(), fundingSymbol);
        m.insert("markPrice".to_string(), Value::Null);
        m.insert("indexPrice".to_string(), Value::Null);
        m.insert("interestRate".to_string(), self.parse_number(Value::Str("0".into()), &[]));
        m.insert("estimatedSettlePrice".to_string(), Value::Null);
        m.insert("timestamp".to_string(), estFundingRateTimestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(estFundingRateTimestamp));
        m.insert("fundingRate".to_string(), self.safe_number_k(fundingRate.clone(), "est_funding_rate", &[]));
        m.insert("fundingTimestamp".to_string(), nextFundingTimestamp.clone());
        m.insert("fundingDatetime".to_string(), self.iso8601(nextFundingTimestamp));
        m.insert("nextFundingRate".to_string(), Value::Null);
        m.insert("nextFundingTimestamp".to_string(), Value::Null);
        m.insert("nextFundingDatetime".to_string(), Value::Null);
        m.insert("previousFundingRate".to_string(), self.safe_number_k(fundingRate, "last_funding_rate", &[]));
        m.insert("previousFundingTimestamp".to_string(), lastFundingRateTimestamp.clone());
        m.insert("previousFundingDatetime".to_string(), self.iso8601(lastFundingRateTimestamp));
        m.insert("interval".to_string(), self.parse_funding_interval(millisecondsInterval).map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null));
    m
});

    Value::Null
}

    pub fn parse_funding_interval(&self, mut interval: Value) -> Option<String> {
        let mut intervals: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("3600000".to_string(), Value::Str("1h".into()));
                m.insert("14400000".to_string(), Value::Str("4h".into()));
                m.insert("28800000".to_string(), Value::Str("8h".into()));
                m.insert("57600000".to_string(), Value::Str("16h".into()));
                m.insert("86400000".to_string(), Value::Str("24h".into()));
            m
        });
        return self.safe_string(intervals, interval.clone(), &[interval.clone()]).as_str().map(str::to_owned);
}

/*
 * @method
 * @name modetrade#fetchFundingInterval
 * @description fetch the current funding rate interval
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/public/get-predicted-funding-rate-for-one-market
 * @param {string} symbol unified market symbol
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [funding rate structure]{@link https://docs.ccxt.com/?id=funding-rate-structure}
 */
    pub async fn fetch_funding_interval(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        return self.fetch_funding_rate(symbol, &[params]).await;

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchFundingRate
 * @description fetch the current funding rate
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/public/get-predicted-funding-rate-for-one-market
 * @param {string} symbol unified market symbol
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [funding rate structure]{@link https://docs.ccxt.com/?id=funding-rate-structure}
 */
    pub async fn fetch_funding_rate(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
            m
        });
        let __ws_arg_1 = self.extend(request, &[params]);
        let mut response: Value = self.v1_public_get_public_funding_rate_symbol(&[__ws_arg_1]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //         "symbol": "PERP_ETH_USDC",
        //         "est_funding_rate": 123,
        //         "est_funding_rate_timestamp": 1683880020000,
        //         "last_funding_rate": 0.0001,
        //         "last_funding_rate_timestamp": 1683878400000,
        //         "next_funding_time": 1683907200000,
        //         "sum_unitary_funding": 521.367
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        return self.parse_funding_rate(data, &[market]);

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchFundingRates
 * @description fetch the current funding rate for multiple markets
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/public/get-predicted-funding-rates-for-all-markets
 * @param {string[]} symbols unified market symbols
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} an array of [funding rate structures]{@link https://docs.ccxt.com/?id=funding-rate-structure}
 */
    pub async fn fetch_funding_rates(&mut self, optional_args: &[Value]) -> Value {
        let mut symbols = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        symbols = self.market_symbols(&[symbols.clone()]);
        let mut response: Value = self.v1_public_get_public_funding_rates(&[params]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //       "rows": [{
        //         "symbol": "PERP_ETH_USDC",
        //         "est_funding_rate": 123,
        //         "est_funding_rate_timestamp": 1683880020000,
        //         "last_funding_rate": 0.0001,
        //         "last_funding_rate_timestamp": 1683878400000,
        //         "next_funding_time": 1683907200000,
        //         "sum_unitary_funding": 521.367
        //       }]
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut rows: Value = self.safe_list_k(data, "rows", &[Value::from(vec![])]);
        return self.parse_funding_rates(rows, &[symbols]);

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchFundingRateHistory
 * @description fetches historical funding rate prices
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/public/get-funding-rate-history-for-one-market
 * @param {string} symbol unified symbol of the market to fetch the funding rate history for
 * @param {int} [since] timestamp in ms of the earliest funding rate to fetch
 * @param {int} [limit] the maximum amount of [funding rate structures]{@link https://docs.ccxt.com/?id=funding-rate-history-structure} to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] timestamp in ms of the latest funding rate
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [availble parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns {object[]} a list of [funding rate structures]{@link https://docs.ccxt.com/?id=funding-rate-history-structure}
 */
    pub async fn fetch_funding_rate_history(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut paginate: Value = Value::Bool(false);
        { let __destr_tmp = self.handle_option_and_params(params.clone(), Value::Str("fetchFundingRateHistory".into()), Value::Str("paginate".into()), &[]); paginate = __destr_tmp.as_array().and_then(|__arr| __arr.get(0)).cloned().unwrap_or(Value::Null); params = __destr_tmp.as_array().and_then(|__arr| __arr.get(1)).cloned().unwrap_or(Value::Null); }
        if is_true(&paginate) {
            return self.fetch_paginated_call_incremental(Value::Str("fetchFundingRateHistory".into()), &[symbol.clone(), since.clone(), limit.clone(), params.clone(), Value::Str("page".into()), Value::Int(25)]).await;
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        if (symbol != Value::Null) {
            let mut market: Value = self.market(symbol.clone());
            symbol = market.as_map().and_then(|__m| __m.get("symbol")).cloned().unwrap_or(Value::Null);
            add_element_to_object(&mut request, &Value::Str("symbol".into()), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
        }
        if (since != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("start_t".to_string(), since.clone()); }
        }
        { let __destr_tmp = self.handle_until_option(Value::Str("end_t".into()), request.clone(), params.clone(), &[Value::Float(0.001)]); request = __destr_tmp.as_array().and_then(|__arr| __arr.get(0)).cloned().unwrap_or(Value::Null); params = __destr_tmp.as_array().and_then(|__arr| __arr.get(1)).cloned().unwrap_or(Value::Null); }
        let __ws_arg_2 = self.extend(request, &[params]);
        let mut response: Value = self.v1_public_get_public_funding_rate_history(&[__ws_arg_2]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //       "rows": [{
        //         "symbol": "PERP_ETH_USDC",
        //         "funding_rate": 0.0001,
        //         "funding_rate_timestamp": 1684224000000,
        //         "next_funding_time": 1684252800000
        //       }],
        //       "meta": {
        //         "total": 9,
        //         "records_per_page": 25,
        //         "current_page": 1
        //       }
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut result: Value = self.safe_list_k(data, "rows", &[Value::from(vec![])]);
        let mut rates: Value = Value::from(vec![]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_953: bool = true;
            while { if !__for_first_953 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_953 = false; i.as_f64().unwrap_or(f64::NAN) < ((result.len() as i64) as f64) } {
            let mut entry: Value = result.as_array().and_then(|__arr| match &i { Value::Int(__n) => __arr.get(*__n as usize), Value::Str(__s) => __s.parse::<usize>().ok().and_then(|__n| __arr.get(__n)), _ => None }).cloned().unwrap_or(Value::Null);
            let mut marketId: Value = self.safe_string_k(entry.clone(), "symbol", &[]);
            let mut timestamp: Value = self.safe_integer_k(entry.clone(), "funding_rate_timestamp", &[]);
            append_to_array(&mut rates, Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("info".to_string(), entry.clone());
                    m.insert("symbol".to_string(), self.safe_symbol(marketId, &[]));
                    m.insert("fundingRate".to_string(), self.safe_number_k(entry, "funding_rate", &[]));
                    m.insert("timestamp".to_string(), timestamp.clone());
                    m.insert("datetime".to_string(), self.iso8601(timestamp));
                m
            }));
        }
        }
        let mut sorted: Value = self.sort_by(rates, Value::Str("timestamp".into()), &[]);
        return self.filter_by_symbol_since_limit(sorted, &[symbol, since, limit]);

    Value::Null
}

    pub fn parse_income(&self, mut income: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        // {
        //         "symbol": "PERP_ETH_USDC",
        //         "funding_rate": 0.00046875,
        //         "mark_price": 2100,
        //         "funding_fee": 0.000016,
        //         "payment_type": "Pay",
        //         "status": "Accrued",
        //         "created_time": 1682235722003,
        //         "updated_time": 1682235722003
        // }
        //
        let mut marketId: Value = self.safe_string_k(income.clone(), "symbol", &[]);
        let mut symbol: Value = self.safe_symbol(marketId, &[market]);
        let mut amount: Value = self.safe_string_k(income.clone(), "funding_fee", &[]);
        let mut code: Value = self.safe_currency_code(Value::Str("USDC".into()), &[]);
        let mut timestamp: Value = self.safe_integer_k(income.clone(), "updated_time", &[]);
        let mut rate: Value = self.safe_number_k(income.clone(), "funding_rate", &[]);
        let mut paymentType: Option<String> = self.safe_string_k(income.clone(), "payment_type", &[]).as_str().map(str::to_owned);
        amount = (if (paymentType.as_deref() == Some("Pay")) { crate::precise::Precise::stringNeg(&amount) } else { amount.clone() });
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), income);
        m.insert("symbol".to_string(), symbol);
        m.insert("code".to_string(), code);
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp));
        m.insert("id".to_string(), Value::Null);
        m.insert("amount".to_string(), self.parse_number(amount, &[]));
        m.insert("rate".to_string(), rate);
    m
});

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchFundingHistory
 * @description fetch the history of funding payments paid and received on this account
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-funding-fee-history
 * @param {string} [symbol] unified market symbol
 * @param {int} [since] the earliest time in ms to fetch funding history for
 * @param {int} [limit] the maximum number of funding history structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [availble parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns {object} a [funding history structure]{@link https://docs.ccxt.com/?id=funding-history-structure}
 */
    pub async fn fetch_funding_history(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut paginate: Value = Value::Bool(false);
        { let __destr_tmp = self.handle_option_and_params(params.clone(), Value::Str("fetchFundingHistory".into()), Value::Str("paginate".into()), &[]); paginate = __destr_tmp.as_array().and_then(|__arr| __arr.get(0)).cloned().unwrap_or(Value::Null); params = __destr_tmp.as_array().and_then(|__arr| __arr.get(1)).cloned().unwrap_or(Value::Null); }
        if is_true(&paginate) {
            return self.fetch_paginated_call_incremental(Value::Str("fetchFundingHistory".into()), &[symbol.clone(), since.clone(), limit.clone(), params.clone(), Value::Str("page".into()), Value::Int(500)]).await;
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut market: Value = Value::Null;
        if (symbol != Value::Null) {
            market = self.market(symbol);
            if let Value::Dict(__d12) = &mut request { std::sync::Arc::make_mut(__d12).insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null)); }
        }
        if (since != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("start_t".to_string(), since.clone()); }
        }
        let mut until: Value = self.safe_integer_k(params.clone(), "until", &[]); // unified in milliseconds
        params = self.omit(params.clone(), Value::from(vec![Value::Str("until".into())]), &[]);
        if (until != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("end_t".to_string(), until); }
        }
        if (limit != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("size".to_string(), crate::runtime::Math::min(&limit, &Value::Int(500))); }
        }
        let __ws_arg_3 = self.extend(request, &[params]);
        let mut response: Value = self.v1_private_get_funding_fee_history(&[__ws_arg_3]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //         "meta": {
        //             "total": 9,
        //             "records_per_page": 25,
        //             "current_page": 1
        //         },
        //         "rows": [{
        //                 "symbol": "PERP_ETH_USDC",
        //                 "funding_rate": 0.00046875,
        //                 "mark_price": 2100,
        //                 "funding_fee": 0.000016,
        //                 "payment_type": "Pay",
        //                 "status": "Accrued",
        //                 "created_time": 1682235722003,
        //                 "updated_time": 1682235722003
        //         }]
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut rows: Value = self.safe_list_k(data, "rows", &[Value::from(vec![])]);
        return self.parse_incomes(rows, &[market, since, limit]);

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchTradingFees
 * @description fetch the trading fees for multiple markets
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-account-information
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a dictionary of [fee structures]{@link https://docs.ccxt.com/?id=fee-structure} indexed by market symbols
 */
    pub async fn fetch_trading_fees(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut response: Value = self.v1_private_get_client_info(&[params]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //         "account_id": "<string>",
        //         "email": "test@test.com",
        //         "account_mode": "FUTURES",
        //         "max_leverage": 20,
        //         "taker_fee_rate": 123,
        //         "maker_fee_rate": 123,
        //         "futures_taker_fee_rate": 123,
        //         "futures_maker_fee_rate": 123,
        //         "maintenance_cancel_orders": true,
        //         "imr_factor": {
        //             "PERP_BTC_USDC": 123,
        //             "PERP_ETH_USDC": 123,
        //             "PERP_NEAR_USDC": 123
        //         },
        //         "max_notional": {
        //             "PERP_BTC_USDC": 123,
        //             "PERP_ETH_USDC": 123,
        //             "PERP_NEAR_USDC": 123
        //         }
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response.clone(), "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut maker: Value = self.safe_string_k(data.clone(), "futures_maker_fee_rate", &[]);
        let mut taker: Value = self.safe_string_k(data, "futures_taker_fee_rate", &[]);
        let mut result: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut symbols: Value = self.symbols.clone();
        if (symbols != Value::Null) {
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_954: bool = true;
                while { if !__for_first_954 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_954 = false; i.as_f64().unwrap_or(f64::NAN) < ((symbols.len() as i64) as f64) } {
                let mut symbol: Value = symbols.as_array().and_then(|__arr| match &i { Value::Int(__n) => __arr.get(*__n as usize), Value::Str(__s) => __s.parse::<usize>().ok().and_then(|__n| __arr.get(__n)), _ => None }).cloned().unwrap_or(Value::Null);
                add_element_to_object(&mut result, &symbol, Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), response.clone());
        m.insert("symbol".to_string(), symbol.clone());
        m.insert("maker".to_string(), self.parse_number(crate::precise::Precise::stringDiv(&maker, &Value::Str("10000".into())), &[]));
        m.insert("taker".to_string(), self.parse_number(crate::precise::Precise::stringDiv(&taker, &Value::Str("10000".into())), &[]));
        m.insert("percentage".to_string(), Value::Bool(true));
        m.insert("tierBased".to_string(), Value::Bool(true));
    m
}));
            }
            }
        }
        return result;

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchOrderBook
 * @description fetches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/orderbook-snapshot
 * @param {string} symbol unified symbol of the market to fetch the order book for
 * @param {int} [limit] the maximum amount of order book entries to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order book structure]{@link https://docs.ccxt.com/?id=order-book-structure}
 */
    pub async fn fetch_order_book(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut limit = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
            m
        });
        if (limit != Value::Null) {
            limit = crate::runtime::Math::min(&limit, &Value::Int(1000));
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("max_level".to_string(), limit); }
        }
        let __ws_arg_4 = self.extend(request, &[params]);
        let mut response: Value = self.v1_private_get_orderbook_symbol(&[__ws_arg_4]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //       "asks": [{
        //         "price": 10669.4,
        //         "quantity": 1.56263218
        //       }],
        //       "bids": [{
        //         "price": 10669.4,
        //         "quantity": 1.56263218
        //       }],
        //       "timestamp": 123
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut timestamp: Value = self.safe_integer_k(data.clone(), "timestamp", &[]);
        return self.parse_order_book(data, symbol, &[timestamp, Value::Str("bids".into()), Value::Str("asks".into()), Value::Str("price".into()), Value::Str("quantity".into())]);

    Value::Null
}

    pub fn parse_ohlcv(&self, mut ohlcv: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        return Value::from(vec![self.safe_integer_k(ohlcv.clone(), "start_timestamp", &[]), self.safe_number_k(ohlcv.clone(), "open", &[]), self.safe_number_k(ohlcv.clone(), "high", &[]), self.safe_number_k(ohlcv.clone(), "low", &[]), self.safe_number_k(ohlcv.clone(), "close", &[]), self.safe_number_k(ohlcv, "volume", &[])]);

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchOHLCV
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/public/get-kline
 * @description fetches historical candlestick data containing the open, high, low, and close price, and the volume of a market
 * @param {string} symbol unified symbol of the market to fetch OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] max=1000, max=100 when since is defined and is less than (now - (999 * (timeframe in ms)))
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 */
    pub async fn fetch_ohlcv(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut timeframe = get_arg(optional_args, 0, Value::Str("1m".into()));
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
                m.insert("type".to_string(), self.safe_string(self.timeframes.clone(), timeframe.clone(), &[timeframe.clone()]));
            m
        });
        if (limit != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("limit".to_string(), crate::runtime::Math::min(&limit, &Value::Int(1000))); }
        }
        let __ws_arg_5 = self.extend(request, &[params]);
        let mut response: Value = self.v1_private_get_kline(&[__ws_arg_5]).await;
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //       "rows": [{
        //         "open": 66166.23,
        //         "close": 66124.56,
        //         "low": 66038.06,
        //         "high": 66176.97,
        //         "volume": 23.45528526,
        //         "amount": 1550436.21725288,
        //         "symbol": "PERP_BTC_USDC",
        //         "type": "1m",
        //         "start_timestamp": 1636388220000,
        //         "end_timestamp": 1636388280000
        //       }]
        //     }
        // }
        //
        let mut rows: Value = self.safe_list_k(data, "rows", &[Value::from(vec![])]);
        return self.parse_ohlc_vs(rows, &[market, timeframe, since, limit]);

    Value::Null
}

    pub fn parse_order(&self, mut order: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        // Possible input functions:
        // * createOrder
        // * createOrders
        // * cancelOrder
        // * fetchOrder
        // * fetchOrders
        // const isFromFetchOrder = ('order_tag' in order); TO_DO
        //
        // stop order after creating it:
        //   {
        //     "orderId": "1578938",
        //     "clientOrderId": "0",
        //     "algoType": "STOP_LOSS",
        //     "quantity": "0.1"
        //   }
        // stop order after fetching it:
        //   {
        //       "algoOrderId": "1578958",
        //       "clientOrderId": "0",
        //       "rootAlgoOrderId": "1578958",
        //       "parentAlgoOrderId": "0",
        //       "symbol": "SPOT_LTC_USDT",
        //       "orderTag": "default",
        //       "algoType": "STOP_LOSS",
        //       "side": "BUY",
        //       "quantity": "0.1",
        //       "isTriggered": false,
        //       "triggerPrice": "100",
        //       "triggerStatus": "USELESS",
        //       "type": "LIMIT",
        //       "rootAlgoStatus": "CANCELLED",
        //       "algoStatus": "CANCELLED",
        //       "triggerPriceType": "MARKET_PRICE",
        //       "price": "75",
        //       "triggerTime": "0",
        //       "totalExecutedQuantity": "0",
        //       "averageExecutedPrice": "0",
        //       "totalFee": "0",
        //       "feeAsset": '',
        //       "reduceOnly": false,
        //       "createdTime": "1686149609.744",
        //       "updatedTime": "1686149903.362"
        //   }
        //
        let mut timestamp: Value = self.safe_integer_n(order.clone(), Value::from(vec![Value::Str("timestamp".into()), Value::Str("created_time".into()), Value::Str("createdTime".into())]), &[]);
        let mut orderId: Value = self.safe_string_n(order.clone(), Value::from(vec![Value::Str("order_id".into()), Value::Str("orderId".into()), Value::Str("algoOrderId".into())]), &[]);
        let mut clientOrderId: Value = self.omit_zero(self.safe_string2(order.clone(), Value::Str("client_order_id".into()), Value::Str("clientOrderId".into()), &[])); // Somehow, this always returns 0 for limit order
        let mut marketId: Value = self.safe_string_k(order.clone(), "symbol", &[]);
        market = self.safe_market(&[marketId, market.clone()]);
        let mut symbol: Value = market.as_map().and_then(|__m| __m.get("symbol")).cloned().unwrap_or(Value::Null);
        let mut price: Value = self.safe_string2(order.clone(), Value::Str("order_price".into()), Value::Str("price".into()), &[]);
        let mut amount: Value = self.safe_string2(order.clone(), Value::Str("order_quantity".into()), Value::Str("quantity".into()), &[]); // This is base amount
        let mut cost: Value = self.safe_string2(order.clone(), Value::Str("order_amount".into()), Value::Str("amount".into()), &[]); // This is quote amount
        let mut orderType: Value = self.safe_string_lower2(order.clone(), Value::Str("order_type".into()), Value::Str("type".into()), &[]);
        let mut status: Value = self.safe_string2(order.clone(), Value::Str("status".into()), Value::Str("algoStatus".into()), &[]);
        let mut success: Value = self.safe_bool_k(order.clone(), "success", &[]);
        if (success != Value::Null) {
            status = (if success.as_bool() == Some(true) { Value::Str("NEW".into()) } else { Value::Str("REJECTED".into()) });
        }
        let mut side: Value = self.safe_string_lower(order.clone(), Value::Str("side".into()), &[]);
        let mut filled: Value = self.omit_zero(self.safe_string2(order.clone(), Value::Str("executed".into()), Value::Str("totalExecutedQuantity".into()), &[]));
        let mut average: Value = self.omit_zero(self.safe_string2(order.clone(), Value::Str("average_executed_price".into()), Value::Str("averageExecutedPrice".into()), &[]));
        let mut remaining: Value = crate::precise::Precise::stringSub(&cost, &filled);
        let mut fee: Value = self.safe_number2(order.clone(), Value::Str("total_fee".into()), Value::Str("totalFee".into()), &[]);
        let mut feeCurrency: Value = self.safe_string2(order.clone(), Value::Str("fee_asset".into()), Value::Str("feeAsset".into()), &[]);
        let mut transactions: Value = self.safe_value_k(order.clone(), "Transactions", &[]);
        let mut triggerPrice: Value = self.safe_number_k(order.clone(), "triggerPrice", &[]);
        let mut takeProfitPrice: Value = Value::Null;
        let mut stopLossPrice: Value = Value::Null;
        let mut childOrders: Value = self.safe_list_k(order.clone(), "childOrders", &[]);
        if (childOrders != Value::Null) {
            let mut first: Value = self.safe_dict(childOrders, Value::Int(0), &[]);
            let mut innerChildOrders: Value = self.safe_list_k(first, "childOrders", &[Value::from(vec![])]);
            let mut innerChildOrdersLength: f64 = ((innerChildOrders.len() as i64) as f64);
            if innerChildOrdersLength > ((0i64) as f64) {
                let mut takeProfitOrder: Value = self.safe_dict(innerChildOrders.clone(), Value::Int(0), &[]);
                let mut stopLossOrder: Value = self.safe_dict(innerChildOrders, Value::Int(1), &[]);
                takeProfitPrice = self.safe_number_k(takeProfitOrder, "triggerPrice", &[]);
                stopLossPrice = self.safe_number_k(stopLossOrder, "triggerPrice", &[]);
            }
        }
        let mut lastUpdateTimestamp: Value = self.safe_integer2(order.clone(), Value::Str("updatedTime".into()), Value::Str("updated_time".into()), &[]);
        return self.safe_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), orderId);
        m.insert("clientOrderId".to_string(), clientOrderId);
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp));
        m.insert("lastTradeTimestamp".to_string(), Value::Null);
        m.insert("lastUpdateTimestamp".to_string(), lastUpdateTimestamp);
        m.insert("status".to_string(), self.parse_order_status(status).map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null));
        m.insert("symbol".to_string(), symbol);
        m.insert("type".to_string(), self.parse_order_type(orderType.clone()).map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null));
        m.insert("timeInForce".to_string(), self.parse_time_in_force(orderType).map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null));
        m.insert("postOnly".to_string(), Value::Null);
        m.insert("reduceOnly".to_string(), self.safe_bool_k(order.clone(), "reduce_only", &[]));
        m.insert("side".to_string(), side);
        m.insert("price".to_string(), price);
        m.insert("triggerPrice".to_string(), triggerPrice);
        m.insert("takeProfitPrice".to_string(), takeProfitPrice);
        m.insert("stopLossPrice".to_string(), stopLossPrice);
        m.insert("average".to_string(), average);
        m.insert("amount".to_string(), amount);
        m.insert("filled".to_string(), filled);
        m.insert("remaining".to_string(), remaining);
        m.insert("cost".to_string(), cost);
        m.insert("trades".to_string(), transactions);
        m.insert("fee".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("cost".to_string(), fee);
        m.insert("currency".to_string(), feeCurrency);
    m
}));
        m.insert("info".to_string(), order);
    m
}), &[market]);

    Value::Null
}

    pub fn parse_time_in_force(&self, mut timeInForce: Value) -> Option<String> {
        let mut timeInForces: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("ioc".to_string(), Value::Str("IOC".into()));
                m.insert("fok".to_string(), Value::Str("FOK".into()));
                m.insert("post_only".to_string(), Value::Str("PO".into()));
            m
        });
        if (timeInForce == Value::Null) {
            return None;
        }
        return self.safe_string(timeInForces, timeInForce, &[]).as_str().map(str::to_owned);
}

    pub fn parse_order_status(&self, mut status: Value) -> Option<String> {
        if (status != Value::Null) {
            let mut statuses: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("NEW".to_string(), Value::Str("open".into()));
                    m.insert("FILLED".to_string(), Value::Str("closed".into()));
                    m.insert("CANCEL_SENT".to_string(), Value::Str("canceled".into()));
                    m.insert("CANCEL_ALL_SENT".to_string(), Value::Str("canceled".into()));
                    m.insert("CANCELLED".to_string(), Value::Str("canceled".into()));
                    m.insert("PARTIAL_FILLED".to_string(), Value::Str("open".into()));
                    m.insert("REJECTED".to_string(), Value::Str("rejected".into()));
                    m.insert("INCOMPLETE".to_string(), Value::Str("open".into()));
                    m.insert("COMPLETED".to_string(), Value::Str("closed".into()));
                m
            });
            if (status == Value::Null) {
                return None;
            }
            return self.safe_string(statuses, status.clone(), &[status.clone()]).as_str().map(str::to_owned);
        }
        return None;
}

    pub fn parse_order_type(&self, mut type_var: Value) -> Option<String> {
        let mut types: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("LIMIT".to_string(), Value::Str("limit".into()));
                m.insert("MARKET".to_string(), Value::Str("market".into()));
                m.insert("POST_ONLY".to_string(), Value::Str("limit".into()));
            m
        });
        if (type_var == Value::Null) {
            return None;
        }
        return self.safe_string_lower(types, type_var.clone(), &[type_var.clone()]).as_str().map(str::to_owned);
}

    pub fn create_order_request(&self, mut symbol: Value, mut type_var: Value, mut side: Value, mut amount: Value, optional_args: &[Value]) -> Value {
        let mut price = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (side == Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(format!("{}{}", self.id.clone(), Value::Str(" requires a side argument".into()))));
        }
        if (type_var == Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(format!("{}{}", self.id.clone(), Value::Str(" requires a type argument".into()))));
        }
        /*
         * @method
         * @ignore
         * @name modetrade#createOrderRequest
         * @description helper function to build the request
         * @param {string} symbol unified symbol of the market to create an order in
         * @param {string} type 'market' or 'limit'
         * @param {string} side 'buy' or 'sell'
         * @param {float} amount how much you want to trade in units of the base currency
         * @param {float} [price] the price that the order is to be fulfilled, in units of the quote currency, ignored in market orders
         * @param {object} [params] extra parameters specific to the exchange API endpoint
         * @returns {object} request to be sent to the exchange
         */
        let mut reduceOnly: Value = self.safe_bool2(params.clone(), Value::Str("reduceOnly".into()), Value::Str("reduce_only".into()), &[]);
        let mut orderType: Value = to_upper(&type_var);
        let mut market: Value = self.market(symbol.clone());
        if (side == Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(format!("{}{}", self.id.clone(), Value::Str(" createOrder() requires a side argument".into()))));
        }
        let mut orderSide: Value = to_upper(&side);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
                m.insert("side".to_string(), orderSide.clone());
            m
        });
        let mut triggerPrice: Value = self.safe_string2(params.clone(), Value::Str("triggerPrice".into()), Value::Str("stopPrice".into()), &[]);
        let mut stopLoss: Value = self.safe_value_k(params.clone(), "stopLoss", &[]);
        let mut takeProfit: Value = self.safe_value_k(params.clone(), "takeProfit", &[]);
        let mut hasStopLoss: bool = stopLoss != Value::Null;
        let mut hasTakeProfit: bool = takeProfit != Value::Null;
        let mut algoType: Option<String> = self.safe_string_k(params.clone(), "algoType", &[]).as_str().map(str::to_owned);
        let mut isConditional: bool = (triggerPrice != Value::Null) || hasStopLoss || hasTakeProfit || (self.safe_value_k(params.clone(), "childOrders", &[]) != Value::Null);
        let mut isMarket: Value = Value::Bool(orderType.as_str() == Some("MARKET"));
        let mut timeInForce: Option<String> = self.safe_string_lower(params.clone(), Value::Str("timeInForce".into()), &[]).as_str().map(str::to_owned);
        let mut postOnly: Value = self.is_post_only(isMarket.clone(), Value::Null, &[params.clone()]);
        let mut orderQtyKey: Value = (if isConditional { Value::Str("quantity".into()) } else { Value::Str("order_quantity".into()) });
        let mut priceKey: Value = (if isConditional { Value::Str("price".into()) } else { Value::Str("order_price".into()) });
        let mut typeKey: Value = (if isConditional { Value::Str("type".into()) } else { Value::Str("order_type".into()) });
        add_element_to_object(&mut request, &typeKey, orderType); // LIMIT/MARKET/IOC/FOK/POST_ONLY/ASK/BID
        if !isConditional {
            if is_true(&postOnly) {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("order_type".to_string(), Value::Str("POST_ONLY".into())); }
            }  else if (timeInForce.as_deref() == Some("fok")) {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("order_type".to_string(), Value::Str("FOK".into())); }
            }  else if (timeInForce.as_deref() == Some("ioc")) {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("order_type".to_string(), Value::Str("IOC".into())); }
            }
        }
        if (reduceOnly.as_bool() == Some(true)) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("reduce_only".to_string(), reduceOnly); }
        }
        if (price != Value::Null) {
            add_element_to_object(&mut request, &priceKey, self.price_to_precision(symbol.clone(), price));
        }
        if matches!(&isMarket, Value::Bool(true)) && !isConditional {
            add_element_to_object(&mut request, &orderQtyKey, self.amount_to_precision(symbol.clone(), amount.clone()));
        }  else if (algoType.as_deref() != Some("POSITIONAL_TP_SL")) {
            add_element_to_object(&mut request, &orderQtyKey, self.amount_to_precision(symbol.clone(), amount));
        }
        let mut clientOrderId: Value = self.safe_string_n(params.clone(), Value::from(vec![Value::Str("clOrdID".into()), Value::Str("clientOrderId".into()), Value::Str("client_order_id".into())]), &[]);
        if (clientOrderId != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("client_order_id".to_string(), clientOrderId); }
        }
        if (triggerPrice != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("trigger_price".to_string(), self.price_to_precision(symbol.clone(), triggerPrice)); }
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("algo_type".to_string(), Value::Str("STOP".into())); }
        }  else if hasStopLoss || hasTakeProfit {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("algo_type".to_string(), Value::Str("TP_SL".into())); }
            let mut outterOrder: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
                    m.insert("reduce_only".to_string(), Value::Bool(false));
                    m.insert("algo_type".to_string(), Value::Str("POSITIONAL_TP_SL".into()));
                    m.insert("child_orders".to_string(), Value::from(vec![]));
                m
            });
            let mut childOrders: Value = match &outterOrder { Value::Dict(__m15) => __m15.get("child_orders").cloned().unwrap_or(Value::Null), _ => Value::Null };
            let mut closeSide: Value = (if (orderSide.as_str() == Some("BUY")) { Value::Str("SELL".into()) } else { Value::Str("BUY".into()) });
            if hasStopLoss {
                let mut stopLossPrice: Value = self.safe_number2(stopLoss.clone(), Value::Str("triggerPrice".into()), Value::Str("price".into()), &[stopLoss]);
                let mut stopLossOrder: Value = Value::Map({
                    let mut m = indexmap::IndexMap::new();
                        m.insert("side".to_string(), closeSide.clone());
                        m.insert("algo_type".to_string(), Value::Str("TP_SL".into()));
                        m.insert("trigger_price".to_string(), self.price_to_precision(symbol.clone(), stopLossPrice));
                        m.insert("type".to_string(), Value::Str("LIMIT".into()));
                        m.insert("reduce_only".to_string(), Value::Bool(true));
                    m
                });
                append_to_array(&mut childOrders, stopLossOrder);
            }
            if hasTakeProfit {
                let mut takeProfitPrice: Value = self.safe_number2(takeProfit.clone(), Value::Str("triggerPrice".into()), Value::Str("price".into()), &[takeProfit]);
                let mut takeProfitOrder: Value = Value::Map({
                    let mut m = indexmap::IndexMap::new();
                        m.insert("side".to_string(), closeSide);
                        m.insert("algo_type".to_string(), Value::Str("TP_SL".into()));
                        m.insert("trigger_price".to_string(), self.price_to_precision(symbol, takeProfitPrice));
                        m.insert("type".to_string(), Value::Str("LIMIT".into()));
                        m.insert("reduce_only".to_string(), Value::Bool(true));
                    m
                });
                append_to_array(&mut childOrders, takeProfitOrder);
            }
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("child_orders".to_string(), Value::from(vec![outterOrder])); }
        }
        params = self.omit(params.clone(), Value::from(vec![Value::Str("reduceOnly".into()), Value::Str("reduce_only".into()), Value::Str("clOrdID".into()), Value::Str("clientOrderId".into()), Value::Str("client_order_id".into()), Value::Str("postOnly".into()), Value::Str("timeInForce".into()), Value::Str("stopPrice".into()), Value::Str("triggerPrice".into()), Value::Str("stopLoss".into()), Value::Str("takeProfit".into())]), &[]);
        return self.extend(request, &[params]);

    Value::Null
}

/*
 * @method
 * @name modetrade#createOrder
 * @description create a trade order
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/create-order
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/create-algo-order
 * @param {string} symbol unified symbol of the market to create an order in
 * @param {string} type 'market' or 'limit'
 * @param {string} side 'buy' or 'sell'
 * @param {float} amount how much of currency you want to trade in units of base currency
 * @param {float} [price] the price at which the order is to be fulfilled, in units of the quote currency, ignored in market orders
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {float} [params.triggerPrice] The price a trigger order is triggered at
 * @param {object} [params.takeProfit] *takeProfit object in params* containing the triggerPrice at which the attached take profit order will be triggered (perpetual swap markets only)
 * @param {float} [params.takeProfit.triggerPrice] take profit trigger price
 * @param {object} [params.stopLoss] *stopLoss object in params* containing the triggerPrice at which the attached stop loss order will be triggered (perpetual swap markets only)
 * @param {float} [params.stopLoss.triggerPrice] stop loss trigger price
 * @param {string} [params.algoType] 'STOP' or 'TP_SL' or 'POSITIONAL_TP_SL'
 * @param {bool} [params.reduceOnly] true or false whether the order is reduce-only
 * @param {bool} [params.postOnly] true or false whether the order is post-only
 * @param {string} [params.timeInForce] 'IOC', 'FOK' or 'PO'
 * @param {object[]} [params.childOrders] *algo order only* a list of child orders passed through to the exchange
 * @param {string} [params.clientOrderId] a unique id for the order
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn create_order(&mut self, mut symbol: Value, mut type_var: Value, mut side: Value, mut amount: Value, optional_args: &[Value]) -> Value {
        let mut price = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = self.create_order_request(symbol, type_var.clone(), side, amount, &[price, params.clone()]);
        let mut triggerPrice: Option<String> = self.safe_string2(params.clone(), Value::Str("triggerPrice".into()), Value::Str("stopPrice".into()), &[]).as_str().map(str::to_owned);
        let mut stopLoss: Value = self.safe_value_k(params.clone(), "stopLoss", &[]);
        let mut takeProfit: Value = self.safe_value_k(params.clone(), "takeProfit", &[]);
        let mut isConditional: bool = (triggerPrice.is_some()) || (stopLoss != Value::Null) || (takeProfit != Value::Null) || (self.safe_value_k(params, "childOrders", &[]) != Value::Null);
        let mut response: Value = Value::Null;
        if isConditional {
            response = self.v1_private_post_algo_order(&[request.clone()]).await;
        }  else {
            response = self.v1_private_post_order(&[request]).await;
        }
        let mut data: Value = self.safe_dict_k(response.clone(), "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        add_element_to_object(&mut data, &Value::Str("timestamp".into()), self.safe_integer_k(response, "timestamp", &[]));
        let mut order: Value = self.parse_order(data, &[market]);
        add_element_to_object(&mut order, &Value::Str("type".into()), type_var);
        return order;

    Value::Null
}

/*
 * @method
 * @name modetrade#createOrders
 * @description *contract only* create a list of trade orders
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/batch-create-order
 * @param {Array} orders list of orders to create, each object should contain the parameters required by createOrder, namely symbol, type, side, amount, price and params
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn create_orders(&mut self, mut orders: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut ordersRequests: Value = Value::from(vec![]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_955: bool = true;
            while { if !__for_first_955 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_955 = false; i.as_f64().unwrap_or(f64::NAN) < ((orders.len() as i64) as f64) } {
            let mut rawOrder: Value = orders.as_array().and_then(|__arr| match &i { Value::Int(__n) => __arr.get(*__n as usize), Value::Str(__s) => __s.parse::<usize>().ok().and_then(|__n| __arr.get(__n)), _ => None }).cloned().unwrap_or(Value::Null);
            let mut marketId: Value = self.safe_string_k(rawOrder.clone(), "symbol", &[]);
            if (marketId == Value::Null) {
                panic!("{}", crate::exchange_errors::arguments_required(format!("{}{}", self.id.clone(), Value::Str(" createOrders() requires a symbol for each order".into()))));
            }
            let mut type_var: Value = self.safe_string_k(rawOrder.clone(), "type", &[Value::Str("".into())]);
            let mut side: Value = self.safe_string_k(rawOrder.clone(), "side", &[]);
            let mut amount: Value = self.safe_value_k(rawOrder.clone(), "amount", &[]);
            let mut price: Value = self.safe_value_k(rawOrder.clone(), "price", &[]);
            let mut orderParams: Value = self.safe_dict_k(rawOrder, "params", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            let mut triggerPrice: Option<String> = self.safe_string2(orderParams.clone(), Value::Str("triggerPrice".into()), Value::Str("stopPrice".into()), &[]).as_str().map(str::to_owned);
            let mut stopLoss: Value = self.safe_dict_k(orderParams.clone(), "stopLoss", &[]);
            let mut takeProfit: Value = self.safe_dict_k(orderParams.clone(), "takeProfit", &[]);
            let mut isConditional: bool = (triggerPrice.is_some()) || (stopLoss != Value::Null) || (takeProfit != Value::Null) || (self.safe_value_k(orderParams.clone(), "childOrders", &[]) != Value::Null);
            if isConditional {
                panic!("{}", crate::exchange_errors::not_supported(format!("{}{}", self.id.clone(), Value::Str(" createOrders() only support non-stop order".into()))));
            }
            let mut orderRequest: Value = self.create_order_request(marketId, type_var, side, amount, &[price, orderParams]);
            append_to_array(&mut ordersRequests, orderRequest);
        }
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("orders".to_string(), ordersRequests);
            m
        });
        let __ws_arg_6 = self.extend(request, &[params]);
        let mut response: Value = self.v1_private_post_batch_order(&[__ws_arg_6]).await;
        //
        //     {
        //         "success": true,
        //         "timestamp": 1702989203988,
        //         "data": {
        //             "rows": [{
        //                 "order_id": 13,
        //                 "client_order_id": "testclientid",
        //                 "order_type": "LIMIT",
        //                 "order_price": 100.12,
        //                 "order_quantity": 0.987654,
        //                 "order_amount": 0.8,
        //                 "error_message": "none"
        //             }]
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut rows: Value = self.safe_list_k(data, "rows", &[Value::from(vec![])]);
        return self.parse_orders(rows, &[]);

    Value::Null
}

/*
 * @method
 * @name modetrade#editOrder
 * @description edit a trade order
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/edit-order
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/edit-algo-order
 * @param {string} id order id
 * @param {string} symbol unified symbol of the market to create an order in
 * @param {string} type 'market' or 'limit'
 * @param {string} side 'buy' or 'sell'
 * @param {float} amount how much of currency you want to trade in units of base currency
 * @param {float} [price] the price at which the order is to be fulfilled, in units of the quote currency, ignored in market orders
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {float} [params.triggerPrice] The price a trigger order is triggered at
 * @param {float} [params.stopLossPrice] price to trigger stop-loss orders
 * @param {float} [params.takeProfitPrice] price to trigger take-profit orders
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn edit_order(&mut self, mut id: Value, mut symbol: Value, mut type_var: Value, mut side: Value, optional_args: &[Value]) -> Value {
        let mut amount = get_arg(optional_args, 0, Value::Null);
        let mut price = get_arg(optional_args, 1, Value::Null);
        let mut params = get_arg(optional_args, 2, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("order_id".to_string(), id);
            m
        });
        let mut triggerPrice: Value = self.safe_string_n(params.clone(), Value::from(vec![Value::Str("triggerPrice".into()), Value::Str("stopPrice".into()), Value::Str("takeProfitPrice".into()), Value::Str("stopLossPrice".into())]), &[]);
        if (triggerPrice != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("triggerPrice".to_string(), self.price_to_precision(symbol.clone(), triggerPrice.clone())); }
        }
        let mut isConditional: bool = (triggerPrice != Value::Null) || (self.safe_value_k(params.clone(), "childOrders", &[]) != Value::Null);
        let mut orderQtyKey: Value = (if isConditional { Value::Str("quantity".into()) } else { Value::Str("order_quantity".into()) });
        let mut priceKey: Value = (if isConditional { Value::Str("price".into()) } else { Value::Str("order_price".into()) });
        if (price != Value::Null) {
            add_element_to_object(&mut request, &priceKey, self.price_to_precision(symbol.clone(), price));
        }
        if (amount != Value::Null) {
            add_element_to_object(&mut request, &orderQtyKey, self.amount_to_precision(symbol, amount));
        }
        params = self.omit(params.clone(), Value::from(vec![Value::Str("stopPrice".into()), Value::Str("triggerPrice".into()), Value::Str("takeProfitPrice".into()), Value::Str("stopLossPrice".into()), Value::Str("trailingTriggerPrice".into()), Value::Str("trailingAmount".into()), Value::Str("trailingPercent".into())]), &[]);
        let mut response: Value = Value::Null;
        if isConditional {
            let __ws_arg_7 = self.extend(request.clone(), &[params.clone()]);
            response = self.v1_private_put_algo_order(&[__ws_arg_7]).await;
        }  else {
            if let Value::Dict(__d12) = &mut request { std::sync::Arc::make_mut(__d12).insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null)); }
            if (side != Value::Null) {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("side".to_string(), to_upper(&side)); }
            }
            let mut orderType: Value = to_upper(&type_var);
            let mut timeInForce: Option<String> = self.safe_string_lower(params.clone(), Value::Str("timeInForce".into()), &[]).as_str().map(str::to_owned);
            let mut isMarket: Value = Value::Bool(orderType.as_str() == Some("MARKET"));
            let mut postOnly: Value = self.is_post_only(isMarket, Value::Null, &[params.clone()]);
            if is_true(&postOnly) {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("order_type".to_string(), Value::Str("POST_ONLY".into())); }
            }  else if (timeInForce.as_deref() == Some("fok")) {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("order_type".to_string(), Value::Str("FOK".into())); }
            }  else if (timeInForce.as_deref() == Some("ioc")) {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("order_type".to_string(), Value::Str("IOC".into())); }
            }  else {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("order_type".to_string(), orderType); }
            }
            let mut clientOrderId: Value = self.safe_string_n(params.clone(), Value::from(vec![Value::Str("clOrdID".into()), Value::Str("clientOrderId".into()), Value::Str("client_order_id".into())]), &[]);
            params = self.omit(params.clone(), Value::from(vec![Value::Str("clOrdID".into()), Value::Str("clientOrderId".into()), Value::Str("client_order_id".into()), Value::Str("postOnly".into()), Value::Str("timeInForce".into())]), &[]);
            if (clientOrderId != Value::Null) {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("client_order_id".to_string(), clientOrderId); }
            }
            // request['side'] = side.toUpperCase ();
            // request['symbol'] = market['id'];
            let __ws_arg_8 = self.extend(request, &[params]);
            response = self.v1_private_put_order(&[__ws_arg_8]).await;
        }
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //       "status": "EDIT_SENT"
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response.clone(), "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        add_element_to_object(&mut data, &Value::Str("timestamp".into()), self.safe_integer_k(response, "timestamp", &[]));
        return self.parse_order(data, &[market]);

    Value::Null
}

/*
 * @method
 * @name modetrade#cancelOrder
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/cancel-order
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/cancel-order-by-client_order_id
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/cancel-algo-order
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/cancel-algo-order-by-client_order_id
 * @description cancels an open order
 * @param {string} id order id
 * @param {string} symbol unified symbol of the market the order was made in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {boolean} [params.trigger] whether the order is a stop/algo order
 * @param {string} [params.clientOrderId] a unique id for the order
 * @returns {object} An [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn cancel_order(&mut self, mut id: Value, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut trigger: Value = self.safe_bool2(params.clone(), Value::Str("stop".into()), Value::Str("trigger".into()), &[Value::Bool(false)]);
        params = self.omit(params.clone(), Value::from(vec![Value::Str("stop".into()), Value::Str("trigger".into())]), &[]);
        if (trigger.as_bool() != Some(true)) && (symbol == Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(format!("{}{}", self.id.clone(), Value::Str(" cancelOrder() requires a symbol argument".into()))));
        }
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = Value::Null;
        if (symbol != Value::Null) {
            market = self.market(symbol.clone());
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("symbol".to_string(), self.safe_string_k(market, "id", &[]));
            m
        });
        let mut clientOrderIdUnified: Value = self.safe_string2(params.clone(), Value::Str("clOrdID".into()), Value::Str("clientOrderId".into()), &[]);
        let mut clientOrderIdExchangeSpecific: Value = self.safe_string_k(params.clone(), "client_order_id", &[clientOrderIdUnified]);
        let mut isByClientOrder: bool = clientOrderIdExchangeSpecific != Value::Null;
        let mut response: Value = Value::Null;
        if (trigger.as_bool() == Some(true)) {
            if isByClientOrder {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("client_order_id".to_string(), clientOrderIdExchangeSpecific.clone()); }
                params = self.omit(params.clone(), Value::from(vec![Value::Str("clOrdID".into()), Value::Str("clientOrderId".into()), Value::Str("client_order_id".into())]), &[]);
                let __ws_arg_9 = self.extend(request.clone(), &[params.clone()]);
                response = self.v1_private_delete_algo_client_order(&[__ws_arg_9]).await;
            }  else {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("order_id".to_string(), id.clone()); }
                let __ws_arg_10 = self.extend(request.clone(), &[params.clone()]);
                response = self.v1_private_delete_algo_order(&[__ws_arg_10]).await;
            }
        }  else {
            if isByClientOrder {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("client_order_id".to_string(), clientOrderIdExchangeSpecific.clone()); }
                params = self.omit(params.clone(), Value::from(vec![Value::Str("clOrdID".into()), Value::Str("clientOrderId".into()), Value::Str("client_order_id".into())]), &[]);
                let __ws_arg_11 = self.extend(request.clone(), &[params.clone()]);
                response = self.v1_private_delete_client_order(&[__ws_arg_11]).await;
            }  else {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("order_id".to_string(), id.clone()); }
                let __ws_arg_12 = self.extend(request, &[params]);
                response = self.v1_private_delete_order(&[__ws_arg_12]).await;
            }
        }
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203988,
        //     "data": {
        //       "status": "CANCEL_SENT"
        //     }
        // }
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203988,
        //     "status": "CANCEL_SENT"
        // }
        //
        let mut extendParams: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("symbol".to_string(), symbol);
            m
        });
        if isByClientOrder {
            if let Value::Dict(__d) = &mut extendParams { std::sync::Arc::make_mut(__d).insert("client_order_id".to_string(), clientOrderIdExchangeSpecific); }
        }  else {
            if let Value::Dict(__d) = &mut extendParams { std::sync::Arc::make_mut(__d).insert("id".to_string(), id); }
        }
        if (trigger.as_bool() == Some(true)) {
            let __ws_arg_13 = self.parse_order(response.clone(), &[]);
            return self.extend(__ws_arg_13, &[extendParams.clone()]);
        }
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let __ws_arg_14 = self.parse_order(data, &[]);
        return self.extend(__ws_arg_14, &[extendParams]);

    Value::Null
}

/*
 * @method
 * @name modetrade#cancelOrders
 * @description cancel multiple orders
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/batch-cancel-orders
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/batch-cancel-orders-by-client_order_id
 * @param {string[]} ids order ids
 * @param {string} [symbol] unified market symbol
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string[]} [params.client_order_ids] max length 10 e.g. ["my_id_1","my_id_2"], encode the double quotes. No space after comma
 * @returns {object} an list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn cancel_orders(&mut self, mut ids: Value, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut clientOrderIds: Value = self.safe_list_n(params.clone(), Value::from(vec![Value::Str("clOrdIDs".into()), Value::Str("clientOrderIds".into()), Value::Str("client_order_ids".into())]), &[]);
        params = self.omit(params.clone(), Value::from(vec![Value::Str("clOrdIDs".into()), Value::Str("clientOrderIds".into()), Value::Str("client_order_ids".into())]), &[]);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut response: Value = Value::Null;
        if (clientOrderIds != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("client_order_ids".to_string(), join(&clientOrderIds, &Value::Str(",".into()))); }
            let __ws_arg_15 = self.extend(request.clone(), &[params.clone()]);
            response = self.v1_private_delete_client_batch_order(&[__ws_arg_15]).await;
        }  else {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("order_ids".to_string(), join(&ids, &Value::Str(",".into()))); }
            let __ws_arg_16 = self.extend(request, &[params]);
            response = self.v1_private_delete_batch_order(&[__ws_arg_16]).await;
        }
        return Value::from(vec![self.safe_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), response);
    m
}), &[])]);

    Value::Null
}

/*
 * @method
 * @name modetrade#cancelAllOrders
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/cancel-all-pending-algo-orders
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/cancel-all-pending-orders
 * @description cancel all open orders in a market
 * @param {string} [symbol] unified market symbol
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {boolean} [params.trigger] whether the order is a stop/algo order
 * @returns {object} an list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn cancel_all_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut trigger: Value = self.safe_bool2(params.clone(), Value::Str("stop".into()), Value::Str("trigger".into()), &[]);
        params = self.omit(params.clone(), Value::from(vec![Value::Str("stop".into()), Value::Str("trigger".into())]), &[]);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        if (symbol != Value::Null) {
            let mut market: Value = self.market(symbol);
            if let Value::Dict(__d12) = &mut request { std::sync::Arc::make_mut(__d12).insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null)); }
        }
        let mut response: Value = Value::Null;
        if (trigger.as_bool() == Some(true)) {
            let __ws_arg_17 = self.extend(request.clone(), &[params.clone()]);
            response = self.v1_private_delete_algo_orders(&[__ws_arg_17]).await;
        }  else {
            let __ws_arg_18 = self.extend(request, &[params]);
            response = self.v1_private_delete_orders(&[__ws_arg_18]).await;
        }
        return Value::from(vec![self.safe_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), response);
    m
}), &[])]);

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchOrder
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-order-by-order_id
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-order-by-client_order_id
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-algo-order-by-order_id
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-algo-order-by-client_order_id
 * @description fetches information on an order made by the user
 * @param {string} id the order id
 * @param {string} symbol unified symbol of the market the order was made in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {boolean} [params.trigger] whether the order is a stop/algo order
 * @param {string} [params.clientOrderId] a unique id for the order
 * @returns {object} An [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn fetch_order(&mut self, mut id: Value, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = Value::Null;
        if (symbol != Value::Null) {
            market = self.market(symbol);
        }
        let mut trigger: Value = self.safe_bool2(params.clone(), Value::Str("stop".into()), Value::Str("trigger".into()), &[Value::Bool(false)]);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut clientOrderId: Value = self.safe_string_n(params.clone(), Value::from(vec![Value::Str("clOrdID".into()), Value::Str("clientOrderId".into()), Value::Str("client_order_id".into())]), &[]);
        params = self.omit(params.clone(), Value::from(vec![Value::Str("stop".into()), Value::Str("trigger".into()), Value::Str("clOrdID".into()), Value::Str("clientOrderId".into()), Value::Str("client_order_id".into())]), &[]);
        let mut response: Value = Value::Null;
        if (trigger.as_bool() == Some(true)) {
            if (clientOrderId != Value::Null) && (clientOrderId.as_str() != Some("")) {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("client_order_id".to_string(), clientOrderId.clone()); }
                let __ws_arg_19 = self.extend(request.clone(), &[params.clone()]);
                response = self.v1_private_get_algo_client_order_client_order_id(&[__ws_arg_19]).await;
            }  else {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("oid".to_string(), id.clone()); }
                let __ws_arg_20 = self.extend(request.clone(), &[params.clone()]);
                response = self.v1_private_get_algo_order_oid(&[__ws_arg_20]).await;
            }
        }  else {
            if (clientOrderId != Value::Null) && (clientOrderId.as_str() != Some("")) {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("client_order_id".to_string(), clientOrderId); }
                let __ws_arg_21 = self.extend(request.clone(), &[params.clone()]);
                response = self.v1_private_get_client_order_client_order_id(&[__ws_arg_21]).await;
            }  else {
                if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("oid".to_string(), id); }
                let __ws_arg_22 = self.extend(request, &[params]);
                response = self.v1_private_get_order_oid(&[__ws_arg_22]).await;
            }
        }
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //         "order_id": 78151,
        //         "user_id": 12345,
        //         "price": 0.67772,
        //         "type": "LIMIT",
        //         "quantity": 20,
        //         "amount": 10,
        //         "executed_quantity": 20,
        //         "total_executed_quantity": 20,
        //         "visible_quantity": 1,
        //         "symbol": "PERP_BTC_USDC",
        //         "side": "BUY",
        //         "status": "FILLED",
        //         "total_fee": 0.5,
        //         "fee_asset": "BTC",
        //         "client_order_id": 1,
        //         "average_executed_price": 0.67772,
        //         "created_time": 1653563963000,
        //         "updated_time": 1653564213000,
        //         "realized_pnl": 123
        //     }
        // }
        //
        let mut orders: Value = self.safe_dict_k(response.clone(), "data", &[response.clone()]);
        return self.parse_order(orders, &[market]);

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchOrders
 * @description fetches information on multiple orders made by the user
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-orders
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-algo-orders
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve, max 500, or max 100 when params.trigger (or the legacy params.stop) is true
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {boolean} [params.trigger] whether the order is a stop/algo order
 * @param {boolean} [params.is_triggered] whether the order has been triggered (false by default)
 * @param {string} [params.side] 'buy' or 'sell'
 * @param {boolean} [params.paginate] set to true if you want to fetch orders with pagination
 * @param {int} params.until timestamp in ms of the latest order to fetch
 * @returns {Order[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn fetch_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut paginate: Value = Value::Bool(false);
        let mut isTrigger: Value = self.safe_bool2(params.clone(), Value::Str("stop".into()), Value::Str("trigger".into()), &[Value::Bool(false)]);
        let mut maxLimit: Value = (if (isTrigger.as_bool() == Some(true)) { Value::Int(100) } else { Value::Int(500) });
        { let __destr_tmp = self.handle_option_and_params(params.clone(), Value::Str("fetchOrders".into()), Value::Str("paginate".into()), &[]); paginate = __destr_tmp.as_array().and_then(|__arr| __arr.get(0)).cloned().unwrap_or(Value::Null); params = __destr_tmp.as_array().and_then(|__arr| __arr.get(1)).cloned().unwrap_or(Value::Null); }
        if is_true(&paginate) {
            return self.fetch_paginated_call_incremental(Value::Str("fetchOrders".into()), &[symbol.clone(), since.clone(), limit.clone(), params.clone(), Value::Str("page".into()), maxLimit.clone()]).await;
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut market: Value = Value::Null;
        params = self.omit(params.clone(), Value::from(vec![Value::Str("stop".into()), Value::Str("trigger".into())]), &[]);
        if (symbol != Value::Null) {
            market = self.market(symbol);
            add_element_to_object(&mut request, &Value::Str("symbol".into()), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
        }
        if (since != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("start_t".to_string(), since.clone()); }
        }
        if (limit != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("size".to_string(), crate::runtime::Math::min(&limit, &maxLimit)); }
        }  else {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("size".to_string(), maxLimit); }
        }
        if (isTrigger.as_bool() == Some(true)) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("algo_type".to_string(), Value::Str("STOP".into())); }
        }
        { let __destr_tmp = self.handle_until_option(Value::Str("end_t".into()), request.clone(), params.clone(), &[]); request = __destr_tmp.as_array().and_then(|__arr| __arr.get(0)).cloned().unwrap_or(Value::Null); params = __destr_tmp.as_array().and_then(|__arr| __arr.get(1)).cloned().unwrap_or(Value::Null); }
        let mut response: Value = Value::Null;
        if (isTrigger.as_bool() == Some(true)) {
            let __ws_arg_23 = self.extend(request.clone(), &[params.clone()]);
            response = self.v1_private_get_algo_orders(&[__ws_arg_23]).await;
        }  else {
            let __ws_arg_24 = self.extend(request, &[params]);
            response = self.v1_private_get_orders(&[__ws_arg_24]).await;
        }
        //
        //     {
        //         "success": true,
        //         "timestamp": 1702989203989,
        //         "data": {
        //             "meta": {
        //                 "total": 9,
        //                 "records_per_page": 25,
        //                 "current_page": 1
        //             },
        //             "rows": [{
        //                 "order_id": 78151,
        //                 "user_id": 12345,
        //                 "price": 0.67772,
        //                 "type": "LIMIT",
        //                 "quantity": 20,
        //                 "amount": 10,
        //                 "executed_quantity": 20,
        //                 "total_executed_quantity": 20,
        //                 "visible_quantity": 1,
        //                 "symbol": "PERP_BTC_USDC",
        //                 "side": "BUY",
        //                 "status": "FILLED",
        //                 "total_fee": 0.5,
        //                 "fee_asset": "BTC",
        //                 "client_order_id": 1,
        //                 "average_executed_price": 0.67772,
        //                 "created_time": 1653563963000,
        //                 "updated_time": 1653564213000,
        //                 "realized_pnl": 123
        //             }]
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict_k(response.clone(), "data", &[response.clone()]);
        let mut orders: Value = self.safe_list_k(data, "rows", &[Value::from(vec![])]);
        return self.parse_orders(orders, &[market, since, limit]);

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchOpenOrders
 * @description fetches information on multiple orders made by the user
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-orders
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-algo-orders
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve, max 500, or max 100 when params.trigger (or the legacy params.stop) is true
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {boolean} [params.trigger] whether the order is a stop/algo order
 * @param {boolean} [params.is_triggered] whether the order has been triggered (false by default)
 * @param {string} [params.side] 'buy' or 'sell'
 * @param {int} params.until timestamp in ms of the latest order to fetch
 * @param {boolean} [params.paginate] set to true if you want to fetch orders with pagination
 * @returns {Order[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn fetch_open_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut extendedParams: Value = self.extend(params, &[Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("status".to_string(), Value::Str("INCOMPLETE".into()));
            m
        })]);
        return self.fetch_orders(&[symbol, since, limit, extendedParams]).await;

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchClosedOrders
 * @description fetches information on multiple orders made by the user
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-orders
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-algo-orders
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve, max 500, or max 100 when params.trigger (or the legacy params.stop) is true
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {boolean} [params.trigger] whether the order is a stop/algo order
 * @param {boolean} [params.is_triggered] whether the order has been triggered (false by default)
 * @param {string} [params.side] 'buy' or 'sell'
 * @param {int} params.until timestamp in ms of the latest order to fetch
 * @param {boolean} [params.paginate] set to true if you want to fetch orders with pagination
 * @returns {Order[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn fetch_closed_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut extendedParams: Value = self.extend(params, &[Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("status".to_string(), Value::Str("COMPLETED".into()));
            m
        })]);
        return self.fetch_orders(&[symbol, since, limit, extendedParams]).await;

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchOrderTrades
 * @description fetch all the trades made from a single order
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-all-trades-of-specific-order
 * @param {string} id order id
 * @param {string} symbol unified market symbol
 * @param {int} [since] the earliest time in ms to fetch trades for
 * @param {int} [limit] the maximum number of trades to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=trade-structure}
 */
    pub async fn fetch_order_trades(&mut self, mut id: Value, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = Value::Null;
        if (symbol != Value::Null) {
            market = self.market(symbol);
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("oid".to_string(), id);
            m
        });
        let __ws_arg_25 = self.extend(request, &[params.clone()]);
        let mut response: Value = self.v1_private_get_order_oid_trades(&[__ws_arg_25]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //       "rows": [{
        //         "id": 2,
        //         "symbol": "PERP_BTC_USDC",
        //         "fee": 0.0001,
        //         "fee_asset": "USDC",
        //         "side": "BUY",
        //         "order_id": 1,
        //         "executed_price": 123,
        //         "executed_quantity": 0.05,
        //         "executed_timestamp": 1567382401000,
        //         "is_maker": 1,
        //         "realized_pnl": 123
        //       }]
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut trades: Value = self.safe_list_k(data, "rows", &[Value::from(vec![])]);
        return self.parse_trades(trades, &[market, since, limit, params]);

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchMyTrades
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-trades
 * @description fetch all trades made by the user
 * @param {string} symbol unified market symbol
 * @param {int} [since] the earliest time in ms to fetch trades for
 * @param {int} [limit] the maximum number of trades structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {boolean} [params.paginate] set to true if you want to fetch trades with pagination
 * @param {int} params.until timestamp in ms of the latest trade to fetch
 * @returns {Trade[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=trade-structure}
 */
    pub async fn fetch_my_trades(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut paginate: Value = Value::Bool(false);
        { let __destr_tmp = self.handle_option_and_params(params.clone(), Value::Str("fetchMyTrades".into()), Value::Str("paginate".into()), &[]); paginate = __destr_tmp.as_array().and_then(|__arr| __arr.get(0)).cloned().unwrap_or(Value::Null); params = __destr_tmp.as_array().and_then(|__arr| __arr.get(1)).cloned().unwrap_or(Value::Null); }
        if is_true(&paginate) {
            return self.fetch_paginated_call_incremental(Value::Str("fetchMyTrades".into()), &[symbol.clone(), since.clone(), limit.clone(), params.clone(), Value::Str("page".into()), Value::Int(500)]).await;
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut market: Value = Value::Null;
        if (symbol != Value::Null) {
            market = self.market(symbol);
            add_element_to_object(&mut request, &Value::Str("symbol".into()), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
        }
        if (since != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("start_t".to_string(), since.clone()); }
        }
        if (limit != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("size".to_string(), limit.clone()); }
        }  else {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("size".to_string(), Value::Int(500)); }
        }
        { let __destr_tmp = self.handle_until_option(Value::Str("end_t".into()), request.clone(), params.clone(), &[]); request = __destr_tmp.as_array().and_then(|__arr| __arr.get(0)).cloned().unwrap_or(Value::Null); params = __destr_tmp.as_array().and_then(|__arr| __arr.get(1)).cloned().unwrap_or(Value::Null); }
        let __ws_arg_26 = self.extend(request, &[params.clone()]);
        let mut response: Value = self.v1_private_get_trades(&[__ws_arg_26]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //       "meta": {
        //         "total": 9,
        //         "records_per_page": 25,
        //         "current_page": 1
        //       },
        //       "rows": [{
        //         "id": 2,
        //         "symbol": "PERP_BTC_USDC",
        //         "fee": 0.0001,
        //         "fee_asset": "USDC",
        //         "side": "BUY",
        //         "order_id": 1,
        //         "executed_price": 123,
        //         "executed_quantity": 0.05,
        //         "executed_timestamp": 1567382401000,
        //         "is_maker": 1,
        //         "realized_pnl": 123
        //       }]
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut trades: Value = self.safe_list_k(data, "rows", &[Value::from(vec![])]);
        return self.parse_trades(trades, &[market, since, limit, params]);

    Value::Null
}

    pub fn parse_balance(&self, mut response: Value) -> Value {
        let mut result: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("info".to_string(), response.clone());
            m
        });
        let mut balances: Value = self.safe_list_k(response, "holding", &[Value::from(vec![])]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_956: bool = true;
            while { if !__for_first_956 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_956 = false; i.as_f64().unwrap_or(f64::NAN) < ((balances.len() as i64) as f64) } {
            let mut balance: Value = balances.as_array().and_then(|__arr| match &i { Value::Int(__n) => __arr.get(*__n as usize), Value::Str(__s) => __s.parse::<usize>().ok().and_then(|__n| __arr.get(__n)), _ => None }).cloned().unwrap_or(Value::Null);
            let mut code: Value = self.safe_currency_code(self.safe_string_k(balance.clone(), "token", &[]), &[]);
            let mut account: Value = self.account();
            if let Value::Dict(__d) = &mut account { std::sync::Arc::make_mut(__d).insert("total".to_string(), self.safe_string_k(balance.clone(), "holding", &[])); }
            if let Value::Dict(__d) = &mut account { std::sync::Arc::make_mut(__d).insert("used".to_string(), self.safe_string_k(balance, "frozen", &[])); }
            if (code != Value::Null) {
                add_element_to_object(&mut result, &code, account);
            }
        }
        }
        return self.safe_balance(result);

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchBalance
 * @description query for balance and get the amount of funds available for trading or funds locked in orders
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-current-holding
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [balance structure]{@link https://docs.ccxt.com/?id=balance-structure}
 */
    pub async fn fetch_balance(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut response: Value = self.v1_private_get_client_holding(&[params]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //       "holding": [{
        //         "updated_time": 1580794149000,
        //         "token": "BTC",
        //         "holding": -28.000752,
        //         "frozen": 123,
        //         "pending_short": -2000
        //       }]
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[]);
        return self.parse_balance(data);

    Value::Null
}

    pub async fn get_asset_history_rows(&mut self, optional_args: &[Value]) -> Value {
        let mut code = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut currency: Value = Value::Null;
        if (code != Value::Null) {
            currency = self.currency(code);
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("balance_token".to_string(), currency.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null)); }
        }
        if (since != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("start_t".to_string(), since); }
        }
        if (limit != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("pageSize".to_string(), limit); }
        }
        let mut transactionType: Value = self.safe_string_k(params.clone(), "type", &[]);
        params = self.omit(params.clone(), Value::Str("type".into()), &[]);
        if (transactionType != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("type".to_string(), transactionType); }
        }
        let __ws_arg_27 = self.extend(request, &[params]);
        let mut response: Value = self.v1_private_get_asset_history(&[__ws_arg_27]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //       "meta": {
        //         "total": 9,
        //         "records_per_page": 25,
        //         "current_page": 1
        //       },
        //       "rows": [{
        //         "id": "230707030600002",
        //         "tx_id": "0x4b0714c63cc7abae72bf68e84e25860b88ca651b7d27dad1e32bf4c027fa5326",
        //         "side": "WITHDRAW",
        //         "token": "USDC",
        //         "amount": 555,
        //         "fee": 123,
        //         "trans_status": "FAILED",
        //         "created_time": 1688699193034,
        //         "updated_time": 1688699193096,
        //         "chain_id": "986532"
        //       }]
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        return Value::from(vec![currency, self.safe_list_k(data, "rows", &[Value::from(vec![])])]);

    Value::Null
}

    pub fn parse_ledger_entry(&self, mut item: Value, optional_args: &[Value]) -> Value {
        let mut currency = get_arg(optional_args, 0, Value::Null);
        let mut currencyId: Value = self.safe_string_k(item.clone(), "token", &[]);
        let mut code: Value = self.safe_currency_code(currencyId.clone(), &[currency.clone()]);
        currency = self.safe_currency(currencyId, &[currency.clone()]);
        let mut amount: Value = self.safe_number_k(item.clone(), "amount", &[]);
        let mut side: Option<String> = self.safe_string_k(item.clone(), "token_side", &[]).as_str().map(str::to_owned);
        let mut direction: Value = (if (side.as_deref() == Some("DEPOSIT")) { Value::Str("in".into()) } else { Value::Str("out".into()) });
        let mut timestamp: Value = self.safe_integer_k(item.clone(), "created_time", &[]);
        let mut fee: Value = self.parse_token_and_fee_temp(item.clone(), Value::Str("fee_token".into()), Value::Str("fee_amount".into()));
        return self.safe_ledger_entry(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), self.safe_string_k(item.clone(), "id", &[]));
        m.insert("currency".to_string(), code);
        m.insert("account".to_string(), self.safe_string_k(item.clone(), "account", &[]));
        m.insert("referenceAccount".to_string(), Value::Null);
        m.insert("referenceId".to_string(), self.safe_string_k(item.clone(), "tx_id", &[]));
        m.insert("status".to_string(), self.parse_transaction_status(self.safe_string_k(item.clone(), "status", &[])).map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null));
        m.insert("amount".to_string(), amount);
        m.insert("before".to_string(), Value::Null);
        m.insert("after".to_string(), Value::Null);
        m.insert("fee".to_string(), fee);
        m.insert("direction".to_string(), direction);
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp));
        m.insert("type".to_string(), self.parse_ledger_entry_type(self.safe_string_k(item.clone(), "type", &[])).map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null));
        m.insert("info".to_string(), item);
    m
}), &[currency]);

    Value::Null
}

    pub fn parse_ledger_entry_type(&self, mut type_var: Value) -> Option<String> {
        let mut types: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("BALANCE".to_string(), Value::Str("transaction".into()));
                m.insert("COLLATERAL".to_string(), Value::Str("transfer".into()));
            m
        });
        return self.safe_string(types, type_var.clone(), &[type_var.clone()]).as_str().map(str::to_owned);
}

/*
 * @method
 * @name modetrade#fetchLedger
 * @description fetch the history of changes, actions done by the user or operations that altered the balance of the user
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-asset-history
 * @param {string} [code] unified currency code, default is undefined
 * @param {int} [since] timestamp in ms of the earliest ledger entry, default is undefined
 * @param {int} [limit] max number of ledger entries to return, default is undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ledger structure]{@link https://docs.ccxt.com/?id=ledger-entry-structure}
 */
    pub async fn fetch_ledger(&mut self, optional_args: &[Value]) -> Value {
        let mut code = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut currencyRows: Value = self.get_asset_history_rows(&[code, since.clone(), limit.clone(), params.clone()]).await;
        let mut currency: Value = self.safe_value(currencyRows.clone(), Value::Int(0), &[]);
        let mut rows: Value = self.safe_list(currencyRows, Value::Int(1), &[]);
        return self.parse_ledger(rows, &[currency, since, limit, params]);

    Value::Null
}

    pub fn parse_transaction(&self, mut transaction: Value, optional_args: &[Value]) -> Value {
        let mut currency = get_arg(optional_args, 0, Value::Null);
        // example in fetchLedger
        let mut code: Value = self.safe_string_k(transaction.clone(), "token", &[]);
        let mut movementDirection: Value = self.safe_string_lower(transaction.clone(), Value::Str("token_side".into()), &[]);
        if (movementDirection.as_str() == Some("withdraw")) {
            movementDirection = Value::Str("withdrawal".into());
        }
        let mut fee: Value = self.parse_token_and_fee_temp(transaction.clone(), Value::Str("fee_token".into()), Value::Str("fee_amount".into()));
        let mut addressTo: Value = self.safe_string_k(transaction.clone(), "target_address", &[]);
        let mut addressFrom: Value = self.safe_string_k(transaction.clone(), "source_address", &[]);
        let mut timestamp: Value = self.safe_integer_k(transaction.clone(), "created_time", &[]);
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), transaction.clone());
        m.insert("id".to_string(), self.safe_string2(transaction.clone(), Value::Str("id".into()), Value::Str("withdraw_id".into()), &[]));
        m.insert("txid".to_string(), self.safe_string_k(transaction.clone(), "tx_id", &[]));
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp));
        m.insert("address".to_string(), Value::Null);
        m.insert("addressFrom".to_string(), addressFrom);
        m.insert("addressTo".to_string(), addressTo);
        m.insert("tag".to_string(), self.safe_string_k(transaction.clone(), "extra", &[]));
        m.insert("tagFrom".to_string(), Value::Null);
        m.insert("tagTo".to_string(), Value::Null);
        m.insert("type".to_string(), movementDirection);
        m.insert("amount".to_string(), self.safe_number_k(transaction.clone(), "amount", &[]));
        m.insert("currency".to_string(), code);
        m.insert("status".to_string(), self.parse_transaction_status(self.safe_string_k(transaction.clone(), "status", &[])).map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null));
        m.insert("updated".to_string(), self.safe_integer_k(transaction, "updated_time", &[]));
        m.insert("comment".to_string(), Value::Null);
        m.insert("internal".to_string(), Value::Null);
        m.insert("fee".to_string(), fee);
        m.insert("network".to_string(), Value::Null);
    m
});

    Value::Null
}

    pub fn parse_transaction_status(&self, mut status: Value) -> Option<String> {
        let mut statuses: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("NEW".to_string(), Value::Str("pending".into()));
                m.insert("CONFIRMING".to_string(), Value::Str("pending".into()));
                m.insert("PROCESSING".to_string(), Value::Str("pending".into()));
                m.insert("COMPLETED".to_string(), Value::Str("ok".into()));
                m.insert("CANCELED".to_string(), Value::Str("canceled".into()));
            m
        });
        if (status == Value::Null) {
            return None;
        }
        return self.safe_string(statuses, status.clone(), &[status.clone()]).as_str().map(str::to_owned);
}

/*
 * @method
 * @name modetrade#fetchDeposits
 * @description fetch all deposits made to an account
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-asset-history
 * @param {string} code unified currency code
 * @param {int} [since] the earliest time in ms to fetch deposits for
 * @param {int} [limit] the maximum number of deposits structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [transaction structures]{@link https://docs.ccxt.com/?id=transaction-structure}
 */
    pub async fn fetch_deposits(&mut self, optional_args: &[Value]) -> Value {
        let mut code = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("side".to_string(), Value::Str("DEPOSIT".into()));
            m
        });
        let __ws_arg_28 = self.extend(request, &[params]);
        return self.fetch_deposits_withdrawals(&[code, since, limit, __ws_arg_28]).await;

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchWithdrawals
 * @description fetch all withdrawals made from an account
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-asset-history
 * @param {string} code unified currency code
 * @param {int} [since] the earliest time in ms to fetch withdrawals for
 * @param {int} [limit] the maximum number of withdrawals structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [transaction structures]{@link https://docs.ccxt.com/?id=transaction-structure}
 */
    pub async fn fetch_withdrawals(&mut self, optional_args: &[Value]) -> Value {
        let mut code = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("side".to_string(), Value::Str("WITHDRAW".into()));
            m
        });
        let __ws_arg_29 = self.extend(request, &[params]);
        return self.fetch_deposits_withdrawals(&[code, since, limit, __ws_arg_29]).await;

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchDepositsWithdrawals
 * @description fetch history of deposits and withdrawals
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-asset-history
 * @param {string} [code] unified currency code for the currency of the deposit/withdrawals, default is undefined
 * @param {int} [since] timestamp in ms of the earliest deposit/withdrawal, default is undefined
 * @param {int} [limit] max number of deposit/withdrawals to return, default is undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a list of [transaction structure]{@link https://docs.ccxt.com/?id=transaction-structure}
 */
    pub async fn fetch_deposits_withdrawals(&mut self, optional_args: &[Value]) -> Value {
        let mut code = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let __ws_arg_30 = self.extend(request, &[params.clone()]);
        let mut currencyRows: Value = self.get_asset_history_rows(&[code, since.clone(), limit.clone(), __ws_arg_30]).await;
        let mut currency: Value = self.safe_value(currencyRows.clone(), Value::Int(0), &[]);
        let mut rows: Value = self.safe_list(currencyRows, Value::Int(1), &[Value::from(vec![])]);
        return self.parse_transactions(rows, &[currency, since, limit, params]);

    Value::Null
}

    pub async fn get_withdraw_nonce(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut response: Value = self.v1_private_get_withdraw_nonce(&[params]).await;
        //
        //     {
        //         "success": true,
        //         "timestamp": 1702989203989,
        //         "data": {
        //             "withdraw_nonce": 1
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        return self.safe_number_k(data, "withdraw_nonce", &[]);

    Value::Null
}

    pub fn hash_message(&self, mut message: Value) -> Option<String> {
        return add(&Value::Str("0x".into()), &self.hash(message, Value::Str("keccak".into()), &[Value::Str("hex".into())])).as_str().map(str::to_owned);
}

    pub fn sign_hash(&self, mut hash: Value, mut privateKey: Value) -> Option<String> {
        let mut signature: Value = ecdsa(hash.as_str().map(|__s| { let __c: Vec<char> = __s.chars().collect(); let __l = __c.len() as i64; let __i = (__l - 64).max(0); let __j = __l; if __i <= __j { __c[__i as usize..__j as usize].iter().collect::<String>() } else { String::new() } }).map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null), privateKey.as_str().map(|__s| { let __c: Vec<char> = __s.chars().collect(); let __l = __c.len() as i64; let __i = (__l - 64).max(0); let __j = __l; if __i <= __j { __c[__i as usize..__j as usize].iter().collect::<String>() } else { String::new() } }).map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null), Value::Str("secp256k1".into()), Value::Null);
        let mut r: Value = signature.as_map().and_then(|__m| __m.get("r")).cloned().unwrap_or(Value::Null);
        let mut s: Value = signature.as_map().and_then(|__m| __m.get("s")).cloned().unwrap_or(Value::Null);
        let mut v: Value = self.int_to_base16(self.sum(&[Value::Int(27), signature.as_map().and_then(|__m| __m.get("v")).cloned().unwrap_or(Value::Null)]), &[]);
        return Value::Str(format!("{}{}", Value::Str(format!("{}{}", Value::Str(format!("{}{}", Value::Str("0x".into()), pad_start(&r, &Value::Int(64), &Value::Str("0".into()))).into()), pad_start(&s, &Value::Int(64), &Value::Str("0".into()))).into()), v).into()).as_str().map(str::to_owned);
}

    pub fn sign_message(&self, mut message: Value, mut privateKey: Value) -> Option<String> {
        return self.sign_hash(self.hash_message(message).map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null), privateKey.as_str().map(|__s| { let __c: Vec<char> = __s.chars().collect(); let __l = __c.len() as i64; let __i = (__l - 64).max(0); let __j = __l; if __i <= __j { __c[__i as usize..__j as usize].iter().collect::<String>() } else { String::new() } }).map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null));
}

/*
 * @method
 * @name modetrade#withdraw
 * @description make a withdrawal
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/create-withdraw-request
 * @param {string} code unified currency code
 * @param {float} amount the amount to withdraw
 * @param {string} address the address to withdraw to
 * @param {string} tag
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [transaction structure]{@link https://docs.ccxt.com/?id=transaction-structure}
 */
    pub async fn withdraw(&mut self, mut code: Value, mut amount: Value, mut address: Value, optional_args: &[Value]) -> Value {
        let mut tag = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        self.check_address(&[address.clone()]);
        if (code != Value::Null) {
            code = to_upper(&code);
            if (code.as_str() != Some("USDC")) {
                panic!("{}", crate::exchange_errors::not_supported(format!("{}{}", self.id.clone(), Value::Str(" withdraw() only support USDC".into()))));
            }
        }
        let mut currency: Value = self.currency(code.clone());
        let mut verifyingContractAddress: Value = self.safe_string_k(self.options.clone(), "verifyingContractAddress", &[]);
        let mut chainId: Value = self.safe_string_k(params.clone(), "chainId", &[]);
        let mut currencyNetworks: Value = self.safe_dict_k(currency.clone(), "networks", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut coinNetwork: Value = (if (chainId == Value::Null) { Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}) } else { self.safe_dict(currencyNetworks, chainId.clone(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]) });
        let mut coinNetworkId: Option<f64> = self.safe_number_k(coinNetwork, "id", &[]).as_f64();
        if (coinNetworkId.is_none()) {
            panic!("{}", crate::exchange_errors::bad_request(format!("{}{}", self.id.clone(), Value::Str(" withdraw() require chainId parameter".into()))));
        }
        let mut withdrawNonce: Value = self.get_withdraw_nonce(&[params.clone()]).await;
        let mut nonce: Value = self.nonce();
        let mut domain: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("chainId".to_string(), chainId.clone());
                m.insert("name".to_string(), Value::Str("Orderly".into()));
                m.insert("verifyingContract".to_string(), verifyingContractAddress.clone());
                m.insert("version".to_string(), Value::Str("1".into()));
            m
        });
        let mut messageTypes: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("Withdraw".to_string(), Value::from(vec![Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("brokerId".into()));
        m.insert("type".to_string(), Value::Str("string".into()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("chainId".into()));
        m.insert("type".to_string(), Value::Str("uint256".into()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("receiver".into()));
        m.insert("type".to_string(), Value::Str("address".into()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("token".into()));
        m.insert("type".to_string(), Value::Str("string".into()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("amount".into()));
        m.insert("type".to_string(), Value::Str("uint256".into()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("withdrawNonce".into()));
        m.insert("type".to_string(), Value::Str("uint64".into()));
    m
}), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("timestamp".into()));
        m.insert("type".to_string(), Value::Str("uint64".into()));
    m
})]));
            m
        });
        let mut withdrawRequest: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("brokerId".to_string(), self.safe_string_k(self.options.clone(), "keyBrokerId", &[Value::Str("mode".into())]));
                m.insert("chainId".to_string(), self.parse_to_int(chainId));
                m.insert("receiver".to_string(), address.clone());
                m.insert("token".to_string(), code);
                m.insert("amount".to_string(), to_string_val(&amount));
                m.insert("withdrawNonce".to_string(), withdrawNonce);
                m.insert("timestamp".to_string(), nonce);
            m
        });
        let mut msg: Value = self.eth_encode_structured_data(domain, messageTypes, withdrawRequest.clone());
        let mut signature: Value = self.sign_message(msg, self.privateKey.clone()).map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("signature".to_string(), signature);
                m.insert("userAddress".to_string(), address);
                m.insert("verifyingContract".to_string(), verifyingContractAddress);
                m.insert("message".to_string(), withdrawRequest);
            m
        });
        params = self.omit(params.clone(), Value::Str("chainId".into()), &[]);
        let __ws_arg_31 = self.extend(request, &[params]);
        let mut response: Value = self.v1_private_post_withdraw_request(&[__ws_arg_31]).await;
        //
        //     {
        //         "success": true,
        //         "timestamp": 1702989203989,
        //         "data": {
        //             "withdraw_id": 123
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        return self.parse_transaction(data, &[currency]);

    Value::Null
}

    pub fn parse_leverage(&self, mut leverage: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        let mut leverageValue: Value = self.safe_integer_k(leverage.clone(), "max_leverage", &[]);
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), leverage);
        m.insert("symbol".to_string(), self.safe_string_k(market, "symbol", &[]));
        m.insert("marginMode".to_string(), Value::Null);
        m.insert("longLeverage".to_string(), leverageValue.clone());
        m.insert("shortLeverage".to_string(), leverageValue);
    m
});

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchLeverage
 * @description fetch the set leverage for a market
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-account-information
 * @param {string} symbol unified market symbol
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [leverage structure]{@link https://docs.ccxt.com/?id=leverage-structure}
 */
    pub async fn fetch_leverage(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol);
        let mut response: Value = self.v1_private_get_client_info(&[params]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //         "account_id": "<string>",
        //         "email": "test@test.com",
        //         "account_mode": "FUTURES",
        //         "max_leverage": 20,
        //         "taker_fee_rate": 123,
        //         "maker_fee_rate": 123,
        //         "futures_taker_fee_rate": 123,
        //         "futures_maker_fee_rate": 123,
        //         "maintenance_cancel_orders": true,
        //         "imr_factor": {
        //             "PERP_BTC_USDC": 123,
        //             "PERP_ETH_USDC": 123,
        //             "PERP_NEAR_USDC": 123
        //         },
        //         "max_notional": {
        //             "PERP_BTC_USDC": 123,
        //             "PERP_ETH_USDC": 123,
        //             "PERP_NEAR_USDC": 123
        //         }
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        return self.parse_leverage(data, &[market]);

    Value::Null
}

/*
 * @method
 * @name modetrade#setLeverage
 * @description set the level of leverage for a market
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/update-leverage-setting
 * @param {int} [leverage] the rate of leverage
 * @param {string} [symbol] unified market symbol
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} response from the exchange
 */
    pub async fn set_leverage(&mut self, mut leverage: Value, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut isMinLeverage: bool = leverage.as_f64().unwrap_or(f64::NAN) < ((1i64) as f64);
        let mut isMaxLeverage: bool = leverage.as_f64().unwrap_or(f64::NAN) > ((50i64) as f64);
        if isMinLeverage || isMaxLeverage {
            panic!("{}", crate::exchange_errors::bad_request(format!("{}{}", self.id.clone(), Value::Str(" leverage should be between 1 and 50".into()))));
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("leverage".to_string(), leverage);
            m
        });
        let __ws_arg_32 = self.extend(request, &[params]);
        return self.v1_private_post_client_leverage(&[__ws_arg_32]).await;

    Value::Null
}

    pub fn parse_position(&self, mut position: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        // {
        //     "IMR_withdraw_orders": 0.1,
        //     "MMR_with_orders": 0.05,
        //     "average_open_price": 27908.14386047,
        //     "cost_position": -139329.358492,
        //     "est_liq_price": 117335.92899428,
        //     "fee_24_h": 123,
        //     "imr": 0.1,
        //     "last_sum_unitary_funding": 70.38,
        //     "mark_price": 27794.9,
        //     "mmr": 0.05,
        //     "pending_long_qty": 123,
        //     "pending_short_qty": 123,
        //     "pnl_24_h": 123,
        //     "position_qty": -5,
        //     "settle_price": 27865.8716984,
        //     "symbol": "PERP_BTC_USDC",
        //     "timestamp": 1685429350571,
        //     "unsettled_pnl": 354.858492
        // }
        //
        let mut contract: Value = self.safe_string_k(position.clone(), "symbol", &[]);
        market = self.safe_market(&[contract, market.clone()]);
        let mut size: Value = self.safe_string_k(position.clone(), "position_qty", &[]);
        let mut side: Value = Value::Null;
        if is_true(&crate::precise::Precise::stringGt(&size, &Value::Str("0".into()))) {
            side = Value::Str("long".into());
        }  else {
            side = Value::Str("short".into());
        }
        let mut contractSize: Value = self.safe_string_k(market.clone(), "contractSize", &[]);
        let mut markPrice: Value = self.safe_string_k(position.clone(), "mark_price", &[]);
        let mut timestamp: Value = self.safe_integer_k(position.clone(), "timestamp", &[]);
        let mut entryPrice: Value = self.safe_string_k(position.clone(), "average_open_price", &[]);
        let mut unrealisedPnl: Value = self.safe_string_k(position.clone(), "unsettled_pnl", &[]);
        size = crate::precise::Precise::stringAbs(&size);
        let mut notional: Value = crate::precise::Precise::stringMul(&size, &markPrice);
        return self.safe_position(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), position.clone());
        m.insert("id".to_string(), Value::Null);
        m.insert("symbol".to_string(), self.safe_string_k(market, "symbol", &[]));
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp));
        m.insert("lastUpdateTimestamp".to_string(), Value::Null);
        m.insert("initialMargin".to_string(), Value::Null);
        m.insert("initialMarginPercentage".to_string(), Value::Null);
        m.insert("maintenanceMargin".to_string(), Value::Null);
        m.insert("maintenanceMarginPercentage".to_string(), Value::Null);
        m.insert("entryPrice".to_string(), self.parse_number(entryPrice, &[]));
        m.insert("notional".to_string(), self.parse_number(notional, &[]));
        m.insert("leverage".to_string(), Value::Null);
        m.insert("unrealizedPnl".to_string(), self.parse_number(unrealisedPnl, &[]));
        m.insert("contracts".to_string(), self.parse_number(size, &[]));
        m.insert("contractSize".to_string(), self.parse_number(contractSize, &[]));
        m.insert("marginRatio".to_string(), Value::Null);
        m.insert("liquidationPrice".to_string(), self.safe_number_k(position, "est_liq_price", &[]));
        m.insert("markPrice".to_string(), self.parse_number(markPrice, &[]));
        m.insert("lastPrice".to_string(), Value::Null);
        m.insert("collateral".to_string(), Value::Null);
        m.insert("marginMode".to_string(), Value::Str("cross".into()));
        m.insert("marginType".to_string(), Value::Null);
        m.insert("side".to_string(), side);
        m.insert("percentage".to_string(), Value::Null);
        m.insert("hedged".to_string(), Value::Null);
        m.insert("stopLossPrice".to_string(), Value::Null);
        m.insert("takeProfitPrice".to_string(), Value::Null);
    m
}));

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchPosition
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-one-position-info
 * @description fetch data on an open position
 * @param {string} symbol unified market symbol of the market the position is held in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [position structure]{@link https://docs.ccxt.com/?id=position-structure}
 */
    pub async fn fetch_position(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        if (symbol == Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(format!("{}{}", self.id.clone(), Value::Str(" fetchPosition() requires a symbol argument".into()))));
        }
        let mut market: Value = self.market(symbol);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
            m
        });
        let __ws_arg_33 = self.extend(request, &[params]);
        let mut response: Value = self.v1_private_get_position_symbol(&[__ws_arg_33]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //         "IMR_withdraw_orders": 0.1,
        //         "MMR_with_orders": 0.05,
        //         "average_open_price": 27908.14386047,
        //         "cost_position": -139329.358492,
        //         "est_liq_price": 117335.92899428,
        //         "fee_24_h": 123,
        //         "imr": 0.1,
        //         "last_sum_unitary_funding": 70.38,
        //         "mark_price": 27794.9,
        //         "mmr": 0.05,
        //         "pending_long_qty": 123,
        //         "pending_short_qty": 123,
        //         "pnl_24_h": 123,
        //         "position_qty": -5,
        //         "settle_price": 27865.8716984,
        //         "symbol": "PERP_BTC_USDC",
        //         "timestamp": 1685429350571,
        //         "unsettled_pnl": 354.858492
        //     }
        // }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        return self.parse_position(data, &[market]);

    Value::Null
}

/*
 * @method
 * @name modetrade#fetchPositions
 * @description fetch all open positions
 * @see https://orderly.network/docs/build-on-omnichain/restful-api/private/get-all-positions-info
 * @param {string[]} [symbols] list of unified market symbols
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [position structure]{@link https://docs.ccxt.com/?id=position-structure}
 */
    pub async fn fetch_positions(&mut self, optional_args: &[Value]) -> Value {
        let mut symbols = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut response: Value = self.v1_private_get_positions(&[params]).await;
        //
        // {
        //     "success": true,
        //     "timestamp": 1702989203989,
        //     "data": {
        //         "current_margin_ratio_with_orders": 1.2385,
        //         "free_collateral": 450315.09115,
        //         "initial_margin_ratio": 0.1,
        //         "initial_margin_ratio_with_orders": 0.1,
        //         "maintenance_margin_ratio": 0.05,
        //         "maintenance_margin_ratio_with_orders": 0.05,
        //         "margin_ratio": 1.2385,
        //         "open_margin_ratio": 1.2102,
        //         "total_collateral_value": 489865.71329,
        //         "total_pnl_24_h": 123,
        //         "rows": [{
        //             "IMR_withdraw_orders": 0.1,
        //             "MMR_with_orders": 0.05,
        //             "average_open_price": 27908.14386047,
        //             "cost_position": -139329.358492,
        //             "est_liq_price": 117335.92899428,
        //             "fee_24_h": 123,
        //             "imr": 0.1,
        //             "last_sum_unitary_funding": 70.38,
        //             "mark_price": 27794.9,
        //             "mmr": 0.05,
        //             "pending_long_qty": 123,
        //             "pending_short_qty": 123,
        //             "pnl_24_h": 123,
        //             "position_qty": -5,
        //             "settle_price": 27865.8716984,
        //             "symbol": "PERP_BTC_USDC",
        //             "timestamp": 1685429350571,
        //             "unsettled_pnl": 354.858492
        //         }]
        //     }
        // }
        //
        let mut result: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut positions: Value = self.safe_list_k(result, "rows", &[Value::from(vec![])]);
        return self.parse_positions(positions, &[symbols]);

    Value::Null
}

    pub fn nonce(&self) -> Value {
        return self.milliseconds();

    Value::Null
}

    pub fn sign(&self, mut path: Value, optional_args: &[Value]) -> Value {
        let mut section = get_arg(optional_args, 0, Value::Str("public".into()));
        let mut method = get_arg(optional_args, 1, Value::Str("GET".into()));
        let mut params = get_arg(optional_args, 2, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut headers = get_arg(optional_args, 3, Value::Null);
        let mut body = get_arg(optional_args, 4, Value::Null);
        let mut version: Value = get_value(&section, &Value::Int(0));
        let mut access: Value = get_value(&section, &Value::Int(1));
        let mut pathWithParams: Value = self.implode_params(path.clone(), params.clone());
        let mut url: Value = Value::Str(format!("{}{}", Value::Str(format!("{}{}", add(&get_value(&self.urls.as_map().and_then(|__m| __m.get("api")).cloned().unwrap_or(Value::Null), &access), &Value::Str("/".into())), version).into()), Value::Str("/".into())).into());
        params = self.omit(params.clone(), self.extract_params(path.clone()), &[]);
        params = self.keysort(params.clone(), &[]);
        if (access.as_str() == Some("public")) {
            url = Value::Str(format!("{}{}", url, pathWithParams).into());
            if ((object_keys(&params).len() as i64) as f64) > ((0i64) as f64) {
                url = Value::Str(format!("{}{}", url, Value::Str(format!("{}{}", Value::Str("?".into()), self.urlencode(params.clone(), &[])).into())).into());
            }
        }  else {
            self.check_required_credentials(&[]);
            let mut isPostOrPut: bool = (method.as_str() == Some("POST")) || (method.as_str() == Some("PUT"));
            let mut isOrder: bool = (path.as_str() == Some("algo/order")) || (path.as_str() == Some("order")) || (path.as_str() == Some("batch-order"));
            if isPostOrPut && isOrder {
                let mut isSandboxMode: Value = self.safe_bool_k(self.options.clone(), "sandboxMode", &[Value::Bool(false)]);
                if (isSandboxMode.as_bool() != Some(true)) {
                    let mut brokerId: Value = self.safe_string_k(self.options.clone(), "brokerId", &[Value::Str("CCXTMODE".into())]);
                    if (path.as_str() == Some("batch-order")) {
                        let mut ordersList: Value = self.safe_list_k(params.clone(), "orders", &[Value::from(vec![])]);
                        {
                                                        let mut i: Value = Value::Int(0);
                            let mut __for_first_957: bool = true;
                            while { if !__for_first_957 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_957 = false; i.as_f64().unwrap_or(f64::NAN) < ((ordersList.len() as i64) as f64) } {
                            add_element_to_object(get_value_mut(get_value_mut(&mut params, &Value::Str("orders".into())), &i), &Value::Str("order_tag".into()), brokerId.clone());
                        }
                        }
                    }  else {
                        add_element_to_object(&mut params, &Value::Str("order_tag".into()), brokerId);
                    }
                }
                params = self.keysort(params.clone(), &[]);
            }
            let mut auth: Value = Value::Str("".into());
            let mut ts: Value = to_string_val(&self.nonce());
            url = Value::Str(format!("{}{}", url, pathWithParams).into());
            let mut apiKey: Value = self.apiKey.clone();
            if Value::Int(apiKey.as_str().and_then(|__s| __s.find("ed25519:")).map(|__i| __i as i64).unwrap_or(-1)).as_f64().unwrap_or(f64::NAN) < ((0i64) as f64) {
                apiKey = Value::Str(format!("{}{}", Value::Str("ed25519:".into()), apiKey).into());
            }
            headers = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("orderly-account-id".to_string(), self.accountId.clone());
                    m.insert("orderly-key".to_string(), apiKey);
                    m.insert("orderly-timestamp".to_string(), ts.clone());
                m
            });
            auth = Value::Str(format!("{}{}", Value::Str(format!("{}{}", Value::Str(format!("{}{}", Value::Str(format!("{}{}", Value::Str(format!("{}{}", ts, method).into()), Value::Str("/".into())).into()), version).into()), Value::Str("/".into())).into()), pathWithParams).into());
            if (method.as_str() == Some("POST")) || (method.as_str() == Some("PUT")) {
                body = json_stringify(&params);
                auth = add(&auth, &body);
                add_element_to_object(&mut headers, &Value::Str("content-type".into()), Value::Str("application/json".into()));
            }  else {
                if ((object_keys(&params).len() as i64) as f64) > ((0i64) as f64) {
                    url = Value::Str(format!("{}{}", url, Value::Str(format!("{}{}", Value::Str("?".into()), self.urlencode(params.clone(), &[])).into())).into());
                    auth = Value::Str(format!("{}{}", auth, Value::Str(format!("{}{}", Value::Str("?".into()), self.rawencode(params.clone(), &[])).into())).into());
                }
                add_element_to_object(&mut headers, &Value::Str("content-type".into()), Value::Str("application/x-www-form-urlencoded".into()));
                if (method.as_str() == Some("DELETE")) {
                    body = Value::Str("".into());
                }
            }
            let mut secret: Value = self.secret.clone();
            if Value::Int(secret.as_str().and_then(|__s| __s.find("ed25519:")).map(|__i| __i as i64).unwrap_or(-1)).as_f64().unwrap_or(f64::NAN) >= ((0i64) as f64) {
                let mut parts: Value = split(&secret, &Value::Str("ed25519:".into()));
                secret = parts.as_array().and_then(|__arr| __arr.get(1)).cloned().unwrap_or(Value::Null);
            }
            let mut signature: Value = eddsa(self.encode(auth), self.base58_to_binary(secret, &[]), Value::Str("ed25519".into()));
            add_element_to_object(&mut headers, &Value::Str("orderly-signature".into()), self.urlencode_base64(self.base64_to_binary(signature, &[]), &[]));
        }
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("url".to_string(), url);
        m.insert("method".to_string(), method);
        m.insert("body".to_string(), body);
        m.insert("headers".to_string(), headers);
    m
});

    Value::Null
}

    pub fn handle_errors(&self, mut httpCode: Value, mut reason: Value, mut url: Value, mut method: Value, mut headers: Value, mut body: Value, mut response: Value, mut requestHeaders: Value, mut requestBody: Value) -> Value {
        if (response == Value::Null) || (response == Value::Null) {
            return Value::Null;
        }
        //
        //     400 Bad Request {"success":false,"code":-1012,"message":"Amount is required for buy market orders when margin disabled."}
        //                     {"code":"-1011","message":"The system is under maintenance.","success":false}
        //
        let mut success: Value = self.safe_bool_k(response.clone(), "success", &[]);
        let mut errorCode: Value = self.safe_string_k(response.clone(), "code", &[]);
        if (success.as_bool() != Some(true)) {
            let mut feedback: Value = Value::Str(format!("{}{}", Value::Str(format!("{}{}", self.id.clone(), Value::Str(" ".into())).into()), json_stringify(&response)).into());
            self.throw_broadly_matched_exception(self.exceptions.as_map().and_then(|__m| __m.get("broad")).cloned().unwrap_or(Value::Null), body, feedback.clone());
            self.throw_exactly_matched_exception(self.exceptions.as_map().and_then(|__m| __m.get("exact")).cloned().unwrap_or(Value::Null), errorCode, feedback.clone());
            panic!("{}", crate::exchange_errors::exchange_error(feedback));
        }
        return Value::Null;

    Value::Null
}
}
