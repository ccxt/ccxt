// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#![allow(unused, non_snake_case, clippy::all)]
use crate::Value;
use crate::get_value;
use crate::runtime::*;
// Base methods are now trait methods (review #1: static dispatch). Bring the
// traits into scope so `self.market(...)`, `self.safe_market(...)`,
// `self.load_markets(...)`, … on this Core resolve to the base defaults.
use crate::exchange_generated::ExchangeBase;
use crate::exchange::ExchangeRuntime;
// Dynamic `this[method](...)` re-entries are emitted as
// `self.call_dynamic_checked(...)` (blanket-impl'd on every Core) so an
// unresolvable name raises NotSupported instead of yielding a silent Null.
use crate::exchange::CallDynamicChecked;


pub struct RevolutxCore {
    pub exchange: crate::exchange::Exchange,
}

impl RevolutxCore {
    pub fn new(config: Option<crate::Value>) -> Self {
        let mut s = Self { exchange: crate::exchange::Exchange::new(config) };
        s.init();
        s
    }

    pub fn init(&mut self) {
        let described = RevolutxCore::describe(self);
        self.initialize_properties(described);
        <Self as crate::exchange_generated::ExchangeBase>::after_construct(self);
    }

    /// Compatibility no-op. The old pointer-based dispatch needed a post-move
    /// `bind()`; static trait dispatch (review #1) needs no binding, so this
    /// just exists so callers that still call it keep compiling.
    #[inline]
    pub fn bind(&mut self) {}
}

impl crate::exchange::DerivedExchange for RevolutxCore {
    fn parse_ticker(&self, ticker: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on RevolutxCore.
        RevolutxCore::parse_ticker(self, ticker, &[market])
    }
    fn parse_trade(&self, trade: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on RevolutxCore.
        RevolutxCore::parse_trade(self, trade, &[market])
    }
    fn parse_order(&self, order: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on RevolutxCore.
        RevolutxCore::parse_order(self, order, &[market])
    }
    fn parse_market(&self, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on RevolutxCore.
        RevolutxCore::parse_market(self, market)
    }
    fn parse_ohlcv(&self, ohlcv: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on RevolutxCore.
        RevolutxCore::parse_ohlcv(self, ohlcv, &[market])
    }
    fn parse_currency(&self, currency: crate::Value) -> crate::Value {
        // Forward to the inherent method on RevolutxCore.
        RevolutxCore::parse_currency(self, currency)
    }
    fn parse_my_trade(&self, trade: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on RevolutxCore.
        RevolutxCore::parse_my_trade(self, trade, &[market])
    }
    fn sign(&self, path: crate::Value, api: crate::Value, method: crate::Value, params: crate::Value, headers: crate::Value, body: crate::Value) -> crate::Value {
        // Forward to the inherent method on RevolutxCore.
        RevolutxCore::sign(self, path, &[api, method, params, headers, body])
    }
    fn handle_errors(&self, code: crate::Value, reason: crate::Value, url: crate::Value, method: crate::Value, headers: crate::Value, body: crate::Value, response: crate::Value, request_headers: crate::Value, request_body: crate::Value) -> crate::Value {
        // Forward to the inherent method on RevolutxCore.
        RevolutxCore::handle_errors(self, code, reason, url, method, headers, body, response, request_headers, request_body)
    }
}

impl crate::exchange_generated::ExchangeBase for RevolutxCore {
    fn call_dynamic<'a>(&'a mut self, method: &'a str, args: Vec<crate::Value>)
        -> std::pin::Pin<Box<dyn std::future::Future<Output = crate::Value> + Send + 'a>>
    {
        Box::pin(async move {
            match method {
                "cancel_all_orders" => self.cancel_all_orders(&args[..]).await,
                "cancel_order" => self.cancel_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "create_order" => self.create_order(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), args.get(3).cloned().unwrap_or(crate::Value::Null), &args[4.min(args.len())..]).await,
                "edit_order" => self.edit_order(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), args.get(3).cloned().unwrap_or(crate::Value::Null), &args[4.min(args.len())..]).await,
                "fetch_balance" => self.fetch_balance(&args[..]).await,
                "fetch_closed_orders" => self.fetch_closed_orders(&args[..]).await,
                "fetch_currencies" => self.fetch_currencies(&args[..]).await,
                "fetch_markets" => self.fetch_markets(&args[..]).await,
                "fetch_my_trades" => self.fetch_my_trades(&args[..]).await,
                "fetch_ohlcv" => self.fetch_ohlcv(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "fetch_open_orders" => self.fetch_open_orders(&args[..]).await,
                "fetch_order" => self.fetch_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "fetch_order_book" => self.fetch_order_book(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "fetch_orders" => self.fetch_orders(&args[..]).await,
                "fetch_ticker" => self.fetch_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "fetch_tickers" => self.fetch_tickers(&args[..]).await,
                "fetch_trades" => self.fetch_trades(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]).await,
                "handle_errors" => self.handle_errors(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), args.get(3).cloned().unwrap_or(crate::Value::Null), args.get(4).cloned().unwrap_or(crate::Value::Null), args.get(5).cloned().unwrap_or(crate::Value::Null), args.get(6).cloned().unwrap_or(crate::Value::Null), args.get(7).cloned().unwrap_or(crate::Value::Null), args.get(8).cloned().unwrap_or(crate::Value::Null)),
                "parse_currency" => self.parse_currency(args.get(0).cloned().unwrap_or(crate::Value::Null)),
                "parse_market" => self.parse_market(args.get(0).cloned().unwrap_or(crate::Value::Null)),
                "parse_my_trade" => self.parse_my_trade(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "parse_ohlcv" => self.parse_ohlcv(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "parse_order" => self.parse_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "parse_ticker" => self.parse_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "parse_trade" => self.parse_trade(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                "sign" => self.sign(args.get(0).cloned().unwrap_or(crate::Value::Null), &args[1.min(args.len())..]),
                // Fall through to the base-only methods (cancelOrderWithClientOrderId, …).
                _ => self.call_dynamic_base(method, args).await,
            }
        })
    }
}

impl std::ops::Deref for RevolutxCore {
    type Target = crate::exchange::Exchange;
    fn deref(&self) -> &crate::exchange::Exchange { &self.exchange }
}

impl std::ops::DerefMut for RevolutxCore {
    fn deref_mut(&mut self) -> &mut crate::exchange::Exchange { &mut self.exchange }
}

impl RevolutxCore {
    pub fn describe(&self) -> Value {
        return self.deep_extend(self.super_describe(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), Value::Str("revolutx".into()));
        m.insert("name".to_string(), Value::Str("Revolut X".into()));
        m.insert("countries".to_string(), Value::from(vec![Value::Str("GB".into())]));
        m.insert("rateLimit".to_string(), Value::Int(1000));
        m.insert("certified".to_string(), Value::Bool(false));
        m.insert("pro".to_string(), Value::Bool(false));
        m.insert("has".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("CORS".to_string(), Value::Null);
        m.insert("spot".to_string(), Value::Bool(true));
        m.insert("margin".to_string(), Value::Bool(false));
        m.insert("swap".to_string(), Value::Bool(false));
        m.insert("future".to_string(), Value::Bool(false));
        m.insert("option".to_string(), Value::Bool(false));
        m.insert("cancelAllOrders".to_string(), Value::Bool(true));
        m.insert("cancelOrder".to_string(), Value::Bool(true));
        m.insert("createOrder".to_string(), Value::Bool(true));
        m.insert("editOrder".to_string(), Value::Bool(true));
        m.insert("fetchBalance".to_string(), Value::Bool(true));
        m.insert("fetchClosedOrders".to_string(), Value::Bool(true));
        m.insert("fetchCurrencies".to_string(), Value::Bool(true));
        m.insert("fetchMarkets".to_string(), Value::Bool(true));
        m.insert("fetchMyTrades".to_string(), Value::Bool(true));
        m.insert("fetchOHLCV".to_string(), Value::Bool(true));
        m.insert("fetchOpenOrders".to_string(), Value::Bool(true));
        m.insert("fetchOrder".to_string(), Value::Bool(true));
        m.insert("fetchOrderBook".to_string(), Value::Bool(true));
        m.insert("fetchOrders".to_string(), Value::Bool(true));
        m.insert("fetchTicker".to_string(), Value::Bool(true));
        m.insert("fetchTickers".to_string(), Value::Bool(true));
        m.insert("fetchTrades".to_string(), Value::Bool(true));
    m
}));
        m.insert("timeframes".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("1m".to_string(), Value::Int(1));
        m.insert("5m".to_string(), Value::Int(5));
        m.insert("15m".to_string(), Value::Int(15));
        m.insert("30m".to_string(), Value::Int(30));
        m.insert("1h".to_string(), Value::Int(60));
        m.insert("4h".to_string(), Value::Int(240));
        m.insert("1d".to_string(), Value::Int(1440));
        m.insert("2d".to_string(), Value::Int(2880));
        m.insert("4d".to_string(), Value::Int(5760));
        m.insert("1w".to_string(), Value::Int(10080));
        m.insert("2w".to_string(), Value::Int(20160));
        m.insert("4w".to_string(), Value::Int(40320));
    m
}));
        m.insert("urls".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("logo".to_string(), Value::Str("https://github.com/user-attachments/assets/594ab1a5-3985-4f79-89f4-c40ef7c33867".into()));
        m.insert("api".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("public".to_string(), Value::Str("https://revx.revolut.com/api".into()));
        m.insert("private".to_string(), Value::Str("https://revx.revolut.com/api".into()));
    m
}));
        m.insert("www".to_string(), Value::Str("https://exchange.revolut.com".into()));
        m.insert("doc".to_string(), Value::Str("https://developer.revolut.com/docs/api/revolut-x-crypto-exchange".into()));
        m.insert("fees".to_string(), Value::Str("https://exchange.revolut.com/fees".into()));
    m
}));
        m.insert("api".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("public".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("get".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("2.0/public/order-book/{symbol}".to_string(), Value::Int(1));
        m.insert("1.0/public/tickers".to_string(), Value::Int(1));
        m.insert("1.0/public/candles/{symbol}".to_string(), Value::Int(1));
        m.insert("1.0/public/trades/all".to_string(), Value::Int(1));
        m.insert("1.0/public/configuration/currencies".to_string(), Value::Int(1));
        m.insert("1.0/public/configuration/pairs".to_string(), Value::Int(1));
    m
}));
    m
}));
        m.insert("private".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("get".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("1.0/balances".to_string(), Value::Int(1));
        m.insert("1.0/orders/active".to_string(), Value::Int(1));
        m.insert("1.0/orders/historical".to_string(), Value::Int(1));
        m.insert("1.0/orders/{venue_order_id}".to_string(), Value::Int(1));
        m.insert("1.0/orders/fills/{venue_order_id}".to_string(), Value::Int(1));
        m.insert("1.0/trades/private/{symbol}".to_string(), Value::Int(1));
        m.insert("1.0/transactions".to_string(), Value::Int(1));
    m
}));
        m.insert("post".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("1.0/orders".to_string(), Value::Int(1));
    m
}));
        m.insert("put".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("1.0/orders/{venue_order_id}".to_string(), Value::Int(1));
    m
}));
        m.insert("delete".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("1.0/orders".to_string(), Value::Int(1));
        m.insert("1.0/orders/{venue_order_id}".to_string(), Value::Int(1));
    m
}));
    m
}));
    m
}));
        m.insert("requiredCredentials".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("apiKey".to_string(), Value::Bool(true));
        m.insert("secret".to_string(), Value::Bool(false));
        m.insert("privateKey".to_string(), Value::Bool(true));
    m
}));
        m.insert("fees".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("trading".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("tierBased".to_string(), Value::Bool(false));
        m.insert("percentage".to_string(), Value::Bool(true));
        m.insert("taker".to_string(), self.parse_number(Value::Str("0.0009".into()), &[]));
        m.insert("maker".to_string(), self.parse_number(Value::Str("0".into()), &[]));
    m
}));
    m
}));
        m.insert("options".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("region".to_string(), Value::Null);
    m
}));
        m.insert("precisionMode".to_string(), Value::Int(crate::runtime::TICK_SIZE));
        m.insert("features".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("spot".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("sandbox".to_string(), Value::Bool(false));
        m.insert("createOrder".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("triggerPrice".to_string(), Value::Bool(false));
        m.insert("triggerPriceType".to_string(), Value::Null);
        m.insert("triggerDirection".to_string(), Value::Bool(false));
        m.insert("stopLossPrice".to_string(), Value::Bool(false));
        m.insert("takeProfitPrice".to_string(), Value::Bool(false));
        m.insert("attachedStopLossTakeProfit".to_string(), Value::Null);
        m.insert("timeInForce".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("GTC".to_string(), Value::Bool(true));
        m.insert("IOC".to_string(), Value::Bool(true));
        m.insert("FOK".to_string(), Value::Bool(false));
        m.insert("PO".to_string(), Value::Bool(false));
        m.insert("GTD".to_string(), Value::Bool(false));
    m
}));
        m.insert("hedged".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("leverage".to_string(), Value::Bool(false));
        m.insert("marketBuyByCost".to_string(), Value::Bool(true));
        m.insert("marketBuyRequiresPrice".to_string(), Value::Bool(false));
        m.insert("selfTradePrevention".to_string(), Value::Bool(false));
        m.insert("iceberg".to_string(), Value::Bool(false));
    m
}));
        m.insert("createOrders".to_string(), Value::Null);
        m.insert("fetchMyTrades".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(1900));
        m.insert("daysBack".to_string(), Value::Int(30));
        m.insert("untilDays".to_string(), Value::Null);
        m.insert("symbolRequired".to_string(), Value::Bool(true));
    m
}));
        m.insert("fetchOrder".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("trigger".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
        m.insert("fetchOpenOrders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(300));
        m.insert("trigger".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
        m.insert("fetchOrders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(1900));
        m.insert("daysBack".to_string(), Value::Int(30));
        m.insert("untilDays".to_string(), Value::Null);
        m.insert("trigger".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
        m.insert("fetchClosedOrders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(1900));
        m.insert("daysBack".to_string(), Value::Int(30));
        m.insert("daysBackCanceled".to_string(), Value::Null);
        m.insert("untilDays".to_string(), Value::Null);
        m.insert("trigger".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
        m.insert("fetchOHLCV".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("limit".to_string(), Value::Int(50000));
    m
}));
    m
}));
    m
}));
        m.insert("httpExceptions".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("400".to_string(), Value::Str("BadRequest".into()).clone());
        m.insert("403".to_string(), Value::Str("PermissionDenied".into()).clone());
        m.insert("404".to_string(), Value::Str("OrderNotFound".into()).clone());
        m.insert("409".to_string(), Value::Str("ExchangeError".into()).clone());
        m.insert("429".to_string(), Value::Str("RateLimitExceeded".into()).clone());
    m
}));
        m.insert("exceptions".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("exact".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        m.insert("broad".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("insufficient".to_string(), Value::Str("InsufficientFunds".into()).clone());
        m.insert("invalid order".to_string(), Value::Str("InvalidOrder".into()).clone());
        m.insert("not found".to_string(), Value::Str("OrderNotFound".into()).clone());
        m.insert("timestamp is in the future".to_string(), Value::Str("InvalidNonce".into()).clone());
    m
}));
    m
}));
    m
})]);

    Value::Null
}

    pub fn sign(&self, mut path: Value, optional_args: &[Value]) -> Value {
        let mut api = get_arg(optional_args, 0, Value::Str("public".into()));
        let mut method = get_arg(optional_args, 1, Value::Str("GET".into()));
        let mut params = get_arg(optional_args, 2, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut headers = get_arg(optional_args, 3, Value::Null);
        let mut body = get_arg(optional_args, 4, Value::Null);
        let mut implodedPath: Value = self.implode_params(path.clone(), params.clone());
        let mut query: Value = self.omit(params, self.extract_params(path), &[]);
        let mut queryKeys: Value = object_keys(&query);
        let mut queryLength: f64 = ((queryKeys.len() as i64) as f64);
        let mut url: Value = Value::Str(format!("{}{}", add(&get_value(&self.urls.as_map().and_then(|__m| __m.get("api")).cloned().unwrap_or(Value::Null), &api), &Value::Str("/".into())), implodedPath).into());
        let mut queryString: Value = Value::Str("".into());
        if (api.as_str() == Some("private")) {
            self.check_required_credentials(&[]);
            let mut timestamp: Value = to_string_val(&self.milliseconds());
            if (method.as_str() == Some("GET")) {
                if queryLength > ((0i64) as f64) {
                    queryString = self.urlencode(query.clone(), &[]);
                    url = Value::Str(format!("{}{}", url, Value::Str(format!("{}{}", Value::Str("?".into()), queryString).into())).into());
                }
            }  else if (method.as_str() == Some("DELETE")) {
                if queryLength > ((0i64) as f64) {
                    queryString = self.urlencode(query.clone(), &[]);
                    url = Value::Str(format!("{}{}", url, Value::Str(format!("{}{}", Value::Str("?".into()), queryString).into())).into());
                }
            }  else {
                body = json_stringify(&query);
            }
            let mut requestPath: Value = Value::Str(format!("{}{}", Value::Str("/api/".into()), implodedPath).into());
            let mut bodyString: Value = Value::Str("".into());
            if (body != Value::Null) {
                bodyString = body.clone();
            }
            let mut message: Value = Value::Str(format!("{}{}", Value::Str(format!("{}{}", Value::Str(format!("{}{}", Value::Str(format!("{}{}", timestamp, to_upper(&method)).into()), requestPath).into()), queryString).into()), bodyString).into());
            let mut signature: Value = eddsa(self.encode(message), self.privateKey.clone(), Value::Str("ed25519".into()));
            headers = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("X-Revx-API-Key".to_string(), self.apiKey.clone());
                    m.insert("X-Revx-Timestamp".to_string(), timestamp);
                    m.insert("X-Revx-Signature".to_string(), signature);
                m
            });
            if (method.as_str() == Some("POST")) || (method.as_str() == Some("PUT")) {
                add_element_to_object(&mut headers, &Value::Str("Content-Type".into()), Value::Str("application/json".into()));
            }
        }  else {
            if (method.as_str() == Some("GET")) {
                if queryLength > ((0i64) as f64) {
                    queryString = self.urlencode(query.clone(), &[]);
                    url = Value::Str(format!("{}{}", url, Value::Str(format!("{}{}", Value::Str("?".into()), queryString).into())).into());
                }
            }  else {
                body = json_stringify(&query);
                headers = Value::Map({
                    let mut m = indexmap::IndexMap::new();
                        m.insert("Content-Type".to_string(), Value::Str("application/json".into()));
                    m
                });
            }
        }
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("url".to_string(), url);
        m.insert("method".to_string(), method);
        m.insert("body".to_string(), body);
        m.insert("headers".to_string(), headers.clone());
    m
});

    Value::Null
}

/*
 * @method
 * @name revolutx#parseMarket
 * @description parses a market from the exchange's market data
 * @ignore
 * @param {object} market the raw market data from the exchange
 * @returns {object} a [market structure]{@link https://docs.ccxt.com/?id=market-structure}
 */
    pub fn parse_market(&self, mut market: Value) -> Value {
        let mut id: Value = self.safe_string_k(market.clone(), "id", &[]);
        let mut base: Value = self.safe_string_k(market.clone(), "base", &[Value::Str("".into())]);
        let mut quote: Value = self.safe_string_k(market.clone(), "quote", &[Value::Str("".into())]);
        let mut baseId: Value = base.clone();
        let mut quoteId: Value = quote.clone();
        let mut baseStep: Value = self.safe_string_k(market.clone(), "base_step", &[]);
        let mut quoteStep: Value = self.safe_string_k(market.clone(), "quote_step", &[]);
        let mut minOrderSize: Value = self.safe_string_k(market.clone(), "min_order_size", &[]);
        let mut maxOrderSize: Value = self.safe_string_k(market.clone(), "max_order_size", &[]);
        let mut minOrderSizeQuote: Value = self.safe_string_k(market.clone(), "min_order_size_quote", &[]);
        let mut status: Option<String> = self.safe_string_k(market.clone(), "status", &[]).as_str().map(str::to_owned);
        let mut active: Value = (Value::Bool(status.as_deref() == Some("active")));
        let mut symbol: Value = Value::Str(format!("{}{}", Value::Str(format!("{}{}", base, Value::Str("/".into())).into()), quote).into());
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), id);
        m.insert("symbol".to_string(), symbol);
        m.insert("base".to_string(), base);
        m.insert("quote".to_string(), quote);
        m.insert("baseId".to_string(), baseId);
        m.insert("quoteId".to_string(), quoteId);
        m.insert("active".to_string(), active);
        m.insert("type".to_string(), Value::Str("spot".into()));
        m.insert("spot".to_string(), Value::Bool(true));
        m.insert("margin".to_string(), Value::Bool(false));
        m.insert("swap".to_string(), Value::Bool(false));
        m.insert("future".to_string(), Value::Bool(false));
        m.insert("option".to_string(), Value::Bool(false));
        m.insert("contract".to_string(), Value::Bool(false));
        m.insert("settle".to_string(), Value::Null);
        m.insert("settleId".to_string(), Value::Null);
        m.insert("contractSize".to_string(), Value::Null);
        m.insert("linear".to_string(), Value::Null);
        m.insert("inverse".to_string(), Value::Null);
        m.insert("expiry".to_string(), Value::Null);
        m.insert("expiryDatetime".to_string(), Value::Null);
        m.insert("strike".to_string(), Value::Null);
        m.insert("optionType".to_string(), Value::Null);
        m.insert("taker".to_string(), self.parse_number(Value::Str("0.0009".into()), &[]));
        m.insert("maker".to_string(), self.parse_number(Value::Str("0".into()), &[]));
        m.insert("percentage".to_string(), Value::Bool(true));
        m.insert("tierBased".to_string(), Value::Bool(false));
        m.insert("feeSide".to_string(), Value::Str("get".into()));
        m.insert("precision".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("amount".to_string(), self.parse_number(baseStep, &[]));
        m.insert("price".to_string(), self.parse_number(quoteStep, &[]));
    m
}));
        m.insert("limits".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("amount".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), self.parse_number(minOrderSize, &[]));
        m.insert("max".to_string(), self.parse_number(maxOrderSize, &[]));
    m
}));
        m.insert("price".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Null);
        m.insert("max".to_string(), Value::Null);
    m
}));
        m.insert("cost".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), self.parse_number(minOrderSizeQuote, &[]));
        m.insert("max".to_string(), Value::Null);
    m
}));
        m.insert("leverage".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Null);
        m.insert("max".to_string(), Value::Null);
    m
}));
        m.insert("market".to_string(), Value::Null);
    m
}));
        m.insert("created".to_string(), Value::Null);
        m.insert("info".to_string(), market);
    m
});

    Value::Null
}

/*
 * @method
 * @name revolutx#fetchMarkets
 * @description retrieves all available markets on the exchange
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.region] the region to filter markets by (e.g. EEA, UK)
 * @returns {object[]} an array of [market structures]{@link https://docs.ccxt.com/?id=market-structure}
 */
    pub async fn fetch_markets(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut region: Value = self.safe_string2(params.clone(), Value::Str("region".into()), Value::Str("region".into()), &[self.options.as_map().and_then(|__m| __m.get("region")).cloned().unwrap_or(Value::Null)]);
        if (region != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("region".to_string(), region); }
        }
        let __ws_arg_0 = self.extend(request, &[params]);
        let mut response: Value = self.public_get10_public_configuration_pairs(&[__ws_arg_0]).await;
        //
        //     {
        //         "BTC/USD": {
        //             "base": "BTC", "quote": "USD",
        //             "base_step": "0.0000001", "quote_step": "0.01",
        //             "min_order_size": "0.0000001", "max_order_size": "1000",
        //             "min_order_size_quote": "0.01", "status": "active"
        //         }
        //     }
        //
        let mut markets: Value = self.safe_dict_k(response.clone(), "data", &[response.clone()]);
        let mut keys: Value = object_keys(&markets);
        let mut result: Value = Value::from(vec![]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1075: bool = true;
            while { if !__for_first_1075 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_1075 = false; i.as_f64().unwrap_or(f64::NAN) < ((keys.len() as i64) as f64) } {
            let mut key: Value = keys.as_array().and_then(|__arr| match &i { Value::Int(__n) => __arr.get(*__n as usize), Value::Str(__s) => __s.parse::<usize>().ok().and_then(|__n| __arr.get(__n)), _ => None }).cloned().unwrap_or(Value::Null);
            let mut market: Value = self.safe_dict(markets.clone(), key, &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            let mut base: Value = self.safe_string_k(market.clone(), "base", &[]);
            let mut quote: Value = self.safe_string_k(market.clone(), "quote", &[]);
            let mut marketId: Value = Value::Str(format!("{}{}", Value::Str(format!("{}{}", base, Value::Str("-".into())).into()), quote).into());
            let mut marketData: Value = self.extend(market, &[Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("id".to_string(), marketId);
                m
            })]);
            append_to_array(&mut result, self.parse_market(marketData));
        }
        }
        return result;

    Value::Null
}

/*
 * @method
 * @name revolutx#parseCurrency
 * @description parses a currency from the exchange's currency data
 * @ignore
 * @param {object} currency the raw currency data from the exchange
 * @returns {object} a [currency structure]{@link https://docs.ccxt.com/?id=currency-structure}
 */
    pub fn parse_currency(&self, mut currency: Value) -> Value {
        let mut id: Value = self.safe_string2(currency.clone(), Value::Str("id".into()), Value::Str("symbol".into()), &[Value::Str("".into())]);
        let mut code: Value = self.safe_currency_code(id.clone(), &[]);
        let mut name: Value = self.safe_string_k(currency.clone(), "name", &[]);
        let mut scale: Value = self.safe_integer_k(currency.clone(), "scale", &[]);
        let mut status: Option<String> = self.safe_string_k(currency.clone(), "status", &[]).as_str().map(str::to_owned);
        let mut active: Value = (Value::Bool(status.as_deref() == Some("active")));
        let mut assetType: Option<String> = self.safe_string_k(currency.clone(), "asset_type", &[]).as_str().map(str::to_owned);
        let mut type_var: Value = (if (assetType.as_deref() == Some("crypto")) { Value::Str("crypto".into()) } else { Value::Str("fiat".into()) });
        let mut precision: Value = (if (scale != Value::Null) { crate::runtime::Math::pow(&Value::Int(10), &negate(&scale)) } else { Value::Null });
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), currency);
        m.insert("id".to_string(), id);
        m.insert("code".to_string(), code);
        m.insert("name".to_string(), name);
        m.insert("active".to_string(), active);
        m.insert("type".to_string(), type_var);
        m.insert("deposit".to_string(), Value::Null);
        m.insert("withdraw".to_string(), Value::Null);
        m.insert("precision".to_string(), self.parse_number(precision, &[]));
        m.insert("fee".to_string(), Value::Null);
        m.insert("limits".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("amount".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Null);
        m.insert("max".to_string(), Value::Null);
    m
}));
        m.insert("withdraw".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Null);
        m.insert("max".to_string(), Value::Null);
    m
}));
        m.insert("deposit".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Null);
        m.insert("max".to_string(), Value::Null);
    m
}));
    m
}));
        m.insert("networks".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
    m
});

    Value::Null
}

/*
 * @method
 * @name revolutx#fetchCurrencies
 * @description fetches all available currencies on the exchange
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.region] the region to filter currencies by
 * @returns {object} a dictionary of [currency structures]{@link https://docs.ccxt.com/?id=currency-structure}
 */
    pub async fn fetch_currencies(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut region: Value = self.safe_string2(params.clone(), Value::Str("region".into()), Value::Str("region".into()), &[self.options.as_map().and_then(|__m| __m.get("region")).cloned().unwrap_or(Value::Null)]);
        if (region != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("region".to_string(), region); }
        }
        let __ws_arg_1 = self.extend(request, &[params]);
        let mut response: Value = self.public_get10_public_configuration_currencies(&[__ws_arg_1]).await;
        //
        //     {
        //         "BTC": { "symbol": "BTC", "name": "Bitcoin", "scale": 8, "asset_type": "crypto", "status": "active" },
        //         "USD": { "symbol": "$", "name": "US Dollar", "scale": 2, "asset_type": "fiat", "status": "active" }
        //     }
        //
        let mut currencies: Value = self.safe_dict_k(response.clone(), "data", &[response.clone()]);
        let mut keys: Value = object_keys(&currencies);
        let mut result: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1076: bool = true;
            while { if !__for_first_1076 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_1076 = false; i.as_f64().unwrap_or(f64::NAN) < ((keys.len() as i64) as f64) } {
            let mut key: Value = keys.as_array().and_then(|__arr| match &i { Value::Int(__n) => __arr.get(*__n as usize), Value::Str(__s) => __s.parse::<usize>().ok().and_then(|__n| __arr.get(__n)), _ => None }).cloned().unwrap_or(Value::Null);
            let mut currency: Value = self.safe_dict(currencies.clone(), key.clone(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            let mut currencyData: Value = self.extend(currency, &[Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("id".to_string(), key);
                m
            })]);
            let mut parsed: Value = self.parse_currency(currencyData);
            let mut code: Value = self.safe_string_k(parsed.clone(), "code", &[Value::Str("".into())]);
            if (code.as_str() == Some("")) {
                continue;
            }
            add_element_to_object(&mut result, &code, parsed);
        }
        }
        return result;

    Value::Null
}

/*
 * @method
 * @name revolutx#parseTicker
 * @description parses a ticker from the exchange's ticker data
 * @ignore
 * @param {object} ticker the raw ticker data from the exchange
 * @param {object} [market] the market the ticker is for
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
    pub fn parse_ticker(&self, mut ticker: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        let mut tickerSymbol: Value = self.safe_string_k(ticker.clone(), "symbol", &[]);
        let mut symbol: Value = self.safe_symbol(tickerSymbol, &[market.clone(), Value::Str("/".into())]);
        let mut bid: Value = self.safe_string_k(ticker.clone(), "bid", &[]);
        let mut ask: Value = self.safe_string_k(ticker.clone(), "ask", &[]);
        let mut last: Value = self.safe_string_k(ticker.clone(), "last_price", &[]);
        let mut high: Value = self.safe_string_k(ticker.clone(), "high_24h", &[]);
        let mut low: Value = self.safe_string_k(ticker.clone(), "low_24h", &[]);
        let mut priceChange: Value = self.safe_string_k(ticker.clone(), "price_change_24h", &[]);
        let mut baseVolume: Value = self.safe_string_k(ticker.clone(), "volume_24h", &[]);
        let mut timestamp: Value = self.safe_integer_k(ticker.clone(), "timestamp", &[]);
        let mut open: Value = Value::Null;
        if (last != Value::Null) && (priceChange != Value::Null) {
            open = crate::precise::Precise::stringSub(&last, &priceChange);
        }
        let mut percentage: Value = Value::Null;
        if (open != Value::Null) && (priceChange != Value::Null) {
            let mut percentageString: Value = crate::precise::Precise::stringDivPrec(&priceChange, &open, &Value::Int(8));
            percentage = self.parse_number(crate::precise::Precise::stringMul(&percentageString, &Value::Str("100".into())), &[]);
        }
        return self.safe_ticker(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("symbol".to_string(), symbol);
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp));
        m.insert("high".to_string(), high);
        m.insert("low".to_string(), low);
        m.insert("bid".to_string(), bid);
        m.insert("bidVolume".to_string(), Value::Null);
        m.insert("ask".to_string(), ask);
        m.insert("askVolume".to_string(), Value::Null);
        m.insert("vwap".to_string(), Value::Null);
        m.insert("open".to_string(), open);
        m.insert("close".to_string(), last.clone());
        m.insert("last".to_string(), last);
        m.insert("previousClose".to_string(), Value::Null);
        m.insert("change".to_string(), priceChange);
        m.insert("percentage".to_string(), percentage);
        m.insert("average".to_string(), Value::Null);
        m.insert("baseVolume".to_string(), baseVolume);
        m.insert("quoteVolume".to_string(), Value::Null);
        m.insert("info".to_string(), ticker);
    m
}), &[market]);

    Value::Null
}

/*
 * @method
 * @name revolutx#fetchTickers
 * @description fetches price tickers for multiple markets, statistical information calculated over the past 24 hours for each market
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data
 * @param {string[]|undefined} symbols unified symbols of the markets to fetch the ticker for, all market tickers are returned if not assigned
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.region] the region to fetch tickers for (e.g. EEA, UK)
 * @returns {object} a dictionary of [ticker structures]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
    pub async fn fetch_tickers(&mut self, optional_args: &[Value]) -> Value {
        let mut symbols = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        if (symbols != Value::Null) {
            let mut marketIds: Value = Value::from(vec![]);
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_1077: bool = true;
                while { if !__for_first_1077 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_1077 = false; i.as_f64().unwrap_or(f64::NAN) < ((symbols.len() as i64) as f64) } {
                let mut symbol: Value = symbols.as_array().and_then(|__arr| match &i { Value::Int(__n) => __arr.get(*__n as usize), Value::Str(__s) => __s.parse::<usize>().ok().and_then(|__n| __arr.get(__n)), _ => None }).cloned().unwrap_or(Value::Null);
                let mut market: Value = self.market(symbol.clone());
                append_to_array(&mut marketIds, market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
            }
            }
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("symbols".to_string(), join(&marketIds, &Value::Str(",".into()))); }
        }
        let mut region: Value = self.safe_string2(params.clone(), Value::Str("region".into()), Value::Str("region".into()), &[self.options.as_map().and_then(|__m| __m.get("region")).cloned().unwrap_or(Value::Null)]);
        if (region != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("region".to_string(), region); }
        }
        let __ws_arg_2 = self.extend(request, &[params]);
        let mut response: Value = self.public_get10_public_tickers(&[__ws_arg_2]).await;
        //
        //     {
        //         "data": [
        //             { "symbol": "BTC/USD", "bid": "119950.00", "ask": "120050.00", "mid": "120000.00",
        //               "last_price": "119980.00", "low_24h": "115000.00", "high_24h": "122500.00",
        //               "price_change_24h": "2480.00", "volume_24h": "135.42000000", "region": "EEA" }
        //         ],
        //         "metadata": { "timestamp": 1785313433816 }
        //     }
        //
        let mut data: Value = self.safe_list_k(response.clone(), "data", &[Value::from(vec![])]);
        let mut metadata: Value = self.safe_dict_k(response, "metadata", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut timestamp: Value = self.safe_integer_k(metadata, "timestamp", &[]);
        let mut result: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1078: bool = true;
            while { if !__for_first_1078 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_1078 = false; i.as_f64().unwrap_or(f64::NAN) < ((data.len() as i64) as f64) } {
            let mut tickerData: Value = self.safe_dict(data.clone(), i.clone(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            add_element_to_object(&mut tickerData, &Value::Str("timestamp".into()), timestamp.clone());
            let mut ticker: Value = self.parse_ticker(tickerData, &[]);
            let mut symbol: Value = self.safe_string_k(ticker.clone(), "symbol", &[Value::Str("".into())]);
            if (symbol.as_str() == Some("")) {
                continue;
            }
            add_element_to_object(&mut result, &symbol, ticker);
        }
        }
        if (symbols != Value::Null) {
            let mut filtered: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                m
            });
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_1079: bool = true;
                while { if !__for_first_1079 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_1079 = false; i.as_f64().unwrap_or(f64::NAN) < ((symbols.len() as i64) as f64) } {
                let mut s: Value = symbols.as_array().and_then(|__arr| match &i { Value::Int(__n) => __arr.get(*__n as usize), Value::Str(__s) => __s.parse::<usize>().ok().and_then(|__n| __arr.get(__n)), _ => None }).cloned().unwrap_or(Value::Null);
                if (in_op(&result, &s)) {
                    add_element_to_object(&mut filtered, &s, get_value(&result, &s));
                }
            }
            }
            return filtered;
        }
        return result;

    Value::Null
}

/*
 * @method
 * @name revolutx#fetchTicker
 * @description fetches a price ticker for a given market symbol
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data
 * @param {string} symbol unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.region] the region to fetch the ticker for
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
    pub async fn fetch_ticker(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut tickers: Value = self.fetch_tickers(&[Value::from(vec![symbol.clone()]), params]).await;
        let mut ticker: Value = self.safe_dict(tickers, symbol.clone(), &[]);
        if (ticker == Value::Null) {
            panic!("{}", crate::exchange_errors::exchange_error(format!("{}{}", Value::Str(format!("{}{}", self.id.clone(), Value::Str(" fetchTicker() could not find ticker for symbol ".into())).into()), symbol)));
        }
        return ticker;

    Value::Null
}

/*
 * @method
 * @name revolutx#fetchOrderBook
 * @description fetches the current order book snapshot for a given market symbol
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data
 * @param {string} symbol unified symbol of the market to fetch the order book for
 * @param {int} [limit] the maximum number of orders to return (1-50, default 50)
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.region] the region to fetch the order book for
 * @returns {object} an [order book structure]{@link https://docs.ccxt.com/?id=order-book-structure}
 */
    pub async fn fetch_order_book(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut limit = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
            m
        });
        if (limit != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("limit".to_string(), limit); }
        }
        let mut region: Value = self.safe_string2(params.clone(), Value::Str("region".into()), Value::Str("region".into()), &[self.options.as_map().and_then(|__m| __m.get("region")).cloned().unwrap_or(Value::Null)]);
        if (region != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("region".to_string(), region); }
        }
        let __ws_arg_3 = self.extend(request, &[params]);
        let mut response: Value = self.public_get20_public_order_book_symbol(&[__ws_arg_3]).await;
        //
        //     {
        //         "data": {
        //             "asks": [ { "price": "4005.00", "quantity": "1.7000", "count": 3 } ],
        //             "bids": [ { "price": "4000.00", "quantity": "0.25", "count": 1 } ]
        //         },
        //         "metadata": { "region": "UK", "timestamp": 1785313433816 }
        //     }
        //
        let mut data: Value = self.safe_dict_k(response.clone(), "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut metadata: Value = self.safe_dict_k(response, "metadata", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut timestamp: Value = self.safe_integer_k(metadata, "timestamp", &[]);
        return self.parse_order_book(data, symbol, &[timestamp, Value::Str("bids".into()), Value::Str("asks".into()), Value::Str("price".into()), Value::Str("quantity".into())]);

    Value::Null
}

/*
 * @method
 * @name revolutx#parseOHLCV
 * @description parses an OHLCV candle from the exchange's candle data
 * @ignore
 * @param {object} ohlcv the raw candle data from the exchange
 * @param {object} [market] the market the candle is for
 * @returns {int[]} an [OHLCV structure]{@link https://docs.ccxt.com/?id=ohlcv-structure}
 */
    pub fn parse_ohlcv(&self, mut ohlcv: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        let mut timestamp: Value = self.safe_integer_k(ohlcv.clone(), "start", &[]);
        let mut open: Value = self.safe_number_k(ohlcv.clone(), "open", &[]);
        let mut high: Value = self.safe_number_k(ohlcv.clone(), "high", &[]);
        let mut low: Value = self.safe_number_k(ohlcv.clone(), "low", &[]);
        let mut close: Value = self.safe_number_k(ohlcv.clone(), "close", &[]);
        let mut volume: Value = self.safe_number_k(ohlcv, "volume", &[]);
        return Value::from(vec![timestamp, open, high, low, close, volume]);

    Value::Null
}

/*
 * @method
 * @name revolutx#fetchOHLCV
 * @description fetches historical candlestick data for a given market symbol
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data
 * @param {string} symbol unified symbol of the market to fetch OHLCV data for
 * @param {string} timeframe the length of time each candle represents (e.g. 1m, 5m, 1h, 1d)
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum number of candles to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] timestamp in ms of the latest candle to fetch
 * @param {string} [params.region] the region to fetch candles for
 * @returns {int[][]} a list of [OHLCV structures]{@link https://docs.ccxt.com/?id=ohlcv-structure}
 */
    pub async fn fetch_ohlcv(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut timeframe = get_arg(optional_args, 0, Value::Str("1m".into()));
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
                m.insert("interval".to_string(), self.safe_integer(self.timeframes.clone(), timeframe.clone(), &[Value::Int(5)]));
            m
        });
        if (since != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("since".to_string(), since.clone()); }
        }
        let mut until: Value = self.safe_integer2(params.clone(), Value::Str("until".into()), Value::Str("until".into()), &[]);
        if (until != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("until".to_string(), until); }
        }  else {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("until".to_string(), self.milliseconds()); }
        }
        let mut region: Value = self.safe_string2(params.clone(), Value::Str("region".into()), Value::Str("region".into()), &[self.options.as_map().and_then(|__m| __m.get("region")).cloned().unwrap_or(Value::Null)]);
        if (region != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("region".to_string(), region); }
        }
        let __ws_arg_4 = self.extend(request, &[params]);
        let mut response: Value = self.public_get10_public_candles_symbol(&[__ws_arg_4]).await;
        //
        //     {
        //         "data": [
        //             { "start": 1785309833816, "open": "119800.00", "high": "120100.00",
        //               "low": "119700.00", "close": "120000.00", "volume": "0.25000000" }
        //         ],
        //         "metadata": { "region": "EEA", "timestamp": 1785313433816 }
        //     }
        //
        let mut data: Value = self.safe_list_k(response, "data", &[Value::from(vec![])]);
        return self.parse_ohlc_vs(data, &[market, timeframe, since, limit]);

    Value::Null
}

/*
 * @method
 * @name revolutx#parseTrade
 * @description parses a trade from the exchange's public trade data
 * @ignore
 * @param {object} trade the raw trade data from the exchange
 * @param {object} [market] the market the trade was executed in
 * @returns {object} a [trade structure]{@link https://docs.ccxt.com/?id=trade-structure}
 */
    pub fn parse_trade(&self, mut trade: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        let mut id: Value = self.safe_string_k(trade.clone(), "id", &[]);
        let mut tradeSymbol: Value = self.safe_string_k(trade.clone(), "symbol", &[]);
        let mut symbol: Value = self.safe_symbol(tradeSymbol, &[market, Value::Str("/".into())]);
        let mut price: Value = self.safe_number_k(trade.clone(), "price", &[]);
        let mut amount: Value = self.safe_number_k(trade.clone(), "quantity", &[]);
        let mut side: Value = self.safe_string_lower(trade.clone(), Value::Str("side".into()), &[]);
        let mut timestamp: Value = self.safe_integer_k(trade.clone(), "timestamp", &[]);
        let mut cost: Value = Value::Null;
        if (price != Value::Null) && (amount != Value::Null) {
            cost = (match (&(price), &(amount)) { (Value::Int(x), Value::Int(y)) => Value::Int(x * y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 * *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x * *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x * y), _ => Value::Null });
        }
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), trade);
        m.insert("id".to_string(), id);
        m.insert("order".to_string(), Value::Null);
        m.insert("symbol".to_string(), symbol);
        m.insert("side".to_string(), side);
        m.insert("type".to_string(), Value::Null);
        m.insert("takerOrMaker".to_string(), Value::Null);
        m.insert("price".to_string(), price);
        m.insert("amount".to_string(), amount);
        m.insert("cost".to_string(), cost);
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp));
        m.insert("fee".to_string(), Value::Null);
        m.insert("fees".to_string(), Value::from(vec![]));
    m
});

    Value::Null
}

/*
 * @method
 * @name revolutx#fetchTrades
 * @description fetches the public trade history for a given market symbol
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data
 * @param {string} symbol unified symbol of the market to fetch trades for
 * @param {int} [since] timestamp in ms of the earliest trade to fetch
 * @param {int} [limit] the maximum number of trades to return (1-1900, default 1900)
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] timestamp in ms of the latest trade to fetch
 * @param {string} [params.cursor] pagination cursor from the previous response
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=trade-structure}
 */
    pub async fn fetch_trades(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut since = get_arg(optional_args, 0, Value::Null);
        let mut limit = get_arg(optional_args, 1, Value::Null);
        let mut params = get_arg(optional_args, 2, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = Value::Null;
        if (symbol != Value::Null) {
            market = self.market(symbol.clone());
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        if (market != Value::Null) {
            if let Value::Dict(__d12) = &mut request { std::sync::Arc::make_mut(__d12).insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null)); }
        }
        if (since != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("start_date".to_string(), since.clone()); }
        }
        let mut until: Value = self.safe_integer2(params.clone(), Value::Str("until".into()), Value::Str("until".into()), &[]);
        if (until != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("end_date".to_string(), until); }
        }  else if (since != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("end_date".to_string(), self.milliseconds()); }
        }
        if (limit != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("limit".to_string(), crate::runtime::Math::min(&limit, &Value::Int(1900))); }
        }
        let mut cursor: Value = self.safe_string_k(params.clone(), "cursor", &[]);
        if (cursor != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("cursor".to_string(), cursor); }
        }
        let __ws_arg_5 = self.extend(request, &[params]);
        let mut response: Value = self.public_get10_public_trades_all(&[__ws_arg_5]).await;
        //
        //     {
        //         "data": [
        //             { "id": "3b2b202b-7668-43cf-a6c8-b3354e7f4c52", "symbol": "BTC/USD",
        //               "price": "119980.00", "quantity": "0.01000000", "side": "sell", "timestamp": 1785313433816 }
        //         ],
        //         "metadata": { "timestamp": 1785313433816, "next_cursor": "..." }
        //     }
        //
        let mut data: Value = self.safe_list_k(response, "data", &[Value::from(vec![])]);
        let mut result: Value = Value::from(vec![]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1080: bool = true;
            while { if !__for_first_1080 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_1080 = false; i.as_f64().unwrap_or(f64::NAN) < ((data.len() as i64) as f64) } {
            let mut trade: Value = self.safe_dict(data.clone(), i.clone(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            append_to_array(&mut result, self.parse_trade(trade, &[market.clone()]));
        }
        }
        return self.filter_by_symbol_since_limit(self.sort_by(result, Value::Str("timestamp".into()), &[]), &[symbol, since, limit]);

    Value::Null
}

/*
 * @method
 * @name revolutx#fetchBalance
 * @description fetches the current balance for the authenticated user
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [balance structure]{@link https://docs.ccxt.com/?id=balance-structure}
 */
    pub async fn fetch_balance(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut response: Value = self.private_get10_balances(&[params]).await;
        //
        //     [
        //         { "currency": "BTC", "available": "1.25000000", "reserved": "0.10000000", "total": "1.35000000" },
        //         { "currency": "USD", "available": "50000.00", "reserved": "1000.00", "total": "51000.00", "staked": "32.00000000" }
        //     ]
        //
        let mut data: Value = (if (matches!(&response, Value::Arr(_))) { response.clone() } else { self.safe_list_k(response.clone(), "data", &[Value::from(vec![])]) });
        let mut result: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("info".to_string(), response);
            m
        });
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1081: bool = true;
            while { if !__for_first_1081 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_1081 = false; i.as_f64().unwrap_or(f64::NAN) < ((data.len() as i64) as f64) } {
            let mut balance: Value = self.safe_dict(data.clone(), i.clone(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            let mut currency: Value = self.safe_string_k(balance.clone(), "currency", &[]);
            let mut code: Value = self.safe_currency_code(currency, &[]);
            if (code == Value::Null) {
                continue;
            }
            let mut account: Value = self.account();
            if let Value::Dict(__d) = &mut account { std::sync::Arc::make_mut(__d).insert("free".to_string(), self.safe_string_k(balance.clone(), "available", &[])); }
            let mut reserved: Value = self.safe_string_k(balance.clone(), "reserved", &[]);
            let mut staked: Value = self.safe_string_k(balance.clone(), "staked", &[]);
            let mut used: Value = reserved.clone();
            if (staked != Value::Null) {
                used = (if (reserved == Value::Null) { staked.clone() } else { crate::precise::Precise::stringAdd(&reserved, &staked) });
            }
            if let Value::Dict(__d) = &mut account { std::sync::Arc::make_mut(__d).insert("used".to_string(), used); }
            if let Value::Dict(__d) = &mut account { std::sync::Arc::make_mut(__d).insert("total".to_string(), self.safe_string_k(balance, "total", &[])); }
            add_element_to_object(&mut result, &code, account);
        }
        }
        return self.safe_balance(result);

    Value::Null
}

/*
 * @method
 * @name revolutx#parseOrderStatus
 * @description parses the order status from the exchange's status to the unified ccxt status
 * @ignore
 * @param {string} status the exchange-specific order status
 * @returns {string|undefined} the unified order status
 */
    pub fn parse_order_status(&self, mut status: Value) -> Option<String> {
        let mut statuses: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("pending_new".to_string(), Value::Str("open".into()));
                m.insert("new".to_string(), Value::Str("open".into()));
                m.insert("partially_filled".to_string(), Value::Str("open".into()));
                m.insert("filled".to_string(), Value::Str("closed".into()));
                m.insert("cancelled".to_string(), Value::Str("canceled".into()));
                m.insert("canceled".to_string(), Value::Str("canceled".into()));
                m.insert("rejected".to_string(), Value::Str("rejected".into()));
                m.insert("replaced".to_string(), Value::Str("open".into()));
            m
        });
        return self.safe_string(statuses, status.clone(), &[status.clone()]).as_str().map(str::to_owned);
}

/*
 * @method
 * @name revolutx#parseOrder
 * @description parses an order from the exchange's order data
 * @ignore
 * @param {object} order the raw order data from the exchange
 * @param {object} [market] the market the order was placed in
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub fn parse_order(&self, mut order: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        let mut orderId: Value = self.safe_string2(order.clone(), Value::Str("id".into()), Value::Str("venue_order_id".into()), &[]);
        let mut clientOrderId: Value = self.safe_string_k(order.clone(), "client_order_id", &[]);
        let mut orderSymbol: Value = self.safe_string_k(order.clone(), "symbol", &[]);
        let mut symbol: Value = self.safe_symbol(orderSymbol, &[market.clone(), Value::Str("/".into())]);
        let mut side: Value = self.safe_string_lower(order.clone(), Value::Str("side".into()), &[]);
        let mut orderType: Value = self.safe_string_lower(order.clone(), Value::Str("type".into()), &[]);
        let mut quantity: Value = self.safe_string_k(order.clone(), "quantity", &[]);
        let mut filledQuantity: Value = self.safe_string_k(order.clone(), "filled_quantity", &[]);
        let mut leavesQuantity: Value = self.safe_string_k(order.clone(), "leaves_quantity", &[]);
        let mut price: Value = self.safe_string_k(order.clone(), "price", &[]);
        let mut averageFillPrice: Value = self.safe_string_k(order.clone(), "average_fill_price", &[]);
        let mut amount: Value = self.safe_string_k(order.clone(), "amount", &[]);
        let mut filledAmount: Value = self.safe_string_k(order.clone(), "filled_amount", &[]);
        let mut totalFee: Value = self.safe_string_k(order.clone(), "total_fee", &[]);
        let mut feeCurrency: Value = self.safe_string_k(order.clone(), "fee_currency", &[]);
        let mut status: Value = self.parse_order_status(self.safe_string_k(order.clone(), "status", &[])).map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null);
        let mut timeInForce: Value = self.safe_string_upper(order.clone(), Value::Str("time_in_force".into()), &[]);
        let mut createdDate: Value = self.safe_integer_k(order.clone(), "created_date", &[]);
        let mut updatedDate: Value = self.safe_integer_k(order.clone(), "updated_date", &[]);
        let mut fee: Value = Value::Null;
        if (totalFee != Value::Null) {
            fee = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("cost".to_string(), self.parse_number(totalFee, &[]));
                    m.insert("currency".to_string(), feeCurrency);
                m
            });
        }
        let mut amountValue: Value = Value::Null;
        if (quantity != Value::Null) {
            amountValue = quantity;
        }  else if (amount != Value::Null) {
            amountValue = amount;
        }
        let mut filledValue: Value = Value::Null;
        if (filledQuantity != Value::Null) {
            filledValue = filledQuantity;
        }  else if (filledAmount != Value::Null) {
            filledValue = filledAmount;
        }
        let mut remainingValue: Value = Value::Null;
        if (leavesQuantity != Value::Null) {
            remainingValue = leavesQuantity;
        }
        return self.safe_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), orderId);
        m.insert("clientOrderId".to_string(), clientOrderId);
        m.insert("symbol".to_string(), symbol);
        m.insert("side".to_string(), side);
        m.insert("type".to_string(), orderType);
        m.insert("price".to_string(), self.parse_number(price, &[]));
        m.insert("average".to_string(), self.parse_number(averageFillPrice, &[]));
        m.insert("amount".to_string(), self.parse_number(amountValue, &[]));
        m.insert("filled".to_string(), self.parse_number(filledValue, &[]));
        m.insert("remaining".to_string(), self.parse_number(remainingValue, &[]));
        m.insert("status".to_string(), status);
        m.insert("timeInForce".to_string(), timeInForce);
        m.insert("timestamp".to_string(), createdDate.clone());
        m.insert("datetime".to_string(), self.iso8601(createdDate));
        m.insert("lastUpdateTimestamp".to_string(), updatedDate);
        m.insert("fee".to_string(), fee);
        m.insert("info".to_string(), order);
    m
}), &[market]);

    Value::Null
}

/*
 * @method
 * @name revolutx#createOrder
 * @description create a trade order
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-trading
 * @param {string} symbol unified symbol of the market to create an order in
 * @param {string} type 'limit' or 'market'
 * @param {string} side 'buy' or 'sell'
 * @param {float} amount how much of the base currency you want to trade
 * @param {float} [price] the price at which the order is to be fulfilled, in units of the quote currency, ignored in market orders
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.clientOrderId] a custom client order id (UUID)
 * @param {float} [params.cost] the order cost in units of the quote currency (alternative to amount)
 * @param {string} [params.timeInForce] 'gtc' or 'ioc' for limit orders
 * @param {string[]} [params.executionInstructions] limit order instructions, e.g. ['post_only'] or ['allow_taker']
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn create_order(&mut self, mut symbol: Value, mut type_var: Value, mut side: Value, mut amount: Value, optional_args: &[Value]) -> Value {
        let mut price = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol.clone());
        let mut clientOrderId: Value = self.safe_string2(params.clone(), Value::Str("clientOrderId".into()), Value::Str("client_order_id".into()), &[self.uuid(&[])]);
        let mut cost: Value = self.safe_string2(params.clone(), Value::Str("cost".into()), Value::Str("quote_size".into()), &[]);
        let mut timeInForce: Value = self.safe_string_lower2(params.clone(), Value::Str("timeInForce".into()), Value::Str("time_in_force".into()), &[]);
        let mut executionInstructions: Value = self.safe_list_k(params.clone(), "executionInstructions", &[self.safe_list(params.clone(), Value::Str("execution_instructions".into()), &[])]);
        let mut orderConfiguration: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        if (type_var.as_str() == Some("limit")) {
            let mut limitConfig: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                m
            });
            if (cost != Value::Null) {
                if let Value::Dict(__d) = &mut limitConfig { std::sync::Arc::make_mut(__d).insert("quote_size".to_string(), self.cost_to_precision(symbol.clone(), cost.clone())); }
            }  else {
                if let Value::Dict(__d) = &mut limitConfig { std::sync::Arc::make_mut(__d).insert("base_size".to_string(), self.amount_to_precision(symbol.clone(), amount.clone())); }
            }
            if let Value::Dict(__d) = &mut limitConfig { std::sync::Arc::make_mut(__d).insert("price".to_string(), self.price_to_precision(symbol.clone(), price)); }
            if (timeInForce != Value::Null) {
                if let Value::Dict(__d) = &mut limitConfig { std::sync::Arc::make_mut(__d).insert("time_in_force".to_string(), timeInForce.clone()); }
            }
            if (executionInstructions != Value::Null) {
                if let Value::Dict(__d) = &mut limitConfig { std::sync::Arc::make_mut(__d).insert("execution_instructions".to_string(), executionInstructions.clone()); }
            }
            if let Value::Dict(__d) = &mut orderConfiguration { std::sync::Arc::make_mut(__d).insert("limit".to_string(), limitConfig); }
        }  else if (type_var.as_str() == Some("market")) {
            if (timeInForce != Value::Null) {
                panic!("{}", crate::exchange_errors::invalid_order(format!("{}{}", self.id.clone(), Value::Str(" createOrder() timeInForce is only supported for limit orders".into()))));
            }
            if (executionInstructions != Value::Null) {
                panic!("{}", crate::exchange_errors::invalid_order(format!("{}{}", self.id.clone(), Value::Str(" createOrder() executionInstructions are only supported for limit orders".into()))));
            }
            let mut marketConfig: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                m
            });
            if (cost != Value::Null) {
                if let Value::Dict(__d) = &mut marketConfig { std::sync::Arc::make_mut(__d).insert("quote_size".to_string(), self.cost_to_precision(symbol.clone(), cost)); }
            }  else {
                if let Value::Dict(__d) = &mut marketConfig { std::sync::Arc::make_mut(__d).insert("base_size".to_string(), self.amount_to_precision(symbol, amount)); }
            }
            if let Value::Dict(__d) = &mut orderConfiguration { std::sync::Arc::make_mut(__d).insert("market".to_string(), marketConfig); }
        }  else {
            panic!("{}", crate::exchange_errors::invalid_order(format!("{}{}", Value::Str(format!("{}{}", self.id.clone(), Value::Str(" createOrder() does not support order type ".into())).into()), type_var)));
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("client_order_id".to_string(), clientOrderId);
                m.insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
                m.insert("side".to_string(), side.clone());
                m.insert("order_configuration".to_string(), orderConfiguration);
            m
        });
        let __ws_arg_6 = self.extend(request, &[self.omit(params, Value::from(vec![Value::Str("cost".into()), Value::Str("quote_size".into()), Value::Str("clientOrderId".into()), Value::Str("client_order_id".into()), Value::Str("timeInForce".into()), Value::Str("time_in_force".into()), Value::Str("executionInstructions".into()), Value::Str("execution_instructions".into())]), &[])]);
        let mut response: Value = self.private_post10_orders(&[__ws_arg_6]).await;
        //
        //     {
        //         "data": [
        //             { "venue_order_id": "7a52e92e-8639-4fe1-abaa-68d3a2d5234b", "client_order_id": "...", "state": "new" }
        //         ]
        //     }
        //
        let mut data: Value = self.safe_value_k(response.clone(), "data", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut orderData: Value = (if (matches!(&data, Value::Arr(_))) { self.safe_dict(data.clone(), Value::Int(0), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]) } else { self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]) });
        let mut venueOrderId: Value = self.safe_string_k(orderData.clone(), "venue_order_id", &[]);
        let mut state: Value = self.safe_string_k(orderData.clone(), "state", &[]);
        let __ws_arg_7 = self.extend(orderData, &[Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), venueOrderId);
        m.insert("symbol".to_string(), crate::value::get_value_k(&market, "id"));
        m.insert("status".to_string(), state);
        m.insert("side".to_string(), side);
        m.insert("type".to_string(), type_var);
    m
})]);
        let mut order: Value = self.parse_order(__ws_arg_7, &[market]);
        return order;

    Value::Null
}

/*
 * @method
 * @name revolutx#cancelOrder
 * @description cancels an open order by its id
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-trading
 * @param {string} id the order id (venue order id)
 * @param {string} symbol not used by this exchange
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn cancel_order(&mut self, mut id: Value, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("venue_order_id".to_string(), id.clone());
            m
        });
        let __ws_arg_8 = self.extend(request, &[params]);
        let mut response: Value = self.private_delete10_orders_venue_order_id(&[__ws_arg_8]).await;
        return self.safe_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), response);
        m.insert("id".to_string(), id);
        m.insert("status".to_string(), Value::Str("canceled".into()));
    m
}), &[]);

    Value::Null
}

/*
 * @method
 * @name revolutx#cancelAllOrders
 * @description cancels all open orders
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-trading
 * @param {string} [symbol] not used by this exchange
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an empty [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn cancel_all_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        self.private_delete10_orders(&[params]).await;
        return Value::from(vec![]);

    Value::Null
}

/*
 * @method
 * @name revolutx#fetchOrder
 * @description fetches an order by its id
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data
 * @param {string} id the order id (venue order id)
 * @param {string} symbol unified symbol of the market the order was made in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn fetch_order(&mut self, mut id: Value, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("venue_order_id".to_string(), id);
            m
        });
        let __ws_arg_9 = self.extend(request, &[params]);
        let mut response: Value = self.private_get10_orders_venue_order_id(&[__ws_arg_9]).await;
        //
        //     {
        //         "data": {
        //             "id": "uuid", "client_order_id": "uuid", "symbol": "BTC/USD",
        //             "side": "buy", "type": "limit", "quantity": "0.002", "filled_quantity": "0",
        //             "leaves_quantity": "0.002", "amount": "240.00", "filled_amount": "0",
        //             "price": "120000.00", "average_fill_price": "0", "total_fee": "0",
        //             "fee_currency": "USD", "status": "new", "time_in_force": "gtc",
        //             "execution_instructions": ["allow_taker"],
        //             "created_date": 1785309833816, "updated_date": 1785313433816
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut market: Value = Value::Null;
        if (symbol != Value::Null) {
            market = self.market(symbol);
        }
        return self.parse_order(data, &[market]);

    Value::Null
}

/*
 * @method
 * @name revolutx#fetchOpenOrders
 * @description fetches all open orders for the authenticated user
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data
 * @param {string|undefined} symbol unified symbol of the market to fetch open orders for
 * @param {int} [since] timestamp in ms of the earliest order to fetch
 * @param {int} [limit] the maximum number of orders to return (1-300, default 300)
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.cursor] pagination cursor from the previous response
 * @param {string[]} [params.orderStates] filter by order states, e.g. ['new', 'partially_filled']
 * @param {string[]} [params.orderTypes] filter by order types, e.g. ['limit', 'market']
 * @param {string} [params.side] filter by side, 'buy' or 'sell'
 * @returns {object[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn fetch_open_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        if (symbol != Value::Null) {
            let mut market: Value = self.market(symbol.clone());
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("symbols".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null)); }
        }
        if (limit != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("limit".to_string(), limit.clone()); }
        }
        let mut cursor: Value = self.safe_string_k(params.clone(), "cursor", &[]);
        if (cursor != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("cursor".to_string(), cursor); }
        }
        let mut orderStates: Value = self.safe_list2(params.clone(), Value::Str("orderStates".into()), Value::Str("order_states".into()), &[]);
        if (orderStates != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("order_states".to_string(), join(&orderStates, &Value::Str(",".into()))); }
        }
        let mut orderTypes: Value = self.safe_list2(params.clone(), Value::Str("orderTypes".into()), Value::Str("order_types".into()), &[]);
        if (orderTypes != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("order_types".to_string(), join(&orderTypes, &Value::Str(",".into()))); }
        }
        let mut side: Value = self.safe_string_k(params.clone(), "side", &[]);
        if (side != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("side".to_string(), side); }
        }
        let __ws_arg_10 = self.extend(request, &[self.omit(params, Value::from(vec![Value::Str("cursor".into()), Value::Str("orderStates".into()), Value::Str("order_states".into()), Value::Str("orderTypes".into()), Value::Str("order_types".into()), Value::Str("side".into())]), &[])]);
        let mut response: Value = self.private_get10_orders_active(&[__ws_arg_10]).await;
        //
        //     {
        //         "data": [ { "id": "uuid", "client_order_id": "uuid", "symbol": "BTC/USD", ... } ],
        //         "metadata": { "timestamp": 1785313433816, "next_cursor": "..." }
        //     }
        //
        let mut data: Value = self.safe_list_k(response, "data", &[Value::from(vec![])]);
        let mut result: Value = Value::from(vec![]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1082: bool = true;
            while { if !__for_first_1082 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_1082 = false; i.as_f64().unwrap_or(f64::NAN) < ((data.len() as i64) as f64) } {
            let mut order: Value = self.safe_dict(data.clone(), i.clone(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            append_to_array(&mut result, self.parse_order(order, &[]));
        }
        }
        return self.filter_by_symbol_since_limit(result, &[symbol, since, limit]);

    Value::Null
}

/*
 * @method
 * @name revolutx#fetchOrders
 * @description fetches historical orders for the authenticated user
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data
 * @param {string|undefined} symbol unified symbol of the market to fetch orders for
 * @param {int} [since] timestamp in ms of the earliest order to fetch, the lookup window is limited to 30 days
 * @param {int} [limit] the maximum number of orders to return (1-1900, default 1900)
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] timestamp in ms of the latest order to fetch
 * @param {string} [params.cursor] pagination cursor from the previous response
 * @param {string[]} [params.orderStates] filter by order states, e.g. ['filled', 'cancelled', 'rejected']
 * @param {string[]} [params.orderTypes] filter by order types, e.g. ['limit', 'market']
 * @returns {object[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn fetch_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        if (symbol != Value::Null) {
            let mut market: Value = self.market(symbol.clone());
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("symbols".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null)); }
        }
        let mut thirtyDays: Value = Value::Int(2592000000);
        let mut until: Value = self.safe_integer2(params.clone(), Value::Str("until".into()), Value::Str("until".into()), &[]);
        if (since != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("start_date".to_string(), since.clone()); }
        }  else if (until != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("start_date".to_string(), (match (&(until), &(thirtyDays)) { (Value::Int(x), Value::Int(y)) => Value::Int(x - y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 - *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x - *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x - y), _ => Value::Null })); }
        }
        if (until != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("end_date".to_string(), until); }
        }  else if (since != Value::Null) {
            let mut now: Value = self.milliseconds();
            let mut defaultEnd: Value = (match (&(since), &(thirtyDays)) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null });
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("end_date".to_string(), (if (defaultEnd.as_f64().unwrap_or(f64::NAN) < now.as_f64().unwrap_or(f64::NAN)) { defaultEnd } else { now })); }
        }
        if (limit != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("limit".to_string(), limit.clone()); }
        }
        let mut cursor: Value = self.safe_string_k(params.clone(), "cursor", &[]);
        if (cursor != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("cursor".to_string(), cursor); }
        }
        let mut orderStates: Value = self.safe_list2(params.clone(), Value::Str("orderStates".into()), Value::Str("order_states".into()), &[]);
        if (orderStates != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("order_states".to_string(), join(&orderStates, &Value::Str(",".into()))); }
        }
        let mut orderTypes: Value = self.safe_list2(params.clone(), Value::Str("orderTypes".into()), Value::Str("order_types".into()), &[]);
        if (orderTypes != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("order_types".to_string(), join(&orderTypes, &Value::Str(",".into()))); }
        }
        let __ws_arg_11 = self.extend(request, &[self.omit(params, Value::from(vec![Value::Str("until".into()), Value::Str("cursor".into()), Value::Str("orderStates".into()), Value::Str("order_states".into()), Value::Str("orderTypes".into()), Value::Str("order_types".into())]), &[])]);
        let mut response: Value = self.private_get10_orders_historical(&[__ws_arg_11]).await;
        let mut data: Value = self.safe_list_k(response, "data", &[Value::from(vec![])]);
        let mut result: Value = Value::from(vec![]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1083: bool = true;
            while { if !__for_first_1083 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_1083 = false; i.as_f64().unwrap_or(f64::NAN) < ((data.len() as i64) as f64) } {
            let mut order: Value = self.safe_dict(data.clone(), i.clone(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            append_to_array(&mut result, self.parse_order(order, &[]));
        }
        }
        return self.filter_by_symbol_since_limit(result, &[symbol, since, limit]);

    Value::Null
}

/*
 * @method
 * @name revolutx#fetchClosedOrders
 * @description fetches closed (filled, cancelled, rejected) orders for the authenticated user
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data
 * @param {string|undefined} symbol unified symbol of the market to fetch orders for
 * @param {int} [since] timestamp in ms of the earliest order to fetch, the lookup window is limited to 30 days
 * @param {int} [limit] the maximum number of orders to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn fetch_closed_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut orderStates: Value = self.safe_list2(params.clone(), Value::Str("orderStates".into()), Value::Str("order_states".into()), &[Value::from(vec![Value::Str("filled".into()), Value::Str("cancelled".into()), Value::Str("rejected".into()), Value::Str("replaced".into())])]);
        let __ws_arg_12 = self.omit(params, Value::from(vec![Value::Str("orderStates".into()), Value::Str("order_states".into())]), &[]);
        let mut requestParams: Value = self.extend(__ws_arg_12, &[Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("order_states".to_string(), orderStates);
            m
        })]);
        return self.fetch_orders(&[symbol, since, limit, requestParams]).await;

    Value::Null
}

/*
 * @method
 * @name revolutx#parseMyTrade
 * @description parses a trade from the exchange's private trade format
 * @ignore
 * @param {object} trade the raw trade data from the exchange
 * @param {object} [market] the market the trade was executed in
 * @returns {object} a [trade structure]{@link https://docs.ccxt.com/?id=trade-structure}
 */
    pub fn parse_my_trade(&self, mut trade: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        let mut id: Value = self.safe_string_k(trade.clone(), "tid", &[]);
        let mut orderId: Value = self.safe_string_k(trade.clone(), "oid", &[]);
        let mut price: Value = self.safe_number_k(trade.clone(), "p", &[]);
        let mut amount: Value = self.safe_number_k(trade.clone(), "q", &[]);
        let mut side: Value = self.safe_string_lower(trade.clone(), Value::Str("s".into()), &[]);
        let mut timestamp: Value = self.safe_integer2(trade.clone(), Value::Str("tdt".into()), Value::Str("pdt".into()), &[]);
        let mut isMaker: Value = self.safe_bool_k(trade.clone(), "im", &[Value::Bool(false)]);
        let mut takerOrMaker: Value = (if isMaker.as_bool() == Some(true) { Value::Str("maker".into()) } else { Value::Str("taker".into()) });
        let mut cost: Value = Value::Null;
        if (price != Value::Null) && (amount != Value::Null) {
            cost = (match (&(price), &(amount)) { (Value::Int(x), Value::Int(y)) => Value::Int(x * y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 * *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x * *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x * y), _ => Value::Null });
        }
        let mut symbol: Value = self.safe_symbol(Value::Null, &[market]);
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), trade);
        m.insert("id".to_string(), id);
        m.insert("order".to_string(), orderId);
        m.insert("symbol".to_string(), symbol);
        m.insert("side".to_string(), side);
        m.insert("type".to_string(), Value::Null);
        m.insert("takerOrMaker".to_string(), takerOrMaker);
        m.insert("price".to_string(), price);
        m.insert("amount".to_string(), amount);
        m.insert("cost".to_string(), cost);
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp));
        m.insert("fee".to_string(), Value::Null);
        m.insert("fees".to_string(), Value::from(vec![]));
    m
});

    Value::Null
}

/*
 * @method
 * @name revolutx#fetchMyTrades
 * @description fetches the trade history for the authenticated user
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data
 * @param {string} symbol unified symbol of the market to fetch trades for
 * @param {int} [since] timestamp in ms of the earliest trade to fetch, the lookup window is limited to 30 days
 * @param {int} [limit] the maximum number of trades to return (1-1900, default 1900)
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] timestamp in ms of the latest trade to fetch
 * @param {string} [params.cursor] pagination cursor from the previous response
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=trade-structure}
 */
    pub async fn fetch_my_trades(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        if (symbol == Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(format!("{}{}", self.id.clone(), Value::Str(" fetchMyTrades() requires a symbol parameter".into()))));
        }
        let mut market: Value = self.market(symbol);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("symbol".to_string(), market.as_map().and_then(|__m| __m.get("id")).cloned().unwrap_or(Value::Null));
            m
        });
        let mut thirtyDays: Value = Value::Int(2592000000);
        let mut until: Value = self.safe_integer2(params.clone(), Value::Str("until".into()), Value::Str("until".into()), &[]);
        if (since != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("start_date".to_string(), since.clone()); }
        }  else if (until != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("start_date".to_string(), (match (&(until), &(thirtyDays)) { (Value::Int(x), Value::Int(y)) => Value::Int(x - y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 - *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x - *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x - y), _ => Value::Null })); }
        }
        if (until != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("end_date".to_string(), until); }
        }  else if (since != Value::Null) {
            let mut now: Value = self.milliseconds();
            let mut defaultEnd: Value = (match (&(since), &(thirtyDays)) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null });
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("end_date".to_string(), (if (defaultEnd.as_f64().unwrap_or(f64::NAN) < now.as_f64().unwrap_or(f64::NAN)) { defaultEnd } else { now })); }
        }
        if (limit != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("limit".to_string(), limit); }
        }
        let mut cursor: Value = self.safe_string_k(params.clone(), "cursor", &[]);
        if (cursor != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("cursor".to_string(), cursor); }
        }
        let __ws_arg_13 = self.extend(request, &[self.omit(params, Value::from(vec![Value::Str("until".into())]), &[])]);
        let mut response: Value = self.private_get10_trades_private_symbol(&[__ws_arg_13]).await;
        //
        //     {
        //         "data": [
        //             { "tdt": 1785309833816, "p": "119900.00", "q": "0.00100000",
        //               "tid": "ad3e8787ab623ba5a1dfea53819be6f9", "oid": "2affb2ac-4cf7-4bbf-b7b2-fc1e885bdc2c",
        //               "s": "buy", "im": false }
        //         ],
        //         "metadata": { "timestamp": 1785313433816, "next_cursor": "..." }
        //     }
        //
        let mut data: Value = self.safe_list_k(response, "data", &[Value::from(vec![])]);
        let mut result: Value = Value::from(vec![]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_1084: bool = true;
            while { if !__for_first_1084 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_1084 = false; i.as_f64().unwrap_or(f64::NAN) < ((data.len() as i64) as f64) } {
            let mut trade: Value = self.safe_dict(data.clone(), i.clone(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
            append_to_array(&mut result, self.parse_my_trade(trade, &[market.clone()]));
        }
        }
        return result;

    Value::Null
}

/*
 * @method
 * @name revolutx#editOrder
 * @description replaces an existing order
 * @see https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-trading
 * @param {string} id the order id (venue order id) to replace
 * @param {string} symbol unified symbol of the market
 * @param {string} type 'limit' or 'market'
 * @param {string} side 'buy' or 'sell'
 * @param {float} amount the new amount
 * @param {float} [price] the new price
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.clientOrderId] a custom client order id (UUID)
 * @param {float} [params.cost] the new order cost in units of the quote currency
 * @param {string} [params.timeInForce] e.g. gtc
 * @param {string[]} [params.executionInstructions] e.g. ['post_only']
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn edit_order(&mut self, mut id: Value, mut symbol: Value, mut type_var: Value, mut side: Value, optional_args: &[Value]) -> Value {
        let mut amount = get_arg(optional_args, 0, Value::Null);
        let mut price = get_arg(optional_args, 1, Value::Null);
        let mut params = get_arg(optional_args, 2, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        // note: the exchange assigns a new venue_order_id on replace — the returned order carries the new id
        if (self.markets.clone() == Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol.clone());
        let mut clientOrderId: Value = self.safe_string2(params.clone(), Value::Str("clientOrderId".into()), Value::Str("client_order_id".into()), &[self.uuid(&[])]);
        let mut cost: Value = self.safe_string2(params.clone(), Value::Str("cost".into()), Value::Str("quote_size".into()), &[]);
        let mut timeInForce: Value = self.safe_string_lower2(params.clone(), Value::Str("timeInForce".into()), Value::Str("time_in_force".into()), &[]);
        let mut executionInstructions: Value = self.safe_list_k(params.clone(), "executionInstructions", &[self.safe_list(params.clone(), Value::Str("execution_instructions".into()), &[])]);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("client_order_id".to_string(), clientOrderId);
                m.insert("venue_order_id".to_string(), id);
            m
        });
        if (cost != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("quote_size".to_string(), self.cost_to_precision(symbol.clone(), cost)); }
        }  else if (amount != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("base_size".to_string(), self.amount_to_precision(symbol.clone(), amount)); }
        }
        if (price != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("price".to_string(), self.price_to_precision(symbol, price)); }
        }
        if (timeInForce != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("time_in_force".to_string(), timeInForce); }
        }
        if (executionInstructions != Value::Null) {
            if let Value::Dict(__d) = &mut request { std::sync::Arc::make_mut(__d).insert("execution_instructions".to_string(), executionInstructions); }
        }
        let __ws_arg_14 = self.extend(request, &[self.omit(params, Value::from(vec![Value::Str("clientOrderId".into()), Value::Str("client_order_id".into()), Value::Str("cost".into()), Value::Str("quote_size".into()), Value::Str("timeInForce".into()), Value::Str("time_in_force".into()), Value::Str("executionInstructions".into()), Value::Str("execution_instructions".into())]), &[])]);
        let mut response: Value = self.private_put10_orders_venue_order_id(&[__ws_arg_14]).await;
        //
        //     {
        //         "data": [
        //             { "venue_order_id": "7a52e92e-8639-4fe1-abaa-68d3a2d5234b", "client_order_id": "...", "state": "new" }
        //         ]
        //     }
        //
        let mut data: Value = self.safe_value_k(response.clone(), "data", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut orderData: Value = (if (matches!(&data, Value::Arr(_))) { self.safe_dict(data.clone(), Value::Int(0), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]) } else { self.safe_dict_k(response, "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]) });
        let mut newVenueOrderId: Value = self.safe_string_k(orderData.clone(), "venue_order_id", &[]);
        let mut state: Value = self.safe_string_k(orderData.clone(), "state", &[]);
        let __ws_arg_15 = self.extend(orderData, &[Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), newVenueOrderId);
        m.insert("symbol".to_string(), crate::value::get_value_k(&market, "id"));
        m.insert("status".to_string(), state);
        m.insert("side".to_string(), side);
        m.insert("type".to_string(), type_var);
    m
})]);
        let mut order: Value = self.parse_order(__ws_arg_15, &[market]);
        return order;

    Value::Null
}

    pub fn handle_errors(&self, mut code: Value, mut reason: Value, mut url: Value, mut method: Value, mut headers: Value, mut body: Value, mut response: Value, mut requestHeaders: Value, mut requestBody: Value) -> Value {
        if code.as_f64().unwrap_or(f64::NAN) >= ((400i64) as f64) {
            if (response == Value::Null) {
                return Value::Null;
            }
            let mut feedback: Value = Value::Str(format!("{}{}", Value::Str(format!("{}{}", self.id.clone(), Value::Str(" ".into())).into()), body).into());
            let mut errorMessage: Value = Value::Null;
            if matches!(&response, Value::Dict(_)) {
                errorMessage = self.safe_string2(response.clone(), Value::Str("message".into()), Value::Str("error".into()), &[]);
            }
            if (errorMessage != Value::Null) {
                self.throw_broadly_matched_exception(self.exceptions.as_map().and_then(|__m| __m.get("broad")).cloned().unwrap_or(Value::Null), errorMessage, feedback);
            }
            return Value::Null;
        }
        return Value::Null;

    Value::Null
}
}
