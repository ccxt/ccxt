// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code
//
// Aggregator: re-exports every per-exchange typed wrapper at the crate
// root so users can write `use ccxt::Binance;` instead of the full
// `use ccxt::exchanges::binance_typed::Binance;` path, and declares the
// `TypedExchange` trait — the unified interface every wrapper implements
// (its `impl` blocks live in each `<id>_typed.rs`).
//
// Generated by `build/generateRustWrappers.ts`.

#![allow(unused_imports, unused_variables, clippy::all)]

use crate::Value;
use crate::types::*;

/// Unified typed interface implemented by every typed wrapper
/// (`ccxt::Binance`, `ccxt::Kraken`, …). Lets you write exchange-agnostic
/// code — generic (`fn f<E: TypedExchange>(ex: &mut E)`) or dynamic
/// (`Box<dyn TypedExchange>`) — with native typed returns. A method the
/// exchange does not implement resolves to a `NotSupported` error at
/// runtime (raised by the underlying Core).
#[async_trait::async_trait]
pub trait TypedExchange: Send {
    /// Object-safe dynamic dispatch — the ONE method each wrapper
    /// implements. Routes `method(args)` to the underlying Core and
    /// surfaces its panic-based errors as `Result`.
    async fn call_raw(&mut self, method: &str, args: Vec<Value>) -> crate::Result<Value>;

    /// Loads and caches markets (untyped setup for symbol resolution).
    async fn load_markets(&mut self, reload: bool) -> Value {
        self.call_raw("load_markets", vec![Value::Bool(reload)]).await.unwrap_or(Value::Null)
    }
    /// Typed `fetchCurrencies`.
    async fn fetch_currencies(&mut self, params: Value) -> crate::Result<Currencies> {
        let v = self.call_raw("fetch_currencies", vec![params]).await?;
        Ok(dict_from_value(&v, Currency::from_value))
    }
    /// Typed `fetchMarkets`.
    async fn fetch_markets(&mut self, params: Value) -> crate::Result<Vec<Market>> {
        let v = self.call_raw("fetch_markets", vec![params]).await?;
        Ok(vec_from_value(&v, Market::from_value))
    }
    /// Typed `fetchDepositAddresses`.
    async fn fetch_deposit_addresses(&mut self, codes: Option<Vec<String>>, params: Value) -> crate::Result<Vec<DepositAddress>> {
        let v = self.call_raw("fetch_deposit_addresses", vec![match codes { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params]).await?;
        Ok(vec_from_value(&v, DepositAddress::from_value))
    }
    /// Typed `fetchMarginMode`.
    async fn fetch_margin_mode(&mut self, symbol: &str, params: Value) -> crate::Result<MarginMode> {
        let v = self.call_raw("fetch_margin_mode", vec![Value::Str(symbol.to_string()), params]).await?;
        Ok(MarginMode::from_value(v))
    }
    /// Typed `fetchMarginModes`.
    async fn fetch_margin_modes(&mut self, symbols: Option<Vec<String>>, params: Value) -> crate::Result<MarginModes> {
        let v = self.call_raw("fetch_margin_modes", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params]).await?;
        Ok(dict_from_value(&v, MarginMode::from_value))
    }
    /// Typed `fetchTime`.
    async fn fetch_time(&mut self, params: Value) -> crate::Result<Option<i64>> {
        let v = self.call_raw("fetch_time", vec![params]).await?;
        Ok(match v { Value::Int(n) => Some(n), _ => None })
    }
    /// Typed `fetchFundingRates`.
    async fn fetch_funding_rates(&mut self, symbols: Option<Vec<String>>, params: Value) -> crate::Result<FundingRates> {
        let v = self.call_raw("fetch_funding_rates", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params]).await?;
        Ok(dict_from_value(&v, FundingRate::from_value))
    }
    /// Typed `fetchFundingIntervals`.
    async fn fetch_funding_intervals(&mut self, symbols: Option<Vec<String>>, params: Value) -> crate::Result<FundingRates> {
        let v = self.call_raw("fetch_funding_intervals", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params]).await?;
        Ok(dict_from_value(&v, FundingRate::from_value))
    }
    /// Typed `transfer`.
    async fn transfer(&mut self, code: &str, amount: f64, from_account: &str, to_account: &str, params: Value) -> crate::Result<Transfer> {
        let v = self.call_raw("transfer", vec![Value::Str(code.to_string()), Value::Float(amount), Value::Str(from_account.to_string()), Value::Str(to_account.to_string()), params]).await?;
        Ok(Transfer::from_value(v))
    }
    /// Typed `withdraw`.
    async fn withdraw(&mut self, code: &str, amount: f64, address: &str, tag: Option<&str>, params: Value) -> crate::Result<Transaction> {
        let v = self.call_raw("withdraw", vec![Value::Str(code.to_string()), Value::Float(amount), Value::Str(address.to_string()), tag.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(Transaction::from_value(v))
    }
    /// Typed `createDepositAddress`.
    async fn create_deposit_address(&mut self, code: &str, params: Value) -> crate::Result<DepositAddress> {
        let v = self.call_raw("create_deposit_address", vec![Value::Str(code.to_string()), params]).await?;
        Ok(DepositAddress::from_value(v))
    }
    /// Typed `fetchLeverage`.
    async fn fetch_leverage(&mut self, symbol: &str, params: Value) -> crate::Result<Leverage> {
        let v = self.call_raw("fetch_leverage", vec![Value::Str(symbol.to_string()), params]).await?;
        Ok(Leverage::from_value(v))
    }
    /// Typed `fetchLeverages`.
    async fn fetch_leverages(&mut self, symbols: Option<Vec<String>>, params: Value) -> crate::Result<Leverages> {
        let v = self.call_raw("fetch_leverages", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params]).await?;
        Ok(dict_from_value(&v, Leverage::from_value))
    }
    /// Typed `fetchDepositAddressesByNetwork`.
    async fn fetch_deposit_addresses_by_network(&mut self, code: &str, params: Value) -> crate::Result<Vec<DepositAddress>> {
        let v = self.call_raw("fetch_deposit_addresses_by_network", vec![Value::Str(code.to_string()), params]).await?;
        Ok(vec_from_value(&v, DepositAddress::from_value))
    }
    /// Typed `fetchOpenInterestHistory`.
    async fn fetch_open_interest_history(&mut self, symbol: &str, timeframe: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<OpenInterest>> {
        let v = self.call_raw("fetch_open_interest_history", vec![Value::Str(symbol.to_string()), timeframe.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, OpenInterest::from_value))
    }
    /// Typed `fetchOpenInterests`.
    async fn fetch_open_interests(&mut self, symbols: Option<Vec<String>>, params: Value) -> crate::Result<OpenInterests> {
        let v = self.call_raw("fetch_open_interests", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params]).await?;
        Ok(dict_from_value(&v, OpenInterest::from_value))
    }
    /// Typed `fetchBorrowInterest`.
    async fn fetch_borrow_interest(&mut self, code: Option<&str>, symbol: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<BorrowInterest>> {
        let v = self.call_raw("fetch_borrow_interest", vec![code.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, BorrowInterest::from_value))
    }
    /// Typed `fetchLedger`.
    async fn fetch_ledger(&mut self, code: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<LedgerEntry>> {
        let v = self.call_raw("fetch_ledger", vec![code.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, LedgerEntry::from_value))
    }
    /// Typed `fetchLedgerEntry`.
    async fn fetch_ledger_entry(&mut self, id: &str, code: Option<&str>, params: Value) -> crate::Result<LedgerEntry> {
        let v = self.call_raw("fetch_ledger_entry", vec![Value::Str(id.to_string()), code.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(LedgerEntry::from_value(v))
    }
    /// Typed `fetchBalance`.
    async fn fetch_balance(&mut self, params: Value) -> crate::Result<Balances> {
        let v = self.call_raw("fetch_balance", vec![params]).await?;
        Ok(Balances::from_value(v))
    }
    /// Typed `fetchPartialBalance`.
    async fn fetch_partial_balance(&mut self, part: Value, params: Value) -> crate::Result<Balances> {
        let v = self.call_raw("fetch_partial_balance", vec![part, params]).await?;
        Ok(Balances::from_value(v))
    }
    /// Typed `fetchFreeBalance`.
    async fn fetch_free_balance(&mut self, params: Value) -> crate::Result<Balances> {
        let v = self.call_raw("fetch_free_balance", vec![params]).await?;
        Ok(Balances::from_value(v))
    }
    /// Typed `fetchUsedBalance`.
    async fn fetch_used_balance(&mut self, params: Value) -> crate::Result<Balances> {
        let v = self.call_raw("fetch_used_balance", vec![params]).await?;
        Ok(Balances::from_value(v))
    }
    /// Typed `fetchTotalBalance`.
    async fn fetch_total_balance(&mut self, params: Value) -> crate::Result<Balances> {
        let v = self.call_raw("fetch_total_balance", vec![params]).await?;
        Ok(Balances::from_value(v))
    }
    /// Typed `fetchStatus`.
    async fn fetch_status(&mut self, params: Value) -> crate::Result<Status> {
        let v = self.call_raw("fetch_status", vec![params]).await?;
        Ok(Status::from_value(v))
    }
    /// Typed `fetchCrossBorrowRate`.
    async fn fetch_cross_borrow_rate(&mut self, code: &str, params: Value) -> crate::Result<BorrowRate> {
        let v = self.call_raw("fetch_cross_borrow_rate", vec![Value::Str(code.to_string()), params]).await?;
        Ok(BorrowRate::from_value(v))
    }
    /// Typed `fetchIsolatedBorrowRate`.
    async fn fetch_isolated_borrow_rate(&mut self, symbol: &str, params: Value) -> crate::Result<BorrowRate> {
        let v = self.call_raw("fetch_isolated_borrow_rate", vec![Value::Str(symbol.to_string()), params]).await?;
        Ok(BorrowRate::from_value(v))
    }
    /// Typed `fetchSpotTickers`.
    async fn fetch_spot_tickers(&mut self, symbols: Option<Vec<String>>, params: Value) -> crate::Result<Tickers> {
        let v = self.call_raw("fetch_spot_tickers", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params]).await?;
        Ok(dict_from_value(&v, Ticker::from_value))
    }
    /// Typed `fetchContractTickers`.
    async fn fetch_contract_tickers(&mut self, symbols: Option<Vec<String>>, params: Value) -> crate::Result<Tickers> {
        let v = self.call_raw("fetch_contract_tickers", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params]).await?;
        Ok(dict_from_value(&v, Ticker::from_value))
    }
    /// Typed `fetchOrderBooks`.
    async fn fetch_order_books(&mut self, symbols: Option<Vec<String>>, limit: Option<i64>, params: Value) -> crate::Result<OrderBooks> {
        let v = self.call_raw("fetch_order_books", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(dict_from_value(&v, OrderBook::from_value))
    }
    /// Typed `createTwapOrder`.
    async fn create_twap_order(&mut self, symbol: &str, side: &str, amount: f64, duration: f64, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_twap_order", vec![Value::Str(symbol.to_string()), Value::Str(side.to_string()), Value::Float(amount), Value::Float(duration), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createSpotOrders`.
    async fn create_spot_orders(&mut self, orders: Value, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("create_spot_orders", vec![orders, params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `createContractOrders`.
    async fn create_contract_orders(&mut self, orders: Value, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("create_contract_orders", vec![orders, params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `cancelSpotOrder`.
    async fn cancel_spot_order(&mut self, id: &str, symbol: Option<&str>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("cancel_spot_order", vec![Value::Str(id.to_string()), symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `cancelContractOrder`.
    async fn cancel_contract_order(&mut self, id: &str, symbol: Option<&str>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("cancel_contract_order", vec![Value::Str(id.to_string()), symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `cancelAllSpotOrders`.
    async fn cancel_all_spot_orders(&mut self, symbol: Option<&str>, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("cancel_all_spot_orders", vec![symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `cancelAllContractOrders`.
    async fn cancel_all_contract_orders(&mut self, symbol: Option<&str>, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("cancel_all_contract_orders", vec![symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `cancelOrdersForSymbols`.
    async fn cancel_orders_for_symbols(&mut self, orders: Value, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("cancel_orders_for_symbols", vec![orders, params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `fetchMyLiquidations`.
    async fn fetch_my_liquidations(&mut self, symbol: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Liquidation>> {
        let v = self.call_raw("fetch_my_liquidations", vec![symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Liquidation::from_value))
    }
    /// Typed `fetchLiquidations`.
    async fn fetch_liquidations(&mut self, symbol: &str, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Liquidation>> {
        let v = self.call_raw("fetch_liquidations", vec![Value::Str(symbol.to_string()), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Liquidation::from_value))
    }
    /// Typed `fetchGreeks`.
    async fn fetch_greeks(&mut self, symbol: &str, params: Value) -> crate::Result<Greeks> {
        let v = self.call_raw("fetch_greeks", vec![Value::Str(symbol.to_string()), params]).await?;
        Ok(Greeks::from_value(v))
    }
    /// Typed `fetchAllGreeks`.
    async fn fetch_all_greeks(&mut self, symbols: Option<Vec<String>>, params: Value) -> crate::Result<Vec<Greeks>> {
        let v = self.call_raw("fetch_all_greeks", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params]).await?;
        Ok(vec_from_value(&v, Greeks::from_value))
    }
    /// Typed `fetchDepositsWithdrawals`.
    async fn fetch_deposits_withdrawals(&mut self, code: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Transaction>> {
        let v = self.call_raw("fetch_deposits_withdrawals", vec![code.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Transaction::from_value))
    }
    /// Typed `fetchDeposits`.
    async fn fetch_deposits(&mut self, code: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Transaction>> {
        let v = self.call_raw("fetch_deposits", vec![code.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Transaction::from_value))
    }
    /// Typed `fetchWithdrawals`.
    async fn fetch_withdrawals(&mut self, code: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Transaction>> {
        let v = self.call_raw("fetch_withdrawals", vec![code.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Transaction::from_value))
    }
    /// Typed `fetchDepositAddress`.
    async fn fetch_deposit_address(&mut self, code: &str, params: Value) -> crate::Result<DepositAddress> {
        let v = self.call_raw("fetch_deposit_address", vec![Value::Str(code.to_string()), params]).await?;
        Ok(DepositAddress::from_value(v))
    }
    /// Typed `fetchContractDepositAddress`.
    async fn fetch_contract_deposit_address(&mut self, code: &str, params: Value) -> crate::Result<DepositAddress> {
        let v = self.call_raw("fetch_contract_deposit_address", vec![Value::Str(code.to_string()), params]).await?;
        Ok(DepositAddress::from_value(v))
    }
    /// Typed `fetchMarketLeverageTiers`.
    async fn fetch_market_leverage_tiers(&mut self, symbol: &str, params: Value) -> crate::Result<Vec<LeverageTier>> {
        let v = self.call_raw("fetch_market_leverage_tiers", vec![Value::Str(symbol.to_string()), params]).await?;
        Ok(vec_from_value(&v, LeverageTier::from_value))
    }
    /// Typed `fetchTradingFees`.
    async fn fetch_trading_fees(&mut self, params: Value) -> crate::Result<TradingFees> {
        let v = self.call_raw("fetch_trading_fees", vec![params]).await?;
        Ok(dict_from_value(&v, TradingFee::from_value))
    }
    /// Typed `fetchConvertCurrencies`.
    async fn fetch_convert_currencies(&mut self, params: Value) -> crate::Result<Currencies> {
        let v = self.call_raw("fetch_convert_currencies", vec![params]).await?;
        Ok(dict_from_value(&v, Currency::from_value))
    }
    /// Typed `fetchFundingRate`.
    async fn fetch_funding_rate(&mut self, symbol: &str, params: Value) -> crate::Result<FundingRate> {
        let v = self.call_raw("fetch_funding_rate", vec![Value::Str(symbol.to_string()), params]).await?;
        Ok(FundingRate::from_value(v))
    }
    /// Typed `fetchFundingInterval`.
    async fn fetch_funding_interval(&mut self, symbol: &str, params: Value) -> crate::Result<FundingRate> {
        let v = self.call_raw("fetch_funding_interval", vec![Value::Str(symbol.to_string()), params]).await?;
        Ok(FundingRate::from_value(v))
    }
    /// Typed `fetchTransactions`.
    async fn fetch_transactions(&mut self, code: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Transaction>> {
        let v = self.call_raw("fetch_transactions", vec![code.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Transaction::from_value))
    }
    /// Typed `fetchTransfer`.
    async fn fetch_transfer(&mut self, id: &str, code: Option<&str>, params: Value) -> crate::Result<Transfer> {
        let v = self.call_raw("fetch_transfer", vec![Value::Str(id.to_string()), code.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(Transfer::from_value(v))
    }
    /// Typed `fetchTransfers`.
    async fn fetch_transfers(&mut self, code: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Transfer>> {
        let v = self.call_raw("fetch_transfers", vec![code.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Transfer::from_value))
    }
    /// Typed `closePosition`.
    async fn close_position(&mut self, symbol: &str, side: Option<&str>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("close_position", vec![Value::Str(symbol.to_string()), side.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `closeAllPositions`.
    async fn close_all_positions(&mut self, params: Value) -> crate::Result<Vec<Position>> {
        let v = self.call_raw("close_all_positions", vec![params]).await?;
        Ok(vec_from_value(&v, Position::from_value))
    }
    /// Typed `editOrders`.
    async fn edit_orders(&mut self, orders: Value, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("edit_orders", vec![orders, params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `fetchCanceledAndClosedOrders`.
    async fn fetch_canceled_and_closed_orders(&mut self, symbol: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("fetch_canceled_and_closed_orders", vec![symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `fetchPositionHistory`.
    async fn fetch_position_history(&mut self, symbol: &str, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Position>> {
        let v = self.call_raw("fetch_position_history", vec![Value::Str(symbol.to_string()), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Position::from_value))
    }
    /// Typed `fetchPositionsHistory`.
    async fn fetch_positions_history(&mut self, symbols: Option<Vec<String>>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Position>> {
        let v = self.call_raw("fetch_positions_history", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Position::from_value))
    }
    /// Typed `fetchPositionsRisk`.
    async fn fetch_positions_risk(&mut self, symbols: Option<Vec<String>>, params: Value) -> crate::Result<Vec<Position>> {
        let v = self.call_raw("fetch_positions_risk", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params]).await?;
        Ok(vec_from_value(&v, Position::from_value))
    }
    /// Typed `fetchPositionsForSymbol`.
    async fn fetch_positions_for_symbol(&mut self, symbol: &str, params: Value) -> crate::Result<Vec<Position>> {
        let v = self.call_raw("fetch_positions_for_symbol", vec![Value::Str(symbol.to_string()), params]).await?;
        Ok(vec_from_value(&v, Position::from_value))
    }
    /// Typed `fetchBidsAsks`.
    async fn fetch_bids_asks(&mut self, symbols: Option<Vec<String>>, params: Value) -> crate::Result<Tickers> {
        let v = self.call_raw("fetch_bids_asks", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params]).await?;
        Ok(dict_from_value(&v, Ticker::from_value))
    }
    /// Typed `fetchMarkPrice`.
    async fn fetch_mark_price(&mut self, symbol: &str, params: Value) -> crate::Result<Ticker> {
        let v = self.call_raw("fetch_mark_price", vec![Value::Str(symbol.to_string()), params]).await?;
        Ok(Ticker::from_value(v))
    }
    /// Typed `fetchMarkPrices`.
    async fn fetch_mark_prices(&mut self, symbols: Option<Vec<String>>, params: Value) -> crate::Result<Tickers> {
        let v = self.call_raw("fetch_mark_prices", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params]).await?;
        Ok(dict_from_value(&v, Ticker::from_value))
    }
    /// Typed `fetchL3OrderBook`.
    async fn fetch_l3_order_book(&mut self, symbol: &str, limit: Option<i64>, params: Value) -> crate::Result<OrderBook> {
        let v = self.call_raw("fetch_l3_order_book", vec![Value::Str(symbol.to_string()), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(OrderBook::from_value(v))
    }
    /// Typed `fetchTrades`.
    async fn fetch_trades(&mut self, symbol: &str, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Trade>> {
        let v = self.call_raw("fetch_trades", vec![Value::Str(symbol.to_string()), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Trade::from_value))
    }
    /// Typed `fetchOrderBook`.
    async fn fetch_order_book(&mut self, symbol: &str, limit: Option<i64>, params: Value) -> crate::Result<OrderBook> {
        let v = self.call_raw("fetch_order_book", vec![Value::Str(symbol.to_string()), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(OrderBook::from_value(v))
    }
    /// Typed `fetchOpenInterest`.
    async fn fetch_open_interest(&mut self, symbol: &str, params: Value) -> crate::Result<OpenInterest> {
        let v = self.call_raw("fetch_open_interest", vec![Value::Str(symbol.to_string()), params]).await?;
        Ok(OpenInterest::from_value(v))
    }
    /// Typed `editLimitBuyOrder`.
    async fn edit_limit_buy_order(&mut self, id: &str, symbol: &str, amount: f64, price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("edit_limit_buy_order", vec![Value::Str(id.to_string()), Value::Str(symbol.to_string()), Value::Float(amount), price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `editLimitSellOrder`.
    async fn edit_limit_sell_order(&mut self, id: &str, symbol: &str, amount: f64, price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("edit_limit_sell_order", vec![Value::Str(id.to_string()), Value::Str(symbol.to_string()), Value::Float(amount), price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `editLimitOrder`.
    async fn edit_limit_order(&mut self, id: &str, symbol: &str, side: &str, amount: f64, price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("edit_limit_order", vec![Value::Str(id.to_string()), Value::Str(symbol.to_string()), Value::Str(side.to_string()), Value::Float(amount), price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `editOrder`.
    async fn edit_order(&mut self, id: &str, symbol: &str, type_: &str, side: &str, amount: Option<f64>, price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("edit_order", vec![Value::Str(id.to_string()), Value::Str(symbol.to_string()), Value::Str(type_.to_string()), Value::Str(side.to_string()), amount.map(Value::Float).unwrap_or(Value::Null), price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `editOrderWithClientOrderId`.
    async fn edit_order_with_client_order_id(&mut self, client_order_id: &str, symbol: &str, type_: &str, side: &str, amount: Option<f64>, price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("edit_order_with_client_order_id", vec![Value::Str(client_order_id.to_string()), Value::Str(symbol.to_string()), Value::Str(type_.to_string()), Value::Str(side.to_string()), amount.map(Value::Float).unwrap_or(Value::Null), price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `fetchPosition`.
    async fn fetch_position(&mut self, symbol: &str, params: Value) -> crate::Result<Position> {
        let v = self.call_raw("fetch_position", vec![Value::Str(symbol.to_string()), params]).await?;
        Ok(Position::from_value(v))
    }
    /// Typed `fetchPositions`.
    async fn fetch_positions(&mut self, symbols: Option<Vec<String>>, params: Value) -> crate::Result<Vec<Position>> {
        let v = self.call_raw("fetch_positions", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params]).await?;
        Ok(vec_from_value(&v, Position::from_value))
    }
    /// Typed `fetchTicker`.
    async fn fetch_ticker(&mut self, symbol: &str, params: Value) -> crate::Result<Ticker> {
        let v = self.call_raw("fetch_ticker", vec![Value::Str(symbol.to_string()), params]).await?;
        Ok(Ticker::from_value(v))
    }
    /// Typed `fetchTickers`.
    async fn fetch_tickers(&mut self, symbols: Option<Vec<String>>, params: Value) -> crate::Result<Tickers> {
        let v = self.call_raw("fetch_tickers", vec![match symbols { Some(list) => Value::Arr(std::sync::Arc::new(list.into_iter().map(Value::Str).collect())), None => Value::Null }, params]).await?;
        Ok(dict_from_value(&v, Ticker::from_value))
    }
    /// Typed `fetchOrder`.
    async fn fetch_order(&mut self, id: &str, symbol: Option<&str>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("fetch_order", vec![Value::Str(id.to_string()), symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `fetchOrderWithClientOrderId`.
    async fn fetch_order_with_client_order_id(&mut self, client_order_id: &str, symbol: Option<&str>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("fetch_order_with_client_order_id", vec![Value::Str(client_order_id.to_string()), symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `fetchOrderStatus`.
    async fn fetch_order_status(&mut self, id: &str, symbol: Option<&str>, params: Value) -> crate::Result<Option<String>> {
        let v = self.call_raw("fetch_order_status", vec![Value::Str(id.to_string()), symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(match v { Value::Str(s) => Some(s), _ => None })
    }
    /// Typed `fetchUnifiedOrder`.
    async fn fetch_unified_order(&mut self, order: Value, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("fetch_unified_order", vec![order, params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createOrder`.
    async fn create_order(&mut self, symbol: &str, type_: &str, side: &str, amount: f64, price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_order", vec![Value::Str(symbol.to_string()), Value::Str(type_.to_string()), Value::Str(side.to_string()), Value::Float(amount), price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createTrailingAmountOrder`.
    async fn create_trailing_amount_order(&mut self, symbol: &str, type_: &str, side: &str, amount: f64, price: Option<f64>, trailing_amount: Option<f64>, trailing_trigger_price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_trailing_amount_order", vec![Value::Str(symbol.to_string()), Value::Str(type_.to_string()), Value::Str(side.to_string()), Value::Float(amount), price.map(Value::Float).unwrap_or(Value::Null), trailing_amount.map(Value::Float).unwrap_or(Value::Null), trailing_trigger_price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createTrailingPercentOrder`.
    async fn create_trailing_percent_order(&mut self, symbol: &str, type_: &str, side: &str, amount: f64, price: Option<f64>, trailing_percent: Option<f64>, trailing_trigger_price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_trailing_percent_order", vec![Value::Str(symbol.to_string()), Value::Str(type_.to_string()), Value::Str(side.to_string()), Value::Float(amount), price.map(Value::Float).unwrap_or(Value::Null), trailing_percent.map(Value::Float).unwrap_or(Value::Null), trailing_trigger_price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createMarketOrderWithCost`.
    async fn create_market_order_with_cost(&mut self, symbol: &str, side: &str, cost: f64, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_market_order_with_cost", vec![Value::Str(symbol.to_string()), Value::Str(side.to_string()), Value::Float(cost), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createMarketBuyOrderWithCost`.
    async fn create_market_buy_order_with_cost(&mut self, symbol: &str, cost: f64, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_market_buy_order_with_cost", vec![Value::Str(symbol.to_string()), Value::Float(cost), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createMarketSellOrderWithCost`.
    async fn create_market_sell_order_with_cost(&mut self, symbol: &str, cost: f64, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_market_sell_order_with_cost", vec![Value::Str(symbol.to_string()), Value::Float(cost), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createTriggerOrder`.
    async fn create_trigger_order(&mut self, symbol: &str, type_: &str, side: &str, amount: f64, price: Option<f64>, trigger_price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_trigger_order", vec![Value::Str(symbol.to_string()), Value::Str(type_.to_string()), Value::Str(side.to_string()), Value::Float(amount), price.map(Value::Float).unwrap_or(Value::Null), trigger_price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createStopLossOrder`.
    async fn create_stop_loss_order(&mut self, symbol: &str, type_: &str, side: &str, amount: f64, price: Option<f64>, stop_loss_price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_stop_loss_order", vec![Value::Str(symbol.to_string()), Value::Str(type_.to_string()), Value::Str(side.to_string()), Value::Float(amount), price.map(Value::Float).unwrap_or(Value::Null), stop_loss_price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createTakeProfitOrder`.
    async fn create_take_profit_order(&mut self, symbol: &str, type_: &str, side: &str, amount: f64, price: Option<f64>, take_profit_price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_take_profit_order", vec![Value::Str(symbol.to_string()), Value::Str(type_.to_string()), Value::Str(side.to_string()), Value::Float(amount), price.map(Value::Float).unwrap_or(Value::Null), take_profit_price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createOrderWithTakeProfitAndStopLoss`.
    async fn create_order_with_take_profit_and_stop_loss(&mut self, symbol: &str, type_: &str, side: &str, amount: f64, price: Option<f64>, take_profit: Option<f64>, stop_loss: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_order_with_take_profit_and_stop_loss", vec![Value::Str(symbol.to_string()), Value::Str(type_.to_string()), Value::Str(side.to_string()), Value::Float(amount), price.map(Value::Float).unwrap_or(Value::Null), take_profit.map(Value::Float).unwrap_or(Value::Null), stop_loss.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createOrders`.
    async fn create_orders(&mut self, orders: Value, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("create_orders", vec![orders, params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `cancelOrder`.
    async fn cancel_order(&mut self, id: &str, symbol: Option<&str>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("cancel_order", vec![Value::Str(id.to_string()), symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `cancelOrderWithClientOrderId`.
    async fn cancel_order_with_client_order_id(&mut self, client_order_id: &str, symbol: Option<&str>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("cancel_order_with_client_order_id", vec![Value::Str(client_order_id.to_string()), symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `cancelOrders`.
    async fn cancel_orders(&mut self, ids: Vec<String>, symbol: Option<&str>, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("cancel_orders", vec![Value::Arr(std::sync::Arc::new(ids.into_iter().map(Value::Str).collect())), symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `cancelOrdersWithClientOrderIds`.
    async fn cancel_orders_with_client_order_ids(&mut self, client_order_ids: Vec<String>, symbol: Option<&str>, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("cancel_orders_with_client_order_ids", vec![Value::Arr(std::sync::Arc::new(client_order_ids.into_iter().map(Value::Str).collect())), symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `cancelAllOrders`.
    async fn cancel_all_orders(&mut self, symbol: Option<&str>, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("cancel_all_orders", vec![symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `cancelUnifiedOrder`.
    async fn cancel_unified_order(&mut self, order: Value, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("cancel_unified_order", vec![order, params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `fetchOrders`.
    async fn fetch_orders(&mut self, symbol: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("fetch_orders", vec![symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `fetchOrderTrades`.
    async fn fetch_order_trades(&mut self, id: &str, symbol: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Trade>> {
        let v = self.call_raw("fetch_order_trades", vec![Value::Str(id.to_string()), symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Trade::from_value))
    }
    /// Typed `fetchOpenOrders`.
    async fn fetch_open_orders(&mut self, symbol: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("fetch_open_orders", vec![symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `fetchClosedOrders`.
    async fn fetch_closed_orders(&mut self, symbol: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("fetch_closed_orders", vec![symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `fetchCanceledOrders`.
    async fn fetch_canceled_orders(&mut self, symbol: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Order>> {
        let v = self.call_raw("fetch_canceled_orders", vec![symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Order::from_value))
    }
    /// Typed `fetchMyTrades`.
    async fn fetch_my_trades(&mut self, symbol: Option<&str>, since: Option<i64>, limit: Option<i64>, params: Value) -> crate::Result<Vec<Trade>> {
        let v = self.call_raw("fetch_my_trades", vec![symbol.map(|s| Value::Str(s.to_string())).unwrap_or(Value::Null), since.map(Value::Int).unwrap_or(Value::Null), limit.map(Value::Int).unwrap_or(Value::Null), params]).await?;
        Ok(vec_from_value(&v, Trade::from_value))
    }
    /// Typed `createLimitOrder`.
    async fn create_limit_order(&mut self, symbol: &str, side: &str, amount: f64, price: f64, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_limit_order", vec![Value::Str(symbol.to_string()), Value::Str(side.to_string()), Value::Float(amount), Value::Float(price), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createMarketOrder`.
    async fn create_market_order(&mut self, symbol: &str, side: &str, amount: f64, price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_market_order", vec![Value::Str(symbol.to_string()), Value::Str(side.to_string()), Value::Float(amount), price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createLimitBuyOrder`.
    async fn create_limit_buy_order(&mut self, symbol: &str, amount: f64, price: f64, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_limit_buy_order", vec![Value::Str(symbol.to_string()), Value::Float(amount), Value::Float(price), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createLimitSellOrder`.
    async fn create_limit_sell_order(&mut self, symbol: &str, amount: f64, price: f64, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_limit_sell_order", vec![Value::Str(symbol.to_string()), Value::Float(amount), Value::Float(price), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createMarketBuyOrder`.
    async fn create_market_buy_order(&mut self, symbol: &str, amount: f64, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_market_buy_order", vec![Value::Str(symbol.to_string()), Value::Float(amount), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createMarketSellOrder`.
    async fn create_market_sell_order(&mut self, symbol: &str, amount: f64, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_market_sell_order", vec![Value::Str(symbol.to_string()), Value::Float(amount), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createPostOnlyOrder`.
    async fn create_post_only_order(&mut self, symbol: &str, type_: &str, side: &str, amount: f64, price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_post_only_order", vec![Value::Str(symbol.to_string()), Value::Str(type_.to_string()), Value::Str(side.to_string()), Value::Float(amount), price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createReduceOnlyOrder`.
    async fn create_reduce_only_order(&mut self, symbol: &str, type_: &str, side: &str, amount: f64, price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_reduce_only_order", vec![Value::Str(symbol.to_string()), Value::Str(type_.to_string()), Value::Str(side.to_string()), Value::Float(amount), price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createStopOrder`.
    async fn create_stop_order(&mut self, symbol: &str, type_: &str, side: &str, amount: f64, price: Option<f64>, trigger_price: Option<f64>, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_stop_order", vec![Value::Str(symbol.to_string()), Value::Str(type_.to_string()), Value::Str(side.to_string()), Value::Float(amount), price.map(Value::Float).unwrap_or(Value::Null), trigger_price.map(Value::Float).unwrap_or(Value::Null), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createStopLimitOrder`.
    async fn create_stop_limit_order(&mut self, symbol: &str, side: &str, amount: f64, price: f64, trigger_price: f64, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_stop_limit_order", vec![Value::Str(symbol.to_string()), Value::Str(side.to_string()), Value::Float(amount), Value::Float(price), Value::Float(trigger_price), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `createStopMarketOrder`.
    async fn create_stop_market_order(&mut self, symbol: &str, side: &str, amount: f64, trigger_price: f64, params: Value) -> crate::Result<Order> {
        let v = self.call_raw("create_stop_market_order", vec![Value::Str(symbol.to_string()), Value::Str(side.to_string()), Value::Float(amount), Value::Float(trigger_price), params]).await?;
        Ok(Order::from_value(v))
    }
    /// Typed `fetchTradingFee`.
    async fn fetch_trading_fee(&mut self, symbol: &str, params: Value) -> crate::Result<TradingFee> {
        let v = self.call_raw("fetch_trading_fee", vec![Value::Str(symbol.to_string()), params]).await?;
        Ok(TradingFee::from_value(v))
    }
}

pub use crate::exchanges::alpaca_typed::Alpaca;
pub use crate::exchanges::apex_typed::Apex;
pub use crate::exchanges::aster_typed::Aster;
pub use crate::exchanges::backpack_typed::Backpack;
pub use crate::exchanges::bequant_typed::Bequant;
pub use crate::exchanges::bigone_typed::Bigone;
pub use crate::exchanges::binance_typed::Binance;
pub use crate::exchanges::binancecoinm_typed::Binancecoinm;
pub use crate::exchanges::binanceus_typed::Binanceus;
pub use crate::exchanges::binanceusdm_typed::Binanceusdm;
pub use crate::exchanges::bingx_typed::Bingx;
pub use crate::exchanges::bit2c_typed::Bit2c;
pub use crate::exchanges::bitbank_typed::Bitbank;
pub use crate::exchanges::bitbns_typed::Bitbns;
pub use crate::exchanges::bitfinex_typed::Bitfinex;
pub use crate::exchanges::bitflyer_typed::Bitflyer;
pub use crate::exchanges::bitget_typed::Bitget;
pub use crate::exchanges::bithumb_typed::Bithumb;
pub use crate::exchanges::bitmex_typed::Bitmex;
pub use crate::exchanges::bitopro_typed::Bitopro;
pub use crate::exchanges::bitrue_typed::Bitrue;
pub use crate::exchanges::bitso_typed::Bitso;
pub use crate::exchanges::bitstamp_typed::Bitstamp;
pub use crate::exchanges::bitteam_typed::Bitteam;
pub use crate::exchanges::bittrade_typed::Bittrade;
pub use crate::exchanges::bitvavo_typed::Bitvavo;
pub use crate::exchanges::blockchaincom_typed::Blockchaincom;
pub use crate::exchanges::blofin_typed::Blofin;
pub use crate::exchanges::btcbox_typed::Btcbox;
pub use crate::exchanges::btcmarkets_typed::Btcmarkets;
pub use crate::exchanges::btcturk_typed::Btcturk;
pub use crate::exchanges::bullish_typed::Bullish;
pub use crate::exchanges::bybit_typed::Bybit;
pub use crate::exchanges::bybiteu_typed::Bybiteu;
pub use crate::exchanges::bydfi_typed::Bydfi;
pub use crate::exchanges::cex_typed::Cex;
pub use crate::exchanges::coinbase_typed::Coinbase;
pub use crate::exchanges::coinbaseexchange_typed::Coinbaseexchange;
pub use crate::exchanges::coinbaseinternational_typed::Coinbaseinternational;
pub use crate::exchanges::coincheck_typed::Coincheck;
pub use crate::exchanges::coinex_typed::Coinex;
pub use crate::exchanges::coinmate_typed::Coinmate;
pub use crate::exchanges::coinone_typed::Coinone;
pub use crate::exchanges::coinsph_typed::Coinsph;
pub use crate::exchanges::coinspot_typed::Coinspot;
pub use crate::exchanges::cryptocom_typed::Cryptocom;
pub use crate::exchanges::cryptomus_typed::Cryptomus;
pub use crate::exchanges::deepcoin_typed::Deepcoin;
pub use crate::exchanges::delta_typed::Delta;
pub use crate::exchanges::deribit_typed::Deribit;
pub use crate::exchanges::derive_typed::Derive;
pub use crate::exchanges::digifinex_typed::Digifinex;
pub use crate::exchanges::dydx_typed::Dydx;
pub use crate::exchanges::extended_typed::Extended;
pub use crate::exchanges::fmfwio_typed::Fmfwio;
pub use crate::exchanges::foxbit_typed::Foxbit;
pub use crate::exchanges::gate_typed::Gate;
pub use crate::exchanges::gateeu_typed::Gateeu;
pub use crate::exchanges::gemini_typed::Gemini;
pub use crate::exchanges::grvt_typed::Grvt;
pub use crate::exchanges::hashkey_typed::Hashkey;
pub use crate::exchanges::hibachi_typed::Hibachi;
pub use crate::exchanges::hitbtc_typed::Hitbtc;
pub use crate::exchanges::hollaex_typed::Hollaex;
pub use crate::exchanges::htx_typed::Htx;
pub use crate::exchanges::hyperliquid_typed::Hyperliquid;
pub use crate::exchanges::independentreserve_typed::Independentreserve;
pub use crate::exchanges::indodax_typed::Indodax;
pub use crate::exchanges::kraken_typed::Kraken;
pub use crate::exchanges::krakenfutures_typed::Krakenfutures;
pub use crate::exchanges::kucoin_typed::Kucoin;
pub use crate::exchanges::kucoinfutures_typed::Kucoinfutures;
pub use crate::exchanges::latoken_typed::Latoken;
pub use crate::exchanges::lbank_typed::Lbank;
pub use crate::exchanges::lighter_typed::Lighter;
pub use crate::exchanges::luno_typed::Luno;
pub use crate::exchanges::mercado_typed::Mercado;
pub use crate::exchanges::mexc_typed::Mexc;
pub use crate::exchanges::modetrade_typed::Modetrade;
pub use crate::exchanges::mudrex_typed::Mudrex;
pub use crate::exchanges::myokx_typed::Myokx;
pub use crate::exchanges::nado_typed::Nado;
pub use crate::exchanges::ndax_typed::Ndax;
pub use crate::exchanges::okx_typed::Okx;
pub use crate::exchanges::okxus_typed::Okxus;
pub use crate::exchanges::onetrading_typed::Onetrading;
pub use crate::exchanges::p2b_typed::P2b;
pub use crate::exchanges::pacifica_typed::Pacifica;
pub use crate::exchanges::paradex_typed::Paradex;
pub use crate::exchanges::paymium_typed::Paymium;
pub use crate::exchanges::phemex_typed::Phemex;
pub use crate::exchanges::poloniex_typed::Poloniex;
pub use crate::exchanges::tokocrypto_typed::Tokocrypto;
pub use crate::exchanges::toobit_typed::Toobit;
pub use crate::exchanges::upbit_typed::Upbit;
pub use crate::exchanges::weex_typed::Weex;
pub use crate::exchanges::whitebit_typed::Whitebit;
pub use crate::exchanges::woo_typed::Woo;
pub use crate::exchanges::woofipro_typed::Woofipro;
pub use crate::exchanges::xt_typed::Xt;
pub use crate::exchanges::zaif_typed::Zaif;
pub use crate::exchanges::zebpay_typed::Zebpay;
