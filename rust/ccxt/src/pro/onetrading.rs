// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#![allow(unused, non_snake_case, clippy::all)]
use crate::Value;
use crate::get_value;
use crate::runtime::*;
use crate::pro::*;


pub struct OnetradingCore {
    pub parent: crate::exchanges::onetrading::OnetradingCore,
}

impl OnetradingCore {
    pub fn new(config: Option<crate::Value>) -> Self {
        let mut s = Self { parent: crate::exchanges::onetrading::OnetradingCore::new(config) };
        s.init();
        s
    }

    /// One-shot setup: populates describe() data onto the inner Exchange and
    /// builds the implicit API table. Idempotent — calling more than once is
    /// safe.
    ///
    /// Deliberately does NOT install the derived-dispatch pointers: new()
    /// runs init() while self is still a movable local, so binding here
    /// would capture a soon-invalid address (review P0 #1). An un-bound Core
    /// is safe-but-inert — derived_ptr defaults to DEFAULT_DERIVED and
    /// derived_core_ptr is null, so virtual calls fall back to the base.
    /// Binding happens later at a pinned/boxed address via bind(), which the
    /// sanctioned constructors call once the Core is heap-stable.
    pub fn init(&mut self) {
        // Populate describe()-derived fields.
        let described = OnetradingCore::describe(self);
        self.exchange.api      = crate::get_value(&described, &crate::Value::Str("api".to_string()));
        self.exchange.urls     = crate::get_value(&described, &crate::Value::Str("urls".to_string()));
        self.exchange.has      = crate::get_value(&described, &crate::Value::Str("has".to_string()));
        // rateLimit drives the throttle spacing. describe() usually declares a
        // venue-specific value (binance 50ms, …); new() ran apply_config before
        // init(), so a config-supplied rateLimit is already in place. Only fall
        // back to describe()'s value when the field is still the base 2000ms
        // default, so caller config keeps precedence (review #8 — init() used
        // to drop describe().rateLimit, leaving every venue at 2000ms).
        let __described_rate_limit = crate::get_value(&described, &crate::Value::Str("rateLimit".to_string()));
        if matches!(__described_rate_limit, crate::Value::Int(_) | crate::Value::Float(_))
            && matches!(self.exchange.rateLimit, crate::Value::Int(2000)) {
            self.exchange.rateLimit = __described_rate_limit;
        }
        // Merge describe() options INTO whatever apply_config (or
        // earlier setup) already populated. Caller-supplied options
        // (e.g. portfolioMargin via fixture) take precedence, then
        // describe()'s defaults fill in any gaps.
        let __described_options = crate::get_value(&described, &crate::Value::Str("options".to_string()));
        // Capture the describe()'s explicit networksById BEFORE the merge
        // — the merge below overlays the base Exchange's options (whose
        // after_construct left an EMPTY networksById), which would
        // otherwise clobber the manual mappings like binance BSC to BEP20.
        let __described_networks_by_id = crate::get_value(&__described_options, &crate::Value::Str("networksById".to_string()));
        if let (crate::Value::Dict(existing), crate::Value::Dict(defaults)) =
            (&self.exchange.options.clone(), &__described_options)
        {
            let mut merged = (**defaults).clone();
            for (k, v) in existing.iter() { merged.insert(k.clone(), v.clone()); }
            self.exchange.options = crate::Value::Map(merged);
        } else if !matches!(self.exchange.options, crate::Value::Dict(_)) {
            self.exchange.options = __described_options;
        }
        // Derive options.networksById (CCXT's createNetworksByIdObject):
        // start from the auto-inverted networks (generated), then overlay
        // the describe()'s explicit networksById so manual mappings win —
        // mirrors TS extend(generated, manual). Without the manual
        // overlay, an exchange whose networks defines both BSC to BSC and
        // BEP20 to BSC would invert to BSC to BSC (wrong) by iteration
        // order instead of the intended BSC to BEP20.
        if let crate::Value::Dict(opts_arc) = self.exchange.options.clone() {
            let mut opts = std::sync::Arc::try_unwrap(opts_arc).unwrap_or_else(|a| (*a).clone());
            let mut by_id: indexmap::IndexMap<String, crate::Value> = indexmap::IndexMap::new();
            if let Some(crate::Value::Dict(networks)) = opts.get("networks") {
                for (code, id) in networks.iter() {
                    if let crate::Value::Str(id_s) = id {
                        by_id.entry(id_s.clone())
                             .or_insert_with(|| crate::Value::Str(code.clone()));
                    }
                }
            }
            if let crate::Value::Dict(manual) = &__described_networks_by_id {
                for (k, v) in manual.iter() { by_id.insert(k.clone(), v.clone()); }
            }
            opts.insert("networksById".to_string(), crate::Value::Map(by_id));
            self.exchange.options = crate::Value::Map(opts);
        }
        self.exchange.hostname = crate::get_value(&described, &crate::Value::Str("hostname".to_string()));
        self.exchange.version  = crate::get_value(&described, &crate::Value::Str("version".to_string()));
        self.exchange.id       = crate::get_value(&described, &crate::Value::Str("id".to_string()));
        self.exchange.name     = crate::get_value(&described, &crate::Value::Str("name".to_string()));
        self.exchange.exceptions = crate::get_value(&described, &crate::Value::Str("exceptions".to_string()));
        // Only override the base-default requiredCredentials when the
        // exchange's describe() actually provides them (super.describe()
        // is stubbed, so most exchanges' describe() omits this).
        { let __rc = crate::get_value(&described, &crate::Value::Str("requiredCredentials".to_string())); if !matches!(__rc, crate::Value::Null) { self.exchange.requiredCredentials = __rc; } }
        // Merge describe()'s commonCurrencies over the base defaults so
        // exchange-specific aliases (bitfinex UST to USDT, onetrading
        // MIOTA to IOTA) reach commonCurrencyCode / safeCurrencyCode.
        { let __cc = crate::get_value(&described, &crate::Value::Str("commonCurrencies".to_string())); if let crate::Value::Dict(extra) = __cc { let extra = std::sync::Arc::try_unwrap(extra).unwrap_or_else(|a| (*a).clone()); if let crate::Value::Dict(base) = &mut self.exchange.commonCurrencies { let base = std::sync::Arc::make_mut(base); for (k, v) in extra { base.insert(k, v); } } else { self.exchange.commonCurrencies = crate::Value::Map(extra); } } }
        self.exchange.precisionMode = crate::get_value(&described, &crate::Value::Str("precisionMode".to_string()));
        self.exchange.timeframes = crate::get_value(&described, &crate::Value::Str("timeframes".to_string()));
        self.exchange.fees = crate::get_value(&described, &crate::Value::Str("fees".to_string()));
        // `features` carries the describe() block that drives
        // unified-method tests (e.g. `features.spot.fetchCurrencies.private`
        // tells testFetchCurrencies to skip the length check). It's set
        // after `Exchange::new` because `features_generator` (in
        // `after_construct`) bails when `features == Null` — so we
        // assign and re-run the generator here.
        self.exchange.features = crate::get_value(&described, &crate::Value::Str("features".to_string()));
        if !matches!(self.exchange.features, crate::Value::Null) {
            self.exchange.features_generator();
        }
        // Markets and currencies may have been populated already by
        // the constructor config (test runners pass them in via
        // Exchange::new(Some(config-with-markets)) — same as CCXT TS).
        // Don't reset them here.
        self.exchange.build_implicit_api();
    }

    /// Re-bind the derived-exchange pointer. Needed if the struct's
    /// address moves after `new()` (e.g. `Box::new` the value).
    pub fn bind(&mut self) {
        let ptr: *const (dyn crate::exchange::DerivedExchange + 'static) =
            self as &Self as *const dyn crate::exchange::DerivedExchange;
        self.exchange.bind_derived(ptr);
        // Async dispatch hook: lets base Exchange methods route async
        // calls (cancel_order, fetch_order, …) to this Core's
        // call_dynamic, bypassing the base stubs.
        let core_ptr = self as *mut Self as *mut ();
        self.exchange.bind_call_async(core_ptr, OnetradingCore::__call_dynamic_dispatch);
    }

    /// Type-erased trampoline used by Exchange::dispatch_to_derived.
    /// Casts the void pointer back to `*mut Self` and forwards to the
    /// real `call_dynamic`. Safety: the pointer must come from the
    /// matching Core's bind() — guaranteed by Exchange::bind_call_async.
    pub fn __call_dynamic_dispatch<'a>(
        ptr: *mut (),
        method: &'a str,
        args: Vec<crate::Value>,
    ) -> std::pin::Pin<Box<dyn std::future::Future<Output = crate::Value> + 'a>> {
        Box::pin(async move {
            let s: &mut Self = unsafe { &mut *(ptr as *mut Self) };
            s.call_dynamic(method, args).await
        })
    }

    /// Dispatch by method name. Mirrors Go's CallInternal /
    /// C#'s callInternal — lets the CLI call any method without
    /// hard-coding signatures. Accepts the canonical snake_case name.
    /// Returns Value::Null for unknown names.
    pub async fn call_dynamic(&mut self, method: &str, args: Vec<crate::Value>) -> crate::Value {
        match method {
            "authenticate" => self.authenticate(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "find_timeframe" => self.find_timeframe(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "handle_authentication_message" => self.handle_authentication_message(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
            "handle_error_message" => self.handle_error_message(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
            "handle_heartbeat" => self.handle_heartbeat(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
            "handle_price_point_updates" => self.handle_price_point_updates(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
            "handle_subscriptions" => self.handle_subscriptions(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
            "parse_trading_order" => self.parse_trading_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "parse_trading_order_status" => self.parse_trading_order_status(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "parse_ws_order_status" => self.parse_ws_order_status(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "parse_ws_ticker" => self.parse_ws_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "watch_balance" => self.watch_balance(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_many" => self.watch_many(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), &args.get(3..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_my_trades" => self.watch_my_trades(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_ohlcv" => self.watch_ohlcv(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_order_book" => self.watch_order_book(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_orders" => self.watch_orders(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_ticker" => self.watch_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_tickers" => self.watch_tickers(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            // Go-style inheritance: an un-overridden method dispatches
            // to the parent exchange Core.
            _ => self.parent.call_dynamic(method, args).await,
        }
    }
}

impl crate::exchange::DerivedExchange for OnetradingCore {
    fn parse_ticker(&self, ticker: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_ticker(&self.parent, ticker, market)
    }
    fn parse_trade(&self, trade: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_trade(&self.parent, trade, market)
    }
    fn parse_order(&self, order: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_order(&self.parent, order, market)
    }
    fn parse_market(&self, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_market(&self.parent, market)
    }
    fn parse_ohlcv(&self, ohlcv: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_ohlcv(&self.parent, ohlcv, market)
    }
    fn parse_order_book(&self, ob: crate::Value, symbol: crate::Value, ts: crate::Value, bk: crate::Value, ak: crate::Value, pk: crate::Value, ak2: crate::Value, ck: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_order_book(&self.parent, ob, symbol, ts, bk, ak, pk, ak2, ck)
    }
    fn parse_balance(&self, response: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_balance(&self.parent, response)
    }
    fn parse_position(&self, position: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_position(&self.parent, position, market)
    }
    fn parse_funding_rate(&self, rate: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_funding_rate(&self.parent, rate, market)
    }
    fn parse_deposit(&self, tx: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_deposit(&self.parent, tx, currency)
    }
    fn parse_deposit_address(&self, depositAddress: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_deposit_address(&self.parent, depositAddress, currency)
    }
    fn parse_last_price(&self, entry: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_last_price(&self.parent, entry, market)
    }
    fn parse_withdrawal(&self, tx: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_withdrawal(&self.parent, tx, currency)
    }
    fn parse_ledger_entry(&self, entry: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_ledger_entry(&self.parent, entry, currency)
    }
    fn parse_transfer(&self, transfer: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_transfer(&self.parent, transfer, currency)
    }
    fn parse_currency(&self, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_currency(&self.parent, currency)
    }
    fn parse_bid_ask(&self, bidask: crate::Value, price_key: crate::Value, amount_key: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_bid_ask(&self.parent, bidask, price_key, amount_key, market)
    }
    fn parse_open_interest(&self, interest: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_open_interest(&self.parent, interest, market)
    }
    fn parse_liquidation(&self, liquidation: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_liquidation(&self.parent, liquidation, market)
    }
    fn parse_funding_rate_history(&self, entry: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_funding_rate_history(&self.parent, entry, market)
    }
    fn parse_margin_modification(&self, data: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_margin_modification(&self.parent, data, market)
    }
    fn parse_account(&self, account: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_account(&self.parent, account)
    }
    fn parse_my_trade(&self, trade: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_my_trade(&self.parent, trade, market)
    }
    fn parse_transaction(&self, transaction: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_transaction(&self.parent, transaction, currency)
    }
    fn parse_borrow_interest(&self, info: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_borrow_interest(&self.parent, info, market)
    }
    fn parse_adl_rank(&self, info: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_adl_rank(&self.parent, info, market)
    }
    fn parse_income(&self, info: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_income(&self.parent, info, market)
    }
    fn parse_greeks(&self, greeks: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_greeks(&self.parent, greeks, market)
    }
    fn parse_margin_mode(&self, margin_mode: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_margin_mode(&self.parent, margin_mode, market)
    }
    fn parse_conversion(&self, conversion: crate::Value, from_currency: crate::Value, to_currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_conversion(&self.parent, conversion, from_currency, to_currency)
    }
    fn parse_borrow_rate(&self, info: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_borrow_rate(&self.parent, info, currency)
    }
    fn parse_leverage(&self, leverage: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_leverage(&self.parent, leverage, market)
    }
    fn parse_market_leverage_tiers(&self, info: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_market_leverage_tiers(&self.parent, info, market)
    }
    fn parse_deposit_withdraw_fee(&self, fee: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_deposit_withdraw_fee(&self.parent, fee, currency)
    }
    fn parse_prediction_trade(&self, trade: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_prediction_trade(&self.parent, trade, market)
    }
    fn parse_prediction_order(&self, order: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_prediction_order(&self.parent, order, market)
    }
    fn parse_prediction_position(&self, position: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_prediction_position(&self.parent, position, market)
    }
    fn create_expired_option_market(&self, symbol: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::create_expired_option_market(&self.parent, symbol)
    }
    fn sign(&self, path: crate::Value, api: crate::Value, method: crate::Value, params: crate::Value, headers: crate::Value, body: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::sign(&self.parent, path, api, method, params, headers, body)
    }
    fn handle_errors(&self, code: crate::Value, reason: crate::Value, url: crate::Value, method: crate::Value, headers: crate::Value, body: crate::Value, response: crate::Value, request_headers: crate::Value, request_body: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::handle_errors(&self.parent, code, reason, url, method, headers, body, response, request_headers, request_body)
    }
}

impl std::ops::Deref for OnetradingCore {
    type Target = crate::exchanges::onetrading::OnetradingCore;
    fn deref(&self) -> &Self::Target { &self.parent }
}

impl std::ops::DerefMut for OnetradingCore {
    fn deref_mut(&mut self) -> &mut Self::Target { &mut self.parent }
}

impl OnetradingCore {
    pub fn describe(&self) -> Value {
        return self.deep_extend(self.parent.describe(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("has".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("ws".to_string(), Value::Bool(true));
        m.insert("watchBalance".to_string(), Value::Bool(true));
        m.insert("watchTicker".to_string(), Value::Bool(true));
        m.insert("watchTickers".to_string(), Value::Bool(true));
        m.insert("watchTrades".to_string(), Value::Bool(false));
        m.insert("watchTradesForSymbols".to_string(), Value::Bool(false));
        m.insert("watchMyTrades".to_string(), Value::Bool(true));
        m.insert("watchOrders".to_string(), Value::Bool(true));
        m.insert("watchOrderBook".to_string(), Value::Bool(true));
        m.insert("watchOHLCV".to_string(), Value::Bool(true));
    m
}));
        m.insert("urls".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("api".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("ws".to_string(), Value::Str("wss://streams.onetrading.com/".to_string()));
    m
}));
    m
}));
        m.insert("options".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("bp_remaining_quota".to_string(), Value::Int(200));
        m.insert("timeframes".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("1m".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("unit".to_string(), Value::Str("MINUTES".to_string()));
        m.insert("period".to_string(), Value::Int(1));
    m
}));
        m.insert("5m".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("unit".to_string(), Value::Str("MINUTES".to_string()));
        m.insert("period".to_string(), Value::Int(5));
    m
}));
        m.insert("15m".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("unit".to_string(), Value::Str("MINUTES".to_string()));
        m.insert("period".to_string(), Value::Int(15));
    m
}));
        m.insert("30m".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("unit".to_string(), Value::Str("MINUTES".to_string()));
        m.insert("period".to_string(), Value::Int(30));
    m
}));
        m.insert("1h".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("unit".to_string(), Value::Str("HOURS".to_string()));
        m.insert("period".to_string(), Value::Int(1));
    m
}));
        m.insert("4h".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("unit".to_string(), Value::Str("HOURS".to_string()));
        m.insert("period".to_string(), Value::Int(4));
    m
}));
        m.insert("1d".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("unit".to_string(), Value::Str("DAYS".to_string()));
        m.insert("period".to_string(), Value::Int(1));
    m
}));
        m.insert("1w".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("unit".to_string(), Value::Str("WEEKS".to_string()));
        m.insert("period".to_string(), Value::Int(1));
    m
}));
        m.insert("1M".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("unit".to_string(), Value::Str("MONTHS".to_string()));
        m.insert("period".to_string(), Value::Int(1));
    m
}));
    m
}));
    m
}));
        m.insert("streaming".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        m.insert("exceptions".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
    m
})]);

    Value::Null
}

/*
 * @method
 * @name onetrading#watchBalance
 * @see https://developers.bitpanda.com/exchange/#account-history-channel
 * @description watch balance and get the amount of funds available for trading or funds locked in orders
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [balance structure]{@link https://docs.ccxt.com/?id=balance-structure}
 */
    pub async fn watch_balance(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.authenticate(&[params.clone()]).await;
        let mut url: Value = get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string()));
        let mut messageHash: Value = Value::Str("balance".to_string());
        let mut subscribeHash: Value = Value::Str("ACCOUNT_HISTORY".to_string());
        let mut bpRemainingQuota: Value = self.safe_integer_k(self.options.clone(), "bp_remaining_quota", &[Value::Int(200)]);
        let mut subscribe: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("type".to_string(), Value::Str("SUBSCRIBE".to_string()));
                m.insert("bp_remaining_quota".to_string(), bpRemainingQuota.clone());
                m.insert("channels".to_string(), Value::List(vec![Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("ACCOUNT_HISTORY".to_string()));
    m
})]));
            m
        });
        let mut request: Value = self.deep_extend(subscribe.clone(), &[params.clone()]);
        return self.watch(url.clone(), messageHash.clone(), &[request.clone(), subscribeHash.clone(), request.clone()]).await;

    Value::Null
}

    pub fn handle_balance_snapshot(&mut self, mut client: Value, mut message: Value) {
        //
        // snapshot
        //     {
        //         "account_id": "b355abb8-aaae-4fae-903c-c60ff74723c6",
        //         "type": "BALANCES_SNAPSHOT",
        //         "channel_name": "ACCOUNT_HISTORY",
        //         "time": "2019-04-01T13:39:17.155Z",
        //         "balances": [{
        //                 "account_id": "b355abb8-aaae-4fae-903c-c60ff74723c6",
        //                 "currency_code": "BTC",
        //                 "change": "0.5",
        //                 "available": "10.0",
        //                 "locked": "1.1234567",
        //                 "sequence": 1,
        //                 "time": "2019-04-01T13:39:17.155Z"
        //             },
        //             {
        //                 "account_id": "b355abb8-aaae-4fae-903c-c60ff74723c6",
        //                 "currency_code": "ETH",
        //                 "change": "0.5",
        //                 "available": "10.0",
        //                 "locked": "1.1234567",
        //                 "sequence": 2,
        //                 "time": "2019-04-01T13:39:17.155Z"
        //             }
        //         ]
        //     }
        //
        { let __t = self.parse_balance(message.clone()); self.balance = __t; }
        let mut messageHash: Value = Value::Str("balance".to_string());
        client.resolve(&[self.balance.clone(), messageHash.clone()]);
}

/*
 * @method
 * @name onetrading#watchTicker
 * @see https://developers.bitpanda.com/exchange/#market-ticker-channel
 * @description watches a price ticker, a statistical calculation with the information calculated over the past 24 hours for a specific market
 * @param {string} symbol unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
    pub async fn watch_ticker(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol.clone());
        symbol = get_value(&market, &Value::Str("symbol".to_string()));
        let mut subscriptionHash: Value = Value::Str("MARKET_TICKER".to_string());
        let mut messageHash: Value = add(&Value::Str("ticker.".to_string()), &symbol);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("type".to_string(), Value::Str("SUBSCRIBE".to_string()));
                m.insert("channels".to_string(), Value::List(vec![Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("MARKET_TICKER".to_string()));
        m.insert("price_points_mode".to_string(), Value::Str("INLINE".to_string()));
    m
})]));
            m
        });
        return self.watch_many(messageHash.clone(), request.clone(), subscriptionHash.clone(), &[Value::List(vec![symbol.clone()]), params.clone()]).await;

    Value::Null
}

/*
 * @method
 * @name onetrading#watchTickers
 * @see https://developers.bitpanda.com/exchange/#market-ticker-channel
 * @description watches price tickers, a statistical calculation with the information for all markets or those specified.
 * @param {string} symbols unified symbols of the markets to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an array of [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
    pub async fn watch_tickers(&mut self, optional_args: &[Value]) -> Value {
        let mut symbols = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        symbols = self.market_symbols(&[symbols.clone()]);
        if is_equal(&symbols, &Value::Null) {
            symbols = Value::List(vec![]);
        }
        let mut subscriptionHash: Value = Value::Str("MARKET_TICKER".to_string());
        let mut messageHash: Value = Value::Str("tickers".to_string());
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("type".to_string(), Value::Str("SUBSCRIBE".to_string()));
                m.insert("channels".to_string(), Value::List(vec![Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("MARKET_TICKER".to_string()));
        m.insert("price_points_mode".to_string(), Value::Str("INLINE".to_string()));
    m
})]));
            m
        });
        let mut tickers: Value = self.watch_many(messageHash.clone(), request.clone(), subscriptionHash.clone(), &[symbols.clone(), params.clone()]).await;
        return self.filter_by_array(tickers.clone(), Value::Str("symbol".to_string()), &[symbols.clone()]);

    Value::Null
}

    pub fn handle_ticker(&self, mut client: Value, mut message: Value) {
        //
        //     {
        //         "ticker_updates": [{
        //             "instrument": "ETH_BTC",
        //             "last_price": "0.053752",
        //             "price_change": "0.000623",
        //             "price_change_percentage": "1.17",
        //             "high": "0.055",
        //             "low": "0.052662",
        //             "volume": "6.3821593247"
        //         }],
        //         "channel_name": "MARKET_TICKER",
        //         "type": "MARKET_TICKER_UPDATES",
        //         "time": "2022-06-23T16:41:00.004162Z"
        //     }
        //
        let mut tickers: Value = self.safe_value_k(message.clone(), "ticker_updates", &[Value::List(vec![])]);
        let mut datetime: Value = self.safe_string_k(message.clone(), "time", &[]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_544: bool = true;
            while { if !__for_first_544 { i = add(&i, &Value::Int(1)); } __for_first_544 = false; is_less_than(&i, &get_array_length(&tickers)) } {
            let mut ticker: Value = get_value(&tickers, &i);
            let mut ticker: Value = get_value(&tickers, &i);
            let mut marketId: Value = self.safe_string_k(ticker.clone(), "instrument", &[]);
            let mut symbol: Value = self.safe_symbol(marketId.clone(), &[]);
            { let __be_tmp = self.parse_ws_ticker(ticker.clone(), &[]); add_element_to_object(&mut self.tickers.clone(), &symbol, __be_tmp); };
            let mut timestamp: Value = self.parse8601(datetime.clone());
            add_element_to_object(get_value_mut(unsafe { crate::runtime::coerce_value_to_mut(&self.tickers) }, &symbol), &Value::Str("timestamp".to_string()), timestamp.clone());
            { let __be_tmp = self.iso8601(timestamp.clone()); add_element_to_object(get_value_mut(unsafe { crate::runtime::coerce_value_to_mut(&self.tickers) }, &symbol), &Value::Str("datetime".to_string()), __be_tmp); };
            client.resolve(&[get_value(&self.tickers, &symbol), add(&Value::Str("ticker.".to_string()), &symbol)]);
        }
        }
        client.resolve(&[self.tickers.clone(), Value::Str("tickers".to_string())]);
}

    pub fn parse_ws_ticker(&self, mut ticker: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        //     {
        //         "instrument": "ETH_BTC",
        //         "last_price": "0.053752",
        //         "price_change": "-0.000623",
        //         "price_change_percentage": "-1.17",
        //         "high": "0.055",
        //         "low": "0.052662",
        //         "volume": "6.3821593247"
        //     }
        //
        let mut marketId: Value = self.safe_string_k(ticker.clone(), "instrument", &[]);
        return self.safe_ticker(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("symbol".to_string(), self.safe_symbol(marketId.clone(), &[market.clone()]));
        m.insert("timestamp".to_string(), Value::Null);
        m.insert("datetime".to_string(), Value::Null);
        m.insert("high".to_string(), self.safe_string_k(ticker.clone(), "high", &[]));
        m.insert("low".to_string(), self.safe_string_k(ticker.clone(), "low", &[]));
        m.insert("bid".to_string(), Value::Null);
        m.insert("bidVolume".to_string(), Value::Null);
        m.insert("ask".to_string(), Value::Null);
        m.insert("askVolume".to_string(), Value::Null);
        m.insert("vwap".to_string(), Value::Null);
        m.insert("open".to_string(), Value::Null);
        m.insert("close".to_string(), self.safe_string_k(ticker.clone(), "last_price", &[]));
        m.insert("last".to_string(), self.safe_string_k(ticker.clone(), "last_price", &[]));
        m.insert("previousClose".to_string(), Value::Null);
        m.insert("change".to_string(), self.safe_string_k(ticker.clone(), "price_change", &[]));
        m.insert("percentage".to_string(), self.safe_string_k(ticker.clone(), "price_change_percentage", &[]));
        m.insert("average".to_string(), Value::Null);
        m.insert("baseVolume".to_string(), Value::Null);
        m.insert("quoteVolume".to_string(), self.safe_number_k(ticker.clone(), "volume", &[]));
        m.insert("info".to_string(), ticker.clone());
    m
}), &[market.clone()]);

    Value::Null
}

/*
 * @method
 * @name onetrading#watchMyTrades
 * @see https://developers.bitpanda.com/exchange/#account-history-channel
 * @description get the list of trades associated with the user
 * @param {string} symbol unified symbol of the market to fetch trades for. Use 'any' to watch all trades
 * @param {int} [since] timestamp in ms of the earliest trade to fetch
 * @param {int} [limit] the maximum amount of trades to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=public-trades}
 */
    pub async fn watch_my_trades(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut messageHash: Value = Value::Str("myTrades".to_string());
        if !is_equal(&symbol, &Value::Null) {
            let mut market: Value = self.market(symbol.clone());
            symbol = get_value(&market, &Value::Str("symbol".to_string()));
            messageHash = add(&messageHash, &add(&Value::Str(":".to_string()), &symbol));
        }
        self.authenticate(&[params.clone()]).await;
        let mut url: Value = get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string()));
        let mut subscribeHash: Value = Value::Str("ACCOUNT_HISTORY".to_string());
        let mut bpRemainingQuota: Value = self.safe_integer_k(self.options.clone(), "bp_remaining_quota", &[Value::Int(200)]);
        let mut subscribe: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("type".to_string(), Value::Str("SUBSCRIBE".to_string()));
                m.insert("bp_remaining_quota".to_string(), bpRemainingQuota.clone());
                m.insert("channels".to_string(), Value::List(vec![Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("ACCOUNT_HISTORY".to_string()));
    m
})]));
            m
        });
        let mut request: Value = self.deep_extend(subscribe.clone(), &[params.clone()]);
        let mut trades: Value = self.watch(url.clone(), messageHash.clone(), &[request.clone(), subscribeHash.clone(), request.clone()]).await;
        if is_true(&self.newUpdates) {
            limit = trades.get_limit(symbol.clone(), limit.clone());
        }
        trades = self.filter_by_symbol_since_limit(trades.clone(), &[symbol.clone(), since.clone(), limit.clone()]);
        let mut numTrades: Value = get_array_length(&trades);
        if is_equal(&numTrades, &Value::Int(0)) {
            return Box::pin(self.watch_my_trades(&[symbol.clone(), since.clone(), limit.clone(), params.clone()])).await;
        }
        return trades;

    Value::Null
}

/*
 * @method
 * @name onetrading#watchOrderBook
 * @see https://developers.bitpanda.com/exchange/#market-ticker-channel
 * @description watches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
 * @param {string} symbol unified symbol of the market to fetch the order book for
 * @param {int} [limit] the maximum amount of order book entries to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} A dictionary of [order book structures]{@link https://docs.ccxt.com/?id=order-book-structure}
 */
    pub async fn watch_order_book(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut limit = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol.clone());
        symbol = get_value(&market, &Value::Str("symbol".to_string()));
        let mut messageHash: Value = add(&Value::Str("book:".to_string()), &symbol);
        let mut subscriptionHash: Value = Value::Str("ORDER_BOOK".to_string());
        let mut depth: Value = Value::Int(0);
        if !is_equal(&limit, &Value::Null) {
            depth = limit.clone();
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("type".to_string(), Value::Str("SUBSCRIBE".to_string()));
                m.insert("channels".to_string(), Value::List(vec![Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("ORDER_BOOK".to_string()));
        m.insert("depth".to_string(), depth.clone());
    m
})]));
            m
        });
        let mut orderbook: Value = self.watch_many(messageHash.clone(), request.clone(), subscriptionHash.clone(), &[Value::List(vec![symbol.clone()]), params.clone()]).await;
        return orderbook.limit();

    Value::Null
}

    pub fn handle_order_book(&self, mut client: Value, mut message: Value) {
        //
        //  snapshot
        //     {
        //         "instrument_code": "ETH_BTC",
        //         "bids": [
        //             ['0.053595', "4.5352"],
        //             ...
        //         ],
        //         "asks": [
        //             ['0.055455', "0.2821"],
        //             ...
        //         ],
        //         "channel_name": "ORDER_BOOK",
        //         "type": "ORDER_BOOK_SNAPSHOT",
        //         "time": "2022-06-23T15:38:02.196282Z"
        //     }
        //
        //  update
        //     {
        //         "instrument_code": "ETH_BTC",
        //         "changes": [
        //             ["BUY", '0.053593', "8.0587"]
        //         ],
        //         "channel_name": "ORDER_BOOK",
        //         "type": "ORDER_BOOK_UPDATE",
        //         "time": "2022-06-23T15:38:02.751301Z"
        //     }
        //
        let mut type_var: Value = self.safe_string_k(message.clone(), "type", &[]);
        let mut marketId: Value = self.safe_string_k(message.clone(), "instrument_code", &[]);
        let mut symbol: Value = self.safe_symbol(marketId.clone(), &[]);
        let mut dateTime: Value = self.safe_string_k(message.clone(), "time", &[]);
        let mut timestamp: Value = self.parse8601(dateTime.clone());
        let mut channel: Value = add(&Value::Str("book:".to_string()), &symbol);
        let mut orderbook: Value = self.safe_value(self.orderbooks.clone(), symbol.clone(), &[]);
        if is_equal(&orderbook, &Value::Null) {
            orderbook = self.order_book(&[Value::Map({
                let mut m = indexmap::IndexMap::new();
                m
            })]);
        }
        if is_equal(&type_var, &Value::Str("ORDER_BOOK_SNAPSHOT".to_string())) {
            let mut snapshot: Value = self.parse_order_book(message.clone(), symbol.clone(), &[timestamp.clone(), Value::Str("bids".to_string()), Value::Str("asks".to_string())]);
            orderbook.reset(snapshot.clone());
        }  else if is_equal(&type_var, &Value::Str("ORDER_BOOK_UPDATE".to_string())) {
            let mut changes: Value = self.safe_value_k(message.clone(), "changes", &[Value::List(vec![])]);
            self.handle_deltas(orderbook.clone(), changes.clone());
        }  else {
            panic!("{}", crate::exchange_errors::not_supported(add(&add(&self.id, &Value::Str(" watchOrderBook() did not recognize message type ".to_string())), &type_var)));
        }
        add_element_to_object(&mut orderbook, &Value::Str("nonce".to_string()), timestamp.clone());
        add_element_to_object(&mut orderbook, &Value::Str("timestamp".to_string()), timestamp.clone());
        add_element_to_object(&mut orderbook, &Value::Str("datetime".to_string()), self.iso8601(timestamp.clone()));
        add_element_to_object(&mut self.orderbooks.clone(), &symbol, orderbook.clone());
        client.resolve(&[orderbook.clone(), channel.clone()]);
}

    pub fn handle_delta(&self, mut orderbook: Value, mut delta: Value) {
        //
        //   [ 'BUY', "0.053595", "0" ]
        //
        let mut bidAsk: Value = self.parse_order_book_bid_ask(delta.clone(), &[Value::Int(1), Value::Int(2)]);
        let mut type_var: Value = self.safe_string(delta.clone(), Value::Int(0), &[]);
        if is_equal(&type_var, &Value::Str("BUY".to_string())) {
            let mut bids: Value = get_value(&orderbook, &Value::Str("bids".to_string()));
            bids.store_array(bidAsk.clone());
        }  else if is_equal(&type_var, &Value::Str("SELL".to_string())) {
            let mut asks: Value = get_value(&orderbook, &Value::Str("asks".to_string()));
            asks.store_array(bidAsk.clone());
        }  else {
            panic!("{}", crate::exchange_errors::not_supported(add(&add(&self.id, &Value::Str(" watchOrderBook () received unknown change type ".to_string())), &self.json(delta.clone()))));
        }
}

    pub fn handle_deltas(&self, mut orderbook: Value, mut deltas: Value) {
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_545: bool = true;
            while { if !__for_first_545 { i = add(&i, &Value::Int(1)); } __for_first_545 = false; is_less_than(&i, &get_array_length(&deltas)) } {
            self.handle_delta(orderbook.clone(), get_value(&deltas, &i));
        }
        }
}

/*
 * @method
 * @name onetrading#watchOrders
 * @see https://developers.bitpanda.com/exchange/#account-history-channel
 * @description watches information on multiple orders made by the user
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.channel] can listen to orders using ACCOUNT_HISTORY or TRADING
 * @returns {object[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn watch_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut messageHash: Value = Value::Str("orders".to_string());
        if !is_equal(&symbol, &Value::Null) {
            let mut market: Value = self.market(symbol.clone());
            symbol = get_value(&market, &Value::Str("symbol".to_string()));
            messageHash = add(&messageHash, &add(&Value::Str(":".to_string()), &symbol));
        }
        self.authenticate(&[params.clone()]).await;
        let mut url: Value = get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string()));
        let mut subscribeHash: Value = self.safe_string_k(params.clone(), "channel", &[Value::Str("ACCOUNT_HISTORY".to_string())]);
        let mut bpRemainingQuota: Value = self.safe_integer_k(self.options.clone(), "bp_remaining_quota", &[Value::Int(200)]);
        let mut subscribe: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("type".to_string(), Value::Str("SUBSCRIBE".to_string()));
                m.insert("bp_remaining_quota".to_string(), bpRemainingQuota.clone());
                m.insert("channels".to_string(), Value::List(vec![Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), subscribeHash.clone());
    m
})]));
            m
        });
        let mut request: Value = self.deep_extend(subscribe.clone(), &[params.clone()]);
        let mut orders: Value = self.watch(url.clone(), messageHash.clone(), &[request.clone(), subscribeHash.clone(), request.clone()]).await;
        if is_true(&self.newUpdates) {
            limit = orders.get_limit(symbol.clone(), limit.clone());
        }
        orders = self.filter_by_symbol_since_limit(orders.clone(), &[symbol.clone(), since.clone(), limit.clone()]);
        let mut numOrders: Value = get_array_length(&orders);
        if is_equal(&numOrders, &Value::Int(0)) {
            return Box::pin(self.watch_orders(&[symbol.clone(), since.clone(), limit.clone(), params.clone()])).await;
        }
        return orders;

    Value::Null
}

    pub fn handle_trading(&mut self, mut client: Value, mut message: Value) {
        //
        //     {
        //         "order_book_sequence": 892925263,
        //         "side": "BUY",
        //         "amount": "0.00046",
        //         "trade_id": "d67b9b69-ab76-480f-9ba3-b33582202836",
        //         "matched_as": "TAKER",
        //         "matched_amount": "0.00046",
        //         "matched_price": "22231.08",
        //         "instrument_code": "BTC_EUR",
        //         "order_id": "7b39f316-0a71-4bfd-adda-3062e6f0bd37",
        //         "remaining": "0.0",
        //         "channel_name": "TRADING",
        //         "type": "FILL",
        //         "time": "2022-07-21T12:41:22.883341Z"
        //     }
        //
        //     {
        //         "status": "CANCELLED",
        //         "order_book_sequence": 892928424,
        //         "amount": "0.0003",
        //         "side": "SELL",
        //         "price": "50338.65",
        //         "instrument_code": "BTC_EUR",
        //         "order_id": "b3994a08-a9e8-4a79-a08b-33e3480382df",
        //         "remaining": "0.0003",
        //         "channel_name": "TRADING",
        //         "type": "DONE",
        //         "time": "2022-07-21T12:44:24.267000Z"
        //     }
        //
        //     {
        //         "order_book_sequence": 892934476,
        //         "side": "SELL",
        //         "amount": "0.00051",
        //         "price": "22349.02",
        //         "instrument_code": "BTC_EUR",
        //         "order_id": "1c6c585c-ec3d-4b94-9292-6c3d04a31dc8",
        //         "remaining": "0.00051",
        //         "channel_name": "TRADING",
        //         "type": "BOOKED",
        //         "time": "2022-07-21T12:50:10.093000Z"
        //     }
        //
        if is_equal(&self.orders, &Value::Null) {
            let mut limit: Value = self.safe_integer_k(self.options.clone(), "ordersLimit", &[Value::Int(1000)]);
            self.orders = ArrayCacheBySymbolById::new(limit.clone());
        }
        let mut order: Value = self.parse_trading_order(message.clone(), &[]);
        let mut orders: Value = self.orders.clone();
        orders.append(order.clone());
        client.resolve(&[self.orders.clone(), add(&Value::Str("orders:".to_string()), &get_value(&order, &Value::Str("symbol".to_string())))]);
        client.resolve(&[self.orders.clone(), Value::Str("orders".to_string())]);
}

    pub fn parse_trading_order(&self, mut order: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        //     {
        //         "order_book_sequence": 892925263,
        //         "side": "BUY",
        //         "amount": "0.00046",
        //         "trade_id": "d67b9b69-ab76-480f-9ba3-b33582202836",
        //         "matched_as": "TAKER",
        //         "matched_amount": "0.00046",
        //         "matched_price": "22231.08",
        //         "instrument_code": "BTC_EUR",
        //         "order_id": "7b39f316-0a71-4bfd-adda-3062e6f0bd37",
        //         "remaining": "0.0",
        //         "channel_name": "TRADING",
        //         "type": "FILL",
        //         "time": "2022-07-21T12:41:22.883341Z"
        //     }
        //
        //     {
        //         "status": "CANCELLED",
        //         "order_book_sequence": 892928424,
        //         "amount": "0.0003",
        //         "side": "SELL",
        //         "price": "50338.65",
        //         "instrument_code": "BTC_EUR",
        //         "order_id": "b3994a08-a9e8-4a79-a08b-33e3480382df",
        //         "remaining": "0.0003",
        //         "channel_name": "TRADING",
        //         "type": "DONE",
        //         "time": "2022-07-21T12:44:24.267000Z"
        //     }
        //
        //     {
        //         "order_book_sequence": 892934476,
        //         "side": "SELL",
        //         "amount": "0.00051",
        //         "price": "22349.02",
        //         "instrument_code": "BTC_EUR",
        //         "order_id": "1c6c585c-ec3d-4b94-9292-6c3d04a31dc8",
        //         "remaining": "0.00051",
        //         "channel_name": "TRADING",
        //         "type": "BOOKED",
        //         "time": "2022-07-21T12:50:10.093000Z"
        //     }
        //
        //     {
        //         "type":"UPDATE",
        //         "channel_name": "TRADING",
        //         "instrument_code": "BTC_EUR",
        //         "order_id": "1e842f13-762a-4745-9f3b-07f1b43e7058",
        //         "client_id": "d75fb03b-b599-49e9-b926-3f0b6d103206",
        //         "time": "2020-01-11T01:01:01.999Z",
        //         "remaining": "1.23456",
        //         "order_book_sequence": 42,
        //         "status": "APPLIED",
        //         "amount": "1.35756",
        //         "amount_delta": "0.123",
        //         "modification_id": "cc0eed67-aecc-4fb4-a625-ff3890ceb4cc"
        //     }
        //  tracked
        //     {
        //         "type": "STOP_TRACKED",
        //         "channel_name": "TRADING",
        //         "instrument_code": "BTC_EUR",
        //         "order_id": "1e842f13-762a-4745-9f3b-07f1b43e7058",
        //         "client_id": "d75fb03b-b599-49e9-b926-3f0b6d103206",
        //         "time": "2020-01-11T01:01:01.999Z",
        //         "remaining": "1.23456",
        //         "order_book_sequence": 42,
        //         "trigger_price": "12345.67",
        //         "current_price": "11111.11"
        //     }
        //
        //     {
        //         "type": "STOP_TRIGGERED",
        //         "channel_name": "TRADING",
        //         "instrument_code": "BTC_EUR",
        //         "order_id": "1e842f13-762a-4745-9f3b-07f1b43e7058",
        //         "client_id": "d75fb03b-b599-49e9-b926-3f0b6d103206",
        //         "time": "2020-01-11T01:01:01.999Z",
        //         "remaining": "1.23456",
        //         "order_book_sequence": 42,
        //         "price": "13333.33"
        //     }
        //
        let mut datetime: Value = self.safe_string_k(order.clone(), "time", &[]);
        let mut marketId: Value = self.safe_string_k(order.clone(), "instrument_code", &[]);
        let mut symbol: Value = self.safe_symbol(marketId.clone(), &[market.clone(), Value::Str("_".to_string())]);
        return self.safe_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), self.safe_string_k(order.clone(), "order_id", &[]));
        m.insert("clientOrderId".to_string(), self.safe_string_k(order.clone(), "client_id", &[]));
        m.insert("info".to_string(), order.clone());
        m.insert("timestamp".to_string(), self.parse8601(datetime.clone()));
        m.insert("datetime".to_string(), datetime.clone());
        m.insert("lastTradeTimestamp".to_string(), Value::Null);
        m.insert("symbol".to_string(), symbol.clone());
        m.insert("type".to_string(), Value::Null);
        m.insert("timeInForce".to_string(), Value::Null);
        m.insert("postOnly".to_string(), Value::Null);
        m.insert("side".to_string(), self.safe_string_lower(order.clone(), Value::Str("side".to_string()), &[]));
        m.insert("price".to_string(), self.safe_number2(order.clone(), Value::Str("price".to_string()), Value::Str("matched_price".to_string()), &[]));
        m.insert("stopPrice".to_string(), self.safe_number_k(order.clone(), "trigger_price", &[]));
        m.insert("amount".to_string(), self.safe_number_k(order.clone(), "amount", &[]));
        m.insert("cost".to_string(), Value::Null);
        m.insert("average".to_string(), Value::Null);
        m.insert("filled".to_string(), Value::Null);
        m.insert("remaining".to_string(), self.safe_string_k(order.clone(), "remaining", &[]));
        m.insert("status".to_string(), self.parse_trading_order_status(self.safe_string_k(order.clone(), "status", &[])));
        m.insert("fee".to_string(), Value::Null);
        m.insert("trades".to_string(), Value::Null);
    m
}), &[market.clone()]);

    Value::Null
}

    pub fn parse_trading_order_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("CANCELLED".to_string(), Value::Str("canceled".to_string()));
                m.insert("SELF_TRADE".to_string(), Value::Str("rejected".to_string()));
                m.insert("FILLED_FULLY".to_string(), Value::Str("closed".to_string()));
                m.insert("INSUFFICIENT_FUNDS".to_string(), Value::Str("rejected".to_string()));
                m.insert("INSUFFICIENT_LIQUIDITY".to_string(), Value::Str("rejected".to_string()));
                m.insert("TIME_TO_MARKET_EXCEEDED".to_string(), Value::Str("rejected".to_string()));
                m.insert("LAST_PRICE_UNKNOWN".to_string(), Value::Str("rejected".to_string()));
            m
        });
        return self.safe_string(statuses.clone(), status.clone(), &[status.clone()]);

    Value::Null
}

    pub fn handle_orders(&mut self, mut client: Value, mut message: Value) {
        //
        //  snapshot
        //     {
        //         "account_id": "4920221a-48dc-423e-b336-bb65baccc7bd",
        //         "orders": [{
        //             "order": {
        //                 "order_id": "30e2de8f-9a34-472f-bcf8-3af4b7757626",
        //                 "account_holder": "49202c1a-48dc-423e-b336-bb65baccc7bd",
        //                 "account_id": "49202c1a-48dc-423e-b336-bb65baccc7bd",
        //                 "instrument_code": "BTC_EUR",
        //                 "time": "2022-06-28T06:10:02.587345Z",
        //                 "side": "SELL",
        //                 "price": "19645.48",
        //                 "amount": "0.00052",
        //                 "filled_amount": "0.00052",
        //                 "type": "MARKET",
        //                 "sequence": 7633339971,
        //                 "status": "FILLED_FULLY",
        //                 "average_price": "19645.48",
        //                 "is_post_only": false,
        //                 "order_book_sequence": 866885897,
        //                 "time_last_updated": "2022-06-28T06:10:02.766983Z",
        //                 "update_modification_sequence": 866885897
        //             },
        //             "trades": [{
        //                 "fee": {
        //                     "fee_amount": "0.01532347",
        //                     "fee_currency": "EUR",
        //                     "fee_percentage": "0.15",
        //                     "fee_group_id": "default",
        //                     "fee_type": "TAKER",
        //                     "running_trading_volume": "0.0",
        //                     "collection_type": "STANDARD"
        //                 },
        //                 "trade": {
        //                     "trade_id": "d83e302e-0b3a-4269-aa7d-ecf007cbe577",
        //                     "order_id": "30e2de8f-9a34-472f-bcf8-3af4b7757626",
        //                     "account_holder": "49203c1a-48dc-423e-b336-bb65baccc7bd",
        //                     "account_id": "4920221a-48dc-423e-b336-bb65baccc7bd",
        //                     "amount": "0.00052",
        //                     "side": "SELL",
        //                     "instrument_code": "BTC_EUR",
        //                     "price": "19645.48",
        //                     "time": "2022-06-28T06:10:02.693246Z",
        //                     "price_tick_sequence": 0,
        //                     "sequence": 7633339971
        //                 }
        //             }]
        //         }],
        //         "channel_name": "ACCOUNT_HISTORY",
        //         "type": "INACTIVE_ORDERS_SNAPSHOT",
        //         "time": "2022-06-28T06:11:52.469242Z"
        //     }
        //
        //
        if is_equal(&self.orders, &Value::Null) {
            let mut limit: Value = self.safe_integer_k(self.options.clone(), "ordersLimit", &[Value::Int(1000)]);
            self.orders = ArrayCacheBySymbolById::new(limit.clone());
        }
        if is_equal(&self.myTrades, &Value::Null) {
            let mut limit: Value = self.safe_integer_k(self.options.clone(), "tradesLimit", &[Value::Int(1000)]);
            self.myTrades = ArrayCacheBySymbolById::new(limit.clone());
        }
        let mut rawOrders: Value = self.safe_value_k(message.clone(), "orders", &[Value::List(vec![])]);
        let mut rawOrdersLength: Value = get_array_length(&rawOrders);
        if is_equal(&rawOrdersLength, &Value::Int(0)) {
            return;
        }
        let mut orders: Value = self.orders.clone();
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_547: bool = true;
            while { if !__for_first_547 { i = add(&i, &Value::Int(1)); } __for_first_547 = false; is_less_than(&i, &get_array_length(&rawOrders)) } {
            let mut order: Value = self.parse_order(get_value(&rawOrders, &i), &[]);
            let mut symbol: Value = self.safe_string_k(order.clone(), "symbol", &[Value::Str("".to_string())]);
            orders.append(order.clone());
            client.resolve(&[self.orders.clone(), add(&Value::Str("orders:".to_string()), &symbol)]);
            let mut rawTrades: Value = self.safe_value_k(get_value(&rawOrders, &i), "trades", &[Value::List(vec![])]);
            {
                                let mut ii: Value = Value::Int(0);
                let mut __for_first_546: bool = true;
                while { if !__for_first_546 { ii = add(&ii, &Value::Int(1)); } __for_first_546 = false; is_less_than(&ii, &get_array_length(&rawTrades)) } {
                let mut trade: Value = self.parse_trade(get_value(&rawTrades, &ii), &[]);
                symbol = self.safe_string_k(trade.clone(), "symbol", &[symbol.clone()]);
                self.myTrades.append(trade.clone());
                client.resolve(&[self.myTrades.clone(), add(&Value::Str("myTrades:".to_string()), &symbol)]);
            }
            }
        }
        }
        client.resolve(&[self.orders.clone(), Value::Str("orders".to_string())]);
        client.resolve(&[self.myTrades.clone(), Value::Str("myTrades".to_string())]);
}

    pub fn handle_account_update(&mut self, mut client: Value, mut message: Value) {
        //
        // order created
        //     {
        //         "account_id": "49302c1a-48dc-423e-b336-bb65baccc7bd",
        //         "sequence": 7658332018,
        //         "update": {
        //             "type": "ORDER_CREATED",
        //             "activity": "TRADING",
        //             "account_holder": "43202c1a-48dc-423e-b336-bb65baccc7bd",
        //             "account_id": "49202c1a-48dc-423e-b336-bb65baccc7bd",
        //             "order_id": "8893fd69-5ebd-496b-aaa4-269b4c18aa77",
        //             "time": "2022-06-29T04:33:29.661257Z",
        //             "order": {
        //                 "time_in_force": "GOOD_TILL_CANCELLED",
        //                 "is_post_only": false,
        //                 "order_id": "8892fd69-5ebd-496b-aaa4-269b4c18aa77",
        //                 "account_holder": "43202c1a-48dc-423e-b336-bb65baccc7bd",
        //                 "account_id": "49302c1a-48dc-423e-b336-bb65baccc7bd",
        //                 "instrument_code": "BTC_EUR",
        //                 "time": "2022-06-29T04:33:29.656896Z",
        //                 "side": "SELL",
        //                 "price": "50338.65",
        //                 "amount": "0.00021",
        //                 "filled_amount": "0.0",
        //                 "type": "LIMIT"
        //             },
        //             "locked": {
        //                 "currency_code": "BTC",
        //                 "amount": "0.00021",
        //                 "new_available": "0.00017",
        //                 "new_locked": "0.00021"
        //             },
        //             "id": "26e9c36a-b231-4bb0-a686-aa915a2fc9e6",
        //             "sequence": 7658332018
        //         },
        //         "channel_name": "ACCOUNT_HISTORY",
        //         "type": "ACCOUNT_UPDATE",
        //         "time": "2022-06-29T04:33:29.684517Z"
        //     }
        //
        //  order rejected
        //     {
        //         "account_id": "49302c1a-48dc-423e-b336-bb65baccc7bd",
        //         "sequence": 7658332018,
        //         "update": {
        //             "id": "d3fe6025-5b27-4df6-a957-98b8d131cb9d",
        //             "type": "ORDER_REJECTED",
        //             "activity": "TRADING",
        //             "account_id": "b355abb8-aaae-4fae-903c-c60ff74723c6",
        //             "sequence": 0,
        //             "timestamp": "2018-08-01T13:39:15.590Z",
        //             "reason": "INSUFFICIENT_FUNDS",
        //             "order_id": "6f991342-da2c-45c6-8830-8bf519cfc8cc",
        //             "client_id": "fb497387-8223-4111-87dc-66a86f98a7cf",
        //             "unlocked": {
        //                 "currency_code": "BTC",
        //                 "amount": "1.5",
        //                 "new_locked": "2.0",
        //                 "new_available": "1.5"
        //             }
        //         }
        //     }
        //
        //  order closed
        //     {
        //         "account_id": "49202c1a-48dc-423e-b336-bb65baccc7bd",
        //         "sequence": 7658471216,
        //         "update": {
        //             "type": "ORDER_CLOSED",
        //             "activity": "TRADING",
        //             "account_holder": "49202c1a-48dc-423e-b336-bb65baccc7bd",
        //             "account_id": "49202c1a-48dc-423e-b336-bb65baccc7bd",
        //             "time": "2022-06-29T04:43:57.169616Z",
        //             "order_id": "8892fd69-5ebd-496b-aaa4-269b4c18aa77",
        //             "unlocked": {
        //                 "currency_code": "BTC",
        //                 "amount": "0.00021",
        //                 "new_available": "0.00038",
        //                 "new_locked": "0.0"
        //             },
        //             "order_book_sequence": 867964191,
        //             "id": "26c5e1d7-65ba-4a11-a661-14c0130ff484",
        //             "sequence": 7658471216
        //         },
        //         "channel_name": "ACCOUNT_HISTORY",
        //         "type": "ACCOUNT_UPDATE",
        //         "time": "2022-06-29T04:43:57.182153Z"
        //     }
        //
        //  trade settled
        //     {
        //         "account_id": "49202c1a-48dc-423e-b336-bb65baccc7bd",
        //         "sequence": 7658502878,
        //         "update": {
        //             "type": "TRADE_SETTLED",
        //             "activity": "TRADING",
        //             "account_holder": "49202c1a-48dc-423e-b336-bb65baccc7bd",
        //             "account_id": "49202c1a-48dc-423e-b336-bb65baccc7bd",
        //             "time": "2022-06-29T04:46:12.933091Z",
        //             "order_id": "ad19951a-b616-401d-a062-8d0609f038a4",
        //             "order_book_sequence": 867965579,
        //             "filled_amount": "0.00052",
        //             "order": {
        //                 "amount": "0.00052",
        //                 "filled_amount": "0.00052"
        //             },
        //             "trade": {
        //                 "trade_id": "21039eb9-2df0-4227-be2d-0ea9b691ac66",
        //                 "order_id": "ad19951a-b616-401d-a062-8d0609f038a4",
        //                 "account_holder": "49202c1a-48dc-423e-b336-bb65baccc7bd",
        //                 "account_id": "49202c1a-48dc-423e-b336-bb65baccc7bd",
        //                 "amount": "0.00052",
        //                 "side": "BUY",
        //                 "instrument_code": "BTC_EUR",
        //                 "price": "19309.29",
        //                 "time": "2022-06-29T04:46:12.870581Z",
        //                 "price_tick_sequence": 0
        //             },
        //             "fee": {
        //                 "fee_amount": "0.00000078",
        //                 "fee_currency": "BTC",
        //                 "fee_percentage": "0.15",
        //                 "fee_group_id": "default",
        //                 "fee_type": "TAKER",
        //                 "running_trading_volume": "0.00052",
        //                 "collection_type": "STANDARD"
        //             },
        //             "spent": {
        //                 "currency_code": "EUR",
        //                 "amount": "10.0408308",
        //                 "new_available": "0.0",
        //                 "new_locked": "0.15949533"
        //             },
        //             "credited": {
        //                 "currency_code": "BTC",
        //                 "amount": "0.00051922",
        //                 "new_available": "0.00089922",
        //                 "new_locked": "0.0"
        //             },
        //             "unlocked": {
        //                 "currency_code": "EUR",
        //                 "amount": "0.0",
        //                 "new_available": "0.0",
        //                 "new_locked": "0.15949533"
        //             },
        //             "id": "22b40199-2508-4176-8a14-d4785c933444",
        //             "sequence": 7658502878
        //         },
        //         "channel_name": "ACCOUNT_HISTORY",
        //         "type": "ACCOUNT_UPDATE",
        //         "time": "2022-06-29T04:46:12.941837Z"
        //     }
        //
        //  Trade Settled with BEST fee collection enabled
        //     {
        //         "account_id": "49302c1a-48dc-423e-b336-bb65baccc7bd",
        //         "sequence": 7658951984,
        //         "update": {
        //             "id": "70e00504-d892-456f-9aae-4da7acb36aac",
        //             "sequence": 361792,
        //             "order_book_sequence": 123456,
        //             "type": "TRADE_SETTLED",
        //             "activity": "TRADING",
        //             "account_id": "379a12c0-4560-11e9-82fe-2b25c6f7d123",
        //             "time": "2019-10-22T12:09:55.731Z",
        //             "order_id": "9fcdd91c-7f6e-45f4-9956-61cddba55de5",
        //             "client_id": "fb497387-8223-4111-87dc-66a86f98a7cf",
        //             "order": {
        //                 "amount": "0.5",
        //                 "filled_amount": "0.5"
        //             },
        //             "trade": {
        //                 "trade_id": "a828b63e-b2cb-48f0-8d99-8fc22cf98e08",
        //                 "order_id": "9fcdd91c-7f6e-45f4-9956-61cddba55de5",
        //                 "account_id": "379a12c0-4560-11e9-82fe-2b25c6f7d123",
        //                 "amount": "0.5",
        //                 "side": "BUY",
        //                 "instrument_code": "BTC_EUR",
        //                 "price": "7451.6",
        //                 "time": "2019-10-22T12:09:55.667Z"
        //             },
        //             "fee": {
        //                 "fee_amount": "23.28625",
        //                 "fee_currency": "BEST",
        //                 "fee_percentage": "0.075",
        //                 "fee_group_id": "default",
        //                 "fee_type": "TAKER",
        //                 "running_trading_volume": "0.10058",
        //                 "collection_type": "BEST",
        //                 "applied_best_eur_rate": "1.04402"
        //             },
        //             "spent": {
        //                 "currency_code": "EUR",
        //                 "amount": "3725.8",
        //                 "new_available": "14517885.0675703028781",
        //                 "new_locked": "2354.882"
        //             },
        //             "spent_on_fees": {
        //                 "currency_code": "BEST",
        //                 "amount": "23.28625",
        //                 "new_available": "9157.31375",
        //                 "new_locked": "0.0"
        //             },
        //             "credited": {
        //                 "currency_code": "BTC",
        //                 "amount": "0.5",
        //                 "new_available": "5839.89633700481",
        //                 "new_locked": "0.0"
        //             },
        //             "unlocked": {
        //                 "currency_code": "EUR",
        //                 "amount": "0.15",
        //                 "new_available": "14517885.0675703028781",
        //                 "new_locked": "2354.882"
        //             }
        //         }
        //         "channel_name": "ACCOUNT_HISTORY",
        //         "type": "ACCOUNT_UPDATE",
        //         "time": "2022-06-29T05:18:51.760338Z"
        //     }
        //
        if is_equal(&self.orders, &Value::Null) {
            let mut limit: Value = self.safe_integer_k(self.options.clone(), "ordersLimit", &[Value::Int(1000)]);
            self.orders = ArrayCacheBySymbolById::new(limit.clone());
        }
        if is_equal(&self.myTrades, &Value::Null) {
            let mut limit: Value = self.safe_integer_k(self.options.clone(), "tradesLimit", &[Value::Int(1000)]);
            self.myTrades = ArrayCacheBySymbolById::new(limit.clone());
        }
        let mut symbol: Value = Value::Null;
        let mut orders: Value = self.orders.clone();
        let mut update: Value = self.safe_value_k(message.clone(), "update", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut updateType: Value = self.safe_string_k(update.clone(), "type", &[]);
        if is_equal(&updateType, &Value::Str("ORDER_REJECTED".to_string())) || is_equal(&updateType, &Value::Str("ORDER_CLOSED".to_string())) || is_equal(&updateType, &Value::Str("STOP_ORDER_TRIGGERED".to_string())) {
            let mut orderId: Value = self.safe_string_k(update.clone(), "order_id", &[]);
            let mut datetime: Value = self.safe_string2(update.clone(), Value::Str("time".to_string()), Value::Str("timestamp".to_string()), &[]);
            let mut previousOrderArray: Value = self.filter_by_array(self.orders.clone(), Value::Str("id".to_string()), &[orderId.clone(), Value::Bool(false)]);
            let mut previousOrder: Value = self.safe_value(previousOrderArray.clone(), Value::Int(0), &[Value::Map({
                let mut m = indexmap::IndexMap::new();
                m
            })]);
            symbol = get_value(&previousOrder, &Value::Str("symbol".to_string()));
            let mut filled: Value = self.safe_string_k(update.clone(), "filled_amount", &[]);
            let mut status: Value = self.parse_ws_order_status(updateType.clone());
            if is_equal(&updateType, &Value::Str("ORDER_CLOSED".to_string())) && is_true(&crate::precise::Precise::stringEq(&filled, &Value::Str("0".to_string()))) {
                status = Value::Str("canceled".to_string());
            }
            let mut orderObject: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("id".to_string(), orderId.clone());
                    m.insert("symbol".to_string(), symbol.clone());
                    m.insert("status".to_string(), status.clone());
                    m.insert("timestamp".to_string(), self.parse8601(datetime.clone()));
                    m.insert("datetime".to_string(), datetime.clone());
                m
            });
            orders.append(orderObject.clone());
        }  else {
            let mut parsed: Value = self.parse_order(update.clone(), &[]);
            symbol = self.safe_string_k(parsed.clone(), "symbol", &[Value::Str("".to_string())]);
            orders.append(parsed.clone());
        }
        client.resolve(&[self.orders.clone(), add(&Value::Str("orders:".to_string()), &symbol)]);
        client.resolve(&[self.orders.clone(), Value::Str("orders".to_string())]);
        // update balance
        let mut balanceKeys: Value = Value::List(vec![Value::Str("locked".to_string()), Value::Str("unlocked".to_string()), Value::Str("spent".to_string()), Value::Str("spent_on_fees".to_string()), Value::Str("credited".to_string()), Value::Str("deducted".to_string())]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_548: bool = true;
            while { if !__for_first_548 { i = add(&i, &Value::Int(1)); } __for_first_548 = false; is_less_than(&i, &get_array_length(&balanceKeys)) } {
            let mut newBalance: Value = self.safe_value(update.clone(), get_value(&balanceKeys, &i), &[]);
            if !is_equal(&newBalance, &Value::Null) {
                self.update_balance(newBalance.clone());
            }
        }
        }
        client.resolve(&[self.balance.clone(), Value::Str("balance".to_string())]);
        // update trades
        if is_equal(&updateType, &Value::Str("TRADE_SETTLED".to_string())) {
            let mut parsed: Value = self.parse_trade(update.clone(), &[]);
            symbol = self.safe_string_k(parsed.clone(), "symbol", &[Value::Str("".to_string())]);
            let mut myTrades: Value = self.myTrades.clone();
            myTrades.append(parsed.clone());
            client.resolve(&[self.myTrades.clone(), add(&Value::Str("myTrades:".to_string()), &symbol)]);
            client.resolve(&[self.myTrades.clone(), Value::Str("myTrades".to_string())]);
        }
}

    pub fn parse_ws_order_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("ORDER_REJECTED".to_string(), Value::Str("rejected".to_string()));
                m.insert("ORDER_CLOSED".to_string(), Value::Str("closed".to_string()));
                m.insert("STOP_ORDER_TRIGGERED".to_string(), Value::Str("triggered".to_string()));
            m
        });
        return self.safe_string(statuses.clone(), status.clone(), &[status.clone()]);

    Value::Null
}

    pub fn update_balance(&mut self, mut balance: Value) {
        //
        //     {
        //         "currency_code": "EUR",
        //         "amount": "0.0",
        //         "new_available": "0.0",
        //         "new_locked": "0.15949533"
        //     }
        //
        let mut currencyId: Value = self.safe_string_k(balance.clone(), "currency_code", &[]);
        let mut code: Value = self.safe_currency_code(currencyId.clone(), &[]);
        let mut account: Value = self.account();
        add_element_to_object(&mut account, &Value::Str("free".to_string()), self.safe_string_k(balance.clone(), "new_available", &[]));
        add_element_to_object(&mut account, &Value::Str("used".to_string()), self.safe_string_k(balance.clone(), "new_locked", &[]));
        add_element_to_object(&mut self.balance.clone(), &code, account.clone());
        { let __t = self.safe_balance(self.balance.clone()); self.balance = __t; }
}

/*
 * @method
 * @name onetrading#watchOHLCV
 * @see https://developers.bitpanda.com/exchange/#candlesticks-channel
 * @description watches historical candlestick data containing the open, high, low, and close price, and the volume of a market
 * @param {string} symbol unified symbol of the market to fetch OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum amount of candles to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 */
    pub async fn watch_ohlcv(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut timeframe = get_arg(optional_args, 0, Value::Str("1m".to_string()));
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.market(symbol.clone());
        symbol = get_value(&market, &Value::Str("symbol".to_string()));
        let mut marketId: Value = get_value(&market, &Value::Str("id".to_string()));
        let mut url: Value = get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string()));
        let mut timeframes: Value = self.safe_value_k(self.options.clone(), "timeframes", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut timeframeId: Value = self.safe_value(timeframes.clone(), timeframe.clone(), &[]);
        if is_equal(&timeframeId, &Value::Null) {
            panic!("{}", crate::exchange_errors::not_supported(add(&self.id, &Value::Str(" this interval is not supported, please provide one of the supported timeframes".to_string()))));
        }
        let mut messageHash: Value = add(&add(&add(&Value::Str("ohlcv.".to_string()), &symbol), &Value::Str(".".to_string())), &timeframe);
        let mut subscriptionHash: Value = Value::Str("CANDLESTICKS".to_string());
        let mut client: Value = self.safe_value(self.clients.clone(), url.clone(), &[]);
        let mut type_var: Value = Value::Str("SUBSCRIBE".to_string());
        let mut subscription: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        if !is_equal(&client, &Value::Null) {
            subscription = self.safe_value(get_value(&client, &Value::Str("subscriptions".to_string())), subscriptionHash.clone(), &[]);
            if !is_equal(&subscription, &Value::Null) {
                let mut ohlcvMarket: Value = self.safe_value(subscription.clone(), marketId.clone(), &[Value::Map({
                    let mut m = indexmap::IndexMap::new();
                    m
                })]);
                let mut marketSubscribed: Value = self.safe_bool(ohlcvMarket.clone(), timeframe.clone(), &[Value::Bool(false)]);
                if !is_true(&marketSubscribed) {
                    type_var = Value::Str("UPDATE_SUBSCRIPTION".to_string());
                    add_element_to_object(&mut get_value(&client, &Value::Str("subscriptions".to_string())), &subscriptionHash, Value::Null);
                }
            }  else {
                subscription = Value::Map({
                    let mut m = indexmap::IndexMap::new();
                    m
                });
            }
        }
        let mut subscriptionMarketId: Value = self.safe_value(subscription.clone(), marketId.clone(), &[]);
        if is_equal(&subscriptionMarketId, &Value::Null) {
            add_element_to_object(&mut subscription, &marketId, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        }
        add_element_to_object(get_value_mut(&mut subscription, &marketId), &timeframe, Value::Bool(true));
        let mut properties: Value = Value::List(vec![]);
        let mut marketIds: Value = object_keys(&subscription);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_550: bool = true;
            while { if !__for_first_550 { i = add(&i, &Value::Int(1)); } __for_first_550 = false; is_less_than(&i, &get_array_length(&marketIds)) } {
            let mut marketIdtimeframes: Value = object_keys(&get_value(&subscription, &get_value(&marketIds, &i)));
            {
                                let mut ii: Value = Value::Int(0);
                let mut __for_first_549: bool = true;
                while { if !__for_first_549 { ii = add(&ii, &Value::Int(1)); } __for_first_549 = false; is_less_than(&ii, &get_array_length(&marketIdtimeframes)) } {
                let mut marketTimeframeId: Value = self.safe_value(timeframes.clone(), timeframe.clone(), &[]);
                let mut property: Value = Value::Map({
                    let mut m = indexmap::IndexMap::new();
                        m.insert("instrument_code".to_string(), get_value(&marketIds, &i));
                        m.insert("time_granularity".to_string(), marketTimeframeId.clone());
                    m
                });
                append_to_array(&mut properties, property.clone());
            }
            }
        }
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("type".to_string(), type_var.clone());
                m.insert("channels".to_string(), Value::List(vec![Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("name".to_string(), Value::Str("CANDLESTICKS".to_string()));
        m.insert("properties".to_string(), properties.clone());
    m
})]));
            m
        });
        let __ws_arg_0 = self.deep_extend(request.clone(), &[params.clone()]);
        let mut ohlcv: Value = self.watch(url.clone(), messageHash.clone(), &[__ws_arg_0, subscriptionHash.clone(), subscription.clone()]).await;
        if is_true(&self.newUpdates) {
            limit = ohlcv.get_limit(symbol.clone(), limit.clone());
        }
        return self.filter_by_since_limit(ohlcv.clone(), &[since.clone(), limit.clone(), Value::Int(0), Value::Bool(true)]);

    Value::Null
}

    pub fn handle_ohlcv(&self, mut client: Value, mut message: Value) {
        //
        //  snapshot
        //     {
        //         "instrument_code": "BTC_EUR",
        //         "granularity": { unit: "MONTHS", period: 1 },
        //         "high": "29750.81",
        //         "low": "16764.59",
        //         "open": "29556.02",
        //         "close": "20164.55",
        //         "volume": "107518944.610659",
        //         "last_sequence": 2275507,
        //         "channel_name": "CANDLESTICKS",
        //         "type": "CANDLESTICK_SNAPSHOT",
        //         "time": "2022-06-30T23:59:59.999000Z"
        //     }
        //
        //  update
        //     {
        //         "instrument_code": "BTC_EUR",
        //         "granularity": {
        //             "unit": "MINUTES",
        //             "period": 1
        //         },
        //         "high": "20164.16",
        //         "low": "20164.16",
        //         "open": "20164.16",
        //         "close": "20164.16",
        //         "volume": "3645.2768448",
        //         "last_sequence": 2275511,
        //         "channel_name": "CANDLESTICKS",
        //         "type": "CANDLESTICK",
        //         "time": "2022-06-24T21:20:59.999000Z"
        //     }
        //
        let mut marketId: Value = self.safe_string_k(message.clone(), "instrument_code", &[]);
        let mut symbol: Value = self.safe_symbol(marketId.clone(), &[]);
        let mut dateTime: Value = self.safe_string_k(message.clone(), "time", &[]);
        let mut timeframeId: Value = self.safe_value_k(message.clone(), "granularity", &[]);
        let mut timeframes: Value = self.safe_value_k(self.options.clone(), "timeframes", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut timeframe: Value = self.find_timeframe(timeframeId.clone(), &[timeframes.clone()]);
        let mut channel: Value = add(&add(&add(&Value::Str("ohlcv.".to_string()), &symbol), &Value::Str(".".to_string())), &timeframe);
        let mut parsed: Value = Value::List(vec![self.parse8601(dateTime.clone()), self.safe_number_k(message.clone(), "open", &[]), self.safe_number_k(message.clone(), "high", &[]), self.safe_number_k(message.clone(), "low", &[]), self.safe_number_k(message.clone(), "close", &[]), self.safe_number_k(message.clone(), "volume", &[])]);
        { let __be_tmp = self.safe_value(self.ohlcvs.clone(), symbol.clone(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]); add_element_to_object(&mut self.ohlcvs.clone(), &symbol, __be_tmp); };
        let mut stored: Value = self.safe_value(get_value(&self.ohlcvs, &symbol), timeframe.clone(), &[]);
        if is_equal(&stored, &Value::Null) {
            let mut limit: Value = self.safe_integer_k(self.options.clone(), "OHLCVLimit", &[Value::Int(1000)]);
            stored = ArrayCacheByTimestamp::new(limit.clone());
        }
        stored.append(parsed.clone());
        add_element_to_object(get_value_mut(unsafe { crate::runtime::coerce_value_to_mut(&self.ohlcvs) }, &symbol), &timeframe, stored.clone());
        client.resolve(&[stored.clone(), channel.clone()]);
}

    pub fn find_timeframe(&self, mut timeframe: Value, optional_args: &[Value]) -> Value {
        let mut timeframes = get_arg(optional_args, 0, Value::Null);
        timeframes = Value::Bool(is_true(&timeframes) || is_true(&self.timeframes));
        let mut keys: Value = object_keys(&timeframes);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_551: bool = true;
            while { if !__for_first_551 { i = add(&i, &Value::Int(1)); } __for_first_551 = false; is_less_than(&i, &get_array_length(&keys)) } {
            let mut key: Value = get_value(&keys, &i);
            let mut key: Value = get_value(&keys, &i);
            if is_equal(&get_value(&get_value(&timeframes, &key), &Value::Str("unit".to_string())), &get_value(&timeframe, &Value::Str("unit".to_string()))) && is_equal(&get_value(&get_value(&timeframes, &key), &Value::Str("period".to_string())), &get_value(&timeframe, &Value::Str("period".to_string()))) {
                return key;
            }
        }
        }
        return Value::Null;

    Value::Null
}

    pub fn handle_subscriptions(&self, mut client: Value, mut message: Value) -> Value {
        return message;

    Value::Null
}

    pub fn handle_heartbeat(&self, mut client: Value, mut message: Value) -> Value {
        return message;

    Value::Null
}

    pub fn handle_error_message(&self, mut client: Value, mut message: Value) -> Value {
        panic!("{}", crate::exchange_errors::exchange_error(add(&add(&self.id, &Value::Str(" ".to_string())), &self.json(message.clone()))));

    Value::Null
}

    pub fn handle_message(&self, mut client: Value, mut message: Value) {
        let mut error: Value = self.safe_value_k(message.clone(), "error", &[]);
        if !is_equal(&error, &Value::Null) {
            self.handle_error_message(client.clone(), message.clone());
            return;
        }
        let mut type_var: Value = self.safe_value_k(message.clone(), "type", &[]);
        let mut handlers: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("ORDER_BOOK_UPDATE".to_string(), Value::Null.clone());
                m.insert("ORDER_BOOK_SNAPSHOT".to_string(), Value::Null.clone());
                m.insert("ACTIVE_ORDERS_SNAPSHOT".to_string(), Value::Null.clone());
                m.insert("INACTIVE_ORDERS_SNAPSHOT".to_string(), Value::Null.clone());
                m.insert("ACCOUNT_UPDATE".to_string(), Value::Null.clone());
                m.insert("BALANCES_SNAPSHOT".to_string(), Value::Null.clone());
                m.insert("SUBSCRIPTIONS".to_string(), Value::Null.clone());
                m.insert("SUBSCRIPTION_UPDATED".to_string(), Value::Null.clone());
                m.insert("PRICE_TICK".to_string(), Value::Null.clone());
                m.insert("PRICE_TICK_HISTORY".to_string(), Value::Null.clone());
                m.insert("HEARTBEAT".to_string(), Value::Null.clone());
                m.insert("MARKET_TICKER_UPDATES".to_string(), Value::Null.clone());
                m.insert("PRICE_POINT_UPDATES".to_string(), Value::Null.clone());
                m.insert("CANDLESTICK_SNAPSHOT".to_string(), Value::Null.clone());
                m.insert("CANDLESTICK".to_string(), Value::Null.clone());
                m.insert("AUTHENTICATED".to_string(), Value::Null.clone());
                m.insert("FILL".to_string(), Value::Null.clone());
                m.insert("DONE".to_string(), Value::Null.clone());
                m.insert("BOOKED".to_string(), Value::Null.clone());
                m.insert("UPDATE".to_string(), Value::Null.clone());
                m.insert("TRACKED".to_string(), Value::Null.clone());
                m.insert("TRIGGERED".to_string(), Value::Null.clone());
                m.insert("STOP_TRACKED".to_string(), Value::Null.clone());
                m.insert("STOP_TRIGGERED".to_string(), Value::Null.clone());
            m
        });
        let mut handler: Value = self.safe_value(handlers.clone(), type_var.clone(), &[]);
        if !is_equal(&handler, &Value::Null) {
            handler.call(&[client.clone(), message.clone()]);
        }
}

    pub fn handle_price_point_updates(&self, mut client: Value, mut message: Value) -> Value {
        return message;

    Value::Null
}

    pub fn handle_authentication_message(&self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //        "channel_name": "SYSTEM",
        //        "type": "AUTHENTICATED",
        //        "time": "2022-06-24T20:45:25.447488Z"
        //    }
        //
        let mut future: Value = self.safe_value(get_value(&client, &Value::Str("futures".to_string())), Value::Str("authenticated".to_string()), &[]);
        if !is_equal(&future, &Value::Null) {
            future.resolve(&[Value::Bool(true)]);
        }
        return message;

    Value::Null
}

    pub async fn watch_many(&mut self, mut messageHash: Value, mut request: Value, mut subscriptionHash: Value, optional_args: &[Value]) -> Value {
        let mut symbols = get_arg(optional_args, 0, Value::List(vec![]));
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut marketIds: Value = Value::List(vec![]);
        let mut numSymbols: Value = get_array_length(&symbols);
        if is_equal(&numSymbols, &Value::Int(0)) {
            marketIds = object_keys(&self.markets_by_id);
        }  else {
            marketIds = self.market_ids(&[symbols.clone()]);
        }
        let mut url: Value = get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string()));
        let mut client: Value = self.safe_value(self.clients.clone(), url.clone(), &[]);
        let mut type_var: Value = Value::Str("SUBSCRIBE".to_string());
        let mut subscription: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        if !is_equal(&client, &Value::Null) {
            subscription = self.safe_value(get_value(&client, &Value::Str("subscriptions".to_string())), subscriptionHash.clone(), &[]);
            if !is_equal(&subscription, &Value::Null) {
                {
                                        let mut i: Value = Value::Int(0);
                    let mut __for_first_552: bool = true;
                    while { if !__for_first_552 { i = add(&i, &Value::Int(1)); } __for_first_552 = false; is_less_than(&i, &get_array_length(&marketIds)) } {
                    let mut marketId: Value = get_value(&marketIds, &i);
                    let mut marketId: Value = get_value(&marketIds, &i);
                    let mut marketSubscribed: Value = self.safe_bool(subscription.clone(), marketId.clone(), &[Value::Bool(false)]);
                    if !is_true(&marketSubscribed) {
                        type_var = Value::Str("UPDATE_SUBSCRIPTION".to_string());
                        add_element_to_object(&mut get_value(&client, &Value::Str("subscriptions".to_string())), &subscriptionHash, Value::Null);
                    }
                }
                }
            }  else {
                subscription = Value::Map({
                    let mut m = indexmap::IndexMap::new();
                    m
                });
            }
        }
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_553: bool = true;
            while { if !__for_first_553 { i = add(&i, &Value::Int(1)); } __for_first_553 = false; is_less_than(&i, &get_array_length(&marketIds)) } {
            let mut marketId: Value = get_value(&marketIds, &i);
            let mut marketId: Value = get_value(&marketIds, &i);
            add_element_to_object(&mut subscription, &marketId, Value::Bool(true));
        }
        }
        add_element_to_object(&mut request, &Value::Str("type".to_string()), type_var.clone());
        add_element_to_object(get_value_mut(get_value_mut(&mut request, &Value::Str("channels".to_string())), &Value::Int(0)), &Value::Str("instrument_codes".to_string()), object_keys(&subscription));
        let __ws_arg_1 = self.deep_extend(request.clone(), &[params.clone()]);
        return self.watch(url.clone(), messageHash.clone(), &[__ws_arg_1, subscriptionHash.clone(), subscription.clone()]).await;

    Value::Null
}

    pub async fn authenticate(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut url: Value = get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string()));
        let mut client: Value = self.client(&[url.clone()]);
        let mut messageHash: Value = Value::Str("authenticated".to_string());
        let mut future: Value = client.reusable_future(Value::Str("authenticated".to_string()));
        let mut authenticated: Value = self.safe_value(get_value(&client, &Value::Str("subscriptions".to_string())), messageHash.clone(), &[]);
        if is_equal(&authenticated, &Value::Null) {
            self.check_required_credentials(&[]);
            let mut request: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("type".to_string(), Value::Str("AUTHENTICATE".to_string()));
                    m.insert("api_token".to_string(), self.apiKey.clone());
                m
            });
            let __ws_arg_2 = self.extend(request.clone(), &[params.clone()]);
            self.watch(url.clone(), messageHash.clone(), &[__ws_arg_2, messageHash.clone()]).await;
        }
        return get_value(&future, &Value::Str("await".to_string()));

    Value::Null
}
}
