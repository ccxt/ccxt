// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#![allow(unused, non_snake_case, clippy::all)]
use crate::Value;
use crate::get_value;
use crate::runtime::*;
use crate::pro::*;


pub struct BitmartCore {
    pub parent: crate::exchanges::bitmart::BitmartCore,
}

impl BitmartCore {
    pub fn new(config: Option<crate::Value>) -> Self {
        let mut s = Self { parent: crate::exchanges::bitmart::BitmartCore::new(config) };
        s.init();
        s
    }

    /// One-shot setup: populates describe() data onto the inner Exchange and
    /// builds the implicit API table. Idempotent — calling more than once is
    /// safe.
    ///
    /// Deliberately does NOT install the derived-dispatch pointers: new()
    /// runs init() while self is still a movable local, so binding here
    /// would capture a soon-invalid address (review P0 #1). An un-bound Core
    /// is safe-but-inert — derived_ptr defaults to DEFAULT_DERIVED and
    /// derived_core_ptr is null, so virtual calls fall back to the base.
    /// Binding happens later at a pinned/boxed address via bind(), which the
    /// sanctioned constructors call once the Core is heap-stable.
    pub fn init(&mut self) {
        // Populate describe()-derived fields.
        let described = BitmartCore::describe(self);
        self.exchange.api      = crate::get_value(&described, &crate::Value::Str("api".to_string()));
        self.exchange.urls     = crate::get_value(&described, &crate::Value::Str("urls".to_string()));
        self.exchange.has      = crate::get_value(&described, &crate::Value::Str("has".to_string()));
        // rateLimit drives the throttle spacing. describe() usually declares a
        // venue-specific value (binance 50ms, …); new() ran apply_config before
        // init(), so a config-supplied rateLimit is already in place. Only fall
        // back to describe()'s value when the field is still the base 2000ms
        // default, so caller config keeps precedence (review #8 — init() used
        // to drop describe().rateLimit, leaving every venue at 2000ms).
        let __described_rate_limit = crate::get_value(&described, &crate::Value::Str("rateLimit".to_string()));
        if matches!(__described_rate_limit, crate::Value::Int(_) | crate::Value::Float(_))
            && matches!(self.exchange.rateLimit, crate::Value::Int(2000)) {
            self.exchange.rateLimit = __described_rate_limit;
        }
        // Merge describe() options INTO whatever apply_config (or
        // earlier setup) already populated. Caller-supplied options
        // (e.g. portfolioMargin via fixture) take precedence, then
        // describe()'s defaults fill in any gaps.
        let __described_options = crate::get_value(&described, &crate::Value::Str("options".to_string()));
        // Capture the describe()'s explicit networksById BEFORE the merge
        // — the merge below overlays the base Exchange's options (whose
        // after_construct left an EMPTY networksById), which would
        // otherwise clobber the manual mappings like binance BSC to BEP20.
        let __described_networks_by_id = crate::get_value(&__described_options, &crate::Value::Str("networksById".to_string()));
        if let (crate::Value::Dict(existing), crate::Value::Dict(defaults)) =
            (&self.exchange.options.clone(), &__described_options)
        {
            let mut merged = (**defaults).clone();
            for (k, v) in existing.iter() { merged.insert(k.clone(), v.clone()); }
            self.exchange.options = crate::Value::Map(merged);
        } else if !matches!(self.exchange.options, crate::Value::Dict(_)) {
            self.exchange.options = __described_options;
        }
        // Derive options.networksById (CCXT's createNetworksByIdObject):
        // start from the auto-inverted networks (generated), then overlay
        // the describe()'s explicit networksById so manual mappings win —
        // mirrors TS extend(generated, manual). Without the manual
        // overlay, an exchange whose networks defines both BSC to BSC and
        // BEP20 to BSC would invert to BSC to BSC (wrong) by iteration
        // order instead of the intended BSC to BEP20.
        if let crate::Value::Dict(opts_arc) = self.exchange.options.clone() {
            let mut opts = std::sync::Arc::try_unwrap(opts_arc).unwrap_or_else(|a| (*a).clone());
            let mut by_id: indexmap::IndexMap<String, crate::Value> = indexmap::IndexMap::new();
            if let Some(crate::Value::Dict(networks)) = opts.get("networks") {
                for (code, id) in networks.iter() {
                    if let crate::Value::Str(id_s) = id {
                        by_id.entry(id_s.clone())
                             .or_insert_with(|| crate::Value::Str(code.clone()));
                    }
                }
            }
            if let crate::Value::Dict(manual) = &__described_networks_by_id {
                for (k, v) in manual.iter() { by_id.insert(k.clone(), v.clone()); }
            }
            opts.insert("networksById".to_string(), crate::Value::Map(by_id));
            self.exchange.options = crate::Value::Map(opts);
        }
        self.exchange.hostname = crate::get_value(&described, &crate::Value::Str("hostname".to_string()));
        self.exchange.version  = crate::get_value(&described, &crate::Value::Str("version".to_string()));
        self.exchange.id       = crate::get_value(&described, &crate::Value::Str("id".to_string()));
        self.exchange.name     = crate::get_value(&described, &crate::Value::Str("name".to_string()));
        self.exchange.exceptions = crate::get_value(&described, &crate::Value::Str("exceptions".to_string()));
        // Only override the base-default requiredCredentials when the
        // exchange's describe() actually provides them (super.describe()
        // is stubbed, so most exchanges' describe() omits this).
        { let __rc = crate::get_value(&described, &crate::Value::Str("requiredCredentials".to_string())); if !matches!(__rc, crate::Value::Null) { self.exchange.requiredCredentials = __rc; } }
        // Merge describe()'s commonCurrencies over the base defaults so
        // exchange-specific aliases (bitfinex UST to USDT, onetrading
        // MIOTA to IOTA) reach commonCurrencyCode / safeCurrencyCode.
        { let __cc = crate::get_value(&described, &crate::Value::Str("commonCurrencies".to_string())); if let crate::Value::Dict(extra) = __cc { let extra = std::sync::Arc::try_unwrap(extra).unwrap_or_else(|a| (*a).clone()); if let crate::Value::Dict(base) = &mut self.exchange.commonCurrencies { let base = std::sync::Arc::make_mut(base); for (k, v) in extra { base.insert(k, v); } } else { self.exchange.commonCurrencies = crate::Value::Map(extra); } } }
        self.exchange.precisionMode = crate::get_value(&described, &crate::Value::Str("precisionMode".to_string()));
        self.exchange.timeframes = crate::get_value(&described, &crate::Value::Str("timeframes".to_string()));
        self.exchange.fees = crate::get_value(&described, &crate::Value::Str("fees".to_string()));
        // `features` carries the describe() block that drives
        // unified-method tests (e.g. `features.spot.fetchCurrencies.private`
        // tells testFetchCurrencies to skip the length check). It's set
        // after `Exchange::new` because `features_generator` (in
        // `after_construct`) bails when `features == Null` — so we
        // assign and re-run the generator here.
        self.exchange.features = crate::get_value(&described, &crate::Value::Str("features".to_string()));
        if !matches!(self.exchange.features, crate::Value::Null) {
            self.exchange.features_generator();
        }
        // Markets and currencies may have been populated already by
        // the constructor config (test runners pass them in via
        // Exchange::new(Some(config-with-markets)) — same as CCXT TS).
        // Don't reset them here.
        self.exchange.build_implicit_api();
    }

    /// Re-bind the derived-exchange pointer. Needed if the struct's
    /// address moves after `new()` (e.g. `Box::new` the value).
    pub fn bind(&mut self) {
        let ptr: *const (dyn crate::exchange::DerivedExchange + 'static) =
            self as &Self as *const dyn crate::exchange::DerivedExchange;
        self.exchange.bind_derived(ptr);
        // Async dispatch hook: lets base Exchange methods route async
        // calls (cancel_order, fetch_order, …) to this Core's
        // call_dynamic, bypassing the base stubs.
        let core_ptr = self as *mut Self as *mut ();
        self.exchange.bind_call_async(core_ptr, BitmartCore::__call_dynamic_dispatch);
    }

    /// Type-erased trampoline used by Exchange::dispatch_to_derived.
    /// Casts the void pointer back to `*mut Self` and forwards to the
    /// real `call_dynamic`. Safety: the pointer must come from the
    /// matching Core's bind() — guaranteed by Exchange::bind_call_async.
    pub fn __call_dynamic_dispatch<'a>(
        ptr: *mut (),
        method: &'a str,
        args: Vec<crate::Value>,
    ) -> std::pin::Pin<Box<dyn std::future::Future<Output = crate::Value> + 'a>> {
        Box::pin(async move {
            let s: &mut Self = unsafe { &mut *(ptr as *mut Self) };
            s.call_dynamic(method, args).await
        })
    }

    /// Dispatch by method name. Mirrors Go's CallInternal /
    /// C#'s callInternal — lets the CLI call any method without
    /// hard-coding signatures. Accepts the canonical snake_case name.
    /// Returns Value::Null for unknown names.
    pub async fn call_dynamic(&mut self, method: &str, args: Vec<crate::Value>) -> crate::Value {
        match method {
            "authenticate" => self.authenticate(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "get_params_for_multiple_sub" => self.get_params_for_multiple_sub(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), &args.get(2..).unwrap_or(&[]).to_vec()[..]),
            "get_un_sub_params" => self.get_un_sub_params(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "handle_error_message" => self.handle_error_message(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
            "handle_order_book_message" => self.handle_order_book_message(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null)),
            "handle_subscription_status" => self.handle_subscription_status(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
            "handle_trade_loop" => self.handle_trade_loop(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "load_balance_snapshot" => self.load_balance_snapshot(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null)).await,
            "parse_market_type" => self.parse_market_type(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "parse_topic" => self.parse_topic(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "parse_ws_bid_ask" => self.parse_ws_bid_ask(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "parse_ws_order" => self.parse_ws_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "parse_ws_order_side" => self.parse_ws_order_side(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "parse_ws_order_status" => self.parse_ws_order_status(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "parse_ws_position" => self.parse_ws_position(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "parse_ws_swap_ticker" => self.parse_ws_swap_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "parse_ws_trade" => self.parse_ws_trade(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "subscribe" => self.subscribe(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), args.get(3).cloned().unwrap_or(crate::Value::Null), &args.get(4..).unwrap_or(&[]).to_vec()[..]).await,
            "subscribe_multiple" => self.subscribe_multiple(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), &args.get(3..).unwrap_or(&[]).to_vec()[..]).await,
            "subscription_exists_for_hash" => self.subscription_exists_for_hash(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null)),
            "un_watch_ohlcv" => self.un_watch_ohlcv(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "un_watch_order_book" => self.un_watch_order_book(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "un_watch_order_book_for_symbols" => self.un_watch_order_book_for_symbols(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "un_watch_orders" => self.un_watch_orders(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "un_watch_positions" => self.un_watch_positions(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "un_watch_ticker" => self.un_watch_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "un_watch_tickers" => self.un_watch_tickers(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "un_watch_trades" => self.un_watch_trades(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "un_watch_trades_for_symbols" => self.un_watch_trades_for_symbols(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_balance" => self.watch_balance(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_bids_asks" => self.watch_bids_asks(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_funding_rate" => self.watch_funding_rate(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_funding_rates" => self.watch_funding_rates(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_ohlcv" => self.watch_ohlcv(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_order_book" => self.watch_order_book(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_order_book_for_symbols" => self.watch_order_book_for_symbols(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_orders" => self.watch_orders(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_positions" => self.watch_positions(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_ticker" => self.watch_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_tickers" => self.watch_tickers(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_trades" => self.watch_trades(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "watch_trades_for_symbols" => self.watch_trades_for_symbols(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            // Go-style inheritance: an un-overridden method dispatches
            // to the parent exchange Core.
            _ => self.parent.call_dynamic(method, args).await,
        }
    }
}

impl crate::exchange::DerivedExchange for BitmartCore {
    fn parse_ticker(&self, ticker: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_ticker(&self.parent, ticker, market)
    }
    fn parse_trade(&self, trade: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_trade(&self.parent, trade, market)
    }
    fn parse_order(&self, order: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_order(&self.parent, order, market)
    }
    fn parse_market(&self, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_market(&self.parent, market)
    }
    fn parse_ohlcv(&self, ohlcv: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_ohlcv(&self.parent, ohlcv, market)
    }
    fn parse_order_book(&self, ob: crate::Value, symbol: crate::Value, ts: crate::Value, bk: crate::Value, ak: crate::Value, pk: crate::Value, ak2: crate::Value, ck: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_order_book(&self.parent, ob, symbol, ts, bk, ak, pk, ak2, ck)
    }
    fn parse_balance(&self, response: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_balance(&self.parent, response)
    }
    fn parse_position(&self, position: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_position(&self.parent, position, market)
    }
    fn parse_funding_rate(&self, rate: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_funding_rate(&self.parent, rate, market)
    }
    fn parse_deposit(&self, tx: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_deposit(&self.parent, tx, currency)
    }
    fn parse_deposit_address(&self, depositAddress: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_deposit_address(&self.parent, depositAddress, currency)
    }
    fn parse_last_price(&self, entry: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_last_price(&self.parent, entry, market)
    }
    fn parse_withdrawal(&self, tx: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_withdrawal(&self.parent, tx, currency)
    }
    fn parse_ledger_entry(&self, entry: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_ledger_entry(&self.parent, entry, currency)
    }
    fn parse_transfer(&self, transfer: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_transfer(&self.parent, transfer, currency)
    }
    fn parse_currency(&self, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_currency(&self.parent, currency)
    }
    fn parse_bid_ask(&self, bidask: crate::Value, price_key: crate::Value, amount_key: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_bid_ask(&self.parent, bidask, price_key, amount_key, market)
    }
    fn parse_open_interest(&self, interest: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_open_interest(&self.parent, interest, market)
    }
    fn parse_liquidation(&self, liquidation: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_liquidation(&self.parent, liquidation, market)
    }
    fn parse_funding_rate_history(&self, entry: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_funding_rate_history(&self.parent, entry, market)
    }
    fn parse_margin_modification(&self, data: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_margin_modification(&self.parent, data, market)
    }
    fn parse_account(&self, account: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_account(&self.parent, account)
    }
    fn parse_my_trade(&self, trade: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_my_trade(&self.parent, trade, market)
    }
    fn parse_transaction(&self, transaction: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_transaction(&self.parent, transaction, currency)
    }
    fn parse_borrow_interest(&self, info: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_borrow_interest(&self.parent, info, market)
    }
    fn parse_adl_rank(&self, info: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_adl_rank(&self.parent, info, market)
    }
    fn parse_income(&self, info: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_income(&self.parent, info, market)
    }
    fn parse_greeks(&self, greeks: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_greeks(&self.parent, greeks, market)
    }
    fn parse_margin_mode(&self, margin_mode: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_margin_mode(&self.parent, margin_mode, market)
    }
    fn parse_conversion(&self, conversion: crate::Value, from_currency: crate::Value, to_currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_conversion(&self.parent, conversion, from_currency, to_currency)
    }
    fn parse_borrow_rate(&self, info: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_borrow_rate(&self.parent, info, currency)
    }
    fn parse_leverage(&self, leverage: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_leverage(&self.parent, leverage, market)
    }
    fn parse_market_leverage_tiers(&self, info: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_market_leverage_tiers(&self.parent, info, market)
    }
    fn parse_deposit_withdraw_fee(&self, fee: crate::Value, currency: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_deposit_withdraw_fee(&self.parent, fee, currency)
    }
    fn parse_prediction_trade(&self, trade: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_prediction_trade(&self.parent, trade, market)
    }
    fn parse_prediction_order(&self, order: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_prediction_order(&self.parent, order, market)
    }
    fn parse_prediction_position(&self, position: crate::Value, market: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::parse_prediction_position(&self.parent, position, market)
    }
    fn create_expired_option_market(&self, symbol: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::create_expired_option_market(&self.parent, symbol)
    }
    fn sign(&self, path: crate::Value, api: crate::Value, method: crate::Value, params: crate::Value, headers: crate::Value, body: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::sign(&self.parent, path, api, method, params, headers, body)
    }
    fn handle_errors(&self, code: crate::Value, reason: crate::Value, url: crate::Value, method: crate::Value, headers: crate::Value, body: crate::Value, response: crate::Value, request_headers: crate::Value, request_body: crate::Value) -> crate::Value {
        crate::exchange::DerivedExchange::handle_errors(&self.parent, code, reason, url, method, headers, body, response, request_headers, request_body)
    }
}

impl std::ops::Deref for BitmartCore {
    type Target = crate::exchanges::bitmart::BitmartCore;
    fn deref(&self) -> &Self::Target { &self.parent }
}

impl std::ops::DerefMut for BitmartCore {
    fn deref_mut(&mut self) -> &mut Self::Target { &mut self.parent }
}

impl BitmartCore {
    pub fn describe(&self) -> Value {
        return self.deep_extend(self.parent.describe(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("has".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("createOrderWs".to_string(), Value::Bool(false));
        m.insert("editOrderWs".to_string(), Value::Bool(false));
        m.insert("fetchOpenOrdersWs".to_string(), Value::Bool(false));
        m.insert("fetchOrderWs".to_string(), Value::Bool(false));
        m.insert("cancelOrderWs".to_string(), Value::Bool(false));
        m.insert("cancelOrdersWs".to_string(), Value::Bool(false));
        m.insert("cancelAllOrdersWs".to_string(), Value::Bool(false));
        m.insert("ws".to_string(), Value::Bool(true));
        m.insert("watchBalance".to_string(), Value::Bool(true));
        m.insert("watchFundingRate".to_string(), Value::Bool(true));
        m.insert("watchFundingRates".to_string(), Value::Bool(true));
        m.insert("watchTicker".to_string(), Value::Bool(true));
        m.insert("watchTickers".to_string(), Value::Bool(true));
        m.insert("watchBidsAsks".to_string(), Value::Bool(true));
        m.insert("watchOrderBook".to_string(), Value::Bool(true));
        m.insert("watchOrderBookForSymbols".to_string(), Value::Bool(true));
        m.insert("watchOrders".to_string(), Value::Bool(true));
        m.insert("watchTrades".to_string(), Value::Bool(true));
        m.insert("watchTradesForSymbols".to_string(), Value::Bool(true));
        m.insert("watchOHLCV".to_string(), Value::Bool(true));
        m.insert("watchPosition".to_string(), Value::Str("emulated".to_string()));
        m.insert("watchPositions".to_string(), Value::Bool(true));
        m.insert("unWatchBidsAsks".to_string(), Value::Bool(false));
        m.insert("unWatchOHLCV".to_string(), Value::Bool(true));
        m.insert("unWatchOrderBook".to_string(), Value::Bool(true));
        m.insert("unWatchOrderBookForSymbols".to_string(), Value::Bool(true));
        m.insert("unWatchOrders".to_string(), Value::Bool(true));
        m.insert("unWatchPositions".to_string(), Value::Bool(true));
        m.insert("unWatchTicker".to_string(), Value::Bool(true));
        m.insert("unWatchTickers".to_string(), Value::Bool(true));
        m.insert("unWatchTrades".to_string(), Value::Bool(true));
        m.insert("unWatchTradesForSymbols".to_string(), Value::Bool(true));
    m
}));
        m.insert("urls".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("api".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("ws".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("spot".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("public".to_string(), Value::Str("wss://ws-manager-compress.{hostname}/api?protocol=1.1".to_string()));
        m.insert("private".to_string(), Value::Str("wss://ws-manager-compress.{hostname}/user?protocol=1.1".to_string()));
    m
}));
        m.insert("swap".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("public".to_string(), Value::Str("wss://openapi-ws-v2.{hostname}/api?protocol=1.1".to_string()));
        m.insert("private".to_string(), Value::Str("wss://openapi-ws-v2.{hostname}/user?protocol=1.1".to_string()));
    m
}));
    m
}));
    m
}));
    m
}));
        m.insert("options".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("defaultType".to_string(), Value::Str("spot".to_string()));
        m.insert("watchBalance".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("fetchBalanceSnapshot".to_string(), Value::Bool(true));
        m.insert("awaitBalanceSnapshot".to_string(), Value::Bool(false));
    m
}));
        m.insert("watchOrderBook".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("depth".to_string(), Value::Str("depth/increase100".to_string()));
    m
}));
        m.insert("watchOrderBookForSymbols".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("depth".to_string(), Value::Str("depth/increase100".to_string()));
    m
}));
        m.insert("watchTrades".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("ignoreDuplicates".to_string(), Value::Bool(true));
    m
}));
        m.insert("ws".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("inflate".to_string(), Value::Bool(true));
    m
}));
        m.insert("timeframes".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("1m".to_string(), Value::Str("1m".to_string()));
        m.insert("3m".to_string(), Value::Str("3m".to_string()));
        m.insert("5m".to_string(), Value::Str("5m".to_string()));
        m.insert("15m".to_string(), Value::Str("15m".to_string()));
        m.insert("30m".to_string(), Value::Str("30m".to_string()));
        m.insert("45m".to_string(), Value::Str("45m".to_string()));
        m.insert("1h".to_string(), Value::Str("1H".to_string()));
        m.insert("2h".to_string(), Value::Str("2H".to_string()));
        m.insert("3h".to_string(), Value::Str("3H".to_string()));
        m.insert("4h".to_string(), Value::Str("4H".to_string()));
        m.insert("1d".to_string(), Value::Str("1D".to_string()));
        m.insert("1w".to_string(), Value::Str("1W".to_string()));
        m.insert("1M".to_string(), Value::Str("1M".to_string()));
    m
}));
    m
}));
        m.insert("streaming".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("keepAlive".to_string(), Value::Int(15000));
    m
}));
    m
})]);

    Value::Null
}

    pub async fn subscribe(&mut self, mut unifiedName: Value, mut channel: Value, mut symbol: Value, mut type_var: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut market: Value = self.market(symbol.clone());
        let mut url: Value = self.implode_hostname(get_value(&get_value(&get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string())), &type_var), &Value::Str("public".to_string())));
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut messageHash: Value = Value::Null;
        let mut rawHash: Value = Value::Null;
        let mut unsubscribe: Value = self.safe_bool_k(params.clone(), "unsubscribe", &[Value::Bool(false)]);
        let mut prefix: Value = Value::Str("".to_string());
        let mut requestOp: Value = Value::Str("subscribe".to_string());
        if is_true(&unsubscribe) {
            params = self.omit(params.clone(), Value::Str("unsubscribe".to_string()), &[]);
            prefix = Value::Str("unsubscribe::".to_string());
            requestOp = Value::Str("unsubscribe".to_string());
        }
        messageHash = add(&add(&unifiedName, &Value::Str("::".to_string())), &symbol);
        if is_equal(&type_var, &Value::Str("spot".to_string())) {
            rawHash = add(&add(&add(&Value::Str("spot/".to_string()), &channel), &Value::Str(":".to_string())), &get_value(&market, &Value::Str("id".to_string())));
            request = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("op".to_string(), requestOp.clone());
                    m.insert("args".to_string(), Value::List(vec![rawHash.clone()]));
                m
            });
        }  else {
            rawHash = add(&add(&add(&Value::Str("futures/".to_string()), &channel), &Value::Str(":".to_string())), &get_value(&market, &Value::Str("id".to_string())));
            let mut speed: Value = self.safe_string_k(params.clone(), "speed", &[]);
            if !is_equal(&speed, &Value::Null) {
                params = self.omit(params.clone(), Value::Str("speed".to_string()), &[]);
                messageHash = add(&messageHash, &add(&Value::Str(":".to_string()), &speed));
            }
            request = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("action".to_string(), requestOp.clone());
                    m.insert("args".to_string(), Value::List(vec![rawHash.clone()]));
                m
            });
        }
        messageHash = add(&prefix, &messageHash);
        let __ws_arg_0 = self.deep_extend(request.clone(), &[params.clone()]);
        return self.watch(url.clone(), messageHash.clone(), &[__ws_arg_0, messageHash.clone()]).await;

    Value::Null
}

    pub async fn subscribe_multiple(&mut self, mut unifiedName: Value, mut channel: Value, mut type_var: Value, optional_args: &[Value]) -> Value {
        let mut symbols = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        symbols = self.market_symbols(&[symbols.clone(), type_var.clone(), Value::Bool(false), Value::Bool(true)]);
        if is_equal(&symbols, &Value::Null) {
            symbols = Value::List(vec![]);
        }
        let mut url: Value = self.implode_hostname(get_value(&get_value(&get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string())), &type_var), &Value::Str("public".to_string())));
        let mut channelType: Value = ternary(is_true(&(is_equal(&type_var, &Value::Str("spot".to_string())))), Value::Str("spot".to_string()), Value::Str("futures".to_string()));
        let mut actionType: Value = ternary(is_true(&(is_equal(&type_var, &Value::Str("spot".to_string())))), Value::Str("op".to_string()), Value::Str("action".to_string()));
        let mut rawSubscriptions: Value = Value::List(vec![]);
        let mut messageHashes: Value = Value::List(vec![]);
        let mut unsubscribe: Value = self.safe_bool_k(params.clone(), "unsubscribe", &[Value::Bool(false)]);
        let mut prefix: Value = Value::Str("".to_string());
        let mut requestOp: Value = Value::Str("subscribe".to_string());
        if is_true(&unsubscribe) {
            params = self.omit(params.clone(), Value::Str("unsubscribe".to_string()), &[]);
            prefix = Value::Str("unsubscribe::".to_string());
            requestOp = Value::Str("unsubscribe".to_string());
        }
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_120: bool = true;
            while { if !__for_first_120 { i = add(&i, &Value::Int(1)); } __for_first_120 = false; is_less_than(&i, &get_array_length(&symbols)) } {
            let mut market: Value = self.market(get_value(&symbols, &i));
            let mut rawHash: Value = add(&add(&add(&add(&channelType, &Value::Str("/".to_string())), &channel), &Value::Str(":".to_string())), &get_value(&market, &Value::Str("id".to_string())));
            let mut messageHash: Value = add(&add(&add(&prefix, &unifiedName), &Value::Str("::".to_string())), &get_value(&market, &Value::Str("symbol".to_string())));
            append_to_array(&mut messageHashes, messageHash.clone());
            if !is_true(&self.subscription_exists_for_hash(url.clone(), messageHash.clone())) {
                append_to_array(&mut rawSubscriptions, rawHash.clone());
            }
        }
        }
        // as an exclusion, futures "tickers" need one generic request for all symbols
        // if ((type !== 'spot') && (channel === 'ticker')) {
        //     rawSubscriptions = [ channelType + '/' + channel ];
        // }
        // Exchange update from 2025-02-11 supports subscription by trading pair for swap
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("args".to_string(), rawSubscriptions.clone());
            m
        });
        add_element_to_object(&mut request, &actionType, requestOp.clone());
        let __ws_arg_1 = self.deep_extend(request.clone(), &[params.clone()]);
        return self.watch_multiple(url.clone(), messageHashes.clone(), &[__ws_arg_1, messageHashes.clone()]).await;

    Value::Null
}

/*
 * @method
 * @name bitmart#watchBalance
 * @see https://developer-pro.bitmart.com/en/spot/#private-balance-change
 * @see https://developer-pro.bitmart.com/en/futuresv2/#private-assets-channel
 * @description watch balance and get the amount of funds available for trading or funds locked in orders
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [balance structure]{@link https://docs.ccxt.com/?id=balance-structure}
 */
    pub async fn watch_balance(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut type_var: Value = Value::Str("spot".to_string());
        { let __destr_tmp = self.handle_market_type_and_params(Value::Str("watchBalance".to_string()), &[Value::Null, params.clone()]); type_var = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        self.authenticate(type_var.clone(), &[params.clone()]).await;
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        if is_equal(&type_var, &Value::Str("spot".to_string())) {
            request = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("op".to_string(), Value::Str("subscribe".to_string()));
                    m.insert("args".to_string(), Value::List(vec![Value::Str("spot/user/balance:BALANCE_UPDATE".to_string())]));
                m
            });
        }  else {
            request = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("action".to_string(), Value::Str("subscribe".to_string()));
                    m.insert("args".to_string(), Value::List(vec![Value::Str("futures/asset:USDT".to_string()), Value::Str("futures/asset:BTC".to_string()), Value::Str("futures/asset:ETH".to_string())]));
                m
            });
        }
        let mut messageHash: Value = add(&Value::Str("balance:".to_string()), &type_var);
        let mut url: Value = self.implode_hostname(get_value(&get_value(&get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string())), &type_var), &Value::Str("private".to_string())));
        let mut client: Value = self.client(&[url.clone()]);
        self.set_balance_cache(client.clone(), type_var.clone(), messageHash.clone());
        let mut fetchBalanceSnapshot: Value = Value::Null;
        let mut awaitBalanceSnapshot: Value = Value::Null;
        { let __destr_tmp = self.handle_option_and_params(params.clone(), Value::Str("watchBalance".to_string()), Value::Str("fetchBalanceSnapshot".to_string()), &[Value::Bool(true)]); fetchBalanceSnapshot = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        { let __destr_tmp = self.handle_option_and_params(params.clone(), Value::Str("watchBalance".to_string()), Value::Str("awaitBalanceSnapshot".to_string()), &[Value::Bool(false)]); awaitBalanceSnapshot = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        if is_true(&fetchBalanceSnapshot) && is_true(&awaitBalanceSnapshot) {
            client.future(&[add(&type_var, &Value::Str(":fetchBalanceSnapshot".to_string()))]);
        }
        let __ws_arg_2 = self.deep_extend(request.clone(), &[params.clone()]);
        return self.watch(url.clone(), messageHash.clone(), &[__ws_arg_2, messageHash.clone()]).await;

    Value::Null
}

    pub fn set_balance_cache(&mut self, mut client: Value, mut type_var: Value, mut subscribeHash: Value) {
        if is_true(&Value::Bool(in_op(&get_value(&client, &Value::Str("subscriptions".to_string())), &subscribeHash))) {
            return;
        }
        let mut options: Value = self.safe_dict_k(self.options.clone(), "watchBalance", &[]);
        let mut snapshot: Value = self.safe_bool_k(options.clone(), "fetchBalanceSnapshot", &[Value::Bool(true)]);
        if is_true(&snapshot) {
            let mut messageHash: Value = add(&add(&type_var, &Value::Str(":".to_string())), &Value::Str("fetchBalanceSnapshot".to_string()));
            if !is_true(&(Value::Bool(in_op(&get_value(&client, &Value::Str("futures".to_string())), &messageHash)))) {
                client.future(&[messageHash.clone()]);
                self.spawn(&[Value::Null.clone(), client.clone(), messageHash.clone(), type_var.clone()]);
            }
        }
        add_element_to_object(&mut self.balance.clone(), &type_var, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
}

    pub async fn load_balance_snapshot(&mut self, mut client: Value, mut messageHash: Value, mut type_var: Value) -> Value {
        let mut response: Value = self.fetch_balance(&[Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("type".to_string(), type_var.clone());
            m
        })]).await;
        let __ws_arg_3 = self.safe_value(self.balance.clone(), type_var.clone(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        { let __be_tmp = self.extend(response.clone(), &[__ws_arg_3]); add_element_to_object(&mut self.balance, &type_var, __be_tmp); };
        // don't remove the future from the .futures cache
        if is_true(&Value::Bool(in_op(&get_value(&client, &Value::Str("futures".to_string())), &messageHash))) {
            let mut future: Value = get_value(&get_value(&client, &Value::Str("futures".to_string())), &messageHash);
            future.resolve(&[]);
            client.resolve(&[get_value(&self.balance, &type_var), add(&Value::Str("balance:".to_string()), &type_var)]);
        }

    Value::Null
}

    pub fn handle_balance(&self, mut client: Value, mut message: Value) {
        //
        // spot
        //    {
        //        "data":[
        //           {
        //              "balance_details":[
        //                 {
        //                    "av_bal":"0.206000000000000000000000000000",
        //                    "ccy":"LTC",
        //                    "fz_bal":"0.100000000000000000000000000000"
        //                 }
        //              ],
        //              "event_time":"1701632345416",
        //              "event_type":"TRANSACTION_COMPLETED"
        //           }
        //        ],
        //        "table":"spot/user/balance"
        //    }
        // swap
        //    {
        //        group: 'futures/asset:USDT',
        //        data: {
        //            currency: 'USDT',
        //            available_balance: '37.19688649135',
        //            position_deposit: '0.788687546',
        //            frozen_balance: '0'
        //        }
        //    }
        //
        let mut channel: Value = self.safe_string2(message.clone(), Value::Str("table".to_string()), Value::Str("group".to_string()), &[]);
        if is_equal(&channel, &Value::Null) {
            return;
        }
        let mut data: Value = self.safe_value_k(message.clone(), "data", &[]);
        if is_equal(&data, &Value::Null) {
            return;
        }
        let mut isSpot: Value = Value::Bool(is_greater_than_or_equal(&get_index_of(&channel, &Value::Str("spot".to_string())), &Value::Int(0)));
        let mut type_var: Value = ternary(is_true(&isSpot), Value::Str("spot".to_string()), Value::Str("swap".to_string()));
        add_element_to_object(get_value_mut(unsafe { crate::runtime::coerce_value_to_mut(&self.balance) }, &type_var), &Value::Str("info".to_string()), message.clone());
        if is_true(&isSpot) {
            if !is_true(&Value::Bool(is_array(&data))) {
                return;
            }
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_122: bool = true;
                while { if !__for_first_122 { i = add(&i, &Value::Int(1)); } __for_first_122 = false; is_less_than(&i, &get_array_length(&data)) } {
                let mut timestamp: Value = self.safe_integer_k(message.clone(), "event_time", &[]);
                add_element_to_object(get_value_mut(unsafe { crate::runtime::coerce_value_to_mut(&self.balance) }, &type_var), &Value::Str("timestamp".to_string()), timestamp.clone());
                { let __be_tmp = self.iso8601(timestamp.clone()); add_element_to_object(get_value_mut(unsafe { crate::runtime::coerce_value_to_mut(&self.balance) }, &type_var), &Value::Str("datetime".to_string()), __be_tmp); };
                let mut balanceDetails: Value = self.safe_list_k(get_value(&data, &i), "balance_details", &[Value::List(vec![])]);
                {
                                        let mut ii: Value = Value::Int(0);
                    let mut __for_first_121: bool = true;
                    while { if !__for_first_121 { ii = add(&ii, &Value::Int(1)); } __for_first_121 = false; is_less_than(&ii, &get_array_length(&balanceDetails)) } {
                    let mut rawBalance: Value = get_value(&balanceDetails, &i);
                    let mut rawBalance: Value = get_value(&balanceDetails, &i);
                    let mut account: Value = self.account();
                    let mut currencyId: Value = self.safe_string_k(rawBalance.clone(), "ccy", &[]);
                    let mut code: Value = self.safe_currency_code(currencyId.clone(), &[]);
                    add_element_to_object(&mut account, &Value::Str("free".to_string()), self.safe_string_k(rawBalance.clone(), "av_bal", &[]));
                    add_element_to_object(&mut account, &Value::Str("used".to_string()), self.safe_string_k(rawBalance.clone(), "fz_bal", &[]));
                    add_element_to_object(get_value_mut(unsafe { crate::runtime::coerce_value_to_mut(&self.balance) }, &type_var), &code, account.clone());
                }
                }
            }
            }
        }  else {
            let mut currencyId: Value = self.safe_string_k(data.clone(), "currency", &[]);
            let mut code: Value = self.safe_currency_code(currencyId.clone(), &[]);
            let mut account: Value = self.account();
            add_element_to_object(&mut account, &Value::Str("free".to_string()), self.safe_string_k(data.clone(), "available_balance", &[]));
            add_element_to_object(&mut account, &Value::Str("used".to_string()), self.safe_string_k(data.clone(), "frozen_balance", &[]));
            add_element_to_object(get_value_mut(unsafe { crate::runtime::coerce_value_to_mut(&self.balance) }, &type_var), &code, account.clone());
        }
        { let __be_tmp = self.safe_balance(get_value(&self.balance, &type_var)); add_element_to_object(&mut self.balance.clone(), &type_var, __be_tmp); };
        let mut messageHash: Value = add(&Value::Str("balance:".to_string()), &type_var);
        client.resolve(&[get_value(&self.balance, &type_var), messageHash.clone()]);
}

/*
 * @method
 * @name bitmart#watchTrades
 * @see https://developer-pro.bitmart.com/en/spot/#public-trade-channel
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-trade-channel
 * @description get the list of most recent trades for a particular symbol
 * @param {string} symbol unified symbol of the market to fetch trades for
 * @param {int} [since] timestamp in ms of the earliest trade to fetch
 * @param {int} [limit] the maximum amount of trades to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=public-trades}
 */
    pub async fn watch_trades(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut since = get_arg(optional_args, 0, Value::Null);
        let mut limit = get_arg(optional_args, 1, Value::Null);
        let mut params = get_arg(optional_args, 2, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        return self.watch_trades_for_symbols(Value::List(vec![symbol.clone()]), &[since.clone(), limit.clone(), params.clone()]).await;

    Value::Null
}

/*
 * @method
 * @name bitmart#watchTradesForSymbols
 * @see https://developer-pro.bitmart.com/en/spot/#public-trade-channel
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-trade-channel
 * @description get the list of most recent trades for a list of symbols
 * @param {string[]} symbols unified symbol of the market to fetch trades for
 * @param {int} [since] timestamp in ms of the earliest trade to fetch
 * @param {int} [limit] the maximum amount of trades to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=public-trades}
 */
    pub async fn watch_trades_for_symbols(&mut self, mut symbols: Value, optional_args: &[Value]) -> Value {
        let mut since = get_arg(optional_args, 0, Value::Null);
        let mut limit = get_arg(optional_args, 1, Value::Null);
        let mut params = get_arg(optional_args, 2, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut marketType: Value = Value::Null;
        { let __destr_tmp = self.get_params_for_multiple_sub(Value::Str("watchTradesForSymbols".to_string()), symbols.clone(), &[limit.clone(), params.clone()]); symbols = get_value(&__destr_tmp, &Value::Int(0)); marketType = get_value(&__destr_tmp, &Value::Int(1)); params = get_value(&__destr_tmp, &Value::Int(2)); }
        let mut channelName: Value = Value::Str("trade".to_string());
        let mut trades: Value = self.subscribe_multiple(Value::Str("trade".to_string()), channelName.clone(), marketType.clone(), &[symbols.clone(), params.clone()]).await;
        if is_true(&self.newUpdates) {
            let mut first: Value = self.safe_dict(trades.clone(), Value::Int(0), &[]);
            let mut tradeSymbol: Value = self.safe_string_k(first.clone(), "symbol", &[]);
            limit = trades.get_limit(tradeSymbol.clone(), limit.clone());
        }
        let mut result: Value = self.filter_by_since_limit(trades.clone(), &[since.clone(), limit.clone(), Value::Str("timestamp".to_string()), Value::Bool(true)]);
        if is_true(&self.handle_option(Value::Str("watchTrades".to_string()), Value::Str("ignoreDuplicates".to_string()), &[Value::Bool(true)])) {
            let mut filtered: Value = self.remove_repeated_trades_from_array(result.clone());
            filtered = self.sort_by(filtered.clone(), Value::Str("timestamp".to_string()), &[]);
            return filtered;
        }
        return result;

    Value::Null
}

/*
 * @method
 * @name bitmart#unWatchTrades
 * @description unWatches from the stream channel
 * @see https://developer-pro.bitmart.com/en/spot/#public-trade-channel
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-trade-channel
 * @param {string} symbol unified symbol of the market to fetch trades for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=public-trades}
 */
    pub async fn un_watch_trades(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        return self.un_watch_trades_for_symbols(Value::List(vec![symbol.clone()]), &[params.clone()]).await;

    Value::Null
}

/*
 * @method
 * @name bitmart#unWatchTradesForSymbols
 * @description unsubscribes from the trades channel
 * @see https://developer-pro.bitmart.com/en/spot/#public-trade-channel
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-trade-channel
 * @param {string[]} symbols unified symbol of the market to fetch trades for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=public-trades}
 */
    pub async fn un_watch_trades_for_symbols(&mut self, mut symbols: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut marketType: Value = Value::Null;
        { let __destr_tmp = self.get_params_for_multiple_sub(Value::Str("unWatchTradesForSymbols".to_string()), symbols.clone(), &[Value::Null, params.clone()]); symbols = get_value(&__destr_tmp, &Value::Int(0)); marketType = get_value(&__destr_tmp, &Value::Int(1)); params = get_value(&__destr_tmp, &Value::Int(2)); }
        let mut channelName: Value = Value::Str("trade".to_string());
        params = self.extend(params.clone(), &[Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("unsubscribe".to_string(), Value::Bool(true));
            m
        })]);
        return self.subscribe_multiple(Value::Str("trade".to_string()), channelName.clone(), marketType.clone(), &[symbols.clone(), params.clone()]).await;

    Value::Null
}

    pub fn get_params_for_multiple_sub(&self, mut methodName: Value, mut symbols: Value, optional_args: &[Value]) -> Value {
        let mut limit = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        symbols = self.market_symbols(&[symbols.clone(), Value::Null, Value::Bool(false), Value::Bool(true)]);
        let mut length: Value = get_array_length(&symbols);
        if is_greater_than(&length, &Value::Int(20)) {
            panic!("{}", crate::exchange_errors::not_supported(add(&add(&add(&self.id, &Value::Str(" ".to_string())), &methodName), &Value::Str("() accepts a maximum of 20 symbols in one request".to_string()))));
        }
        let mut market: Value = self.market(get_value(&symbols, &Value::Int(0)));
        let mut marketType: Value = Value::Null;
        { let __destr_tmp = self.handle_market_type_and_params(methodName.clone(), &[market.clone(), params.clone()]); marketType = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        return Value::List(vec![symbols.clone(), marketType.clone(), params.clone()]);

    Value::Null
}

/*
 * @method
 * @name bitmart#watchTicker
 * @see https://developer-pro.bitmart.com/en/spot/#public-ticker-channel
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-ticker-channel
 * @description watches a price ticker, a statistical calculation with the information calculated over the past 24 hours for a specific market
 * @param {string} symbol unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
    pub async fn watch_ticker(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        symbol = self.symbol(symbol.clone());
        let mut tickers: Value = self.watch_tickers(&[Value::List(vec![symbol.clone()]), params.clone()]).await;
        return get_value(&tickers, &symbol);

    Value::Null
}

/*
 * @method
 * @name bitmart#watchTickers
 * @see https://developer-pro.bitmart.com/en/spot/#public-ticker-channel
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-ticker-channel
 * @description watches a price ticker, a statistical calculation with the information calculated over the past 24 hours for all markets of a specific list
 * @param {string[]} symbols unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
    pub async fn watch_tickers(&mut self, optional_args: &[Value]) -> Value {
        let mut symbols = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.get_market_from_symbols(&[symbols.clone()]);
        let mut marketType: Value = Value::Null;
        { let __destr_tmp = self.handle_market_type_and_params(Value::Str("watchTickers".to_string()), &[market.clone(), params.clone()]); marketType = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        let mut ticker: Value = self.subscribe_multiple(Value::Str("ticker".to_string()), Value::Str("ticker".to_string()), marketType.clone(), &[symbols.clone(), params.clone()]).await;
        if is_true(&self.newUpdates) {
            let mut tickers: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                m
            });
            add_element_to_object(&mut tickers, &get_value(&ticker, &Value::Str("symbol".to_string())), ticker.clone());
            return tickers;
        }
        return self.filter_by_array(self.tickers.clone(), Value::Str("symbol".to_string()), &[symbols.clone()]);

    Value::Null
}

/*
 * @method
 * @name bitmart#unWatchTicker
 * @description unWatches a price ticker, a statistical calculation with the information calculated over the past 24 hours for a specific market
 * @see https://developer-pro.bitmart.com/en/spot/#public-ticker-channel
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-ticker-channel
 * @param {string} symbol unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
    pub async fn un_watch_ticker(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        return self.un_watch_tickers(&[Value::List(vec![symbol.clone()]), params.clone()]).await;

    Value::Null
}

/*
 * @method
 * @name bitmart#unWatchTickers
 * @description unWatches a price ticker, a statistical calculation with the information calculated over the past 24 hours for all markets of a specific list
 * @see https://developer-pro.bitmart.com/en/spot/#public-ticker-channel
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-ticker-channel
 * @param {string[]} symbols unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
    pub async fn un_watch_tickers(&mut self, optional_args: &[Value]) -> Value {
        let mut symbols = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.get_market_from_symbols(&[symbols.clone()]);
        let mut marketType: Value = Value::Null;
        { let __destr_tmp = self.handle_market_type_and_params(Value::Str("watchTickers".to_string()), &[market.clone(), params.clone()]); marketType = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        params = self.extend(params.clone(), &[Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("unsubscribe".to_string(), Value::Bool(true));
            m
        })]);
        return self.subscribe_multiple(Value::Str("ticker".to_string()), Value::Str("ticker".to_string()), marketType.clone(), &[symbols.clone(), params.clone()]).await;

    Value::Null
}

/*
 * @method
 * @name bitmart#watchBidsAsks
 * @see https://developer-pro.bitmart.com/en/spot/#public-ticker-channel
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-ticker-channel
 * @description watches best bid & ask for symbols
 * @param {string[]} symbols unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
    pub async fn watch_bids_asks(&mut self, optional_args: &[Value]) -> Value {
        let mut symbols = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        symbols = self.market_symbols(&[symbols.clone(), Value::Null, Value::Bool(false)]);
        if is_equal(&symbols, &Value::Null) {
            symbols = Value::List(vec![]);
        }
        let mut firstMarket: Value = self.get_market_from_symbols(&[symbols.clone()]);
        let mut marketType: Value = Value::Null;
        { let __destr_tmp = self.handle_market_type_and_params(Value::Str("watchBidsAsks".to_string()), &[firstMarket.clone(), params.clone()]); marketType = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        let mut url: Value = self.implode_hostname(get_value(&get_value(&get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string())), &marketType), &Value::Str("public".to_string())));
        let mut channelType: Value = ternary(is_true(&(is_equal(&marketType, &Value::Str("spot".to_string())))), Value::Str("spot/bookTicker".to_string()), Value::Str("futures/ticker".to_string()));
        let mut actionType: Value = ternary(is_true(&(is_equal(&marketType, &Value::Str("spot".to_string())))), Value::Str("op".to_string()), Value::Str("action".to_string()));
        let mut rawSubscriptions: Value = Value::List(vec![]);
        let mut messageHashes: Value = Value::List(vec![]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_123: bool = true;
            while { if !__for_first_123 { i = add(&i, &Value::Int(1)); } __for_first_123 = false; is_less_than(&i, &get_array_length(&symbols)) } {
            let mut market: Value = self.market(get_value(&symbols, &i));
            let mut rawHash: Value = add(&add(&channelType, &Value::Str(":".to_string())), &get_value(&market, &Value::Str("id".to_string())));
            let mut messageHash: Value = add(&Value::Str("bidask::".to_string()), &get_value(&market, &Value::Str("symbol".to_string())));
            append_to_array(&mut messageHashes, messageHash.clone());
            if !is_true(&self.subscription_exists_for_hash(url.clone(), messageHash.clone())) {
                append_to_array(&mut rawSubscriptions, rawHash.clone());
            }
        }
        }
        if !is_equal(&marketType, &Value::Str("spot".to_string())) {
            rawSubscriptions = Value::List(vec![channelType.clone()]);
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("args".to_string(), rawSubscriptions.clone());
            m
        });
        add_element_to_object(&mut request, &actionType, Value::Str("subscribe".to_string()));
        let mut newTickers: Value = self.watch_multiple(url.clone(), messageHashes.clone(), &[request.clone(), messageHashes.clone()]).await;
        if is_true(&self.newUpdates) {
            let mut tickers: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                m
            });
            add_element_to_object(&mut tickers, &get_value(&newTickers, &Value::Str("symbol".to_string())), newTickers.clone());
            return tickers;
        }
        return self.filter_by_array(self.bidsasks.clone(), Value::Str("symbol".to_string()), &[symbols.clone()]);

    Value::Null
}

    pub fn handle_bid_ask(&self, mut client: Value, mut message: Value) {
        let mut table: Value = self.safe_string_k(message.clone(), "table", &[]);
        let mut isSpot: Value = Value::Bool(!is_equal(&table, &Value::Null));
        let mut rawTickers: Value = Value::List(vec![]);
        if is_true(&isSpot) {
            rawTickers = self.safe_list_k(message.clone(), "data", &[Value::List(vec![])]);
        }  else {
            rawTickers = Value::List(vec![self.safe_dict_k(message.clone(), "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})])]);
        }
        if !is_true(&get_array_length(&rawTickers)) {
            return;
        }
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_124: bool = true;
            while { if !__for_first_124 { i = add(&i, &Value::Int(1)); } __for_first_124 = false; is_less_than(&i, &get_array_length(&rawTickers)) } {
            let mut ticker: Value = self.parse_ws_bid_ask(get_value(&rawTickers, &i), &[]);
            let mut symbol: Value = get_value(&ticker, &Value::Str("symbol".to_string()));
            if !is_equal(&symbol, &Value::Null) {
                add_element_to_object(&mut self.bidsasks.clone(), &symbol, ticker.clone());
            }
            let mut messageHash: Value = add(&Value::Str("bidask::".to_string()), &symbol);
            client.resolve(&[ticker.clone(), messageHash.clone()]);
        }
        }
}

    pub fn parse_ws_bid_ask(&self, mut ticker: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        let mut marketId: Value = self.safe_string_k(ticker.clone(), "symbol", &[]);
        market = self.safe_market(&[marketId.clone(), market.clone()]);
        let mut symbol: Value = self.safe_string_k(market.clone(), "symbol", &[]);
        let mut timestamp: Value = self.safe_integer_k(ticker.clone(), "ms_t", &[]);
        return self.safe_ticker(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("symbol".to_string(), symbol.clone());
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp.clone()));
        m.insert("ask".to_string(), self.safe_string2(ticker.clone(), Value::Str("ask_px".to_string()), Value::Str("ask_price".to_string()), &[]));
        m.insert("askVolume".to_string(), self.safe_string2(ticker.clone(), Value::Str("ask_sz".to_string()), Value::Str("ask_vol".to_string()), &[]));
        m.insert("bid".to_string(), self.safe_string2(ticker.clone(), Value::Str("bid_px".to_string()), Value::Str("bid_price".to_string()), &[]));
        m.insert("bidVolume".to_string(), self.safe_string2(ticker.clone(), Value::Str("bid_sz".to_string()), Value::Str("bid_vol".to_string()), &[]));
        m.insert("info".to_string(), ticker.clone());
    m
}), &[market.clone()]);

    Value::Null
}

/*
 * @method
 * @name bitmart#watchOrders
 * @description watches information on multiple orders made by the user
 * @see https://developer-pro.bitmart.com/en/spot/#private-order-progress
 * @see https://developer-pro.bitmart.com/en/futuresv2/#private-order-channel
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn watch_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = Value::Null;
        let mut messageHash: Value = Value::Str("orders".to_string());
        if !is_equal(&symbol, &Value::Null) {
            symbol = self.symbol(symbol.clone());
            market = self.market(symbol.clone());
            messageHash = add(&Value::Str("orders::".to_string()), &symbol);
        }
        let mut type_var: Value = Value::Str("spot".to_string());
        { let __destr_tmp = self.handle_market_type_and_params(Value::Str("watchOrders".to_string()), &[market.clone(), params.clone()]); type_var = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        self.authenticate(type_var.clone(), &[params.clone()]).await;
        let mut request: Value = Value::Null;
        if is_equal(&type_var, &Value::Str("spot".to_string())) {
            let mut argsRequest: Value = Value::Str("spot/user/order:".to_string());
            if is_true(&(!is_equal(&symbol, &Value::Null))) && is_true(&(!is_equal(&market, &Value::Null))) {
                argsRequest = add(&argsRequest, &get_value(&market, &Value::Str("id".to_string())));
            }  else {
                argsRequest = Value::Str("spot/user/orders:ALL_SYMBOLS".to_string());
            }
            request = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("op".to_string(), Value::Str("subscribe".to_string()));
                    m.insert("args".to_string(), Value::List(vec![argsRequest.clone()]));
                m
            });
        }  else {
            request = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("action".to_string(), Value::Str("subscribe".to_string()));
                    m.insert("args".to_string(), Value::List(vec![Value::Str("futures/order".to_string())]));
                m
            });
        }
        let mut url: Value = self.implode_hostname(get_value(&get_value(&get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string())), &type_var), &Value::Str("private".to_string())));
        let __ws_arg_4 = self.deep_extend(request.clone(), &[params.clone()]);
        let mut newOrders: Value = self.watch(url.clone(), messageHash.clone(), &[__ws_arg_4, messageHash.clone()]).await;
        if is_true(&self.newUpdates) {
            return newOrders;
        }
        return self.filter_by_symbol_since_limit(self.orders.clone(), &[symbol.clone(), since.clone(), limit.clone(), Value::Bool(true)]);

    Value::Null
}

/*
 * @method
 * @name bitmart#unWatchOrders
 * @description unWatches information on multiple orders made by the user
 * @see https://developer-pro.bitmart.com/en/spot/#private-order-progress
 * @see https://developer-pro.bitmart.com/en/futuresv2/#private-order-channel
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn un_watch_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = Value::Null;
        let mut messageHash: Value = Value::Str("unsubscribe::orders".to_string());
        if !is_equal(&symbol, &Value::Null) {
            symbol = self.symbol(symbol.clone());
            market = self.market(symbol.clone());
            if is_true(&get_value(&market, &Value::Str("swap".to_string()))) {
                panic!("{}", crate::exchange_errors::not_supported(add(&self.id, &Value::Str(" unWatchOrders() does not support a symbol for swap markets, unWatch from all markets only".to_string()))));
            }
            messageHash = add(&messageHash, &add(&Value::Str("::".to_string()), &symbol));
        }
        let mut type_var: Value = Value::Str("spot".to_string());
        { let __destr_tmp = self.handle_market_type_and_params(Value::Str("watchOrders".to_string()), &[market.clone(), params.clone()]); type_var = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        self.authenticate(type_var.clone(), &[params.clone()]).await;
        let mut request: Value = Value::Null;
        if is_equal(&type_var, &Value::Str("spot".to_string())) {
            let mut argsRequest: Value = Value::Str("spot/user/order:".to_string());
            if is_true(&(!is_equal(&symbol, &Value::Null))) && is_true(&(!is_equal(&market, &Value::Null))) {
                argsRequest = add(&argsRequest, &get_value(&market, &Value::Str("id".to_string())));
            }  else {
                argsRequest = Value::Str("spot/user/orders:ALL_SYMBOLS".to_string());
            }
            request = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("op".to_string(), Value::Str("unsubscribe".to_string()));
                    m.insert("args".to_string(), Value::List(vec![argsRequest.clone()]));
                m
            });
        }  else {
            request = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("action".to_string(), Value::Str("unsubscribe".to_string()));
                    m.insert("args".to_string(), Value::List(vec![Value::Str("futures/order".to_string())]));
                m
            });
        }
        let mut url: Value = self.implode_hostname(get_value(&get_value(&get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string())), &type_var), &Value::Str("private".to_string())));
        let __ws_arg_5 = self.deep_extend(request.clone(), &[params.clone()]);
        return self.watch(url.clone(), messageHash.clone(), &[__ws_arg_5, messageHash.clone()]).await;

    Value::Null
}

    pub fn handle_orders(&mut self, mut client: Value, mut message: Value) {
        //
        // spot
        //    {
        //        "data":[
        //            {
        //                "symbol": "LTC_USDT",
        //                "notional": '',
        //                "side": "buy",
        //                "last_fill_time": "0",
        //                "ms_t": "1646216634000",
        //                "type": "limit",
        //                "filled_notional": "0.000000000000000000000000000000",
        //                "last_fill_price": "0",
        //                "size": "0.500000000000000000000000000000",
        //                "price": "50.000000000000000000000000000000",
        //                "last_fill_count": "0",
        //                "filled_size": "0.000000000000000000000000000000",
        //                "margin_trading": "0",
        //                "state": "8",
        //                "order_id": "24807076628",
        //                "order_type": "0"
        //              }
        //        ],
        //        "table":"spot/user/order"
        //    }
        // swap
        //    {
        //        "group":"futures/order",
        //        "data":[
        //           {
        //              "action":2,
        //              "order":{
        //                 "order_id":"2312045036986775",
        //                 "client_order_id":"",
        //                 "price":"71.61707928",
        //                 "size":"1",
        //                 "symbol":"LTCUSDT",
        //                 "state":1,
        //                 "side":4,
        //                 "type":"market",
        //                 "leverage":"1",
        //                 "open_type":"cross",
        //                 "deal_avg_price":"0",
        //                 "deal_size":"0",
        //                 "create_time":1701625324646,
        //                 "update_time":1701625324640,
        //                 "plan_order_id":"",
        //                 "last_trade":null
        //              }
        //           }
        //        ]
        //    }
        //
        let mut orders: Value = self.safe_list_k(message.clone(), "data", &[]);
        if is_equal(&orders, &Value::Null) {
            return;
        }
        let mut ordersLength: Value = get_array_length(&orders);
        let mut newOrders: Value = Value::List(vec![]);
        let mut symbols: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        if is_greater_than(&ordersLength, &Value::Int(0)) {
            let mut limit: Value = self.safe_integer_k(self.options.clone(), "ordersLimit", &[Value::Int(1000)]);
            if is_equal(&self.orders, &Value::Null) {
                self.orders = ArrayCacheBySymbolById::new(limit.clone());
            }
            let mut stored: Value = self.orders.clone();
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_125: bool = true;
                while { if !__for_first_125 { i = add(&i, &Value::Int(1)); } __for_first_125 = false; is_less_than(&i, &get_array_length(&orders)) } {
                let mut order: Value = self.parse_ws_order(get_value(&orders, &i), &[]);
                stored.append(order.clone());
                append_to_array(&mut newOrders, order.clone());
                let mut symbol: Value = get_value(&order, &Value::Str("symbol".to_string()));
                if !is_equal(&symbol, &Value::Null) {
                    add_element_to_object(&mut symbols, &symbol, Value::Bool(true));
                }
            }
            }
        }
        let mut messageHash: Value = Value::Str("orders".to_string());
        let mut symbolKeys: Value = object_keys(&symbols);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_126: bool = true;
            while { if !__for_first_126 { i = add(&i, &Value::Int(1)); } __for_first_126 = false; is_less_than(&i, &get_array_length(&symbolKeys)) } {
            let mut symbol: Value = get_value(&symbolKeys, &i);
            let mut symbol: Value = get_value(&symbolKeys, &i);
            let mut symbolSpecificMessageHash: Value = add(&add(&messageHash, &Value::Str("::".to_string())), &symbol);
            client.resolve(&[newOrders.clone(), symbolSpecificMessageHash.clone()]);
        }
        }
        client.resolve(&[newOrders.clone(), messageHash.clone()]);
}

    pub fn parse_ws_order(&self, mut order: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        // spot
        //    {
        //        "symbol": "LTC_USDT",
        //        "notional": '',
        //        "side": "buy",
        //        "last_fill_time": "0",
        //        "ms_t": "1646216634000",
        //        "type": "limit",
        //        "filled_notional": "0.000000000000000000000000000000",
        //        "last_fill_price": "0",
        //        "size": "0.500000000000000000000000000000",
        //        "price": "50.000000000000000000000000000000",
        //        "last_fill_count": "0",
        //        "filled_size": "0.000000000000000000000000000000",
        //        "margin_trading": "0",
        //        "state": "8",
        //        "order_id": "24807076628",
        //        "order_type": "0"
        //    }
        // swap
        //    {
        //       "action":2,
        //       "order":{
        //          "order_id":"2312045036986775",
        //          "client_order_id":"",
        //          "price":"71.61707928",
        //          "size":"1",
        //          "symbol":"LTCUSDT",
        //          "state":1,
        //          "side":4,
        //          "type":"market",
        //          "leverage":"1",
        //          "open_type":"cross",
        //          "deal_avg_price":"0",
        //          "deal_size":"0",
        //          "create_time":1701625324646,
        //          "update_time":1701625324640,
        //          "plan_order_id":"",
        //          "last_trade":null
        //       }
        //    }
        //
        let mut action: Value = self.safe_number_k(order.clone(), "action", &[]);
        let mut isSpot: Value = Value::Bool(is_equal(&action, &Value::Null));
        if is_true(&isSpot) {
            let mut marketId: Value = self.safe_string_k(order.clone(), "symbol", &[]);
            market = self.safe_market(&[marketId.clone(), market.clone(), Value::Str("_".to_string()), Value::Str("spot".to_string())]);
            let mut id: Value = self.safe_string_k(order.clone(), "order_id", &[]);
            let mut clientOrderId: Value = self.safe_string_k(order.clone(), "clientOid", &[]);
            let mut price: Value = self.safe_string_k(order.clone(), "price", &[]);
            let mut filled: Value = self.safe_string_k(order.clone(), "filled_size", &[]);
            let mut amount: Value = self.safe_string_k(order.clone(), "size", &[]);
            let mut type_var: Value = self.safe_string_k(order.clone(), "type", &[]);
            let mut rawState: Value = self.safe_string_k(order.clone(), "state", &[]);
            let mut status: Value = self.parse_order_status_by_type(get_value(&market, &Value::Str("type".to_string())), rawState.clone());
            let mut timestamp: Value = self.safe_integer_k(order.clone(), "ms_t", &[]);
            let mut symbol: Value = get_value(&market, &Value::Str("symbol".to_string()));
            let mut side: Value = self.safe_string_lower(order.clone(), Value::Str("side".to_string()), &[]);
            return self.safe_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), order.clone());
        m.insert("symbol".to_string(), symbol.clone());
        m.insert("id".to_string(), id.clone());
        m.insert("clientOrderId".to_string(), clientOrderId.clone());
        m.insert("timestamp".to_string(), Value::Null);
        m.insert("datetime".to_string(), Value::Null);
        m.insert("lastTradeTimestamp".to_string(), timestamp.clone());
        m.insert("type".to_string(), type_var.clone());
        m.insert("timeInForce".to_string(), Value::Null);
        m.insert("postOnly".to_string(), Value::Null);
        m.insert("side".to_string(), side.clone());
        m.insert("price".to_string(), price.clone());
        m.insert("stopPrice".to_string(), Value::Null);
        m.insert("triggerPrice".to_string(), Value::Null);
        m.insert("amount".to_string(), amount.clone());
        m.insert("cost".to_string(), Value::Null);
        m.insert("average".to_string(), Value::Null);
        m.insert("filled".to_string(), filled.clone());
        m.insert("remaining".to_string(), Value::Null);
        m.insert("status".to_string(), status.clone());
        m.insert("fee".to_string(), Value::Null);
        m.insert("trades".to_string(), Value::Null);
    m
}), &[market.clone()]);
        }  else {
            let mut orderInfo: Value = self.safe_dict_k(order.clone(), "order", &[]);
            let mut marketId: Value = self.safe_string_k(orderInfo.clone(), "symbol", &[]);
            let mut symbol: Value = self.safe_symbol(marketId.clone(), &[market.clone(), Value::Str("".to_string()), Value::Str("swap".to_string())]);
            let mut orderId: Value = self.safe_string_k(orderInfo.clone(), "order_id", &[]);
            let mut timestamp: Value = self.safe_integer_k(orderInfo.clone(), "create_time", &[]);
            let mut updatedTimestamp: Value = self.safe_integer_k(orderInfo.clone(), "update_time", &[]);
            let mut lastTrade: Value = self.safe_dict_k(orderInfo.clone(), "last_trade", &[]);
            let mut cachedOrders: Value = self.orders.clone();
            let mut orders: Value = self.safe_value(cachedOrders.hashmap(), symbol.clone(), &[Value::Map({
                let mut m = indexmap::IndexMap::new();
                m
            })]);
            let mut cachedOrder: Value = ternary(is_true(&(is_equal(&orderId, &Value::Null))), Value::Null, self.safe_value(orders.clone(), orderId.clone(), &[]));
            let mut trades: Value = Value::Null;
            if !is_equal(&cachedOrder, &Value::Null) {
                trades = self.safe_value_k(order.clone(), "trades", &[]);
            }
            if !is_equal(&lastTrade, &Value::Null) {
                if is_equal(&trades, &Value::Null) {
                    trades = Value::List(vec![]);
                }
                append_to_array(&mut trades, lastTrade.clone());
            }
            return self.safe_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), order.clone());
        m.insert("symbol".to_string(), symbol.clone());
        m.insert("id".to_string(), orderId.clone());
        m.insert("clientOrderId".to_string(), self.safe_string_k(orderInfo.clone(), "client_order_id", &[]));
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp.clone()));
        m.insert("lastTradeTimestamp".to_string(), updatedTimestamp.clone());
        m.insert("type".to_string(), self.safe_string_k(orderInfo.clone(), "type", &[]));
        m.insert("timeInForce".to_string(), Value::Null);
        m.insert("postOnly".to_string(), Value::Null);
        m.insert("side".to_string(), self.parse_ws_order_side(self.safe_string_k(orderInfo.clone(), "side", &[])));
        m.insert("price".to_string(), self.safe_string_k(orderInfo.clone(), "price", &[]));
        m.insert("stopPrice".to_string(), Value::Null);
        m.insert("triggerPrice".to_string(), Value::Null);
        m.insert("amount".to_string(), self.safe_string_k(orderInfo.clone(), "size", &[]));
        m.insert("cost".to_string(), Value::Null);
        m.insert("average".to_string(), self.safe_string_k(orderInfo.clone(), "deal_avg_price", &[]));
        m.insert("filled".to_string(), self.safe_string_k(orderInfo.clone(), "deal_size", &[]));
        m.insert("remaining".to_string(), Value::Null);
        m.insert("status".to_string(), self.parse_ws_order_status(self.safe_string_k(order.clone(), "action", &[])));
        m.insert("fee".to_string(), Value::Null);
        m.insert("trades".to_string(), trades.clone());
    m
}), &[market.clone()]);
        }

    Value::Null
}

    pub fn parse_ws_order_status(&self, mut statusId: Value) -> Value {
        let mut statuses: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("1".to_string(), Value::Str("closed".to_string()));
                m.insert("2".to_string(), Value::Str("open".to_string()));
                m.insert("3".to_string(), Value::Str("canceled".to_string()));
                m.insert("4".to_string(), Value::Str("closed".to_string()));
                m.insert("5".to_string(), Value::Str("canceled".to_string()));
                m.insert("6".to_string(), Value::Str("open".to_string()));
                m.insert("7".to_string(), Value::Str("open".to_string()));
                m.insert("8".to_string(), Value::Str("closed".to_string()));
                m.insert("9".to_string(), Value::Str("closed".to_string()));
            m
        });
        return self.safe_string(statuses.clone(), statusId.clone(), &[statusId.clone()]);

    Value::Null
}

    pub fn parse_ws_order_side(&self, mut sideId: Value) -> Value {
        let mut sides: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("1".to_string(), Value::Str("buy".to_string()));
                m.insert("2".to_string(), Value::Str("buy".to_string()));
                m.insert("3".to_string(), Value::Str("sell".to_string()));
                m.insert("4".to_string(), Value::Str("sell".to_string()));
            m
        });
        return self.safe_string(sides.clone(), sideId.clone(), &[sideId.clone()]);

    Value::Null
}

/*
 * @method
 * @name bitmart#watchPositions
 * @see https://developer-pro.bitmart.com/en/futures/#private-position-channel
 * @description watch all open positions
 * @param {string[]|undefined} symbols list of unified market symbols
 * @param {int} [since] the earliest time in ms to fetch positions
 * @param {int} [limit] the maximum number of positions to retrieve
 * @param {object} params extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [position structure]{@link https://docs.ccxt.com/en/latest/manual.html#position-structure}
 */
    pub async fn watch_positions(&mut self, optional_args: &[Value]) -> Value {
        let mut symbols = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut type_var: Value = Value::Str("swap".to_string());
        self.authenticate(type_var.clone(), &[params.clone()]).await;
        symbols = self.market_symbols(&[symbols.clone(), Value::Str("swap".to_string()), Value::Bool(true), Value::Bool(true), Value::Bool(false)]);
        let mut messageHash: Value = Value::Str("positions".to_string());
        if !is_equal(&symbols, &Value::Null) {
            messageHash = add(&messageHash, &add(&Value::Str("::".to_string()), &join(&symbols, &Value::Str(",".to_string()))));
        }
        let mut subscriptionHash: Value = Value::Str("futures/position".to_string());
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("action".to_string(), Value::Str("subscribe".to_string()));
                m.insert("args".to_string(), Value::List(vec![Value::Str("futures/position".to_string())]));
            m
        });
        let mut url: Value = self.implode_hostname(get_value(&get_value(&get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string())), &type_var), &Value::Str("private".to_string())));
        let __ws_arg_6 = self.deep_extend(request.clone(), &[params.clone()]);
        let mut newPositions: Value = self.watch(url.clone(), messageHash.clone(), &[__ws_arg_6, subscriptionHash.clone()]).await;
        if is_true(&self.newUpdates) {
            return newPositions;
        }
        return self.filter_by_symbols_since_limit(self.positions.clone(), &[symbols.clone(), since.clone(), limit.clone()]);

    Value::Null
}

/*
 * @method
 * @name bitmart#unWatchPositions
 * @description unWatches all open positions
 * @see https://developer-pro.bitmart.com/en/futures/#private-position-channel
 * @param {string[]} [symbols] list of unified market symbols
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} status of the unwatch request
 */
    pub async fn un_watch_positions(&mut self, optional_args: &[Value]) -> Value {
        let mut symbols = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if !is_equal(&symbols, &Value::Null) {
            let mut length: Value = get_array_length(&symbols);
            if is_greater_than(&length, &Value::Int(0)) {
                panic!("{}", crate::exchange_errors::not_supported(add(&self.id, &Value::Str(" unWatchPositions() does not support a list of symbols, unWatch from all markets only".to_string()))));
            }
        }
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("action".to_string(), Value::Str("unsubscribe".to_string()));
                m.insert("args".to_string(), Value::List(vec![Value::Str("futures/position".to_string())]));
            m
        });
        let mut messageHash: Value = Value::Str("unsubscribe::positions".to_string());
        let mut url: Value = self.implode_hostname(get_value(&get_value(&get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string())), &Value::Str("swap".to_string())), &Value::Str("private".to_string())));
        let __ws_arg_7 = self.deep_extend(request.clone(), &[params.clone()]);
        return self.watch(url.clone(), messageHash.clone(), &[__ws_arg_7, messageHash.clone()]).await;

    Value::Null
}

    pub fn handle_positions(&mut self, mut client: Value, mut message: Value) {
        //
        //    {
        //        "group":"futures/position",
        //        "data":[
        //           {
        //              "symbol":"LTCUSDT",
        //              "hold_volume":"5",
        //              "position_type":2,
        //              "open_type":2,
        //              "frozen_volume":"0",
        //              "close_volume":"0",
        //              "hold_avg_price":"71.582",
        //              "close_avg_price":"0",
        //              "open_avg_price":"71.582",
        //              "liquidate_price":"0",
        //              "create_time":1701623327513,
        //              "update_time":1701627620439
        //           },
        //           {
        //              "symbol":"LTCUSDT",
        //              "hold_volume":"6",
        //              "position_type":1,
        //              "open_type":2,
        //              "frozen_volume":"0",
        //              "close_volume":"0",
        //              "hold_avg_price":"71.681666666666666667",
        //              "close_avg_price":"0",
        //              "open_avg_price":"71.681666666666666667",
        //              "liquidate_price":"0",
        //              "create_time":1701621167225,
        //              "update_time":1701628152614
        //           }
        //        ]
        //    }
        //
        let mut data: Value = self.safe_list_k(message.clone(), "data", &[Value::List(vec![])]);
        if is_equal(&self.positions, &Value::Null) {
            self.positions = ArrayCacheBySymbolBySide::new(Value::Null);
        }
        let mut cache: Value = self.positions.clone();
        let mut newPositions: Value = Value::List(vec![]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_127: bool = true;
            while { if !__for_first_127 { i = add(&i, &Value::Int(1)); } __for_first_127 = false; is_less_than(&i, &get_array_length(&data)) } {
            let mut rawPosition: Value = get_value(&data, &i);
            let mut rawPosition: Value = get_value(&data, &i);
            let mut position: Value = self.parse_ws_position(rawPosition.clone(), &[]);
            append_to_array(&mut newPositions, position.clone());
            cache.append(position.clone());
        }
        }
        let mut messageHashes: Value = self.find_message_hashes(client.clone(), Value::Str("positions::".to_string()));
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_128: bool = true;
            while { if !__for_first_128 { i = add(&i, &Value::Int(1)); } __for_first_128 = false; is_less_than(&i, &get_array_length(&messageHashes)) } {
            let mut messageHash: Value = get_value(&messageHashes, &i);
            let mut messageHash: Value = get_value(&messageHashes, &i);
            let mut parts: Value = split(&messageHash, &Value::Str("::".to_string()));
            let mut symbolsString: Value = get_value(&parts, &Value::Int(1));
            let mut symbols: Value = split(&symbolsString, &Value::Str(",".to_string()));
            let mut positions: Value = self.filter_by_array(newPositions.clone(), Value::Str("symbol".to_string()), &[symbols.clone(), Value::Bool(false)]);
            if !is_true(&self.is_empty(positions.clone())) {
                client.resolve(&[positions.clone(), messageHash.clone()]);
            }
        }
        }
        client.resolve(&[newPositions.clone(), Value::Str("positions".to_string())]);
}

    pub fn parse_ws_position(&self, mut position: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        //    {
        //       "symbol":"LTCUSDT",
        //       "hold_volume":"6",
        //       "position_type":1,
        //       "open_type":2,
        //       "frozen_volume":"0",
        //       "close_volume":"0",
        //       "hold_avg_price":"71.681666666666666667",
        //       "close_avg_price":"0",
        //       "open_avg_price":"71.681666666666666667",
        //       "liquidate_price":"0",
        //       "create_time":1701621167225,
        //       "update_time":1701628152614
        //    }
        //
        let mut marketId: Value = self.safe_string_k(position.clone(), "symbol", &[]);
        market = self.safe_market(&[marketId.clone(), market.clone(), Value::Null, Value::Str("swap".to_string())]);
        let mut symbol: Value = get_value(&market, &Value::Str("symbol".to_string()));
        let mut openTimestamp: Value = self.safe_integer_k(position.clone(), "create_time", &[]);
        let mut timestamp: Value = self.safe_integer_k(position.clone(), "update_time", &[]);
        let mut side: Value = self.safe_integer_k(position.clone(), "position_type", &[]);
        let mut marginModeId: Value = self.safe_integer_k(position.clone(), "open_type", &[]);
        return self.safe_position(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), position.clone());
        m.insert("id".to_string(), Value::Null);
        m.insert("symbol".to_string(), symbol.clone());
        m.insert("timestamp".to_string(), openTimestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(openTimestamp.clone()));
        m.insert("lastUpdateTimestamp".to_string(), timestamp.clone());
        m.insert("hedged".to_string(), Value::Null);
        m.insert("side".to_string(), ternary(is_true(&(is_equal(&side, &Value::Int(1)))), Value::Str("long".to_string()), Value::Str("short".to_string())));
        m.insert("contracts".to_string(), self.safe_number_k(position.clone(), "hold_volume", &[]));
        m.insert("contractSize".to_string(), self.safe_number_k(market.clone(), "contractSize", &[]));
        m.insert("entryPrice".to_string(), self.safe_number_k(position.clone(), "open_avg_price", &[]));
        m.insert("markPrice".to_string(), self.safe_number_k(position.clone(), "hold_avg_price", &[]));
        m.insert("lastPrice".to_string(), Value::Null);
        m.insert("notional".to_string(), Value::Null);
        m.insert("leverage".to_string(), Value::Null);
        m.insert("collateral".to_string(), Value::Null);
        m.insert("initialMargin".to_string(), Value::Null);
        m.insert("initialMarginPercentage".to_string(), Value::Null);
        m.insert("maintenanceMargin".to_string(), Value::Null);
        m.insert("maintenanceMarginPercentage".to_string(), Value::Null);
        m.insert("unrealizedPnl".to_string(), Value::Null);
        m.insert("realizedPnl".to_string(), Value::Null);
        m.insert("liquidationPrice".to_string(), self.safe_number_k(position.clone(), "liquidate_price", &[]));
        m.insert("marginMode".to_string(), ternary(is_true(&(is_equal(&marginModeId, &Value::Int(1)))), Value::Str("isolated".to_string()), Value::Str("cross".to_string())));
        m.insert("percentage".to_string(), Value::Null);
        m.insert("marginRatio".to_string(), Value::Null);
        m.insert("stopLossPrice".to_string(), Value::Null);
        m.insert("takeProfitPrice".to_string(), Value::Null);
    m
}));

    Value::Null
}

    pub fn handle_trade(&self, mut client: Value, mut message: Value) {
        //
        // spot
        //    {
        //        "table": "spot/trade",
        //        "data": [
        //            {
        //                "price": "52700.50",
        //                "s_t": 1630982050,
        //                "side": "buy",
        //                "size": "0.00112",
        //                "symbol": "BTC_USDT"
        //            },
        //        ]
        //    }
        //
        // swap
        //    {
        //        "group":"futures/trade:BTCUSDT",
        //        "data":[
        //           {
        //              "trade_id":6798697637,
        //              "symbol":"BTCUSDT",
        //              "deal_price":"39735.8",
        //              "deal_vol":"2",
        //              "way":1,
        //              "created_at":"2023-12-03T15:48:23.517518538Z",
        //              "m": true,
        //           }
        //        ]
        //    }
        //
        let mut data: Value = self.safe_list_k(message.clone(), "data", &[]);
        if is_equal(&data, &Value::Null) {
            return;
        }
        let mut symbol: Value = Value::Null;
        let mut length: Value = get_array_length(&data);
        let mut isSwap: Value = (Value::Bool(in_op(&message, &Value::Str("group".to_string()))));
        if is_true(&isSwap) {
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_129: bool = true;
                while { if !__for_first_129 { i = add(&i, &Value::Int(1)); } __for_first_129 = false; is_less_than(&i, &length) } {
                let mut index: Value = subtract(&subtract(&length, &i), &Value::Int(1));
                symbol = self.handle_trade_loop(get_value(&data, &index));
            }
            }
        }  else {
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_130: bool = true;
                while { if !__for_first_130 { i = add(&i, &Value::Int(1)); } __for_first_130 = false; is_less_than(&i, &length) } {
                symbol = self.handle_trade_loop(get_value(&data, &i));
            }
            }
        }
        if !is_equal(&symbol, &Value::Null) {
            client.resolve(&[get_value(&self.trades, &symbol), add(&Value::Str("trade::".to_string()), &symbol)]);
        }
}

    pub fn handle_trade_loop(&self, mut entry: Value) -> Value {
        let mut trade: Value = self.parse_ws_trade(entry.clone(), &[]);
        let mut symbol: Value = get_value(&trade, &Value::Str("symbol".to_string()));
        if is_equal(&symbol, &Value::Null) {
            return symbol;
        }
        let mut tradesLimit: Value = self.safe_integer_k(self.options.clone(), "tradesLimit", &[Value::Int(1000)]);
        if is_equal(&self.safe_value(self.trades.clone(), symbol.clone(), &[]), &Value::Null) {
            add_element_to_object(&mut self.trades.clone(), &symbol, ArrayCache::new(tradesLimit.clone()));
        }
        let mut stored: Value = get_value(&self.trades, &symbol);
        stored.append(trade.clone());
        return symbol;

    Value::Null
}

    pub fn parse_ws_trade(&self, mut trade: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        // spot
        //     {
        //         "ms_t": 1740320841473,
        //         "price": "2806.54",
        //         "s_t": 1740320841,
        //         "side": "sell",
        //         "size": "0.77598",
        //         "symbol": "ETH_USDT"
        //     }
        //
        // swap
        //     {
        //         "trade_id": "3000000245258661",
        //         "symbol": "ETHUSDT",
        //         "deal_price": "2811.1",
        //         "deal_vol": "1858",
        //         "way": 2,
        //         "m": true,
        //         "created_at": "2025-02-23T13:59:59.646490751Z"
        //     }
        //
        let mut marketId: Value = self.safe_string_k(trade.clone(), "symbol", &[]);
        market = self.safe_market(&[marketId.clone(), market.clone()]);
        let mut timestamp: Value = self.safe_integer_k(trade.clone(), "ms_t", &[]);
        let mut datetime: Value = Value::Null;
        if is_equal(&timestamp, &Value::Null) {
            datetime = self.safe_string_k(trade.clone(), "created_at", &[]);
            timestamp = self.parse8601(datetime.clone());
        }  else {
            datetime = self.iso8601(timestamp.clone());
        }
        let mut takerOrMaker: Value = Value::Null; // true for public trades
        let mut side: Value = self.safe_string_k(trade.clone(), "side", &[]);
        let mut buyerMaker: Value = self.safe_bool_k(trade.clone(), "m", &[]);
        if !is_equal(&buyerMaker, &Value::Null) {
            if is_equal(&side, &Value::Null) {
                if is_true(&buyerMaker) {
                    side = Value::Str("sell".to_string());
                }  else {
                    side = Value::Str("buy".to_string());
                }
            }
            takerOrMaker = Value::Str("taker".to_string());
        }
        return self.safe_trade(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), trade.clone());
        m.insert("id".to_string(), self.safe_string_k(trade.clone(), "trade_id", &[]));
        m.insert("order".to_string(), Value::Null);
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), datetime.clone());
        m.insert("symbol".to_string(), get_value(&market, &Value::Str("symbol".to_string())));
        m.insert("type".to_string(), Value::Null);
        m.insert("side".to_string(), side.clone());
        m.insert("price".to_string(), self.safe_string2(trade.clone(), Value::Str("price".to_string()), Value::Str("deal_price".to_string()), &[]));
        m.insert("amount".to_string(), self.safe_string2(trade.clone(), Value::Str("size".to_string()), Value::Str("deal_vol".to_string()), &[]));
        m.insert("cost".to_string(), Value::Null);
        m.insert("takerOrMaker".to_string(), takerOrMaker.clone());
        m.insert("fee".to_string(), Value::Null);
    m
}), &[market.clone()]);

    Value::Null
}

    pub fn handle_ticker(&self, mut client: Value, mut message: Value) {
        //
        //    {
        //        "data": [
        //            {
        //                "base_volume_24h": "78615593.81",
        //                "high_24h": "52756.97",
        //                "last_price": "52638.31",
        //                "low_24h": "50991.35",
        //                "open_24h": "51692.03",
        //                "s_t": 1630981727,
        //                "symbol": "BTC_USDT"
        //            }
        //        ],
        //        "table": "spot/ticker"
        //    }
        //
        //     {
        //         "data": {
        //             "symbol": "ETHUSDT",
        //             "last_price": "2807.73",
        //             "volume_24": "2227011952",
        //             "range": "0.0273398194664491",
        //             "mark_price": "2807.5",
        //             "index_price": "2808.71047619",
        //             "ask_price": "2808.04",
        //             "ask_vol": "7371",
        //             "bid_price": "2807.28",
        //             "bid_vol": "3561"
        //         },
        //         "group": "futures/ticker:ETHUSDT@100ms"
        //     }
        //
        self.handle_bid_ask(client.clone(), message.clone());
        let mut table: Value = self.safe_string_k(message.clone(), "table", &[]);
        let mut isSpot: Value = Value::Bool(!is_equal(&table, &Value::Null));
        let mut rawTickers: Value = Value::List(vec![]);
        if is_true(&isSpot) {
            rawTickers = self.safe_list_k(message.clone(), "data", &[Value::List(vec![])]);
        }  else {
            rawTickers = Value::List(vec![self.safe_dict_k(message.clone(), "data", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})])]);
        }
        if !is_true(&get_array_length(&rawTickers)) {
            return;
        }
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_131: bool = true;
            while { if !__for_first_131 { i = add(&i, &Value::Int(1)); } __for_first_131 = false; is_less_than(&i, &get_array_length(&rawTickers)) } {
            let mut ticker: Value = ternary(is_true(&isSpot), self.parse_ticker(get_value(&rawTickers, &i), &[]), self.parse_ws_swap_ticker(get_value(&rawTickers, &i), &[]));
            let mut symbol: Value = get_value(&ticker, &Value::Str("symbol".to_string()));
            if !is_equal(&symbol, &Value::Null) {
                add_element_to_object(&mut self.tickers.clone(), &symbol, ticker.clone());
            }
            let mut messageHash: Value = add(&Value::Str("ticker::".to_string()), &symbol);
            client.resolve(&[ticker.clone(), messageHash.clone()]);
        }
        }
}

    pub fn parse_ws_swap_ticker(&self, mut ticker: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        //     {
        //         "symbol": "ETHUSDT",
        //         "last_price": "2807.73",
        //         "volume_24": "2227011952",
        //         "range": "0.0273398194664491",
        //         "mark_price": "2807.5",
        //         "index_price": "2808.71047619",
        //         "ask_price": "2808.04",
        //         "ask_vol": "7371",
        //         "bid_price": "2807.28",
        //         "bid_vol": "3561"
        //     }
        //
        let mut marketId: Value = self.safe_string_k(ticker.clone(), "symbol", &[]);
        return self.safe_ticker(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("symbol".to_string(), self.safe_symbol(marketId.clone(), &[market.clone(), Value::Str("".to_string()), Value::Str("swap".to_string())]));
        m.insert("timestamp".to_string(), Value::Null);
        m.insert("datetime".to_string(), Value::Null);
        m.insert("high".to_string(), Value::Null);
        m.insert("low".to_string(), Value::Null);
        m.insert("bid".to_string(), self.safe_string_k(ticker.clone(), "bid_price", &[]));
        m.insert("bidVolume".to_string(), self.safe_string_k(ticker.clone(), "bid_vol", &[]));
        m.insert("ask".to_string(), self.safe_string_k(ticker.clone(), "ask_price", &[]));
        m.insert("askVolume".to_string(), self.safe_string_k(ticker.clone(), "ask_vol", &[]));
        m.insert("vwap".to_string(), Value::Null);
        m.insert("open".to_string(), Value::Null);
        m.insert("close".to_string(), Value::Null);
        m.insert("last".to_string(), self.safe_string_k(ticker.clone(), "last_price", &[]));
        m.insert("previousClose".to_string(), Value::Null);
        m.insert("change".to_string(), Value::Null);
        m.insert("percentage".to_string(), Value::Null);
        m.insert("average".to_string(), Value::Null);
        m.insert("baseVolume".to_string(), Value::Null);
        m.insert("quoteVolume".to_string(), self.safe_string_k(ticker.clone(), "volume_24", &[]));
        m.insert("info".to_string(), ticker.clone());
        m.insert("markPrice".to_string(), self.safe_string_k(ticker.clone(), "mark_price", &[]));
        m.insert("indexPrice".to_string(), self.safe_string_k(ticker.clone(), "index_price", &[]));
    m
}), &[market.clone()]);

    Value::Null
}

/*
 * @method
 * @name bitmart#watchOHLCV
 * @see https://developer-pro.bitmart.com/en/spot/#public-kline-channel
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-klinebin-channel
 * @description watches historical candlestick data containing the open, high, low, and close price, and the volume of a market
 * @param {string} symbol unified symbol of the market to fetch OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum amount of candles to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 */
    pub async fn watch_ohlcv(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut timeframe = get_arg(optional_args, 0, Value::Str("1m".to_string()));
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        symbol = self.symbol(symbol.clone());
        let mut market: Value = self.market(symbol.clone());
        let mut type_var: Value = Value::Str("spot".to_string());
        { let __destr_tmp = self.handle_market_type_and_params(Value::Str("watchOHLCV".to_string()), &[market.clone(), params.clone()]); type_var = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        let mut timeframes: Value = self.safe_dict_k(self.options.clone(), "timeframes", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut interval: Value = self.safe_string(timeframes.clone(), timeframe.clone(), &[]);
        let mut name: Value = Value::Null;
        if is_equal(&type_var, &Value::Str("spot".to_string())) {
            name = add(&Value::Str("kline".to_string()), &interval);
        }  else {
            name = add(&Value::Str("klineBin".to_string()), &interval);
        }
        let mut ohlcv: Value = self.subscribe(Value::Str("ohlcv".to_string()), name.clone(), symbol.clone(), type_var.clone(), &[params.clone()]).await;
        if is_true(&self.newUpdates) {
            limit = ohlcv.get_limit(symbol.clone(), limit.clone());
        }
        return self.filter_by_since_limit(ohlcv.clone(), &[since.clone(), limit.clone(), Value::Int(0), Value::Bool(true)]);

    Value::Null
}

/*
 * @method
 * @name bitmart#unWatchOHLCV
 * @description unWatches historical candlestick data containing the open, high, low, and close price, and the volume of a market
 * @see https://developer-pro.bitmart.com/en/spot/#public-kline-channel
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-klinebin-channel
 * @param {string} symbol unified symbol of the market to fetch OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 */
    pub async fn un_watch_ohlcv(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut timeframe = get_arg(optional_args, 0, Value::Str("1m".to_string()));
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        symbol = self.symbol(symbol.clone());
        let mut market: Value = self.market(symbol.clone());
        let mut type_var: Value = Value::Str("spot".to_string());
        { let __destr_tmp = self.handle_market_type_and_params(Value::Str("unWatchOHLCV".to_string()), &[market.clone(), params.clone()]); type_var = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        let mut timeframes: Value = self.safe_dict_k(self.options.clone(), "timeframes", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut interval: Value = self.safe_string(timeframes.clone(), timeframe.clone(), &[]);
        let mut name: Value = Value::Null;
        if is_equal(&type_var, &Value::Str("spot".to_string())) {
            name = add(&Value::Str("kline".to_string()), &interval);
        }  else {
            name = add(&Value::Str("klineBin".to_string()), &interval);
        }
        params = self.extend(params.clone(), &[Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("unsubscribe".to_string(), Value::Bool(true));
            m
        })]);
        return self.subscribe(Value::Str("ohlcv".to_string()), name.clone(), symbol.clone(), type_var.clone(), &[params.clone()]).await;

    Value::Null
}

    pub fn handle_ohlcv(&self, mut client: Value, mut message: Value) {
        //
        //    {
        //        "data": [
        //            {
        //                "candle": [
        //                    1631056350,
        //                    "46532.83",
        //                    "46555.71",
        //                    "46511.41",
        //                    "46555.71",
        //                    "0.25"
        //                ],
        //                "symbol": "BTC_USDT"
        //            }
        //        ],
        //        "table": "spot/kline1m"
        //    }
        // swap
        //    {
        //        "group":"futures/klineBin1m:BTCUSDT",
        //        "data":{
        //           "symbol":"BTCUSDT",
        //           "items":[
        //              {
        //                 "o":"39635.8",
        //                 "h":"39636",
        //                 "l":"39614.4",
        //                 "c":"39629.7",
        //                 "v":"31852",
        //                 "ts":1701617761
        //              }
        //           ]
        //        }
        //    }
        //
        let mut channel: Value = self.safe_string2(message.clone(), Value::Str("table".to_string()), Value::Str("group".to_string()), &[Value::Str("".to_string())]);
        let mut isSpot: Value = Value::Bool(is_greater_than_or_equal(&get_index_of(&channel, &Value::Str("spot".to_string())), &Value::Int(0)));
        let mut data: Value = self.safe_value_k(message.clone(), "data", &[]);
        if is_equal(&data, &Value::Null) {
            return;
        }
        let mut parts: Value = split(&channel, &Value::Str("/".to_string()));
        let mut part1: Value = self.safe_string(parts.clone(), Value::Int(1), &[Value::Str("".to_string())]);
        let mut interval: Value = replace_str(&part1, &Value::Str("kline".to_string()), &Value::Str("".to_string()));
        interval = replace_str(&interval, &Value::Str("Bin".to_string()), &Value::Str("".to_string()));
        let mut intervalParts: Value = split(&interval, &Value::Str(":".to_string()));
        interval = self.safe_string(intervalParts.clone(), Value::Int(0), &[Value::Str("".to_string())]);
        // use a reverse lookup in a static map instead
        let mut timeframes: Value = self.safe_dict_k(self.options.clone(), "timeframes", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut timeframe: Value = self.find_timeframe(interval.clone(), &[timeframes.clone()]);
        if is_equal(&timeframe, &Value::Null) {
            return;
        }
        let mut duration: Value = self.parse_timeframe(timeframe.clone());
        let mut durationInMs: Value = multiply(&duration, &Value::Int(1000));
        if is_true(&isSpot) {
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_132: bool = true;
                while { if !__for_first_132 { i = add(&i, &Value::Int(1)); } __for_first_132 = false; is_less_than(&i, &get_array_length(&data)) } {
                let mut marketId: Value = self.safe_string_k(get_value(&data, &i), "symbol", &[]);
                let mut market: Value = self.safe_market(&[marketId.clone()]);
                let mut symbol: Value = get_value(&market, &Value::Str("symbol".to_string()));
                let mut rawOHLCV: Value = self.safe_list_k(get_value(&data, &i), "candle", &[]);
                let mut parsed: Value = self.parse_ohlcv(rawOHLCV.clone(), &[market.clone()]);
                { let __be_tmp = multiply(&self.parse_to_int(divide(&self.parse_to_int(get_value(&parsed, &Value::Int(0))), &durationInMs)), &durationInMs); add_element_to_object(&mut parsed, &Value::Int(0), __be_tmp); };
                { let __be_tmp = self.safe_value(self.ohlcvs.clone(), symbol.clone(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]); add_element_to_object(&mut self.ohlcvs.clone(), &symbol, __be_tmp); };
                let mut stored: Value = self.safe_value(get_value(&self.ohlcvs, &symbol), timeframe.clone(), &[]);
                if is_equal(&stored, &Value::Null) {
                    let mut limit: Value = self.safe_integer_k(self.options.clone(), "OHLCVLimit", &[Value::Int(1000)]);
                    stored = ArrayCacheByTimestamp::new(limit.clone());
                    add_element_to_object(get_value_mut(unsafe { crate::runtime::coerce_value_to_mut(&self.ohlcvs) }, &symbol), &timeframe, stored.clone());
                }
                stored.append(parsed.clone());
                let mut messageHash: Value = add(&Value::Str("ohlcv::".to_string()), &symbol);
                client.resolve(&[stored.clone(), messageHash.clone()]);
            }
            }
        }  else {
            let mut marketId: Value = self.safe_string_k(data.clone(), "symbol", &[]);
            let mut market: Value = self.safe_market(&[marketId.clone(), Value::Null, Value::Null, Value::Str("swap".to_string())]);
            let mut symbol: Value = get_value(&market, &Value::Str("symbol".to_string()));
            let mut items: Value = self.safe_list_k(data.clone(), "items", &[Value::List(vec![])]);
            { let __be_tmp = self.safe_value(self.ohlcvs.clone(), symbol.clone(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]); add_element_to_object(&mut self.ohlcvs.clone(), &symbol, __be_tmp); };
            let mut stored: Value = self.safe_value(get_value(&self.ohlcvs, &symbol), timeframe.clone(), &[]);
            if is_equal(&stored, &Value::Null) {
                let mut limit: Value = self.safe_integer_k(self.options.clone(), "OHLCVLimit", &[Value::Int(1000)]);
                stored = ArrayCacheByTimestamp::new(limit.clone());
                add_element_to_object(get_value_mut(unsafe { crate::runtime::coerce_value_to_mut(&self.ohlcvs) }, &symbol), &timeframe, stored.clone());
            }
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_133: bool = true;
                while { if !__for_first_133 { i = add(&i, &Value::Int(1)); } __for_first_133 = false; is_less_than(&i, &get_array_length(&items)) } {
                let mut candle: Value = get_value(&items, &i);
                let mut candle: Value = get_value(&items, &i);
                let mut parsed: Value = self.parse_ohlcv(candle.clone(), &[market.clone()]);
                stored.append(parsed.clone());
            }
            }
            client.resolve(&[stored.clone(), add(&Value::Str("ohlcv::".to_string()), &symbol)]);
        }
}

/*
 * @method
 * @name bitmart#watchOrderBook
 * @see https://developer-pro.bitmart.com/en/spot/#public-depth-all-channel
 * @see https://developer-pro.bitmart.com/en/spot/#public-depth-increase-channel
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-depth-channel
 * @description watches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
 * @param {string} symbol unified symbol of the market to fetch the order book for
 * @param {int} [limit] the maximum amount of order book entries to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.speed] *futures only* '100ms' or '200ms'
 * @returns {object} A dictionary of [order book structures]{@link https://docs.ccxt.com/?id=order-book-structure}
 */
    pub async fn watch_order_book(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut limit = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut options: Value = self.safe_dict_k(self.options.clone(), "watchOrderBook", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut depth: Value = self.safe_string_k(options.clone(), "depth", &[Value::Str("depth/increase100".to_string())]);
        symbol = self.symbol(symbol.clone());
        let mut market: Value = self.market(symbol.clone());
        let mut type_var: Value = Value::Str("spot".to_string());
        { let __destr_tmp = self.handle_market_type_and_params(Value::Str("watchOrderBook".to_string()), &[market.clone(), params.clone()]); type_var = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        if is_equal(&type_var, &Value::Str("swap".to_string())) && is_equal(&depth, &Value::Str("depth/increase100".to_string())) {
            depth = Value::Str("depth50".to_string());
        }
        let mut orderbook: Value = self.subscribe(Value::Str("orderbook".to_string()), depth.clone(), symbol.clone(), type_var.clone(), &[params.clone()]).await;
        return orderbook.limit();

    Value::Null
}

/*
 * @method
 * @name bitmart#unWatchOrderBook
 * @description unWatches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
 * @see https://developer-pro.bitmart.com/en/spot/#public-depth-all-channel
 * @see https://developer-pro.bitmart.com/en/spot/#public-depth-increase-channel
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-depth-channel
 * @param {string} symbol unified array of symbols
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} A dictionary of [order book structures]{@link https://docs.ccxt.com/?id=order-book-structure}
 */
    pub async fn un_watch_order_book(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut options: Value = self.safe_dict_k(self.options.clone(), "watchOrderBook", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut depth: Value = self.safe_string_k(options.clone(), "depth", &[Value::Str("depth/increase100".to_string())]);
        symbol = self.symbol(symbol.clone());
        let mut market: Value = self.market(symbol.clone());
        let mut type_var: Value = Value::Str("spot".to_string());
        { let __destr_tmp = self.handle_market_type_and_params(Value::Str("unWatchOrderBook".to_string()), &[market.clone(), params.clone()]); type_var = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        if is_equal(&type_var, &Value::Str("swap".to_string())) && is_equal(&depth, &Value::Str("depth/increase100".to_string())) {
            depth = Value::Str("depth50".to_string());
        }
        params = self.extend(params.clone(), &[Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("unsubscribe".to_string(), Value::Bool(true));
            m
        })]);
        return self.subscribe(Value::Str("orderbook".to_string()), depth.clone(), symbol.clone(), type_var.clone(), &[params.clone()]).await;

    Value::Null
}

    pub fn handle_delta(&self, mut bookside: Value, mut delta: Value) {
        let mut price: Value = self.safe_float(delta.clone(), Value::Int(0), &[]);
        let mut amount: Value = self.safe_float(delta.clone(), Value::Int(1), &[]);
        bookside.store(price.clone(), amount.clone());
}

    pub fn handle_deltas(&self, mut bookside: Value, mut deltas: Value) {
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_134: bool = true;
            while { if !__for_first_134 { i = add(&i, &Value::Int(1)); } __for_first_134 = false; is_less_than(&i, &get_array_length(&deltas)) } {
            self.handle_delta(bookside.clone(), get_value(&deltas, &i));
        }
        }
}

    pub fn handle_order_book_message(&self, mut client: Value, mut message: Value, mut orderbook: Value) -> Value {
        //
        //     {
        //         "asks": [
        //             [ '46828.38', "0.21847" ],
        //             [ '46830.68', "0.08232" ],
        //             [ '46832.08', "0.09285" ],
        //             [ '46837.82', "0.02028" ],
        //             [ '46839.43', "0.15068" ]
        //         ],
        //         "bids": [
        //             [ '46820.78', "0.00444" ],
        //             [ '46814.33', "0.00234" ],
        //             [ '46813.50', "0.05021" ],
        //             [ '46808.14', "0.00217" ],
        //             [ '46808.04', "0.00013" ]
        //         ],
        //         "ms_t": 1631044962431,
        //         "symbol": "BTC_USDT"
        //     }
        //
        let mut asks: Value = self.safe_list_k(message.clone(), "asks", &[Value::List(vec![])]);
        let mut bids: Value = self.safe_list_k(message.clone(), "bids", &[Value::List(vec![])]);
        self.handle_deltas(get_value(&orderbook, &Value::Str("asks".to_string())), asks.clone());
        self.handle_deltas(get_value(&orderbook, &Value::Str("bids".to_string())), bids.clone());
        let mut timestamp: Value = self.safe_integer_k(message.clone(), "ms_t", &[]);
        let mut marketId: Value = self.safe_string_k(message.clone(), "symbol", &[]);
        let mut symbol: Value = self.safe_symbol(marketId.clone(), &[]);
        add_element_to_object(&mut orderbook, &Value::Str("symbol".to_string()), symbol.clone());
        add_element_to_object(&mut orderbook, &Value::Str("timestamp".to_string()), timestamp.clone());
        add_element_to_object(&mut orderbook, &Value::Str("datetime".to_string()), self.iso8601(timestamp.clone()));
        return orderbook;

    Value::Null
}

    pub fn handle_order_book(&self, mut client: Value, mut message: Value) {
        //
        // spot depth-all
        //
        //    {
        //        "data": [
        //            {
        //                "asks": [
        //                    [ '46828.38', "0.21847" ],
        //                    [ '46830.68', "0.08232" ],
        //                    ...
        //                ],
        //                "bids": [
        //                    [ '46820.78', "0.00444" ],
        //                    [ '46814.33', "0.00234" ],
        //                    ...
        //                ],
        //                "ms_t": 1631044962431,
        //                "symbol": "BTC_USDT"
        //            }
        //        ],
        //        "table": "spot/depth5"
        //    }
        //
        // spot increse depth snapshot
        //
        //    {
        //        "data":[
        //           {
        //               "asks":[
        //                   [ "43652.52", "0.02039" ],
        //                   ...
        //                ],
        //                "bids":[
        //                   [ "43652.51", "0.00500" ],
        //                   ...
        //                ],
        //                "ms_t":1703376836487,
        //                "symbol":"BTC_USDT",
        //                "type":"snapshot", // or update
        //                "version":2141731
        //           }
        //        ],
        //        "table":"spot/depth/increase100"
        //    }
        //
        // swap
        //
        //    {
        //        "group":"futures/depth50:BTCUSDT",
        //        "data":{
        //           "symbol":"BTCUSDT",
        //           "way":1,
        //           "depths":[
        //              {
        //                 "price":"39509.8",
        //                 "vol":"2379"
        //              },
        //              {
        //                 "price":"39509.6",
        //                 "vol":"6815"
        //              },
        //              ...
        //           ],
        //           "ms_t":1701566021194
        //        }
        //    }
        //
        let mut isSpot: Value = (Value::Bool(in_op(&message, &Value::Str("table".to_string()))));
        let mut datas: Value = Value::List(vec![]);
        if is_true(&isSpot) {
            datas = self.safe_list_k(message.clone(), "data", &[datas.clone()]);
        }  else {
            let mut orderBookEntry: Value = self.safe_dict_k(message.clone(), "data", &[]);
            if !is_equal(&orderBookEntry, &Value::Null) {
                append_to_array(&mut datas, orderBookEntry.clone());
            }
        }
        let mut length: Value = get_array_length(&datas);
        if is_less_than_or_equal(&length, &Value::Int(0)) {
            return;
        }
        let mut channelName: Value = self.safe_string2(message.clone(), Value::Str("table".to_string()), Value::Str("group".to_string()), &[Value::Str("".to_string())]);
        // find limit subscribed to
        let mut limitsToCheck: Value = Value::List(vec![Value::Str("100".to_string()), Value::Str("50".to_string()), Value::Str("20".to_string()), Value::Str("10".to_string()), Value::Str("5".to_string())]);
        let mut limit: Value = Value::Int(0);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_135: bool = true;
            while { if !__for_first_135 { i = add(&i, &Value::Int(1)); } __for_first_135 = false; is_less_than(&i, &get_array_length(&limitsToCheck)) } {
            let mut limitString: Value = get_value(&limitsToCheck, &i);
            let mut limitString: Value = get_value(&limitsToCheck, &i);
            if is_greater_than_or_equal(&get_index_of(&channelName, &limitString), &Value::Int(0)) {
                limit = self.parse_to_int(limitString.clone());
                break;
            }
        }
        }
        if is_true(&isSpot) {
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_136: bool = true;
                while { if !__for_first_136 { i = add(&i, &Value::Int(1)); } __for_first_136 = false; is_less_than(&i, &get_array_length(&datas)) } {
                let mut update: Value = get_value(&datas, &i);
                let mut update: Value = get_value(&datas, &i);
                let mut marketId: Value = self.safe_string_k(update.clone(), "symbol", &[]);
                let mut symbol: Value = self.safe_symbol(marketId.clone(), &[]);
                if !is_true(&(Value::Bool(in_op(&self.orderbooks, &symbol)))) {
                    let mut ob: Value = self.order_book(&[Value::Map({
                        let mut m = indexmap::IndexMap::new();
                        m
                    }), limit.clone()]);
                    add_element_to_object(&mut ob, &Value::Str("symbol".to_string()), symbol.clone());
                    add_element_to_object(&mut self.orderbooks.clone(), &symbol, ob.clone());
                }
                let mut orderbook: Value = get_value(&self.orderbooks, &symbol);
                let mut type_var: Value = self.safe_string_k(update.clone(), "type", &[]);
                if is_true(&(is_equal(&type_var, &Value::Str("snapshot".to_string())))) || is_true(&(!is_true(&(is_greater_than_or_equal(&get_index_of(&channelName, &Value::Str("increase".to_string())), &Value::Int(0)))))) {
                    orderbook.reset(Value::Map({
                        let mut m = indexmap::IndexMap::new();
                        m
                    }));
                }
                self.handle_order_book_message(client.clone(), update.clone(), orderbook.clone());
                let mut timestamp: Value = self.safe_integer_k(update.clone(), "ms_t", &[]);
                if is_equal(&get_value(&orderbook, &Value::Str("timestamp".to_string())), &Value::Null) {
                    add_element_to_object(&mut orderbook, &Value::Str("timestamp".to_string()), timestamp.clone());
                    add_element_to_object(&mut orderbook, &Value::Str("datetime".to_string()), self.iso8601(timestamp.clone()));
                }
                let mut messageHash: Value = add(&Value::Str("orderbook::".to_string()), &symbol);
                client.resolve(&[orderbook.clone(), messageHash.clone()]);
            }
            }
        }  else {
            let mut data: Value = get_value(&datas, &Value::Int(0)); // contract markets always contain only one member
            let mut depths: Value = get_value(&data, &Value::Str("depths".to_string()));
            let mut marketId: Value = self.safe_string_k(data.clone(), "symbol", &[]);
            let mut symbol: Value = self.safe_symbol(marketId.clone(), &[]);
            if !is_true(&(Value::Bool(in_op(&self.orderbooks, &symbol)))) {
                let mut ob: Value = self.order_book(&[Value::Map({
                    let mut m = indexmap::IndexMap::new();
                    m
                }), limit.clone()]);
                add_element_to_object(&mut ob, &Value::Str("symbol".to_string()), symbol.clone());
                add_element_to_object(&mut self.orderbooks.clone(), &symbol, ob.clone());
            }
            let mut orderbook: Value = get_value(&self.orderbooks, &symbol);
            let mut way: Value = self.safe_integer_k(data.clone(), "way", &[]);
            let mut side: Value = ternary(is_true(&(is_equal(&way, &Value::Int(1)))), Value::Str("bids".to_string()), Value::Str("asks".to_string()));
            if is_equal(&way, &Value::Int(1)) {
                add_element_to_object(&mut orderbook, &side, Bids::new(Value::List(vec![]), limit));
            }  else {
                add_element_to_object(&mut orderbook, &side, Asks::new(Value::List(vec![]), limit));
            }
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_137: bool = true;
                while { if !__for_first_137 { i = add(&i, &Value::Int(1)); } __for_first_137 = false; is_less_than(&i, &get_array_length(&depths)) } {
                let mut depth: Value = get_value(&depths, &i);
                let mut depth: Value = get_value(&depths, &i);
                let mut price: Value = self.safe_number_k(depth.clone(), "price", &[]);
                let mut amount: Value = self.safe_number_k(depth.clone(), "vol", &[]);
                let mut orderbookSide: Value = self.safe_value(orderbook.clone(), side.clone(), &[]);
                orderbookSide.store(price.clone(), amount.clone());
            }
            }
            let mut bidsLength: Value = get_array_length(&get_value(&orderbook, &Value::Str("bids".to_string())));
            let mut asksLength: Value = get_array_length(&get_value(&orderbook, &Value::Str("asks".to_string())));
            if is_true(&(is_equal(&bidsLength, &Value::Int(0)))) || is_true(&(is_equal(&asksLength, &Value::Int(0)))) {
                return;
            }
            let mut timestamp: Value = self.safe_integer_k(data.clone(), "ms_t", &[]);
            add_element_to_object(&mut orderbook, &Value::Str("timestamp".to_string()), timestamp.clone());
            add_element_to_object(&mut orderbook, &Value::Str("datetime".to_string()), self.iso8601(timestamp.clone()));
            let mut messageHash: Value = add(&Value::Str("orderbook::".to_string()), &symbol);
            client.resolve(&[orderbook.clone(), messageHash.clone()]);
        }
}

/*
 * @method
 * @name bitmart#watchOrderBookForSymbols
 * @description watches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
 * @see https://developer-pro.bitmart.com/en/spot/#public-depth-increase-channel
 * @param {string[]} symbols unified array of symbols
 * @param {int} [limit] the maximum amount of order book entries to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.depth] the type of order book to subscribe to, default is 'depth/increase100', also accepts 'depth5' or 'depth20' or depth50
 * @returns {object} A dictionary of [order book structures]{@link https://docs.ccxt.com/?id=order-book-structure}
 */
    pub async fn watch_order_book_for_symbols(&mut self, mut symbols: Value, optional_args: &[Value]) -> Value {
        let mut limit = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut type_var: Value = Value::Null;
        { let __destr_tmp = self.get_params_for_multiple_sub(Value::Str("watchOrderBookForSymbols".to_string()), symbols.clone(), &[limit.clone(), params.clone()]); symbols = get_value(&__destr_tmp, &Value::Int(0)); type_var = get_value(&__destr_tmp, &Value::Int(1)); params = get_value(&__destr_tmp, &Value::Int(2)); }
        let mut channel: Value = Value::Null;
        { let __destr_tmp = self.handle_option_and_params(params.clone(), Value::Str("watchOrderBookForSymbols".to_string()), Value::Str("depth".to_string()), &[Value::Str("depth/increase100".to_string())]); channel = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        if is_equal(&type_var, &Value::Str("swap".to_string())) && is_equal(&channel, &Value::Str("depth/increase100".to_string())) {
            channel = Value::Str("depth50".to_string());
        }
        let mut orderbook: Value = self.subscribe_multiple(Value::Str("orderbook".to_string()), channel.clone(), type_var.clone(), &[symbols.clone(), params.clone()]).await;
        return orderbook.limit();

    Value::Null
}

/*
 * @method
 * @name bitmart#unWatchOrderBookForSymbols
 * @description unWatches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
 * @see https://developer-pro.bitmart.com/en/spot/#public-depth-increase-channel
 * @param {string[]} symbols unified array of symbols
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.depth] the type of order book to subscribe to, default is 'depth/increase100', also accepts 'depth5' or 'depth20' or depth50
 * @returns {object} A dictionary of [order book structures]{@link https://docs.ccxt.com/?id=order-book-structure}
 */
    pub async fn un_watch_order_book_for_symbols(&mut self, mut symbols: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut type_var: Value = Value::Null;
        { let __destr_tmp = self.get_params_for_multiple_sub(Value::Str("unWatchOrderBookForSymbols".to_string()), symbols.clone(), &[Value::Null, params.clone()]); symbols = get_value(&__destr_tmp, &Value::Int(0)); type_var = get_value(&__destr_tmp, &Value::Int(1)); params = get_value(&__destr_tmp, &Value::Int(2)); }
        let mut channel: Value = Value::Null;
        { let __destr_tmp = self.handle_option_and_params(params.clone(), Value::Str("unWatchOrderBookForSymbols".to_string()), Value::Str("depth".to_string()), &[Value::Str("depth/increase100".to_string())]); channel = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        if is_equal(&type_var, &Value::Str("swap".to_string())) && is_equal(&channel, &Value::Str("depth/increase100".to_string())) {
            channel = Value::Str("depth50".to_string());
        }
        params = self.extend(params.clone(), &[Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("unsubscribe".to_string(), Value::Bool(true));
            m
        })]);
        return self.subscribe_multiple(Value::Str("orderbook".to_string()), channel.clone(), type_var.clone(), &[symbols.clone(), params.clone()]).await;

    Value::Null
}

/*
 * @method
 * @name bitmart#watchFundingRate
 * @description watch the current funding rate
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-funding-rate-channel
 * @param {string} symbol unified market symbol
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [funding rate structure]{@link https://docs.ccxt.com/?id=funding-rate-structure}
 */
    pub async fn watch_funding_rate(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        symbol = self.symbol(symbol.clone());
        let mut fundingRate: Value = self.watch_funding_rates(&[Value::List(vec![symbol.clone()]), params.clone()]).await;
        return get_value(&fundingRate, &symbol);

    Value::Null
}

/*
 * @method
 * @name bitmart#watchFundingRates
 * @description watch the funding rate for multiple markets
 * @see https://developer-pro.bitmart.com/en/futuresv2/#public-funding-rate-channel
 * @param {string[]} symbols a list of unified market symbols
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a dictionary of [funding rate structures]{@link https://docs.ccxt.com/?id=funding-rate-structure}, indexed by market symbols
 */
    pub async fn watch_funding_rates(&mut self, optional_args: &[Value]) -> Value {
        let mut symbols = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&symbols, &Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(add(&self.id, &Value::Str(" watchFundingRates() requires an array of symbols".to_string()))));
        }
        if is_equal(&self.markets, &Value::Null) {
            self.load_markets(&[]).await;
        }
        let mut market: Value = self.get_market_from_symbols(&[symbols.clone()]);
        let mut marketType: Value = Value::Null;
        { let __destr_tmp = self.handle_market_type_and_params(Value::Str("watchFundingRates".to_string()), &[market.clone(), params.clone()]); marketType = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        let mut fundingRate: Value = self.subscribe_multiple(Value::Str("fundingRate".to_string()), Value::Str("fundingRate".to_string()), marketType.clone(), &[symbols.clone(), params.clone()]).await;
        if is_true(&self.newUpdates) {
            let mut fundingRates: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                m
            });
            add_element_to_object(&mut fundingRates, &get_value(&fundingRate, &Value::Str("symbol".to_string())), fundingRate.clone());
            return fundingRates;
        }
        return self.filter_by_array(self.fundingRates.clone(), Value::Str("symbol".to_string()), &[symbols.clone()]);

    Value::Null
}

    pub fn handle_funding_rate(&self, mut client: Value, mut message: Value) {
        //
        //     {
        //         "data": {
        //             "symbol": "BTCUSDT",
        //             "fundingRate": "0.0000561",
        //             "fundingTime": 1770978448000,
        //             "nextFundingRate": "-0.0000195",
        //             "nextFundingTime": 1770998400000,
        //             "funding_upper_limit": "0.0375",
        //             "funding_lower_limit": "-0.0375",
        //             "ts": 1770978448970
        //         },
        //         "group": "futures/fundingRate:BTCUSDT"
        //     }
        //
        let mut data: Value = self.safe_dict_k(message.clone(), "data", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut fundingRate: Value = self.parse_funding_rate(data.clone(), &[]);
        let mut symbol: Value = get_value(&fundingRate, &Value::Str("symbol".to_string()));
        if !is_equal(&symbol, &Value::Null) {
            add_element_to_object(&mut self.fundingRates.clone(), &symbol, fundingRate.clone());
        }
        let mut messageHash: Value = add(&Value::Str("fundingRate::".to_string()), &symbol);
        client.resolve(&[fundingRate.clone(), messageHash.clone()]);
}

    pub async fn authenticate(&mut self, mut type_var: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.check_required_credentials(&[]);
        let mut url: Value = self.implode_hostname(get_value(&get_value(&get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &Value::Str("ws".to_string())), &type_var), &Value::Str("private".to_string())));
        let mut messageHash: Value = Value::Str("authenticated".to_string());
        let mut client: Value = self.client(&[url.clone()]);
        let mut future: Value = client.reusable_future(messageHash.clone());
        let mut authenticated: Value = self.safe_value(get_value(&client, &Value::Str("subscriptions".to_string())), messageHash.clone(), &[]);
        if is_equal(&authenticated, &Value::Null) {
            let mut timestamp: Value = to_string_val(&self.milliseconds());
            let mut memo: Value = self.uid.clone();
            let mut path: Value = Value::Str("bitmart.WebSocket".to_string());
            let mut auth: Value = add(&add(&add(&add(&timestamp, &Value::Str("#".to_string())), &memo), &Value::Str("#".to_string())), &path);
            let mut signature: Value = self.hmac(self.encode(auth.clone()), self.encode(self.secret.clone()), Value::Str("sha256".to_string()), &[]);
            let mut request: Value = Value::Null;
            if is_equal(&type_var, &Value::Str("spot".to_string())) {
                request = Value::Map({
                    let mut m = indexmap::IndexMap::new();
                        m.insert("op".to_string(), Value::Str("login".to_string()));
                        m.insert("args".to_string(), Value::List(vec![self.apiKey.clone(), timestamp.clone(), signature.clone()]));
                    m
                });
            }  else {
                request = Value::Map({
                    let mut m = indexmap::IndexMap::new();
                        m.insert("action".to_string(), Value::Str("access".to_string()));
                        m.insert("args".to_string(), Value::List(vec![self.apiKey.clone(), timestamp.clone(), signature.clone(), Value::Str("web".to_string())]));
                    m
                });
            }
            let mut message: Value = self.extend(request.clone(), &[params.clone()]);
            self.watch(url.clone(), messageHash.clone(), &[message.clone(), messageHash.clone()]).await;
        }
        return get_value(&future, &Value::Str("await".to_string()));

    Value::Null
}

    pub fn handle_subscription_status(&self, mut client: Value, mut message: Value) -> Value {
        return message;

    Value::Null
}

    pub fn handle_authenticate(&self, mut client: Value, mut message: Value) {
        //
        // spot
        //    { event: "login" }
        // swap
        //    { action: 'access', success: true }
        //
        let mut messageHash: Value = Value::Str("authenticated".to_string());
        let mut future: Value = self.safe_value(get_value(&client, &Value::Str("futures".to_string())), messageHash.clone(), &[]);
        future.resolve(&[Value::Bool(true)]);
}

    pub fn handle_error_message(&self, mut client: Value, mut message: Value) -> Value {
        //
        //    { event: "error", message: "Invalid sign", errorCode: 30013 }
        //    {"event":"error","message":"Unrecognized request: {\"event\":\"subscribe\",\"channel\":\"spot/depth:BTC-USDT\"}","errorCode":30039}
        //    {
        //        action: '',
        //        group: 'futures/trade:BTCUSDT',
        //        success: false,
        //        request: { action: '', args: [ 'futures/trade:BTCUSDT' ] },
        //        error: 'Invalid action [] for group [futures/trade:BTCUSDT]'
        //    }
        //
        let mut errorCode: Value = self.safe_string_k(message.clone(), "errorCode", &[]);
        let mut error: Value = self.safe_string_k(message.clone(), "error", &[]);
        // Duplicate-subscription notice errorCode 90008: bitmart's WS rejects
        // a re-subscribe attempt on a topic that's already active on this
        // connection, but the original subscription keeps delivering data —
        // so treat it as benign. Without this short-circuit, the generic
        // client.reject below kills every unrelated in-flight future —
        // e.g. a watchOHLCV waiting on its kline subscription gets rejected
        // by an orderbook 90008 raised on the same socket.
        if is_equal(&errorCode, &Value::Str("90008".to_string())) {
            return Value::Bool(false);
        }
        let _try_result = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            if !is_equal(&errorCode, &Value::Null) || !is_equal(&error, &Value::Null) {
                let mut feedback: Value = add(&add(&self.id, &Value::Str(" ".to_string())), &self.json(message.clone()));
                self.throw_exactly_matched_exception(get_value(&self.exceptions, &Value::Str("exact".to_string())), errorCode.clone(), feedback.clone());
                let mut messageString: Value = self.safe_value_k(message.clone(), "message", &[error.clone()]);
                self.throw_broadly_matched_exception(get_value(&self.exceptions, &Value::Str("broad".to_string())), messageString.clone(), feedback.clone());
                let mut action: Value = self.safe_string_k(message.clone(), "action", &[]);
                if is_equal(&action, &Value::Str("access".to_string())) {
                    panic!("{}", crate::exchange_errors::authentication_error(feedback));
                }
                panic!("{}", crate::exchange_errors::exchange_error(feedback));
            }
            return Value::Bool(false);
         #[allow(unreachable_code)] { Value::Null }}));
match _try_result { Ok(__try_ok) => { if !matches!(__try_ok, Value::Null) { return __try_ok; } return Value::Null; } Err(_try_err) => { let e: Value = panic_to_value(_try_err); 
            if is_true(&(is_instance(&e, &Value::Str("AuthenticationError".to_string())))) {
                let mut messageHash: Value = Value::Str("authenticated".to_string());
                client.reject(&[e.clone(), messageHash.clone()]);
                if is_true(&Value::Bool(in_op(&get_value(&client, &Value::Str("subscriptions".to_string())), &messageHash))) {
                    remove(&mut get_value(&client, &Value::Str("subscriptions".to_string())), &messageHash);
                }
            }
            client.reject(&[e.clone()]);
            return Value::Bool(true);
         } }

    Value::Null
}

    pub fn handle_un_subscription(&mut self, mut client: Value, mut message: Value) {
        //
        // spot
        //     {
        //         "topic": "spot/ticker:ETH_USDT",
        //         "event": "unsubscribe"
        //     }
        //
        // swap
        //     {
        //         "action": "unsubscribe",
        //         "group": "futures/ticker:ETHUSDT",
        //         "success": true,
        //         "request": {
        //             "action": "unsubscribe",
        //             "args": [
        //                 "futures/ticker:ETHUSDT"
        //             ]
        //         }
        //     }
        //
        let mut messageTopic: Value = self.safe_string2(message.clone(), Value::Str("topic".to_string()), Value::Str("group".to_string()), &[Value::Str("".to_string())]);
        let mut unSubMessageTopic: Value = add(&Value::Str("unsubscribe::".to_string()), &messageTopic);
        // one message includes info about one unsubscription only even if we requested multiple
        // so we can not just create subscription object in unWatch method and use it here
        // we need to reconstruct subscription params from the messageTopic
        let mut subscription: Value = self.get_un_sub_params(messageTopic.clone());
        let mut subHash: Value = self.safe_string_k(subscription.clone(), "subHash", &[]);
        let mut unsubHash: Value = add(&Value::Str("unsubscribe::".to_string()), &subHash);
        let mut subHashIsPrefix: Value = self.safe_bool_k(subscription.clone(), "subHashIsPrefix", &[Value::Bool(false)]);
        // clean up both ways of storing subscription and unsubscription
        self.clean_unsubscription(client.clone(), subHash.clone(), unsubHash.clone(), &[subHashIsPrefix.clone()]);
        self.clean_unsubscription(client.clone(), messageTopic.clone(), unSubMessageTopic.clone(), &[subHashIsPrefix.clone()]);
        self.clean_cache(subscription.clone());
}

    pub fn get_un_sub_params(&self, mut messageTopic: Value) -> Value {
        let mut parts: Value = split(&messageTopic, &Value::Str(":".to_string()));
        let mut channel: Value = self.safe_string(parts.clone(), Value::Int(0), &[Value::Str("".to_string())]);
        let mut marketTypeAndTopic: Value = split(&channel, &Value::Str("/".to_string()));
        let mut rawMarketType: Value = self.safe_string_lower(marketTypeAndTopic.clone(), Value::Int(0), &[]);
        let mut marketType: Value = self.parse_market_type(rawMarketType.clone());
        let mut topic: Value = self.safe_string(marketTypeAndTopic.clone(), Value::Int(1), &[Value::Str("".to_string())]);
        let mut thirdPart: Value = self.safe_string(marketTypeAndTopic.clone(), Value::Int(2), &[]);
        if !is_equal(&thirdPart, &Value::Null) {
            topic = add(&topic, &add(&Value::Str("/".to_string()), &thirdPart));
        }
        let mut marketId: Value = self.safe_string(parts.clone(), Value::Int(1), &[]);
        let mut symbols: Value = Value::List(vec![]);
        let mut symbol: Value = Value::Null;
        let mut subHash: Value = topic.clone();
        let mut hashDelimiter: Value = Value::Str(":".to_string());
        let mut subHashIsPrefix: Value = Value::Bool(false);
        let mut parsedTopic: Value = self.parse_topic(topic.clone());
        if is_true(&(is_equal(&parsedTopic, &Value::Str("orders".to_string())))) || is_true(&(is_equal(&parsedTopic, &Value::Str("positions".to_string())))) {
            subHash = parsedTopic.clone();
            hashDelimiter = Value::Str("::".to_string());
        }
        if is_true(&(!is_equal(&marketId, &Value::Null))) && is_true(&(!is_equal(&marketId, &Value::Str("ALL_SYMBOLS".to_string())))) {
            // if marketId is defined, we have a single symbol subscription
            let mut delimiter: Value = ternary(is_true(&(is_equal(&marketType, &Value::Str("spot".to_string())))), Value::Str("_".to_string()), Value::Str("".to_string()));
            let mut market: Value = self.safe_market(&[marketId.clone(), Value::Null, delimiter.clone(), marketType.clone()]);
            symbol = get_value(&market, &Value::Str("symbol".to_string()));
            subHash = add(&subHash, &add(&hashDelimiter, &symbol));
            append_to_array(&mut symbols, symbol.clone());
        }  else {
            subHashIsPrefix = Value::Bool(true); // need to clean all subHashes with this prefix
        }
        let mut symbolsAndTimeframes: Value = Value::List(vec![]);
        if is_true(&Value::Bool(starts_with(&topic, &Value::Str("kline".to_string())))) {
            let mut interval: Value = replace_str(&topic, &Value::Str("kline".to_string()), &Value::Str("".to_string()));
            if is_true(&Value::Bool(starts_with(&interval, &Value::Str("Bin".to_string())))) {
                // swap market
                interval = replace_str(&interval, &Value::Str("Bin".to_string()), &Value::Str("".to_string()));
            }
            let mut timeframes: Value = self.safe_dict_k(self.options.clone(), "timeframes", &[Value::Map({
                let mut m = indexmap::IndexMap::new();
                m
            })]);
            let mut timeframe: Value = self.find_timeframe(interval.clone(), &[timeframes.clone()]);
            let mut symbolAndTimeframe: Value = Value::List(vec![symbol.clone(), timeframe.clone()]);
            append_to_array(&mut symbolsAndTimeframes, symbolAndTimeframe.clone());
        }
        let mut result: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("topic".to_string(), parsedTopic.clone());
                m.insert("symbols".to_string(), symbols.clone());
                m.insert("subHash".to_string(), subHash.clone());
                m.insert("symbolsAndTimeframes".to_string(), symbolsAndTimeframes.clone());
                m.insert("subHashIsPrefix".to_string(), subHashIsPrefix.clone());
            m
        });
        return result;

    Value::Null
}

    pub fn parse_topic(&self, mut topic: Value) -> Value {
        if is_true(&Value::Bool(starts_with(&topic, &Value::Str("depth".to_string())))) {
            return Value::Str("orderbook".to_string());
        }
        if is_true(&Value::Bool(starts_with(&topic, &Value::Str("kline".to_string())))) {
            return Value::Str("ohlcv".to_string());
        }
        let mut topics: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("ticker".to_string(), Value::Str("ticker".to_string()));
                m.insert("trade".to_string(), Value::Str("trades".to_string()));
                m.insert("user/order".to_string(), Value::Str("orders".to_string()));
                m.insert("user/orders".to_string(), Value::Str("orders".to_string()));
                m.insert("order".to_string(), Value::Str("orders".to_string()));
                m.insert("position".to_string(), Value::Str("positions".to_string()));
            m
        });
        return self.safe_string(topics.clone(), topic.clone(), &[topic.clone()]);

    Value::Null
}

    pub fn parse_market_type(&self, mut marketType: Value) -> Value {
        let mut types: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("spot".to_string(), Value::Str("spot".to_string()));
                m.insert("futures".to_string(), Value::Str("swap".to_string()));
            m
        });
        return self.safe_string(types.clone(), marketType.clone(), &[marketType.clone()]);

    Value::Null
}

    pub fn subscription_exists_for_hash(&mut self, mut url: Value, mut hash: Value) -> Value {
        let mut client: Value = self.client(&[url.clone()]);
        return (Value::Bool(in_op(&get_value(&client, &Value::Str("subscriptions".to_string())), &hash)));

    Value::Null
}

    pub fn handle_message(&self, mut client: Value, mut message: Value) {
        if is_true(&self.handle_error_message(client.clone(), message.clone())) {
            return;
        }
        //
        //     {"event":"error","message":"Unrecognized request: {\"event\":\"subscribe\",\"channel\":\"spot/depth:BTC-USDT\"}","errorCode":30039}
        //
        // subscribe events on spot:
        //
        //     {"event":"subscribe", "topic":"spot/kline1m:BTC_USDT" }
        //
        // subscribe on contracts:
        //
        //     {"action":"subscribe", "group":"futures/klineBin1m:BTCUSDT", "success":true, "request":{"action":"subscribe", "args":[ "futures/klineBin1m:BTCUSDT" ] } }
        //
        // regular updates - spot
        //
        //     {
        //         "table": "spot/depth",
        //         "action": "partial",
        //         "data": [
        //             {
        //                 "instrument_id":   "BTC-USDT",
        //                 "asks": [
        //                     ["5301.8", "0.03763319", "1"],
        //                     ["5302.4", "0.00305", "2"],
        //                 ],
        //                 "bids": [
        //                     ["5301.7", "0.58911427", "6"],
        //                     ["5301.6", "0.01222922", "4"],
        //                 ],
        //                 "timestamp": "2020-03-16T03:25:00.440Z",
        //                 "checksum": -2088736623
        //             }
        //         ]
        //     }
        //
        // regular updates - contracts
        //
        //     {
        //         group: "futures/klineBin1m:BTCUSDT",
        //         data: {
        //           symbol: "BTCUSDT",
        //           items: [ { o: "67944.7", "h": .... } ],
        //         },
        //       }
        //
        //     { data: '', table: "spot/user/order" }
        //
        // the only realiable way (for both spot & swap) is to check 'data' key
        let mut isDataUpdate: Value = (Value::Bool(in_op(&message, &Value::Str("data".to_string()))));
        if !is_true(&isDataUpdate) {
            let mut event: Value = self.safe_string2(message.clone(), Value::Str("event".to_string()), Value::Str("action".to_string()), &[]);
            if !is_equal(&event, &Value::Null) {
                let mut methods: Value = Value::Map({
                    let mut m = indexmap::IndexMap::new();
                        m.insert("login".to_string(), Value::Null.clone());
                        m.insert("access".to_string(), Value::Null.clone());
                        m.insert("subscribe".to_string(), Value::Null.clone());
                        m.insert("unsubscribe".to_string(), Value::Null.clone());
                    m
                });
                let mut method: Value = self.safe_value(methods.clone(), event.clone(), &[]);
                if !is_equal(&method, &Value::Null) {
                    method.call(&[client.clone(), message.clone()]);
                }
            }
        }  else {
            let mut channel: Value = self.safe_string2(message.clone(), Value::Str("table".to_string()), Value::Str("group".to_string()), &[Value::Str("".to_string())]);
            let mut methods: Value = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("depth".to_string(), Value::Null.clone());
                    m.insert("bookTicker".to_string(), Value::Null.clone());
                    m.insert("ticker".to_string(), Value::Null.clone());
                    m.insert("trade".to_string(), Value::Null.clone());
                    m.insert("kline".to_string(), Value::Null.clone());
                    m.insert("order".to_string(), Value::Null.clone());
                    m.insert("position".to_string(), Value::Null.clone());
                    m.insert("balance".to_string(), Value::Null.clone());
                    m.insert("asset".to_string(), Value::Null.clone());
                m
            });
            if is_greater_than_or_equal(&get_index_of(&channel, &Value::Str("fundingRate".to_string())), &Value::Int(0)) {
                self.handle_funding_rate(client.clone(), message.clone());
                return;
            }
            // 'ticker' is a substring of 'bookTicker', so a bookTicker channel could
            // be wrongly captured by (or double-dispatched with) the 'ticker' key in a
            // first-match loop (in Go map iteration order is randomized). Check the
            // bookTicker prefix explicitly, then fall back to a simple first-match.
            if is_greater_than_or_equal(&get_index_of(&channel, &Value::Str("bookTicker".to_string())), &Value::Int(0)) {
                self.handle_bid_ask(client.clone(), message.clone());
                return;
            }
            let mut keys: Value = object_keys(&methods);
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_138: bool = true;
                while { if !__for_first_138 { i = add(&i, &Value::Int(1)); } __for_first_138 = false; is_less_than(&i, &get_array_length(&keys)) } {
                let mut key: Value = get_value(&keys, &i);
                let mut key: Value = get_value(&keys, &i);
                if is_greater_than_or_equal(&get_index_of(&channel, &key), &Value::Int(0)) {
                    let mut method: Value = self.safe_value(methods.clone(), key.clone(), &[]);
                    method.call(&[client.clone(), message.clone()]);
                    return;
                }
            }
            }
        }
}
}
