// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#![allow(unused, non_snake_case, clippy::all)]
use crate::Value;
use crate::get_value;
use crate::runtime::*;


pub struct CoinbaseexchangeCore {
    pub exchange: crate::exchange::Exchange,
}

impl CoinbaseexchangeCore {
    pub fn new(config: Option<crate::Value>) -> Self {
        let mut s = Self { exchange: crate::exchange::Exchange::new(config) };
        s.init();
        s
    }

    /// One-shot setup: binds the derived-exchange dispatcher, populates
    /// describe() data onto the inner Exchange, and builds the implicit
    /// API table. Idempotent — calling more than once is safe.
    pub fn init(&mut self) {
        // Bind the trait-object for virtual dispatch (parse_ticker, …).
        let ptr: *const (dyn crate::exchange::DerivedExchange + 'static) =
            self as &Self as *const dyn crate::exchange::DerivedExchange;
        self.exchange.bind_derived(ptr);
        // Populate describe()-derived fields.
        let described = CoinbaseexchangeCore::describe(self);
        self.exchange.api      = crate::get_value(&described, &crate::Value::Str("api".to_string()));
        self.exchange.urls     = crate::get_value(&described, &crate::Value::Str("urls".to_string()));
        self.exchange.has      = crate::get_value(&described, &crate::Value::Str("has".to_string()));
        // Merge describe() options INTO whatever apply_config (or
        // earlier setup) already populated. Caller-supplied options
        // (e.g. portfolioMargin via fixture) take precedence, then
        // describe()'s defaults fill in any gaps.
        let __described_options = crate::get_value(&described, &crate::Value::Str("options".to_string()));
        // Capture the describe()'s explicit networksById BEFORE the merge
        // — the merge below overlays the base Exchange's options (whose
        // after_construct left an EMPTY networksById), which would
        // otherwise clobber the manual mappings like binance BSC to BEP20.
        let __described_networks_by_id = crate::get_value(&__described_options, &crate::Value::Str("networksById".to_string()));
        if let (crate::Value::Dict(existing), crate::Value::Dict(defaults)) =
            (&self.exchange.options.clone(), &__described_options)
        {
            let mut merged = (**defaults).clone();
            for (k, v) in existing.iter() { merged.insert(k.clone(), v.clone()); }
            self.exchange.options = crate::Value::Map(merged);
        } else if !matches!(self.exchange.options, crate::Value::Dict(_)) {
            self.exchange.options = __described_options;
        }
        // Derive options.networksById (CCXT's createNetworksByIdObject):
        // start from the auto-inverted networks (generated), then overlay
        // the describe()'s explicit networksById so manual mappings win —
        // mirrors TS extend(generated, manual). Without the manual
        // overlay, an exchange whose networks defines both BSC to BSC and
        // BEP20 to BSC would invert to BSC to BSC (wrong) by iteration
        // order instead of the intended BSC to BEP20.
        if let crate::Value::Dict(opts_arc) = self.exchange.options.clone() {
            let mut opts = std::sync::Arc::try_unwrap(opts_arc).unwrap_or_else(|a| (*a).clone());
            let mut by_id: indexmap::IndexMap<String, crate::Value> = indexmap::IndexMap::new();
            if let Some(crate::Value::Dict(networks)) = opts.get("networks") {
                for (code, id) in networks.iter() {
                    if let crate::Value::Str(id_s) = id {
                        by_id.entry(id_s.clone())
                             .or_insert_with(|| crate::Value::Str(code.clone()));
                    }
                }
            }
            if let crate::Value::Dict(manual) = &__described_networks_by_id {
                for (k, v) in manual.iter() { by_id.insert(k.clone(), v.clone()); }
            }
            opts.insert("networksById".to_string(), crate::Value::Map(by_id));
            self.exchange.options = crate::Value::Map(opts);
        }
        self.exchange.hostname = crate::get_value(&described, &crate::Value::Str("hostname".to_string()));
        self.exchange.version  = crate::get_value(&described, &crate::Value::Str("version".to_string()));
        self.exchange.id       = crate::get_value(&described, &crate::Value::Str("id".to_string()));
        self.exchange.name     = crate::get_value(&described, &crate::Value::Str("name".to_string()));
        self.exchange.exceptions = crate::get_value(&described, &crate::Value::Str("exceptions".to_string()));
        // Only override the base-default requiredCredentials when the
        // exchange's describe() actually provides them (super.describe()
        // is stubbed, so most exchanges' describe() omits this).
        { let __rc = crate::get_value(&described, &crate::Value::Str("requiredCredentials".to_string())); if !matches!(__rc, crate::Value::Null) { self.exchange.requiredCredentials = __rc; } }
        // Merge describe()'s commonCurrencies over the base defaults so
        // exchange-specific aliases (bitfinex UST to USDT, onetrading
        // MIOTA to IOTA) reach commonCurrencyCode / safeCurrencyCode.
        { let __cc = crate::get_value(&described, &crate::Value::Str("commonCurrencies".to_string())); if let crate::Value::Dict(extra) = __cc { let extra = std::sync::Arc::try_unwrap(extra).unwrap_or_else(|a| (*a).clone()); if let crate::Value::Dict(base) = &mut self.exchange.commonCurrencies { let base = std::sync::Arc::make_mut(base); for (k, v) in extra { base.insert(k, v); } } else { self.exchange.commonCurrencies = crate::Value::Map(extra); } } }
        self.exchange.precisionMode = crate::get_value(&described, &crate::Value::Str("precisionMode".to_string()));
        self.exchange.timeframes = crate::get_value(&described, &crate::Value::Str("timeframes".to_string()));
        self.exchange.fees = crate::get_value(&described, &crate::Value::Str("fees".to_string()));
        // `features` carries the describe() block that drives
        // unified-method tests (e.g. `features.spot.fetchCurrencies.private`
        // tells testFetchCurrencies to skip the length check). It's set
        // after `Exchange::new` because `features_generator` (in
        // `after_construct`) bails when `features == Null` — so we
        // assign and re-run the generator here.
        self.exchange.features = crate::get_value(&described, &crate::Value::Str("features".to_string()));
        if !matches!(self.exchange.features, crate::Value::Null) {
            self.exchange.features_generator();
        }
        // Markets and currencies may have been populated already by
        // the constructor config (test runners pass them in via
        // Exchange::new(Some(config-with-markets)) — same as CCXT TS).
        // Don't reset them here.
        self.exchange.build_implicit_api();
    }

    /// Re-bind the derived-exchange pointer. Needed if the struct's
    /// address moves after `new()` (e.g. `Box::new` the value).
    pub fn bind(&mut self) {
        let ptr: *const (dyn crate::exchange::DerivedExchange + 'static) =
            self as &Self as *const dyn crate::exchange::DerivedExchange;
        self.exchange.bind_derived(ptr);
        // Async dispatch hook: lets base Exchange methods route async
        // calls (cancel_order, fetch_order, …) to this Core's
        // call_dynamic, bypassing the base stubs.
        let core_ptr = self as *mut Self as *mut ();
        self.exchange.bind_call_async(core_ptr, CoinbaseexchangeCore::__call_dynamic_dispatch);
    }

    /// Type-erased trampoline used by Exchange::dispatch_to_derived.
    /// Casts the void pointer back to `*mut Self` and forwards to the
    /// real `call_dynamic`. Safety: the pointer must come from the
    /// matching Core's bind() — guaranteed by Exchange::bind_call_async.
    pub fn __call_dynamic_dispatch<'a>(
        ptr: *mut (),
        method: &'a str,
        args: Vec<crate::Value>,
    ) -> std::pin::Pin<Box<dyn std::future::Future<Output = crate::Value> + 'a>> {
        Box::pin(async move {
            let s: &mut Self = unsafe { &mut *(ptr as *mut Self) };
            s.call_dynamic(method, args).await
        })
    }

    /// Dispatch by method name. Mirrors Go's CallInternal /
    /// C#'s callInternal — lets the CLI call any method without
    /// hard-coding signatures. Accepts the canonical snake_case name.
    /// Returns Value::Null for unknown names.
    pub async fn call_dynamic(&mut self, method: &str, args: Vec<crate::Value>) -> crate::Value {
        match method {
            "cancel_all_orders" => self.cancel_all_orders(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "cancel_order" => self.cancel_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "create_deposit_address" => self.create_deposit_address(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "create_order" => self.create_order(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), args.get(3).cloned().unwrap_or(crate::Value::Null), &args.get(4..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_accounts" => self.fetch_accounts(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_balance" => self.fetch_balance(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_closed_orders" => self.fetch_closed_orders(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_currencies" => self.fetch_currencies(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_deposits" => self.fetch_deposits(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_deposits_withdrawals" => self.fetch_deposits_withdrawals(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_ledger" => self.fetch_ledger(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_markets" => self.fetch_markets(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_my_trades" => self.fetch_my_trades(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_ohlcv" => self.fetch_ohlcv(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_open_orders" => self.fetch_open_orders(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_order" => self.fetch_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_order_book" => self.fetch_order_book(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_order_trades" => self.fetch_order_trades(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_orders" => self.fetch_orders(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_payment_methods" => self.fetch_payment_methods(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_ticker" => self.fetch_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_tickers" => self.fetch_tickers(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_time" => self.fetch_time(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_trades" => self.fetch_trades(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_trading_fees" => self.fetch_trading_fees(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "fetch_withdrawals" => self.fetch_withdrawals(&args.get(0..).unwrap_or(&[]).to_vec()[..]).await,
            "handle_errors" => self.handle_errors(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), args.get(3).cloned().unwrap_or(crate::Value::Null), args.get(4).cloned().unwrap_or(crate::Value::Null), args.get(5).cloned().unwrap_or(crate::Value::Null), args.get(6).cloned().unwrap_or(crate::Value::Null), args.get(7).cloned().unwrap_or(crate::Value::Null), args.get(8).cloned().unwrap_or(crate::Value::Null)),
            "parse_account" => self.parse_account(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "parse_balance" => self.parse_balance(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "parse_currency" => self.parse_currency(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "parse_ledger_entry" => self.parse_ledger_entry(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "parse_ledger_entry_type" => self.parse_ledger_entry_type(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "parse_ohlcv" => self.parse_ohlcv(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "parse_order" => self.parse_order(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "parse_order_status" => self.parse_order_status(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "parse_ticker" => self.parse_ticker(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "parse_trade" => self.parse_trade(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "parse_transaction" => self.parse_transaction(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "parse_transaction_status" => self.parse_transaction_status(args.get(0).cloned().unwrap_or(crate::Value::Null)),
            "request" => self.request(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]).await,
            "sign" => self.sign(args.get(0).cloned().unwrap_or(crate::Value::Null), &args.get(1..).unwrap_or(&[]).to_vec()[..]),
            "withdraw" => self.withdraw(args.get(0).cloned().unwrap_or(crate::Value::Null), args.get(1).cloned().unwrap_or(crate::Value::Null), args.get(2).cloned().unwrap_or(crate::Value::Null), &args.get(3..).unwrap_or(&[]).to_vec()[..]).await,
            // Fall through to the base Exchange impl so inherited
            // methods (cancelOrderWithClientOrderId, …) dispatch.
            _ => self.exchange.call_dynamic(method, args).await,
        }
    }
}

impl crate::exchange::DerivedExchange for CoinbaseexchangeCore {
    fn parse_ticker(&self, ticker: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on CoinbaseexchangeCore.
        #[allow(invalid_reference_casting)]
        let me = unsafe { &mut *(self as *const CoinbaseexchangeCore as *mut CoinbaseexchangeCore) };
        CoinbaseexchangeCore::parse_ticker(me, ticker, &[market.clone()])
    }
    fn parse_trade(&self, trade: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on CoinbaseexchangeCore.
        #[allow(invalid_reference_casting)]
        let me = unsafe { &mut *(self as *const CoinbaseexchangeCore as *mut CoinbaseexchangeCore) };
        CoinbaseexchangeCore::parse_trade(me, trade, &[market.clone()])
    }
    fn parse_order(&self, order: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on CoinbaseexchangeCore.
        #[allow(invalid_reference_casting)]
        let me = unsafe { &mut *(self as *const CoinbaseexchangeCore as *mut CoinbaseexchangeCore) };
        CoinbaseexchangeCore::parse_order(me, order, &[market.clone()])
    }
    fn parse_ohlcv(&self, ohlcv: crate::Value, market: crate::Value) -> crate::Value {
        // Forward to the inherent method on CoinbaseexchangeCore.
        #[allow(invalid_reference_casting)]
        let me = unsafe { &mut *(self as *const CoinbaseexchangeCore as *mut CoinbaseexchangeCore) };
        CoinbaseexchangeCore::parse_ohlcv(me, ohlcv, &[market.clone()])
    }
    fn parse_balance(&self, response: crate::Value) -> crate::Value {
        // Forward to the inherent method on CoinbaseexchangeCore.
        #[allow(invalid_reference_casting)]
        let me = unsafe { &mut *(self as *const CoinbaseexchangeCore as *mut CoinbaseexchangeCore) };
        CoinbaseexchangeCore::parse_balance(me, response)
    }
    fn parse_ledger_entry(&self, entry: crate::Value, currency: crate::Value) -> crate::Value {
        // Forward to the inherent method on CoinbaseexchangeCore.
        #[allow(invalid_reference_casting)]
        let me = unsafe { &mut *(self as *const CoinbaseexchangeCore as *mut CoinbaseexchangeCore) };
        CoinbaseexchangeCore::parse_ledger_entry(me, entry, &[currency.clone()])
    }
    fn parse_currency(&self, currency: crate::Value) -> crate::Value {
        // Forward to the inherent method on CoinbaseexchangeCore.
        #[allow(invalid_reference_casting)]
        let me = unsafe { &mut *(self as *const CoinbaseexchangeCore as *mut CoinbaseexchangeCore) };
        CoinbaseexchangeCore::parse_currency(me, currency)
    }
    fn parse_account(&self, account: crate::Value) -> crate::Value {
        // Forward to the inherent method on CoinbaseexchangeCore.
        #[allow(invalid_reference_casting)]
        let me = unsafe { &mut *(self as *const CoinbaseexchangeCore as *mut CoinbaseexchangeCore) };
        CoinbaseexchangeCore::parse_account(me, account)
    }
    fn parse_transaction(&self, transaction: crate::Value, currency: crate::Value) -> crate::Value {
        // Forward to the inherent method on CoinbaseexchangeCore.
        #[allow(invalid_reference_casting)]
        let me = unsafe { &mut *(self as *const CoinbaseexchangeCore as *mut CoinbaseexchangeCore) };
        CoinbaseexchangeCore::parse_transaction(me, transaction, &[currency.clone()])
    }
    fn sign(&self, path: crate::Value, api: crate::Value, method: crate::Value, params: crate::Value, headers: crate::Value, body: crate::Value) -> crate::Value {
        // Forward to the inherent method on CoinbaseexchangeCore.
        #[allow(invalid_reference_casting)]
        let me = unsafe { &mut *(self as *const CoinbaseexchangeCore as *mut CoinbaseexchangeCore) };
        CoinbaseexchangeCore::sign(me, path, &[api.clone(), method.clone(), params.clone(), headers.clone(), body.clone()])
    }
    fn handle_errors(&self, code: crate::Value, reason: crate::Value, url: crate::Value, method: crate::Value, headers: crate::Value, body: crate::Value, response: crate::Value, request_headers: crate::Value, request_body: crate::Value) -> crate::Value {
        // Forward to the inherent method on CoinbaseexchangeCore.
        #[allow(invalid_reference_casting)]
        let me = unsafe { &mut *(self as *const CoinbaseexchangeCore as *mut CoinbaseexchangeCore) };
        CoinbaseexchangeCore::handle_errors(me, code, reason, url, method, headers, body, response, request_headers, request_body)
    }
}

impl std::ops::Deref for CoinbaseexchangeCore {
    type Target = crate::exchange::Exchange;
    fn deref(&self) -> &Self::Target { &self.exchange }
}

impl std::ops::DerefMut for CoinbaseexchangeCore {
    fn deref_mut(&mut self) -> &mut Self::Target { &mut self.exchange }
}

impl CoinbaseexchangeCore {
    pub fn describe(&self) -> Value {
        return self.deep_extend(self.super_describe(), &[Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), Value::Str("coinbaseexchange".to_string()));
        m.insert("name".to_string(), Value::Str("Coinbase Exchange".to_string()));
        m.insert("countries".to_string(), Value::List(vec![Value::Str("US".to_string())]));
        m.insert("rateLimit".to_string(), Value::Int(100));
        m.insert("userAgent".to_string(), get_value(&self.userAgents, &Value::Str("chrome".to_string())));
        m.insert("pro".to_string(), Value::Bool(true));
        m.insert("has".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("CORS".to_string(), Value::Bool(true));
        m.insert("spot".to_string(), Value::Bool(true));
        m.insert("margin".to_string(), Value::Bool(false));
        m.insert("swap".to_string(), Value::Bool(false));
        m.insert("future".to_string(), Value::Bool(false));
        m.insert("option".to_string(), Value::Bool(false));
        m.insert("addMargin".to_string(), Value::Bool(false));
        m.insert("borrowCrossMargin".to_string(), Value::Bool(false));
        m.insert("borrowIsolatedMargin".to_string(), Value::Bool(false));
        m.insert("borrowMargin".to_string(), Value::Bool(false));
        m.insert("cancelAllOrders".to_string(), Value::Bool(true));
        m.insert("cancelOrder".to_string(), Value::Bool(true));
        m.insert("closeAllPositions".to_string(), Value::Bool(false));
        m.insert("closePosition".to_string(), Value::Bool(false));
        m.insert("createDepositAddress".to_string(), Value::Bool(true));
        m.insert("createOrder".to_string(), Value::Bool(true));
        m.insert("createOrderWithTakeProfitAndStopLoss".to_string(), Value::Bool(false));
        m.insert("createOrderWithTakeProfitAndStopLossWs".to_string(), Value::Bool(false));
        m.insert("createPostOnlyOrder".to_string(), Value::Bool(false));
        m.insert("createReduceOnlyOrder".to_string(), Value::Bool(false));
        m.insert("createStopLimitOrder".to_string(), Value::Bool(true));
        m.insert("createStopMarketOrder".to_string(), Value::Bool(true));
        m.insert("createStopOrder".to_string(), Value::Bool(true));
        m.insert("fetchAccounts".to_string(), Value::Bool(true));
        m.insert("fetchBalance".to_string(), Value::Bool(true));
        m.insert("fetchBorrowInterest".to_string(), Value::Bool(false));
        m.insert("fetchBorrowRate".to_string(), Value::Bool(false));
        m.insert("fetchBorrowRateHistories".to_string(), Value::Bool(false));
        m.insert("fetchBorrowRateHistory".to_string(), Value::Bool(false));
        m.insert("fetchBorrowRates".to_string(), Value::Bool(false));
        m.insert("fetchBorrowRatesPerSymbol".to_string(), Value::Bool(false));
        m.insert("fetchClosedOrders".to_string(), Value::Bool(true));
        m.insert("fetchCrossBorrowRate".to_string(), Value::Bool(false));
        m.insert("fetchCrossBorrowRates".to_string(), Value::Bool(false));
        m.insert("fetchCurrencies".to_string(), Value::Bool(true));
        m.insert("fetchDepositAddress".to_string(), Value::Bool(false));
        m.insert("fetchDeposits".to_string(), Value::Bool(true));
        m.insert("fetchDepositsWithdrawals".to_string(), Value::Bool(true));
        m.insert("fetchFundingHistory".to_string(), Value::Bool(false));
        m.insert("fetchFundingInterval".to_string(), Value::Bool(false));
        m.insert("fetchFundingIntervals".to_string(), Value::Bool(false));
        m.insert("fetchFundingRate".to_string(), Value::Bool(false));
        m.insert("fetchFundingRateHistory".to_string(), Value::Bool(false));
        m.insert("fetchFundingRates".to_string(), Value::Bool(false));
        m.insert("fetchGreeks".to_string(), Value::Bool(false));
        m.insert("fetchIndexOHLCV".to_string(), Value::Bool(false));
        m.insert("fetchIsolatedBorrowRate".to_string(), Value::Bool(false));
        m.insert("fetchIsolatedBorrowRates".to_string(), Value::Bool(false));
        m.insert("fetchIsolatedPositions".to_string(), Value::Bool(false));
        m.insert("fetchLedger".to_string(), Value::Bool(true));
        m.insert("fetchLeverage".to_string(), Value::Bool(false));
        m.insert("fetchLeverages".to_string(), Value::Bool(false));
        m.insert("fetchLeverageTiers".to_string(), Value::Bool(false));
        m.insert("fetchLiquidations".to_string(), Value::Bool(false));
        m.insert("fetchLongShortRatio".to_string(), Value::Bool(false));
        m.insert("fetchLongShortRatioHistory".to_string(), Value::Bool(false));
        m.insert("fetchMarginAdjustmentHistory".to_string(), Value::Bool(false));
        m.insert("fetchMarginMode".to_string(), Value::Bool(false));
        m.insert("fetchMarginModes".to_string(), Value::Bool(false));
        m.insert("fetchMarketLeverageTiers".to_string(), Value::Bool(false));
        m.insert("fetchMarkets".to_string(), Value::Bool(true));
        m.insert("fetchMarkOHLCV".to_string(), Value::Bool(false));
        m.insert("fetchMarkPrices".to_string(), Value::Bool(false));
        m.insert("fetchMyLiquidations".to_string(), Value::Bool(false));
        m.insert("fetchMySettlementHistory".to_string(), Value::Bool(false));
        m.insert("fetchMyTrades".to_string(), Value::Bool(true));
        m.insert("fetchOHLCV".to_string(), Value::Bool(true));
        m.insert("fetchOpenInterest".to_string(), Value::Bool(false));
        m.insert("fetchOpenInterestHistory".to_string(), Value::Bool(false));
        m.insert("fetchOpenInterests".to_string(), Value::Bool(false));
        m.insert("fetchOpenOrders".to_string(), Value::Bool(true));
        m.insert("fetchOption".to_string(), Value::Bool(false));
        m.insert("fetchOptionChain".to_string(), Value::Bool(false));
        m.insert("fetchOrder".to_string(), Value::Bool(true));
        m.insert("fetchOrderBook".to_string(), Value::Bool(true));
        m.insert("fetchOrders".to_string(), Value::Bool(true));
        m.insert("fetchOrderTrades".to_string(), Value::Bool(true));
        m.insert("fetchPosition".to_string(), Value::Bool(false));
        m.insert("fetchPositionHistory".to_string(), Value::Bool(false));
        m.insert("fetchPositionMode".to_string(), Value::Bool(false));
        m.insert("fetchPositions".to_string(), Value::Bool(false));
        m.insert("fetchPositionsForSymbol".to_string(), Value::Bool(false));
        m.insert("fetchPositionsHistory".to_string(), Value::Bool(false));
        m.insert("fetchPositionsRisk".to_string(), Value::Bool(false));
        m.insert("fetchPremiumIndexOHLCV".to_string(), Value::Bool(false));
        m.insert("fetchSettlementHistory".to_string(), Value::Bool(false));
        m.insert("fetchTicker".to_string(), Value::Bool(true));
        m.insert("fetchTickers".to_string(), Value::Bool(true));
        m.insert("fetchTime".to_string(), Value::Bool(true));
        m.insert("fetchTrades".to_string(), Value::Bool(true));
        m.insert("fetchTradingFee".to_string(), Value::Bool(false));
        m.insert("fetchTradingFees".to_string(), Value::Bool(true));
        m.insert("fetchTransactions".to_string(), Value::Str("emulated".to_string()));
        m.insert("fetchVolatilityHistory".to_string(), Value::Bool(false));
        m.insert("fetchWithdrawals".to_string(), Value::Bool(true));
        m.insert("reduceMargin".to_string(), Value::Bool(false));
        m.insert("repayCrossMargin".to_string(), Value::Bool(false));
        m.insert("repayIsolatedMargin".to_string(), Value::Bool(false));
        m.insert("repayMargin".to_string(), Value::Bool(false));
        m.insert("setLeverage".to_string(), Value::Bool(false));
        m.insert("setMargin".to_string(), Value::Bool(false));
        m.insert("setMarginMode".to_string(), Value::Bool(false));
        m.insert("setPositionMode".to_string(), Value::Bool(false));
        m.insert("withdraw".to_string(), Value::Bool(true));
    m
}));
        m.insert("timeframes".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("1m".to_string(), Value::Int(60));
        m.insert("5m".to_string(), Value::Int(300));
        m.insert("15m".to_string(), Value::Int(900));
        m.insert("1h".to_string(), Value::Int(3600));
        m.insert("6h".to_string(), Value::Int(21600));
        m.insert("1d".to_string(), Value::Int(86400));
    m
}));
        m.insert("hostname".to_string(), Value::Str("exchange.coinbase.com".to_string()));
        m.insert("urls".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("test".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("public".to_string(), Value::Str("https://api-public.sandbox.exchange.coinbase.com".to_string()));
        m.insert("private".to_string(), Value::Str("https://api-public.sandbox.exchange.coinbase.com".to_string()));
    m
}));
        m.insert("logo".to_string(), Value::Str("https://github.com/ccxt/ccxt/assets/43336371/34a65553-88aa-4a38-a714-064bd228b97e".to_string()));
        m.insert("api".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("public".to_string(), Value::Str("https://api.{hostname}".to_string()));
        m.insert("private".to_string(), Value::Str("https://api.{hostname}".to_string()));
    m
}));
        m.insert("www".to_string(), Value::Str("https://coinbase.com/".to_string()));
        m.insert("doc".to_string(), Value::Str("https://docs.cloud.coinbase.com/exchange/docs/".to_string()));
        m.insert("fees".to_string(), Value::List(vec![Value::Str("https://docs.pro.coinbase.com/#fees".to_string()), Value::Str("https://support.pro.coinbase.com/customer/en/portal/articles/2945310-fees".to_string())]));
    m
}));
        m.insert("requiredCredentials".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("apiKey".to_string(), Value::Bool(true));
        m.insert("secret".to_string(), Value::Bool(true));
        m.insert("password".to_string(), Value::Bool(true));
    m
}));
        m.insert("api".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("public".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("get".to_string(), Value::List(vec![Value::Str("currencies".to_string()), Value::Str("products".to_string()), Value::Str("products/{id}".to_string()), Value::Str("products/{id}/book".to_string()), Value::Str("products/{id}/candles".to_string()), Value::Str("products/{id}/stats".to_string()), Value::Str("products/{id}/ticker".to_string()), Value::Str("products/{id}/trades".to_string()), Value::Str("time".to_string()), Value::Str("products/spark-lines".to_string()), Value::Str("products/volume-summary".to_string())]));
    m
}));
        m.insert("private".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("get".to_string(), Value::List(vec![Value::Str("address-book".to_string()), Value::Str("accounts".to_string()), Value::Str("accounts/{id}".to_string()), Value::Str("accounts/{id}/holds".to_string()), Value::Str("accounts/{id}/ledger".to_string()), Value::Str("accounts/{id}/transfers".to_string()), Value::Str("coinbase-accounts".to_string()), Value::Str("fills".to_string()), Value::Str("funding".to_string()), Value::Str("fees".to_string()), Value::Str("margin/profile_information".to_string()), Value::Str("margin/buying_power".to_string()), Value::Str("margin/withdrawal_power".to_string()), Value::Str("margin/withdrawal_power_all".to_string()), Value::Str("margin/exit_plan".to_string()), Value::Str("margin/liquidation_history".to_string()), Value::Str("margin/position_refresh_amounts".to_string()), Value::Str("margin/status".to_string()), Value::Str("oracle".to_string()), Value::Str("orders".to_string()), Value::Str("orders/{id}".to_string()), Value::Str("orders/client:{client_oid}".to_string()), Value::Str("otc/orders".to_string()), Value::Str("payment-methods".to_string()), Value::Str("position".to_string()), Value::Str("profiles".to_string()), Value::Str("profiles/{id}".to_string()), Value::Str("reports/{report_id}".to_string()), Value::Str("transfers".to_string()), Value::Str("transfers/{transfer_id}".to_string()), Value::Str("users/self/exchange-limits".to_string()), Value::Str("users/self/hold-balances".to_string()), Value::Str("users/self/trailing-volume".to_string()), Value::Str("withdrawals/fee-estimate".to_string()), Value::Str("conversions/{conversion_id}".to_string()), Value::Str("conversions".to_string()), Value::Str("conversions/fees".to_string()), Value::Str("loans/lending-overview".to_string()), Value::Str("loans/lending-overview-xm".to_string()), Value::Str("loans/loan-preview".to_string()), Value::Str("loans/loan-preview-xm".to_string()), Value::Str("loans/repayment-preview".to_string()), Value::Str("loans/repayment-preview-xm".to_string()), Value::Str("loans/interest/{loan_id}".to_string()), Value::Str("loans/interest/history/{loan_id}".to_string()), Value::Str("loans/interest".to_string()), Value::Str("loans/assets".to_string()), Value::Str("loans".to_string())]));
        m.insert("post".to_string(), Value::List(vec![Value::Str("conversions".to_string()), Value::Str("deposits/coinbase-account".to_string()), Value::Str("deposits/payment-method".to_string()), Value::Str("coinbase-accounts/{id}/addresses".to_string()), Value::Str("funding/repay".to_string()), Value::Str("orders".to_string()), Value::Str("position/close".to_string()), Value::Str("profiles/margin-transfer".to_string()), Value::Str("profiles/transfer".to_string()), Value::Str("reports".to_string()), Value::Str("withdrawals/coinbase".to_string()), Value::Str("withdrawals/coinbase-account".to_string()), Value::Str("withdrawals/crypto".to_string()), Value::Str("withdrawals/payment-method".to_string()), Value::Str("loans/open".to_string()), Value::Str("loans/repay-interest".to_string()), Value::Str("loans/repay-principal".to_string())]));
        m.insert("delete".to_string(), Value::List(vec![Value::Str("orders".to_string()), Value::Str("orders/client:{client_oid}".to_string()), Value::Str("orders/{id}".to_string())]));
    m
}));
    m
}));
        m.insert("commonCurrencies".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("CGLD".to_string(), Value::Str("CELO".to_string()));
    m
}));
        m.insert("precisionMode".to_string(), Value::Int(crate::runtime::TICK_SIZE));
        m.insert("fees".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("trading".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("tierBased".to_string(), Value::Bool(true));
        m.insert("percentage".to_string(), Value::Bool(true));
        m.insert("maker".to_string(), self.parse_number(Value::Str("0.004".to_string()), &[]));
        m.insert("taker".to_string(), self.parse_number(Value::Str("0.006".to_string()), &[]));
    m
}));
        m.insert("funding".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("tierBased".to_string(), Value::Bool(false));
        m.insert("percentage".to_string(), Value::Bool(false));
        m.insert("withdraw".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("BCH".to_string(), Value::Int(0));
        m.insert("BTC".to_string(), Value::Int(0));
        m.insert("LTC".to_string(), Value::Int(0));
        m.insert("ETH".to_string(), Value::Int(0));
        m.insert("EUR".to_string(), Value::Float(0.15));
        m.insert("USD".to_string(), Value::Int(25));
    m
}));
        m.insert("deposit".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("BCH".to_string(), Value::Int(0));
        m.insert("BTC".to_string(), Value::Int(0));
        m.insert("LTC".to_string(), Value::Int(0));
        m.insert("ETH".to_string(), Value::Int(0));
        m.insert("EUR".to_string(), Value::Float(0.15));
        m.insert("USD".to_string(), Value::Int(10));
    m
}));
    m
}));
    m
}));
        m.insert("features".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("default".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("sandbox".to_string(), Value::Bool(true));
        m.insert("createOrder".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(true));
        m.insert("triggerPrice".to_string(), Value::Bool(true));
        m.insert("triggerPriceType".to_string(), Value::Null);
        m.insert("triggerDirection".to_string(), Value::Bool(false));
        m.insert("stopLossPrice".to_string(), Value::Bool(false));
        m.insert("takeProfitPrice".to_string(), Value::Bool(false));
        m.insert("attachedStopLossTakeProfit".to_string(), Value::Null);
        m.insert("timeInForce".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("IOC".to_string(), Value::Bool(true));
        m.insert("FOK".to_string(), Value::Bool(true));
        m.insert("PO".to_string(), Value::Bool(true));
        m.insert("GTD".to_string(), Value::Bool(true));
    m
}));
        m.insert("hedged".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("leverage".to_string(), Value::Bool(false));
        m.insert("marketBuyByCost".to_string(), Value::Bool(false));
        m.insert("marketBuyRequiresPrice".to_string(), Value::Bool(false));
        m.insert("selfTradePrevention".to_string(), Value::Bool(false));
        m.insert("iceberg".to_string(), Value::Bool(true));
    m
}));
        m.insert("createOrders".to_string(), Value::Null);
        m.insert("fetchMyTrades".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(100));
        m.insert("daysBack".to_string(), Value::Int(100000));
        m.insert("untilDays".to_string(), Value::Int(100000));
        m.insert("symbolRequired".to_string(), Value::Bool(true));
    m
}));
        m.insert("fetchOrder".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("trigger".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
        m.insert("fetchOpenOrders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(100));
        m.insert("trigger".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
        m.insert("fetchOrders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(100));
        m.insert("daysBack".to_string(), Value::Int(100000));
        m.insert("untilDays".to_string(), Value::Int(100000));
        m.insert("trigger".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
        m.insert("fetchClosedOrders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(100));
        m.insert("daysBack".to_string(), Value::Int(100000));
        m.insert("daysBackCanceled".to_string(), Value::Int(1));
        m.insert("untilDays".to_string(), Value::Int(100000));
        m.insert("trigger".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
        m.insert("fetchOHLCV".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("limit".to_string(), Value::Int(300));
    m
}));
    m
}));
        m.insert("spot".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("extends".to_string(), Value::Str("default".to_string()));
    m
}));
        m.insert("swap".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("linear".to_string(), Value::Null);
        m.insert("inverse".to_string(), Value::Null);
    m
}));
        m.insert("future".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("linear".to_string(), Value::Null);
        m.insert("inverse".to_string(), Value::Null);
    m
}));
    m
}));
        m.insert("options".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("networks".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("BTC".to_string(), Value::Str("bitcoin".to_string()));
        m.insert("ETH".to_string(), Value::Str("ethereum".to_string()));
        m.insert("SOL".to_string(), Value::Str("solana".to_string()));
        m.insert("ARBONE".to_string(), Value::Str("arbitrum".to_string()));
        m.insert("AVAXC".to_string(), Value::Str("avacchain".to_string()));
        m.insert("MATIC".to_string(), Value::Str("polygon".to_string()));
        m.insert("BASE".to_string(), Value::Str("base".to_string()));
        m.insert("SUI".to_string(), Value::Str("sui".to_string()));
        m.insert("OP".to_string(), Value::Str("optimism".to_string()));
        m.insert("NEAR".to_string(), Value::Str("near".to_string()));
        m.insert("APT".to_string(), Value::Str("aptos".to_string()));
        m.insert("KAVA".to_string(), Value::Str("kava".to_string()));
        m.insert("BLAST".to_string(), Value::Str("blast".to_string()));
        m.insert("XLM".to_string(), Value::Str("stellar".to_string()));
        m.insert("SEI".to_string(), Value::Str("sei".to_string()));
        m.insert("ADA".to_string(), Value::Str("cardano".to_string()));
        m.insert("CORE".to_string(), Value::Str("coredao".to_string()));
        m.insert("ALGO".to_string(), Value::Str("algorand".to_string()));
        m.insert("OSMO".to_string(), Value::Str("osmosis".to_string()));
        m.insert("CELO".to_string(), Value::Str("celo".to_string()));
        m.insert("HBAR".to_string(), Value::Str("hedera".to_string()));
        m.insert("ZKSYNC".to_string(), Value::Str("zksync".to_string()));
        m.insert("STX".to_string(), Value::Str("stacks".to_string()));
        m.insert("XTZ".to_string(), Value::Str("tezos".to_string()));
        m.insert("EGLD".to_string(), Value::Str("elrond".to_string()));
        m.insert("LTC".to_string(), Value::Str("litecoin".to_string()));
        m.insert("ATOM".to_string(), Value::Str("cosmos".to_string()));
        m.insert("FIL".to_string(), Value::Str("filecoin".to_string()));
        m.insert("DOT".to_string(), Value::Str("polkadot".to_string()));
        m.insert("DOGE".to_string(), Value::Str("dogecoin".to_string()));
        m.insert("XRP".to_string(), Value::Str("ripple".to_string()));
        m.insert("DASH".to_string(), Value::Str("dash".to_string()));
    m
}));
    m
}));
        m.insert("exceptions".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("exact".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("Insufficient funds".to_string(), Value::Str("InsufficientFunds".to_string()).clone());
        m.insert("NotFound".to_string(), Value::Str("OrderNotFound".to_string()).clone());
        m.insert("Invalid API Key".to_string(), Value::Str("AuthenticationError".to_string()).clone());
        m.insert("invalid signature".to_string(), Value::Str("AuthenticationError".to_string()).clone());
        m.insert("Invalid Passphrase".to_string(), Value::Str("AuthenticationError".to_string()).clone());
        m.insert("Invalid order id".to_string(), Value::Str("InvalidOrder".to_string()).clone());
        m.insert("Private rate limit exceeded".to_string(), Value::Str("RateLimitExceeded".to_string()).clone());
        m.insert("Trading pair not available".to_string(), Value::Str("PermissionDenied".to_string()).clone());
        m.insert("Product not found".to_string(), Value::Str("InvalidOrder".to_string()).clone());
    m
}));
        m.insert("broad".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("Order already done".to_string(), Value::Str("OrderNotFound".to_string()).clone());
        m.insert("order not found".to_string(), Value::Str("OrderNotFound".to_string()).clone());
        m.insert("price too small".to_string(), Value::Str("InvalidOrder".to_string()).clone());
        m.insert("price too precise".to_string(), Value::Str("InvalidOrder".to_string()).clone());
        m.insert("under maintenance".to_string(), Value::Str("OnMaintenance".to_string()).clone());
        m.insert("size is too small".to_string(), Value::Str("InvalidOrder".to_string()).clone());
        m.insert("Cancel only mode".to_string(), Value::Str("OnMaintenance".to_string()).clone());
    m
}));
    m
}));
    m
})]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchCurrencies
 * @description fetches all available currencies on an exchange
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getcurrencies
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an associative dictionary of currencies
 */
    pub async fn fetch_currencies(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut response: Value = self.public_get_currencies(&[params.clone()]).await;
        return self.parse_currencies(response.clone());

    Value::Null
}

    pub fn parse_currency(&self, mut rawCurrency: Value) -> Value {
        let mut id: Value = self.safe_string_k(rawCurrency.clone(), "id", &[]);
        let mut name: Value = self.safe_string_k(rawCurrency.clone(), "name", &[]);
        let mut code: Value = self.safe_currency_code(id.clone(), &[]);
        let mut details: Value = self.safe_dict_k(rawCurrency.clone(), "details", &[Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
})]);
        let mut networks: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut supportedNetworks: Value = self.safe_list_k(rawCurrency.clone(), "supported_networks", &[Value::List(vec![])]);
        {
                        let mut j: Value = Value::Int(0);
            let mut __for_first_496: bool = true;
            while { if !__for_first_496 { j = add(&j, &Value::Int(1)); } __for_first_496 = false; is_less_than(&j, &get_array_length(&supportedNetworks)) } {
            let mut network: Value = get_value(&supportedNetworks, &j);
            let mut network: Value = get_value(&supportedNetworks, &j);
            let mut networkId: Value = self.safe_string_k(network.clone(), "id", &[]);
            let mut networkCode: Value = self.network_id_to_code(&[networkId.clone()]);
            add_element_to_object(&mut networks, &networkCode, Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), networkId.clone());
        m.insert("name".to_string(), self.safe_string_k(network.clone(), "name", &[]));
        m.insert("network".to_string(), networkCode.clone());
        m.insert("active".to_string(), Value::Bool(is_equal(&self.safe_string_k(network.clone(), "status", &[]), &Value::Str("online".to_string()))));
        m.insert("withdraw".to_string(), Value::Null);
        m.insert("deposit".to_string(), Value::Null);
        m.insert("fee".to_string(), Value::Null);
        m.insert("precision".to_string(), Value::Null);
        m.insert("limits".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("withdraw".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), self.safe_number_k(network.clone(), "min_withdrawal_amount", &[]));
        m.insert("max".to_string(), self.safe_number_k(network.clone(), "max_withdrawal_amount", &[]));
    m
}));
    m
}));
        m.insert("contract".to_string(), self.safe_string_k(network.clone(), "contract_address", &[]));
        m.insert("info".to_string(), network.clone());
    m
}));
        }
        }
        return self.safe_currency_structure(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), id.clone());
        m.insert("code".to_string(), code.clone());
        m.insert("info".to_string(), rawCurrency.clone());
        m.insert("type".to_string(), self.safe_string_k(details.clone(), "type", &[]));
        m.insert("name".to_string(), name.clone());
        m.insert("active".to_string(), Value::Bool(is_equal(&self.safe_string_k(rawCurrency.clone(), "status", &[]), &Value::Str("online".to_string()))));
        m.insert("deposit".to_string(), Value::Null);
        m.insert("withdraw".to_string(), Value::Null);
        m.insert("fee".to_string(), Value::Null);
        m.insert("precision".to_string(), self.safe_number_k(rawCurrency.clone(), "max_precision", &[]));
        m.insert("limits".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("amount".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), self.safe_number_k(details.clone(), "min_size", &[]));
        m.insert("max".to_string(), Value::Null);
    m
}));
        m.insert("withdraw".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), self.safe_number_k(details.clone(), "min_withdrawal_amount", &[]));
        m.insert("max".to_string(), self.safe_number_k(details.clone(), "max_withdrawal_amount", &[]));
    m
}));
    m
}));
        m.insert("networks".to_string(), networks.clone());
    m
}));

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchMarkets
 * @description retrieves data on all markets for coinbaseexchange
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getproducts
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} an array of objects representing market data
 */
    pub async fn fetch_markets(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut response: Value = self.public_get_products(&[params.clone()]).await;
        //
        //     [
        //         {
        //             "id": "BTCAUCTION-USD",
        //             "base_currency": "BTC",
        //             "quote_currency": "USD",
        //             "base_min_size": "0.000016",
        //             "base_max_size": "1500",
        //             "quote_increment": "0.01",
        //             "base_increment": "0.00000001",
        //             "display_name": "BTCAUCTION/USD",
        //             "min_market_funds": "1",
        //             "max_market_funds": "20000000",
        //             "margin_enabled": false,
        //             "fx_stablecoin": false,
        //             "max_slippage_percentage": "0.02000000",
        //             "post_only": false,
        //             "limit_only": false,
        //             "cancel_only": true,
        //             "trading_disabled": false,
        //             "status": "online",
        //             "status_message": '',
        //             "auction_mode": false
        //         },
        //         {
        //             "id": "BTC-USD",
        //             "base_currency": "BTC",
        //             "quote_currency": "USD",
        //             "base_min_size": "0.000016",
        //             "base_max_size": "1500",
        //             "quote_increment": "0.01",
        //             "base_increment": "0.00000001",
        //             "display_name": "BTC/USD",
        //             "min_market_funds": "1",
        //             "max_market_funds": "20000000",
        //             "margin_enabled": false,
        //             "fx_stablecoin": false,
        //             "max_slippage_percentage": "0.02000000",
        //             "post_only": false,
        //             "limit_only": false,
        //             "cancel_only": false,
        //             "trading_disabled": false,
        //             "status": "online",
        //             "status_message": '',
        //             "auction_mode": false
        //         }
        //     ]
        //
        let mut result: Value = Value::List(vec![]);
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_497: bool = true;
            while { if !__for_first_497 { i = add(&i, &Value::Int(1)); } __for_first_497 = false; is_less_than(&i, &get_array_length(&response)) } {
            let mut market: Value = get_value(&response, &i);
            let mut market: Value = get_value(&response, &i);
            let mut id: Value = self.safe_string_k(market.clone(), "id", &[]);
            let mut baseIdquoteIdVariable = split(&id, &Value::Str("-".to_string()));
            let mut baseId: Value = get_value(&baseIdquoteIdVariable, &Value::Int(0));
            let mut quoteId: Value = get_value(&baseIdquoteIdVariable, &Value::Int(1));
            // BTCAUCTION-USD vs BTC-USD conflict workaround, see the output sample above
            // const baseId = this.safeString (market, 'base_currency');
            // const quoteId = this.safeString (market, 'quote_currency');
            let mut base: Value = self.safe_currency_code(baseId.clone(), &[]);
            let mut quote: Value = self.safe_currency_code(quoteId.clone(), &[]);
            let mut status: Value = self.safe_string_k(market.clone(), "status", &[]);
            let __ws_arg_4 = get_value(&self.fees, &Value::Str("trading".to_string()));
            let __ws_arg_0 = self.safe_value_k(market.clone(), "margin_enabled", &[]);
            let __ws_arg_1 = self.safe_number_k(market.clone(), "base_increment", &[]);
            let __ws_arg_2 = self.safe_number_k(market.clone(), "quote_increment", &[]);
            let __ws_arg_3 = self.safe_number_k(market.clone(), "min_market_funds", &[]);
            append_to_array(&mut result, self.extend(__ws_arg_4, &[Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("id".to_string(), id.clone());
                    m.insert("symbol".to_string(), add(&add(&base, &Value::Str("/".to_string())), &quote));
                    m.insert("base".to_string(), base.clone());
                    m.insert("quote".to_string(), quote.clone());
                    m.insert("settle".to_string(), Value::Null);
                    m.insert("baseId".to_string(), baseId.clone());
                    m.insert("quoteId".to_string(), quoteId.clone());
                    m.insert("settleId".to_string(), Value::Null);
                    m.insert("type".to_string(), Value::Str("spot".to_string()));
                    m.insert("spot".to_string(), Value::Bool(true));
                    m.insert("margin".to_string(), __ws_arg_0);
                    m.insert("swap".to_string(), Value::Bool(false));
                    m.insert("future".to_string(), Value::Bool(false));
                    m.insert("option".to_string(), Value::Bool(false));
                    m.insert("active".to_string(), Value::Bool((is_equal(&status, &Value::Str("online".to_string())))));
                    m.insert("contract".to_string(), Value::Bool(false));
                    m.insert("linear".to_string(), Value::Null);
                    m.insert("inverse".to_string(), Value::Null);
                    m.insert("contractSize".to_string(), Value::Null);
                    m.insert("expiry".to_string(), Value::Null);
                    m.insert("expiryDatetime".to_string(), Value::Null);
                    m.insert("strike".to_string(), Value::Null);
                    m.insert("optionType".to_string(), Value::Null);
                    m.insert("precision".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("amount".to_string(), __ws_arg_1);
        m.insert("price".to_string(), __ws_arg_2);
    m
}));
                    m.insert("limits".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("leverage".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Null);
        m.insert("max".to_string(), Value::Null);
    m
}));
        m.insert("amount".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Null);
        m.insert("max".to_string(), Value::Null);
    m
}));
        m.insert("price".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), Value::Null);
        m.insert("max".to_string(), Value::Null);
    m
}));
        m.insert("cost".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("min".to_string(), __ws_arg_3);
        m.insert("max".to_string(), Value::Null);
    m
}));
    m
}));
                    m.insert("created".to_string(), Value::Null);
                    m.insert("info".to_string(), market.clone());
                m
            })]));
        }
        }
        return result;

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchAccounts
 * @description fetch all the accounts associated with a profile
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getaccounts
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a dictionary of [account structures]{@link https://docs.ccxt.com/?id=account-structure} indexed by the account type
 */
    pub async fn fetch_accounts(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        let mut response: Value = self.private_get_accounts(&[params.clone()]).await;
        return self.parse_accounts(response.clone(), &[params.clone()]);

    Value::Null
}

    pub fn parse_account(&self, mut account: Value) -> Value {
        //
        //     {
        //         "id": "4aac9c60-cbda-4396-9da4-4aa71e95fba0",
        //         "currency": "BTC",
        //         "balance": "0.0000000000000000",
        //         "available": "0",
        //         "hold": "0.0000000000000000",
        //         "profile_id": "b709263e-f42a-4c7d-949a-a95c83d065da"
        //     }
        //
        let mut currencyId: Value = self.safe_string_k(account.clone(), "currency", &[]);
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), self.safe_string_k(account.clone(), "id", &[]));
        m.insert("type".to_string(), Value::Null);
        m.insert("code".to_string(), self.safe_currency_code(currencyId.clone(), &[]));
        m.insert("info".to_string(), account.clone());
    m
});

    Value::Null
}

    pub fn parse_balance(&self, mut response: Value) -> Value {
        let mut result: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("info".to_string(), response.clone());
            m
        });
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_498: bool = true;
            while { if !__for_first_498 { i = add(&i, &Value::Int(1)); } __for_first_498 = false; is_less_than(&i, &get_array_length(&response)) } {
            let mut balance: Value = get_value(&response, &i);
            let mut balance: Value = get_value(&response, &i);
            let mut currencyId: Value = self.safe_string_k(balance.clone(), "currency", &[]);
            let mut code: Value = self.safe_currency_code(currencyId.clone(), &[]);
            let mut account: Value = self.account();
            add_element_to_object(&mut account, &Value::Str("free".to_string()), self.safe_string_k(balance.clone(), "available", &[]));
            add_element_to_object(&mut account, &Value::Str("used".to_string()), self.safe_string_k(balance.clone(), "hold", &[]));
            add_element_to_object(&mut account, &Value::Str("total".to_string()), self.safe_string_k(balance.clone(), "balance", &[]));
            add_element_to_object(&mut result, &code, account.clone());
        }
        }
        return self.safe_balance(result.clone());

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchBalance
 * @description query for balance and get the amount of funds available for trading or funds locked in orders
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getaccounts
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [balance structure]{@link https://docs.ccxt.com/?id=balance-structure}
 */
    pub async fn fetch_balance(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        let mut response: Value = self.private_get_accounts(&[params.clone()]).await;
        return self.parse_balance(response.clone());

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchOrderBook
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getproductbook
 * @description fetches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
 * @param {string} symbol unified symbol of the market to fetch the order book for
 * @param {int} [limit] the maximum amount of order book entries to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} A dictionary of [order book structures]{@link https://docs.ccxt.com/?id=order-book-structure} indexed by market symbols
 */
    pub async fn fetch_order_book(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut limit = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        // level 1 - only the best bid and ask
        // level 2 - top 50 bids and asks (aggregated)
        // level 3 - full order book (non aggregated)
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("id".to_string(), self.market_id(symbol.clone()));
                m.insert("level".to_string(), Value::Int(2));
            m
        });
        let __ws_arg_5 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.public_get_products_id_book(&[__ws_arg_5]).await;
        //
        //     {
        //         "sequence":1924393896,
        //         "bids":[
        //             ["0.01825","24.34811287",2],
        //             ["0.01824","72.5463",3],
        //             ["0.01823","424.54298049",6],
        //         ],
        //         "asks":[
        //             ["0.01826","171.10414904",4],
        //             ["0.01827","22.60427028",1],
        //             ["0.01828","397.46018784",7],
        //         ]
        //     }
        //
        let mut orderbook: Value = self.parse_order_book(response.clone(), symbol.clone(), &[]);
        add_element_to_object(&mut orderbook, &Value::Str("nonce".to_string()), self.safe_integer_k(response.clone(), "sequence", &[]));
        return orderbook;

    Value::Null
}

    pub fn parse_ticker(&self, mut ticker: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        // fetchTickers
        //
        //      [
        //         1639472400, // timestamp
        //         4.26, // low
        //         4.38, // high
        //         4.35, // open
        //         4.27 // close
        //      ]
        //
        // fetchTicker
        //
        //     publicGetProductsIdTicker
        //
        //     {
        //         "trade_id":843439,
        //         "price":"0.997999",
        //         "size":"80.29769",
        //         "time":"2020-01-28T02:13:33.012523Z",
        //         "bid":"0.997094",
        //         "ask":"0.998",
        //         "volume":"1903188.03750000"
        //     }
        //
        //     publicGetProductsIdStats
        //
        //     {
        //         "open": "34.19000000",
        //         "high": "95.70000000",
        //         "low": "7.06000000",
        //         "volume": "2.41000000"
        //     }
        //
        let mut timestamp: Value = Value::Null;
        let mut bid: Value = Value::Null;
        let mut ask: Value = Value::Null;
        let mut last: Value = Value::Null;
        let mut high: Value = Value::Null;
        let mut low: Value = Value::Null;
        let mut open: Value = Value::Null;
        let mut volume: Value = Value::Null;
        let mut symbol: Value = ternary(is_true(&(is_equal(&market, &Value::Null))), Value::Null, get_value(&market, &Value::Str("symbol".to_string())));
        if is_true(&Value::Bool(is_array(&ticker))) {
            last = self.safe_string(ticker.clone(), Value::Int(4), &[]);
            timestamp = self.milliseconds();
        }  else {
            timestamp = self.parse8601(self.safe_value_k(ticker.clone(), "time", &[]));
            bid = self.safe_string_k(ticker.clone(), "bid", &[]);
            ask = self.safe_string_k(ticker.clone(), "ask", &[]);
            high = self.safe_string_k(ticker.clone(), "high", &[]);
            low = self.safe_string_k(ticker.clone(), "low", &[]);
            open = self.safe_string_k(ticker.clone(), "open", &[]);
            last = self.safe_string2(ticker.clone(), Value::Str("price".to_string()), Value::Str("last".to_string()), &[]);
            volume = self.safe_string_k(ticker.clone(), "volume", &[]);
        }
        return self.safe_ticker(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("symbol".to_string(), symbol.clone());
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp.clone()));
        m.insert("high".to_string(), high.clone());
        m.insert("low".to_string(), low.clone());
        m.insert("bid".to_string(), bid.clone());
        m.insert("bidVolume".to_string(), Value::Null);
        m.insert("ask".to_string(), ask.clone());
        m.insert("askVolume".to_string(), Value::Null);
        m.insert("vwap".to_string(), Value::Null);
        m.insert("open".to_string(), open.clone());
        m.insert("close".to_string(), last.clone());
        m.insert("last".to_string(), last.clone());
        m.insert("previousClose".to_string(), Value::Null);
        m.insert("change".to_string(), Value::Null);
        m.insert("percentage".to_string(), Value::Null);
        m.insert("average".to_string(), Value::Null);
        m.insert("baseVolume".to_string(), volume.clone());
        m.insert("quoteVolume".to_string(), Value::Null);
        m.insert("info".to_string(), ticker.clone());
    m
}), &[market.clone()]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchTickers
 * @description fetches price tickers for multiple markets, statistical information calculated over the past 24 hours for each market
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getproduct
 * @param {string[]|undefined} symbols unified symbols of the markets to fetch the ticker for, all market tickers are returned if not assigned
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a dictionary of [ticker structures]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
    pub async fn fetch_tickers(&mut self, optional_args: &[Value]) -> Value {
        let mut symbols = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        symbols = self.market_symbols(&[symbols.clone()]);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let __ws_arg_6 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.public_get_products_spark_lines(&[__ws_arg_6]).await;
        //
        //     {
        //         YYY-USD: [
        //             [
        //                 1639472400, // timestamp
        //                 4.26, // low
        //                 4.38, // high
        //                 4.35, // open
        //                 4.27 // close
        //             ],
        //             [
        //                 1639468800,
        //                 4.31,
        //                 4.45,
        //                 4.35,
        //                 4.35
        //             ],
        //         ]
        //     }
        //
        let mut result: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut marketIds: Value = object_keys(&response);
        let mut delimiter: Value = Value::Str("-".to_string());
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_499: bool = true;
            while { if !__for_first_499 { i = add(&i, &Value::Int(1)); } __for_first_499 = false; is_less_than(&i, &get_array_length(&marketIds)) } {
            let mut marketId: Value = get_value(&marketIds, &i);
            let mut marketId: Value = get_value(&marketIds, &i);
            let mut entry: Value = self.safe_value(response.clone(), marketId.clone(), &[Value::List(vec![])]);
            let mut first: Value = self.safe_value(entry.clone(), Value::Int(0), &[Value::List(vec![])]);
            let mut market: Value = self.safe_market(&[marketId.clone(), Value::Null, delimiter.clone()]);
            let mut symbol: Value = get_value(&market, &Value::Str("symbol".to_string()));
            add_element_to_object(&mut result, &symbol, self.parse_ticker(first.clone(), &[market.clone()]));
        }
        }
        return self.filter_by_array_tickers(result.clone(), Value::Str("symbol".to_string()), &[symbols.clone()]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchTicker
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getproductticker
 * @description fetches a price ticker, a statistical calculation with the information calculated over the past 24 hours for a specific market
 * @param {string} symbol unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
    pub async fn fetch_ticker(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("id".to_string(), get_value(&market, &Value::Str("id".to_string())));
            m
        });
        // publicGetProductsIdTicker or publicGetProductsIdStats
        let mut method: Value = self.safe_string_k(self.options.clone(), "fetchTickerMethod", &[Value::Str("publicGetProductsIdTicker".to_string())]);
        let __ws_arg_7 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.call_method(method.clone(), &[__ws_arg_7]).await;
        return self.parse_ticker(response.clone(), &[market.clone()]);

    Value::Null
}

    pub fn parse_trade(&mut self, mut trade: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        //     {
        //         "type": "match",
        //         "trade_id": 82047307,
        //         "maker_order_id": "0f358725-2134-435e-be11-753912a326e0",
        //         "taker_order_id": "252b7002-87a3-425c-ac73-f5b9e23f3caf",
        //         "order_id": "d50ec984-77a8-460a-b958-66f114b0de9b",
        //         "side": "sell",
        //         "size": "0.00513192",
        //         "price": "9314.78",
        //         "product_id": "BTC-USD",
        //         "profile_id": "6244401d-c078-40d9-b305-7ad3551bc3b0",
        //         "sequence": 12038915443,
        //         "time": "2020-01-31T20:03:41.158814Z"
        //         "created_at": "2014-11-07T22:19:28.578544Z",
        //         "liquidity": "T",
        //         "fee": "0.00025",
        //         "settled": true,
        //         "usd_volume": "0.0924556000000000",
        //         "user_id": "595eb864313c2b02ddf2937d"
        //     }
        //
        let mut timestamp: Value = self.parse8601(self.safe_string2(trade.clone(), Value::Str("time".to_string()), Value::Str("created_at".to_string()), &[]));
        let mut marketId: Value = self.safe_string_k(trade.clone(), "product_id", &[]);
        market = self.safe_market(&[marketId.clone(), market.clone(), Value::Str("-".to_string())]);
        let mut feeRate: Value = Value::Null;
        let mut takerOrMaker: Value = Value::Null;
        let mut cost: Value = Value::Null;
        let mut feeCurrencyId: Value = self.safe_string_lower(market.clone(), Value::Str("quoteId".to_string()), &[]);
        if !is_equal(&feeCurrencyId, &Value::Null) {
            let mut costField: Value = add(&feeCurrencyId, &Value::Str("_value".to_string()));
            cost = self.safe_string(trade.clone(), costField.clone(), &[]);
            let mut liquidity: Value = self.safe_string_k(trade.clone(), "liquidity", &[]);
            if !is_equal(&liquidity, &Value::Null) {
                takerOrMaker = ternary(is_true(&(is_equal(&liquidity, &Value::Str("T".to_string())))), Value::Str("taker".to_string()), Value::Str("maker".to_string()));
                feeRate = self.safe_string(market.clone(), takerOrMaker.clone(), &[]);
            }
        }
        let mut feeCost: Value = self.safe_string2(trade.clone(), Value::Str("fill_fees".to_string()), Value::Str("fee".to_string()), &[]);
        let mut fee: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("cost".to_string(), feeCost.clone());
                m.insert("currency".to_string(), get_value(&market, &Value::Str("quote".to_string())));
                m.insert("rate".to_string(), feeRate.clone());
            m
        });
        let mut id: Value = self.safe_string_k(trade.clone(), "trade_id", &[]);
        let mut side: Value = ternary(is_true(&(is_equal(&get_value(&trade, &Value::Str("side".to_string())), &Value::Str("buy".to_string())))), Value::Str("sell".to_string()), Value::Str("buy".to_string()));
        let mut orderId: Value = self.safe_string_k(trade.clone(), "order_id", &[]);
        // Coinbase Pro returns inverted side to fetchMyTrades vs fetchTrades
        let mut makerOrderId: Value = self.safe_string_k(trade.clone(), "maker_order_id", &[]);
        let mut takerOrderId: Value = self.safe_string_k(trade.clone(), "taker_order_id", &[]);
        if is_true(&(!is_equal(&orderId, &Value::Null))) || is_true(&(is_true(&(!is_equal(&makerOrderId, &Value::Null))) && is_true(&(!is_equal(&takerOrderId, &Value::Null))))) {
            side = ternary(is_true(&(is_equal(&get_value(&trade, &Value::Str("side".to_string())), &Value::Str("buy".to_string())))), Value::Str("buy".to_string()), Value::Str("sell".to_string()));
        }
        let mut price: Value = self.safe_string_k(trade.clone(), "price", &[]);
        let mut amount: Value = self.safe_string_k(trade.clone(), "size", &[]);
        let mut symbol: Value = get_value(&market, &Value::Str("symbol".to_string()));
        return self.safe_trade(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), id.clone());
        m.insert("order".to_string(), orderId.clone());
        m.insert("info".to_string(), trade.clone());
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp.clone()));
        m.insert("symbol".to_string(), symbol.clone());
        m.insert("type".to_string(), Value::Null);
        m.insert("takerOrMaker".to_string(), takerOrMaker.clone());
        m.insert("side".to_string(), side.clone());
        m.insert("price".to_string(), price.clone());
        m.insert("amount".to_string(), amount.clone());
        m.insert("fee".to_string(), fee.clone());
        m.insert("cost".to_string(), cost.clone());
    m
}), &[market.clone()]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchMyTrades
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getfills
 * @description fetch all trades made by the user
 * @param {string} symbol unified market symbol
 * @param {int} [since] the earliest time in ms to fetch trades for
 * @param {int} [limit] the maximum number of trades structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] the latest time in ms to fetch trades for
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [availble parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns {Trade[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=trade-structure}
 */
    pub async fn fetch_my_trades(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        if is_equal(&symbol, &Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(add(&self.id, &Value::Str(" fetchMyTrades() requires a symbol argument".to_string()))));
        }
        let mut paginate: Value = Value::Bool(false);
        { let __destr_tmp = self.handle_option_and_params(params.clone(), Value::Str("fetchMyTrades".to_string()), Value::Str("paginate".to_string()), &[]); paginate = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        if is_true(&paginate) {
            return self.fetch_paginated_call_dynamic(Value::Str("fetchMyTrades".to_string()), &[symbol.clone(), since.clone(), limit.clone(), params.clone(), Value::Int(100)]).await;
        }
        self.load_markets(&[]).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("product_id".to_string(), get_value(&market, &Value::Str("id".to_string())));
            m
        });
        if !is_equal(&limit, &Value::Null) {
            add_element_to_object(&mut request, &Value::Str("limit".to_string()), limit.clone());
        }
        if !is_equal(&since, &Value::Null) {
            add_element_to_object(&mut request, &Value::Str("start_date".to_string()), self.iso8601(since.clone()));
        }
        let mut until: Value = self.safe_value2(params.clone(), Value::Str("until".to_string()), Value::Str("end_date".to_string()), &[]);
        if !is_equal(&until, &Value::Null) {
            params = self.omit(params.clone(), Value::List(vec![Value::Str("until".to_string())]), &[]);
            add_element_to_object(&mut request, &Value::Str("end_date".to_string()), self.iso8601(until.clone()));
        }
        let __ws_arg_8 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.private_get_fills(&[__ws_arg_8]).await;
        return self.parse_trades(response.clone(), &[market.clone(), since.clone(), limit.clone()]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchTrades
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getproducttrades
 * @description get the list of most recent trades for a particular symbol
 * @param {string} symbol unified symbol of the market to fetch trades for
 * @param {int} [since] timestamp in ms of the earliest trade to fetch
 * @param {int} [limit] the maximum amount of trades to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {Trade[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=public-trades}
 */
    pub async fn fetch_trades(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut since = get_arg(optional_args, 0, Value::Null);
        let mut limit = get_arg(optional_args, 1, Value::Null);
        let mut params = get_arg(optional_args, 2, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("id".to_string(), get_value(&market, &Value::Str("id".to_string())));
            m
        });
        if !is_equal(&limit, &Value::Null) {
            add_element_to_object(&mut request, &Value::Str("limit".to_string()), limit.clone()); // default 100
        }
        let __ws_arg_9 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.public_get_products_id_trades(&[__ws_arg_9]).await;
        return self.parse_trades(response.clone(), &[market.clone(), since.clone(), limit.clone()]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchTradingFees
 * @description fetch the trading fees for multiple markets
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getfees
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a dictionary of [fee structures]{@link https://docs.ccxt.com/?id=fee-structure} indexed by market symbols
 */
    pub async fn fetch_trading_fees(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        let mut response: Value = self.private_get_fees(&[params.clone()]).await;
        //
        //    {
        //        "maker_fee_rate": "0.0050",
        //        "taker_fee_rate": "0.0050",
        //        "usd_volume": "43806.92"
        //    }
        //
        let mut maker: Value = self.safe_number_k(response.clone(), "maker_fee_rate", &[]);
        let mut taker: Value = self.safe_number_k(response.clone(), "taker_fee_rate", &[]);
        let mut result: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_500: bool = true;
            while { if !__for_first_500 { i = add(&i, &Value::Int(1)); } __for_first_500 = false; is_less_than(&i, &get_array_length(&self.symbols)) } {
            let mut symbol: Value = get_value(&self.symbols, &i);
            add_element_to_object(&mut result, &symbol, Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), response.clone());
        m.insert("symbol".to_string(), symbol.clone());
        m.insert("maker".to_string(), maker.clone());
        m.insert("taker".to_string(), taker.clone());
        m.insert("percentage".to_string(), Value::Bool(true));
        m.insert("tierBased".to_string(), Value::Bool(true));
    m
}));
        }
        }
        return result;

    Value::Null
}

    pub fn parse_ohlcv(&self, mut ohlcv: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        return Value::List(vec![self.safe_timestamp(ohlcv.clone(), Value::Int(0), &[]), self.safe_number(ohlcv.clone(), Value::Int(3), &[]), self.safe_number(ohlcv.clone(), Value::Int(2), &[]), self.safe_number(ohlcv.clone(), Value::Int(1), &[]), self.safe_number(ohlcv.clone(), Value::Int(4), &[]), self.safe_number(ohlcv.clone(), Value::Int(5), &[])]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchOHLCV
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getproductcandles
 * @description fetches historical candlestick data containing the open, high, low, and close price, and the volume of a market
 * @param {string} symbol unified symbol of the market to fetch OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum amount of candles to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] the latest time in ms to fetch trades for
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [availble parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 */
    pub async fn fetch_ohlcv(&mut self, mut symbol: Value, optional_args: &[Value]) -> Value {
        let mut timeframe = get_arg(optional_args, 0, Value::Str("1m".to_string()));
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        let mut paginate: Value = Value::Bool(false);
        { let __destr_tmp = self.handle_option_and_params(params.clone(), Value::Str("fetchOHLCV".to_string()), Value::Str("paginate".to_string()), &[Value::Bool(false)]); paginate = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        if is_true(&paginate) {
            return self.fetch_paginated_call_deterministic(Value::Str("fetchOHLCV".to_string()), &[symbol.clone(), since.clone(), limit.clone(), timeframe.clone(), params.clone(), Value::Int(300)]).await;
        }
        let mut market: Value = self.market(symbol.clone());
        let mut parsedTimeframe: Value = self.safe_integer(self.timeframes.clone(), timeframe.clone(), &[]);
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("id".to_string(), get_value(&market, &Value::Str("id".to_string())));
            m
        });
        if !is_equal(&parsedTimeframe, &Value::Null) {
            add_element_to_object(&mut request, &Value::Str("granularity".to_string()), parsedTimeframe.clone());
        }  else {
            add_element_to_object(&mut request, &Value::Str("granularity".to_string()), timeframe.clone());
        }
        let mut until: Value = self.safe_value2(params.clone(), Value::Str("until".to_string()), Value::Str("end".to_string()), &[]);
        params = self.omit(params.clone(), Value::List(vec![Value::Str("until".to_string())]), &[]);
        if !is_equal(&since, &Value::Null) {
            add_element_to_object(&mut request, &Value::Str("start".to_string()), self.iso8601(since.clone()));
            if is_equal(&limit, &Value::Null) {
                // https://docs.pro.coinbase.com/#get-historic-rates
                limit = Value::Int(300); // max = 300
            }  else {
                limit = crate::runtime::Math::min(&Value::Int(300), &limit);
            }
            if is_equal(&until, &Value::Null) {
                let mut parsedTimeframeMilliseconds: Value = multiply(&parsedTimeframe, &Value::Int(1000));
                if is_true(&self.is_round_number(mod_val(&since, &parsedTimeframeMilliseconds))) {
                    add_element_to_object(&mut request, &Value::Str("end".to_string()), self.iso8601(self.sum(&[multiply(&(subtract(&limit, &Value::Int(1))), &parsedTimeframeMilliseconds), since.clone()])));
                }  else {
                    add_element_to_object(&mut request, &Value::Str("end".to_string()), self.iso8601(self.sum(&[multiply(&limit, &parsedTimeframeMilliseconds), since.clone()])));
                }
            }  else {
                add_element_to_object(&mut request, &Value::Str("end".to_string()), self.iso8601(until.clone()));
            }
        }
        let __ws_arg_10 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.public_get_products_id_candles(&[__ws_arg_10]).await;
        return self.parse_ohlc_vs(response.clone(), &[market.clone(), timeframe.clone(), since.clone(), limit.clone()]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchTime
 * @description fetches the current integer timestamp in milliseconds from the exchange server
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int} the current integer timestamp in milliseconds from the exchange server
 */
    pub async fn fetch_time(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut response: Value = self.public_get_time(&[params.clone()]).await;
        return self.safe_timestamp(response.clone(), Value::Str("epoch".to_string()), &[]);

    Value::Null
}

    pub fn parse_order_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("pending".to_string(), Value::Str("open".to_string()));
                m.insert("active".to_string(), Value::Str("open".to_string()));
                m.insert("open".to_string(), Value::Str("open".to_string()));
                m.insert("done".to_string(), Value::Str("closed".to_string()));
                m.insert("canceled".to_string(), Value::Str("canceled".to_string()));
                m.insert("canceling".to_string(), Value::Str("open".to_string()));
            m
        });
        return self.safe_string(statuses.clone(), status.clone(), &[status.clone()]);

    Value::Null
}

    pub fn parse_order(&mut self, mut order: Value, optional_args: &[Value]) -> Value {
        let mut market = get_arg(optional_args, 0, Value::Null);
        //
        // createOrder
        //
        //     {
        //         "id": "d0c5340b-6d6c-49d9-b567-48c4bfca13d2",
        //         "price": "0.10000000",
        //         "size": "0.01000000",
        //         "product_id": "BTC-USD",
        //         "side": "buy",
        //         "stp": "dc",
        //         "type": "limit",
        //         "time_in_force": "GTC",
        //         "post_only": false,
        //         "created_at": "2016-12-08T20:02:28.53864Z",
        //         "fill_fees": "0.0000000000000000",
        //         "filled_size": "0.00000000",
        //         "executed_value": "0.0000000000000000",
        //         "status": "pending",
        //         "settled": false
        //     }
        //
        let mut timestamp: Value = self.parse8601(self.safe_string_k(order.clone(), "created_at", &[]));
        let mut marketId: Value = self.safe_string_k(order.clone(), "product_id", &[]);
        market = self.safe_market(&[marketId.clone(), market.clone(), Value::Str("-".to_string())]);
        let mut status: Value = self.parse_order_status(self.safe_string_k(order.clone(), "status", &[]));
        let mut doneReason: Value = self.safe_string_k(order.clone(), "done_reason", &[]);
        if is_true(&(is_equal(&status, &Value::Str("closed".to_string())))) && is_true(&(is_equal(&doneReason, &Value::Str("canceled".to_string())))) {
            status = Value::Str("canceled".to_string());
        }
        let mut price: Value = self.safe_string_k(order.clone(), "price", &[]);
        let mut filled: Value = self.safe_string_k(order.clone(), "filled_size", &[]);
        let mut amount: Value = self.safe_string_k(order.clone(), "size", &[filled.clone()]);
        let mut cost: Value = self.safe_string_k(order.clone(), "executed_value", &[]);
        let mut feeCost: Value = self.safe_number_k(order.clone(), "fill_fees", &[]);
        let mut fee: Value = Value::Null;
        if !is_equal(&feeCost, &Value::Null) {
            fee = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("cost".to_string(), feeCost.clone());
                    m.insert("currency".to_string(), get_value(&market, &Value::Str("quote".to_string())));
                    m.insert("rate".to_string(), Value::Null);
                m
            });
        }
        let mut id: Value = self.safe_string_k(order.clone(), "id", &[]);
        let mut type_var: Value = self.safe_string_k(order.clone(), "type", &[]);
        let mut side: Value = self.safe_string_k(order.clone(), "side", &[]);
        let mut timeInForce: Value = self.safe_string_k(order.clone(), "time_in_force", &[]);
        let mut postOnly: Value = self.safe_value_k(order.clone(), "post_only", &[]);
        let mut triggerPrice: Value = self.safe_number_k(order.clone(), "stop_price", &[]);
        let mut clientOrderId: Value = self.safe_string_k(order.clone(), "client_oid", &[]);
        return self.safe_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("id".to_string(), id.clone());
        m.insert("clientOrderId".to_string(), clientOrderId.clone());
        m.insert("info".to_string(), order.clone());
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp.clone()));
        m.insert("lastTradeTimestamp".to_string(), Value::Null);
        m.insert("status".to_string(), status.clone());
        m.insert("symbol".to_string(), get_value(&market, &Value::Str("symbol".to_string())));
        m.insert("type".to_string(), type_var.clone());
        m.insert("timeInForce".to_string(), timeInForce.clone());
        m.insert("postOnly".to_string(), postOnly.clone());
        m.insert("side".to_string(), side.clone());
        m.insert("price".to_string(), price.clone());
        m.insert("triggerPrice".to_string(), triggerPrice.clone());
        m.insert("cost".to_string(), cost.clone());
        m.insert("amount".to_string(), amount.clone());
        m.insert("filled".to_string(), filled.clone());
        m.insert("remaining".to_string(), Value::Null);
        m.insert("fee".to_string(), fee.clone());
        m.insert("average".to_string(), Value::Null);
        m.insert("trades".to_string(), Value::Null);
    m
}), &[market.clone()]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchOrder
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getorder
 * @description fetches information on an order made by the user
 * @param {string} id the order id
 * @param {string} symbol not used by coinbaseexchange fetchOrder
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} An [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn fetch_order(&mut self, mut id: Value, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut clientOrderId: Value = self.safe_string2(params.clone(), Value::Str("clientOrderId".to_string()), Value::Str("client_oid".to_string()), &[]);
        let mut method: Value = Value::Null;
        if is_equal(&clientOrderId, &Value::Null) {
            method = Value::Str("privateGetOrdersId".to_string());
            add_element_to_object(&mut request, &Value::Str("id".to_string()), id.clone());
        }  else {
            method = Value::Str("privateGetOrdersClientClientOid".to_string());
            add_element_to_object(&mut request, &Value::Str("client_oid".to_string()), clientOrderId.clone());
            params = self.omit(params.clone(), Value::List(vec![Value::Str("clientOrderId".to_string()), Value::Str("client_oid".to_string())]), &[]);
        }
        let __ws_arg_11 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.call_method(method.clone(), &[__ws_arg_11]).await;
        return self.parse_order(response.clone(), &[]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchOrderTrades
 * @description fetch all the trades made from a single order
 * @param {string} id order id
 * @param {string} symbol unified market symbol
 * @param {int} [since] the earliest time in ms to fetch trades for
 * @param {int} [limit] the maximum number of trades to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=trade-structure}
 */
    pub async fn fetch_order_trades(&mut self, mut id: Value, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        let mut market: Value = Value::Null;
        if !is_equal(&symbol, &Value::Null) {
            market = self.market(symbol.clone());
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("order_id".to_string(), id.clone());
            m
        });
        let __ws_arg_12 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.private_get_fills(&[__ws_arg_12]).await;
        return self.parse_trades(response.clone(), &[market.clone(), since.clone(), limit.clone()]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchOrders
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getorders
 * @description fetches information on multiple orders made by the user
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] the latest time in ms to fetch open orders for
 * @returns {Order[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn fetch_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("status".to_string(), Value::Str("all".to_string()));
            m
        });
        let __ws_arg_13 = self.extend(request.clone(), &[params.clone()]);
        return self.fetch_open_orders(&[symbol.clone(), since.clone(), limit.clone(), __ws_arg_13]).await;

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchOpenOrders
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getorders
 * @description fetch all unfilled currently open orders
 * @param {string} symbol unified market symbol
 * @param {int} [since] the earliest time in ms to fetch open orders for
 * @param {int} [limit] the maximum number of  open orders structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] the latest time in ms to fetch open orders for
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [availble parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns {Order[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn fetch_open_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        let mut paginate: Value = Value::Bool(false);
        { let __destr_tmp = self.handle_option_and_params(params.clone(), Value::Str("fetchOpenOrders".to_string()), Value::Str("paginate".to_string()), &[]); paginate = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        if is_true(&paginate) {
            return self.fetch_paginated_call_dynamic(Value::Str("fetchOpenOrders".to_string()), &[symbol.clone(), since.clone(), limit.clone(), params.clone(), Value::Int(100)]).await;
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut market: Value = Value::Null;
        if !is_equal(&symbol, &Value::Null) {
            market = self.market(symbol.clone());
            add_element_to_object(&mut request, &Value::Str("product_id".to_string()), get_value(&market, &Value::Str("id".to_string())));
        }
        if !is_equal(&limit, &Value::Null) {
            add_element_to_object(&mut request, &Value::Str("limit".to_string()), limit.clone()); // default 100
        }
        if !is_equal(&since, &Value::Null) {
            add_element_to_object(&mut request, &Value::Str("start_date".to_string()), self.iso8601(since.clone()));
        }
        let mut until: Value = self.safe_value2(params.clone(), Value::Str("until".to_string()), Value::Str("end_date".to_string()), &[]);
        if !is_equal(&until, &Value::Null) {
            params = self.omit(params.clone(), Value::List(vec![Value::Str("until".to_string())]), &[]);
            add_element_to_object(&mut request, &Value::Str("end_date".to_string()), self.iso8601(until.clone()));
        }
        let __ws_arg_14 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.private_get_orders(&[__ws_arg_14]).await;
        return self.parse_orders(response.clone(), &[market.clone(), since.clone(), limit.clone()]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchClosedOrders
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getorders
 * @description fetches information on multiple closed orders made by the user
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] the latest time in ms to fetch open orders for
 * @returns {Order[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn fetch_closed_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("status".to_string(), Value::Str("done".to_string()));
            m
        });
        let __ws_arg_15 = self.extend(request.clone(), &[params.clone()]);
        return self.fetch_open_orders(&[symbol.clone(), since.clone(), limit.clone(), __ws_arg_15]).await;

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#createOrder
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_postorders
 * @description create a trade order
 * @param {string} symbol unified symbol of the market to create an order in
 * @param {string} type 'market' or 'limit'
 * @param {string} side 'buy' or 'sell'
 * @param {float} amount how much of currency you want to trade in units of base currency
 * @param {float} [price] the price at which the order is to be fulfilled, in units of the quote currency, ignored in market orders
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn create_order(&mut self, mut symbol: Value, mut type_var: Value, mut side: Value, mut amount: Value, optional_args: &[Value]) -> Value {
        let mut price = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("type".to_string(), type_var.clone());
                m.insert("side".to_string(), side.clone());
                m.insert("product_id".to_string(), get_value(&market, &Value::Str("id".to_string())));
            m
        });
        let mut clientOrderId: Value = self.safe_string2(params.clone(), Value::Str("clientOrderId".to_string()), Value::Str("client_oid".to_string()), &[]);
        if !is_equal(&clientOrderId, &Value::Null) {
            add_element_to_object(&mut request, &Value::Str("client_oid".to_string()), clientOrderId.clone());
        }
        let mut triggerPrice: Value = self.safe_number_n(params.clone(), Value::List(vec![Value::Str("stopPrice".to_string()), Value::Str("stop_price".to_string()), Value::Str("triggerPrice".to_string())]), &[]);
        if !is_equal(&triggerPrice, &Value::Null) {
            add_element_to_object(&mut request, &Value::Str("stop_price".to_string()), self.price_to_precision(symbol.clone(), triggerPrice.clone()));
        }
        let mut timeInForce: Value = self.safe_string2(params.clone(), Value::Str("timeInForce".to_string()), Value::Str("time_in_force".to_string()), &[]);
        if !is_equal(&timeInForce, &Value::Null) {
            add_element_to_object(&mut request, &Value::Str("time_in_force".to_string()), timeInForce.clone());
        }
        let mut postOnly: Value = self.safe_value2(params.clone(), Value::Str("postOnly".to_string()), Value::Str("post_only".to_string()), &[Value::Bool(false)]);
        if is_true(&postOnly) {
            add_element_to_object(&mut request, &Value::Str("post_only".to_string()), Value::Bool(true));
        }
        params = self.omit(params.clone(), Value::List(vec![Value::Str("timeInForce".to_string()), Value::Str("time_in_force".to_string()), Value::Str("stopPrice".to_string()), Value::Str("stop_price".to_string()), Value::Str("clientOrderId".to_string()), Value::Str("client_oid".to_string()), Value::Str("postOnly".to_string()), Value::Str("post_only".to_string()), Value::Str("triggerPrice".to_string())]), &[]);
        if is_equal(&type_var, &Value::Str("limit".to_string())) {
            add_element_to_object(&mut request, &Value::Str("price".to_string()), self.price_to_precision(symbol.clone(), price.clone()));
            add_element_to_object(&mut request, &Value::Str("size".to_string()), self.amount_to_precision(symbol.clone(), amount.clone()));
        }  else if is_equal(&type_var, &Value::Str("market".to_string())) {
            let mut cost: Value = self.safe_number2(params.clone(), Value::Str("cost".to_string()), Value::Str("funds".to_string()), &[]);
            if is_equal(&cost, &Value::Null) {
                if !is_equal(&price, &Value::Null) {
                    cost = multiply(&amount, &price);
                }
            }  else {
                params = self.omit(params.clone(), Value::List(vec![Value::Str("cost".to_string()), Value::Str("funds".to_string())]), &[]);
            }
            if !is_equal(&cost, &Value::Null) {
                add_element_to_object(&mut request, &Value::Str("funds".to_string()), self.cost_to_precision(symbol.clone(), cost.clone()));
            }  else {
                add_element_to_object(&mut request, &Value::Str("size".to_string()), self.amount_to_precision(symbol.clone(), amount.clone()));
            }
        }
        let __ws_arg_16 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.private_post_orders(&[__ws_arg_16]).await;
        return self.parse_order(response.clone(), &[market.clone()]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#cancelOrder
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_deleteorder
 * @description cancels an open order
 * @param {string} id order id
 * @param {string} symbol unified symbol of the market the order was made in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} An [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn cancel_order(&mut self, mut id: Value, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut clientOrderId: Value = self.safe_string2(params.clone(), Value::Str("clientOrderId".to_string()), Value::Str("client_oid".to_string()), &[]);
        let mut method: Value = Value::Null;
        if is_equal(&clientOrderId, &Value::Null) {
            method = Value::Str("privateDeleteOrdersId".to_string());
            add_element_to_object(&mut request, &Value::Str("id".to_string()), id.clone());
        }  else {
            method = Value::Str("privateDeleteOrdersClientClientOid".to_string());
            add_element_to_object(&mut request, &Value::Str("client_oid".to_string()), clientOrderId.clone());
            params = self.omit(params.clone(), Value::List(vec![Value::Str("clientOrderId".to_string()), Value::Str("client_oid".to_string())]), &[]);
        }
        let mut market: Value = Value::Null;
        if !is_equal(&symbol, &Value::Null) {
            market = self.market(symbol.clone());
            add_element_to_object(&mut request, &Value::Str("product_id".to_string()), get_value(&market, &Value::Str("symbol".to_string()))); // the request will be more performant if you include it
        }
        let __ws_arg_17 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.call_method(method.clone(), &[__ws_arg_17]).await;
        return self.safe_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), response.clone());
    m
}), &[]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#cancelAllOrders
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_deleteorders
 * @description cancel all open orders
 * @param {string} symbol unified market symbol, only orders in the market of this symbol are cancelled when symbol is not undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
    pub async fn cancel_all_orders(&mut self, optional_args: &[Value]) -> Value {
        let mut symbol = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        let mut market: Value = Value::Null;
        if !is_equal(&symbol, &Value::Null) {
            market = self.market(symbol.clone());
            add_element_to_object(&mut request, &Value::Str("product_id".to_string()), get_value(&market, &Value::Str("symbol".to_string()))); // the request will be more performant if you include it
        }
        let __ws_arg_18 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.private_delete_orders(&[__ws_arg_18]).await;
        return Value::List(vec![self.safe_order(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), response.clone());
    m
}), &[])]);

    Value::Null
}

    pub async fn fetch_payment_methods(&mut self, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        return self.private_get_payment_methods(&[params.clone()]).await;

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#withdraw
 * @description make a withdrawal
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_postwithdrawpaymentmethod
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_postwithdrawcoinbaseaccount
 * @param {string} code unified currency code
 * @param {float} amount the amount to withdraw
 * @param {string} address the address to withdraw to
 * @param {string} tag
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [transaction structure]{@link https://docs.ccxt.com/?id=transaction-structure}
 */
    pub async fn withdraw(&mut self, mut code: Value, mut amount: Value, mut address: Value, optional_args: &[Value]) -> Value {
        let mut tag = get_arg(optional_args, 0, Value::Null);
        let mut params = get_arg(optional_args, 1, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        { let __destr_tmp = self.handle_withdraw_tag_and_params(tag.clone(), params.clone()); tag = get_value(&__destr_tmp, &Value::Int(0)); params = get_value(&__destr_tmp, &Value::Int(1)); }
        self.check_address(&[address.clone()]);
        self.load_markets(&[]).await;
        let mut currency: Value = self.currency(code.clone());
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("currency".to_string(), get_value(&currency, &Value::Str("id".to_string())));
                m.insert("amount".to_string(), amount.clone());
            m
        });
        let mut method: Value = Value::Str("privatePostWithdrawals".to_string());
        if is_true(&Value::Bool(in_op(&params, &Value::Str("payment_method_id".to_string())))) {
            method = add(&method, &Value::Str("PaymentMethod".to_string()));
        }  else if is_true(&Value::Bool(in_op(&params, &Value::Str("coinbase_account_id".to_string())))) {
            method = add(&method, &Value::Str("CoinbaseAccount".to_string()));
        }  else {
            method = add(&method, &Value::Str("Crypto".to_string()));
            add_element_to_object(&mut request, &Value::Str("crypto_address".to_string()), address.clone());
            if !is_equal(&tag, &Value::Null) {
                add_element_to_object(&mut request, &Value::Str("destination_tag".to_string()), tag.clone());
            }
        }
        let __ws_arg_19 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.call_method(method.clone(), &[__ws_arg_19]).await;
        if !is_true(&response) {
            panic!("{}", crate::exchange_errors::exchange_error(add(&add(&self.id, &Value::Str(" withdraw() error: ".to_string())), &self.json(response.clone()))));
        }
        return self.parse_transaction(response.clone(), &[currency.clone()]);

    Value::Null
}

    pub fn parse_ledger_entry_type(&self, mut type_var: Value) -> Value {
        let mut types: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("transfer".to_string(), Value::Str("transfer".to_string()));
                m.insert("match".to_string(), Value::Str("trade".to_string()));
                m.insert("fee".to_string(), Value::Str("fee".to_string()));
                m.insert("rebate".to_string(), Value::Str("rebate".to_string()));
                m.insert("conversion".to_string(), Value::Str("trade".to_string()));
            m
        });
        return self.safe_string(types.clone(), type_var.clone(), &[type_var.clone()]);

    Value::Null
}

    pub fn parse_ledger_entry(&self, mut item: Value, optional_args: &[Value]) -> Value {
        let mut currency = get_arg(optional_args, 0, Value::Null);
        //  {
        //      "id": "12087495079",
        //      "amount": "-0.0100000000000000",
        //      "balance": "0.0645419900000000",
        //      "created_at": "2021-10-28T17:14:32.593168Z",
        //      "type": "transfer",
        //      "details": {
        //          "from": "2f74edf7-1440-4586-86dc-ae58c5693691",
        //          "profile_transfer_id": "3ef093ad-2482-40d1-8ede-2f89cff5099e",
        //          "to": "dda99503-4980-4b60-9549-0b770ee51336"
        //      }
        //  },
        //  {
        //     "id": "11740725774",
        //     "amount": "-1.7565669701255000",
        //     "balance": "0.0016490047745000",
        //     "created_at": "2021-10-22T03:47:34.764122Z",
        //     "type": "fee",
        //     "details": {
        //         "order_id": "ad06abf4-95ab-432a-a1d8-059ef572e296",
        //         "product_id": "ETH-DAI",
        //         "trade_id": "1740617"
        //     }
        //  }
        let mut id: Value = self.safe_string_k(item.clone(), "id", &[]);
        let mut amountString: Value = self.safe_string_k(item.clone(), "amount", &[]);
        let mut direction: Value = Value::Null;
        let mut afterString: Value = self.safe_string_k(item.clone(), "balance", &[]);
        let mut beforeString: Value = crate::precise::Precise::stringSub(&afterString, &amountString);
        if is_true(&crate::precise::Precise::stringLt(&amountString, &Value::Str("0".to_string()))) {
            direction = Value::Str("out".to_string());
            amountString = crate::precise::Precise::stringAbs(&amountString);
        }  else {
            direction = Value::Str("in".to_string());
        }
        let mut amount: Value = self.parse_number(amountString.clone(), &[]);
        let mut after: Value = self.parse_number(afterString.clone(), &[]);
        let mut before: Value = self.parse_number(beforeString.clone(), &[]);
        let mut timestamp: Value = self.parse8601(self.safe_value_k(item.clone(), "created_at", &[]));
        let mut type_var: Value = self.parse_ledger_entry_type(self.safe_string_k(item.clone(), "type", &[]));
        let mut code: Value = self.safe_currency_code(Value::Null, &[currency.clone()]);
        let mut details: Value = self.safe_value_k(item.clone(), "details", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut account: Value = Value::Null;
        let mut referenceAccount: Value = Value::Null;
        let mut referenceId: Value = Value::Null;
        if is_equal(&type_var, &Value::Str("transfer".to_string())) {
            account = self.safe_string_k(details.clone(), "from", &[]);
            referenceAccount = self.safe_string_k(details.clone(), "to", &[]);
            referenceId = self.safe_string_k(details.clone(), "profile_transfer_id", &[]);
        }  else {
            referenceId = self.safe_string_k(details.clone(), "order_id", &[]);
        }
        let mut status: Value = Value::Str("ok".to_string());
        return self.safe_ledger_entry(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), item.clone());
        m.insert("id".to_string(), id.clone());
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp.clone()));
        m.insert("direction".to_string(), direction.clone());
        m.insert("account".to_string(), account.clone());
        m.insert("referenceAccount".to_string(), referenceAccount.clone());
        m.insert("referenceId".to_string(), referenceId.clone());
        m.insert("type".to_string(), type_var.clone());
        m.insert("currency".to_string(), code.clone());
        m.insert("amount".to_string(), amount.clone());
        m.insert("before".to_string(), before.clone());
        m.insert("after".to_string(), after.clone());
        m.insert("status".to_string(), status.clone());
        m.insert("fee".to_string(), Value::Null);
    m
}), &[currency.clone()]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchLedger
 * @description fetch the history of changes, actions done by the user or operations that altered the balance of the user
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getaccountledger
 * @param {string} code unified currency code, default is undefined
 * @param {int} [since] timestamp in ms of the earliest ledger entry, default is undefined
 * @param {int} [limit] max number of ledger entries to return, default is undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] the latest time in ms to fetch trades for
 * @returns {object} a [ledger structure]{@link https://docs.ccxt.com/?id=ledger-entry-structure}
 */
    pub async fn fetch_ledger(&mut self, optional_args: &[Value]) -> Value {
        let mut code = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        // https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getaccountledger
        if is_equal(&code, &Value::Null) {
            panic!("{}", crate::exchange_errors::arguments_required(add(&self.id, &Value::Str(" fetchLedger() requires a code param".to_string()))));
        }
        self.load_markets(&[]).await;
        self.load_accounts(&[]).await;
        let mut currency: Value = self.currency(code.clone());
        let mut accountsByCurrencyCode: Value = self.index_by(self.accounts.clone(), Value::Str("code".to_string()));
        let mut account: Value = self.safe_value(accountsByCurrencyCode.clone(), code.clone(), &[]);
        if is_equal(&account, &Value::Null) {
            panic!("{}", crate::exchange_errors::exchange_error(add(&add(&self.id, &Value::Str(" fetchLedger() could not find account id for ".to_string())), &code)));
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("id".to_string(), get_value(&account, &Value::Str("id".to_string())));
            m
        });
        if !is_equal(&since, &Value::Null) {
            add_element_to_object(&mut request, &Value::Str("start_date".to_string()), self.iso8601(since.clone()));
        }
        if !is_equal(&limit, &Value::Null) {
            add_element_to_object(&mut request, &Value::Str("limit".to_string()), limit.clone()); // default 100
        }
        let mut until: Value = self.safe_value2(params.clone(), Value::Str("until".to_string()), Value::Str("end_date".to_string()), &[]);
        if !is_equal(&until, &Value::Null) {
            params = self.omit(params.clone(), Value::List(vec![Value::Str("until".to_string())]), &[]);
            add_element_to_object(&mut request, &Value::Str("end_date".to_string()), self.iso8601(until.clone()));
        }
        let __ws_arg_20 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.private_get_accounts_id_ledger(&[__ws_arg_20]).await;
        {
                        let mut i: Value = Value::Int(0);
            let mut __for_first_501: bool = true;
            while { if !__for_first_501 { i = add(&i, &Value::Int(1)); } __for_first_501 = false; is_less_than(&i, &get_array_length(&response)) } {
            add_element_to_object(get_value_mut(&mut response, &i), &Value::Str("currency".to_string()), code.clone());
        }
        }
        return self.parse_ledger(response.clone(), &[currency.clone(), since.clone(), limit.clone()]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchDepositsWithdrawals
 * @description fetch history of deposits and withdrawals
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_gettransfers
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getaccounttransfers
 * @param {string} [code] unified currency code for the currency of the deposit/withdrawals, default is undefined
 * @param {int} [since] timestamp in ms of the earliest deposit/withdrawal, default is undefined
 * @param {int} [limit] max number of deposit/withdrawals to return, default is undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.id] account id, when defined, the endpoint used is '/accounts/{account_id}/transfers/' instead of '/transfers/'
 * @returns {object} a list of [transaction structure]{@link https://docs.ccxt.com/?id=transaction-structure}
 */
    pub async fn fetch_deposits_withdrawals(&mut self, optional_args: &[Value]) -> Value {
        let mut code = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        self.load_accounts(&[]).await;
        let mut currency: Value = Value::Null;
        let mut id: Value = self.safe_string_k(params.clone(), "id", &[]); // account id
        if is_equal(&id, &Value::Null) {
            if !is_equal(&code, &Value::Null) {
                currency = self.currency(code.clone());
                let mut accountsByCurrencyCode: Value = self.index_by(self.accounts.clone(), Value::Str("code".to_string()));
                let mut account: Value = self.safe_value(accountsByCurrencyCode.clone(), code.clone(), &[]);
                if is_equal(&account, &Value::Null) {
                    panic!("{}", crate::exchange_errors::exchange_error(add(&add(&self.id, &Value::Str(" fetchDepositsWithdrawals() could not find account id for ".to_string())), &code)));
                }
                id = get_value(&account, &Value::Str("id".to_string()));
            }
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        });
        if !is_equal(&id, &Value::Null) {
            add_element_to_object(&mut request, &Value::Str("id".to_string()), id.clone());
        }
        if !is_equal(&limit, &Value::Null) {
            add_element_to_object(&mut request, &Value::Str("limit".to_string()), limit.clone());
        }
        let mut response: Value = Value::Null;
        if is_equal(&id, &Value::Null) {
            let __ws_arg_21 = self.extend(request.clone(), &[params.clone()]);
            response = self.private_get_transfers(&[__ws_arg_21]).await;
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_502: bool = true;
                while { if !__for_first_502 { i = add(&i, &Value::Int(1)); } __for_first_502 = false; is_less_than(&i, &get_array_length(&response)) } {
                let mut account_id: Value = self.safe_string_k(get_value(&response, &i), "account_id", &[]);
                let mut account: Value = self.safe_value(self.accountsById.clone(), account_id.clone(), &[]);
                let mut codeInner: Value = self.safe_string_k(account.clone(), "code", &[]);
                add_element_to_object(get_value_mut(&mut response, &i), &Value::Str("currency".to_string()), codeInner.clone());
            }
            }
        }  else {
            let __ws_arg_22 = self.extend(request.clone(), &[params.clone()]);
            response = self.private_get_accounts_id_transfers(&[__ws_arg_22]).await;
            {
                                let mut i: Value = Value::Int(0);
                let mut __for_first_503: bool = true;
                while { if !__for_first_503 { i = add(&i, &Value::Int(1)); } __for_first_503 = false; is_less_than(&i, &get_array_length(&response)) } {
                add_element_to_object(get_value_mut(&mut response, &i), &Value::Str("currency".to_string()), code.clone());
            }
            }
        }
        return self.parse_transactions(response.clone(), &[currency.clone(), since.clone(), limit.clone()]);

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchDeposits
 * @description fetch all deposits made to an account
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_gettransfers
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getaccounttransfers
 * @param {string} code unified currency code
 * @param {int} [since] the earliest time in ms to fetch deposits for
 * @param {int} [limit] the maximum number of deposits structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [transaction structures]{@link https://docs.ccxt.com/?id=transaction-structure}
 */
    pub async fn fetch_deposits(&mut self, optional_args: &[Value]) -> Value {
        let mut code = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let __ws_arg_23 = self.extend(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("type".to_string(), Value::Str("deposit".to_string()));
    m
}), &[params.clone()]);
        return self.fetch_deposits_withdrawals(&[code.clone(), since.clone(), limit.clone(), __ws_arg_23]).await;

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#fetchWithdrawals
 * @description fetch all withdrawals made from an account
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_gettransfers
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_getaccounttransfers
 * @param {string} code unified currency code
 * @param {int} [since] the earliest time in ms to fetch withdrawals for
 * @param {int} [limit] the maximum number of withdrawals structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [transaction structures]{@link https://docs.ccxt.com/?id=transaction-structure}
 */
    pub async fn fetch_withdrawals(&mut self, optional_args: &[Value]) -> Value {
        let mut code = get_arg(optional_args, 0, Value::Null);
        let mut since = get_arg(optional_args, 1, Value::Null);
        let mut limit = get_arg(optional_args, 2, Value::Null);
        let mut params = get_arg(optional_args, 3, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let __ws_arg_24 = self.extend(Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("type".to_string(), Value::Str("withdraw".to_string()));
    m
}), &[params.clone()]);
        return self.fetch_deposits_withdrawals(&[code.clone(), since.clone(), limit.clone(), __ws_arg_24]).await;

    Value::Null
}

    pub fn parse_transaction_status(&self, mut transaction: Value) -> Value {
        let mut canceled: Value = self.safe_value_k(transaction.clone(), "canceled_at", &[]);
        if is_true(&canceled) {
            return Value::Str("canceled".to_string());
        }
        let mut processed: Value = self.safe_value_k(transaction.clone(), "processed_at", &[]);
        let mut completed: Value = self.safe_value_k(transaction.clone(), "completed_at", &[]);
        if is_true(&completed) {
            return Value::Str("ok".to_string());
        }  else if is_true(&processed) && !is_true(&completed) {
            return Value::Str("failed".to_string());
        }  else {
            return Value::Str("pending".to_string());
        }

    Value::Null
}

    pub fn parse_transaction(&self, mut transaction: Value, optional_args: &[Value]) -> Value {
        let mut currency = get_arg(optional_args, 0, Value::Null);
        //
        // privateGetTransfers
        //
        //    [
        //        {
        //            "id": "bee6fd7c-afb2-4e47-8298-671d09997d16",
        //            "type": "deposit",
        //            "created_at": "2022-12-21 00:48:45.477503+00",
        //            "completed_at": null,
        //            "account_id": "sal3802-36bd-46be-a7b8-alsjf383sldak",     // only from privateGetTransfers
        //            "user_id": "6382048209f92as392039dlks2",                  // only from privateGetTransfers
        //            "amount": "0.01000000",
        //            "details": {
        //                "network": "litecoin",
        //                "crypto_address": "MKemtnCFUYKsNWaf5EMYMpwSszcXWFDtTY",
        //                "coinbase_account_id": "fl2b6925-f6ba-403n-jj03-40fl435n430f",
        //                "coinbase_transaction_id": "63a25bb13cb5cf0001d2cf17", // withdrawals only
        //                "crypto_transaction_hash": "752f35570736341e2a253f7041a34cf1e196fc56128c900fd03d99da899d94c1",
        //                "tx_service_transaction_id": "1873249104",
        //                "coinbase_payment_method_id": ""
        //            },
        //            "canceled_at": null,
        //            "processed_at": null,
        //            "user_nonce": null,
        //            "idem": "5e3201b0-e390-5k3k-a913-c32932049242",
        //            "profile_id": "k3k302a8-c4dk-4f49-9d39-3203923wpk39",
        //            "currency": "LTC"
        //        }
        //    ]
        //
        let mut details: Value = self.safe_value_k(transaction.clone(), "details", &[Value::Map({
            let mut m = indexmap::IndexMap::new();
            m
        })]);
        let mut timestamp: Value = self.parse8601(self.safe_string_k(transaction.clone(), "created_at", &[]));
        let mut currencyId: Value = self.safe_string_k(transaction.clone(), "currency", &[]);
        let mut code: Value = self.safe_currency_code(currencyId.clone(), &[currency.clone()]);
        let mut amount: Value = self.safe_number_k(transaction.clone(), "amount", &[]);
        let mut type_var: Value = self.safe_string_k(transaction.clone(), "type", &[]);
        let mut address: Value = self.safe_string_k(details.clone(), "crypto_address", &[]);
        address = self.safe_string_k(transaction.clone(), "crypto_address", &[address.clone()]);
        let mut fee: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("currency".to_string(), Value::Null);
                m.insert("cost".to_string(), Value::Null);
                m.insert("rate".to_string(), Value::Null);
            m
        });
        if is_equal(&type_var, &Value::Str("withdraw".to_string())) {
            type_var = Value::Str("withdrawal".to_string());
            address = self.safe_string_k(details.clone(), "sent_to_address", &[address.clone()]);
            let mut feeCost: Value = self.safe_number_k(details.clone(), "fee", &[]);
            if !is_equal(&feeCost, &Value::Null) {
                if !is_equal(&amount, &Value::Null) {
                    amount = subtract(&amount, &feeCost);
                }
                add_element_to_object(&mut fee, &Value::Str("cost".to_string()), feeCost.clone());
                add_element_to_object(&mut fee, &Value::Str("currency".to_string()), code.clone());
            }
        }
        let mut networkId: Value = self.safe_string_k(details.clone(), "network", &[]);
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("info".to_string(), transaction.clone());
        m.insert("id".to_string(), self.safe_string_k(transaction.clone(), "id", &[]));
        m.insert("txid".to_string(), self.safe_string_k(details.clone(), "crypto_transaction_hash", &[]));
        m.insert("type".to_string(), type_var.clone());
        m.insert("currency".to_string(), code.clone());
        m.insert("network".to_string(), self.network_id_to_code(&[networkId.clone()]));
        m.insert("amount".to_string(), amount.clone());
        m.insert("status".to_string(), self.parse_transaction_status(transaction.clone()));
        m.insert("timestamp".to_string(), timestamp.clone());
        m.insert("datetime".to_string(), self.iso8601(timestamp.clone()));
        m.insert("address".to_string(), address.clone());
        m.insert("addressFrom".to_string(), Value::Null);
        m.insert("addressTo".to_string(), self.safe_string_k(details.clone(), "crypto_address", &[]));
        m.insert("tag".to_string(), self.safe_string_k(details.clone(), "destination_tag", &[]));
        m.insert("tagFrom".to_string(), Value::Null);
        m.insert("tagTo".to_string(), Value::Null);
        m.insert("updated".to_string(), self.parse8601(self.safe_string_k(transaction.clone(), "processed_at", &[])));
        m.insert("comment".to_string(), Value::Null);
        m.insert("internal".to_string(), Value::Bool(false));
        m.insert("fee".to_string(), fee.clone());
    m
});

    Value::Null
}

/*
 * @method
 * @name coinbaseexchange#createDepositAddress
 * @description create a currency deposit address
 * @see https://docs.cloud.coinbase.com/exchange/reference/exchangerestapi_postcoinbaseaccountaddresses
 * @param {string} code unified currency code of the currency for the deposit address
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [address structure]{@link https://docs.ccxt.com/?id=address-structure}
 */
    pub async fn create_deposit_address(&mut self, mut code: Value, optional_args: &[Value]) -> Value {
        let mut params = get_arg(optional_args, 0, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        self.load_markets(&[]).await;
        let mut currency: Value = self.currency(code.clone());
        let mut accounts: Value = self.safe_value_k(self.options.clone(), "coinbaseAccounts", &[]);
        if is_equal(&accounts, &Value::Null) {
            accounts = self.private_get_coinbase_accounts(&[]).await;
            add_element_to_object(&mut self.options, &Value::Str("coinbaseAccounts".to_string()), accounts.clone()); // cache it
            { let __be_tmp = self.index_by(accounts.clone(), Value::Str("currency".to_string())); add_element_to_object(&mut self.options, &Value::Str("coinbaseAccountsByCurrencyId".to_string()), __be_tmp); };
        }
        let mut currencyId: Value = get_value(&currency, &Value::Str("id".to_string()));
        let mut account: Value = self.safe_value(get_value(&self.options, &Value::Str("coinbaseAccountsByCurrencyId".to_string())), currencyId.clone(), &[]);
        if is_equal(&account, &Value::Null) {
            panic!("{}", crate::exchange_errors::invalid_address(add(&add(&add(&add(&add(&self.id, &Value::Str(" createDepositAddress() could not find currency code ".to_string())), &code), &Value::Str(" with id = ".to_string())), &currencyId), &Value::Str(" in this.options['coinbaseAccountsByCurrencyId']".to_string()))));
        }
        let mut request: Value = Value::Map({
            let mut m = indexmap::IndexMap::new();
                m.insert("id".to_string(), get_value(&account, &Value::Str("id".to_string())));
            m
        });
        let __ws_arg_25 = self.extend(request.clone(), &[params.clone()]);
        let mut response: Value = self.private_post_coinbase_accounts_id_addresses(&[__ws_arg_25]).await;
        let mut address: Value = self.safe_string_k(response.clone(), "address", &[]);
        let mut tag: Value = self.safe_string_k(response.clone(), "destination_tag", &[]);
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("currency".to_string(), code.clone());
        m.insert("address".to_string(), self.check_address(&[address.clone()]));
        m.insert("network".to_string(), Value::Null);
        m.insert("tag".to_string(), tag.clone());
        m.insert("info".to_string(), response.clone());
    m
});

    Value::Null
}

    pub fn sign(&self, mut path: Value, optional_args: &[Value]) -> Value {
        let mut api = get_arg(optional_args, 0, Value::Str("public".to_string()));
        let mut method = get_arg(optional_args, 1, Value::Str("GET".to_string()));
        let mut params = get_arg(optional_args, 2, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut headers = get_arg(optional_args, 3, Value::Null);
        let mut body = get_arg(optional_args, 4, Value::Null);
        let mut request: Value = add(&Value::Str("/".to_string()), &self.implode_params(path.clone(), params.clone()));
        let mut query: Value = self.omit(params.clone(), self.extract_params(path.clone()), &[]);
        if is_equal(&method, &Value::Str("GET".to_string())) {
            if is_true(&get_array_length(&object_keys(&query))) {
                request = add(&request, &add(&Value::Str("?".to_string()), &self.urlencode(query.clone(), &[])));
            }
        }
        let mut url: Value = add(&self.implode_hostname(get_value(&get_value(&self.urls, &Value::Str("api".to_string())), &api)), &request);
        if is_equal(&api, &Value::Str("private".to_string())) {
            self.check_required_credentials(&[]);
            let mut nonce: Value = to_string_val(&self.nonce());
            let mut payload: Value = Value::Str("".to_string());
            if !is_equal(&method, &Value::Str("GET".to_string())) {
                if is_true(&get_array_length(&object_keys(&query))) {
                    body = self.json(query.clone());
                    payload = body.clone();
                }
            }
            let mut what: Value = add(&add(&add(&nonce, &method), &request), &payload);
            let mut secret: Value = Value::Null;
            let _try_result = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
                secret = self.base64_to_binary(self.secret.clone(), &[]);
             #[allow(unreachable_code)] { Value::Null }}));
if let Err(_try_err) = _try_result { let e: Value = panic_to_value(_try_err);
                panic!("{}", crate::exchange_errors::authentication_error(add(&self.id, &Value::Str(" sign() invalid base64 secret".to_string()))));
            }
            let mut signature: Value = self.hmac(self.encode(what.clone()), secret.clone(), Value::Str("sha256".to_string()), &[Value::Str("base64".to_string())]);
            headers = Value::Map({
                let mut m = indexmap::IndexMap::new();
                    m.insert("CB-ACCESS-KEY".to_string(), self.apiKey.clone());
                    m.insert("CB-ACCESS-SIGN".to_string(), signature.clone());
                    m.insert("CB-ACCESS-TIMESTAMP".to_string(), nonce.clone());
                    m.insert("CB-ACCESS-PASSPHRASE".to_string(), self.password.clone());
                    m.insert("Content-Type".to_string(), Value::Str("application/json".to_string()));
                m
            });
        }
        return Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("url".to_string(), url.clone());
        m.insert("method".to_string(), method.clone());
        m.insert("body".to_string(), body.clone());
        m.insert("headers".to_string(), headers.clone());
    m
});

    Value::Null
}

    pub fn handle_errors(&self, mut code: Value, mut reason: Value, mut url: Value, mut method: Value, mut headers: Value, mut body: Value, mut response: Value, mut requestHeaders: Value, mut requestBody: Value) -> Value {
        if is_true(&(is_equal(&code, &Value::Int(400)))) || is_true(&(is_equal(&code, &Value::Int(404)))) {
            if is_equal(&get_value(&body, &Value::Int(0)), &Value::Str("{".to_string())) {
                let mut message: Value = self.safe_string_k(response.clone(), "message", &[]);
                let mut feedback: Value = add(&add(&self.id, &Value::Str(" ".to_string())), &message);
                self.throw_exactly_matched_exception(get_value(&self.exceptions, &Value::Str("exact".to_string())), message.clone(), feedback.clone());
                self.throw_broadly_matched_exception(get_value(&self.exceptions, &Value::Str("broad".to_string())), message.clone(), feedback.clone());
                panic!("{}", crate::exchange_errors::exchange_error(feedback));
            }
            panic!("{}", crate::exchange_errors::exchange_error(add(&add(&self.id, &Value::Str(" ".to_string())), &body)));
        }
        return Value::Null;

    Value::Null
}

    pub async fn request(&mut self, mut path: Value, optional_args: &[Value]) -> Value {
        let mut api = get_arg(optional_args, 0, Value::Str("public".to_string()));
        let mut method = get_arg(optional_args, 1, Value::Str("GET".to_string()));
        let mut params = get_arg(optional_args, 2, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut headers = get_arg(optional_args, 3, Value::Null);
        let mut body = get_arg(optional_args, 4, Value::Null);
        let mut config = get_arg(optional_args, 5, Value::Map({
    let mut m = indexmap::IndexMap::new();
    m
}));
        let mut response: Value = self.fetch2(path.clone(), &[api.clone(), method.clone(), params.clone(), headers.clone(), body.clone(), config.clone()]).await;
        if !is_string(&response) {
            if is_true(&Value::Bool(in_op(&response, &Value::Str("message".to_string())))) {
                panic!("{}", crate::exchange_errors::exchange_error(add(&add(&self.id, &Value::Str(" ".to_string())), &self.json(response.clone()))));
            }
        }
        return response;

    Value::Null
}
}
