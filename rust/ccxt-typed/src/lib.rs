// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code
//
// `ccxt-typed` holds the per-exchange typed unified-method wrappers (`Binance`,
// `Kraken`, … with `fetch_ticker(..) -> Result<Ticker>` etc.) plus the
// `TypedExchange` / `TypedExchangeExt` traits. They were split out of the
// `ccxt` crate so the base crate's single `rustc` invocation stays under the CI
// runner's memory ceiling. The wrapper files reference the base via
// `crate::<module>` paths; the re-exports below make those paths resolve
// unchanged now that the files live in this crate instead of `ccxt`.

pub use ccxt::{
    error, value, types, exchange, precise, runtime, exchange_errors, exchange_stubs,
    exchange_generated,
};
pub use ccxt::{
    Value, get_value, set_value, safe_string, safe_number, safe_integer, safe_bool,
    ExchangeError, Exchange, Result,
};

pub mod exchanges;
pub mod typed;
pub use typed::*;
