// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#![allow(unused, non_snake_case, dead_code, clippy::all)]
use ccxt::Value;
use ccxt::get_value;
use ccxt::runtime::*;
use ccxt::pro::*;

fn watchTickerLoop(mut exchange: Value) -> Value {
    let mut method: Value = Value::Str("watchTicker".to_string());
    while is_true(&Value::Bool(true)) {
        println_val(&Value::Str("creating future".to_string()));
        let mut ticker: Value = exchange.watch_ticker(Value::Str("BTC/USDT".to_string())).await;
        println_val(&Value::Str("ticker recieved, future resolved".to_string()));
    }
}
fn watchOrderBookForSymbolsLoop(mut exchange: Value) -> Value {
    let mut method: Value = Value::Str("watchTicker".to_string());
    while is_true(&Value::Bool(true)) {
        let mut trades: Value = exchange.watch_trades_for_symbols(Value::List(vec![Value::Str("BTC/USDT".to_string()), Value::Str("ETH/USDT".to_string()), Value::Str("LTC/USDT".to_string())])).await;
    }
}
fn closeAfter(mut exchange: Value, mut ms: Value) -> Value {
    sleep(ms).await;
    exchange.close().await;
}
fn testWsClose() -> Value {
    let mut exchange = get_value(&ccxt, &Value::Str("pro".to_string())).binance::new();
    // --------------------------------------------
    println_val(&Value::Str("Testing exchange.close(): No future awaiting, should close with no errors".to_string()));
    exchange.watch_ticker(Value::Str("BTC/USDT".to_string())).await;
    println_val(&Value::Str("ticker received".to_string()));
    exchange.close().await;
    println_val(&Value::Str("PASSED - exchange closed with no errors".to_string()));
    // --------------------------------------------
    println_val(&Value::Str("Testing exchange.close(): call watch_multiple, resolve, should close with no errors".to_string()));
    exchange.watch_trades_for_symbols(Value::List(vec![Value::Str("BTC/USDT".to_string()), Value::Str("ETH/USDT".to_string())])).await;
    println_val(&Value::Str("ticker received".to_string()));
    exchange.close().await;
    println_val(&Value::Str("PASSED - exchange closed with no errors".to_string()));
    // --------------------------------------------
    println_val(&Value::Str("Testing exchange.close(): Awaiting future should throw ClosedByUser".to_string()));
    {
        closeAfter(exchange, Value::Int(5000));
        watchTickerLoop(exchange).await;
    }
    // --------------------------------------------
    println_val(&Value::Str("Test exchange.close(): Call watch_multiple unhandled futures are canceled".to_string()));
    {
        closeAfter(exchange, Value::Int(5000));
        watchOrderBookForSymbolsLoop(exchange).await;
    }
    process.exit(Value::Int(0));
}
