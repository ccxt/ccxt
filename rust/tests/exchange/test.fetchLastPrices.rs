// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

use crate::*;

fn testFetchLastPrices(exchange: Value, skippedProperties: Value, symbol: Value) -> Value {
    let mut method: Value = Value::Str("fetchLastprices".to_string());
    // log ('fetching all tickers at once...')
    let mut response: Value = Value::Null;
    let mut checkedSymbol: Value = Value::Null;
    let _try_result = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        response = exchange.fetchLastPrices().await;
    }));
    if let Err(_e) = _try_result {
        response = exchange.fetchLastPrices(Value::List(vec![symbol])).await;
        checkedSymbol = symbol;
    }
    assert!(is_object(&response), add(&add(&add(&add(&add(&add(&exchange.id, &Value::Str(" ".to_string())), &method), &Value::Str(" ".to_string())), &checkedSymbol), &Value::Str(" must return an object. ".to_string())), &exchange.json(response)));
    let mut values: Value = object_values(&response);
    testSharedMethods.assert!NonEmtpyArray(exchange, skippedProperties, method, values, checkedSymbol);
    let mut atLeastOnePassed: Value = Value::Bool(false);
    {
                let mut i: Value = Value::Int(0);
        while is_less_than(&i, &get_array_length(&values)) {
        // todo: symbol check here
        testLastPrice(exchange, skippedProperties, method, get_value(&values, &i), checkedSymbol);
        atLeastOnePassed = is_true(&atLeastOnePassed) || is_true(&(is_greater_than(&exchange.safeNumber(get_value(&values, &i), Value::Str("price".to_string())), &Value::Int(0))));
        i = add(&i, &Value::Int(1));
    }
    }
    assert!(atLeastOnePassed, add(&add(&add(&add(&add(&exchange.id, &Value::Str(" ".to_string())), &method), &Value::Str(" ".to_string())), &checkedSymbol), &Value::Str(" at least one symbol should pass the test".to_string())));
    return Value::Bool(true);
}