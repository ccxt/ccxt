// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

use crate::*;

fn testFetchLedgerEntry(exchange: Value, skippedProperties: Value, code: Value) -> Value {
    let mut method: Value = Value::Str("fetchLedgerEntry".to_string());
    let mut items: Value = exchange.fetchLedger(code).await;
    let mut length: Value = get_array_length(&items);
    testSharedMethods.assert!NonEmtpyArray(exchange, skippedProperties, method, items, code);
    if is_greater_than(&length, &Value::Int(0)) {
        let mut firstItem: Value = get_value(&items, &Value::Int(0));
        let mut id: Value = get_value(&firstItem, &Value::Str("id".to_string()));
        let mut item: Value = exchange.fetchLedgerEntry(id).await;
        let mut now: Value = exchange.milliseconds();
        testLedgerEntry(exchange, skippedProperties, method, item, code, now);
    }
    return Value::Bool(true);
}