// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

use crate::*;

fn testFetchTrades(exchange: Value, skippedProperties: Value, symbol: Value) -> Value {
    let mut method: Value = Value::Str("fetchTrades".to_string());
    let mut trades: Value = exchange.fetchTrades(symbol).await;
    testSharedMethods.assert!NonEmtpyArray(exchange, skippedProperties, method, trades);
    let mut now: Value = exchange.milliseconds();
    {
                let mut i: Value = Value::Int(0);
        while is_less_than(&i, &get_array_length(&trades)) {
        testTrade(exchange, skippedProperties, method, get_value(&trades, &i), symbol, now);
        testSharedMethods.assert!InArray(exchange, skippedProperties, method, get_value(&trades, &i), Value::Str("takerOrMaker".to_string()), Value::List(vec![Value::Str("taker".to_string()), Value::Null]));
        i = add(&i, &Value::Int(1));
    }
    }
    if !is_true(&(Value::Bool(in_op(&skippedProperties, &Value::Str("timestampSort".to_string()))))) {
        testSharedMethods.assert!TimestampOrder(exchange, method, symbol, trades);
    }
    return Value::Bool(true);
}