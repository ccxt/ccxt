// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#![allow(unused, non_snake_case, dead_code, clippy::all)]
use ccxt::Value;
use ccxt::get_value;
use ccxt::runtime::*;
use crate::test_helpers::*;
// sibling validators / method tests are re-exported from mod.rs
use super::*;

pub async fn testFetchMarkets(mut exchange: Value, mut skippedProperties: Value) -> Value {
    let mut method: Value = Value::Str("fetchMarkets".to_string());
    let mut markets: Value = crate::live_dispatch::dispatch(&mut exchange, "fetch_markets", vec![]).await;
    assert!(ccxt::runtime::is_true(&(exchange.is_dictionary(markets.clone()))));
    let mut marketValues: Value = object_values(&markets);
    crate::tests_support::shared::assert_non_emtpy_array(exchange.clone(), &[skippedProperties.clone(), method.clone(), marketValues.clone()]);
    {
                let mut i: Value = Value::Int(0);
        let mut __for_first_44: bool = true;
        while { if !__for_first_44 { i = add(&i, &Value::Int(1)); } __for_first_44 = false; is_less_than(&i, &get_array_length(&marketValues)) } {
        testMarket(exchange.clone(), skippedProperties.clone(), method.clone(), get_value(&marketValues, &i));
    }
    }
    detectMarketConflicts(exchange, markets);
    return Value::Bool(true);

    Value::Null
}
fn detectMarketConflicts(mut exchange: Value, mut marketValues: Value) -> Value {
    // detect if there are markets with different ids for the same symbol
    let mut ids: Value = Value::Map({
        let mut m = indexmap::IndexMap::new();
        m
    });
    {
                let mut i: Value = Value::Int(0);
        let mut __for_first_45: bool = true;
        while { if !__for_first_45 { i = add(&i, &Value::Int(1)); } __for_first_45 = false; is_less_than(&i, &get_array_length(&marketValues)) } {
        let mut market: Value = get_value(&marketValues, &i);
        let mut symbol: Value = get_value(&market, &Value::Str("symbol".to_string()));
        if !is_true(&(Value::Bool(in_op(&ids, &symbol)))) {
            add_element_to_object(&mut ids, &symbol, get_value(&market, &Value::Str("id".to_string())));
        }  else {
            let mut isDifferent: Value = Value::Bool(!is_equal(&get_value(&ids, &symbol), &get_value(&market, &Value::Str("id".to_string()))));
            assert!(ccxt::runtime::is_true(&(Value::Bool(!is_true(&isDifferent)))));
        }
    }
    }
    return Value::Bool(true);

    Value::Null
}