// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#![allow(unused, non_snake_case, dead_code, clippy::all)]
use ccxt::Value;
use ccxt::get_value;
use ccxt::runtime::*;
use crate::test_helpers::*;
// sibling validators / method tests are re-exported from mod.rs
use super::*;

pub fn testFeatures(mut exchange: Value, mut skippedProperties: Value) -> Value {
    let mut marketTypes: Value = Value::List(vec![Value::Str("spot".to_string()), Value::Str("swap".to_string()), Value::Str("future".to_string()), Value::Str("option".to_string())]);
    let mut subTypes: Value = Value::List(vec![Value::Str("linear".to_string()), Value::Str("inverse".to_string())]);
    let mut features: Value = get_value(&exchange, &Value::Str("features".to_string()));
    let mut keys: Value = object_keys(&features);
    {
                let mut i: Value = Value::Int(0);
        let mut __for_first_1100: bool = true;
        while { if !__for_first_1100 { i = add(&i, &Value::Int(1)); } __for_first_1100 = false; is_less_than(&i, &get_array_length(&keys)) } {
        crate::tests_support::shared::assert_in_array(exchange.clone(), &[skippedProperties.clone(), Value::Str("features".to_string()).clone(), keys.clone(), i.clone(), marketTypes.clone()]);
        let mut marketType: Value = get_value(&keys, &i);
        let mut value: Value = get_value(&features, &marketType);
        // assert!(ccxt::runtime::is_true(&(value !== undefined)));
        if is_equal(&value, &Value::Null) {
            continue;
        }
        if is_equal(&marketType, &Value::Str("spot".to_string())) {
            testFeaturesInner(exchange.clone(), skippedProperties.clone(), value.clone());
        }  else {
            let mut subKeys: Value = object_keys(&value);
            {
                                let mut j: Value = Value::Int(0);
                let mut __for_first_1099: bool = true;
                while { if !__for_first_1099 { j = add(&j, &Value::Int(1)); } __for_first_1099 = false; is_less_than(&j, &get_array_length(&subKeys)) } {
                let mut subKey: Value = get_value(&subKeys, &j);
                crate::tests_support::shared::assert_in_array(exchange.clone(), &[skippedProperties.clone(), Value::Str("features".to_string()).clone(), subKeys.clone(), j.clone(), subTypes.clone()]);
                let mut subValue: Value = get_value(&value, &subKey);
                // sometimes it might not be available for exchange, eg. future>inverse)
                if !is_equal(&subValue, &Value::Null) {
                    testFeaturesInner(exchange.clone(), skippedProperties.clone(), subValue.clone());
                }
            }
            }
        }
    }
    }
    return Value::Bool(true);

    Value::Null
}
pub fn testFeaturesInner(mut exchange: Value, mut skippedProperties: Value, mut featureObj: Value) {
    let mut format: Value = Value::Map({
        let mut m = indexmap::IndexMap::new();
            m.insert("sandbox".to_string(), Value::Bool(false));
            m.insert("createOrder".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("triggerPrice".to_string(), Value::Bool(false));
        m.insert("triggerPriceType".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("mark".to_string(), Value::Bool(false));
        m.insert("last".to_string(), Value::Bool(false));
        m.insert("index".to_string(), Value::Bool(false));
    m
}));
        m.insert("stopLossPrice".to_string(), Value::Bool(false));
        m.insert("takeProfitPrice".to_string(), Value::Bool(false));
        m.insert("attachedStopLossTakeProfit".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("triggerPriceType".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("last".to_string(), Value::Bool(false));
        m.insert("mark".to_string(), Value::Bool(false));
        m.insert("index".to_string(), Value::Bool(false));
    m
}));
        m.insert("price".to_string(), Value::Bool(false));
    m
}));
        m.insert("timeInForce".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("GTC".to_string(), Value::Bool(false));
        m.insert("IOC".to_string(), Value::Bool(false));
        m.insert("FOK".to_string(), Value::Bool(false));
        m.insert("PO".to_string(), Value::Bool(false));
        m.insert("GTD".to_string(), Value::Bool(false));
    m
}));
        m.insert("hedged".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
    m
}));
            m.insert("createOrders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("max".to_string(), Value::Int(5));
    m
}));
            m.insert("fetchMyTrades".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("daysBack".to_string(), Value::Int(0));
        m.insert("limit".to_string(), Value::Int(0));
        m.insert("untilDays".to_string(), Value::Int(0));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
            m.insert("fetchOrder".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("trigger".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
            m.insert("fetchOpenOrders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(0));
        m.insert("trigger".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
            m.insert("fetchOrders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(0));
        m.insert("daysBack".to_string(), Value::Int(0));
        m.insert("untilDays".to_string(), Value::Int(0));
        m.insert("trigger".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
            m.insert("fetchClosedOrders".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("marginMode".to_string(), Value::Bool(false));
        m.insert("limit".to_string(), Value::Int(0));
        m.insert("daysBack".to_string(), Value::Int(0));
        m.insert("daysBackCanceled".to_string(), Value::Int(0));
        m.insert("untilDays".to_string(), Value::Int(0));
        m.insert("trigger".to_string(), Value::Bool(false));
        m.insert("trailing".to_string(), Value::Bool(false));
        m.insert("symbolRequired".to_string(), Value::Bool(false));
    m
}));
            m.insert("fetchOHLCV".to_string(), Value::Map({
    let mut m = indexmap::IndexMap::new();
        m.insert("limit".to_string(), Value::Int(0));
    m
}));
        m
    });
    let mut featureKeys: Value = object_keys(&featureObj);
    let mut allMethods: Value = object_keys(&get_value(&exchange, &Value::Str("has".to_string())));
    {
                let mut i: Value = Value::Int(0);
        let mut __for_first_1101: bool = true;
        while { if !__for_first_1101 { i = add(&i, &Value::Int(1)); } __for_first_1101 = false; is_less_than(&i, &get_array_length(&featureKeys)) } {
        crate::tests_support::shared::assert_in_array(exchange.clone(), &[skippedProperties.clone(), Value::Str("features".to_string()).clone(), featureKeys.clone(), i.clone(), allMethods.clone()]);
        crate::tests_support::shared::assert_structure(exchange.clone(), &[skippedProperties.clone(), Value::Str("features".to_string()).clone(), featureObj.clone(), format.clone(), Value::Null.clone(), Value::Bool(true).clone()]); // deep structure check
    }
    }
}