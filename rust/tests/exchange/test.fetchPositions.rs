// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

use crate::*;

fn testFetchPositions(exchange: Value, skippedProperties: Value, symbol: Value) -> Value {
    let mut method: Value = Value::Str("fetchPositions".to_string());
    let mut now: Value = exchange.milliseconds();
    // without symbol
    let mut positions: Value = exchange.fetchPositions().await;
    testSharedMethods.assert!NonEmtpyArray(exchange, skippedProperties, method, positions, symbol);
    {
                let mut i: Value = Value::Int(0);
        while is_less_than(&i, &get_array_length(&positions)) {
        testPosition(exchange, skippedProperties, method, get_value(&positions, &i), Value::Null, now);
        i = add(&i, &Value::Int(1));
    }
    }
    // testSharedMethods.assert!TimestampOrder (exchange, method, undefined, positions); // currently order of positions does not make sense
    // with symbol
    let mut positionsForSymbol: Value = exchange.fetchPositions(Value::List(vec![symbol])).await;
    assert!(Value::Bool(is_array(&positionsForSymbol)), add(&add(&add(&add(&exchange.id, &Value::Str(" ".to_string())), &method), &Value::Str(" must return an array, returned ".to_string())), &exchange.json(positionsForSymbol)));
    let mut positionsForSymbolLength: Value = get_array_length(&positionsForSymbol);
    assert!(is_less_than_or_equal(&positionsForSymbolLength, &Value::Int(4)), add(&add(&add(&add(&exchange.id, &Value::Str(" ".to_string())), &method), &Value::Str(" positions length for particular symbol should be less than 4, returned ".to_string())), &exchange.json(positionsForSymbol)));
    {
                let mut i: Value = Value::Int(0);
        while is_less_than(&i, &get_array_length(&positionsForSymbol)) {
        testPosition(exchange, skippedProperties, method, get_value(&positionsForSymbol, &i), symbol, now);
        i = add(&i, &Value::Int(1));
    }
    }
    return Value::Bool(true);
}