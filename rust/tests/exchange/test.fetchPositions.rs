// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#![allow(unused, non_snake_case, dead_code, clippy::all)]
use ccxt::Value;
use ccxt::get_value;
use ccxt::runtime::*;
use crate::test_helpers::*;
// sibling validators / method tests are re-exported from mod.rs
use super::*;

pub async fn testFetchPositions(mut exchange: Value, mut skippedProperties: Value, mut symbol: Value) -> Value {
    let mut method: Value = Value::Str("fetchPositions".to_string());
    let mut now: Value = exchange.milliseconds();
    // without symbol
    let mut positions: Value = crate::live_dispatch::dispatch(&mut exchange, "fetch_positions", vec![]).await;
    crate::tests_support::shared::assert_non_emtpy_array(exchange.clone(), &[skippedProperties.clone(), method.clone(), positions.clone(), symbol.clone()]);
    {
                let mut i: Value = Value::Int(0);
        let mut __for_first_1169: bool = true;
        while { if !__for_first_1169 { i = add(&i, &Value::Int(1)); } __for_first_1169 = false; is_less_than(&i, &get_array_length(&positions)) } {
        testPosition(exchange.clone(), skippedProperties.clone(), method.clone(), get_value(&positions, &i), Value::Null, now.clone());
    }
    }
    // testSharedMethods.assertTimestampOrder (exchange, method, undefined, positions); // currently order of positions does not make sense
    // with symbol
    let mut positionsForSymbol: Value = crate::live_dispatch::dispatch(&mut exchange, "fetch_positions", vec![Value::List(vec![symbol.clone()])]).await;
    assert!(ccxt::runtime::is_true(&(Value::Bool(is_array(&positionsForSymbol)))));
    let mut positionsForSymbolLength: Value = get_array_length(&positionsForSymbol);
    assert!(ccxt::runtime::is_true(&(Value::Bool(is_less_than_or_equal(&positionsForSymbolLength, &Value::Int(4))))));
    {
                let mut i: Value = Value::Int(0);
        let mut __for_first_1170: bool = true;
        while { if !__for_first_1170 { i = add(&i, &Value::Int(1)); } __for_first_1170 = false; is_less_than(&i, &get_array_length(&positionsForSymbol)) } {
        testPosition(exchange.clone(), skippedProperties.clone(), method.clone(), get_value(&positionsForSymbol, &i), symbol.clone(), now.clone());
    }
    }
    return Value::Bool(true);

    Value::Null
}