// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

use crate::*;

fn testFetchLeverageTiers(exchange: Value, skippedProperties: Value, symbol: Value) -> Value {
    let mut method: Value = Value::Str("fetchLeverageTiers".to_string());
    let mut tiers: Value = exchange.fetchLeverageTiers(Value::List(vec![Value::Str("symbol".to_string())])).await;
    // const format = {
    //     'RAY/USDT': [
    //       {},
    //     ],
    // };
    assert!(is_object(&tiers), add(&add(&add(&add(&add(&add(&exchange.id, &Value::Str(" ".to_string())), &method), &Value::Str(" ".to_string())), &symbol), &Value::Str(" must return an object. ".to_string())), &exchange.json(tiers)));
    let mut tierKeys: Value = object_keys(&tiers);
    testSharedMethods.assert!NonEmtpyArray(exchange, skippedProperties, method, tierKeys, symbol);
    {
                let mut i: Value = Value::Int(0);
        while is_less_than(&i, &get_array_length(&tierKeys)) {
        let mut tiersForSymbol: Value = get_value(&tiers, &get_value(&tierKeys, &i));
        testSharedMethods.assert!NonEmtpyArray(exchange, skippedProperties, method, tiersForSymbol, symbol);
        {
                        let mut j: Value = Value::Int(0);
            while is_less_than(&j, &get_array_length(&tiersForSymbol)) {
            testLeverageTier(exchange, skippedProperties, method, get_value(&tiersForSymbol, &j));
            j = add(&j, &Value::Int(1));
        }
        }
        i = add(&i, &Value::Int(1));
    }
    }
    return Value::Bool(true);
}