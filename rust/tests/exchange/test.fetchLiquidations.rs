// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

use crate::*;

fn testFetchLiquidations(exchange: Value, skippedProperties: Value, code: Value) -> Value {
    let mut method: Value = Value::Str("fetchLiquidations".to_string());
    if !is_true(&get_value(&exchange.has, &Value::Str("fetchLiquidations".to_string()))) {
        return Value::Bool(true);
    }
    let mut items: Value = exchange.fetchLiquidations(code).await;
    assert!(Value::Bool(is_array(&items)), add(&add(&add(&add(&add(&add(&exchange.id, &Value::Str(" ".to_string())), &method), &Value::Str(" ".to_string())), &code), &Value::Str(" must return an array. ".to_string())), &exchange.json(items)));
    {
                let mut i: Value = Value::Int(0);
        while is_less_than(&i, &get_array_length(&items)) {
        testLiquidation(exchange, skippedProperties, method, get_value(&items, &i), code);
        i = add(&i, &Value::Int(1));
    }
    }
    testSharedMethods.assert!TimestampOrder(exchange, method, code, items);
    return Value::Bool(true);
}