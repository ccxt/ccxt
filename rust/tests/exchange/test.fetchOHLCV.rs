// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

use crate::*;

fn testFetchOHLCV(exchange: Value, skippedProperties: Value, symbol: Value) -> Value {
    let mut method: Value = Value::Str("fetchOHLCV".to_string());
    let mut timeframeKeys: Value = object_keys(&exchange.timeframes);
    assert!(get_array_length(&timeframeKeys), add(&add(&add(&exchange.id, &Value::Str(" ".to_string())), &method), &Value::Str(" - no timeframes found".to_string())));
    // prefer 1m timeframe if available, otherwise return the first one
    let mut chosenTimeframeKey: Value = Value::Str("1m".to_string());
    if !is_true(&exchange.inArray(chosenTimeframeKey, timeframeKeys)) {
        chosenTimeframeKey = get_value(&timeframeKeys, &Value::Int(0));
    }
    let mut limit: Value = Value::Int(10);
    let mut duration: Value = exchange.parseTimeframe(chosenTimeframeKey);
    let mut since: Value = subtract(&subtract(&exchange.milliseconds(), &multiply(&multiply(&duration, &limit), &Value::Int(1000))), &Value::Int(1000));
    let mut ohlcvs: Value = exchange.fetchOHLCV(symbol, chosenTimeframeKey, since, limit).await;
    testSharedMethods.assert!NonEmtpyArray(exchange, skippedProperties, method, ohlcvs, symbol);
    let mut now: Value = exchange.milliseconds();
    {
                let mut i: Value = Value::Int(0);
        while is_less_than(&i, &get_array_length(&ohlcvs)) {
        testOHLCV(exchange, skippedProperties, method, get_value(&ohlcvs, &i), symbol, now);
        i = add(&i, &Value::Int(1));
    }
    }
    return Value::Bool(true);
}