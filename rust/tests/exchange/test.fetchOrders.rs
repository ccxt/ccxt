// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

use crate::*;

fn testFetchOrders(exchange: Value, skippedProperties: Value, symbol: Value) -> Value {
    let mut method: Value = Value::Str("fetchOrders".to_string());
    let mut orders: Value = exchange.fetchOrders(symbol).await;
    assert!(Value::Bool(is_array(&orders)), add(&add(&add(&add(&exchange.id, &Value::Str(" ".to_string())), &method), &Value::Str(" must return an array, returned ".to_string())), &exchange.json(orders)));
    testSharedMethods.assert!NonEmtpyArray(exchange, skippedProperties, method, orders, symbol);
    let mut now: Value = exchange.milliseconds();
    {
                let mut i: Value = Value::Int(0);
        while is_less_than(&i, &get_array_length(&orders)) {
        testOrder(exchange, skippedProperties, method, get_value(&orders, &i), symbol, now);
        i = add(&i, &Value::Int(1));
    }
    }
    testSharedMethods.assert!TimestampOrder(exchange, method, symbol, orders);
    return Value::Bool(true);
}