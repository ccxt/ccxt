// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

use crate::*;

fn testFetchMarginModes(exchange: Value, skippedProperties: Value, symbol: Value) -> Value {
    let mut method: Value = Value::Str("fetchMarginModes".to_string());
    let mut marginModes: Value = exchange.fetchMarginModes(Value::List(vec![Value::Str("symbol".to_string())])).await;
    assert!(is_object(&marginModes), add(&add(&add(&add(&add(&add(&exchange.id, &Value::Str(" ".to_string())), &method), &Value::Str(" ".to_string())), &symbol), &Value::Str(" must return an object. ".to_string())), &exchange.json(marginModes)));
    let mut marginModeKeys: Value = object_keys(&marginModes);
    testSharedMethods.assert!NonEmtpyArray(exchange, skippedProperties, method, marginModes, symbol);
    {
                let mut i: Value = Value::Int(0);
        while is_less_than(&i, &get_array_length(&marginModeKeys)) {
        let mut marginMode: Value = get_value(&marginModes, &get_value(&marginModeKeys, &i));
        testSharedMethods.assert!NonEmtpyArray(exchange, skippedProperties, method, marginMode, symbol);
        testMarginMode(exchange, skippedProperties, method, marginMode);
        i = add(&i, &Value::Int(1));
    }
    }
    return Value::Bool(true);
}