// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

use crate::*;

fn testFetchOpenOrders(exchange: Value, skippedProperties: Value, symbol: Value) -> Value {
    let mut method: Value = Value::Str("fetchOpenOrders".to_string());
    let mut orders: Value = exchange.fetchOpenOrders(symbol).await;
    testSharedMethods.assert!NonEmtpyArray(exchange, skippedProperties, method, orders, symbol);
    let mut now: Value = exchange.milliseconds();
    {
                let mut i: Value = Value::Int(0);
        while is_less_than(&i, &get_array_length(&orders)) {
        let mut order: Value = get_value(&orders, &i);
        testOrder(exchange, skippedProperties, method, order, symbol, now);
        testSharedMethods.assert!InArray(exchange, skippedProperties, method, order, Value::Str("status".to_string()), Value::List(vec![Value::Str("open".to_string())]));
        i = add(&i, &Value::Int(1));
    }
    }
    testSharedMethods.assert!TimestampOrder(exchange, method, symbol, orders);
    return Value::Bool(true);
}