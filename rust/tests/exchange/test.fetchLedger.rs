// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#![allow(unused, non_snake_case, dead_code, clippy::all)]
use ccxt::Value;
use ccxt::get_value;
use ccxt::runtime::*;
use crate::test_helpers::*;
// sibling validators / method tests are re-exported from mod.rs
use super::*;

pub async fn testFetchLedger(mut exchange: Value, mut skippedProperties: Value, mut code: Value) -> Value {
    let mut method: Value = Value::Str("fetchLedger".into());
    let mut items: Value = crate::live_dispatch::dispatch(&mut exchange, "fetch_ledger", vec![code.clone()]).await;
    crate::tests_support::shared::assert_non_emtpy_array(exchange.clone(), &[skippedProperties.clone(), method.clone(), items.clone(), code.clone()]);
    let mut now: Value = exchange.milliseconds();
    {
                let mut i: Value = Value::Int(0);
        let mut __for_first_42: bool = true;
        while { if !__for_first_42 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_42 = false; i.as_f64().unwrap_or(f64::NAN) < Value::Int(items.len() as i64).as_f64().unwrap_or(f64::NAN) } {
        testLedgerEntry(exchange.clone(), skippedProperties.clone(), method.clone(), get_value(&items, &i), code.clone(), now.clone());
    }
    }
    crate::tests_support::shared::assert_timestamp_order(exchange.clone(), &[method.clone(), code.clone(), items.clone()]);
    return Value::Bool(true);

    Value::Null
}