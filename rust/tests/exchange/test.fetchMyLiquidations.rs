// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#![allow(unused, non_snake_case, dead_code, clippy::all)]
use ccxt::Value;
use ccxt::get_value;
use ccxt::runtime::*;
use crate::test_helpers::*;
// sibling validators / method tests are re-exported from mod.rs
use super::*;

pub async fn testFetchMyLiquidations(mut exchange: Value, mut skippedProperties: Value, mut code: Value) -> Value {
    let mut method: Value = Value::Str("fetchMyLiquidations".to_string());
    if (get_value(&exchange, &Value::Str("has".to_string())).as_map().and_then(|__m| __m.get("fetchMyLiquidations")).cloned().unwrap_or(Value::Null) == Value::Null) || (get_value(&exchange, &Value::Str("has".to_string())).as_map().and_then(|__m| __m.get("fetchMyLiquidations")).cloned().unwrap_or(Value::Null).as_bool() == Some(false)) {
        return Value::Bool(true);
    }
    let mut items: Value = crate::live_dispatch::dispatch(&mut exchange, "fetch_my_liquidations", vec![code.clone()]).await;
    assert!(ccxt::runtime::is_true(&(Value::Bool(is_array(&items)))));
    {
                let mut i: Value = Value::Int(0);
        let mut __for_first_1464: bool = true;
        while { if !__for_first_1464 { i = (match (&(i), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_1464 = false; i.as_f64().unwrap_or(f64::NAN) < Value::Int(items.len() as i64).as_f64().unwrap_or(f64::NAN) } {
        testLiquidation(exchange.clone(), skippedProperties.clone(), method.clone(), get_value(&items, &i), code.clone());
    }
    }
    crate::tests_support::shared::assert_timestamp_order(exchange.clone(), &[method.clone(), code.clone(), items.clone()]);
    return Value::Bool(true);

    Value::Null
}