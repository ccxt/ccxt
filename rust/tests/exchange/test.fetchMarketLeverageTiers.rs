// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#![allow(unused, non_snake_case, dead_code, clippy::all)]
use ccxt::Value;
use ccxt::get_value;
use ccxt::runtime::*;
use crate::test_helpers::*;
// sibling validators / method tests are re-exported from mod.rs
use super::*;

pub async fn testFetchMarketLeverageTiers(mut exchange: Value, mut skippedProperties: Value, mut symbol: Value) -> Value {
    let mut method: Value = Value::Str("fetchMarketLeverageTiers".into());
    let mut tiers: Value = crate::live_dispatch::dispatch(&mut exchange, "fetch_market_leverage_tiers", vec![symbol.clone()]).await;
    crate::tests_support::shared::assert_non_emtpy_array(exchange.clone(), &[skippedProperties.clone(), method.clone(), tiers.clone(), symbol.clone()]);
    {
                let mut j: Value = Value::Int(0);
        let mut __for_first_1529: bool = true;
        while { if !__for_first_1529 { j = (match (&(j), &(Value::Int(1))) { (Value::Int(x), Value::Int(y)) => Value::Int(x + y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 + *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x + *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x + y), _ => Value::Null }); } __for_first_1529 = false; j.as_f64().unwrap_or(f64::NAN) < Value::Int(tiers.len() as i64).as_f64().unwrap_or(f64::NAN) } {
        testLeverageTier(exchange.clone(), skippedProperties.clone(), method.clone(), tiers.as_array().and_then(|__arr| match &j { Value::Int(__n) => __arr.get(*__n as usize), Value::Str(__s) => __s.parse::<usize>().ok().and_then(|__n| __arr.get(__n)), _ => None }).cloned().unwrap_or(Value::Null));
    }
    }
    return Value::Bool(true);

    Value::Null
}