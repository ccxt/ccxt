// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

use crate::*;

fn testLoadMarkets(exchange: Value, skippedProperties: Value) -> Value {
    let mut method: Value = Value::Str("loadMarkets".to_string());
    let mut markets: Value = exchange.loadMarkets().await;
    assert!(is_object(&exchange.markets), Value::Str(".markets is not an object".to_string()));
    assert!(Value::Bool(is_array(&exchange.symbols)), Value::Str(".symbols is not an array".to_string()));
    let mut symbolsLength: Value = get_array_length(&exchange.symbols);
    let mut marketKeys: Value = object_keys(&exchange.markets);
    let mut marketKeysLength: Value = get_array_length(&marketKeys);
    assert!(is_greater_than(&symbolsLength, &Value::Int(0)), Value::Str(".symbols count <= 0 (less than or equal to zero)".to_string()));
    assert!(is_greater_than(&marketKeysLength, &Value::Int(0)), Value::Str(".markets objects keys length <= 0 (less than or equal to zero)".to_string()));
    assert!(is_equal(&symbolsLength, &marketKeysLength), Value::Str("number of .symbols is not equal to the number of .markets".to_string()));
    let mut marketValues: Value = object_values(&markets);
    {
                let mut i: Value = Value::Int(0);
        while is_less_than(&i, &get_array_length(&marketValues)) {
        testMarket(exchange, skippedProperties, method, get_value(&marketValues, &i));
        i = add(&i, &Value::Int(1));
    }
    }
    return Value::Bool(true);
}