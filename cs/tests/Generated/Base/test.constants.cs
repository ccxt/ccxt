using ccxt;
using ccxt.pro;
namespace Tests;

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

public partial class BaseTest
{
        public void testConstants()
        {
            // Assert all
            Assert(isEqual(ROUND, 1), "ROUND should be 1");
            Assert(isEqual(TRUNCATE, 0), "TRUNCATE should be 0");
            Assert(isEqual(ROUND_UP, 2), "ROUND_UP should be 2");
            Assert(isEqual(ROUND_DOWN, 3), "ROUND_DOWN should be 3");
            Assert(isEqual(DECIMAL_PLACES, 2), "DECIMAL_PLACES should be 2");
            Assert(isEqual(SIGNIFICANT_DIGITS, 3), "SIGNIFICANT_DIGITS should be 3");
            Assert(isEqual(TICK_SIZE, 4), "TICK_SIZE should be 4");
            Assert(isEqual(NO_PADDING, 5), "NO_PADDING should be 5");
            Assert(isEqual(PAD_WITH_ZERO, 6), "PAD_WITH_ZERO should be 6");
        }
}