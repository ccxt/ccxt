namespace ccxt;

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

public partial class nobitex : Exchange
{
    public override object describe()
    {
        return this.deepExtend(base.describe(), new Dictionary<string, object>() {
            { "id", "nobitex" },
            { "name", "Nobitex" },
            { "countries", new List<object>() {"IR"} },
            { "rateLimit", 1000 },
            { "version", "1" },
            { "certified", false },
            { "pro", false },
            { "has", new Dictionary<string, object>() {
                { "CORS", null },
                { "spot", true },
                { "margin", false },
                { "swap", false },
                { "future", false },
                { "option", false },
                { "addMargin", false },
                { "cancelAllOrders", false },
                { "cancelOrder", false },
                { "cancelOrders", false },
                { "createDepositAddress", false },
                { "createOrder", false },
                { "createStopLimitOrder", false },
                { "createStopMarketOrder", false },
                { "createStopOrder", false },
                { "editOrder", false },
                { "fetchBalance", false },
                { "fetchBorrowInterest", false },
                { "fetchBorrowRateHistories", false },
                { "fetchBorrowRateHistory", false },
                { "fetchClosedOrders", false },
                { "fetchCrossBorrowRate", false },
                { "fetchCrossBorrowRates", false },
                { "fetchCurrencies", false },
                { "fetchDepositAddress", false },
                { "fetchDeposits", false },
                { "fetchFundingHistory", false },
                { "fetchFundingRate", false },
                { "fetchFundingRateHistory", false },
                { "fetchFundingRates", false },
                { "fetchIndexOHLCV", false },
                { "fetchIsolatedBorrowRate", false },
                { "fetchIsolatedBorrowRates", false },
                { "fetchL2OrderBook", false },
                { "fetchLedger", false },
                { "fetchLedgerEntry", false },
                { "fetchLeverageTiers", false },
                { "fetchMarkets", true },
                { "fetchMarkOHLCV", false },
                { "fetchMyTrades", false },
                { "fetchOHLCV", true },
                { "fetchOpenInterestHistory", false },
                { "fetchOpenOrders", false },
                { "fetchOrder", false },
                { "fetchOrderBook", true },
                { "fetchOrders", false },
                { "fetchOrderTrades", "emulated" },
                { "fetchPositions", false },
                { "fetchPremiumIndexOHLCV", false },
                { "fetchTicker", true },
                { "fetchTickers", true },
                { "fetchTime", false },
                { "fetchTrades", false },
                { "fetchTradingFee", false },
                { "fetchTradingFees", false },
                { "fetchWithdrawals", false },
                { "setLeverage", false },
                { "setMarginMode", false },
                { "transfer", false },
                { "withdraw", false },
            } },
            { "comment", "This comment is optional" },
            { "urls", new Dictionary<string, object>() {
                { "logo", "https://cdn.arz.digital/cr-odin/img/exchanges/nobitex/64x64.png" },
                { "api", new Dictionary<string, object>() {
                    { "public", "https://apiv2.nobitex.ir" },
                } },
                { "www", "https://nobitex.ir/" },
                { "doc", new List<object>() {"https://apidocs.nobitex.ir"} },
            } },
            { "timeframes", new Dictionary<string, object>() {
                { "1m", "1" },
                { "5m", "5" },
                { "15m", "15" },
                { "30m", "30" },
                { "1h", "60" },
                { "3h", "180" },
                { "4h", "240" },
                { "6h", "360" },
                { "12h", "720" },
                { "1d", "D" },
                { "2d", "2D" },
                { "3d", "3D" },
            } },
            { "api", new Dictionary<string, object>() {
                { "public", new Dictionary<string, object>() {
                    { "get", new Dictionary<string, object>() {
                        { "market/stats", 1 },
                        { "market/udf/history", 1 },
                        { "v2/orderbook", 1 },
                    } },
                } },
            } },
            { "commonCurrencies", new Dictionary<string, object>() {
                { "RLS", "IRT" },
            } },
            { "fees", new Dictionary<string, object>() {
                { "trading", new Dictionary<string, object>() {
                    { "tierBased", false },
                    { "percentage", true },
                    { "maker", this.parseNumber("0.001") },
                    { "taker", this.parseNumber("0.001") },
                } },
            } },
        });
    }

    public async override Task<object> fetchMarkets(object parameters = null)
    {
        /**
        * @method
        * @name nobitex#fetchMarkets
        * @description retrieves data on all markets for nobitex
        * @see https://apidocs.nobitex.ir/#6ae2dae4a2
        * @param {object} [params] extra parameters specific to the exchange API endpoint
        * @returns {object[]} an array of objects representing market data
        */
        parameters ??= new Dictionary<string, object>();
        object response = await this.publicGetMarketStats();
        object markets = this.safeDict(response, "stats");
        object marketKeys = new List<object>(((IDictionary<string,object>)markets).Keys);
        object result = new List<object>() {};
        for (object i = 0; isLessThan(i, getArrayLength(marketKeys)); postFixIncrement(ref i))
        {
            object symbol = getValue(marketKeys, i);
            if (isTrue(getValue(getValue(markets, symbol), "isClosed")))
            {
                continue;
            }
            ((IDictionary<string,object>)getValue(markets, symbol))["symbol"] = symbol;
            object market = this.parseMarket(getValue(markets, symbol));
            ((IList<object>)result).Add(market);
        }
        return result;
    }

    public override object parseMarket(object market)
    {
        //        btc-rls: {
        // isClosed: false,
        // bestSell: "38820000000",
        // bestBuy: "38819999960",
        // volumeSrc: "4.429488914925",
        // volumeDst: "171731356314.98355049425",
        // latest: "38819999960",
        // mark: "38883424370",
        // dayLow: "38380000000",
        // dayHigh: "39100000000",
        // dayOpen: "38800000000",
        // dayClose: "38819999960",
        // dayChange: "0.05"
        // },
        object id = this.safeString(market, "symbol");
        object symbol = this.safeStringUpper(market, "symbol");
        var baseIdquoteIdVariable = ((string)symbol).Split(new [] {((string)"-")}, StringSplitOptions.None).ToList<object>();
        var baseId = ((IList<object>) baseIdquoteIdVariable)[0];
        var quoteId = ((IList<object>) baseIdquoteIdVariable)[1];
        object bs = this.safeCurrencyCode(baseId);
        object quote = this.safeCurrencyCode(quoteId);
        baseId = ((string)baseId).ToLower();
        quoteId = ((string)quoteId).ToLower();
        return new Dictionary<string, object>() {
            { "id", id },
            { "symbol", add(add(bs, "/"), quote) },
            { "base", bs },
            { "quote", quote },
            { "settle", null },
            { "baseId", baseId },
            { "quoteId", quoteId },
            { "settleId", null },
            { "type", "spot" },
            { "spot", true },
            { "margin", false },
            { "swap", false },
            { "future", false },
            { "option", false },
            { "active", true },
            { "contract", false },
            { "linear", null },
            { "inverse", null },
            { "contractSize", null },
            { "expiry", null },
            { "expiryDatetime", null },
            { "strike", null },
            { "optionType", null },
            { "precision", new Dictionary<string, object>() {
                { "amount", null },
                { "price", null },
            } },
            { "limits", new Dictionary<string, object>() {
                { "leverage", new Dictionary<string, object>() {
                    { "min", null },
                    { "max", null },
                } },
                { "amount", new Dictionary<string, object>() {
                    { "min", null },
                    { "max", null },
                } },
                { "price", new Dictionary<string, object>() {
                    { "min", null },
                    { "max", null },
                } },
                { "cost", new Dictionary<string, object>() {
                    { "min", null },
                    { "max", null },
                } },
            } },
            { "created", null },
            { "info", market },
        };
    }

    public async override Task<object> fetchTickers(object symbols = null, object parameters = null)
    {
        /**
        * @method
        * @name nobitex#fetchTickers
        * @description fetches price tickers for multiple markets, statistical information calculated over the past 24 hours for each market
        * @see https://apidocs.nobitex.ir/#6ae2dae4a2
        * @param {string[]|undefined} symbols unified symbols of the markets to fetch the ticker for, all market tickers are returned if not assigned
        * @param {object} [params] extra parameters specific to the exchange API endpoint
        * @returns {object} a dictionary of [ticker structures]{@link https://docs.ccxt.com/#/?id=ticker-structure}
        */
        parameters ??= new Dictionary<string, object>();
        await this.loadMarkets();
        if (isTrue(!isEqual(symbols, null)))
        {
            symbols = this.marketSymbols(symbols);
        }
        object response = await this.publicGetMarketStats();
        object markets = this.safeDict(response, "stats");
        object marketKeys = new List<object>(((IDictionary<string,object>)markets).Keys);
        object result = new Dictionary<string, object>() {};
        for (object i = 0; isLessThan(i, getArrayLength(marketKeys)); postFixIncrement(ref i))
        {
            object symbol = getValue(marketKeys, i);
            if (isTrue(getValue(getValue(markets, symbol), "isClosed")))
            {
                continue;
            }
            ((IDictionary<string,object>)getValue(markets, symbol))["symbol"] = symbol;
            object ticker = this.parseTicker(getValue(markets, symbol));
            symbol = getValue(ticker, "symbol");
            ((IDictionary<string,object>)result)[(string)symbol] = ticker;
        }
        return this.filterByArrayTickers(result, "symbol", symbols);
    }

    public async override Task<object> fetchTicker(object symbol, object parameters = null)
    {
        /**
        * @method
        * @name nobitex#fetchTicker
        * @description fetches a price ticker, a statistical calculation with the information calculated over the past 24 hours for a specific market
        * @see https://apidocs.nobitex.ir/#6ae2dae4a2
        * @param {string} symbol unified symbol of the market to fetch the ticker for
        * @param {object} [params] extra parameters specific to the exchange API endpoint
        * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/#/?id=ticker-structure}
        */
        parameters ??= new Dictionary<string, object>();
        object ticker = await this.fetchTickers(new List<object>() {symbol});
        return getValue(ticker, symbol);
    }

    public override object parseTicker(object ticker, object market = null)
    {
        //        {
        // symbol: "btc-rls",
        // isClosed: false,
        // bestSell: "112800000000",
        // bestBuy: "112790000000",
        // volumeSrc: "8.4585865026",
        // volumeDst: "952507404308.593429632",
        // latest: "112800000000",
        // mark: "112770303030",
        // dayLow: "111700000000",
        // dayHigh: "114608341550",
        // dayOpen: "112783203040",
        // dayClose: "112800000000",
        // dayChange: "0.01"
        // }
        object marketType = "spot";
        object rawSymbol = this.safeStringLower(ticker, "symbol");
        object parts = ((string)rawSymbol).Split(new [] {((string)"-")}, StringSplitOptions.None).ToList<object>();
        object baseId = getValue(parts, 0);
        object quoteId = getValue(parts, 1);
        if (isTrue(isEqual(quoteId, "rls")))
        {
            quoteId = "irt";
        }
        object bs = this.safeCurrencyCode(((string)baseId).ToUpper());
        object quote = this.safeCurrencyCode(((string)quoteId).ToUpper());
        object marketId = add(add(bs, "/"), quote);
        object marketinfo = this.market(marketId);
        object symbol = this.safeSymbol(marketId, market, null, marketType);
        object high = this.safeFloat(ticker, "dayHigh");
        object low = this.safeFloat(ticker, "dayLow");
        object bid = this.safeFloat(ticker, "bestBuy");
        object ask = this.safeFloat(ticker, "bestSell");
        object open = this.safeFloat(ticker, "dayOpen");
        object close = this.safeFloat(ticker, "dayClose");
        object change = this.safeFloat(ticker, "dayChange");
        object last = this.safeFloat(ticker, "latest");
        object quoteVolume = this.safeFloat(ticker, "volumeDst");
        object baseVolume = this.safeFloat(ticker, "volumeSrc");
        // adjust Nobitex IRT scaling
        if (isTrue(isEqual(getValue(marketinfo, "quote"), "IRT")))
        {
            high = ((bool) isTrue(high)) ? divide(high, 10) : 0;
            low = ((bool) isTrue(low)) ? divide(low, 10) : 0;
            bid = ((bool) isTrue(bid)) ? divide(bid, 10) : 0;
            ask = ((bool) isTrue(ask)) ? divide(ask, 10) : 0;
            open = ((bool) isTrue(open)) ? divide(open, 10) : 0;
            close = ((bool) isTrue(close)) ? divide(close, 10) : 0;
            last = ((bool) isTrue(last)) ? divide(last, 10) : 0;
            quoteVolume = ((bool) isTrue(quoteVolume)) ? divide(quoteVolume, 10) : 0;
        }
        return this.safeTicker(new Dictionary<string, object>() {
            { "symbol", symbol },
            { "timestamp", null },
            { "datetime", null },
            { "high", high },
            { "low", low },
            { "bid", bid },
            { "bidVolume", null },
            { "ask", ask },
            { "askVolume", null },
            { "vwap", null },
            { "open", open },
            { "close", close },
            { "last", last },
            { "previousClose", null },
            { "change", change },
            { "percentage", null },
            { "average", null },
            { "baseVolume", baseVolume },
            { "quoteVolume", quoteVolume },
            { "info", ticker },
        }, market);
    }

    public async override Task<object> fetchOHLCV(object symbol, object timeframe = null, object since = null, object limit = null, object parameters = null)
    {
        /**
        * @method
        * @name nobitex#fetchOHLCV
        * @description fetches historical candlestick data containing the open, high, low, and close price, and the volume of a market
        * @see https://apidocs.nobitex.ir/#ohlc
        * @param {string} symbol unified symbol of the market to fetch OHLCV data for
        * @param {string} timeframe the length of time each candle represents
        * @param {int} [since] timestamp in ms of the earliest candle to fetch
        * @param {int} [limit] the maximum amount of candles to fetch
        * @param {object} [params] extra parameters specific to the exchange API endpoint
        * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
        */
        timeframe ??= "1m";
        parameters ??= new Dictionary<string, object>();
        await this.loadMarkets();
        object market = this.market(symbol);
        object endTime = (new DateTimeOffset(DateTime.UtcNow)).ToUnixTimeMilliseconds();
        if (isTrue(isEqual(getValue(market, "quote"), "IRT")))
        {
            ((IDictionary<string,object>)market)["id"] = ((string)getValue(market, "symbol")).Replace((string)"/", (string)"");
        }
        object request = new Dictionary<string, object>() {
            { "symbol", getValue(market, "id") },
            { "from", subtract((divide(endTime, 1000)), (multiply(multiply(24, 60), 60))) },
            { "to", divide(endTime, 1000) },
            { "resolution", this.safeString(this.timeframes, timeframe, timeframe) },
        };
        if (isTrue(!isEqual(since, null)))
        {
            ((IDictionary<string,object>)request)["from"] = divide(since, 1000);
        }
        ((IDictionary<string,object>)request)["from"] = this.safeInteger(request, "from");
        ((IDictionary<string,object>)request)["to"] = this.safeInteger(request, "to");
        if (isTrue(!isEqual(timeframe, null)))
        {
            ((IDictionary<string,object>)request)["resolution"] = this.safeString(this.timeframes, timeframe, timeframe);
        }
        object response = await this.publicGetMarketUdfHistory(request);
        object openList = this.safeValue(response, "o", new List<object>() {});
        object highList = this.safeList(response, "h", new List<object>() {});
        object lowList = this.safeList(response, "l", new List<object>() {});
        object closeList = this.safeList(response, "c", new List<object>() {});
        object volumeList = this.safeList(response, "v", new List<object>() {});
        object timestampList = this.safeList(response, "t", new List<object>() {});
        object ohlcvs = new List<object>() {};
        for (object i = 0; isLessThan(i, getArrayLength(openList)); postFixIncrement(ref i))
        {
            if (isTrue(isEqual(getValue(market, "quote"), "IRT")))
            {
                ((List<object>)openList)[Convert.ToInt32(i)] = ((bool) isTrue(getValue(openList, i))) ? divide(getValue(openList, i), 10) : 0;
                ((List<object>)highList)[Convert.ToInt32(i)] = ((bool) isTrue(getValue(highList, i))) ? divide(getValue(highList, i), 10) : 0;
                ((List<object>)lowList)[Convert.ToInt32(i)] = ((bool) isTrue(getValue(lowList, i))) ? divide(getValue(lowList, i), 10) : 0;
                ((List<object>)closeList)[Convert.ToInt32(i)] = ((bool) isTrue(getValue(closeList, i))) ? divide(getValue(closeList, i), 10) : 0;
                ((List<object>)volumeList)[Convert.ToInt32(i)] = ((bool) isTrue(getValue(volumeList, i))) ? divide(getValue(volumeList, i), 10) : 0;
            }
            ((IList<object>)ohlcvs).Add(new List<object>() {getValue(timestampList, i), getValue(openList, i), getValue(highList, i), getValue(lowList, i), getValue(closeList, i), getValue(volumeList, i)});
        }
        return this.parseOHLCVs(ohlcvs, market, timeframe, since, limit);
    }

    public async override Task<object> fetchOrderBook(object symbol, object limit = null, object parameters = null)
    {
        /**
        * @method
        * @name nobitex#fetchOrderBooks
        * @description fetches information on open orders with bid (buy) and ask (sell) prices, volumes and other data for multiple markets
        * @see https://apidocs.nobitex.ir/#orderbook
        * @param {string[]|undefined} symbols list of unified market symbols, all symbols fetched if undefined, default is undefined
        * @param {int} [limit] max number of entries per orderbook to return, default is undefined
        * @param {object} [params] extra parameters specific to the exchange API endpoint
        * @returns {object} a dictionary of [order book structures]{@link https://docs.ccxt.com/#/?id=order-book-structure} indexed by market symbol
        */
        parameters ??= new Dictionary<string, object>();
        await this.loadMarkets();
        object market = this.market(symbol);
        object request = new Dictionary<string, object>() {
            { "symbol", ((string)symbol).Replace((string)"/", (string)"") },
        };
        object response = await this.publicGetV2Orderbook(request);
        if (isTrue(isEqual(getValue(market, "quote"), "IRT")))
        {
            object bids = this.safeList(response, "bids");
            object asks = this.safeList(response, "asks");
            for (object i = 0; isLessThan(i, getArrayLength(bids)); postFixIncrement(ref i))
            {
                ((List<object>)getValue(bids, i))[Convert.ToInt32(0)] = ((bool) isTrue(getValue(getValue(bids, i), 0))) ? divide(getValue(getValue(bids, i), 0), 10) : 0;
            }
            for (object i = 0; isLessThan(i, getArrayLength(asks)); postFixIncrement(ref i))
            {
                ((List<object>)getValue(asks, i))[Convert.ToInt32(0)] = ((bool) isTrue(getValue(getValue(asks, i), 0))) ? divide(getValue(getValue(asks, i), 0), 10) : 0;
            }
            ((IDictionary<string,object>)response)["bids"] = bids;
            ((IDictionary<string,object>)response)["asks"] = asks;
        }
        object timestamp = this.safeInteger(response, "lastUpdate");
        return this.parseOrderBook(response, symbol, timestamp);
    }

    public override object sign(object path, object api = null, object method = null, object parameters = null, object headers = null, object body = null)
    {
        api ??= "public";
        method ??= "GET";
        parameters ??= new Dictionary<string, object>();
        object query = this.omit(parameters, this.extractParams(path));
        object url = add(add(getValue(getValue(this.urls, "api"), "public"), "/"), path);
        if (isTrue(isEqual(path, "market/udf/history")))
        {
            url = add(add(add(add(getValue(getValue(this.urls, "api"), "public"), "/"), path), "?"), this.urlencode(query));
        }
        if (isTrue(isEqual(path, "market/stats")))
        {
            url = add(add(url, "?"), this.urlencode(query));
        }
        if (isTrue(isEqual(path, "v2/orderbook")))
        {
            url = add(add(url, "/"), getValue(parameters, "symbol"));
        }
        headers = new Dictionary<string, object>() {
            { "Content-Type", "application/json" },
        };
        return new Dictionary<string, object>() {
            { "url", url },
            { "method", method },
            { "body", body },
            { "headers", headers },
        };
    }
}
