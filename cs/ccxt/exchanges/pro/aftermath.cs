namespace ccxt.pro;

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code


public partial class aftermath { public aftermath(object args = null) : base(args) { } }
public partial class aftermath : ccxt.aftermath
{
    public override object describe()
    {
        return this.deepExtend(base.describe(), new Dictionary<string, object>() {
            { "has", new Dictionary<string, object>() {
                { "ws", true },
                { "watchBalance", false },
                { "watchMyTrades", false },
                { "watchOHLCV", false },
                { "watchOrderBook", true },
                { "watchOrders", false },
                { "watchTicker", false },
                { "watchTickers", false },
                { "watchBidsAsks", false },
                { "watchTrades", true },
                { "watchTradesForSymbols", false },
                { "watchPositions", true },
            } },
            { "urls", new Dictionary<string, object>() {
                { "api", new Dictionary<string, object>() {
                    { "ws", new Dictionary<string, object>() {
                        { "swap", "wss://aftermath.finance/iperps-api/ccxt/stream" },
                    } },
                } },
                { "test", new Dictionary<string, object>() {
                    { "ws", new Dictionary<string, object>() {
                        { "swap", "wss://testnet.aftermath.finance/iperps-api/ccxt/stream" },
                    } },
                } },
            } },
            { "options", new Dictionary<string, object>() {
                { "tradesLimit", 1000 },
                { "ordersLimit", 1000 },
                { "watchPositions", new Dictionary<string, object>() {
                    { "fetchPositionsSnapshot", true },
                    { "awaitPositionsSnapshot", true },
                } },
            } },
            { "streaming", new Dictionary<string, object>() {
                { "keepAlive", 59000 },
            } },
            { "exceptions", new Dictionary<string, object>() {} },
        });
    }

    public async virtual Task<object> watchPublic(object suffix, object messageHash, object message)
    {
        object url = add(add(getValue(getValue(getValue(this.urls, "api"), "ws"), "swap"), "/"), suffix);
        return await this.watch(url, messageHash, this.json(message), messageHash, message);
    }

    public async virtual Task<object> watchPublicMultiple(object suffix, object messageHashes, object message)
    {
        object url = add(add(getValue(getValue(getValue(this.urls, "api"), "ws"), "swap"), "/"), suffix);
        return await this.watchMultiple(url, messageHashes, this.json(message), messageHashes, message);
    }

    /**
     * @method
     * @name aftermath#watchTrades
     * @description watches information on multiple trades made in a market
     * @see https://testnet.aftermath.finance/docs/#/CCXT/service%3A%3Ahandlers%3A%3Accxt%3A%3Astream%3A%3Atrades
     * @param {string} symbol unified market symbol of the market trades were made in
     * @param {int} [since] the earliest time in ms to fetch trades for
     * @param {int} [limit] the maximum number of trade structures to retrieve
     * @param {object} [params] extra parameters specific to the exchange API endpoint
     * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/#/?id=trade-structure}
     */
    public async override Task<object> watchTrades(object symbol, object since = null, object limit = null, object parameters = null)
    {
        parameters ??= new Dictionary<string, object>();
        await this.loadMarkets();
        object market = this.market(symbol);
        symbol = getValue(market, "symbol");
        object topic = add(getValue(market, "id"), "@trade");
        object request = new Dictionary<string, object>() {
            { "chId", getValue(market, "id") },
        };
        object message = this.extend(request, parameters);
        object trades = await this.watchPublic("trades", topic, message);
        if (isTrue(this.newUpdates))
        {
            limit = callDynamically(trades, "getLimit", new object[] {getValue(market, "symbol"), limit});
        }
        return this.filterBySymbolSinceLimit(trades, symbol, since, limit, true);
    }

    public virtual void handleTrade(WebSocketClient client, object message)
    {
        //
        // {
        //     "amount": 0.1,
        //     "cost": 0.1,
        //     "datetime": "string",
        //     "fee": null,
        //     "id": "string",
        //     "order": "string",
        //     "price": 0.1,
        //     "side": null,
        //     "symbol": "string",
        //     "takerOrMaker": null,
        //     "timestamp": null,
        //     "type": "string"
        // }
        //
        object trade = this.parseTrade(message);
        object symbol = getValue(trade, "symbol");
        object market = this.market(symbol);
        if (!isTrue((inOp(this.trades, symbol))))
        {
            object limit = this.safeInteger(this.options, "tradesLimit", 1000);
            var stored = new ArrayCache(limit);
            ((IDictionary<string,object>)this.trades)[(string)symbol] = stored;
        }
        object messageHash = add(getValue(market, "id"), "@trade");
        object trades = getValue(this.trades, symbol);
        callDynamically(trades, "append", new object[] {trade});
        ((IDictionary<string,object>)this.trades)[(string)symbol] = trades;
        callDynamically(client as WebSocketClient, "resolve", new object[] {trades, messageHash});
    }

    /**
     * @method
     * @name aftermath#watchOrderBook
     * @see https://testnet.aftermath.finance/docs/#/CCXT/service%3A%3Ahandlers%3A%3Accxt%3A%3Astream%3A%3Aorderbook
     * @description watches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
     * @param {string} symbol unified symbol of the market to fetch the order book for
     * @param {int} [limit] the maximum amount of order book entries to return.
     * @param {object} [params] extra parameters specific to the exchange API endpoint
     * @returns {object} A dictionary of [order book structures]{@link https://docs.ccxt.com/#/?id=order-book-structure} indexed by market symbols
     */
    public async override Task<object> watchOrderBook(object symbol, object limit = null, object parameters = null)
    {
        parameters ??= new Dictionary<string, object>();
        await this.loadMarkets();
        object market = this.market(symbol);
        symbol = getValue(market, "symbol");
        object topic = add(getValue(market, "id"), "@orderbook");
        object request = new Dictionary<string, object>() {
            { "chId", getValue(market, "id") },
        };
        object message = this.extend(request, parameters);
        object orderbook = await this.watchPublic("orderbook", topic, message);
        return (orderbook as IOrderBook).limit();
    }

    public virtual void handleOrderBook(WebSocketClient client, object message)
    {
        //
        // {
        //     "asks": [
        //       []
        //     ],
        //     "bids": [
        //       []
        //     ],
        //     "datetime": "string",
        //     "nonce": 9007199254740991,
        //     "symbol": "string",
        //     "timestamp": 9007199254740991
        // }
        //
        object symbol = this.safeString(message, "symbol");
        object market = this.market(symbol);
        object topic = add(getValue(market, "id"), "@orderbook");
        if (!isTrue((inOp(this.orderbooks, symbol))))
        {
            object defaultLimit = this.safeInteger(this.options, "watchOrderBookLimit", 1000);
            object subscription = getValue(((WebSocketClient)client).subscriptions, topic);
            object limit = this.safeInteger(subscription, "limit", defaultLimit);
            ((IDictionary<string,object>)this.orderbooks)[(string)symbol] = this.orderBook(new Dictionary<string, object>() {}, limit);
            ((IDictionary<string,object>)subscription)["limit"] = limit;
            this.spawn(this.fetchOrderBookSnapshot, new object[] { client, message, subscription});
        } else
        {
            object orderbook = getValue(this.orderbooks, symbol);
            object prevNonce = this.safeInteger(orderbook, "nonce");
            object nonce = this.safeInteger(message, "nonce");
            if (isTrue(isEqual(nonce, (add(prevNonce, 1)))))
            {
                this.handleOrderBookMessage(client as WebSocketClient, message, orderbook);
                callDynamically(client as WebSocketClient, "resolve", new object[] {orderbook, topic});
            }
        }
    }

    public async virtual Task fetchOrderBookSnapshot(WebSocketClient client, object message, object subscription)
    {
        object symbol = this.safeString(message, "symbol");
        object market = this.market(symbol);
        object messageHash = add(getValue(market, "id"), "@orderbook");
        try
        {
            object defaultLimit = this.safeInteger(this.options, "watchOrderBookLimit", 1000);
            object limit = this.safeInteger(subscription, "limit", defaultLimit);
            object parameters = this.safeDict(subscription, "params");
            object snapshot = await this.fetchRestOrderBookSafe(symbol, limit, parameters);
            if (isTrue(isEqual(this.safeValue(this.orderbooks, symbol), null)))
            {
                // if the orderbook is dropped before the snapshot is received
                return;
            }
            object orderbook = getValue(this.orderbooks, symbol);
            (orderbook as IOrderBook).reset(snapshot);
            ((IDictionary<string,object>)this.orderbooks)[(string)symbol] = orderbook;
            callDynamically(client as WebSocketClient, "resolve", new object[] {orderbook, messageHash});
        } catch(Exception e)
        {
            ((IDictionary<string,object>)((WebSocketClient)client).subscriptions).Remove((string)messageHash);
            ((WebSocketClient)client).reject(e, messageHash);
        }
    }

    public virtual object handleOrderBookMessage(WebSocketClient client, object message, object orderbook)
    {
        this.handleDeltas(getValue(orderbook, "asks"), this.safeValue(message, "asks", new List<object>() {}));
        this.handleDeltas(getValue(orderbook, "bids"), this.safeValue(message, "bids", new List<object>() {}));
        object timestamp = this.safeInteger(message, "timestamp");
        object nonce = this.safeInteger(message, "nonce");
        ((IDictionary<string,object>)orderbook)["timestamp"] = timestamp;
        ((IDictionary<string,object>)orderbook)["datetime"] = this.iso8601(timestamp);
        ((IDictionary<string,object>)orderbook)["nonce"] = nonce;
        return orderbook;
    }

    public override void handleDelta(object bookside, object delta)
    {
        object price = this.safeFloat2(delta, "price", 0);
        object amount = this.safeFloat2(delta, "quantity", 1);
        (bookside as IOrderBookSide).store(price, amount);
    }

    public override void handleDeltas(object bookside, object deltas)
    {
        for (object i = 0; isLessThan(i, getArrayLength(deltas)); postFixIncrement(ref i))
        {
            this.handleDelta(bookside, getValue(deltas, i));
        }
    }

    /**
     * @method
     * @name aftermath#watchPositions
     * @see https://testnet.aftermath.finance/docs/#/CCXT/service%3A%3Ahandlers%3A%3Accxt%3A%3Astream%3A%3Apositions
     * @description watch all open positions
     * @param {string[]|undefined} symbols list of unified market symbols
     * @param {int} [since] the earliest time in ms to fetch positions for
     * @param {int} [limit] the maximum number of position structures to retrieve
     * @param {object} params extra parameters specific to the exchange API endpoint
     * @param {int} [params.accountNumber] account number to query orders for, required
     * @returns {object[]} a list of [position structure]{@link https://docs.ccxt.com/en/latest/manual.html#position-structure}
     */
    public async override Task<object> watchPositions(object symbols = null, object since = null, object limit = null, object parameters = null)
    {
        parameters ??= new Dictionary<string, object>();
        await this.loadMarkets();
        object messageHashes = new List<object>() {};
        symbols = this.marketSymbols(symbols);
        if (!isTrue(this.isEmpty(symbols)))
        {
            for (object i = 0; isLessThan(i, getArrayLength(symbols)); postFixIncrement(ref i))
            {
                object symbol = getValue(symbols, i);
                ((IList<object>)messageHashes).Add(add("positions::", symbol));
            }
        } else
        {
            ((IList<object>)messageHashes).Add("positions");
        }
        object accountNumber = this.safeNumber(parameters, "accountNumber");
        object request = new Dictionary<string, object>() {
            { "accountNumber", accountNumber },
        };
        object message = this.extend(request, parameters);
        object suffix = "positions";
        object url = add(add(getValue(getValue(getValue(this.urls, "api"), "ws"), "swap"), "/"), suffix);
        var client = this.client(url);
        this.setPositionsCache(client as WebSocketClient, symbols, parameters);
        object fetchPositionsSnapshot = this.handleOption("watchPositions", "fetchPositionsSnapshot", true);
        object awaitPositionsSnapshot = this.handleOption("watchPositions", "awaitPositionsSnapshot", true);
        if (isTrue(isTrue(isTrue(fetchPositionsSnapshot) && isTrue(awaitPositionsSnapshot)) && isTrue(isEqual(this.positions, null))))
        {
            object snapshot = await client.future("fetchPositionsSnapshot");
            return this.filterBySymbolsSinceLimit(snapshot, symbols, since, limit, true);
        }
        object newPositions = await this.watchPublicMultiple(suffix, messageHashes, message);
        if (isTrue(this.newUpdates))
        {
            return newPositions;
        }
        return this.filterBySymbolsSinceLimit(this.positions, symbols, since, limit, true);
    }

    public virtual void setPositionsCache(WebSocketClient client, object symbols = null, object parameters = null)
    {
        parameters ??= new Dictionary<string, object>();
        object fetchPositionsSnapshot = this.handleOption("watchPositions", "fetchPositionsSnapshot", false);
        if (isTrue(fetchPositionsSnapshot))
        {
            object messageHash = "fetchPositionsSnapshot";
            if (!isTrue((inOp(client.futures, messageHash))))
            {
                client.future(messageHash);
                this.spawn(this.loadPositionsSnapshot, new object[] { client, messageHash, symbols, parameters});
            }
        } else
        {
            this.positions = new ArrayCacheBySymbolBySide();
        }
    }

    public async virtual Task loadPositionsSnapshot(WebSocketClient client, object messageHash, object symbols, object parameters)
    {
        object positions = await this.fetchPositions(symbols, parameters);
        this.positions = new ArrayCacheBySymbolBySide();
        object cache = this.positions;
        for (object i = 0; isLessThan(i, getArrayLength(positions)); postFixIncrement(ref i))
        {
            object position = getValue(positions, i);
            callDynamically(cache, "append", new object[] {position});
        }
        // don't remove the future from the .futures cache
        var future = getValue(client.futures, messageHash);
        (future as Future).resolve(cache);
        callDynamically(client as WebSocketClient, "resolve", new object[] {cache, "positions"});
    }

    public virtual void handlePositions(WebSocketClient client, object message)
    {
        //
        // {
        //     "collateral": 0.1,
        //     "contractSize": 0.1,
        //     "contracts": 0.1,
        //     "datetime": "string",
        //     "entryPrice": 0.1,
        //     "id": "0x895037c09dd1025a136b5a5789c4ea2481176adf4c3b0c3521d5d2039bdfc3ba:123",
        //     "initialMargin": 0.1,
        //     "initialMarginPercentage": 0.1,
        //     "leverage": 0.1,
        //     "liquidationPrice": 0.1,
        //     "maintenanceMargin": 0.1,
        //     "maintenanceMarginPercentage": 0.1,
        //     "marginMode": "cross",
        //     "marginRatio": 0.1,
        //     "notional": 0.1,
        //     "side": "long",
        //     "symbol": "BTC/USD:USDC",
        //     "timestamp": 9007199254740991,
        //     "unrealizedPnl": 0.1
        // }
        //
        if (isTrue(isEqual(this.positions, null)))
        {
            this.positions = new ArrayCacheBySymbolBySide();
        }
        object cache = this.positions;
        object symbol = this.safeString(message, "symbol");
        object market = this.safeMarket(symbol);
        object position = this.parsePosition(message, market);
        callDynamically(cache, "append", new object[] {position});
        object messageHash = add("positions::", getValue(market, "symbol"));
        callDynamically(client as WebSocketClient, "resolve", new object[] {position, messageHash});
        callDynamically(client as WebSocketClient, "resolve", new object[] {new List<object>() {position}, "positions"});
    }

    public virtual object handleErrorMessage(WebSocketClient client, object message)
    {
        //
        // User error: Expected Message::Text from client, got Ping(b\"\")
        //
        if (isTrue((message is string)))
        {
            if (isTrue(isGreaterThanOrEqual(getIndexOf(message, "error"), 0)))
            {
                try
                {
                    object feedback = add(add(this.id, " "), message);
                    this.throwExactlyMatchedException(getValue(this.exceptions, "exact"), message, feedback);
                    throw new ExchangeError ((string)message) ;
                } catch(Exception error)
                {
                    if (isTrue(error is AuthenticationError))
                    {
                        object messageHash = "authenticated";
                        ((WebSocketClient)client).reject(error, messageHash);
                        if (isTrue(inOp(((WebSocketClient)client).subscriptions, messageHash)))
                        {
                            ((IDictionary<string,object>)((WebSocketClient)client).subscriptions).Remove((string)messageHash);
                        }
                    } else
                    {
                        ((WebSocketClient)client).reject(error);
                    }
                    return true;
                }
            }
        }
        return false;
    }

    public override void handleMessage(WebSocketClient client, object message)
    {
        if (isTrue(this.handleErrorMessage(client as WebSocketClient, message)))
        {
            return;
        }
        // const methods: Dict = {
        //     'trade': this.handleTrade,
        // };
        if (isTrue(inOp(message, "asks")))
        {
            this.handleOrderBook(client as WebSocketClient, message);
        } else if (isTrue(inOp(message, "notional")))
        {
            this.handlePositions(client as WebSocketClient, message);
        } else
        {
            this.handleTrade(client as WebSocketClient, message);
        }
    }
}
