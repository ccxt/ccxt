/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface Orders {
  orderId: bigint;
}

export interface OrderListResponse {
  orderListId: bigint;
  contingencyType: number;
  listStatusType: number;
  listOrderStatus: number;
  transactionTime: bigint;
  orders: Orders[];
  listClientOrderId: Uint8Array;
  symbol: Uint8Array;
}

export class OrderListResponseDecoder {
  private static readonly BLOCK_LENGTH = 19;
  private static readonly TEMPLATE_ID = 313;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): OrderListResponse {
    const view = new DataView(buffer);
    let pos = offset;

    const orderListId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const contingencyType = view.getUint8(pos);
    pos += 1;

    const listStatusType = view.getUint8(pos);
    pos += 1;

    const listOrderStatus = view.getUint8(pos);
    pos += 1;

    const transactionTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    // Skip to end of block for forward compatibility
    pos = offset + OrderListResponseDecoder.BLOCK_LENGTH;

    const orders = this.decodeOrdersGroup(view, pos);
    pos = orders.nextOffset;

    const listClientOrderId = this.decodeVarData(view, pos);
    pos = listClientOrderId.nextOffset;

    const symbol = this.decodeVarData(view, pos);
    pos = symbol.nextOffset;

    return {
      orderListId: orderListId,
      contingencyType: contingencyType,
      listStatusType: listStatusType,
      listOrderStatus: listOrderStatus,
      transactionTime: transactionTime,
      orders: orders.items,
      listClientOrderId: listClientOrderId.value,
      symbol: symbol.value
    };
  }
  private decodeOrdersGroup(view: DataView, offset: number): { items: Orders[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint16(pos, this.littleEndian);
    pos += 2;

    const items: Orders[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      const orderId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      // Skip to next block for forward compatibility
      pos = itemStart + blockLength;

      items.push({
        orderId: orderId
      });
    }

    return { items, nextOffset: pos };
  }

  private decodeVarData(view: DataView, offset: number): { value: Uint8Array, nextOffset: number } {
    let pos = offset;

    const length = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const value = new Uint8Array(view.buffer, view.byteOffset + pos, length);
    pos += length;

    return { value, nextOffset: pos };
  }

  static getBlockLength(): number {
    return OrderListResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return OrderListResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return OrderListResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return OrderListResponseDecoder.SCHEMA_VERSION;
  }
}
