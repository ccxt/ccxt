/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface Ticker24hSymbolFullResponse {
  priceExponent: number;
  qtyExponent: number;
  priceChange: bigint;
  priceChangePercent: number;
  weightedAvgPrice: bigint;
  prevClosePrice: bigint;
  lastPrice: bigint;
  lastQty: number;
  bidPrice: bigint;
  bidQty: bigint;
  askPrice: bigint;
  askQty: bigint;
  openPrice: bigint;
  highPrice: bigint;
  lowPrice: bigint;
  volume: number;
  quoteVolume: number;
  openTime: bigint;
  closeTime: bigint;
  firstId: bigint;
  lastId: bigint;
  numTrades: bigint;
  symbol: Uint8Array;
}

export class Ticker24hSymbolFullResponseDecoder {
  private static readonly BLOCK_LENGTH = 182;
  private static readonly TEMPLATE_ID = 205;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): Ticker24hSymbolFullResponse {
    const view = new DataView(buffer);
    let pos = offset;

    const priceExponent = view.getInt8(pos);
    pos += 1;

    const qtyExponent = view.getInt8(pos);
    pos += 1;

    const priceChange = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const priceChangePercent = view.getFloat32(pos, this.littleEndian);
    pos += 4;

    const weightedAvgPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const prevClosePrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const lastPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const lastQty = view.getUint8(pos);
    pos += 16;

    const bidPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const bidQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const askPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const askQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const openPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const highPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const lowPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const volume = view.getUint8(pos);
    pos += 16;

    const quoteVolume = view.getUint8(pos);
    pos += 16;

    const openTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const closeTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const firstId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const lastId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const numTrades = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    // Skip to end of block for forward compatibility
    pos = offset + Ticker24hSymbolFullResponseDecoder.BLOCK_LENGTH;

    const symbol = this.decodeVarData(view, pos);
    pos = symbol.nextOffset;

    return {
      priceExponent: priceExponent,
      qtyExponent: qtyExponent,
      priceChange: priceChange,
      priceChangePercent: priceChangePercent,
      weightedAvgPrice: weightedAvgPrice,
      prevClosePrice: prevClosePrice,
      lastPrice: lastPrice,
      lastQty: lastQty,
      bidPrice: bidPrice,
      bidQty: bidQty,
      askPrice: askPrice,
      askQty: askQty,
      openPrice: openPrice,
      highPrice: highPrice,
      lowPrice: lowPrice,
      volume: volume,
      quoteVolume: quoteVolume,
      openTime: openTime,
      closeTime: closeTime,
      firstId: firstId,
      lastId: lastId,
      numTrades: numTrades,
      symbol: symbol.value
    };
  }

  private decodeVarData(view: DataView, offset: number): { value: Uint8Array, nextOffset: number } {
    let pos = offset;

    const length = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const value = new Uint8Array(view.buffer, view.byteOffset + pos, length);
    pos += length;

    return { value, nextOffset: pos };
  }

  static getBlockLength(): number {
    return Ticker24hSymbolFullResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return Ticker24hSymbolFullResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return Ticker24hSymbolFullResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return Ticker24hSymbolFullResponseDecoder.SCHEMA_VERSION;
  }
}
