/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface Responses {
}

export interface CancelOpenOrdersResponse {
  responses: Responses[];
}

export class CancelOpenOrdersResponseDecoder {
  private static readonly BLOCK_LENGTH = 0;
  private static readonly TEMPLATE_ID = 306;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): CancelOpenOrdersResponse {
    const view = new DataView(buffer);
    let pos = offset;

    // Skip to end of block for forward compatibility
    pos = offset + CancelOpenOrdersResponseDecoder.BLOCK_LENGTH;

    const responses = this.decodeResponsesGroup(view, pos);
    pos = responses.nextOffset;

    return {
      responses: responses.items
    };
  }
  private decodeResponsesGroup(view: DataView, offset: number): { items: Responses[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const items: Responses[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      // Skip to next block for forward compatibility
      pos = itemStart + blockLength;

      items.push({
      });
    }

    return { items, nextOffset: pos };
  }

  static getBlockLength(): number {
    return CancelOpenOrdersResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return CancelOpenOrdersResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return CancelOpenOrdersResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return CancelOpenOrdersResponseDecoder.SCHEMA_VERSION;
  }
}
