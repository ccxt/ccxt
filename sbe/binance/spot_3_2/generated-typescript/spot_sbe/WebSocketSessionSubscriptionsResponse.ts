/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface Subscriptions {
  subscriptionId: number;
  expirationTime: bigint;
}

export interface WebSocketSessionSubscriptionsResponse {
  subscriptions: Subscriptions[];
}

export class WebSocketSessionSubscriptionsResponseDecoder {
  private static readonly BLOCK_LENGTH = 0;
  private static readonly TEMPLATE_ID = 54;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): WebSocketSessionSubscriptionsResponse {
    const view = new DataView(buffer);
    let pos = offset;

    // Skip to end of block for forward compatibility
    pos = offset + WebSocketSessionSubscriptionsResponseDecoder.BLOCK_LENGTH;

    const subscriptions = this.decodeSubscriptionsGroup(view, pos);
    pos = subscriptions.nextOffset;

    return {
      subscriptions: subscriptions.items
    };
  }
  private decodeSubscriptionsGroup(view: DataView, offset: number): { items: Subscriptions[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const items: Subscriptions[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      const subscriptionId = view.getUint16(pos, this.littleEndian);
      pos += 2;

      const expirationTime = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      // Skip to next block for forward compatibility
      pos = itemStart + blockLength;

      items.push({
        subscriptionId: subscriptionId,
        expirationTime: expirationTime
      });
    }

    return { items, nextOffset: pos };
  }

  static getBlockLength(): number {
    return WebSocketSessionSubscriptionsResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return WebSocketSessionSubscriptionsResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return WebSocketSessionSubscriptionsResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return WebSocketSessionSubscriptionsResponseDecoder.SCHEMA_VERSION;
  }
}
