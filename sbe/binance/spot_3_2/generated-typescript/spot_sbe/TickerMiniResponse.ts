/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface Tickers {
  priceExponent: number;
  qtyExponent: number;
  openPrice: bigint;
  highPrice: bigint;
  lowPrice: bigint;
  lastPrice: bigint;
  volume: number;
  quoteVolume: number;
  openTime: bigint;
  closeTime: bigint;
  firstId: bigint;
  lastId: bigint;
  numTrades: bigint;
}

export interface TickerMiniResponse {
  tickers: Tickers[];
}

export class TickerMiniResponseDecoder {
  private static readonly BLOCK_LENGTH = 0;
  private static readonly TEMPLATE_ID = 216;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): TickerMiniResponse {
    const view = new DataView(buffer);
    let pos = offset;

    // Skip to end of block for forward compatibility
    pos = offset + TickerMiniResponseDecoder.BLOCK_LENGTH;

    const tickers = this.decodeTickersGroup(view, pos);
    pos = tickers.nextOffset;

    return {
      tickers: tickers.items
    };
  }
  private decodeTickersGroup(view: DataView, offset: number): { items: Tickers[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const items: Tickers[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      const priceExponent = view.getInt8(pos);
      pos += 1;

      const qtyExponent = view.getInt8(pos);
      pos += 1;

      const openPrice = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const highPrice = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const lowPrice = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const lastPrice = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const volume = view.getUint8(pos);
      pos += 16;

      const quoteVolume = view.getUint8(pos);
      pos += 16;

      const openTime = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const closeTime = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const firstId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const lastId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const numTrades = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      // Skip to next block for forward compatibility
      pos = itemStart + blockLength;

      items.push({
        priceExponent: priceExponent,
        qtyExponent: qtyExponent,
        openPrice: openPrice,
        highPrice: highPrice,
        lowPrice: lowPrice,
        lastPrice: lastPrice,
        volume: volume,
        quoteVolume: quoteVolume,
        openTime: openTime,
        closeTime: closeTime,
        firstId: firstId,
        lastId: lastId,
        numTrades: numTrades
      });
    }

    return { items, nextOffset: pos };
  }

  static getBlockLength(): number {
    return TickerMiniResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return TickerMiniResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return TickerMiniResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return TickerMiniResponseDecoder.SCHEMA_VERSION;
  }
}
