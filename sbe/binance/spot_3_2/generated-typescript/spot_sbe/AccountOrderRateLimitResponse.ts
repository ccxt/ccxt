/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface RateLimits {
  rateLimitType: number;
  interval: number;
  intervalNum: number;
  rateLimit: bigint;
  numOrders: bigint;
}

export interface AccountOrderRateLimitResponse {
  rateLimits: RateLimits[];
}

export class AccountOrderRateLimitResponseDecoder {
  private static readonly BLOCK_LENGTH = 0;
  private static readonly TEMPLATE_ID = 402;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): AccountOrderRateLimitResponse {
    const view = new DataView(buffer);
    let pos = offset;

    // Skip to end of block for forward compatibility
    pos = offset + AccountOrderRateLimitResponseDecoder.BLOCK_LENGTH;

    const rateLimits = this.decodeRateLimitsGroup(view, pos);
    pos = rateLimits.nextOffset;

    return {
      rateLimits: rateLimits.items
    };
  }
  private decodeRateLimitsGroup(view: DataView, offset: number): { items: RateLimits[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const items: RateLimits[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      const rateLimitType = view.getUint8(pos);
      pos += 1;

      const interval = view.getUint8(pos);
      pos += 1;

      const intervalNum = view.getUint8(pos);
      pos += 1;

      const rateLimit = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const numOrders = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      // Skip to next block for forward compatibility
      pos = itemStart + blockLength;

      items.push({
        rateLimitType: rateLimitType,
        interval: interval,
        intervalNum: intervalNum,
        rateLimit: rateLimit,
        numOrders: numOrders
      });
    }

    return { items, nextOffset: pos };
  }

  static getBlockLength(): number {
    return AccountOrderRateLimitResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return AccountOrderRateLimitResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return AccountOrderRateLimitResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return AccountOrderRateLimitResponseDecoder.SCHEMA_VERSION;
  }
}
