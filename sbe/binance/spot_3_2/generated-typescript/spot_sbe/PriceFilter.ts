/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface PriceFilter {
  filterType: number;
  priceExponent: number;
  minPrice: bigint;
  maxPrice: bigint;
  tickSize: bigint;
}

export class PriceFilterDecoder {
  private static readonly BLOCK_LENGTH = 25;
  private static readonly TEMPLATE_ID = 1;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): PriceFilter {
    const view = new DataView(buffer);
    let pos = offset;

    const filterType = view.getUint8(pos);
    pos += 1;

    const priceExponent = view.getInt8(pos);
    pos += 1;

    const minPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const maxPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const tickSize = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    // Skip to end of block for forward compatibility
    pos = offset + PriceFilterDecoder.BLOCK_LENGTH;

    return {
      filterType: filterType,
      priceExponent: priceExponent,
      minPrice: minPrice,
      maxPrice: maxPrice,
      tickSize: tickSize
    };
  }

  static getBlockLength(): number {
    return PriceFilterDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return PriceFilterDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return PriceFilterDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return PriceFilterDecoder.SCHEMA_VERSION;
  }
}
