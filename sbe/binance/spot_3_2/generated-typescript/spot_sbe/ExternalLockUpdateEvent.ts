/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface ExternalLockUpdateEvent {
  eventTime: bigint;
  clearTime: bigint;
  qtyExponent: number;
  lockedQtyDelta: bigint;
  subscriptionId: number;
  asset: Uint8Array;
}

export class ExternalLockUpdateEventDecoder {
  private static readonly BLOCK_LENGTH = 27;
  private static readonly TEMPLATE_ID = 604;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): ExternalLockUpdateEvent {
    const view = new DataView(buffer);
    let pos = offset;

    const eventTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const clearTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const qtyExponent = view.getInt8(pos);
    pos += 1;

    const lockedQtyDelta = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const subscriptionId = view.getUint16(pos, this.littleEndian);
    pos += 2;

    // Skip to end of block for forward compatibility
    pos = offset + ExternalLockUpdateEventDecoder.BLOCK_LENGTH;

    const asset = this.decodeVarData(view, pos);
    pos = asset.nextOffset;

    return {
      eventTime: eventTime,
      clearTime: clearTime,
      qtyExponent: qtyExponent,
      lockedQtyDelta: lockedQtyDelta,
      subscriptionId: subscriptionId,
      asset: asset.value
    };
  }

  private decodeVarData(view: DataView, offset: number): { value: Uint8Array, nextOffset: number } {
    let pos = offset;

    const length = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const value = new Uint8Array(view.buffer, view.byteOffset + pos, length);
    pos += length;

    return { value, nextOffset: pos };
  }

  static getBlockLength(): number {
    return ExternalLockUpdateEventDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return ExternalLockUpdateEventDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return ExternalLockUpdateEventDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return ExternalLockUpdateEventDecoder.SCHEMA_VERSION;
  }
}
