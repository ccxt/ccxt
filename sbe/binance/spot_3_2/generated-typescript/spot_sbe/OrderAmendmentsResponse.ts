/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface Amendments {
  orderId: bigint;
  executionId: bigint;
  qtyExponent: number;
  origQty: bigint;
  newQty: bigint;
  time: bigint;
  symbol: string;
  origClientOrderId: string;
  newClientOrderId: string;
}

export interface OrderAmendmentsResponse {
  amendments: Amendments[];
}

export class OrderAmendmentsResponseDecoder {
  private static readonly BLOCK_LENGTH = 0;
  private static readonly TEMPLATE_ID = 316;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): OrderAmendmentsResponse {
    const view = new DataView(buffer);
    let pos = offset;

    // Skip to end of block for forward compatibility
    pos = offset + OrderAmendmentsResponseDecoder.BLOCK_LENGTH;

    const amendments = this.decodeAmendmentsGroup(view, pos);
    pos = amendments.nextOffset;

    return {
      amendments: amendments.items
    };
  }
  private decodeAmendmentsGroup(view: DataView, offset: number): { items: Amendments[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const items: Amendments[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      const orderId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const executionId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const qtyExponent = view.getInt8(pos);
      pos += 1;

      const origQty = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const newQty = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const time = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      // Skip to end of block for forward compatibility
      pos = itemStart + blockLength;

      const symbolLen = view.getUint8(pos);
      pos += 1;
      const symbol = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, symbolLen));
      pos += symbolLen;

      const origClientOrderIdLen = view.getUint8(pos);
      pos += 1;
      const origClientOrderId = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, origClientOrderIdLen));
      pos += origClientOrderIdLen;

      const newClientOrderIdLen = view.getUint8(pos);
      pos += 1;
      const newClientOrderId = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, newClientOrderIdLen));
      pos += newClientOrderIdLen;

      items.push({
        orderId: orderId,
        executionId: executionId,
        qtyExponent: qtyExponent,
        origQty: origQty,
        newQty: newQty,
        time: time,
        symbol: symbol,
        origClientOrderId: origClientOrderId,
        newClientOrderId: newClientOrderId
      });
    }

    return { items, nextOffset: pos };
  }

  static getBlockLength(): number {
    return OrderAmendmentsResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return OrderAmendmentsResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return OrderAmendmentsResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return OrderAmendmentsResponseDecoder.SCHEMA_VERSION;
  }
}
