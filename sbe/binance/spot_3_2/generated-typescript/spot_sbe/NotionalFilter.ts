/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface NotionalFilter {
  filterType: number;
  priceExponent: number;
  minNotional: bigint;
  applyMinToMarket: number;
  maxNotional: bigint;
  applyMaxToMarket: number;
  avgPriceMins: number;
}

export class NotionalFilterDecoder {
  private static readonly BLOCK_LENGTH = 23;
  private static readonly TEMPLATE_ID = 6;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): NotionalFilter {
    const view = new DataView(buffer);
    let pos = offset;

    const filterType = view.getUint8(pos);
    pos += 1;

    const priceExponent = view.getInt8(pos);
    pos += 1;

    const minNotional = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const applyMinToMarket = view.getUint8(pos);
    pos += 1;

    const maxNotional = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const applyMaxToMarket = view.getUint8(pos);
    pos += 1;

    const avgPriceMins = view.getInt32(pos, this.littleEndian);
    pos += 4;

    // Skip to end of block for forward compatibility
    pos = offset + NotionalFilterDecoder.BLOCK_LENGTH;

    return {
      filterType: filterType,
      priceExponent: priceExponent,
      minNotional: minNotional,
      applyMinToMarket: applyMinToMarket,
      maxNotional: maxNotional,
      applyMaxToMarket: applyMaxToMarket,
      avgPriceMins: avgPriceMins
    };
  }

  static getBlockLength(): number {
    return NotionalFilterDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return NotionalFilterDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return NotionalFilterDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return NotionalFilterDecoder.SCHEMA_VERSION;
  }
}
