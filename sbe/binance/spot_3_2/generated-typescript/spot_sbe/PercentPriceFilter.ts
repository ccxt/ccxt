/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface PercentPriceFilter {
  filterType: number;
  multiplierExponent: number;
  multiplierUp: bigint;
  multiplierDown: bigint;
  avgPriceMins: number;
}

export class PercentPriceFilterDecoder {
  private static readonly BLOCK_LENGTH = 21;
  private static readonly TEMPLATE_ID = 2;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): PercentPriceFilter {
    const view = new DataView(buffer);
    let pos = offset;

    const filterType = view.getUint8(pos);
    pos += 1;

    const multiplierExponent = view.getInt8(pos);
    pos += 1;

    const multiplierUp = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const multiplierDown = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const avgPriceMins = view.getInt32(pos, this.littleEndian);
    pos += 4;

    // Skip to end of block for forward compatibility
    pos = offset + PercentPriceFilterDecoder.BLOCK_LENGTH;

    return {
      filterType: filterType,
      multiplierExponent: multiplierExponent,
      multiplierUp: multiplierUp,
      multiplierDown: multiplierDown,
      avgPriceMins: avgPriceMins
    };
  }

  static getBlockLength(): number {
    return PercentPriceFilterDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return PercentPriceFilterDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return PercentPriceFilterDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return PercentPriceFilterDecoder.SCHEMA_VERSION;
  }
}
