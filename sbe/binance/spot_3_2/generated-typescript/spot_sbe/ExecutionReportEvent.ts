/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface ExecutionReportEvent {
  eventTime: bigint;
  transactTime: bigint;
  priceExponent: number;
  qtyExponent: number;
  commissionExponent: number;
  orderCreationTime: bigint;
  workingTime: bigint;
  orderId: bigint;
  orderListId: bigint;
  origQty: bigint;
  price: bigint;
  origQuoteOrderQty: bigint;
  icebergQty: bigint;
  stopPrice: bigint;
  orderType: number;
  side: number;
  timeInForce: number;
  executionType: number;
  orderStatus: number;
  tradeId: bigint;
  executionId: bigint;
  executedQty: bigint;
  cummulativeQuoteQty: bigint;
  lastQty: bigint;
  lastPrice: bigint;
  quoteQty: bigint;
  commission: bigint;
  isWorking: number;
  isMaker: number;
  isBestMatch: number;
  matchType: number;
  selfTradePreventionMode: number;
  orderCapacity: number;
  workingFloor: number;
  usedSor: number;
  allocId: bigint;
  trailingDelta: bigint;
  trailingTime: bigint;
  tradeGroupId: bigint;
  preventedQty: bigint;
  lastPreventedQty: bigint;
  preventedMatchId: bigint;
  preventedExecutionQty: bigint;
  preventedExecutionPrice: bigint;
  preventedExecutionQuoteQty: bigint;
  strategyType: number;
  strategyId: bigint;
  counterOrderId: bigint;
  subscriptionId: number;
  pegPriceType: number;
  pegOffsetType: number;
  pegOffsetValue: number;
  peggedPrice: bigint;
  symbol: string;
  clientOrderId: string;
  origClientOrderId: string;
  commissionAsset: string;
  rejectReason: string;
  counterSymbol: string;
}

export class ExecutionReportEventDecoder {
  private static readonly BLOCK_LENGTH = 281;
  private static readonly TEMPLATE_ID = 603;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): ExecutionReportEvent {
    const view = new DataView(buffer);
    let pos = offset;

    const eventTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const transactTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const priceExponent = view.getInt8(pos);
    pos += 1;

    const qtyExponent = view.getInt8(pos);
    pos += 1;

    const commissionExponent = view.getInt8(pos);
    pos += 1;

    const orderCreationTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const workingTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const orderId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const orderListId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const origQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const price = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const origQuoteOrderQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const icebergQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const stopPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const orderType = view.getUint8(pos);
    pos += 1;

    const side = view.getUint8(pos);
    pos += 1;

    const timeInForce = view.getUint8(pos);
    pos += 1;

    const executionType = view.getUint8(pos);
    pos += 1;

    const orderStatus = view.getUint8(pos);
    pos += 1;

    const tradeId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const executionId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const executedQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const cummulativeQuoteQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const lastQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const lastPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const quoteQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const commission = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const isWorking = view.getUint8(pos);
    pos += 1;

    const isMaker = view.getUint8(pos);
    pos += 1;

    const isBestMatch = view.getUint8(pos);
    pos += 1;

    const matchType = view.getUint8(pos);
    pos += 1;

    const selfTradePreventionMode = view.getUint8(pos);
    pos += 1;

    const orderCapacity = view.getUint8(pos);
    pos += 1;

    const workingFloor = view.getUint8(pos);
    pos += 1;

    const usedSor = view.getUint8(pos);
    pos += 1;

    const allocId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const trailingDelta = view.getBigUint64(pos, this.littleEndian);
    pos += 8;

    const trailingTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const tradeGroupId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const preventedQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const lastPreventedQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const preventedMatchId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const preventedExecutionQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const preventedExecutionPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const preventedExecutionQuoteQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const strategyType = view.getInt32(pos, this.littleEndian);
    pos += 4;

    const strategyId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const counterOrderId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const subscriptionId = view.getUint16(pos, this.littleEndian);
    pos += 2;

    const pegPriceType = view.getUint8(pos);
    pos += 1;

    const pegOffsetType = view.getUint8(pos);
    pos += 1;

    const pegOffsetValue = view.getUint8(pos);
    pos += 1;

    const peggedPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    // Skip to end of block for forward compatibility
    pos = offset + ExecutionReportEventDecoder.BLOCK_LENGTH;

    const symbolLen = view.getUint8(pos);
    pos += 1;
    const symbol = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, symbolLen));
    pos += symbolLen;

    const clientOrderIdLen = view.getUint8(pos);
    pos += 1;
    const clientOrderId = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, clientOrderIdLen));
    pos += clientOrderIdLen;

    const origClientOrderIdLen = view.getUint8(pos);
    pos += 1;
    const origClientOrderId = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, origClientOrderIdLen));
    pos += origClientOrderIdLen;

    const commissionAssetLen = view.getUint8(pos);
    pos += 1;
    const commissionAsset = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, commissionAssetLen));
    pos += commissionAssetLen;

    const rejectReasonLen = view.getUint8(pos);
    pos += 1;
    const rejectReason = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, rejectReasonLen));
    pos += rejectReasonLen;

    const counterSymbolLen = view.getUint8(pos);
    pos += 1;
    const counterSymbol = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, counterSymbolLen));
    pos += counterSymbolLen;

    return {
      eventTime: eventTime,
      transactTime: transactTime,
      priceExponent: priceExponent,
      qtyExponent: qtyExponent,
      commissionExponent: commissionExponent,
      orderCreationTime: orderCreationTime,
      workingTime: workingTime,
      orderId: orderId,
      orderListId: orderListId,
      origQty: origQty,
      price: price,
      origQuoteOrderQty: origQuoteOrderQty,
      icebergQty: icebergQty,
      stopPrice: stopPrice,
      orderType: orderType,
      side: side,
      timeInForce: timeInForce,
      executionType: executionType,
      orderStatus: orderStatus,
      tradeId: tradeId,
      executionId: executionId,
      executedQty: executedQty,
      cummulativeQuoteQty: cummulativeQuoteQty,
      lastQty: lastQty,
      lastPrice: lastPrice,
      quoteQty: quoteQty,
      commission: commission,
      isWorking: isWorking,
      isMaker: isMaker,
      isBestMatch: isBestMatch,
      matchType: matchType,
      selfTradePreventionMode: selfTradePreventionMode,
      orderCapacity: orderCapacity,
      workingFloor: workingFloor,
      usedSor: usedSor,
      allocId: allocId,
      trailingDelta: trailingDelta,
      trailingTime: trailingTime,
      tradeGroupId: tradeGroupId,
      preventedQty: preventedQty,
      lastPreventedQty: lastPreventedQty,
      preventedMatchId: preventedMatchId,
      preventedExecutionQty: preventedExecutionQty,
      preventedExecutionPrice: preventedExecutionPrice,
      preventedExecutionQuoteQty: preventedExecutionQuoteQty,
      strategyType: strategyType,
      strategyId: strategyId,
      counterOrderId: counterOrderId,
      subscriptionId: subscriptionId,
      pegPriceType: pegPriceType,
      pegOffsetType: pegOffsetType,
      pegOffsetValue: pegOffsetValue,
      peggedPrice: peggedPrice,
      symbol: symbol,
      clientOrderId: clientOrderId,
      origClientOrderId: origClientOrderId,
      commissionAsset: commissionAsset,
      rejectReason: rejectReason,
      counterSymbol: counterSymbol
    };
  }

  static getBlockLength(): number {
    return ExecutionReportEventDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return ExecutionReportEventDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return ExecutionReportEventDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return ExecutionReportEventDecoder.SCHEMA_VERSION;
  }
}
