/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface Orders {
  orderId: bigint;
  symbol: string;
  clientOrderId: string;
}

export interface ListStatus {
  orderListId: bigint;
  contingencyType: number;
  listOrderStatus: number;
  orders: Orders[];
  listClientOrderId: string;
  symbol: string;
}

export interface RelatedOrders {
  orderId: bigint;
  orderListId: bigint;
  price: bigint;
  qty: bigint;
  executedQty: bigint;
  preventedQty: bigint;
  cumulativeQuoteQty: bigint;
  status: number;
  timeInForce: number;
  orderType: number;
  side: number;
  stopPrice: bigint;
  trailingDelta: bigint;
  trailingTime: bigint;
  icebergQty: bigint;
  workingTime: bigint;
  strategyId: bigint;
  strategyType: number;
  orderCapacity: number;
  workingFloor: number;
  selfTradePreventionMode: number;
  usedSor: number;
  pegPriceType: number;
  pegOffsetType: number;
  pegOffsetValue: number;
  peggedPrice: bigint;
  symbol: string;
  clientOrderId: string;
}

export interface OrderAmendKeepPriorityResponse {
  transactTime: bigint;
  executionId: bigint;
  priceExponent: number;
  qtyExponent: number;
  orderId: bigint;
  orderListId: bigint;
  price: bigint;
  qty: bigint;
  executedQty: bigint;
  preventedQty: bigint;
  cumulativeQuoteQty: bigint;
  status: number;
  timeInForce: number;
  orderType: number;
  side: number;
  stopPrice: bigint;
  trailingDelta: bigint;
  trailingTime: bigint;
  icebergQty: bigint;
  workingTime: bigint;
  strategyId: bigint;
  strategyType: number;
  orderCapacity: number;
  workingFloor: number;
  selfTradePreventionMode: number;
  usedSor: number;
  pegPriceType: number;
  pegOffsetType: number;
  pegOffsetValue: number;
  peggedPrice: bigint;
  listStatus: ListStatus[];
  relatedOrders: RelatedOrders[];
  symbol: string;
  origClientOrderId: string;
  clientOrderId: string;
}

export class OrderAmendKeepPriorityResponseDecoder {
  private static readonly BLOCK_LENGTH = 145;
  private static readonly TEMPLATE_ID = 317;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): OrderAmendKeepPriorityResponse {
    const view = new DataView(buffer);
    let pos = offset;

    const transactTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const executionId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const priceExponent = view.getInt8(pos);
    pos += 1;

    const qtyExponent = view.getInt8(pos);
    pos += 1;

    const orderId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const orderListId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const price = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const qty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const executedQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const preventedQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const cumulativeQuoteQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const status = view.getUint8(pos);
    pos += 1;

    const timeInForce = view.getUint8(pos);
    pos += 1;

    const orderType = view.getUint8(pos);
    pos += 1;

    const side = view.getUint8(pos);
    pos += 1;

    const stopPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const trailingDelta = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const trailingTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const icebergQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const workingTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const strategyId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const strategyType = view.getInt32(pos, this.littleEndian);
    pos += 4;

    const orderCapacity = view.getUint8(pos);
    pos += 1;

    const workingFloor = view.getUint8(pos);
    pos += 1;

    const selfTradePreventionMode = view.getUint8(pos);
    pos += 1;

    const usedSor = view.getUint8(pos);
    pos += 1;

    const pegPriceType = view.getUint8(pos);
    pos += 1;

    const pegOffsetType = view.getUint8(pos);
    pos += 1;

    const pegOffsetValue = view.getUint8(pos);
    pos += 1;

    const peggedPrice = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    // Skip to end of block for forward compatibility
    pos = offset + OrderAmendKeepPriorityResponseDecoder.BLOCK_LENGTH;

    const listStatus = this.decodeListStatusGroup(view, pos);
    pos = listStatus.nextOffset;

    const relatedOrders = this.decodeRelatedOrdersGroup(view, pos);
    pos = relatedOrders.nextOffset;

    const symbolLen = view.getUint8(pos);
    pos += 1;
    const symbol = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, symbolLen));
    pos += symbolLen;

    const origClientOrderIdLen = view.getUint8(pos);
    pos += 1;
    const origClientOrderId = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, origClientOrderIdLen));
    pos += origClientOrderIdLen;

    const clientOrderIdLen = view.getUint8(pos);
    pos += 1;
    const clientOrderId = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, clientOrderIdLen));
    pos += clientOrderIdLen;

    return {
      transactTime: transactTime,
      executionId: executionId,
      priceExponent: priceExponent,
      qtyExponent: qtyExponent,
      orderId: orderId,
      orderListId: orderListId,
      price: price,
      qty: qty,
      executedQty: executedQty,
      preventedQty: preventedQty,
      cumulativeQuoteQty: cumulativeQuoteQty,
      status: status,
      timeInForce: timeInForce,
      orderType: orderType,
      side: side,
      stopPrice: stopPrice,
      trailingDelta: trailingDelta,
      trailingTime: trailingTime,
      icebergQty: icebergQty,
      workingTime: workingTime,
      strategyId: strategyId,
      strategyType: strategyType,
      orderCapacity: orderCapacity,
      workingFloor: workingFloor,
      selfTradePreventionMode: selfTradePreventionMode,
      usedSor: usedSor,
      pegPriceType: pegPriceType,
      pegOffsetType: pegOffsetType,
      pegOffsetValue: pegOffsetValue,
      peggedPrice: peggedPrice,
      listStatus: listStatus.items,
      relatedOrders: relatedOrders.items,
      symbol: symbol,
      origClientOrderId: origClientOrderId,
      clientOrderId: clientOrderId
    };
  }
  private decodeListStatusGroup(view: DataView, offset: number): { items: ListStatus[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint16(pos, this.littleEndian);
    pos += 2;

    const items: ListStatus[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      const orderListId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const contingencyType = view.getUint8(pos);
      pos += 1;

      const listOrderStatus = view.getUint8(pos);
      pos += 1;

      // Skip to end of block for forward compatibility
      pos = itemStart + blockLength;

      const orders = this.decodeOrdersGroup(view, pos);
      pos = orders.nextOffset;

      const listClientOrderIdLen = view.getUint8(pos);
      pos += 1;
      const listClientOrderId = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, listClientOrderIdLen));
      pos += listClientOrderIdLen;

      const symbolLen = view.getUint8(pos);
      pos += 1;
      const symbol = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, symbolLen));
      pos += symbolLen;

      items.push({
        orderListId: orderListId,
        contingencyType: contingencyType,
        listOrderStatus: listOrderStatus,
        orders: orders.items,
        listClientOrderId: listClientOrderId,
        symbol: symbol
      });
    }

    return { items, nextOffset: pos };
  }
  private decodeOrdersGroup(view: DataView, offset: number): { items: Orders[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint16(pos, this.littleEndian);
    pos += 2;

    const items: Orders[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      const orderId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      // Skip to end of block for forward compatibility
      pos = itemStart + blockLength;

      const symbolLen = view.getUint8(pos);
      pos += 1;
      const symbol = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, symbolLen));
      pos += symbolLen;

      const clientOrderIdLen = view.getUint8(pos);
      pos += 1;
      const clientOrderId = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, clientOrderIdLen));
      pos += clientOrderIdLen;

      items.push({
        orderId: orderId,
        symbol: symbol,
        clientOrderId: clientOrderId
      });
    }

    return { items, nextOffset: pos };
  }
  private decodeRelatedOrdersGroup(view: DataView, offset: number): { items: RelatedOrders[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint16(pos, this.littleEndian);
    pos += 2;

    const items: RelatedOrders[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      const orderId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const orderListId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const price = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const qty = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const executedQty = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const preventedQty = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const cumulativeQuoteQty = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const status = view.getUint8(pos);
      pos += 1;

      const timeInForce = view.getUint8(pos);
      pos += 1;

      const orderType = view.getUint8(pos);
      pos += 1;

      const side = view.getUint8(pos);
      pos += 1;

      const stopPrice = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const trailingDelta = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const trailingTime = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const icebergQty = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const workingTime = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const strategyId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const strategyType = view.getInt32(pos, this.littleEndian);
      pos += 4;

      const orderCapacity = view.getUint8(pos);
      pos += 1;

      const workingFloor = view.getUint8(pos);
      pos += 1;

      const selfTradePreventionMode = view.getUint8(pos);
      pos += 1;

      const usedSor = view.getUint8(pos);
      pos += 1;

      const pegPriceType = view.getUint8(pos);
      pos += 1;

      const pegOffsetType = view.getUint8(pos);
      pos += 1;

      const pegOffsetValue = view.getUint8(pos);
      pos += 1;

      const peggedPrice = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      // Skip to end of block for forward compatibility
      pos = itemStart + blockLength;

      const symbolLen = view.getUint8(pos);
      pos += 1;
      const symbol = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, symbolLen));
      pos += symbolLen;

      const clientOrderIdLen = view.getUint8(pos);
      pos += 1;
      const clientOrderId = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, clientOrderIdLen));
      pos += clientOrderIdLen;

      items.push({
        orderId: orderId,
        orderListId: orderListId,
        price: price,
        qty: qty,
        executedQty: executedQty,
        preventedQty: preventedQty,
        cumulativeQuoteQty: cumulativeQuoteQty,
        status: status,
        timeInForce: timeInForce,
        orderType: orderType,
        side: side,
        stopPrice: stopPrice,
        trailingDelta: trailingDelta,
        trailingTime: trailingTime,
        icebergQty: icebergQty,
        workingTime: workingTime,
        strategyId: strategyId,
        strategyType: strategyType,
        orderCapacity: orderCapacity,
        workingFloor: workingFloor,
        selfTradePreventionMode: selfTradePreventionMode,
        usedSor: usedSor,
        pegPriceType: pegPriceType,
        pegOffsetType: pegOffsetType,
        pegOffsetValue: pegOffsetValue,
        peggedPrice: peggedPrice,
        symbol: symbol,
        clientOrderId: clientOrderId
      });
    }

    return { items, nextOffset: pos };
  }

  static getBlockLength(): number {
    return OrderAmendKeepPriorityResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return OrderAmendKeepPriorityResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return OrderAmendKeepPriorityResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return OrderAmendKeepPriorityResponseDecoder.SCHEMA_VERSION;
  }
}
