/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface Klines {
  openTime: bigint;
  openPrice: bigint;
  highPrice: bigint;
  lowPrice: bigint;
  closePrice: bigint;
  volume: number;
  closeTime: bigint;
  quoteVolume: number;
  numTrades: bigint;
  takerBuyBaseVolume: number;
  takerBuyQuoteVolume: number;
}

export interface KlinesResponse {
  priceExponent: number;
  qtyExponent: number;
  klines: Klines[];
}

export class KlinesResponseDecoder {
  private static readonly BLOCK_LENGTH = 2;
  private static readonly TEMPLATE_ID = 203;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): KlinesResponse {
    const view = new DataView(buffer);
    let pos = offset;

    const priceExponent = view.getInt8(pos);
    pos += 1;

    const qtyExponent = view.getInt8(pos);
    pos += 1;

    // Skip to end of block for forward compatibility
    pos = offset + KlinesResponseDecoder.BLOCK_LENGTH;

    const klines = this.decodeKlinesGroup(view, pos);
    pos = klines.nextOffset;

    return {
      priceExponent: priceExponent,
      qtyExponent: qtyExponent,
      klines: klines.items
    };
  }
  private decodeKlinesGroup(view: DataView, offset: number): { items: Klines[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const items: Klines[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      const openTime = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const openPrice = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const highPrice = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const lowPrice = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const closePrice = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const volume = view.getUint8(pos);
      pos += 16;

      const closeTime = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const quoteVolume = view.getUint8(pos);
      pos += 16;

      const numTrades = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const takerBuyBaseVolume = view.getUint8(pos);
      pos += 16;

      const takerBuyQuoteVolume = view.getUint8(pos);
      pos += 16;

      // Skip to next block for forward compatibility
      pos = itemStart + blockLength;

      items.push({
        openTime: openTime,
        openPrice: openPrice,
        highPrice: highPrice,
        lowPrice: lowPrice,
        closePrice: closePrice,
        volume: volume,
        closeTime: closeTime,
        quoteVolume: quoteVolume,
        numTrades: numTrades,
        takerBuyBaseVolume: takerBuyBaseVolume,
        takerBuyQuoteVolume: takerBuyQuoteVolume
      });
    }

    return { items, nextOffset: pos };
  }

  static getBlockLength(): number {
    return KlinesResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return KlinesResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return KlinesResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return KlinesResponseDecoder.SCHEMA_VERSION;
  }
}
