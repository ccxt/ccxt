/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface NewOrderAckResponse {
  orderId: bigint;
  orderListId: bigint;
  transactTime: bigint;
  symbol: Uint8Array;
  clientOrderId: Uint8Array;
}

export class NewOrderAckResponseDecoder {
  private static readonly BLOCK_LENGTH = 24;
  private static readonly TEMPLATE_ID = 300;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 2;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): NewOrderAckResponse {
    const view = new DataView(buffer);
    let pos = offset;

    const orderId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const orderListId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const transactTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    // Skip to end of block for forward compatibility
    pos = offset + NewOrderAckResponseDecoder.BLOCK_LENGTH;

    const symbol = this.decodeVarData(view, pos);
    pos = symbol.nextOffset;

    const clientOrderId = this.decodeVarData(view, pos);
    pos = clientOrderId.nextOffset;

    return {
      orderId: orderId,
      orderListId: orderListId,
      transactTime: transactTime,
      symbol: symbol.value,
      clientOrderId: clientOrderId.value
    };
  }

  private decodeVarData(view: DataView, offset: number): { value: Uint8Array, nextOffset: number } {
    let pos = offset;

    const length = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const value = new Uint8Array(view.buffer, view.byteOffset + pos, length);
    pos += length;

    return { value, nextOffset: pos };
  }

  static getBlockLength(): number {
    return NewOrderAckResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return NewOrderAckResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return NewOrderAckResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return NewOrderAckResponseDecoder.SCHEMA_VERSION;
  }
}
