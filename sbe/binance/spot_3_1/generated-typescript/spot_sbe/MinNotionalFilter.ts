/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface MinNotionalFilter {
  filterType: number;
  priceExponent: number;
  minNotional: bigint;
  applyToMarket: number;
  avgPriceMins: number;
}

export class MinNotionalFilterDecoder {
  private static readonly BLOCK_LENGTH = 14;
  private static readonly TEMPLATE_ID = 5;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 1;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): MinNotionalFilter {
    const view = new DataView(buffer);
    let pos = offset;

    const filterType = view.getUint8(pos);
    pos += 1;

    const priceExponent = view.getInt8(pos);
    pos += 1;

    const minNotional = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const applyToMarket = view.getUint8(pos);
    pos += 1;

    const avgPriceMins = view.getInt32(pos, this.littleEndian);
    pos += 4;

    // Skip to end of block for forward compatibility
    pos = offset + MinNotionalFilterDecoder.BLOCK_LENGTH;

    return {
      filterType: filterType,
      priceExponent: priceExponent,
      minNotional: minNotional,
      applyToMarket: applyToMarket,
      avgPriceMins: avgPriceMins
    };
  }

  static getBlockLength(): number {
    return MinNotionalFilterDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return MinNotionalFilterDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return MinNotionalFilterDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return MinNotionalFilterDecoder.SCHEMA_VERSION;
  }
}
