/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface MarketLotSizeFilter {
  filterType: number;
  qtyExponent: number;
  minQty: bigint;
  maxQty: bigint;
  stepSize: bigint;
}

export class MarketLotSizeFilterDecoder {
  private static readonly BLOCK_LENGTH = 25;
  private static readonly TEMPLATE_ID = 8;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 1;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): MarketLotSizeFilter {
    const view = new DataView(buffer);
    let pos = offset;

    const filterType = view.getUint8(pos);
    pos += 1;

    const qtyExponent = view.getInt8(pos);
    pos += 1;

    const minQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const maxQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const stepSize = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    // Skip to end of block for forward compatibility
    pos = offset + MarketLotSizeFilterDecoder.BLOCK_LENGTH;

    return {
      filterType: filterType,
      qtyExponent: qtyExponent,
      minQty: minQty,
      maxQty: maxQty,
      stepSize: stepSize
    };
  }

  static getBlockLength(): number {
    return MarketLotSizeFilterDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return MarketLotSizeFilterDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return MarketLotSizeFilterDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return MarketLotSizeFilterDecoder.SCHEMA_VERSION;
  }
}
