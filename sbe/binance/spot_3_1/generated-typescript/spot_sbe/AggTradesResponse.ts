/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface AggTrades {
  aggTradeId: bigint;
  price: bigint;
  qty: bigint;
  firstTradeId: bigint;
  lastTradeId: bigint;
  time: bigint;
  isBuyerMaker: number;
  isBestMatch: number;
}

export interface AggTradesResponse {
  priceExponent: number;
  qtyExponent: number;
  aggTrades: AggTrades[];
}

export class AggTradesResponseDecoder {
  private static readonly BLOCK_LENGTH = 2;
  private static readonly TEMPLATE_ID = 202;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 1;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): AggTradesResponse {
    const view = new DataView(buffer);
    let pos = offset;

    const priceExponent = view.getInt8(pos);
    pos += 1;

    const qtyExponent = view.getInt8(pos);
    pos += 1;

    // Skip to end of block for forward compatibility
    pos = offset + AggTradesResponseDecoder.BLOCK_LENGTH;

    const aggTrades = this.decodeAggTradesGroup(view, pos);
    pos = aggTrades.nextOffset;

    return {
      priceExponent: priceExponent,
      qtyExponent: qtyExponent,
      aggTrades: aggTrades.items
    };
  }
  private decodeAggTradesGroup(view: DataView, offset: number): { items: AggTrades[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const items: AggTrades[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      const aggTradeId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const price = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const qty = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const firstTradeId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const lastTradeId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const time = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const isBuyerMaker = view.getUint8(pos);
      pos += 1;

      const isBestMatch = view.getUint8(pos);
      pos += 1;

      // Skip to next block for forward compatibility
      pos = itemStart + blockLength;

      items.push({
        aggTradeId: aggTradeId,
        price: price,
        qty: qty,
        firstTradeId: firstTradeId,
        lastTradeId: lastTradeId,
        time: time,
        isBuyerMaker: isBuyerMaker,
        isBestMatch: isBestMatch
      });
    }

    return { items, nextOffset: pos };
  }

  static getBlockLength(): number {
    return AggTradesResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return AggTradesResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return AggTradesResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return AggTradesResponseDecoder.SCHEMA_VERSION;
  }
}
