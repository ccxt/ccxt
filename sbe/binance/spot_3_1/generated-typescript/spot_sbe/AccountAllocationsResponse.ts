/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface Allocations {
  priceExponent: number;
  qtyExponent: number;
  commissionExponent: number;
  allocationId: bigint;
  allocationType: number;
  orderId: bigint;
  orderListId: bigint;
  sourceTradeId: bigint;
  sourceAllocationId: bigint;
  price: bigint;
  qty: bigint;
  quoteQty: bigint;
  commission: bigint;
  time: bigint;
  isBuyer: number;
  isMaker: number;
  isAllocator: number;
}

export interface AccountAllocationsResponse {
  allocations: Allocations[];
}

export class AccountAllocationsResponseDecoder {
  private static readonly BLOCK_LENGTH = 0;
  private static readonly TEMPLATE_ID = 404;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 1;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): AccountAllocationsResponse {
    const view = new DataView(buffer);
    let pos = offset;

    // Skip to end of block for forward compatibility
    pos = offset + AccountAllocationsResponseDecoder.BLOCK_LENGTH;

    const allocations = this.decodeAllocationsGroup(view, pos);
    pos = allocations.nextOffset;

    return {
      allocations: allocations.items
    };
  }
  private decodeAllocationsGroup(view: DataView, offset: number): { items: Allocations[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const items: Allocations[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      const priceExponent = view.getInt8(pos);
      pos += 1;

      const qtyExponent = view.getInt8(pos);
      pos += 1;

      const commissionExponent = view.getInt8(pos);
      pos += 1;

      const allocationId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const allocationType = view.getUint8(pos);
      pos += 1;

      const orderId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const orderListId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const sourceTradeId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const sourceAllocationId = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const price = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const qty = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const quoteQty = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const commission = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const time = view.getBigInt64(pos, this.littleEndian);
      pos += 8;

      const isBuyer = view.getUint8(pos);
      pos += 1;

      const isMaker = view.getUint8(pos);
      pos += 1;

      const isAllocator = view.getUint8(pos);
      pos += 1;

      // Skip to next block for forward compatibility
      pos = itemStart + blockLength;

      items.push({
        priceExponent: priceExponent,
        qtyExponent: qtyExponent,
        commissionExponent: commissionExponent,
        allocationId: allocationId,
        allocationType: allocationType,
        orderId: orderId,
        orderListId: orderListId,
        sourceTradeId: sourceTradeId,
        sourceAllocationId: sourceAllocationId,
        price: price,
        qty: qty,
        quoteQty: quoteQty,
        commission: commission,
        time: time,
        isBuyer: isBuyer,
        isMaker: isMaker,
        isAllocator: isAllocator
      });
    }

    return { items, nextOffset: pos };
  }

  static getBlockLength(): number {
    return AccountAllocationsResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return AccountAllocationsResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return AccountAllocationsResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return AccountAllocationsResponseDecoder.SCHEMA_VERSION;
  }
}
