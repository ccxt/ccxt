/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface OrderTestWithCommissionsResponse {
  commissionExponent: number;
  discountExponent: number;
  standardCommissionForOrderMaker: bigint;
  standardCommissionForOrderTaker: bigint;
  taxCommissionForOrderMaker: bigint;
  taxCommissionForOrderTaker: bigint;
  discountEnabledForAccount: number;
  discountEnabledForSymbol: number;
  discount: bigint;
  specialCommissionForOrderMaker: bigint;
  specialCommissionForOrderTaker: bigint;
  discountAsset: Uint8Array;
}

export class OrderTestWithCommissionsResponseDecoder {
  private static readonly BLOCK_LENGTH = 60;
  private static readonly TEMPLATE_ID = 315;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 1;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): OrderTestWithCommissionsResponse {
    const view = new DataView(buffer);
    let pos = offset;

    const commissionExponent = view.getInt8(pos);
    pos += 1;

    const discountExponent = view.getInt8(pos);
    pos += 1;

    const standardCommissionForOrderMaker = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const standardCommissionForOrderTaker = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const taxCommissionForOrderMaker = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const taxCommissionForOrderTaker = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const discountEnabledForAccount = view.getUint8(pos);
    pos += 1;

    const discountEnabledForSymbol = view.getUint8(pos);
    pos += 1;

    const discount = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const specialCommissionForOrderMaker = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const specialCommissionForOrderTaker = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    // Skip to end of block for forward compatibility
    pos = offset + OrderTestWithCommissionsResponseDecoder.BLOCK_LENGTH;

    const discountAsset = this.decodeVarData(view, pos);
    pos = discountAsset.nextOffset;

    return {
      commissionExponent: commissionExponent,
      discountExponent: discountExponent,
      standardCommissionForOrderMaker: standardCommissionForOrderMaker,
      standardCommissionForOrderTaker: standardCommissionForOrderTaker,
      taxCommissionForOrderMaker: taxCommissionForOrderMaker,
      taxCommissionForOrderTaker: taxCommissionForOrderTaker,
      discountEnabledForAccount: discountEnabledForAccount,
      discountEnabledForSymbol: discountEnabledForSymbol,
      discount: discount,
      specialCommissionForOrderMaker: specialCommissionForOrderMaker,
      specialCommissionForOrderTaker: specialCommissionForOrderTaker,
      discountAsset: discountAsset.value
    };
  }

  private decodeVarData(view: DataView, offset: number): { value: Uint8Array, nextOffset: number } {
    let pos = offset;

    const length = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const value = new Uint8Array(view.buffer, view.byteOffset + pos, length);
    pos += length;

    return { value, nextOffset: pos };
  }

  static getBlockLength(): number {
    return OrderTestWithCommissionsResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return OrderTestWithCommissionsResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return OrderTestWithCommissionsResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return OrderTestWithCommissionsResponseDecoder.SCHEMA_VERSION;
  }
}
