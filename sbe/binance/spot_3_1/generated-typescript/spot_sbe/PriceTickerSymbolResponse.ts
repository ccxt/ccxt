/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface PriceTickerSymbolResponse {
  priceExponent: number;
  price: bigint;
  symbol: Uint8Array;
}

export class PriceTickerSymbolResponseDecoder {
  private static readonly BLOCK_LENGTH = 9;
  private static readonly TEMPLATE_ID = 209;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 1;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): PriceTickerSymbolResponse {
    const view = new DataView(buffer);
    let pos = offset;

    const priceExponent = view.getInt8(pos);
    pos += 1;

    const price = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    // Skip to end of block for forward compatibility
    pos = offset + PriceTickerSymbolResponseDecoder.BLOCK_LENGTH;

    const symbol = this.decodeVarData(view, pos);
    pos = symbol.nextOffset;

    return {
      priceExponent: priceExponent,
      price: price,
      symbol: symbol.value
    };
  }

  private decodeVarData(view: DataView, offset: number): { value: Uint8Array, nextOffset: number } {
    let pos = offset;

    const length = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const value = new Uint8Array(view.buffer, view.byteOffset + pos, length);
    pos += length;

    return { value, nextOffset: pos };
  }

  static getBlockLength(): number {
    return PriceTickerSymbolResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return PriceTickerSymbolResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return PriceTickerSymbolResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return PriceTickerSymbolResponseDecoder.SCHEMA_VERSION;
  }
}
