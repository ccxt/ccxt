/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface AccountCommissionResponse {
  commissionExponent: number;
  discountExponent: number;
  standardCommissionMaker: bigint;
  standardCommissionTaker: bigint;
  standardCommissionBuyer: bigint;
  standardCommissionSeller: bigint;
  taxCommissionMaker: bigint;
  taxCommissionTaker: bigint;
  taxCommissionBuyer: bigint;
  taxCommissionSeller: bigint;
  discountEnabledForAccount: number;
  discountEnabledForSymbol: number;
  discount: bigint;
  specialCommissionMaker: bigint;
  specialCommissionTaker: bigint;
  specialCommissionBuyer: bigint;
  specialCommissionSeller: bigint;
  symbol: Uint8Array;
  discountAsset: Uint8Array;
}

export class AccountCommissionResponseDecoder {
  private static readonly BLOCK_LENGTH = 108;
  private static readonly TEMPLATE_ID = 405;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 1;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): AccountCommissionResponse {
    const view = new DataView(buffer);
    let pos = offset;

    const commissionExponent = view.getInt8(pos);
    pos += 1;

    const discountExponent = view.getInt8(pos);
    pos += 1;

    const standardCommissionMaker = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const standardCommissionTaker = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const standardCommissionBuyer = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const standardCommissionSeller = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const taxCommissionMaker = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const taxCommissionTaker = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const taxCommissionBuyer = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const taxCommissionSeller = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const discountEnabledForAccount = view.getUint8(pos);
    pos += 1;

    const discountEnabledForSymbol = view.getUint8(pos);
    pos += 1;

    const discount = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const specialCommissionMaker = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const specialCommissionTaker = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const specialCommissionBuyer = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const specialCommissionSeller = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    // Skip to end of block for forward compatibility
    pos = offset + AccountCommissionResponseDecoder.BLOCK_LENGTH;

    const symbol = this.decodeVarData(view, pos);
    pos = symbol.nextOffset;

    const discountAsset = this.decodeVarData(view, pos);
    pos = discountAsset.nextOffset;

    return {
      commissionExponent: commissionExponent,
      discountExponent: discountExponent,
      standardCommissionMaker: standardCommissionMaker,
      standardCommissionTaker: standardCommissionTaker,
      standardCommissionBuyer: standardCommissionBuyer,
      standardCommissionSeller: standardCommissionSeller,
      taxCommissionMaker: taxCommissionMaker,
      taxCommissionTaker: taxCommissionTaker,
      taxCommissionBuyer: taxCommissionBuyer,
      taxCommissionSeller: taxCommissionSeller,
      discountEnabledForAccount: discountEnabledForAccount,
      discountEnabledForSymbol: discountEnabledForSymbol,
      discount: discount,
      specialCommissionMaker: specialCommissionMaker,
      specialCommissionTaker: specialCommissionTaker,
      specialCommissionBuyer: specialCommissionBuyer,
      specialCommissionSeller: specialCommissionSeller,
      symbol: symbol.value,
      discountAsset: discountAsset.value
    };
  }

  private decodeVarData(view: DataView, offset: number): { value: Uint8Array, nextOffset: number } {
    let pos = offset;

    const length = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const value = new Uint8Array(view.buffer, view.byteOffset + pos, length);
    pos += length;

    return { value, nextOffset: pos };
  }

  static getBlockLength(): number {
    return AccountCommissionResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return AccountCommissionResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return AccountCommissionResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return AccountCommissionResponseDecoder.SCHEMA_VERSION;
  }
}
