/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface MaxAssetFilter {
  filterType: number;
  qtyExponent: number;
  maxQty: bigint;
  asset: string;
}

export class MaxAssetFilterDecoder {
  private static readonly BLOCK_LENGTH = 9;
  private static readonly TEMPLATE_ID = 21;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 1;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): MaxAssetFilter {
    const view = new DataView(buffer);
    let pos = offset;

    const filterType = view.getUint8(pos);
    pos += 1;

    const qtyExponent = view.getInt8(pos);
    pos += 1;

    const maxQty = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    // Skip to end of block for forward compatibility
    pos = offset + MaxAssetFilterDecoder.BLOCK_LENGTH;

    const assetLen = view.getUint8(pos);
    pos += 1;
    const asset = new TextDecoder('utf-8').decode(new Uint8Array(view.buffer, view.byteOffset + pos, assetLen));
    pos += assetLen;

    return {
      filterType: filterType,
      qtyExponent: qtyExponent,
      maxQty: maxQty,
      asset: asset
    };
  }

  static getBlockLength(): number {
    return MaxAssetFilterDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return MaxAssetFilterDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return MaxAssetFilterDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return MaxAssetFilterDecoder.SCHEMA_VERSION;
  }
}
