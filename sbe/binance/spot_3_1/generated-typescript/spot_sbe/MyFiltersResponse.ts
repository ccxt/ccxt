/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: spot_sbe
 * DO NOT EDIT
 */

export interface ExchangeFilters {
}

export interface SymbolFilters {
}

export interface AssetFilters {
}

export interface MyFiltersResponse {
  exchangeFilters: ExchangeFilters[];
  symbolFilters: SymbolFilters[];
  assetFilters: AssetFilters[];
}

export class MyFiltersResponseDecoder {
  private static readonly BLOCK_LENGTH = 0;
  private static readonly TEMPLATE_ID = 105;
  private static readonly SCHEMA_ID = 3;
  private static readonly SCHEMA_VERSION = 1;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): MyFiltersResponse {
    const view = new DataView(buffer);
    let pos = offset;

    // Skip to end of block for forward compatibility
    pos = offset + MyFiltersResponseDecoder.BLOCK_LENGTH;

    const exchangeFilters = this.decodeExchangeFiltersGroup(view, pos);
    pos = exchangeFilters.nextOffset;

    const symbolFilters = this.decodeSymbolFiltersGroup(view, pos);
    pos = symbolFilters.nextOffset;

    const assetFilters = this.decodeAssetFiltersGroup(view, pos);
    pos = assetFilters.nextOffset;

    return {
      exchangeFilters: exchangeFilters.items,
      symbolFilters: symbolFilters.items,
      assetFilters: assetFilters.items
    };
  }
  private decodeExchangeFiltersGroup(view: DataView, offset: number): { items: ExchangeFilters[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const items: ExchangeFilters[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      // Skip to next block for forward compatibility
      pos = itemStart + blockLength;

      items.push({
      });
    }

    return { items, nextOffset: pos };
  }
  private decodeSymbolFiltersGroup(view: DataView, offset: number): { items: SymbolFilters[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const items: SymbolFilters[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      // Skip to next block for forward compatibility
      pos = itemStart + blockLength;

      items.push({
      });
    }

    return { items, nextOffset: pos };
  }
  private decodeAssetFiltersGroup(view: DataView, offset: number): { items: AssetFilters[], nextOffset: number } {
    let pos = offset;

    const blockLength = view.getUint16(pos, this.littleEndian);
    pos += 2;
    const numInGroup = view.getUint32(pos, this.littleEndian);
    pos += 4;

    const items: AssetFilters[] = [];

    for (let i = 0; i < numInGroup; i++) {
      const itemStart = pos;

      // Skip to next block for forward compatibility
      pos = itemStart + blockLength;

      items.push({
      });
    }

    return { items, nextOffset: pos };
  }

  static getBlockLength(): number {
    return MyFiltersResponseDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return MyFiltersResponseDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return MyFiltersResponseDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return MyFiltersResponseDecoder.SCHEMA_VERSION;
  }
}
