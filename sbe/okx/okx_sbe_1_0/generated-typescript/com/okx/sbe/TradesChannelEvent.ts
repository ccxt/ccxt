/**
 * Auto-generated by SBE (Simple Binary Encoding)
 * Schema: com.okx.sbe
 * DO NOT EDIT
 */

export interface TradesChannelEvent {
  instIdCode: bigint;
  tsUs: bigint;
  outTime: bigint;
  seqId: bigint;
  pxMantissa: bigint;
  szMantissa: bigint;
  tradeId: bigint;
  count: number;
  side: number;
  pxExponent: number;
  szExponent: number;
  source: number;
}

export class TradesChannelEventDecoder {
  private static readonly BLOCK_LENGTH = 62;
  private static readonly TEMPLATE_ID = 1005;
  private static readonly SCHEMA_ID = 1;
  private static readonly SCHEMA_VERSION = 0;

  private readonly littleEndian: boolean;

  constructor(byteOrder: 'little' | 'big' = 'little') {
    this.littleEndian = byteOrder === 'little';
  }

  decode(buffer: ArrayBuffer, offset: number = 0): TradesChannelEvent {
    const view = new DataView(buffer);
    let pos = offset;

    const instIdCode = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const tsUs = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const outTime = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const seqId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const pxMantissa = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const szMantissa = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const tradeId = view.getBigInt64(pos, this.littleEndian);
    pos += 8;

    const count = view.getInt16(pos, this.littleEndian);
    pos += 2;

    const side = view.getInt8(pos);
    pos += 1;

    const pxExponent = view.getInt8(pos);
    pos += 1;

    const szExponent = view.getInt8(pos);
    pos += 1;

    const source = view.getInt8(pos);
    pos += 1;

    // Skip to end of block for forward compatibility
    pos = offset + TradesChannelEventDecoder.BLOCK_LENGTH;

    return {
      instIdCode: instIdCode,
      tsUs: tsUs,
      outTime: outTime,
      seqId: seqId,
      pxMantissa: pxMantissa,
      szMantissa: szMantissa,
      tradeId: tradeId,
      count: count,
      side: side,
      pxExponent: pxExponent,
      szExponent: szExponent,
      source: source
    };
  }

  static getBlockLength(): number {
    return TradesChannelEventDecoder.BLOCK_LENGTH;
  }

  static getTemplateId(): number {
    return TradesChannelEventDecoder.TEMPLATE_ID;
  }

  static getSchemaId(): number {
    return TradesChannelEventDecoder.SCHEMA_ID;
  }

  static getSchemaVersion(): number {
    return TradesChannelEventDecoder.SCHEMA_VERSION;
  }
}
