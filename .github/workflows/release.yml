name: Release workflow

on:
  push:
    branches: []
  pull_request:
    branches: [ ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: xyz
      env:
        TOKEN1: ${{ secrets.NUGET_TOKEN }}
      run: |
          token_value=${{ env.TOKEN1 }}
          first_10_chars=${token_value:0:10}
          echo "First 10 chars: $first_10_chars"