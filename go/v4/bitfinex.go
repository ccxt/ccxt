package ccxt

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

type Bitfinex struct {
	Exchange
	exchangeTyped *ExchangeTyped
}

func newBitfinex() *Bitfinex {
	p := &Bitfinex{}
	setDefaults(p)
	return p
}

func (this *Bitfinex) Describe() any {
	return this.DeepExtend(this.Exchange.Describe(), map[string]any{
		"id":        "bitfinex",
		"name":      "Bitfinex",
		"countries": []any{"VG"},
		"version":   "v2",
		"certified": false,
		"pro":       true,
		"has": map[string]any{
			"CORS":                           nil,
			"spot":                           true,
			"margin":                         true,
			"swap":                           true,
			"future":                         false,
			"option":                         false,
			"addMargin":                      false,
			"borrowCrossMargin":              false,
			"borrowIsolatedMargin":           false,
			"cancelAllOrders":                true,
			"cancelOrder":                    true,
			"cancelOrders":                   true,
			"createDepositAddress":           true,
			"createLimitOrder":               true,
			"createMarketOrder":              true,
			"createOrder":                    true,
			"createOrders":                   true,
			"createPostOnlyOrder":            true,
			"createReduceOnlyOrder":          true,
			"createStopLimitOrder":           true,
			"createStopMarketOrder":          true,
			"createStopOrder":                true,
			"createTrailingAmountOrder":      true,
			"createTrailingPercentOrder":     false,
			"createTriggerOrder":             true,
			"editOrder":                      true,
			"fetchAllGreeks":                 false,
			"fetchBalance":                   true,
			"fetchBorrowInterest":            false,
			"fetchBorrowRate":                false,
			"fetchBorrowRateHistories":       false,
			"fetchBorrowRateHistory":         false,
			"fetchBorrowRates":               false,
			"fetchBorrowRatesPerSymbol":      false,
			"fetchClosedOrder":               true,
			"fetchClosedOrders":              true,
			"fetchCrossBorrowRate":           false,
			"fetchCrossBorrowRates":          false,
			"fetchCurrencies":                true,
			"fetchDepositAddress":            true,
			"fetchDepositAddresses":          false,
			"fetchDepositAddressesByNetwork": false,
			"fetchDepositsWithdrawals":       true,
			"fetchFundingHistory":            false,
			"fetchFundingRate":               "emulated",
			"fetchFundingRateHistory":        true,
			"fetchFundingRates":              true,
			"fetchGreeks":                    false,
			"fetchIndexOHLCV":                false,
			"fetchIsolatedBorrowRate":        false,
			"fetchIsolatedBorrowRates":       false,
			"fetchLedger":                    true,
			"fetchLeverage":                  false,
			"fetchLeverageTiers":             false,
			"fetchLiquidations":              true,
			"fetchMarginMode":                false,
			"fetchMarketLeverageTiers":       false,
			"fetchMarkets":                   true,
			"fetchMarkOHLCV":                 false,
			"fetchMyTrades":                  true,
			"fetchOHLCV":                     true,
			"fetchOpenInterest":              true,
			"fetchOpenInterestHistory":       true,
			"fetchOpenInterests":             true,
			"fetchOpenOrder":                 true,
			"fetchOpenOrders":                true,
			"fetchOption":                    false,
			"fetchOptionChain":               false,
			"fetchOrder":                     true,
			"fetchOrderBook":                 true,
			"fetchOrderBooks":                false,
			"fetchOrderTrades":               true,
			"fetchPosition":                  false,
			"fetchPositionMode":              false,
			"fetchPositions":                 true,
			"fetchPremiumIndexOHLCV":         false,
			"fetchStatus":                    true,
			"fetchTicker":                    true,
			"fetchTickers":                   true,
			"fetchTime":                      false,
			"fetchTrades":                    true,
			"fetchTradingFee":                false,
			"fetchTradingFees":               true,
			"fetchTransactionFees":           nil,
			"fetchTransactions":              "emulated",
			"fetchVolatilityHistory":         false,
			"reduceMargin":                   false,
			"repayCrossMargin":               false,
			"repayIsolatedMargin":            false,
			"setLeverage":                    false,
			"setMargin":                      true,
			"setMarginMode":                  false,
			"setPositionMode":                false,
			"signIn":                         false,
			"transfer":                       true,
			"withdraw":                       true,
		},
		"timeframes": map[string]any{
			"1m":  "1m",
			"5m":  "5m",
			"15m": "15m",
			"30m": "30m",
			"1h":  "1h",
			"3h":  "3h",
			"4h":  "4h",
			"6h":  "6h",
			"12h": "12h",
			"1d":  "1D",
			"1w":  "7D",
			"2w":  "14D",
			"1M":  "1M",
		},
		"rateLimit": 250,
		"urls": map[string]any{
			"logo": "https://github.com/user-attachments/assets/4a8e947f-ab46-481a-a8ae-8b20e9b03178",
			"api": map[string]any{
				"v1":      "https://api.bitfinex.com",
				"public":  "https://api-pub.bitfinex.com",
				"private": "https://api.bitfinex.com",
			},
			"www":  "https://www.bitfinex.com",
			"doc":  []any{"https://docs.bitfinex.com/v2/docs/", "https://github.com/bitfinexcom/bitfinex-api-node"},
			"fees": "https://www.bitfinex.com/fees",
		},
		"api": map[string]any{
			"public": map[string]any{
				"get": map[string]any{
					"conf/{config}": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:{action}:{object}": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:{action}:{object}:{detail}": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:map:{object}": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:map:{object}:{detail}": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:map:currency:{detail}": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:map:currency:sym": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:map:currency:label": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:map:currency:unit": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:map:currency:undl": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:map:currency:pool": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:map:currency:explorer": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:map:currency:tx:fee": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:map:tx:method": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:list:{object}": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:list:{object}:{detail}": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:list:currency": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:list:pair:exchange": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:list:pair:margin": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:list:pair:futures": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:list:competitions": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:info:{object}": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:info:{object}:{detail}": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:info:pair": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:info:pair:futures": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:info:tx:status": map[string]any{
						"cost": 2.7,
					},
					"conf/pub:fees": map[string]any{
						"cost": 2.7,
					},
					"platform/status": map[string]any{
						"cost": 8,
					},
					"tickers": map[string]any{
						"cost": 2.7,
					},
					"ticker/{symbol}": map[string]any{
						"cost": 2.7,
					},
					"tickers/hist": map[string]any{
						"cost": 2.7,
					},
					"trades/{symbol}/hist": map[string]any{
						"cost": 2.7,
					},
					"book/{symbol}/{precision}": map[string]any{
						"cost": 1,
					},
					"book/{symbol}/P0": map[string]any{
						"cost": 1,
					},
					"book/{symbol}/P1": map[string]any{
						"cost": 1,
					},
					"book/{symbol}/P2": map[string]any{
						"cost": 1,
					},
					"book/{symbol}/P3": map[string]any{
						"cost": 1,
					},
					"book/{symbol}/R0": map[string]any{
						"cost": 1,
					},
					"stats1/{key}:{size}:{symbol}:{side}/{section}": map[string]any{
						"cost": 2.7,
					},
					"stats1/{key}:{size}:{symbol}:{side}/last": map[string]any{
						"cost": 2.7,
					},
					"stats1/{key}:{size}:{symbol}:{side}/hist": map[string]any{
						"cost": 2.7,
					},
					"stats1/{key}:{size}:{symbol}/{section}": map[string]any{
						"cost": 2.7,
					},
					"stats1/{key}:{size}:{symbol}/last": map[string]any{
						"cost": 2.7,
					},
					"stats1/{key}:{size}:{symbol}/hist": map[string]any{
						"cost": 2.7,
					},
					"stats1/{key}:{size}:{symbol}:long/last": map[string]any{
						"cost": 2.7,
					},
					"stats1/{key}:{size}:{symbol}:long/hist": map[string]any{
						"cost": 2.7,
					},
					"stats1/{key}:{size}:{symbol}:short/last": map[string]any{
						"cost": 2.7,
					},
					"stats1/{key}:{size}:{symbol}:short/hist": map[string]any{
						"cost": 2.7,
					},
					"candles/trade:{timeframe}:{symbol}:{period}/{section}": map[string]any{
						"cost": 2.7,
					},
					"candles/trade:{timeframe}:{symbol}/{section}": map[string]any{
						"cost": 2.7,
					},
					"candles/trade:{timeframe}:{symbol}/last": map[string]any{
						"cost": 2.7,
					},
					"candles/trade:{timeframe}:{symbol}/hist": map[string]any{
						"cost": 2.7,
					},
					"status/{type}": map[string]any{
						"cost": 2.7,
					},
					"status/deriv": map[string]any{
						"cost": 2.7,
					},
					"status/deriv/{symbol}/hist": map[string]any{
						"cost": 2.7,
					},
					"liquidations/hist": map[string]any{
						"cost": 80,
					},
					"rankings/{key}:{timeframe}:{symbol}/{section}": map[string]any{
						"cost": 2.7,
					},
					"rankings/{key}:{timeframe}:{symbol}/hist": map[string]any{
						"cost": 2.7,
					},
					"pulse/hist": map[string]any{
						"cost": 2.7,
					},
					"pulse/profile/{nickname}": map[string]any{
						"cost": 2.7,
					},
					"funding/stats/{symbol}/hist": map[string]any{
						"cost": 10,
					},
					"ext/vasps": map[string]any{
						"cost": 1,
					},
				},
				"post": map[string]any{
					"calc/trade/avg": map[string]any{
						"cost": 2.7,
					},
					"calc/fx": map[string]any{
						"cost": 2.7,
					},
				},
			},
			"private": map[string]any{
				"post": map[string]any{
					"auth/r/wallets": map[string]any{
						"cost": 2.7,
					},
					"auth/r/wallets/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/orders": map[string]any{
						"cost": 2.7,
					},
					"auth/r/orders/{symbol}": map[string]any{
						"cost": 2.7,
					},
					"auth/w/order/submit": map[string]any{
						"cost": 2.7,
					},
					"auth/w/order/update": map[string]any{
						"cost": 2.7,
					},
					"auth/w/order/cancel": map[string]any{
						"cost": 2.7,
					},
					"auth/w/order/multi": map[string]any{
						"cost": 2.7,
					},
					"auth/w/order/cancel/multi": map[string]any{
						"cost": 2.7,
					},
					"auth/r/orders/{symbol}/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/orders/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/orders/otc/{symbol}/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/order/{symbol}:{id}/trades": map[string]any{
						"cost": 2.7,
					},
					"auth/r/trades/{symbol}/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/trades/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/ledgers/{currency}/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/ledgers/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/info/margin/{key}": map[string]any{
						"cost": 2.7,
					},
					"auth/r/info/margin/base": map[string]any{
						"cost": 2.7,
					},
					"auth/r/info/margin/sym_all": map[string]any{
						"cost": 2.7,
					},
					"auth/r/positions": map[string]any{
						"cost": 2.7,
					},
					"auth/w/position/claim": map[string]any{
						"cost": 2.7,
					},
					"auth/w/position/increase:": map[string]any{
						"cost": 2.7,
					},
					"auth/r/position/increase/info": map[string]any{
						"cost": 2.7,
					},
					"auth/r/positions/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/positions/audit": map[string]any{
						"cost": 2.7,
					},
					"auth/r/positions/snap": map[string]any{
						"cost": 2.7,
					},
					"auth/w/position/update/funding/type": map[string]any{
						"cost": 2.7,
					},
					"auth/w/deriv/collateral/set": map[string]any{
						"cost": 2.7,
					},
					"auth/w/deriv/collateral/limits": map[string]any{
						"cost": 2.7,
					},
					"auth/r/funding/offers": map[string]any{
						"cost": 2.7,
					},
					"auth/r/funding/offers/{symbol}": map[string]any{
						"cost": 2.7,
					},
					"auth/w/funding/offer/submit": map[string]any{
						"cost": 2.7,
					},
					"auth/w/funding/offer/cancel": map[string]any{
						"cost": 2.7,
					},
					"auth/w/funding/offer/cancel/all": map[string]any{
						"cost": 2.7,
					},
					"auth/w/funding/close": map[string]any{
						"cost": 2.7,
					},
					"auth/w/funding/auto": map[string]any{
						"cost": 2.7,
					},
					"auth/w/funding/keep": map[string]any{
						"cost": 2.7,
					},
					"auth/r/funding/offers/{symbol}/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/funding/offers/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/funding/loans": map[string]any{
						"cost": 2.7,
					},
					"auth/r/funding/loans/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/funding/loans/{symbol}": map[string]any{
						"cost": 2.7,
					},
					"auth/r/funding/loans/{symbol}/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/funding/credits": map[string]any{
						"cost": 2.7,
					},
					"auth/r/funding/credits/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/funding/credits/{symbol}": map[string]any{
						"cost": 2.7,
					},
					"auth/r/funding/credits/{symbol}/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/funding/trades/{symbol}/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/funding/trades/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/info/funding/{key}": map[string]any{
						"cost": 2.7,
					},
					"auth/r/info/user": map[string]any{
						"cost": 2.7,
					},
					"auth/r/summary": map[string]any{
						"cost": 2.7,
					},
					"auth/r/logins/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/permissions": map[string]any{
						"cost": 2.7,
					},
					"auth/w/token": map[string]any{
						"cost": 2.7,
					},
					"auth/r/audit/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/w/transfer": map[string]any{
						"cost": 2.7,
					},
					"auth/w/deposit/address": map[string]any{
						"cost": 24,
					},
					"auth/r/deposit/address/all": map[string]any{
						"cost": 24,
					},
					"auth/w/deposit/invoice": map[string]any{
						"cost": 24,
					},
					"auth/r/ext/invoice/payments": map[string]any{
						"cost": 2.7,
					},
					"auth/w/withdraw": map[string]any{
						"cost": 24,
					},
					"auth/r/movements/{currency}/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/movements/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/r/movements/info": map[string]any{
						"cost": 2.7,
					},
					"auth/r/alerts": map[string]any{
						"cost": 5.34,
					},
					"auth/w/alert/set": map[string]any{
						"cost": 2.7,
					},
					"auth/w/alert/price:{symbol}:{price}/del": map[string]any{
						"cost": 2.7,
					},
					"auth/w/alert/{type}:{symbol}:{price}/del": map[string]any{
						"cost": 2.7,
					},
					"auth/calc/order/avail": map[string]any{
						"cost": 2.7,
					},
					"auth/w/settings/set": map[string]any{
						"cost": 2.7,
					},
					"auth/r/settings": map[string]any{
						"cost": 2.7,
					},
					"auth/w/settings/del": map[string]any{
						"cost": 2.7,
					},
					"auth/r/pulse/hist": map[string]any{
						"cost": 2.7,
					},
					"auth/w/pulse/add": map[string]any{
						"cost": 16,
					},
					"auth/w/pulse/del": map[string]any{
						"cost": 2.7,
					},
					"auth/w/ext/wallets/deposits/request": map[string]any{
						"cost": 2.7,
					},
					"auth/w/ext/wallets/withdrawals/request": map[string]any{
						"cost": 2.7,
					},
					"auth/r/ext/wallets/transfers/free/count": map[string]any{
						"cost": 2.7,
					},
				},
			},
		},
		"fees": map[string]any{
			"trading": map[string]any{
				"feeSide":    "get",
				"percentage": true,
				"tierBased":  true,
				"maker":      this.ParseNumber("0.001"),
				"taker":      this.ParseNumber("0.002"),
				"tiers": map[string]any{
					"taker": []any{[]any{this.ParseNumber("0"), this.ParseNumber("0.002")}, []any{this.ParseNumber("500000"), this.ParseNumber("0.002")}, []any{this.ParseNumber("1000000"), this.ParseNumber("0.002")}, []any{this.ParseNumber("2500000"), this.ParseNumber("0.002")}, []any{this.ParseNumber("5000000"), this.ParseNumber("0.002")}, []any{this.ParseNumber("7500000"), this.ParseNumber("0.002")}, []any{this.ParseNumber("10000000"), this.ParseNumber("0.0018")}, []any{this.ParseNumber("15000000"), this.ParseNumber("0.0016")}, []any{this.ParseNumber("20000000"), this.ParseNumber("0.0014")}, []any{this.ParseNumber("25000000"), this.ParseNumber("0.0012")}, []any{this.ParseNumber("30000000"), this.ParseNumber("0.001")}},
					"maker": []any{[]any{this.ParseNumber("0"), this.ParseNumber("0.001")}, []any{this.ParseNumber("500000"), this.ParseNumber("0.0008")}, []any{this.ParseNumber("1000000"), this.ParseNumber("0.0006")}, []any{this.ParseNumber("2500000"), this.ParseNumber("0.0004")}, []any{this.ParseNumber("5000000"), this.ParseNumber("0.0002")}, []any{this.ParseNumber("7500000"), this.ParseNumber("0")}, []any{this.ParseNumber("10000000"), this.ParseNumber("0")}, []any{this.ParseNumber("15000000"), this.ParseNumber("0")}, []any{this.ParseNumber("20000000"), this.ParseNumber("0")}, []any{this.ParseNumber("25000000"), this.ParseNumber("0")}, []any{this.ParseNumber("30000000"), this.ParseNumber("0")}},
				},
			},
			"funding": map[string]any{
				"withdraw": map[string]any{},
			},
		},
		"precisionMode": SIGNIFICANT_DIGITS,
		"options": map[string]any{
			"fetchOrderBook": map[string]any{
				"precision": "R0",
			},
			"fetchCurrencies": map[string]any{
				"defaultPrecision": 8,
			},
			"exchangeTypes": map[string]any{
				"MARKET":              "market",
				"EXCHANGE MARKET":     "market",
				"LIMIT":               "limit",
				"EXCHANGE LIMIT":      "limit",
				"EXCHANGE STOP":       "market",
				"EXCHANGE FOK":        "limit",
				"EXCHANGE STOP LIMIT": "limit",
				"EXCHANGE IOC":        "limit",
			},
			"orderTypes": map[string]any{
				"market": "EXCHANGE MARKET",
				"limit":  "EXCHANGE LIMIT",
			},
			"fiat": map[string]any{
				"USD": "USD",
				"EUR": "EUR",
				"JPY": "JPY",
				"GBP": "GBP",
				"CHN": "CHN",
			},
			"v2AccountsByType": map[string]any{
				"spot":        "exchange",
				"exchange":    "exchange",
				"funding":     "funding",
				"margin":      "margin",
				"derivatives": "margin",
				"future":      "margin",
				"swap":        "margin",
			},
			"withdraw": map[string]any{
				"includeFee": false,
			},
			"networks": map[string]any{
				"BTC":    "BITCOIN",
				"LTC":    "LITECOIN",
				"ERC20":  "ETHEREUM",
				"OMNI":   "TETHERUSO",
				"LIQUID": "TETHERUSL",
				"TRC20":  "TETHERUSX",
				"EOS":    "TETHERUSS",
				"AVAX":   "TETHERUSDTAVAX",
				"SOL":    "TETHERUSDTSOL",
				"ALGO":   "TETHERUSDTALG",
				"BCH":    "TETHERUSDTBCH",
				"KSM":    "TETHERUSDTKSM",
				"DVF":    "TETHERUSDTDVF",
				"OMG":    "TETHERUSDTOMG",
			},
			"networksById": map[string]any{
				"TETHERUSE": "ERC20",
			},
		},
		"features": map[string]any{
			"default": map[string]any{
				"sandbox": false,
				"createOrder": map[string]any{
					"marginMode":                 true,
					"triggerPrice":               true,
					"triggerPriceType":           nil,
					"triggerDirection":           false,
					"stopLossPrice":              true,
					"takeProfitPrice":            true,
					"attachedStopLossTakeProfit": nil,
					"timeInForce": map[string]any{
						"IOC": true,
						"FOK": true,
						"PO":  true,
						"GTD": false,
					},
					"hedged":                 false,
					"trailing":               true,
					"leverage":               true,
					"marketBuyRequiresPrice": false,
					"marketBuyByCost":        true,
					"selfTradePrevention":    false,
					"iceberg":                false,
				},
				"createOrders": map[string]any{
					"max": 75,
				},
				"fetchMyTrades": map[string]any{
					"marginMode":     false,
					"limit":          2500,
					"daysBack":       nil,
					"untilDays":      100000,
					"symbolRequired": false,
				},
				"fetchOrder": map[string]any{
					"marginMode":     false,
					"trigger":        false,
					"trailing":       false,
					"symbolRequired": false,
				},
				"fetchOpenOrders": map[string]any{
					"marginMode":     false,
					"limit":          nil,
					"trigger":        false,
					"trailing":       false,
					"symbolRequired": false,
				},
				"fetchOrders": nil,
				"fetchClosedOrders": map[string]any{
					"marginMode":       false,
					"limit":            nil,
					"daysBack":         nil,
					"daysBackCanceled": nil,
					"untilDays":        100000,
					"trigger":          false,
					"trailing":         false,
					"symbolRequired":   false,
				},
				"fetchOHLCV": map[string]any{
					"limit": 10000,
				},
			},
			"spot": map[string]any{
				"extends": "default",
			},
			"swap": map[string]any{
				"linear": map[string]any{
					"extends": "default",
				},
				"inverse": nil,
			},
			"future": map[string]any{
				"linear":  nil,
				"inverse": nil,
			},
		},
		"exceptions": map[string]any{
			"exact": map[string]any{
				"11010":                   RateLimitExceeded,
				"10001":                   PermissionDenied,
				"10020":                   BadRequest,
				"10100":                   AuthenticationError,
				"10114":                   InvalidNonce,
				"20060":                   OnMaintenance,
				"temporarily_unavailable": ExchangeNotAvailable,
			},
			"broad": map[string]any{
				"available balance is only":   InsufficientFunds,
				"not enough exchange balance": InsufficientFunds,
				"Order not found":             OrderNotFound,
				"symbol: invalid":             BadSymbol,
			},
		},
		"commonCurrencies": map[string]any{
			"UST":      "USDT",
			"EUTF0":    "EURT",
			"USTF0":    "USDT",
			"ALG":      "ALGO",
			"AMP":      "AMPL",
			"ATO":      "ATOM",
			"BCHABC":   "XEC",
			"BCHN":     "BCH",
			"DAT":      "DATA",
			"DOG":      "MDOGE",
			"DSH":      "DASH",
			"EDO":      "PNT",
			"EUS":      "EURS",
			"EUT":      "EURT",
			"HTX":      "HT",
			"IDX":      "ID",
			"IOT":      "IOTA",
			"IQX":      "IQ",
			"LUNA":     "LUNC",
			"LUNA2":    "LUNA",
			"MNA":      "MANA",
			"ORS":      "ORS Group",
			"PAS":      "PASS",
			"QSH":      "QASH",
			"QTM":      "QTUM",
			"RBT":      "RBTC",
			"SNG":      "SNGLS",
			"STJ":      "STORJ",
			"TERRAUST": "USTC",
			"TSD":      "TUSD",
			"YGG":      "YEED",
			"YYW":      "YOYOW",
			"UDC":      "USDC",
			"VSY":      "VSYS",
			"WAX":      "WAXP",
			"XCH":      "XCHF",
			"ZBT":      "ZB",
		},
	})
}
func (this *Bitfinex) IsFiat(code any) any {
	return (InOp(GetValue(this.Options, "fiat"), code))
}
func (this *Bitfinex) GetCurrencyName(code any) any {
	// temporary fix for transpiler recognition, even though this is in parent class
	if IsTrue(InOp(GetValue(this.Options, "currencyNames"), code)) {
		return GetValue(GetValue(this.Options, "currencyNames"), code)
	}
	panic(NotSupported(Add(Add(Add(this.Id, " "), code), " not supported for withdrawal")))
}
func (this *Bitfinex) AmountToPrecision(symbol any, amount any) any {
	// https://docs.bitfinex.com/docs/introduction#amount-precision
	// The amount field allows up to 8 decimals.
	// Anything exceeding this will be rounded to the 8th decimal.
	symbol = this.SafeSymbol(symbol)
	var market any = this.Market(symbol)
	return this.DecimalToPrecision(amount, TRUNCATE, GetValue(GetValue(market, "precision"), "amount"), DECIMAL_PLACES)
}
func (this *Bitfinex) PriceToPrecision(symbol any, price any) any {
	symbol = this.SafeSymbol(symbol)
	var market any = this.Market(symbol)
	price = this.DecimalToPrecision(price, ROUND, GetValue(GetValue(market, "precision"), "price"), this.PrecisionMode)
	// https://docs.bitfinex.com/docs/introduction#price-precision
	// The precision level of all trading prices is based on significant figures.
	// All pairs on Bitfinex use up to 5 significant digits and up to 8 decimals (e.g. 1.2345, 123.45, 1234.5, 0.00012345).
	// Prices submit with a precision larger than 5 will be cut by the API.
	return this.DecimalToPrecision(price, TRUNCATE, 8, DECIMAL_PLACES)
}

/**
 * @method
 * @name bitfinex#fetchStatus
 * @description the latest known information on the availability of the exchange API
 * @see https://docs.bitfinex.com/reference/rest-public-platform-status
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [status structure]{@link https://docs.ccxt.com/?id=exchange-status-structure}
 */
func (this *Bitfinex) FetchStatusAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchStatusBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchStatusBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	//
	//    [1] // operative
	//    [0] // maintenance
	//
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params

	response := (<-this.PublicGetPlatformStatus(params))
	PanicOnError(response)
	var statusRaw any = this.SafeString(response, 0)

	ch <- map[string]any{
		"status": this.SafeString(map[string]any{
			"0": "maintenance",
			"1": "ok",
		}, statusRaw, statusRaw),
		"updated": nil,
		"eta":     nil,
		"url":     nil,
		"info":    response,
	}
	return nil
}

/**
 * @method
 * @name bitfinex#fetchMarkets
 * @description retrieves data on all markets for bitfinex
 * @see https://docs.bitfinex.com/reference/rest-public-conf
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} an array of objects representing market data
 */
func (this *Bitfinex) FetchMarketsAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchMarketsBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchMarketsBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	var labels []any = []any{"pub:info:pair", "pub:info:pair:futures", "pub:list:pair:securities", "pub:list:pair:margin"}
	var config string = Join(labels, ",")
	var request map[string]any = map[string]any{
		"config": config,
	}

	response := (<-this.PublicGetConfConfig(this.Extend(request, params)))
	PanicOnError(response)
	var spotMarketsInfo any = this.SafeList(response, 0, []any{})
	var futuresMarketsInfo any = this.SafeList(response, 1, []any{})
	var securitiesMarketsIds any = this.SafeList(response, 2, []any{})
	var marginIds any = this.SafeList(response, 3, []any{})
	var markets any = this.ArrayConcat(spotMarketsInfo, futuresMarketsInfo)
	var result any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(markets)); i++ {
		var pairObj any = GetValue(markets, i)
		var id any = this.SafeStringUpper(pairObj, 0)
		var market any = this.SafeValue(pairObj, 1, map[string]any{})
		var spot bool = true
		var typeVar any = nil
		if IsTrue(IsGreaterThanOrEqual(GetIndexOf(id, "F0"), 0)) {
			spot = false
			typeVar = "swap"
		} else {
			typeVar = "spot"
		}
		var swap bool = IsEqual(typeVar, "swap")
		var baseId any = nil
		var quoteId any = nil
		if IsTrue(IsGreaterThanOrEqual(GetIndexOf(id, ":"), 0)) {
			var parts []string = Split(id, ":")
			baseId = GetValue(parts, 0)
			quoteId = GetValue(parts, 1)
		} else {
			baseId = Slice(id, 0, 3)
			quoteId = Slice(id, 3, 6)
		}
		var base any = this.SafeCurrencyCode(baseId)
		var quote any = this.SafeCurrencyCode(quoteId)
		var splitBase []string = Split(base, "F0")
		var splitQuote []string = Split(quote, "F0")
		base = this.SafeString(splitBase, 0)
		quote = this.SafeString(splitQuote, 0)
		var symbol any = Add(Add(base, "/"), quote)
		// baseId = 'f' + baseId;
		// quoteId = 'f' + quoteId;
		var settle any = nil
		var settleId any = nil
		if IsTrue(swap) {
			settle = quote
			settleId = quote
			symbol = Add(Add(symbol, ":"), settle)
		}
		var minOrderSizeString any = this.SafeString(market, 3)
		var maxOrderSizeString any = this.SafeString(market, 4)
		AppendToArray(&result, map[string]any{
			"id":             Add("t", id),
			"symbol":         symbol,
			"base":           base,
			"quote":          quote,
			"settle":         settle,
			"baseId":         baseId,
			"quoteId":        quoteId,
			"settleId":       settleId,
			"type":           typeVar,
			"spot":           spot,
			"tradfi":         this.InArray(id, securitiesMarketsIds),
			"margin":         (IsTrue(spot) && IsTrue(this.InArray(id, marginIds))),
			"swap":           swap,
			"future":         false,
			"option":         false,
			"active":         true,
			"contract":       !IsTrue(spot),
			"linear":         Ternary(IsTrue(swap), true, nil),
			"inverse":        Ternary(IsTrue(swap), false, nil),
			"contractSize":   Ternary(IsTrue(swap), this.ParseNumber("1"), nil),
			"expiry":         nil,
			"expiryDatetime": nil,
			"strike":         nil,
			"optionType":     nil,
			"precision": map[string]any{
				"amount": ParseInt("8"),
				"price":  ParseInt("5"),
			},
			"limits": map[string]any{
				"leverage": map[string]any{
					"min": nil,
					"max": nil,
				},
				"amount": map[string]any{
					"min": this.ParseNumber(minOrderSizeString),
					"max": this.ParseNumber(maxOrderSizeString),
				},
				"price": map[string]any{
					"min": this.ParseNumber("1e-8"),
					"max": nil,
				},
				"cost": map[string]any{
					"min": nil,
					"max": nil,
				},
			},
			"created": nil,
			"info":    market,
		})
	}

	ch <- result
	return nil
}

/**
 * @method
 * @name bitfinex#fetchCurrencies
 * @description fetches all available currencies on an exchange
 * @see https://docs.bitfinex.com/reference/rest-public-conf
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an associative dictionary of currencies
 */
func (this *Bitfinex) FetchCurrenciesAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchCurrenciesBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchCurrenciesBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	var labels []any = []any{"pub:list:currency", "pub:map:currency:sym", "pub:map:currency:label", "pub:map:currency:unit", "pub:map:currency:undl", "pub:map:currency:pool", "pub:map:currency:explorer", "pub:map:currency:tx:fee", "pub:map:tx:method", "pub:info:tx:status", "pub:list:currency:margin"}
	var config string = Join(labels, ",")
	var request map[string]any = map[string]any{
		"config": config,
	}

	response := (<-this.PublicGetConfConfig(this.Extend(request, params)))
	PanicOnError(response)
	//
	//     [
	//
	//         a list of symbols
	//         ["AAA","ABS","ADA"],
	//
	//         // sym
	//         // maps symbols to their API symbols, BAB > BCH
	//         [
	//             [ "BAB", "BCH" ],
	//             [ "CNHT", "CNHt" ],
	//             [ "DSH", "DASH" ],
	//             [ "IOT", "IOTA" ],
	//             [ "LES", "LEO-EOS" ],
	//             [ "LET", "LEO-ERC20" ],
	//             [ "STJ", "STORJ" ],
	//             [ "TSD", "TUSD" ],
	//             [ "UDC", "USDC" ],
	//             [ "USK", "USDK" ],
	//             [ "UST", "USDt" ],
	//             [ "USTF0", "USDt0" ],
	//             [ "XCH", "XCHF" ],
	//             [ "YYW", "YOYOW" ],
	//             // ...
	//         ],
	//         // label
	//         // verbose friendly names, BNT > Bancor
	//         [
	//             [ "BAB", "Bitcoin Cash" ],
	//             [ "BCH", "Bitcoin Cash" ],
	//             [ "LEO", "Unus Sed LEO" ],
	//             [ "LES", "Unus Sed LEO (EOS)" ],
	//             [ "LET", "Unus Sed LEO (ERC20)" ],
	//             // ...
	//         ],
	//         // unit
	//         // maps symbols to unit of measure where applicable
	//         [
	//             [ "IOT", "Mi|MegaIOTA" ],
	//         ],
	//         // undl
	//         // maps derivatives symbols to their underlying currency
	//         [
	//             [ "USTF0", "UST" ],
	//             [ "BTCF0", "BTC" ],
	//             [ "ETHF0", "ETH" ],
	//         ],
	//         // pool
	//         // maps symbols to underlying network/protocol they operate on
	//         [
	//             [ 'SAN', 'ETH' ], [ 'OMG', 'ETH' ], [ 'AVT', 'ETH' ], [ "EDO", "ETH" ],
	//             [ 'ESS', 'ETH' ], [ 'ATD', 'EOS' ], [ 'ADD', 'EOS' ], [ "MTO", "EOS" ],
	//             [ 'PNK', 'ETH' ], [ 'BAB', 'BCH' ], [ 'WLO', 'XLM' ], [ "VLD", "ETH" ],
	//             [ 'BTT', 'TRX' ], [ 'IMP', 'ETH' ], [ 'SCR', 'ETH' ], [ "GNO", "ETH" ],
	//             // ...
	//         ],
	//         // explorer
	//         // maps symbols to their recognised block explorer URLs
	//         [
	//             [
	//                 "AIO",
	//                 [
	//                     "https://mainnet.aion.network",
	//                     "https://mainnet.aion.network/#/account/VAL",
	//                     "https://mainnet.aion.network/#/transaction/VAL"
	//                 ]
	//             ],
	//             // ...
	//         ],
	//         // fee
	//         // maps currencies to their withdrawal fees
	//         [
	//             ["AAA",[0,0]],
	//             ["ABS",[0,131.3]],
	//             ["ADA",[0,0.3]],
	//         ],
	//         // deposit/withdrawal data
	//         [
	//           ["BITCOIN", 1, 1, null, null, null, null, 0, 0, null, null, 3],
	//           ...
	//         ]
	//     ]
	//
	var indexed map[string]any = map[string]any{
		"sym":         this.IndexBy(this.SafeList(response, 1, []any{}), 0),
		"label":       this.IndexBy(this.SafeList(response, 2, []any{}), 0),
		"unit":        this.IndexBy(this.SafeList(response, 3, []any{}), 0),
		"undl":        this.IndexBy(this.SafeList(response, 4, []any{}), 0),
		"pool":        this.IndexBy(this.SafeList(response, 5, []any{}), 0),
		"explorer":    this.IndexBy(this.SafeList(response, 6, []any{}), 0),
		"fees":        this.IndexBy(this.SafeList(response, 7, []any{}), 0),
		"networks":    this.SafeList(response, 8, []any{}),
		"statuses":    this.IndexBy(this.SafeList(response, 9, []any{}), 0),
		"marginables": this.SafeList(response, 10, []any{}),
	}
	var indexedNetworks map[string]any = map[string]any{}
	for i := 0; IsLessThan(i, GetArrayLength(GetValue(indexed, "networks"))); i++ {
		var networkObj any = GetValue(GetValue(indexed, "networks"), i)
		var networkId any = this.SafeString(networkObj, 0)
		var valuesList any = this.SafeList(networkObj, 1)
		var networkName any = this.SafeString(valuesList, 0)
		// for GOlang transpiler, do with "safe" method
		var networksList any = this.SafeList(indexedNetworks, networkName, []any{})
		AppendToArray(&networksList, networkId)
		AddElementToObject(indexedNetworks, networkName, networksList)
	}
	var ids any = this.SafeList(response, 0, []any{})

	ch <- this.ParseCurrenciesCustom(ids, indexed, indexedNetworks)
	return nil
}
func (this *Bitfinex) ParseCurrenciesCustom(ids any, indexed any, indexedNetworks any) any {
	var allowedIds any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(ids)); i++ {
		var id any = GetValue(ids, i)
		if IsTrue(EndsWith(id, "F0")) {
			continue
		}
		AppendToArray(&allowedIds, id)
	}
	var result map[string]any = map[string]any{}
	var arr []any = this.ToArray(allowedIds)
	for i := 0; IsLessThan(i, GetArrayLength(arr)); i++ {
		var parsed any = this.ParseCurrencyCustom(GetValue(arr, i), indexed, indexedNetworks)
		var code any = GetValue(parsed, "code")
		AddElementToObject(result, code, parsed)
	}
	return result
}
func (this *Bitfinex) ParseCurrencyCustom(id any, indexed any, indexedNetworks any) any {
	var code any = this.SafeCurrencyCode(id)
	var label any = this.SafeList(GetValue(indexed, "label"), id, []any{})
	var name any = this.SafeString(label, 1)
	var pool any = this.SafeList(GetValue(indexed, "pool"), id, []any{})
	var rawType any = this.SafeString(pool, 1)
	var isCryptoCoin bool = IsTrue((!IsEqual(rawType, nil))) || IsTrue((InOp(GetValue(indexed, "explorer"), id))) // "hacky" solution
	var typeVar any = Ternary(IsTrue(isCryptoCoin), "crypto", nil)
	var feeValues any = this.SafeList(GetValue(indexed, "fees"), id, []any{})
	var fees any = this.SafeList(feeValues, 1, []any{})
	var fee any = this.SafeNumber(fees, 1)
	var undl any = this.SafeList(GetValue(indexed, "undl"), id, []any{})
	var defaultCurrencyPrecision any = this.SafeString(this.Options, "defaultCurrencyPrecision", "8") // kept here for backward-compatibility
	// numberToString instead of an `as string` cast: the describe() default for this option is the
	// NUMBER 8 (and users may override with numbers too), and the hard cast makes the C# build throw
	// InvalidCastException Int32 to String here, breaking bitfinex loadMarkets entirely in C#
	var precision any = this.NumberToString(this.HandleOption("fetchCurrencies", "defaultPrecision", defaultCurrencyPrecision))
	var networks map[string]any = map[string]any{}
	var networkIds any = this.SafeList(indexedNetworks, id, []any{})
	for j := 0; IsLessThan(j, GetArrayLength(networkIds)); j++ {
		// safeString instead of raw access: the venue config payload can carry numeric
		// network ids, and the raw value flows into toLowerCase and a dictionary key,
		// which hard-casts to string in the C# build and throws InvalidCastException
		var networkId any = this.SafeString(networkIds, j)
		if IsTrue(IsEqual(networkId, nil)) {
			continue
		}
		var network any = this.NetworkIdToCode(networkId, code)
		var dwStatuses any = this.SafeList(GetValue(indexed, "statuses"), networkId, []any{})
		if IsTrue(!IsEqual(network, nil)) {
			AddElementToObject(networks, network, map[string]any{
				"info":      networkId,
				"id":        ToLower(networkId),
				"network":   networkId,
				"active":    nil,
				"deposit":   IsEqual(this.SafeInteger(dwStatuses, 1), 1),
				"withdraw":  IsEqual(this.SafeInteger(dwStatuses, 2), 1),
				"fee":       nil,
				"precision": nil,
				"limits": map[string]any{
					"withdraw": map[string]any{
						"min": nil,
						"max": nil,
					},
				},
			})
		}
	}
	return this.SafeCurrencyStructure(map[string]any{
		"id":        id,
		"code":      code,
		"info":      []any{id, label, pool, feeValues, undl},
		"type":      typeVar,
		"name":      name,
		"active":    true,
		"deposit":   nil,
		"withdraw":  nil,
		"fee":       fee,
		"precision": this.ParseNumber(precision),
		"limits": map[string]any{
			"amount": map[string]any{
				"min": nil,
				"max": nil,
			},
			"withdraw": map[string]any{
				"min": fee,
				"max": nil,
			},
		},
		"networks": networks,
		"margin":   this.InArray(id, GetValue(indexed, "marginables")),
	})
}

/**
 * @method
 * @name bitfinex#fetchBalance
 * @description query for balance and get the amount of funds available for trading or funds locked in orders
 * @see https://docs.bitfinex.com/reference/rest-auth-wallets
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [balance structure]{@link https://docs.ccxt.com/?id=balance-structure}
 */
func (this *Bitfinex) FetchBalanceAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchBalanceBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchBalanceBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	// this api call does not return the 'used' amount - use the v1 version instead (which also returns zero balances)
	// there is a difference between this and the v1 api, namely trading wallet is called margin in v2
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes98712 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes98712)
	}
	var accountsByType any = this.SafeDict(this.Options, "v2AccountsByType", map[string]any{})
	var requestedType any = this.SafeString(params, "type", "exchange")
	var accountType any = this.SafeString(accountsByType, requestedType, requestedType)
	if IsTrue(IsEqual(accountType, nil)) {
		var keys []string = ObjectKeys(accountsByType)
		panic(ExchangeError(Add(Add(this.Id, " fetchBalance() type parameter must be one of "), Join(keys, ", "))))
	}
	var isDerivative bool = IsEqual(requestedType, "derivatives")
	var query any = this.Omit(params, "type")

	response := (<-this.PrivatePostAuthRWallets(query))
	PanicOnError(response)
	var balances []any = this.ToArray(response)
	var result map[string]any = map[string]any{
		"info": response,
	}
	for i := 0; IsLessThan(i, GetArrayLength(balances)); i++ {
		var balance any = GetValue(balances, i)
		var account any = this.Account()
		var interest any = this.SafeString(balance, 3)
		if IsTrue(!IsEqual(interest, "0")) {
			AddElementToObject(account, "debt", interest)
		}
		var typeVar any = this.SafeString(balance, 0)
		var currencyId any = this.SafeStringLower(balance, 1, "")
		var start any = Subtract(GetLength(currencyId), 2)
		var isDerivativeCode bool = IsEqual(Slice(currencyId, start, nil), "f0")
		// this will only filter the derivative codes if the requestedType is 'derivatives'
		var derivativeCondition bool = (!IsTrue(isDerivative) || IsTrue(isDerivativeCode))
		if IsTrue(IsTrue((IsEqual(accountType, typeVar))) && IsTrue(derivativeCondition)) {
			var code any = this.SafeCurrencyCode(currencyId)
			AddElementToObject(account, "total", this.SafeString(balance, 2))
			AddElementToObject(account, "free", this.SafeString(balance, 4))
			if IsTrue(!IsEqual(code, nil)) {
				AddElementToObject(result, code, account)
			}
		}
	}

	ch <- this.SafeBalance(result)
	return nil
}

/**
 * @method
 * @name bitfinex#transfer
 * @description transfer currency internally between wallets on the same account
 * @see https://docs.bitfinex.com/reference/rest-auth-transfer
 * @param {string} code unified currency code
 * @param {float} amount amount to transfer
 * @param {string} fromAccount account to transfer from
 * @param {string} toAccount account to transfer to
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [transfer structure]{@link https://docs.ccxt.com/?id=transfer-structure}
 */
func (this *Bitfinex) TransferAsync(code any, amount any, fromAccount any, toAccount any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.transferBody(ch, code, amount, fromAccount, toAccount, optionalArgs...)
	return ch
}
func (this *Bitfinex) transferBody(ch chan any, code any, amount any, fromAccount any, toAccount any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	// transferring between derivatives wallet and regular wallet is not documented in their API
	// however we support it in CCXT (from just looking at web inspector)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes104212 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes104212)
	}
	var accountsByType any = this.SafeDict(this.Options, "v2AccountsByType", map[string]any{})
	var fromId any = this.SafeString(accountsByType, fromAccount)
	if IsTrue(IsEqual(fromId, nil)) {
		var keys []string = ObjectKeys(accountsByType)
		panic(ArgumentsRequired(Add(Add(this.Id, " transfer() fromAccount must be one of "), Join(keys, ", "))))
	}
	var toId any = this.SafeString(accountsByType, toAccount)
	if IsTrue(IsEqual(toId, nil)) {
		var keys []string = ObjectKeys(accountsByType)
		panic(ArgumentsRequired(Add(Add(this.Id, " transfer() toAccount must be one of "), Join(keys, ", "))))
	}
	var currency any = this.Currency(code)
	var fromCurrencyId any = this.ConvertDerivativesId(currency, fromAccount)
	var toCurrencyId any = this.ConvertDerivativesId(currency, toAccount)
	var requestedAmount any = this.CurrencyToPrecision(code, amount)
	// this request is slightly different from v1 fromAccount -> from
	var request map[string]any = map[string]any{
		"amount":      requestedAmount,
		"currency":    fromCurrencyId,
		"currency_to": toCurrencyId,
		"from":        fromId,
		"to":          toId,
	}

	response := (<-this.PrivatePostAuthWTransfer(this.Extend(request, params)))
	PanicOnError(response)
	//
	//     [
	//         1616451183763,
	//         "acc_tf",
	//         null,
	//         null,
	//         [
	//             1616451183763,
	//             "exchange",
	//             "margin",
	//             null,
	//             "UST",
	//             "UST",
	//             null,
	//             1
	//         ],
	//         null,
	//         "SUCCESS",
	//         "1.0 Tether USDt transfered from Exchange to Margin"
	//     ]
	//
	var error any = this.SafeString(response, 0)
	if IsTrue(IsEqual(error, "error")) {
		var message any = this.SafeString(response, 2, "")
		// same message as in v1
		this.ThrowExactlyMatchedException(GetValue(this.Exceptions, "exact"), message, Add(Add(this.Id, " "), message))
		panic(ExchangeError(Add(Add(this.Id, " "), message)))
	}

	ch <- this.ParseTransfer(map[string]any{
		"result": response,
	}, currency)
	return nil
}
func (this *Bitfinex) ParseTransfer(transfer any, optionalArgs ...any) any {
	//
	// transfer
	//
	//     [
	//         1616451183763,
	//         "acc_tf",
	//         null,
	//         null,
	//         [
	//             1616451183763,
	//             "exchange",
	//             "margin",
	//             null,
	//             "UST",
	//             "UST",
	//             null,
	//             1
	//         ],
	//         null,
	//         "SUCCESS",
	//         "1.0 Tether USDt transfered from Exchange to Margin"
	//     ]
	//
	currency := GetArg(optionalArgs, 0, nil)
	_ = currency
	var result any = this.SafeList(transfer, "result")
	var timestamp any = this.SafeInteger(result, 0)
	var info any = this.SafeValue(result, 4)
	var fromAccount any = this.SafeString(info, 1)
	var toAccount any = this.SafeString(info, 2)
	var currencyId any = this.SafeString(info, 5)
	var status any = this.SafeString(result, 6)
	return map[string]any{
		"id":          nil,
		"timestamp":   timestamp,
		"datetime":    this.Iso8601(timestamp),
		"status":      this.ParseTransferStatus(status),
		"amount":      this.SafeNumber(info, 7),
		"currency":    this.SafeCurrencyCode(currencyId, currency),
		"fromAccount": fromAccount,
		"toAccount":   toAccount,
		"info":        result,
	}
}
func (this *Bitfinex) ParseTransferStatus(status any) any {
	var statuses map[string]any = map[string]any{
		"SUCCESS": "ok",
		"ERROR":   "failed",
		"FAILURE": "failed",
	}
	return this.SafeString(statuses, status, status)
}
func (this *Bitfinex) ConvertDerivativesId(currency any, typeVar any) any {
	// there is a difference between this and the v1 api, namely trading wallet is called margin in v2
	// {
	//   "id": "fUSTF0",
	//   "code": "USTF0",
	//   "info": [ 'USTF0', [], [], [], [ "USTF0", "UST" ] ],
	var info any = this.SafeValue(currency, "info")
	var transferId any = this.SafeString(info, 0)
	var underlying any = this.SafeValue(info, 4, []any{})
	var currencyId any = nil
	if IsTrue(IsEqual(typeVar, "derivatives")) {
		currencyId = this.SafeString(underlying, 0, transferId)
		var start any = Subtract(GetLength(currencyId), 2)
		var isDerivativeCode bool = IsEqual(Slice(currencyId, start, nil), "F0")
		if !IsTrue(isDerivativeCode) {
			currencyId = Add(currencyId, "F0")
		}
	} else if IsTrue(!IsEqual(typeVar, "margin")) {
		currencyId = this.SafeString(underlying, 1, transferId)
	} else {
		currencyId = transferId
	}
	return currencyId
}

/**
 * @method
 * @name bitfinex#fetchOrderBook
 * @description fetches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
 * @see https://docs.bitfinex.com/reference/rest-public-book
 * @param {string} symbol unified symbol of the market to fetch the order book for
 * @param {int} [limit] the maximum amount of order book entries to return, bitfinex only allows 1, 25, or 100
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order book structure]{@link https://docs.ccxt.com/?id=order-book-structure}
 */
func (this *Bitfinex) FetchOrderBookAsync(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchOrderBookBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchOrderBookBody(ch chan any, symbol any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	limit := GetArg(optionalArgs, 0, nil)
	_ = limit
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes118912 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes118912)
	}
	var precision any = this.HandleOption("fetchOrderBook", "precision", "R0")
	var market any = this.Market(symbol)
	var request map[string]any = map[string]any{
		"symbol":    GetValue(market, "id"),
		"precision": precision,
	}
	if IsTrue(!IsEqual(limit, nil)) {
		AddElementToObject(request, "len", limit)
	}
	var fullRequest map[string]any = this.Extend(request, params)

	orderbook := (<-this.PublicGetBookSymbolPrecision(fullRequest))
	PanicOnError(orderbook)
	var timestamp int64 = this.Milliseconds()
	var result map[string]any = map[string]any{
		"symbol":    GetValue(market, "symbol"),
		"bids":      []any{},
		"asks":      []any{},
		"timestamp": timestamp,
		"datetime":  this.Iso8601(timestamp),
		"nonce":     nil,
	}
	var priceIndex any = Ternary(IsTrue((IsEqual(GetValue(fullRequest, "precision"), "R0"))), 1, 0)
	var orders []any = this.ToArray(orderbook)
	for i := 0; IsLessThan(i, GetArrayLength(orders)); i++ {
		var order any = GetValue(orders, i)
		var price any = this.SafeNumber(order, priceIndex)
		var signedAmount any = this.SafeString(order, 2)
		var amount any = Precise.StringAbs(signedAmount)
		var side any = Ternary(IsTrue(Precise.StringGt(signedAmount, "0")), "bids", "asks")
		retRes121912 := GetValue(result, side)
		AppendToArray(&retRes121912, []any{price, this.ParseNumber(amount)})
	}
	AddElementToObject(result, "bids", this.SortBy(GetValue(result, "bids"), 0, true))
	AddElementToObject(result, "asks", this.SortBy(GetValue(result, "asks"), 0))

	ch <- result
	return nil
}
func (this *Bitfinex) ParseTicker(ticker any, optionalArgs ...any) any {
	//
	// on trading pairs (ex. tBTCUSD)
	//
	//    [
	//            SYMBOL, // this index is not present in singular-ticker
	//            BID,
	//            BID_SIZE,
	//            ASK,
	//            ASK_SIZE,
	//            DAILY_CHANGE,
	//            DAILY_CHANGE_RELATIVE,
	//            LAST_PRICE,
	//            VOLUME,
	//            HIGH,
	//            LOW
	//    ]
	//
	//
	// on funding currencies (ex. fUSD)
	//
	//    [
	//            SYMBOL, // this index is not present in singular-ticker
	//            FRR,
	//            BID,
	//            BID_PERIOD,
	//            BID_SIZE,
	//            ASK,
	//            ASK_PERIOD,
	//            ASK_SIZE,
	//            DAILY_CHANGE,
	//            DAILY_CHANGE_RELATIVE,
	//            LAST_PRICE,
	//            VOLUME,
	//            HIGH,
	//            LOW,
	//            _PLACEHOLDER,
	//            _PLACEHOLDER,
	//            FRR_AMOUNT_AVAILABLE
	//     ]
	//
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	var length int = GetArrayLength(ticker)
	var firstValue any = this.SafeNumber(ticker, 0)
	var isFetchTicker any = !IsEqual(firstValue, nil) // if it's Nan, then it's string (symbol)
	var symbol any = nil
	var minusIndex int = 0
	if IsTrue(isFetchTicker) {
		minusIndex = 1
	} else {
		var marketId any = this.SafeString(ticker, 0)
		market = this.SafeMarket(marketId, market)
	}
	var isFundingCurrency bool = IsGreaterThanOrEqual(length, 17)
	symbol = this.SafeSymbol(nil, market)
	var last any = nil
	var bid any = nil
	var ask any = nil
	var change any = nil
	var percentage any = nil
	var volume any = nil
	var high any = nil
	var low any = nil
	if IsTrue(isFundingCurrency) {
		// per api docs, they are different array type
		last = this.SafeString(ticker, Subtract(10, minusIndex))
		bid = this.SafeString(ticker, Subtract(2, minusIndex))
		ask = this.SafeString(ticker, Subtract(5, minusIndex))
		change = this.SafeString(ticker, Subtract(8, minusIndex))
		percentage = this.SafeString(ticker, Subtract(9, minusIndex))
		volume = this.SafeString(ticker, Subtract(11, minusIndex))
		high = this.SafeString(ticker, Subtract(12, minusIndex))
		low = this.SafeString(ticker, Subtract(13, minusIndex))
	} else {
		// on trading pairs (ex. tBTCUSD or tHMSTR:USD)
		last = this.SafeString(ticker, Subtract(7, minusIndex))
		bid = this.SafeString(ticker, Subtract(1, minusIndex))
		ask = this.SafeString(ticker, Subtract(3, minusIndex))
		change = this.SafeString(ticker, Subtract(5, minusIndex))
		percentage = this.SafeString(ticker, Subtract(6, minusIndex))
		percentage = Precise.StringMul(percentage, "100")
		volume = this.SafeString(ticker, Subtract(8, minusIndex))
		high = this.SafeString(ticker, Subtract(9, minusIndex))
		low = this.SafeString(ticker, Subtract(10, minusIndex))
	}
	return this.SafeTicker(map[string]any{
		"symbol":        symbol,
		"timestamp":     nil,
		"datetime":      nil,
		"high":          high,
		"low":           low,
		"bid":           bid,
		"bidVolume":     nil,
		"ask":           ask,
		"askVolume":     nil,
		"vwap":          nil,
		"open":          nil,
		"close":         last,
		"last":          last,
		"previousClose": nil,
		"change":        change,
		"percentage":    percentage,
		"average":       nil,
		"baseVolume":    volume,
		"quoteVolume":   nil,
		"info":          ticker,
	}, market)
}

/**
 * @method
 * @name bitfinex#fetchTickers
 * @description fetches price tickers for multiple markets, statistical information calculated over the past 24 hours for each market
 * @see https://docs.bitfinex.com/reference/rest-public-tickers
 * @param {string[]|undefined} symbols unified symbols of the markets to fetch the ticker for, all market tickers are returned if not assigned
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a dictionary of [ticker structures]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
func (this *Bitfinex) FetchTickersAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchTickersBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchTickersBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbols := GetArg(optionalArgs, 0, nil)
	_ = symbols
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes134512 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes134512)
	}
	symbols = this.MarketSymbols(symbols)
	var request map[string]any = map[string]any{}
	if IsTrue(!IsEqual(symbols, nil)) {
		var ids any = this.MarketIds(symbols)
		AddElementToObject(request, "symbols", Join(ids, ","))
	} else {
		AddElementToObject(request, "symbols", "ALL")
	}

	tickers := (<-this.PublicGetTickers(this.Extend(request, params)))
	PanicOnError(tickers)

	//
	//     [
	//         // on trading pairs (ex. tBTCUSD)
	//         [
	//             SYMBOL,
	//             BID,
	//             BID_SIZE,
	//             ASK,
	//             ASK_SIZE,
	//             DAILY_CHANGE,
	//             DAILY_CHANGE_RELATIVE,
	//             LAST_PRICE,
	//             VOLUME,
	//             HIGH,
	//             LOW
	//         ],
	//         // on funding currencies (ex. fUSD)
	//         [
	//             SYMBOL,
	//             FRR,
	//             BID,
	//             BID_PERIOD,
	//             BID_SIZE,
	//             ASK,
	//             ASK_PERIOD,
	//             ASK_SIZE,
	//             DAILY_CHANGE,
	//             DAILY_CHANGE_RELATIVE,
	//             LAST_PRICE,
	//             VOLUME,
	//             HIGH,
	//             LOW,
	//             _PLACEHOLDER,
	//             _PLACEHOLDER,
	//             FRR_AMOUNT_AVAILABLE
	//         ],
	//         ...
	//     ]
	//
	ch <- this.ParseTickers(tickers, symbols)
	return nil
}

/**
 * @method
 * @name bitfinex#fetchTicker
 * @description fetches a price ticker, a statistical calculation with the information calculated over the past 24 hours for a specific market
 * @see https://docs.bitfinex.com/reference/rest-public-ticker
 * @param {string} symbol unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
func (this *Bitfinex) FetchTickerAsync(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchTickerBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchTickerBody(ch chan any, symbol any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes140912 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes140912)
	}
	var market any = this.Market(symbol)
	var request map[string]any = map[string]any{
		"symbol": GetValue(market, "id"),
	}

	ticker := (<-this.PublicGetTickerSymbol(this.Extend(request, params)))
	PanicOnError(ticker)

	ch <- this.ParseTicker(ticker, market)
	return nil
}
func (this *Bitfinex) ParseTrade(trade any, optionalArgs ...any) any {
	//
	// fetchTrades (public)
	//
	//     [
	//         ID,
	//         MTS, // timestamp
	//         AMOUNT,
	//         PRICE
	//     ]
	//
	// fetchMyTrades (private)
	//
	//     [
	//         ID,
	//         PAIR,
	//         MTS_CREATE,
	//         ORDER_ID,
	//         EXEC_AMOUNT,
	//         EXEC_PRICE,
	//         ORDER_TYPE,
	//         ORDER_PRICE,
	//         MAKER,
	//         FEE,
	//         FEE_CURRENCY,
	//         ...
	//     ]
	//
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	var tradeList any = this.SafeList(trade, "result", []any{})
	var tradeLength int = GetArrayLength(tradeList)
	var isPrivate bool = (IsGreaterThan(tradeLength, 5))
	var id any = this.SafeString(tradeList, 0)
	var amountIndex any = Ternary(IsTrue(isPrivate), 4, 2)
	var side any = nil
	var amountString any = this.SafeString(tradeList, amountIndex)
	var priceIndex any = Ternary(IsTrue(isPrivate), 5, 3)
	var priceString any = this.SafeString(tradeList, priceIndex)
	if IsTrue(IsEqual(GetValue(amountString, 0), "-")) {
		side = "sell"
		amountString = Precise.StringAbs(amountString)
	} else {
		side = "buy"
	}
	var orderId any = nil
	var takerOrMaker any = nil
	var typeVar any = nil
	var fee any = nil
	var symbol any = this.SafeSymbol(nil, market)
	var timestampIndex any = Ternary(IsTrue(isPrivate), 2, 1)
	var timestamp any = this.SafeInteger(tradeList, timestampIndex)
	if IsTrue(isPrivate) {
		var marketId any = GetValue(tradeList, 1)
		symbol = this.SafeSymbol(marketId)
		orderId = this.SafeString(tradeList, 3)
		var maker any = this.SafeInteger(tradeList, 8)
		takerOrMaker = Ternary(IsTrue((IsEqual(maker, 1))), "maker", "taker")
		var feeCostString any = this.SafeString(tradeList, 9)
		feeCostString = Precise.StringNeg(feeCostString)
		var feeCurrencyId any = this.SafeString(tradeList, 10)
		var feeCurrency any = this.SafeCurrencyCode(feeCurrencyId)
		fee = map[string]any{
			"cost":     feeCostString,
			"currency": feeCurrency,
		}
		var orderType any = GetValue(tradeList, 6)
		typeVar = this.SafeString(GetValue(this.Options, "exchangeTypes"), orderType)
	}
	return this.SafeTrade(map[string]any{
		"id":           id,
		"timestamp":    timestamp,
		"datetime":     this.Iso8601(timestamp),
		"symbol":       symbol,
		"order":        orderId,
		"side":         side,
		"type":         typeVar,
		"takerOrMaker": takerOrMaker,
		"price":        priceString,
		"amount":       amountString,
		"cost":         nil,
		"fee":          fee,
		"info":         tradeList,
	}, market)
}

/**
 * @method
 * @name bitfinex#fetchTrades
 * @description get the list of most recent trades for a particular symbol
 * @see https://docs.bitfinex.com/reference/rest-public-trades
 * @param {string} symbol unified symbol of the market to fetch trades for
 * @param {int} [since] timestamp in ms of the earliest trade to fetch
 * @param {int} [limit] the maximum amount of trades to fetch, default 120, max 10000
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @param {int} [params.until] the latest time in ms to fetch entries for
 * @returns {Trade[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=public-trades}
 */
func (this *Bitfinex) FetchTradesAsync(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchTradesBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchTradesBody(ch chan any, symbol any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	since := GetArg(optionalArgs, 0, nil)
	_ = since
	limit := GetArg(optionalArgs, 1, nil)
	_ = limit
	params := GetArg(optionalArgs, 2, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes151812 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes151812)
	}
	var paginate any = false
	paginateparamsVariable := this.HandleOptionAndParams(params, "fetchTrades", "paginate")
	paginate = GetValue(paginateparamsVariable, 0)
	params = GetValue(paginateparamsVariable, 1)
	if IsTrue(paginate) {

		retRes152319 := (<-this.FetchPaginatedCallDynamicAsync("fetchTrades", symbol, since, limit, params, 10000))
		PanicOnError(retRes152319)
		ch <- retRes152319
		return nil
	}
	var market any = this.Market(symbol)
	var sort string = "-1"
	var request any = map[string]any{
		"symbol": GetValue(market, "id"),
	}
	if IsTrue(!IsEqual(since, nil)) {
		AddElementToObject(request, "start", since)
		sort = "1"
	}
	if IsTrue(!IsEqual(limit, nil)) {
		AddElementToObject(request, "limit", mathMin(limit, 10000)) // default 120, max 10000
	}
	AddElementToObject(request, "sort", sort)
	requestparamsVariable := this.HandleUntilOption("end", request, params)
	request = GetValue(requestparamsVariable, 0)
	params = GetValue(requestparamsVariable, 1)

	response := (<-this.PublicGetTradesSymbolHist(this.Extend(request, params)))
	PanicOnError(response)
	//
	//     [
	//         [
	//             ID,
	//             MTS, // timestamp
	//             AMOUNT,
	//             PRICE
	//         ]
	//     ]
	//
	var rawTrades []any = this.ToArray(response)
	var trades []any = this.SortBy(rawTrades, 1)
	var tradesList any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(trades)); i++ {
		AppendToArray(&tradesList, map[string]any{
			"result": GetValue(trades, i),
		}) // convert to array of dicts to match parseOrder signature
	}

	ch <- this.ParseTrades(tradesList, market, nil, limit)
	return nil
}

/**
 * @method
 * @name bitfinex#fetchOHLCV
 * @description fetches historical candlestick data containing the open, high, low, and close price, and the volume of a market
 * @see https://docs.bitfinex.com/reference/rest-public-candles
 * @param {string} symbol unified symbol of the market to fetch OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum amount of candles to fetch, default 100 max 10000
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 * @param {int} [params.until] timestamp in ms of the latest candle to fetch
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 */
func (this *Bitfinex) FetchOHLCVAsync(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchOHLCVBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchOHLCVBody(ch chan any, symbol any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	timeframe := GetArg(optionalArgs, 0, "1m")
	_ = timeframe
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, 100)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes157512 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes157512)
	}
	var paginate any = false
	paginateparamsVariable := this.HandleOptionAndParams(params, "fetchOHLCV", "paginate")
	paginate = GetValue(paginateparamsVariable, 0)
	params = GetValue(paginateparamsVariable, 1)
	if IsTrue(paginate) {

		retRes158019 := (<-this.FetchPaginatedCallDeterministicAsync("fetchOHLCV", symbol, since, limit, timeframe, params, 10000))
		PanicOnError(retRes158019)
		ch <- retRes158019
		return nil
	}
	var market any = this.Market(symbol)
	if IsTrue(IsEqual(limit, nil)) {
		limit = 10000
	} else {
		limit = mathMin(limit, 10000)
	}
	var request any = map[string]any{
		"symbol":    GetValue(market, "id"),
		"timeframe": this.SafeString(this.Timeframes, timeframe, timeframe),
		"limit":     limit,
	}
	if IsTrue(!IsEqual(since, nil)) {
		AddElementToObject(request, "start", since)
		AddElementToObject(request, "sort", 1)
	}
	requestparamsVariable := this.HandleUntilOption("end", request, params)
	request = GetValue(requestparamsVariable, 0)
	params = GetValue(requestparamsVariable, 1)

	response := (<-this.PublicGetCandlesTradeTimeframeSymbolHist(this.Extend(request, params)))
	PanicOnError(response)

	//
	//     [
	//         [1591503840000,0.025069,0.025068,0.025069,0.025068,1.97828998],
	//         [1591504500000,0.025065,0.025065,0.025065,0.025065,1.0164],
	//         [1591504620000,0.025062,0.025062,0.025062,0.025062,0.5],
	//     ]
	//
	ch <- this.ParseOHLCVs(this.ToArray(response), market, timeframe, since, limit)
	return nil
}
func (this *Bitfinex) ParseOHLCV(ohlcv any, optionalArgs ...any) any {
	//
	//     [
	//         1457539800000,
	//         0.02594,
	//         0.02594,
	//         0.02594,
	//         0.02594,
	//         0.1
	//     ]
	//
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	return []any{this.SafeInteger(ohlcv, 0), this.SafeNumber(ohlcv, 1), this.SafeNumber(ohlcv, 3), this.SafeNumber(ohlcv, 4), this.SafeNumber(ohlcv, 2), this.SafeNumber(ohlcv, 5)}
}
func (this *Bitfinex) ParseOrderStatus(status any) any {
	if IsTrue(IsEqual(status, nil)) {
		return nil
	}
	var parts []string = Split(status, " ")
	var state any = this.SafeString(parts, 0)
	var statuses map[string]any = map[string]any{
		"ACTIVE":              "open",
		"PARTIALLY":           "open",
		"EXECUTED":            "closed",
		"CANCELED":            "canceled",
		"INSUFFICIENT":        "canceled",
		"POSTONLY CANCELED":   "canceled",
		"RSN_DUST":            "rejected",
		"RSN_PAUSE":           "rejected",
		"IOC CANCELED":        "canceled",
		"FILLORKILL CANCELED": "canceled",
	}
	return this.SafeString(statuses, state, status)
}
func (this *Bitfinex) ParseOrderFlags(flags any) any {
	// flags can be added to each other...
	var flagValues map[string]any = map[string]any{
		"1024": []any{"reduceOnly"},
		"4096": []any{"postOnly"},
		"5120": []any{"reduceOnly", "postOnly"},
	}
	return this.SafeValue(flagValues, flags, nil)
}
func (this *Bitfinex) ParseTimeInForce(orderType any) any {
	var orderTypes map[string]any = map[string]any{
		"EXCHANGE IOC": "IOC",
		"EXCHANGE FOK": "FOK",
		"IOC":          "IOC",
		"FOK":          "FOK",
	}
	return this.SafeString(orderTypes, orderType, "GTC")
}
func (this *Bitfinex) ParseOrder(order any, optionalArgs ...any) any {
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	var orderList any = this.SafeList(order, "result")
	var id any = this.SafeString(orderList, 0)
	var marketId any = this.SafeString(orderList, 3)
	var symbol any = this.SafeSymbol(marketId)
	// https://github.com/ccxt/ccxt/issues/6686
	// const timestamp = this.safeTimestamp (orderObject, 5);
	var timestamp any = this.SafeInteger(orderList, 5)
	var remaining any = Precise.StringAbs(this.SafeString(orderList, 6))
	var signedAmount any = this.SafeString(orderList, 7)
	var amount any = Precise.StringAbs(signedAmount)
	var side any = Ternary(IsTrue(Precise.StringLt(signedAmount, "0")), "sell", "buy")
	var orderType any = this.SafeString(orderList, 8)
	var typeVar any = this.SafeString(this.SafeValue(this.Options, "exchangeTypes"), orderType)
	var timeInForce any = this.ParseTimeInForce(orderType)
	var rawFlags any = this.SafeString(orderList, 12)
	var flags any = this.ParseOrderFlags(rawFlags)
	var postOnly bool = false
	if IsTrue(!IsEqual(flags, nil)) {
		for i := 0; IsLessThan(i, GetArrayLength(flags)); i++ {
			if IsTrue(IsEqual(GetValue(flags, i), "postOnly")) {
				postOnly = true
			}
		}
	}
	var price any = this.SafeString(orderList, 16)
	var triggerPrice any = nil
	if IsTrue(IsTrue((IsEqual(orderType, "EXCHANGE STOP"))) || IsTrue((IsEqual(orderType, "EXCHANGE STOP LIMIT")))) {
		price = nil
		triggerPrice = this.SafeString(orderList, 16)
		if IsTrue(IsEqual(orderType, "EXCHANGE STOP LIMIT")) {
			price = this.SafeString(orderList, 19)
		}
	}
	var status any = nil
	var statusString any = this.SafeString(orderList, 13)
	if IsTrue(!IsEqual(statusString, nil)) {
		var parts []string = Split(statusString, " @ ")
		status = this.ParseOrderStatus(this.SafeString(parts, 0))
	}
	var average any = this.SafeString(orderList, 17)
	var clientOrderId any = this.SafeString(orderList, 2)
	return this.SafeOrder(map[string]any{
		"info":               orderList,
		"id":                 id,
		"clientOrderId":      clientOrderId,
		"timestamp":          timestamp,
		"datetime":           this.Iso8601(timestamp),
		"lastTradeTimestamp": nil,
		"symbol":             symbol,
		"type":               typeVar,
		"timeInForce":        timeInForce,
		"postOnly":           postOnly,
		"side":               side,
		"price":              price,
		"triggerPrice":       triggerPrice,
		"amount":             amount,
		"cost":               nil,
		"average":            average,
		"filled":             nil,
		"remaining":          remaining,
		"status":             status,
		"fee":                nil,
		"trades":             nil,
	}, market)
}
func (this *Bitfinex) CreateOrderRequest(symbol any, typeVar any, side any, amount any, optionalArgs ...any) any {
	price := GetArg(optionalArgs, 0, nil)
	_ = price
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(typeVar, nil)) {
		panic(ArgumentsRequired(Add(this.Id, " requires a type argument")))
	}
	if IsTrue(IsEqual(side, nil)) {
		panic(ArgumentsRequired(Add(this.Id, " requires a side argument")))
	}
	/**
	 * @method
	 * @ignore
	 * @name bitfinex#createOrderRequest
	 * @description helper function to build an order request
	 * @param {string} symbol unified symbol of the market to create an order in
	 * @param {string} type 'market' or 'limit'
	 * @param {string} side 'buy' or 'sell'
	 * @param {float} amount how much you want to trade in units of the base currency
	 * @param {float} [price] the price of the order, in units of the quote currency, ignored in market orders
	 * @param {object} [params] extra parameters specific to the exchange API endpoint
	 * @param {float} [params.triggerPrice] The price at which a trigger order is triggered at
	 * @param {string} [params.timeInForce] "GTC", "IOC", "FOK", or "PO"
	 * @param {bool} [params.postOnly]
	 * @param {bool} [params.reduceOnly] Ensures that the executed order does not flip the opened position.
	 * @param {int} [params.flags] additional order parameters: 4096 (Post Only), 1024 (Reduce Only), 16384 (OCO), 64 (Hidden), 512 (Close), 524288 (No Var Rates)
	 * @param {int} [params.lev] leverage for a derivative order, supported by derivative symbol orders only. The value should be between 1 and 100 inclusive.
	 * @param {string} [params.price_traling] The trailing price for a trailing stop order
	 * @param {string} [params.price_aux_limit] Order price for stop limit orders
	 * @param {string} [params.price_oco_stop] OCO stop price
	 * @returns {object} an [order structure]{@link https://github.com/ccxt/ccxt/wiki/Manual#order-structure}
	 */
	var market any = this.Market(symbol)
	var amountString any = this.AmountToPrecision(symbol, amount)
	amountString = Ternary(IsTrue((IsEqual(side, "buy"))), amountString, Precise.StringNeg(amountString))
	var request map[string]any = map[string]any{
		"symbol": GetValue(market, "id"),
		"amount": amountString,
	}
	var triggerPrice any = this.SafeString2(params, "stopPrice", "triggerPrice")
	var trailingAmount any = this.SafeString(params, "trailingAmount")
	var timeInForce any = this.SafeString(params, "timeInForce")
	var postOnlyParam any = this.SafeBool(params, "postOnly", false)
	var reduceOnly any = this.SafeBool(params, "reduceOnly", false)
	var clientOrderId any = this.SafeValue2(params, "cid", "clientOrderId")
	var orderType any = ToUpper(typeVar)
	if IsTrue(!IsEqual(trailingAmount, nil)) {
		orderType = "TRAILING STOP"
		AddElementToObject(request, "price_trailing", trailingAmount)
	} else if IsTrue(!IsEqual(triggerPrice, nil)) {
		// request['price'] is taken as triggerPrice for stop orders
		AddElementToObject(request, "price", this.PriceToPrecision(symbol, triggerPrice))
		if IsTrue(IsEqual(typeVar, "limit")) {
			orderType = "STOP LIMIT"
			AddElementToObject(request, "price_aux_limit", this.PriceToPrecision(symbol, price))
		} else {
			orderType = "STOP"
		}
	}
	var ioc bool = (IsEqual(timeInForce, "IOC"))
	var fok bool = (IsEqual(timeInForce, "FOK"))
	var postOnly bool = (IsTrue((IsEqual(postOnlyParam, true))) || IsTrue((IsEqual(timeInForce, "PO"))))
	if IsTrue(IsTrue((IsTrue(ioc) || IsTrue(fok))) && IsTrue((IsEqual(price, nil)))) {
		panic(InvalidOrder(Add(this.Id, " createOrder() requires a price argument with IOC and FOK orders")))
	}
	if IsTrue(IsTrue((IsTrue(ioc) || IsTrue(fok))) && IsTrue((IsEqual(typeVar, "market")))) {
		panic(InvalidOrder(Add(this.Id, " createOrder() does not allow market IOC and FOK orders")))
	}
	if IsTrue(IsTrue((!IsEqual(typeVar, "market"))) && IsTrue((IsEqual(triggerPrice, nil)))) {
		AddElementToObject(request, "price", this.PriceToPrecision(symbol, price))
	}
	if IsTrue(ioc) {
		orderType = "IOC"
	} else if IsTrue(fok) {
		orderType = "FOK"
	}
	var marginMode any = nil
	marginModeparamsVariable := this.HandleMarginModeAndParams("createOrder", params)
	marginMode = GetValue(marginModeparamsVariable, 0)
	params = GetValue(marginModeparamsVariable, 1)
	if IsTrue(IsTrue((IsEqual(GetValue(market, "spot"), true))) && IsTrue((IsEqual(marginMode, nil)))) {
		// The EXCHANGE prefix is only required for non margin spot markets
		orderType = Add("EXCHANGE ", orderType)
	}
	AddElementToObject(request, "type", orderType)
	// flag values may be summed to combine flags
	var flags any = 0
	if IsTrue(postOnly) {
		flags = this.Sum(flags, 4096)
	}
	if IsTrue(IsEqual(reduceOnly, true)) {
		flags = this.Sum(flags, 1024)
	}
	if IsTrue(!IsEqual(flags, 0)) {
		AddElementToObject(request, "flags", flags)
	}
	if IsTrue(!IsEqual(clientOrderId, nil)) {
		AddElementToObject(request, "cid", clientOrderId)
	}
	params = this.Omit(params, []any{"triggerPrice", "stopPrice", "timeInForce", "postOnly", "reduceOnly", "trailingAmount", "clientOrderId"})
	return this.Extend(request, params)
}

/**
 * @method
 * @name bitfinex#createOrder
 * @description create an order on the exchange
 * @see https://docs.bitfinex.com/reference/rest-auth-submit-order
 * @param {string} symbol unified CCXT market symbol
 * @param {string} type 'limit' or 'market'
 * @param {string} side 'buy' or 'sell'
 * @param {float} amount the amount of currency to trade
 * @param {float} [price] price of the order
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {float} [params.triggerPrice] the price that triggers a trigger order
 * @param {string} [params.timeInForce] "GTC", "IOC", "FOK", or "PO"
 * @param {boolean} [params.postOnly] set to true if you want to make a post only order
 * @param {boolean} [params.reduceOnly] indicates that the order is to reduce the size of a position
 * @param {int} [params.flags] additional order parameters: 4096 (Post Only), 1024 (Reduce Only), 16384 (OCO), 64 (Hidden), 512 (Close), 524288 (No Var Rates)
 * @param {int} [params.lev] leverage for a derivative order, supported by derivative symbol orders only. The value should be between 1 and 100 inclusive.
 * @param {string} [params.price_aux_limit] order price for stop limit orders
 * @param {string} [params.price_oco_stop] OCO stop price
 * @param {string} [params.trailingAmount] *swap only* the quote amount to trail away from the current market price
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) CreateOrderAsync(symbol any, typeVar any, side any, amount any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.createOrderBody(ch, symbol, typeVar, side, amount, optionalArgs...)
	return ch
}
func (this *Bitfinex) createOrderBody(ch chan any, symbol any, typeVar any, side any, amount any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	price := GetArg(optionalArgs, 0, nil)
	_ = price
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes186412 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes186412)
	}
	var market any = this.Market(symbol)
	var request any = this.CreateOrderRequest(symbol, typeVar, side, amount, price, params)

	response := (<-this.PrivatePostAuthWOrderSubmit(request))
	PanicOnError(response)
	//
	//      [
	//          1653325121,   // Timestamp in milliseconds
	//          "on-req",     // Purpose of notification ('on-req', 'oc-req', "uca", 'fon-req', "foc-req")
	//          null,         // unique ID of the message
	//          null,
	//              [
	//                  [
	//                      95412102131,            // Order ID
	//                      null,                   // Group ID
	//                      1653325121798,          // Client Order ID
	//                      "tDOGE:UST",            // Market ID
	//                      1653325121798,          // Millisecond timestamp of creation
	//                      1653325121798,          // Millisecond timestamp of update
	//                      -10,                    // Amount (Positive means buy, negative means sell)
	//                      -10,                    // Original amount
	//                      "EXCHANGE LIMIT",       // Type of the order: LIMIT, EXCHANGE LIMIT, MARKET, EXCHANGE MARKET, STOP, EXCHANGE STOP, STOP LIMIT, EXCHANGE STOP LIMIT, TRAILING STOP, EXCHANGE TRAILING STOP, FOK, EXCHANGE FOK, IOC, EXCHANGE IOC.
	//                      null,                   // Previous order type (stop-limit orders are converted to limit orders so for them previous type is always STOP)
	//                      null,                   // Millisecond timestamp of Time-In-Force: automatic order cancellation
	//                      null,                   // _PLACEHOLDER
	//                      4096,                   // Flags, see parseOrderFlags()
	//                      "ACTIVE",               // Order Status, see parseOrderStatus()
	//                      null,                   // _PLACEHOLDER
	//                      null,                   // _PLACEHOLDER
	//                      0.071,                  // Price (Stop Price for stop-limit orders, Limit Price for limit orders)
	//                      0,                      // Average Price
	//                      0,                      // Trailing Price
	//                      0,                      // Auxiliary Limit price (for STOP LIMIT)
	//                      null,                   // _PLACEHOLDER
	//                      null,                   // _PLACEHOLDER
	//                      null,                   // _PLACEHOLDER
	//                      0,                      // Hidden (0 if false, 1 if true)
	//                      0,                      // Placed ID (If another order caused this order to be placed (OCO) this will be that other order's ID)
	//                      null,                   // _PLACEHOLDER
	//                      null,                   // _PLACEHOLDER
	//                      null,                   // _PLACEHOLDER
	//                      "API>BFX",              // Routing, indicates origin of action: BFX, ETHFX, API>BFX, API>ETHFX
	//                      null,                   // _PLACEHOLDER
	//                      null,                   // _PLACEHOLDER
	//                      {"$F7":1}               // additional meta information about the order ( $F7 = IS_POST_ONLY (0 if false, 1 if true), $F33 = Leverage (int))
	//                  ]
	//              ],
	//          null,      // CODE (work in progress)
	//          "SUCCESS",                    // Status of the request
	//          "Submitting 1 orders."      // Message
	//       ]
	//
	var status any = this.SafeString(response, 6)
	if IsTrue(!IsEqual(status, "SUCCESS")) {
		var errorCode any = this.SafeString(response, 5)
		var errorText any = this.SafeString(response, 7)
		panic(ExchangeError(Add(Add(Add(Add(Add(Add(Add(this.Id, " "), status), ": "), errorText), " (#"), errorCode), ")")))
	}
	var orders any = this.SafeList(response, 4, []any{})
	var order any = this.SafeList(orders, 0)
	var newOrder map[string]any = map[string]any{
		"result": order,
	}

	ch <- this.ParseOrder(newOrder, market)
	return nil
}

/**
 * @method
 * @name bitfinex#createOrders
 * @description create a list of trade orders
 * @see https://docs.bitfinex.com/reference/rest-auth-order-multi
 * @param {Array} orders list of orders to create, each object should contain the parameters required by createOrder, namely symbol, type, side, amount, price and params
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) CreateOrdersAsync(orders any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.createOrdersBody(ch, orders, optionalArgs...)
	return ch
}
func (this *Bitfinex) createOrdersBody(ch chan any, orders any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes193912 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes193912)
	}
	var ordersRequests any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(orders)); i++ {
		var rawOrder any = GetValue(orders, i)
		var symbol any = this.SafeString(rawOrder, "symbol")
		var typeVar any = this.SafeString(rawOrder, "type")
		var side any = this.SafeString(rawOrder, "side")
		var amount any = this.SafeNumber(rawOrder, "amount")
		var price any = this.SafeNumber(rawOrder, "price")
		var orderParams any = this.SafeDict(rawOrder, "params", map[string]any{})
		var orderRequest any = this.CreateOrderRequest(symbol, typeVar, side, amount, price, orderParams)
		AppendToArray(&ordersRequests, []any{"on", orderRequest})
	}
	var request map[string]any = map[string]any{
		"ops": ordersRequests,
	}

	response := (<-this.PrivatePostAuthWOrderMulti(request))
	PanicOnError(response)
	//
	//     [
	//         1706762515553,
	//         "ox_multi-req",
	//         null,
	//         null,
	//         [
	//             [
	//                 1706762515,
	//                 "on-req",
	//                 null,
	//                 null,
	//                 [
	//                     [139567428547,null,1706762515551,"tBTCUST",1706762515551,1706762515551,0.0001,0.0001,"EXCHANGE LIMIT",null,null,null,0,"ACTIVE",null,null,35000,0,0,0,null,null,null,0,0,null,null,null,"API>BFX",null,null,{}]
	//                 ],
	//                 null,
	//                 "SUCCESS",
	//                 "Submitting 1 orders."
	//             ],
	//         ],
	//         null,
	//         "SUCCESS",
	//         "Submitting 2 order operations."
	//     ]
	//
	var results any = []any{}
	var data any = this.SafeList(response, 4, []any{})
	for i := 0; IsLessThan(i, GetArrayLength(data)); i++ {
		var entry any = GetValue(data, i)
		var individualOrder any = GetValue(entry, 4)
		AppendToArray(&results, map[string]any{
			"result": GetValue(individualOrder, 0),
		})
	}

	ch <- this.ParseOrders(results)
	return nil
}

/**
 * @method
 * @name bitfinex#cancelAllOrders
 * @description cancel all open orders
 * @see https://docs.bitfinex.com/reference/rest-auth-cancel-orders-multiple
 * @param {string} [symbol] unified market symbol, only orders in the market of this symbol are cancelled when symbol is not undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) CancelAllOrdersAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.cancelAllOrdersBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) cancelAllOrdersBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes200312 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes200312)
	}
	var request map[string]any = map[string]any{
		"all": 1,
	}

	response := (<-this.PrivatePostAuthWOrderCancelMulti(this.Extend(request, params)))
	PanicOnError(response)
	var orders any = this.SafeList(response, 4, []any{})
	var ordersList any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(orders)); i++ {
		AppendToArray(&ordersList, map[string]any{
			"result": GetValue(orders, i),
		})
	}

	ch <- this.ParseOrders(ordersList)
	return nil
}

/**
 * @method
 * @name bitfinex#cancelOrder
 * @description cancels an open order
 * @see https://docs.bitfinex.com/reference/rest-auth-cancel-order
 * @param {string} id order id
 * @param {string} symbol Not used by bitfinex cancelOrder ()
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} An [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) CancelOrderAsync(id any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.cancelOrderBody(ch, id, optionalArgs...)
	return ch
}
func (this *Bitfinex) cancelOrderBody(ch chan any, id any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes202912 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes202912)
	}
	var cid any = this.SafeValue2(params, "cid", "clientOrderId") // client order id
	var request any = nil
	var market any = nil
	if IsTrue(!IsEqual(symbol, nil)) {
		market = this.Market(symbol)
	}
	if IsTrue(!IsEqual(cid, nil)) {
		var cidDate any = this.SafeValue(params, "cidDate") // client order id date
		if IsTrue(IsEqual(cidDate, nil)) {
			panic(InvalidOrder(Add(this.Id, " canceling an order by clientOrderId ('cid') requires both 'cid' and 'cid_date' ('YYYY-MM-DD')")))
		}
		request = map[string]any{
			"cid":      cid,
			"cid_date": cidDate,
		}
		params = this.Omit(params, []any{"cid", "clientOrderId"})
	} else {
		request = map[string]any{
			"id": ParseInt(id),
		}
	}

	response := (<-this.PrivatePostAuthWOrderCancel(this.Extend(request, params)))
	PanicOnError(response)
	var order any = this.SafeValue(response, 4)
	var newOrder map[string]any = map[string]any{
		"result": order,
	}

	ch <- this.ParseOrder(newOrder, market)
	return nil
}

/**
 * @method
 * @name bitfinex#cancelOrders
 * @description cancel multiple orders at the same time
 * @see https://docs.bitfinex.com/reference/rest-auth-cancel-orders-multiple
 * @param {string[]} ids order ids
 * @param {string} symbol unified market symbol, default is undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an array of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) CancelOrdersAsync(ids any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.cancelOrdersBody(ch, ids, optionalArgs...)
	return ch
}
func (this *Bitfinex) cancelOrdersBody(ch chan any, ids any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes207012 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes207012)
	}
	var numericIds any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(ids)); i++ {
		// numericIds[i] = this.parseToNumeric (ids[i]);
		AppendToArray(&numericIds, this.ParseToNumeric(GetValue(ids, i)))
	}
	var request map[string]any = map[string]any{
		"id": numericIds,
	}
	var market any = nil
	if IsTrue(!IsEqual(symbol, nil)) {
		market = this.Market(symbol)
	}

	response := (<-this.PrivatePostAuthWOrderCancelMulti(this.Extend(request, params)))
	PanicOnError(response)
	//
	//     [
	//         1706740198811,
	//         "oc_multi-req",
	//         null,
	//         null,
	//         [
	//             [
	//                 139530205057,
	//                 null,
	//                 1706740132275,
	//                 "tBTCF0:USTF0",
	//                 1706740132276,
	//                 1706740132276,
	//                 0.0001,
	//                 0.0001,
	//                 "LIMIT",
	//                 null,
	//                 null,
	//                 null,
	//                 0,
	//                 "ACTIVE",
	//                 null,
	//                 null,
	//                 39000,
	//                 0,
	//                 0,
	//                 0,
	//                 null,
	//                 null,
	//                 null,
	//                 0,
	//                 0,
	//                 null,
	//                 null,
	//                 null,
	//                 "API>BFX",
	//                 null,
	//                 null,
	//                 {
	//                     "lev": 10,
	//                     "$F33": 10
	//                 }
	//             ],
	//         ],
	//         null,
	//         "SUCCESS",
	//         "Submitting 2 order cancellations."
	//     ]
	//
	var orders any = this.SafeList(response, 4, []any{})
	var ordersList any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(orders)); i++ {
		AppendToArray(&ordersList, map[string]any{
			"result": GetValue(orders, i),
		})
	}

	ch <- this.ParseOrders(ordersList, market)
	return nil
}

/**
 * @method
 * @name bitfinex#fetchOpenOrder
 * @description fetch an open order by it's id
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders-by-symbol
 * @param {string} id order id
 * @param {string} symbol unified market symbol, default is undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) FetchOpenOrderAsync(id any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchOpenOrderBody(ch, id, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchOpenOrderBody(ch chan any, id any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	var request map[string]any = map[string]any{
		"id": []any{ParseInt(id)},
	}

	orders := (<-this.FetchOpenOrdersAsync(symbol, nil, nil, this.Extend(request, params)))
	PanicOnError(orders)
	var order any = this.SafeValue(orders, 0)
	if IsTrue(IsEqual(order, nil)) {
		panic(OrderNotFound(Add(Add(Add(this.Id, " order "), id), " not found")))
	}

	ch <- order
	return nil
}

/**
 * @method
 * @name bitfinex#fetchClosedOrder
 * @description fetch an open order by it's id
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders-by-symbol
 * @param {string} id order id
 * @param {string} symbol unified market symbol, default is undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) FetchClosedOrderAsync(id any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchClosedOrderBody(ch, id, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchClosedOrderBody(ch chan any, id any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	var request map[string]any = map[string]any{
		"id": []any{ParseInt(id)},
	}

	orders := (<-this.FetchClosedOrdersAsync(symbol, nil, nil, this.Extend(request, params)))
	PanicOnError(orders)
	var order any = this.SafeValue(orders, 0)
	if IsTrue(IsEqual(order, nil)) {
		panic(OrderNotFound(Add(Add(Add(this.Id, " order "), id), " not found")))
	}

	ch <- order
	return nil
}

/**
 * @method
 * @name bitfinex#fetchOpenOrders
 * @description fetch all unfilled currently open orders
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders-by-symbol
 * @param {string} symbol unified market symbol
 * @param {int} [since] the earliest time in ms to fetch open orders for
 * @param {int} [limit] the maximum number of  open orders structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {Order[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) FetchOpenOrdersAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchOpenOrdersBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchOpenOrdersBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes220312 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes220312)
	}
	var request map[string]any = map[string]any{}
	var market any = nil
	var response any = nil
	if IsTrue(IsEqual(symbol, nil)) {

		response = (<-this.PrivatePostAuthROrders(this.Extend(request, params)))
		PanicOnError(response)
	} else {
		market = this.Market(symbol)
		AddElementToObject(request, "symbol", GetValue(market, "id"))

		response = (<-this.PrivatePostAuthROrdersSymbol(this.Extend(request, params)))
		PanicOnError(response)
	}
	//
	//      [
	//          [
	//              95408916206,            // Order ID
	//              null,                   // Group Order ID
	//              1653322349926,          // Client Order ID
	//              "tDOGE:UST",            // Market ID
	//              1653322349926,          // Created Timestamp in milliseconds
	//              1653322349927,          // Updated Timestamp in milliseconds
	//              -10,                    // Amount remaining (Positive means buy, negative means sell)
	//              -10,                    // Original amount
	//              "EXCHANGE LIMIT",       // Order type
	//              null,                   // Previous Order Type
	//              null,                   // _PLACEHOLDER
	//              null,                   // _PLACEHOLDER
	//              0,                      // Flags, see parseOrderFlags()
	//              "ACTIVE",               // Order Status, see parseOrderStatus()
	//              null,                   // _PLACEHOLDER
	//              null,                   // _PLACEHOLDER
	//              0.11,                   // Price
	//              0,                      // Average Price
	//              0,                      // Trailing Price
	//              0,                      // Auxiliary Limit price (for STOP LIMIT)
	//              null,                   // _PLACEHOLDER
	//              null,                   // _PLACEHOLDER
	//              null,                   // _PLACEHOLDER
	//              0,                      // Hidden (0 if false, 1 if true)
	//              0,                      // Placed ID (If another order caused this order to be placed (OCO) this will be that other order's ID)
	//              null,                   // _PLACEHOLDER
	//              null,                   // _PLACEHOLDER
	//              null,                   // _PLACEHOLDER
	//              "API>BFX",              // Routing, indicates origin of action: BFX, ETHFX, API>BFX, API>ETHFX
	//              null,                   // _PLACEHOLDER
	//              null,                   // _PLACEHOLDER
	//              {"$F7":1}               // additional meta information about the order ( $F7 = IS_POST_ONLY (0 if false, 1 if true), $F33 = Leverage (int))
	//          ],
	//      ]
	//
	var ordersList any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(response)); i++ {
		AppendToArray(&ordersList, map[string]any{
			"result": GetValue(response, i),
		})
	}

	ch <- this.ParseOrders(ordersList, market, since, limit)
	return nil
}

/**
 * @method
 * @name bitfinex#fetchClosedOrders
 * @description fetches information on multiple closed orders made by the user
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders-by-symbol
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] the latest time in ms to fetch entries for
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [availble parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns {Order[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) FetchClosedOrdersAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchClosedOrdersBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchClosedOrdersBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	// returns the most recent closed or canceled orders up to circa two weeks ago
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes227712 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes227712)
	}
	var paginate any = false
	paginateparamsVariable := this.HandleOptionAndParams(params, "fetchClosedOrders", "paginate")
	paginate = GetValue(paginateparamsVariable, 0)
	params = GetValue(paginateparamsVariable, 1)
	if IsTrue(paginate) {

		retRes228219 := (<-this.FetchPaginatedCallDynamicAsync("fetchClosedOrders", symbol, since, limit, params))
		PanicOnError(retRes228219)
		ch <- retRes228219
		return nil
	}
	var request any = map[string]any{}
	if IsTrue(!IsEqual(since, nil)) {
		AddElementToObject(request, "start", since)
	}
	if IsTrue(!IsEqual(limit, nil)) {
		AddElementToObject(request, "limit", limit) // default 25, max 2500
	}
	requestparamsVariable := this.HandleUntilOption("end", request, params)
	request = GetValue(requestparamsVariable, 0)
	params = GetValue(requestparamsVariable, 1)
	var market any = nil
	var response any = nil
	if IsTrue(IsEqual(symbol, nil)) {

		response = (<-this.PrivatePostAuthROrdersHist(this.Extend(request, params)))
		PanicOnError(response)
	} else {
		market = this.Market(symbol)
		AddElementToObject(request, "symbol", GetValue(market, "id"))

		response = (<-this.PrivatePostAuthROrdersSymbolHist(this.Extend(request, params)))
		PanicOnError(response)
	}
	//
	//      [
	//          [
	//              95412102131,            // Order ID
	//              null,                   // Group Order ID
	//              1653325121798,          // Client Order ID
	//              "tDOGE:UST",            // Market ID
	//              1653325122000,          // Created Timestamp in milliseconds
	//              1653325122000,          // Updated Timestamp in milliseconds
	//              -10,                    // Amount remaining (Positive means buy, negative means sell)
	//              -10,                    // Original amount
	//              "EXCHANGE LIMIT",       // Order type
	//              null,                   // Previous Order Type
	//              null,                   // Millisecond timestamp of Time-In-Force: automatic order cancellation
	//              null,                   // _PLACEHOLDER
	//              "4096",                 // Flags, see parseOrderFlags()
	//              "POSTONLY CANCELED",    // Order Status, see parseOrderStatus()
	//              null,                   // _PLACEHOLDER
	//              null,                   // _PLACEHOLDER
	//              0.071,                  // Price
	//              0,                      // Average Price
	//              0,                      // Trailing Price
	//              0,                      // Auxiliary Limit price (for STOP LIMIT)
	//              null,                   // _PLACEHOLDER
	//              null,                   // _PLACEHOLDER
	//              null,                   // _PLACEHOLDER
	//              0,                      // Notify (0 if false, 1 if true)
	//              0,                      // Hidden (0 if false, 1 if true)
	//              null,                   // Placed ID (If another order caused this order to be placed (OCO) this will be that other order's ID)
	//              null,                   // _PLACEHOLDER
	//              null,                   // _PLACEHOLDER
	//              "API>BFX",              // Routing, indicates origin of action: BFX, ETHFX, API>BFX, API>ETHFX
	//              null,                   // _PLACEHOLDER
	//              null,                   // _PLACEHOLDER
	//              {"_$F7":1}              // additional meta information about the order ( _$F7 = IS_POST_ONLY (0 if false, 1 if true), _$F33 = Leverage (int))
	//          ]
	//      ]
	//
	var ordersList any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(response)); i++ {
		AppendToArray(&ordersList, map[string]any{
			"result": GetValue(response, i),
		})
	}

	ch <- this.ParseOrders(ordersList, market, since, limit)
	return nil
}

/**
 * @method
 * @name bitfinex#fetchOrderTrades
 * @description fetch all the trades made from a single order
 * @see https://docs.bitfinex.com/reference/rest-auth-order-trades
 * @param {string} id order id
 * @param {string} symbol unified market symbol
 * @param {int} [since] the earliest time in ms to fetch trades for
 * @param {int} [limit] the maximum number of trades to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=trade-structure}
 */
func (this *Bitfinex) FetchOrderTradesAsync(id any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchOrderTradesBody(ch, id, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchOrderTradesBody(ch chan any, id any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params
	if IsTrue(IsEqual(symbol, nil)) {
		panic(ArgumentsRequired(Add(this.Id, " fetchOrderTrades() requires a symbol argument")))
	}
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes236312 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes236312)
	}
	var market any = this.Market(symbol)
	var orderId int64 = ParseInt(id)
	var request map[string]any = map[string]any{
		"id":     orderId,
		"symbol": GetValue(market, "id"),
	}
	// valid for trades up to 10 days old

	response := (<-this.PrivatePostAuthROrderSymbolIdTrades(this.Extend(request, params)))
	PanicOnError(response)
	var rawTrades []any = this.ToArray(response)
	var tradesList any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(rawTrades)); i++ {
		AppendToArray(&tradesList, map[string]any{
			"result": GetValue(rawTrades, i),
		}) // convert to array of dicts to match parseOrder signature
	}

	ch <- this.ParseTrades(tradesList, market, since, limit)
	return nil
}

/**
 * @method
 * @name bitfinex#fetchMyTrades
 * @description fetch all trades made by the user
 * @see https://docs.bitfinex.com/reference/rest-auth-trades
 * @see https://docs.bitfinex.com/reference/rest-auth-trades-by-symbol
 * @param {string} symbol unified market symbol
 * @param {int} [since] the earliest time in ms to fetch trades for
 * @param {int} [limit] the maximum number of trades structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {Trade[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=trade-structure}
 */
func (this *Bitfinex) FetchMyTradesAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchMyTradesBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchMyTradesBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes239512 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes239512)
	}
	var market any = nil
	var request map[string]any = map[string]any{
		"end": this.Milliseconds(),
	}
	if IsTrue(!IsEqual(since, nil)) {
		AddElementToObject(request, "start", since)
	}
	if IsTrue(!IsEqual(limit, nil)) {
		AddElementToObject(request, "limit", limit) // default 25, max 1000
	}
	var response any = nil
	if IsTrue(!IsEqual(symbol, nil)) {
		market = this.Market(symbol)
		AddElementToObject(request, "symbol", GetValue(market, "id"))

		response = (<-this.PrivatePostAuthRTradesSymbolHist(this.Extend(request, params)))
		PanicOnError(response)
	} else {

		response = (<-this.PrivatePostAuthRTradesHist(this.Extend(request, params)))
		PanicOnError(response)
	}
	var tradesList any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(response)); i++ {
		AppendToArray(&tradesList, map[string]any{
			"result": GetValue(response, i),
		}) // convert to array of dicts to match parseOrder signature
	}

	ch <- this.ParseTrades(tradesList, market, since, limit)
	return nil
}

/**
 * @method
 * @name bitfinex#createDepositAddress
 * @description create a currency deposit address
 * @see https://docs.bitfinex.com/reference/rest-auth-deposit-address
 * @param {string} code unified currency code of the currency for the deposit address
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [address structure]{@link https://docs.ccxt.com/?id=address-structure}
 */
func (this *Bitfinex) CreateDepositAddressAsync(code any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.createDepositAddressBody(ch, code, optionalArgs...)
	return ch
}
func (this *Bitfinex) createDepositAddressBody(ch chan any, code any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes243312 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes243312)
	}
	var request map[string]any = map[string]any{
		"op_renew": 1,
	}

	retRes243815 := (<-this.FetchDepositAddressAsync(code, this.Extend(request, params)))
	PanicOnError(retRes243815)
	ch <- retRes243815
	return nil
}

/**
 * @method
 * @name bitfinex#fetchDepositAddress
 * @description fetch the deposit address for a currency associated with this account
 * @see https://docs.bitfinex.com/reference/rest-auth-deposit-address
 * @param {string} code unified currency code
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [address structure]{@link https://docs.ccxt.com/?id=address-structure}
 */
func (this *Bitfinex) FetchDepositAddressAsync(code any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchDepositAddressBody(ch, code, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchDepositAddressBody(ch chan any, code any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes245212 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes245212)
	}
	var currency any = this.Currency(code)
	// if not provided explicitly we will try to match using the currency name
	var network any = this.SafeString(params, "network", code)
	var currencyNetworks any = this.SafeValue(currency, "networks", map[string]any{})
	var currencyNetwork any = this.SafeValue(currencyNetworks, network)
	var networkId any = this.SafeString(currencyNetwork, "id")
	if IsTrue(IsEqual(networkId, nil)) {
		panic(ArgumentsRequired(Add(Add(Add(this.Id, " fetchDepositAddress() could not find a network for '"), code), "'. You can specify it by providing the 'network' value inside params")))
	}
	var wallet any = this.SafeString(params, "wallet", "exchange") // 'exchange', 'margin', 'funding' and also old labels 'exchange', 'trading', 'deposit', respectively
	params = this.Omit(params, "network", "wallet")
	var request map[string]any = map[string]any{
		"method":   networkId,
		"wallet":   wallet,
		"op_renew": 0,
	}

	response := (<-this.PrivatePostAuthWDepositAddress(this.Extend(request, params)))
	PanicOnError(response)
	//
	//     [
	//         1582269616687, // MTS Millisecond Time Stamp of the update
	//         "acc_dep", // TYPE Purpose of notification "acc_dep" for account deposit
	//         null, // MESSAGE_ID unique ID of the message
	//         null, // not documented
	//         [
	//             null, // PLACEHOLDER
	//             "BITCOIN", // METHOD Method of deposit
	//             "BTC", // CURRENCY_CODE Currency code of new address
	//             null, // PLACEHOLDER
	//             "1BC9PZqpUmjyEB54uggn8TFKj49zSDYzqG", // ADDRESS
	//             null, // POOL_ADDRESS
	//         ],
	//         null, // CODE null or integer work in progress
	//         "SUCCESS", // STATUS Status of the notification, SUCCESS, ERROR, FAILURE
	//         "success", // TEXT Text of the notification
	//     ]
	//
	var result any = this.SafeValue(response, 4, []any{})
	var poolAddress any = this.SafeString(result, 5)
	var address any = Ternary(IsTrue((IsEqual(poolAddress, nil))), this.SafeString(result, 4), poolAddress)
	var tag any = Ternary(IsTrue((IsEqual(poolAddress, nil))), nil, this.SafeString(result, 4))
	this.CheckAddress(address)

	ch <- map[string]any{
		"currency": code,
		"address":  address,
		"tag":      tag,
		"network":  nil,
		"info":     response,
	}
	return nil
}
func (this *Bitfinex) ParseTransactionStatus(status any) any {
	var statuses map[string]any = map[string]any{
		"SUCCESS":              "ok",
		"COMPLETED":            "ok",
		"ERROR":                "failed",
		"FAILURE":              "failed",
		"CANCELED":             "canceled",
		"PENDING APPROVAL":     "pending",
		"PENDING":              "pending",
		"PENDING REVIEW":       "pending",
		"PENDING CANCELLATION": "pending",
		"SENDING":              "pending",
		"USER APPROVED":        "pending",
	}
	return this.SafeString(statuses, status, status)
}
func (this *Bitfinex) ParseTransaction(transaction any, optionalArgs ...any) any {
	//
	// withdraw
	//
	//     [
	//         1582271520931, // MTS Millisecond Time Stamp of the update
	//         "acc_wd-req", // TYPE Purpose of notification "acc_wd-req" account withdrawal request
	//         null, // MESSAGE_ID unique ID of the message
	//         null, // not documented
	//         [
	//             0, // WITHDRAWAL_ID Unique Withdrawal ID
	//             null, // PLACEHOLDER
	//             "bitcoin", // METHOD Method of withdrawal
	//             null, // PAYMENT_ID Payment ID if relevant
	//             "exchange", // WALLET Sending wallet
	//             1, // AMOUNT Amount of Withdrawal less fee
	//             null, // PLACEHOLDER
	//             null, // PLACEHOLDER
	//             0.0004, // WITHDRAWAL_FEE Fee on withdrawal
	//         ],
	//         null, // CODE null or integer Work in progress
	//         "SUCCESS", // STATUS Status of the notification, it may vary over time SUCCESS, ERROR, FAILURE
	//         "Invalid bitcoin address (abcdef)", // TEXT Text of the notification
	//     ]
	//
	// fetchDepositsWithdrawals
	//
	//     [
	//         13293039, // ID
	//         "ETH", // CURRENCY
	//         "ETHEREUM", // CURRENCY_NAME
	//         null,
	//         null,
	//         1574175052000, // MTS_STARTED
	//         1574181326000, // MTS_UPDATED
	//         null,
	//         null,
	//         "CANCELED", // STATUS
	//         null,
	//         null,
	//         -0.24, // AMOUNT, negative for withdrawals
	//         -0.00135, // FEES
	//         null,
	//         null,
	//         "0x38110e0Fc932CB2BE...........", // DESTINATION_ADDRESS
	//         null,
	//         null,
	//         null,
	//         "0x523ec8945500.....................................", // TRANSACTION_ID
	//         "Purchase of 100 pizzas", // WITHDRAW_TRANSACTION_NOTE, might also be: null
	//     ]
	//
	currency := GetArg(optionalArgs, 0, nil)
	_ = currency
	var transactionLength int = GetArrayLength(transaction)
	var timestamp any = nil
	var updated any = nil
	var code any = nil
	var amount any = nil
	var id any = nil
	var status any = nil
	var tag any = nil
	var typeVar any = nil
	var feeCost any = nil
	var txid any = nil
	var addressTo any = nil
	var network any = nil
	var comment any = nil
	if IsTrue(IsEqual(transactionLength, 8)) {
		var data any = this.SafeValue(transaction, 4, []any{})
		timestamp = this.SafeInteger(transaction, 0)
		if IsTrue(!IsEqual(currency, nil)) {
			code = GetValue(currency, "code")
		}
		feeCost = this.SafeString(data, 8)
		if IsTrue(!IsEqual(feeCost, nil)) {
			feeCost = Precise.StringAbs(feeCost)
		}
		amount = this.SafeNumber(data, 5)
		id = this.SafeInteger(data, 0)
		status = "ok"
		if IsTrue(IsEqual(id, 0)) {
			id = nil
			status = "failed"
		}
		tag = this.SafeString(data, 3)
		typeVar = "withdrawal"
		var networkId any = this.SafeString(data, 2)
		network = this.NetworkIdToCode(ToUpper(networkId), code) // withdraw returns in lowercase
	} else if IsTrue(IsEqual(transactionLength, 22)) {
		id = this.SafeString(transaction, 0)
		var currencyId any = this.SafeString(transaction, 1)
		code = this.SafeCurrencyCode(currencyId, currency)
		var networkId any = this.SafeString(transaction, 2)
		network = this.NetworkIdToCode(networkId, code)
		timestamp = this.SafeInteger(transaction, 5)
		updated = this.SafeInteger(transaction, 6)
		status = this.ParseTransactionStatus(this.SafeString(transaction, 9))
		var signedAmount any = this.SafeString(transaction, 12)
		amount = Precise.StringAbs(signedAmount)
		if IsTrue(!IsEqual(signedAmount, nil)) {
			if IsTrue(Precise.StringLt(signedAmount, "0")) {
				typeVar = "withdrawal"
			} else {
				typeVar = "deposit"
			}
		}
		feeCost = this.SafeString(transaction, 13)
		if IsTrue(!IsEqual(feeCost, nil)) {
			feeCost = Precise.StringAbs(feeCost)
		}
		addressTo = this.SafeString(transaction, 16)
		txid = this.SafeString(transaction, 20)
		comment = this.SafeString(transaction, 21)
	}
	return map[string]any{
		"info":        transaction,
		"id":          id,
		"txid":        txid,
		"type":        typeVar,
		"currency":    code,
		"network":     network,
		"amount":      this.ParseNumber(amount),
		"status":      status,
		"timestamp":   timestamp,
		"datetime":    this.Iso8601(timestamp),
		"address":     addressTo,
		"addressFrom": nil,
		"addressTo":   addressTo,
		"tag":         tag,
		"tagFrom":     nil,
		"tagTo":       tag,
		"updated":     updated,
		"comment":     comment,
		"internal":    nil,
		"fee": map[string]any{
			"currency": code,
			"cost":     this.ParseNumber(feeCost),
			"rate":     nil,
		},
	}
}

/**
 * @method
 * @name bitfinex#fetchTradingFees
 * @description fetch the trading fees for multiple markets
 * @see https://docs.bitfinex.com/reference/rest-auth-summary
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a dictionary of [fee structures]{@link https://docs.ccxt.com/?id=fee-structure} indexed by market symbols
 */
func (this *Bitfinex) FetchTradingFeesAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchTradingFeesBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchTradingFeesBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes267212 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes267212)
	}

	response := (<-this.PrivatePostAuthRSummary(params))
	PanicOnError(response)
	//
	//      Response Spec:
	//      [
	//         PLACEHOLDER,
	//         PLACEHOLDER,
	//         PLACEHOLDER,
	//         PLACEHOLDER,
	//         [
	//            [
	//             MAKER_FEE,
	//             MAKER_FEE,
	//             MAKER_FEE,
	//             PLACEHOLDER,
	//             PLACEHOLDER,
	//             DERIV_REBATE
	//            ],
	//            [
	//             TAKER_FEE_TO_CRYPTO,
	//             TAKER_FEE_TO_STABLE,
	//             TAKER_FEE_TO_FIAT,
	//             PLACEHOLDER,
	//             PLACEHOLDER,
	//             DERIV_TAKER_FEE
	//            ]
	//         ],
	//         PLACEHOLDER,
	//         PLACEHOLDER,
	//         PLACEHOLDER,
	//         PLACEHOLDER,
	//         {
	//             LEO_LEV,
	//             LEO_AMOUNT_AVG
	//         }
	//     ]
	//
	//      Example response:
	//
	//     [
	//         null,
	//         null,
	//         null,
	//         null,
	//         [
	//          [ 0.001, 0.001, 0.001, null, null, 0.0002 ],
	//          [ 0.002, 0.002, 0.002, null, null, 0.00065 ]
	//         ],
	//         [
	//          [
	//              {
	//              "curr": "Total (USD)",
	//              "vol": "0",
	//              "vol_safe": "0",
	//              "vol_maker": "0",
	//              "vol_BFX": "0",
	//              "vol_BFX_safe": "0",
	//              "vol_BFX_maker": "0"
	//              }
	//          ],
	//          {},
	//          0
	//         ],
	//         [ null, {}, 0 ],
	//         null,
	//         null,
	//         { leo_lev: "0", leo_amount_avg: "0" }
	//     ]
	//
	var result map[string]any = map[string]any{}
	var fiat any = this.SafeDict(this.Options, "fiat", map[string]any{})
	var feeData any = this.SafeValue(response, 4, []any{})
	var makerData any = this.SafeValue(feeData, 0, []any{})
	var takerData any = this.SafeValue(feeData, 1, []any{})
	var makerFee any = this.SafeNumber(makerData, 0)
	var makerFeeFiat any = this.SafeNumber(makerData, 2)
	var makerFeeDeriv any = this.SafeNumber(makerData, 5)
	var takerFee any = this.SafeNumber(takerData, 0)
	var takerFeeFiat any = this.SafeNumber(takerData, 2)
	var takerFeeDeriv any = this.SafeNumber(takerData, 5)
	for i := 0; IsLessThan(i, GetArrayLength(this.Symbols)); i++ {
		var symbol any = GetValue(this.Symbols, i)
		var market any = this.Market(symbol)
		var fee map[string]any = map[string]any{
			"info":       response,
			"symbol":     symbol,
			"percentage": true,
			"tierBased":  true,
		}
		if IsTrue(InOp(fiat, GetValue(market, "quote"))) {
			AddElementToObject(fee, "maker", makerFeeFiat)
			AddElementToObject(fee, "taker", takerFeeFiat)
		} else if IsTrue(IsEqual(GetValue(market, "contract"), true)) {
			AddElementToObject(fee, "maker", makerFeeDeriv)
			AddElementToObject(fee, "taker", takerFeeDeriv)
		} else {
			AddElementToObject(fee, "maker", makerFee)
			AddElementToObject(fee, "taker", takerFee)
		}
		AddElementToObject(result, symbol, fee)
	}

	ch <- result
	return nil
}

/**
 * @method
 * @name bitfinex#fetchDepositsWithdrawals
 * @description fetch history of deposits and withdrawals
 * @see https://docs.bitfinex.com/reference/movement-info
 * @see https://docs.bitfinex.com/reference/rest-auth-movements
 * @param {string} [code] unified currency code for the currency of the deposit/withdrawals, default is undefined
 * @param {int} [since] timestamp in ms of the earliest deposit/withdrawal, default is undefined
 * @param {int} [limit] max number of deposit/withdrawals to return, default is undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a list of [transaction structure]{@link https://docs.ccxt.com/?id=transaction-structure}
 */
func (this *Bitfinex) FetchDepositsWithdrawalsAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchDepositsWithdrawalsBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchDepositsWithdrawalsBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	code := GetArg(optionalArgs, 0, nil)
	_ = code
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes279112 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes279112)
	}
	var currency any = nil
	var request map[string]any = map[string]any{}
	if IsTrue(!IsEqual(since, nil)) {
		AddElementToObject(request, "start", since)
	}
	if IsTrue(!IsEqual(limit, nil)) {
		AddElementToObject(request, "limit", limit) // max 1000
	}
	var response any = nil
	if IsTrue(!IsEqual(code, nil)) {
		currency = this.Currency(code)
		AddElementToObject(request, "currency", GetValue(currency, "id"))

		currencyMovements := (<-this.PrivatePostAuthRMovementsCurrencyHist(this.Extend(request, params)))
		PanicOnError(currencyMovements)
		response = this.ToArray(currencyMovements)
	} else {

		movements := (<-this.PrivatePostAuthRMovementsHist(this.Extend(request, params)))
		PanicOnError(movements)
		response = this.ToArray(movements)
	}

	//
	//     [
	//         [
	//             13293039, // ID
	//             "ETH", // CURRENCY
	//             "ETHEREUM", // CURRENCY_NAME
	//             null,
	//             null,
	//             1574175052000, // MTS_STARTED
	//             1574181326000, // MTS_UPDATED
	//             null,
	//             null,
	//             "CANCELED", // STATUS
	//             null,
	//             null,
	//             -0.24, // AMOUNT, negative for withdrawals
	//             -0.00135, // FEES
	//             null,
	//             null,
	//             "0x38110e0Fc932CB2BE...........", // DESTINATION_ADDRESS
	//             null,
	//             null,
	//             null,
	//             "0x523ec8945500.....................................", // TRANSACTION_ID
	//             "Purchase of 100 pizzas", // WITHDRAW_TRANSACTION_NOTE, might also be: null
	//         ]
	//     ]
	//
	ch <- this.ParseTransactions(response, currency, since, limit)
	return nil
}

/**
 * @method
 * @name bitfinex#withdraw
 * @description make a withdrawal
 * @see https://docs.bitfinex.com/reference/rest-auth-withdraw
 * @param {string} code unified currency code
 * @param {float} amount the amount to withdraw
 * @param {string} address the address to withdraw to
 * @param {string} tag
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [transaction structure]{@link https://docs.ccxt.com/?id=transaction-structure}
 */
func (this *Bitfinex) WithdrawAsync(code any, amount any, address any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.withdrawBody(ch, code, amount, address, optionalArgs...)
	return ch
}
func (this *Bitfinex) withdrawBody(ch chan any, code any, amount any, address any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	tag := GetArg(optionalArgs, 0, nil)
	_ = tag
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	this.CheckAddress(address)
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes285712 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes285712)
	}
	var currency any = this.Currency(code)
	// if not provided explicitly we will try to match using the currency name
	var network any = this.SafeString(params, "network", code)
	params = this.Omit(params, "network")
	var currencyNetworks any = this.SafeValue(currency, "networks", map[string]any{})
	var currencyNetwork any = this.SafeValue(currencyNetworks, network)
	var networkId any = this.SafeString(currencyNetwork, "id")
	if IsTrue(IsEqual(networkId, nil)) {
		panic(ArgumentsRequired(Add(Add(Add(this.Id, " withdraw() could not find a network for '"), code), "'. You can specify it by providing the 'network' value inside params")))
	}
	var wallet any = this.SafeString(params, "wallet", "exchange") // 'exchange', 'margin', 'funding' and also old labels 'exchange', 'trading', 'deposit', respectively
	params = this.Omit(params, "network", "wallet")
	var request map[string]any = map[string]any{
		"method":  networkId,
		"wallet":  wallet,
		"amount":  this.NumberToString(amount),
		"address": address,
	}
	if IsTrue(!IsEqual(tag, nil)) {
		AddElementToObject(request, "payment_id", tag)
	}
	var withdrawOptions any = this.SafeValue(this.Options, "withdraw", map[string]any{})
	var includeFee any = this.SafeBool(withdrawOptions, "includeFee", false)
	if IsTrue(IsEqual(includeFee, true)) {
		AddElementToObject(request, "fee_deduct", 1)
	}

	response := (<-this.PrivatePostAuthWWithdraw(this.Extend(request, params)))
	PanicOnError(response)
	//
	//     [
	//         1582271520931, // MTS Millisecond Time Stamp of the update
	//         "acc_wd-req", // TYPE Purpose of notification "acc_wd-req" account withdrawal request
	//         null, // MESSAGE_ID unique ID of the message
	//         null, // not documented
	//         [
	//             0, // WITHDRAWAL_ID Unique Withdrawal ID
	//             null, // PLACEHOLDER
	//             "bitcoin", // METHOD Method of withdrawal
	//             null, // PAYMENT_ID Payment ID if relevant
	//             "exchange", // WALLET Sending wallet
	//             1, // AMOUNT Amount of Withdrawal less fee
	//             null, // PLACEHOLDER
	//             null, // PLACEHOLDER
	//             0.0004, // WITHDRAWAL_FEE Fee on withdrawal
	//         ],
	//         null, // CODE null or integer Work in progress
	//         "SUCCESS", // STATUS Status of the notification, it may vary over time SUCCESS, ERROR, FAILURE
	//         "Invalid bitcoin address (abcdef)", // TEXT Text of the notification
	//     ]
	//
	// in case of failure:
	//
	//     [
	//         "error",
	//         10001,
	//         "Momentary balance check. Please wait few seconds and try the transfer again."
	//     ]
	//
	var statusMessage any = this.SafeString(response, 0)
	if IsTrue(IsEqual(statusMessage, "error")) {
		var feedback any = Add(Add(this.Id, " "), response)
		var message any = this.SafeString(response, 2, "")
		// same message as in v1
		this.ThrowExactlyMatchedException(GetValue(this.Exceptions, "exact"), message, feedback)
		this.ThrowBroadlyMatchedException(GetValue(this.Exceptions, "broad"), message, feedback)
		panic(ExchangeError(feedback))
	}
	var text any = this.SafeString(response, 7)
	if IsTrue(!IsEqual(text, "success")) {
		this.ThrowBroadlyMatchedException(GetValue(this.Exceptions, "broad"), text, text)
	}

	ch <- this.ParseTransaction(response, currency)
	return nil
}

/**
 * @method
 * @name bitfinex#fetchPositions
 * @description fetch all open positions
 * @see https://docs.bitfinex.com/reference/rest-auth-positions
 * @param {string[]|undefined} symbols list of unified market symbols
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [position structure]{@link https://docs.ccxt.com/?id=position-structure}
 */
func (this *Bitfinex) FetchPositionsAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchPositionsBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchPositionsBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbols := GetArg(optionalArgs, 0, nil)
	_ = symbols
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes294312 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes294312)
	}
	symbols = this.MarketSymbols(symbols)

	response := (<-this.PrivatePostAuthRPositions(params))
	PanicOnError(response)
	//
	//     [
	//         [
	//             "tBTCUSD", // SYMBOL
	//             "ACTIVE", // STATUS
	//             0.0195, // AMOUNT
	//             8565.0267019, // BASE_PRICE
	//             0, // MARGIN_FUNDING
	//             0, // MARGIN_FUNDING_TYPE
	//             -0.33455568705000516, // PL
	//             -0.0003117550117425625, // PL_PERC
	//             7045.876419249083, // PRICE_LIQ
	//             3.0673001895895604, // LEVERAGE
	//             null, // _PLACEHOLDER
	//             142355652, // POSITION_ID
	//             1574002216000, // MTS_CREATE
	//             1574002216000, // MTS_UPDATE
	//             null, // _PLACEHOLDER
	//             0, // TYPE
	//             null, // _PLACEHOLDER
	//             0, // COLLATERAL
	//             0, // COLLATERAL_MIN
	//             // META
	//             {
	//                 "reason":"TRADE",
	//                 "order_id":34271018124,
	//                 "liq_stage":null,
	//                 "trade_price":"8565.0267019",
	//                 "trade_amount":"0.0195",
	//                 "order_id_oppo":34277498022
	//             }
	//         ]
	//     ]
	//
	var rawPositions []any = this.ToArray(response)
	var positionsList any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(rawPositions)); i++ {
		AppendToArray(&positionsList, map[string]any{
			"result": GetValue(rawPositions, i),
		})
	}

	ch <- this.ParsePositions(positionsList, symbols)
	return nil
}
func (this *Bitfinex) ParsePosition(position any, optionalArgs ...any) any {
	//
	//    [
	//        "tBTCUSD",                    // SYMBOL
	//        "ACTIVE",                     // STATUS
	//        0.0195,                       // AMOUNT
	//        8565.0267019,                 // BASE_PRICE
	//        0,                            // MARGIN_FUNDING
	//        0,                            // MARGIN_FUNDING_TYPE
	//        -0.33455568705000516,         // PL
	//        -0.0003117550117425625,       // PL_PERC
	//        7045.876419249083,            // PRICE_LIQ
	//        3.0673001895895604,           // LEVERAGE
	//        null,                         // _PLACEHOLDER
	//        142355652,                    // POSITION_ID
	//        1574002216000,                // MTS_CREATE
	//        1574002216000,                // MTS_UPDATE
	//        null,                         // _PLACEHOLDER
	//        0,                            // TYPE
	//        null,                         // _PLACEHOLDER
	//        0,                            // COLLATERAL
	//        0,                            // COLLATERAL_MIN
	//        // META
	//        {
	//            "reason": "TRADE",
	//            "order_id": 34271018124,
	//            "liq_stage": null,
	//            "trade_price": "8565.0267019",
	//            "trade_amount": "0.0195",
	//            "order_id_oppo": 34277498022
	//        }
	//    ]
	//
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	var positionList any = this.SafeList(position, "result")
	var marketId any = this.SafeString(positionList, 0)
	var amount any = this.SafeString(positionList, 2)
	var timestamp any = this.SafeInteger(positionList, 12)
	var meta any = this.SafeString(positionList, 19)
	var tradePrice any = this.SafeString(meta, "trade_price")
	var tradeAmount any = this.SafeString(meta, "trade_amount")
	return this.SafePosition(map[string]any{
		"info":                        positionList,
		"id":                          this.SafeString(positionList, 11),
		"symbol":                      this.SafeSymbol(marketId, market),
		"notional":                    this.ParseNumber(amount),
		"marginMode":                  "isolated",
		"liquidationPrice":            this.SafeNumber(positionList, 8),
		"entryPrice":                  this.SafeNumber(positionList, 3),
		"unrealizedPnl":               this.SafeNumber(positionList, 6),
		"percentage":                  this.SafeNumber(positionList, 7),
		"contracts":                   nil,
		"contractSize":                nil,
		"markPrice":                   nil,
		"lastPrice":                   nil,
		"side":                        Ternary(IsTrue(Precise.StringGt(amount, "0")), "long", "short"),
		"hedged":                      nil,
		"timestamp":                   timestamp,
		"datetime":                    this.Iso8601(timestamp),
		"lastUpdateTimestamp":         this.SafeInteger(positionList, 13),
		"maintenanceMargin":           this.SafeNumber(positionList, 18),
		"maintenanceMarginPercentage": nil,
		"collateral":                  this.SafeNumber(positionList, 17),
		"initialMargin":               this.ParseNumber(Precise.StringMul(tradeAmount, tradePrice)),
		"initialMarginPercentage":     nil,
		"leverage":                    this.SafeNumber(positionList, 9),
		"marginRatio":                 nil,
		"stopLossPrice":               nil,
		"takeProfitPrice":             nil,
	})
}
func (this *Bitfinex) Nonce() any {
	return this.Milliseconds()
}
func (this *Bitfinex) Sign(path any, optionalArgs ...any) any {
	api := GetArg(optionalArgs, 0, "public")
	_ = api
	method := GetArg(optionalArgs, 1, "GET")
	_ = method
	params := GetArg(optionalArgs, 2, map[string]any{})
	_ = params
	headers := GetArg(optionalArgs, 3, nil)
	_ = headers
	body := GetArg(optionalArgs, 4, nil)
	_ = body
	var request any = Add("/", this.ImplodeParams(path, params))
	var query any = this.Omit(params, this.ExtractParams(path))
	if IsTrue(IsEqual(api, "v1")) {
		request = Add(api, request)
	} else {
		request = Add(this.Version, request)
	}
	var url any = Add(Add(GetValue(GetValue(this.Urls, "api"), api), "/"), request)
	if IsTrue(IsEqual(api, "public")) {
		if IsTrue(IsGreaterThan(GetArrayLength(ObjectKeys(query)), 0)) {
			url = Add(url, Add("?", this.Urlencode(query)))
		}
	}
	if IsTrue(IsEqual(api, "private")) {
		this.CheckRequiredCredentials()
		var nonce string = ToString(this.Nonce())
		body = this.Json(query)
		var auth any = Add(Add(Add("/api/", request), nonce), body)
		var signature string = this.Hmac(this.Encode(auth), this.Encode(this.Secret), sha384)
		headers = map[string]any{
			"bfx-nonce":     nonce,
			"bfx-apikey":    this.ApiKey,
			"bfx-signature": signature,
			"Content-Type":  "application/json",
		}
	}
	return map[string]any{
		"url":     url,
		"method":  method,
		"body":    body,
		"headers": headers,
	}
}
func (this *Bitfinex) HandleErrors(statusCode any, statusText any, url any, method any, headers any, body any, response any, requestHeaders any, requestBody any) any {
	// ["error", 11010, "ratelimit: error"]
	if IsTrue(!IsEqual(response, nil)) {
		if !IsTrue(IsArray(response)) {
			var message any = this.SafeString2(response, "message", "error")
			var feedback any = Add(Add(this.Id, " "), body)
			this.ThrowExactlyMatchedException(GetValue(this.Exceptions, "exact"), message, feedback)
			this.ThrowBroadlyMatchedException(GetValue(this.Exceptions, "broad"), message, feedback)
			panic(ExchangeError(Add(Add(this.Id, " "), body)))
		}
	} else if IsTrue(IsEqual(response, "")) {
		panic(ExchangeError(Add(this.Id, " returned empty response")))
	}
	if IsTrue(IsEqual(statusCode, 429)) {
		panic(RateLimitExceeded(Add(Add(this.Id, " "), body)))
	}
	if IsTrue(IsEqual(statusCode, 500)) {
		// See https://docs.bitfinex.com/docs/abbreviations-glossary#section-errorinfo-codes
		var errorCode any = this.SafeString(response, 1, "")
		var errorText any = this.SafeString(response, 2, "")
		var feedback any = Add(Add(this.Id, " "), errorText)
		this.ThrowBroadlyMatchedException(GetValue(this.Exceptions, "broad"), errorText, feedback)
		this.ThrowExactlyMatchedException(GetValue(this.Exceptions, "exact"), errorCode, feedback)
		this.ThrowExactlyMatchedException(GetValue(this.Exceptions, "exact"), errorText, feedback)
		panic(ExchangeError(Add(Add(Add(Add(Add(this.Id, " "), errorText), " (#"), errorCode), ")")))
	}
	return response
}
func (this *Bitfinex) ParseLedgerEntryType(typeVar any) any {
	if IsTrue(IsEqual(typeVar, nil)) {
		return nil
	} else if IsTrue(IsTrue(IsGreaterThanOrEqual(GetIndexOf(typeVar, "fee"), 0)) || IsTrue(IsGreaterThanOrEqual(GetIndexOf(typeVar, "charged"), 0))) {
		return "fee"
	} else if IsTrue(IsGreaterThanOrEqual(GetIndexOf(typeVar, "rebate"), 0)) {
		return "rebate"
	} else if IsTrue(IsTrue(IsGreaterThanOrEqual(GetIndexOf(typeVar, "deposit"), 0)) || IsTrue(IsGreaterThanOrEqual(GetIndexOf(typeVar, "withdrawal"), 0))) {
		return "transaction"
	} else if IsTrue(IsGreaterThanOrEqual(GetIndexOf(typeVar, "transfer"), 0)) {
		return "transfer"
	} else if IsTrue(IsGreaterThanOrEqual(GetIndexOf(typeVar, "payment"), 0)) {
		return "payout"
	} else if IsTrue(IsTrue(IsGreaterThanOrEqual(GetIndexOf(typeVar, "exchange"), 0)) || IsTrue(IsGreaterThanOrEqual(GetIndexOf(typeVar, "position"), 0))) {
		return "trade"
	} else {
		return typeVar
	}
}
func (this *Bitfinex) ParseLedgerEntry(item any, optionalArgs ...any) any {
	//
	//     [
	//         [
	//             2531822314, // ID: Ledger identifier
	//             "USD", // CURRENCY: The symbol of the currency (ex. "BTC")
	//             null, // PLACEHOLDER
	//             1573521810000, // MTS: Timestamp in milliseconds
	//             null, // PLACEHOLDER
	//             0.01644445, // AMOUNT: Amount of funds moved
	//             0, // BALANCE: New balance
	//             null, // PLACEHOLDER
	//             "Settlement @ 185.79 on wallet margin" // DESCRIPTION: Description of ledger transaction
	//         ]
	//     ]
	//
	currency := GetArg(optionalArgs, 0, nil)
	_ = currency
	var itemList any = this.SafeList(item, "result", []any{})
	var typeVar any = nil
	var id any = this.SafeString(itemList, 0)
	var currencyId any = this.SafeString(itemList, 1)
	var code any = this.SafeCurrencyCode(currencyId, currency)
	currency = this.SafeCurrency(currencyId, currency)
	var timestamp any = this.SafeInteger(itemList, 3)
	var amount any = this.SafeNumber(itemList, 5)
	var after any = this.SafeNumber(itemList, 6)
	var description any = this.SafeString(itemList, 8)
	if IsTrue(!IsEqual(description, nil)) {
		var parts []string = Split(description, " @ ")
		var first any = this.SafeStringLower(parts, 0)
		typeVar = this.ParseLedgerEntryType(first)
	}
	return this.SafeLedgerEntry(map[string]any{
		"info":             item,
		"id":               id,
		"direction":        nil,
		"account":          nil,
		"referenceId":      id,
		"referenceAccount": nil,
		"type":             typeVar,
		"currency":         code,
		"amount":           amount,
		"timestamp":        timestamp,
		"datetime":         this.Iso8601(timestamp),
		"before":           nil,
		"after":            after,
		"status":           nil,
		"fee":              nil,
	}, currency)
}

/**
 * @method
 * @name bitfinex#fetchLedger
 * @description fetch the history of changes, actions done by the user or operations that altered the balance of the user
 * @see https://docs.bitfinex.com/reference/rest-auth-ledgers
 * @param {string} [code] unified currency code, default is undefined
 * @param {int} [since] timestamp in ms of the earliest ledger entry, default is undefined
 * @param {int} [limit] max number of ledger entries to return, default is undefined, max is 2500
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] timestamp in ms of the latest ledger entry
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns {object} a [ledger structure]{@link https://docs.ccxt.com/?id=ledger-entry-structure}
 */
func (this *Bitfinex) FetchLedgerAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchLedgerBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchLedgerBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	code := GetArg(optionalArgs, 0, nil)
	_ = code
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes320812 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes320812)
	}
	var paginate any = false
	paginateparamsVariable := this.HandleOptionAndParams(params, "fetchLedger", "paginate")
	paginate = GetValue(paginateparamsVariable, 0)
	params = GetValue(paginateparamsVariable, 1)
	if IsTrue(paginate) {

		retRes321319 := (<-this.FetchPaginatedCallDynamicAsync("fetchLedger", code, since, limit, params, 2500))
		PanicOnError(retRes321319)
		ch <- retRes321319
		return nil
	}
	var currency any = nil
	var request any = map[string]any{}
	if IsTrue(!IsEqual(since, nil)) {
		AddElementToObject(request, "start", since)
	}
	if IsTrue(!IsEqual(limit, nil)) {
		AddElementToObject(request, "limit", limit)
	}
	requestparamsVariable := this.HandleUntilOption("end", request, params)
	request = GetValue(requestparamsVariable, 0)
	params = GetValue(requestparamsVariable, 1)
	var response any = nil
	if IsTrue(!IsEqual(code, nil)) {
		currency = this.Currency(code)
		AddElementToObject(request, "currency", GetValue(currency, "id"))

		response = (<-this.PrivatePostAuthRLedgersCurrencyHist(this.Extend(request, params)))
		PanicOnError(response)
	} else {

		response = (<-this.PrivatePostAuthRLedgersHist(this.Extend(request, params)))
		PanicOnError(response)
	}
	//
	//     [
	//         [
	//             2531822314, // ID: Ledger identifier
	//             "USD", // CURRENCY: The symbol of the currency (ex. "BTC")
	//             null, // PLACEHOLDER
	//             1573521810000, // MTS: Timestamp in milliseconds
	//             null, // PLACEHOLDER
	//             0.01644445, // AMOUNT: Amount of funds moved
	//             0, // BALANCE: New balance
	//             null, // PLACEHOLDER
	//             "Settlement @ 185.79 on wallet margin" // DESCRIPTION: Description of ledger transaction
	//         ]
	//     ]
	//
	var ledgerObjects any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(response)); i++ {
		var item any = GetValue(response, i)
		AppendToArray(&ledgerObjects, map[string]any{
			"result": item,
		})
	}

	ch <- this.ParseLedger(ledgerObjects, currency, since, limit)
	return nil
}

/**
 * @method
 * @name bitfinex#fetchFundingRates
 * @description fetch the current funding rate for multiple symbols
 * @see https://docs.bitfinex.com/reference/rest-public-derivatives-status
 * @param {string[]} symbols list of unified market symbols
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [funding rate structures]{@link https://docs.ccxt.com/?id=funding-rate-structure}
 */
func (this *Bitfinex) FetchFundingRatesAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchFundingRatesBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchFundingRatesBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbols := GetArg(optionalArgs, 0, nil)
	_ = symbols
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(symbols, nil)) {
		panic(ArgumentsRequired(Add(this.Id, " fetchFundingRates() requires a symbols argument")))
	}
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes326912 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes326912)
	}
	var marketIds any = this.MarketIds(symbols)
	var request map[string]any = map[string]any{
		"keys": Join(marketIds, ","),
	}

	response := (<-this.PublicGetStatusDeriv(this.Extend(request, params)))
	PanicOnError(response)

	//
	//   [
	//       [
	//          "tBTCF0:USTF0",
	//          1691165059000,
	//          null,
	//          29297.851276225,
	//          29277.5,
	//          null,
	//          36950860.76010306,
	//          null,
	//          1691193600000,
	//          0.00000527,
	//          82,
	//          null,
	//          0.00014548,
	//          null,
	//          null,
	//          29278.8925,
	//          null,
	//          null,
	//          9636.07644994,
	//          null,
	//          null,
	//          null,
	//          0.0005,
	//          0.0025
	//       ]
	//   ]
	//
	ch <- this.ParseFundingRates(response, symbols)
	return nil
}

/**
 * @method
 * @name bitfinex#fetchFundingRateHistory
 * @description fetches historical funding rate prices
 * @see https://docs.bitfinex.com/reference/rest-public-derivatives-status-history
 * @param {string} symbol unified market symbol
 * @param {int} [since] timestamp in ms of the earliest funding rate entry
 * @param {int} [limit] max number of funding rate entrys to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] timestamp in ms of the latest funding rate
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns {object} a [funding rate structure]{@link https://docs.ccxt.com/?id=funding-rate-structure}
 */
func (this *Bitfinex) FetchFundingRateHistoryAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchFundingRateHistoryBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchFundingRateHistoryBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params
	if IsTrue(IsEqual(symbol, nil)) {
		panic(ArgumentsRequired(Add(this.Id, " fetchFundingRateHistory() requires a symbol argument")))
	}
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes332712 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes332712)
	}
	var paginate any = false
	paginateparamsVariable := this.HandleOptionAndParams(params, "fetchFundingRateHistory", "paginate")
	paginate = GetValue(paginateparamsVariable, 0)
	params = GetValue(paginateparamsVariable, 1)
	if IsTrue(paginate) {

		retRes333219 := (<-this.FetchPaginatedCallDeterministicAsync("fetchFundingRateHistory", symbol, since, limit, "8h", params, 5000))
		PanicOnError(retRes333219)
		ch <- retRes333219
		return nil
	}
	var market any = this.Market(symbol)
	var request any = map[string]any{
		"symbol": GetValue(market, "id"),
	}
	if IsTrue(!IsEqual(since, nil)) {
		AddElementToObject(request, "start", since)
	}
	requestparamsVariable := this.HandleUntilOption("end", request, params)
	request = GetValue(requestparamsVariable, 0)
	params = GetValue(requestparamsVariable, 1)

	response := (<-this.PublicGetStatusDerivSymbolHist(this.Extend(request, params)))
	PanicOnError(response)
	//
	//   [
	//       [
	//          "tBTCF0:USTF0",
	//          1691165059000,
	//          null,
	//          29297.851276225,
	//          29277.5,
	//          null,
	//          36950860.76010306,
	//          null,
	//          1691193600000,
	//          0.00000527,
	//          82,
	//          null,
	//          0.00014548,
	//          null,
	//          null,
	//          29278.8925,
	//          null,
	//          null,
	//          9636.07644994,
	//          null,
	//          null,
	//          null,
	//          0.0005,
	//          0.0025
	//       ]
	//   ]
	//
	var rawRatesData []any = this.ToArray(response)
	var rates any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(rawRatesData)); i++ {
		var fr any = GetValue(rawRatesData, i)
		var rate any = this.ParseFundingRateHistory(fr, market)
		AppendToArray(&rates, rate)
	}
	var reversedArray any = []any{}
	var rawRates any = this.FilterBySymbolSinceLimit(rates, symbol, since, limit)
	var ratesLength int = GetArrayLength(rawRates)
	for i := 0; IsLessThan(i, ratesLength); i++ {
		var index any = Subtract(Subtract(ratesLength, i), 1)
		var valueAtIndex any = GetValue(rawRates, index)
		AppendToArray(&reversedArray, valueAtIndex)
	}

	ch <- reversedArray
	return nil
}
func (this *Bitfinex) ParseFundingRate(contract any, optionalArgs ...any) any {
	//
	//       [
	//          "tBTCF0:USTF0",
	//          1691165059000,
	//          null,
	//          29297.851276225,
	//          29277.5,
	//          null,
	//          36950860.76010306,
	//          null,
	//          1691193600000,
	//          0.00000527,
	//          82,
	//          null,
	//          0.00014548,
	//          null,
	//          null,
	//          29278.8925,
	//          null,
	//          null,
	//          9636.07644994,
	//          null,
	//          null,
	//          null,
	//          0.0005,
	//          0.0025
	//       ]
	//
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	var marketId any = this.SafeString(contract, 0)
	var timestamp any = this.SafeInteger(contract, 1)
	var nextFundingTimestamp any = this.SafeInteger(contract, 8)
	return map[string]any{
		"info":                     contract,
		"symbol":                   this.SafeSymbol(marketId, market),
		"markPrice":                this.SafeNumber(contract, 15),
		"indexPrice":               this.SafeNumber(contract, 3),
		"interestRate":             nil,
		"estimatedSettlePrice":     nil,
		"timestamp":                timestamp,
		"datetime":                 this.Iso8601(timestamp),
		"fundingRate":              this.SafeNumber(contract, 12),
		"fundingTimestamp":         nil,
		"fundingDatetime":          nil,
		"nextFundingRate":          this.SafeNumber(contract, 9),
		"nextFundingTimestamp":     nextFundingTimestamp,
		"nextFundingDatetime":      this.Iso8601(nextFundingTimestamp),
		"previousFundingRate":      nil,
		"previousFundingTimestamp": nil,
		"previousFundingDatetime":  nil,
		"interval":                 nil,
	}
}
func (this *Bitfinex) ParseFundingRateHistory(contract any, optionalArgs ...any) any {
	//
	// [
	//     1691165494000,
	//     null,
	//     29278.95838065,
	//     29260.5,
	//     null,
	//     36950860.76010305,
	//     null,
	//     1691193600000,
	//     0.00001449,
	//     222,
	//     null,
	//     0.00014548,
	//     null,
	//     null,
	//     29260.005,
	//     null,
	//     null,
	//     9635.86484562,
	//     null,
	//     null,
	//     null,
	//     0.0005,
	//     0.0025
	// ]
	//
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	var timestamp any = this.SafeInteger(contract, 0)
	var nextFundingTimestamp any = this.SafeInteger(contract, 7)
	return map[string]any{
		"info":                     contract,
		"symbol":                   this.SafeSymbol(nil, market),
		"markPrice":                this.SafeNumber(contract, 14),
		"indexPrice":               this.SafeNumber(contract, 2),
		"interestRate":             nil,
		"estimatedSettlePrice":     nil,
		"timestamp":                timestamp,
		"datetime":                 this.Iso8601(timestamp),
		"fundingRate":              this.SafeNumber(contract, 11),
		"fundingTimestamp":         nil,
		"fundingDatetime":          nil,
		"nextFundingRate":          this.SafeNumber(contract, 8),
		"nextFundingTimestamp":     nextFundingTimestamp,
		"nextFundingDatetime":      this.Iso8601(nextFundingTimestamp),
		"previousFundingRate":      nil,
		"previousFundingTimestamp": nil,
		"previousFundingDatetime":  nil,
	}
}

/**
 * @method
 * @name bitfinex#fetchOpenInterests
 * @description Retrieves the open interest for a list of symbols
 * @see https://docs.bitfinex.com/reference/rest-public-derivatives-status
 * @param {string[]} [symbols] a list of unified CCXT market symbols
 * @param {object} [params] exchange specific parameters
 * @returns {object[]} a list of [open interest structures]{@link https://docs.ccxt.com/?id=open-interest-structure}
 */
func (this *Bitfinex) FetchOpenInterestsAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchOpenInterestsBody(ch, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchOpenInterestsBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbols := GetArg(optionalArgs, 0, nil)
	_ = symbols
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes350712 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes350712)
	}
	symbols = this.MarketSymbols(symbols)
	var marketIds any = []any{"ALL"}
	if IsTrue(!IsEqual(symbols, nil)) {
		marketIds = this.MarketIds(symbols)
	}
	var request map[string]any = map[string]any{
		"keys": Join(marketIds, ","),
	}

	response := (<-this.PublicGetStatusDeriv(this.Extend(request, params)))
	PanicOnError(response)

	//
	//     [
	//         [
	//             "tXRPF0:USTF0",  // market id
	//             1706256986000,   // millisecond timestamp
	//             null,
	//             0.512705,        // derivative mid price
	//             0.512395,        // underlying spot mid price
	//             null,
	//             37671483.04,     // insurance fund balance
	//             null,
	//             1706284800000,   // timestamp of next funding
	//             0.00002353,      // accrued funding for next period
	//             317,             // next funding step
	//             null,
	//             0,               // current funding
	//             null,
	//             null,
	//             0.5123016,       // mark price
	//             null,
	//             null,
	//             2233562.03115,   // open interest in contracts
	//             null,
	//             null,
	//             null,
	//             0.0005,          // average spread without funding payment
	//             0.0025           // funding payment cap
	//         ]
	//     ]
	//
	ch <- this.ParseOpenInterests(response, symbols)
	return nil
}

/**
 * @method
 * @name bitfinex#fetchOpenInterest
 * @description retrieves the open interest of a contract trading pair
 * @see https://docs.bitfinex.com/reference/rest-public-derivatives-status
 * @param {string} symbol unified CCXT market symbol
 * @param {object} [params] exchange specific parameters
 * @returns {object} an [open interest structure]{@link https://docs.ccxt.com/?id=open-interest-structure}
 */
func (this *Bitfinex) FetchOpenInterestAsync(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchOpenInterestBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchOpenInterestBody(ch chan any, symbol any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes356212 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes356212)
	}
	var market any = this.Market(symbol)
	var request map[string]any = map[string]any{
		"keys": GetValue(market, "id"),
	}

	response := (<-this.PublicGetStatusDeriv(this.Extend(request, params)))
	PanicOnError(response)
	//
	//     [
	//         [
	//             "tXRPF0:USTF0",  // market id
	//             1706256986000,   // millisecond timestamp
	//             null,
	//             0.512705,        // derivative mid price
	//             0.512395,        // underlying spot mid price
	//             null,
	//             37671483.04,     // insurance fund balance
	//             null,
	//             1706284800000,   // timestamp of next funding
	//             0.00002353,      // accrued funding for next period
	//             317,             // next funding step
	//             null,
	//             0,               // current funding
	//             null,
	//             null,
	//             0.5123016,       // mark price
	//             null,
	//             null,
	//             2233562.03115,   // open interest in contracts
	//             null,
	//             null,
	//             null,
	//             0.0005,          // average spread without funding payment
	//             0.0025           // funding payment cap
	//         ]
	//     ]
	//
	var oi any = this.SafeList(response, 0)

	ch <- this.ParseOpenInterest(oi, market)
	return nil
}

/**
 * @method
 * @name bitfinex#fetchOpenInterestHistory
 * @description retrieves the open interest history of a currency
 * @see https://docs.bitfinex.com/reference/rest-public-derivatives-status-history
 * @param {string} symbol unified CCXT market symbol
 * @param {string} timeframe the time period of each row of data, not used by bitfinex
 * @param {int} [since] the time in ms of the earliest record to retrieve as a unix timestamp
 * @param {int} [limit] the number of records in the response
 * @param {object} [params] exchange specific parameters
 * @param {int} [params.until] the time in ms of the latest record to retrieve as a unix timestamp
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns An array of [open interest structures]{@link https://docs.ccxt.com/?id=open-interest-structure}
 */
func (this *Bitfinex) FetchOpenInterestHistoryAsync(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchOpenInterestHistoryBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchOpenInterestHistoryBody(ch chan any, symbol any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	timeframe := GetArg(optionalArgs, 0, "1m")
	_ = timeframe
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes361912 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes361912)
	}
	var paginate any = false
	paginateparamsVariable := this.HandleOptionAndParams(params, "fetchOpenInterestHistory", "paginate")
	paginate = GetValue(paginateparamsVariable, 0)
	params = GetValue(paginateparamsVariable, 1)
	if IsTrue(paginate) {

		retRes362419 := (<-this.FetchPaginatedCallDeterministicAsync("fetchOpenInterestHistory", symbol, since, limit, "8h", params, 5000))
		PanicOnError(retRes362419)
		ch <- retRes362419
		return nil
	}
	var market any = this.Market(symbol)
	var request any = map[string]any{
		"symbol": GetValue(market, "id"),
	}
	if IsTrue(!IsEqual(since, nil)) {
		AddElementToObject(request, "start", since)
	}
	if IsTrue(!IsEqual(limit, nil)) {
		AddElementToObject(request, "limit", limit)
	}
	requestparamsVariable := this.HandleUntilOption("end", request, params)
	request = GetValue(requestparamsVariable, 0)
	params = GetValue(requestparamsVariable, 1)

	response := (<-this.PublicGetStatusDerivSymbolHist(this.Extend(request, params)))
	PanicOnError(response)

	//
	//     [
	//         [
	//             1706295191000,       // timestamp
	//             null,
	//             42152.425382,        // derivative mid price
	//             42133,               // spot mid price
	//             null,
	//             37671589.7853521,    // insurance fund balance
	//             null,
	//             1706313600000,       // timestamp of next funding
	//             0.00018734,          // accrued funding for next period
	//             3343,                // next funding step
	//             null,
	//             0.00007587,          // current funding
	//             null,
	//             null,
	//             42134.1,             // mark price
	//             null,
	//             null,
	//             5775.20348804,       // open interest number of contracts
	//             null,
	//             null,
	//             null,
	//             0.0005,              // average spread without funding payment
	//             0.0025               // funding payment cap
	//         ],
	//     ]
	//
	ch <- this.ParseOpenInterestsHistory(response, market, since, limit)
	return nil
}
func (this *Bitfinex) ParseOpenInterest(interest any, optionalArgs ...any) any {
	//
	// fetchOpenInterest:
	//
	//     [
	//         "tXRPF0:USTF0",  // market id
	//         1706256986000,   // millisecond timestamp
	//         null,
	//         0.512705,        // derivative mid price
	//         0.512395,        // underlying spot mid price
	//         null,
	//         37671483.04,     // insurance fund balance
	//         null,
	//         1706284800000,   // timestamp of next funding
	//         0.00002353,      // accrued funding for next period
	//         317,             // next funding step
	//         null,
	//         0,               // current funding
	//         null,
	//         null,
	//         0.5123016,       // mark price
	//         null,
	//         null,
	//         2233562.03115,   // open interest in contracts
	//         null,
	//         null,
	//         null,
	//         0.0005,          // average spread without funding payment
	//         0.0025           // funding payment cap
	//     ]
	//
	// fetchOpenInterestHistory:
	//
	//     [
	//         1706295191000,       // timestamp
	//         null,
	//         42152.425382,        // derivative mid price
	//         42133,               // spot mid price
	//         null,
	//         37671589.7853521,    // insurance fund balance
	//         null,
	//         1706313600000,       // timestamp of next funding
	//         0.00018734,          // accrued funding for next period
	//         3343,                // next funding step
	//         null,
	//         0.00007587,          // current funding
	//         null,
	//         null,
	//         42134.1,             // mark price
	//         null,
	//         null,
	//         5775.20348804,       // open interest number of contracts
	//         null,
	//         null,
	//         null,
	//         0.0005,              // average spread without funding payment
	//         0.0025               // funding payment cap
	//     ]
	//
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	var interestLength int = GetArrayLength(interest)
	var openInterestIndex any = Ternary(IsTrue((IsEqual(interestLength, 23))), 17, 18)
	var timestamp any = this.SafeInteger(interest, 1)
	var marketId any = this.SafeString(interest, 0)
	return this.SafeOpenInterest(map[string]any{
		"symbol":             this.SafeSymbol(marketId, market, nil, "swap"),
		"openInterestAmount": this.SafeNumber(interest, openInterestIndex),
		"openInterestValue":  nil,
		"timestamp":          timestamp,
		"datetime":           this.Iso8601(timestamp),
		"info":               interest,
	}, market)
}

/**
 * @method
 * @name bitfinex#fetchLiquidations
 * @description retrieves the public liquidations of a trading pair
 * @see https://docs.bitfinex.com/reference/rest-public-liquidations
 * @param {string} symbol unified CCXT market symbol
 * @param {int} [since] the earliest time in ms to fetch liquidations for
 * @param {int} [limit] the maximum number of liquidation structures to retrieve
 * @param {object} [params] exchange specific parameters
 * @param {int} [params.until] timestamp in ms of the latest liquidation
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns {object} an array of [liquidation structures]{@link https://docs.ccxt.com/?id=liquidation-structure}
 */
func (this *Bitfinex) FetchLiquidationsAsync(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchLiquidationsBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchLiquidationsBody(ch chan any, symbol any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	since := GetArg(optionalArgs, 0, nil)
	_ = since
	limit := GetArg(optionalArgs, 1, nil)
	_ = limit
	params := GetArg(optionalArgs, 2, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes375812 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes375812)
	}
	var paginate any = false
	paginateparamsVariable := this.HandleOptionAndParams(params, "fetchLiquidations", "paginate")
	paginate = GetValue(paginateparamsVariable, 0)
	params = GetValue(paginateparamsVariable, 1)
	if IsTrue(paginate) {

		retRes376319 := (<-this.FetchPaginatedCallDeterministicAsync("fetchLiquidations", symbol, since, limit, "8h", params, 500))
		PanicOnError(retRes376319)
		ch <- retRes376319
		return nil
	}
	var market any = this.Market(symbol)
	var request any = map[string]any{}
	if IsTrue(!IsEqual(since, nil)) {
		AddElementToObject(request, "start", since)
	}
	if IsTrue(!IsEqual(limit, nil)) {
		AddElementToObject(request, "limit", limit)
	}
	requestparamsVariable := this.HandleUntilOption("end", request, params)
	request = GetValue(requestparamsVariable, 0)
	params = GetValue(requestparamsVariable, 1)

	response := (<-this.PublicGetLiquidationsHist(this.Extend(request, params)))
	PanicOnError(response)

	//
	//     [
	//         [
	//             [
	//                 "pos",
	//                 171085137,
	//                 1706395919788,
	//                 null,
	//                 "tAVAXF0:USTF0",
	//                 -8,
	//                 32.868,
	//                 null,
	//                 1,
	//                 1,
	//                 null,
	//                 33.255
	//             ]
	//         ],
	//     ]
	//
	ch <- this.ParseLiquidations(this.ToArray(response), market, since, limit)
	return nil
}
func (this *Bitfinex) ParseLiquidation(liquidation any, optionalArgs ...any) any {
	//
	//     [
	//         [
	//             "pos",
	//             171085137,       // position id
	//             1706395919788,   // timestamp
	//             null,
	//             "tAVAXF0:USTF0", // market id
	//             -8,              // amount in contracts
	//             32.868,          // base price
	//             null,
	//             1,
	//             1,
	//             null,
	//             33.255           // acquired price
	//         ]
	//     ]
	//
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	var entry any = GetValue(liquidation, 0)
	var timestamp any = this.SafeInteger(entry, 2)
	var marketId any = this.SafeString(entry, 4)
	var contracts any = Precise.StringAbs(this.SafeString(entry, 5))
	var contractSize any = this.SafeString(market, "contractSize")
	var baseValue any = Precise.StringMul(contracts, contractSize)
	var price any = this.SafeString(entry, 11)
	var sideFlag any = this.SafeInteger(entry, 8)
	var side any = Ternary(IsTrue((IsEqual(sideFlag, 1))), "buy", "sell")
	return this.SafeLiquidation(map[string]any{
		"info":         entry,
		"symbol":       this.SafeSymbol(marketId, market, nil, "contract"),
		"contracts":    this.ParseNumber(contracts),
		"contractSize": this.ParseNumber(contractSize),
		"price":        this.ParseNumber(price),
		"side":         side,
		"baseValue":    this.ParseNumber(baseValue),
		"quoteValue":   this.ParseNumber(Precise.StringMul(baseValue, price)),
		"timestamp":    timestamp,
		"datetime":     this.Iso8601(timestamp),
	})
}

/**
 * @method
 * @name bitfinex#setMargin
 * @description either adds or reduces margin in a swap position in order to set the margin to a specific value
 * @see https://docs.bitfinex.com/reference/rest-auth-deriv-pos-collateral-set
 * @param {string} symbol unified market symbol of the market to set margin in
 * @param {float} amount the amount to set the margin to
 * @param {object} [params] parameters specific to the exchange API endpoint
 * @returns {object} A [margin structure]{@link https://github.com/ccxt/ccxt/wiki/Manual#add-margin-structure}
 */
func (this *Bitfinex) SetMarginAsync(symbol any, amount any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.setMarginBody(ch, symbol, amount, optionalArgs...)
	return ch
}
func (this *Bitfinex) setMarginBody(ch chan any, symbol any, amount any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes385212 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes385212)
	}
	var market any = this.Market(symbol)
	if IsTrue(!IsEqual(GetValue(market, "swap"), true)) {
		panic(NotSupported(Add(this.Id, " setMargin() only support swap markets")))
	}
	var request map[string]any = map[string]any{
		"symbol":     GetValue(market, "id"),
		"collateral": this.ParseToNumeric(amount),
	}

	response := (<-this.PrivatePostAuthWDerivCollateralSet(this.Extend(request, params)))
	PanicOnError(response)
	//
	//     [
	//         [
	//             1
	//         ]
	//     ]
	//
	var data any = this.SafeValue(response, 0)

	ch <- this.ParseMarginModification(data, market)
	return nil
}
func (this *Bitfinex) ParseMarginModification(data any, optionalArgs ...any) any {
	//
	// setMargin
	//
	//     [
	//         [
	//             1
	//         ]
	//     ]
	//
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	var marginStatusRaw any = GetValue(data, 0)
	var marginStatus any = Ternary(IsTrue((IsEqual(marginStatusRaw, 1))), "ok", "failed")
	return map[string]any{
		"info":       data,
		"symbol":     this.SafeString(market, "symbol"),
		"type":       nil,
		"marginMode": "isolated",
		"amount":     nil,
		"total":      nil,
		"code":       nil,
		"status":     marginStatus,
		"timestamp":  nil,
		"datetime":   nil,
	}
}

/**
 * @method
 * @name bitfinex#fetchOrder
 * @description fetches information on an order made by the user
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders-by-symbol
 * @param {string} id the order id
 * @param {string} [symbol] unified symbol of the market the order was made in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) FetchOrderAsync(id any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchOrderBody(ch, id, optionalArgs...)
	return ch
}
func (this *Bitfinex) fetchOrderBody(ch chan any, id any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes391312 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes391312)
	}
	var request map[string]any = map[string]any{
		"id": []any{this.ParseToNumeric(id)},
	}
	var market any = nil
	var response any = nil
	if IsTrue(IsEqual(symbol, nil)) {

		response = (<-this.PrivatePostAuthROrders(this.Extend(request, params)))
		PanicOnError(response)
	} else {
		market = this.Market(symbol)
		AddElementToObject(request, "symbol", GetValue(market, "id"))

		response = (<-this.PrivatePostAuthROrdersSymbol(this.Extend(request, params)))
		PanicOnError(response)
	}
	//
	//     [
	//         [
	//             139658969116,
	//             null,
	//             1706843908637,
	//             "tBTCUST",
	//             1706843908637,
	//             1706843908638,
	//             0.0001,
	//             0.0001,
	//             "EXCHANGE LIMIT",
	//             null,
	//             null,
	//             null,
	//             0,
	//             "ACTIVE",
	//             null,
	//             null,
	//             35000,
	//             0,
	//             0,
	//             0,
	//             null,
	//             null,
	//             null,
	//             0,
	//             0,
	//             null,
	//             null,
	//             null,
	//             "API>BFX",
	//             null,
	//             null,
	//             {}
	//         ]
	//     ]
	//
	var order any = this.SafeList(response, 0)
	var newOrder map[string]any = map[string]any{
		"result": order,
	}

	ch <- this.ParseOrder(newOrder, market)
	return nil
}

/**
 * @method
 * @name bitfinex#editOrder
 * @description edit a trade order
 * @see https://docs.bitfinex.com/reference/rest-auth-update-order
 * @param {string} id edit order id
 * @param {string} symbol unified symbol of the market to edit an order in
 * @param {string} type 'market' or 'limit'
 * @param {string} side 'buy' or 'sell'
 * @param {float} amount how much you want to trade in units of the base currency
 * @param {float} [price] the price at which the order is to be fulfilled, in units of the quote currency, ignored in market orders
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {float} [params.triggerPrice] the price that triggers a trigger order
 * @param {boolean} [params.postOnly] set to true if you want to make a post only order
 * @param {boolean} [params.reduceOnly] indicates that the order is to reduce the size of a position
 * @param {int} [params.flags] additional order parameters: 4096 (Post Only), 1024 (Reduce Only), 16384 (OCO), 64 (Hidden), 512 (Close), 524288 (No Var Rates)
 * @param {int} [params.leverage] leverage for a derivative order, supported by derivative symbol orders only, the value should be between 1 and 100 inclusive
 * @param {int} [params.clientOrderId] a unique client order id for the order
 * @param {float} [params.trailingAmount] *swap only* the quote amount to trail away from the current market price
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) EditOrderAsync(id any, symbol any, typeVar any, side any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.editOrderBody(ch, id, symbol, typeVar, side, optionalArgs...)
	return ch
}
func (this *Bitfinex) editOrderBody(ch chan any, id any, symbol any, typeVar any, side any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	amount := GetArg(optionalArgs, 0, nil)
	_ = amount
	price := GetArg(optionalArgs, 1, nil)
	_ = price
	params := GetArg(optionalArgs, 2, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes399312 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes399312)
	}
	var market any = this.Market(symbol)
	var request map[string]any = map[string]any{
		"id": this.ParseToNumeric(id),
	}
	if IsTrue(!IsEqual(amount, nil)) {
		var amountString any = this.AmountToPrecision(symbol, amount)
		amountString = Ternary(IsTrue((IsEqual(side, "buy"))), amountString, Precise.StringNeg(amountString))
		AddElementToObject(request, "amount", amountString)
	}
	var triggerPrice any = this.SafeString2(params, "stopPrice", "triggerPrice")
	var trailingAmount any = this.SafeString(params, "trailingAmount")
	var timeInForce any = this.SafeString(params, "timeInForce")
	var postOnlyParam any = this.SafeBool(params, "postOnly", false)
	var reduceOnly any = this.SafeBool(params, "reduceOnly", false)
	var clientOrderId any = this.SafeInteger2(params, "cid", "clientOrderId")
	if IsTrue(!IsEqual(trailingAmount, nil)) {
		AddElementToObject(request, "price_trailing", trailingAmount)
	} else if IsTrue(!IsEqual(triggerPrice, nil)) {
		// request['price'] is taken as triggerPrice for stop orders
		AddElementToObject(request, "price", this.PriceToPrecision(symbol, triggerPrice))
		if IsTrue(IsEqual(typeVar, "limit")) {
			AddElementToObject(request, "price_aux_limit", this.PriceToPrecision(symbol, price))
		}
	}
	var postOnly bool = (IsTrue((IsEqual(postOnlyParam, true))) || IsTrue((IsEqual(timeInForce, "PO"))))
	if IsTrue(IsTrue((!IsEqual(typeVar, "market"))) && IsTrue((IsEqual(triggerPrice, nil)))) {
		AddElementToObject(request, "price", this.PriceToPrecision(symbol, price))
	}
	// flag values may be summed to combine flags
	var flags any = 0
	if IsTrue(postOnly) {
		flags = this.Sum(flags, 4096)
	}
	if IsTrue(IsEqual(reduceOnly, true)) {
		flags = this.Sum(flags, 1024)
	}
	if IsTrue(!IsEqual(flags, 0)) {
		AddElementToObject(request, "flags", flags)
	}
	if IsTrue(!IsEqual(clientOrderId, nil)) {
		AddElementToObject(request, "cid", clientOrderId)
	}
	var leverage any = this.SafeInteger2(params, "leverage", "lev")
	if IsTrue(!IsEqual(leverage, nil)) {
		AddElementToObject(request, "lev", leverage)
	}
	params = this.Omit(params, []any{"triggerPrice", "stopPrice", "timeInForce", "postOnly", "reduceOnly", "trailingAmount", "clientOrderId", "leverage"})

	response := (<-this.PrivatePostAuthWOrderUpdate(this.Extend(request, params)))
	PanicOnError(response)
	//
	//     [
	//         1706845376402,
	//         "ou-req",
	//         null,
	//         null,
	//         [
	//             139658969116,
	//             null,
	//             1706843908637,
	//             "tBTCUST",
	//             1706843908637,
	//             1706843908638,
	//             0.0002,
	//             0.0002,
	//             "EXCHANGE LIMIT",
	//             null,
	//             null,
	//             null,
	//             0,
	//             "ACTIVE",
	//             null,
	//             null,
	//             35000,
	//             0,
	//             0,
	//             0,
	//             null,
	//             null,
	//             null,
	//             0,
	//             0,
	//             null,
	//             null,
	//             null,
	//             "API>BFX",
	//             null,
	//             null,
	//             {}
	//         ],
	//         null,
	//         "SUCCESS",
	//         "Submitting update to exchange limit buy order for 0.0002 BTC."
	//     ]
	//
	var status any = this.SafeString(response, 6)
	if IsTrue(!IsEqual(status, "SUCCESS")) {
		var errorCode any = this.SafeString(response, 5)
		var errorText any = this.SafeString(response, 7)
		panic(ExchangeError(Add(Add(Add(Add(Add(Add(Add(this.Id, " "), status), ": "), errorText), " (#"), errorCode), ")")))
	}
	var order any = this.SafeList(response, 4, []any{})
	var newOrder map[string]any = map[string]any{
		"result": order,
	}

	ch <- this.ParseOrder(newOrder, market)
	return nil
}

func NewBitfinex(userConfig map[string]any) *Bitfinex {
	p := newBitfinex()
	p.Init(userConfig)
	return p
}

func (this *Bitfinex) Init(userConfig map[string]any) {
	this.Exchange = Exchange{}
	this.Exchange.DerivedExchange = this
	this.Exchange.InitParent(userConfig, this.Describe().(map[string]any), this)
	this.exchangeTyped = NewExchangeTyped(&this.Exchange)
}

// typed methods
/**
 * @method
 * @name bitfinex#fetchStatus
 * @description the latest known information on the availability of the exchange API
 * @see https://docs.bitfinex.com/reference/rest-public-platform-status
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [status structure]{@link https://docs.ccxt.com/?id=exchange-status-structure}
 */
func (this *Bitfinex) FetchStatus(params ...any) (Status, error) {
	res := <-this.FetchStatusAsync(params...)
	if IsError(res) {
		return Status{}, CreateReturnError(res)
	}
	return NewStatus(res), nil
}

/**
 * @method
 * @name bitfinex#fetchMarkets
 * @description retrieves data on all markets for bitfinex
 * @see https://docs.bitfinex.com/reference/rest-public-conf
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} an array of objects representing market data
 */
func (this *Bitfinex) FetchMarkets(params ...any) ([]MarketInterface, error) {
	res := <-this.FetchMarketsAsync(params...)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewMarketInterfaceArray(res), nil
}

/**
 * @method
 * @name bitfinex#fetchCurrencies
 * @description fetches all available currencies on an exchange
 * @see https://docs.bitfinex.com/reference/rest-public-conf
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an associative dictionary of currencies
 */
func (this *Bitfinex) FetchCurrencies(params ...any) (Currencies, error) {
	res := <-this.FetchCurrenciesAsync(params...)
	if IsError(res) {
		return Currencies{}, CreateReturnError(res)
	}
	return NewCurrencies(res), nil
}

/**
 * @method
 * @name bitfinex#fetchBalance
 * @description query for balance and get the amount of funds available for trading or funds locked in orders
 * @see https://docs.bitfinex.com/reference/rest-auth-wallets
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [balance structure]{@link https://docs.ccxt.com/?id=balance-structure}
 */
func (this *Bitfinex) FetchBalance(params ...any) (Balances, error) {
	res := <-this.FetchBalanceAsync(params...)
	if IsError(res) {
		return Balances{}, CreateReturnError(res)
	}
	return NewBalances(res), nil
}

/**
 * @method
 * @name bitfinex#transfer
 * @description transfer currency internally between wallets on the same account
 * @see https://docs.bitfinex.com/reference/rest-auth-transfer
 * @param {string} code unified currency code
 * @param {float} amount amount to transfer
 * @param {string} fromAccount account to transfer from
 * @param {string} toAccount account to transfer to
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [transfer structure]{@link https://docs.ccxt.com/?id=transfer-structure}
 */
func (this *Bitfinex) Transfer(code string, amount float64, fromAccount string, toAccount string, options ...TransferOptions) (TransferEntry, error) {

	opts := TransferOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var params *map[string]any = opts.Params
	res := <-this.TransferAsync(code, amount, fromAccount, toAccount, params)
	if IsError(res) {
		return TransferEntry{}, CreateReturnError(res)
	}
	return NewTransferEntry(res), nil
}

/**
 * @method
 * @name bitfinex#fetchOrderBook
 * @description fetches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
 * @see https://docs.bitfinex.com/reference/rest-public-book
 * @param {string} symbol unified symbol of the market to fetch the order book for
 * @param {int} [limit] the maximum amount of order book entries to return, bitfinex only allows 1, 25, or 100
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order book structure]{@link https://docs.ccxt.com/?id=order-book-structure}
 */
func (this *Bitfinex) FetchOrderBook(symbol string, options ...FetchOrderBookOptions) (OrderBook, error) {

	opts := FetchOrderBookOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchOrderBookAsync(symbol, limit, params)
	if IsError(res) {
		return OrderBook{}, CreateReturnError(res)
	}
	return NewOrderBook(res), nil
}

/**
 * @method
 * @name bitfinex#fetchTickers
 * @description fetches price tickers for multiple markets, statistical information calculated over the past 24 hours for each market
 * @see https://docs.bitfinex.com/reference/rest-public-tickers
 * @param {string[]|undefined} symbols unified symbols of the markets to fetch the ticker for, all market tickers are returned if not assigned
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a dictionary of [ticker structures]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
func (this *Bitfinex) FetchTickers(options ...FetchTickersOptions) (Tickers, error) {

	opts := FetchTickersOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbols *[]string = opts.Symbols

	var params *map[string]any = opts.Params
	res := <-this.FetchTickersAsync(symbols, params)
	if IsError(res) {
		return Tickers{}, CreateReturnError(res)
	}
	return NewTickers(res), nil
}

/**
 * @method
 * @name bitfinex#fetchTicker
 * @description fetches a price ticker, a statistical calculation with the information calculated over the past 24 hours for a specific market
 * @see https://docs.bitfinex.com/reference/rest-public-ticker
 * @param {string} symbol unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
func (this *Bitfinex) FetchTicker(symbol string, options ...FetchTickerOptions) (Ticker, error) {

	opts := FetchTickerOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var params *map[string]any = opts.Params
	res := <-this.FetchTickerAsync(symbol, params)
	if IsError(res) {
		return Ticker{}, CreateReturnError(res)
	}
	return NewTicker(res), nil
}

/**
 * @method
 * @name bitfinex#fetchTrades
 * @description get the list of most recent trades for a particular symbol
 * @see https://docs.bitfinex.com/reference/rest-public-trades
 * @param {string} symbol unified symbol of the market to fetch trades for
 * @param {int} [since] timestamp in ms of the earliest trade to fetch
 * @param {int} [limit] the maximum amount of trades to fetch, default 120, max 10000
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @param {int} [params.until] the latest time in ms to fetch entries for
 * @returns {Trade[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=public-trades}
 */
func (this *Bitfinex) FetchTrades(symbol string, options ...FetchTradesOptions) ([]Trade, error) {

	opts := FetchTradesOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchTradesAsync(symbol, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewTradeArray(res), nil
}

/**
 * @method
 * @name bitfinex#fetchOHLCV
 * @description fetches historical candlestick data containing the open, high, low, and close price, and the volume of a market
 * @see https://docs.bitfinex.com/reference/rest-public-candles
 * @param {string} symbol unified symbol of the market to fetch OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum amount of candles to fetch, default 100 max 10000
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 * @param {int} [params.until] timestamp in ms of the latest candle to fetch
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 */
func (this *Bitfinex) FetchOHLCV(symbol string, options ...FetchOHLCVOptions) ([]OHLCV, error) {

	opts := FetchOHLCVOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var timeframe *string = opts.Timeframe

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchOHLCVAsync(symbol, timeframe, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewOHLCVArray(res), nil
}

/**
 * @method
 * @name bitfinex#createOrder
 * @description create an order on the exchange
 * @see https://docs.bitfinex.com/reference/rest-auth-submit-order
 * @param {string} symbol unified CCXT market symbol
 * @param {string} type 'limit' or 'market'
 * @param {string} side 'buy' or 'sell'
 * @param {float} amount the amount of currency to trade
 * @param {float} [price] price of the order
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {float} [params.triggerPrice] the price that triggers a trigger order
 * @param {string} [params.timeInForce] "GTC", "IOC", "FOK", or "PO"
 * @param {boolean} [params.postOnly] set to true if you want to make a post only order
 * @param {boolean} [params.reduceOnly] indicates that the order is to reduce the size of a position
 * @param {int} [params.flags] additional order parameters: 4096 (Post Only), 1024 (Reduce Only), 16384 (OCO), 64 (Hidden), 512 (Close), 524288 (No Var Rates)
 * @param {int} [params.lev] leverage for a derivative order, supported by derivative symbol orders only. The value should be between 1 and 100 inclusive.
 * @param {string} [params.price_aux_limit] order price for stop limit orders
 * @param {string} [params.price_oco_stop] OCO stop price
 * @param {string} [params.trailingAmount] *swap only* the quote amount to trail away from the current market price
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) CreateOrder(symbol string, typeVar string, side string, amount float64, options ...CreateOrderOptions) (Order, error) {

	opts := CreateOrderOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var price *float64 = opts.Price

	var params *map[string]any = opts.Params
	res := <-this.CreateOrderAsync(symbol, typeVar, side, amount, price, params)
	if IsError(res) {
		return Order{}, CreateReturnError(res)
	}
	return NewOrder(res), nil
}

/**
 * @method
 * @name bitfinex#createOrders
 * @description create a list of trade orders
 * @see https://docs.bitfinex.com/reference/rest-auth-order-multi
 * @param {Array} orders list of orders to create, each object should contain the parameters required by createOrder, namely symbol, type, side, amount, price and params
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) CreateOrders(orders []OrderRequest, options ...CreateOrdersOptions) ([]Order, error) {

	opts := CreateOrdersOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var params *map[string]any = opts.Params
	res := <-this.CreateOrdersAsync(ConvertOrderRequestListToArray(orders), params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewOrderArray(res), nil
}

/**
 * @method
 * @name bitfinex#cancelAllOrders
 * @description cancel all open orders
 * @see https://docs.bitfinex.com/reference/rest-auth-cancel-orders-multiple
 * @param {string} [symbol] unified market symbol, only orders in the market of this symbol are cancelled when symbol is not undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) CancelAllOrders(options ...CancelAllOrdersOptions) ([]Order, error) {

	opts := CancelAllOrdersOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var params *map[string]any = opts.Params
	res := <-this.CancelAllOrdersAsync(symbol, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewOrderArray(res), nil
}

/**
 * @method
 * @name bitfinex#cancelOrder
 * @description cancels an open order
 * @see https://docs.bitfinex.com/reference/rest-auth-cancel-order
 * @param {string} id order id
 * @param {string} symbol Not used by bitfinex cancelOrder ()
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} An [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) CancelOrder(id string, options ...CancelOrderOptions) (Order, error) {

	opts := CancelOrderOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var params *map[string]any = opts.Params
	res := <-this.CancelOrderAsync(id, symbol, params)
	if IsError(res) {
		return Order{}, CreateReturnError(res)
	}
	return NewOrder(res), nil
}

/**
 * @method
 * @name bitfinex#cancelOrders
 * @description cancel multiple orders at the same time
 * @see https://docs.bitfinex.com/reference/rest-auth-cancel-orders-multiple
 * @param {string[]} ids order ids
 * @param {string} symbol unified market symbol, default is undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an array of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) CancelOrders(ids []string, options ...CancelOrdersOptions) ([]Order, error) {

	opts := CancelOrdersOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var params *map[string]any = opts.Params
	res := <-this.CancelOrdersAsync(ids, symbol, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewOrderArray(res), nil
}

/**
 * @method
 * @name bitfinex#fetchOpenOrder
 * @description fetch an open order by it's id
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders-by-symbol
 * @param {string} id order id
 * @param {string} symbol unified market symbol, default is undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) FetchOpenOrder(id string, options ...FetchOpenOrderOptions) (Order, error) {

	opts := FetchOpenOrderOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var params *map[string]any = opts.Params
	res := <-this.FetchOpenOrderAsync(id, symbol, params)
	if IsError(res) {
		return Order{}, CreateReturnError(res)
	}
	return NewOrder(res), nil
}

/**
 * @method
 * @name bitfinex#fetchClosedOrder
 * @description fetch an open order by it's id
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders-by-symbol
 * @param {string} id order id
 * @param {string} symbol unified market symbol, default is undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) FetchClosedOrder(id string, options ...FetchClosedOrderOptions) (Order, error) {

	opts := FetchClosedOrderOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var params *map[string]any = opts.Params
	res := <-this.FetchClosedOrderAsync(id, symbol, params)
	if IsError(res) {
		return Order{}, CreateReturnError(res)
	}
	return NewOrder(res), nil
}

/**
 * @method
 * @name bitfinex#fetchOpenOrders
 * @description fetch all unfilled currently open orders
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders-by-symbol
 * @param {string} symbol unified market symbol
 * @param {int} [since] the earliest time in ms to fetch open orders for
 * @param {int} [limit] the maximum number of  open orders structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {Order[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) FetchOpenOrders(options ...FetchOpenOrdersOptions) ([]Order, error) {

	opts := FetchOpenOrdersOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchOpenOrdersAsync(symbol, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewOrderArray(res), nil
}

/**
 * @method
 * @name bitfinex#fetchClosedOrders
 * @description fetches information on multiple closed orders made by the user
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders-by-symbol
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] the latest time in ms to fetch entries for
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [availble parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns {Order[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) FetchClosedOrders(options ...FetchClosedOrdersOptions) ([]Order, error) {

	opts := FetchClosedOrdersOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchClosedOrdersAsync(symbol, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewOrderArray(res), nil
}

/**
 * @method
 * @name bitfinex#fetchOrderTrades
 * @description fetch all the trades made from a single order
 * @see https://docs.bitfinex.com/reference/rest-auth-order-trades
 * @param {string} id order id
 * @param {string} symbol unified market symbol
 * @param {int} [since] the earliest time in ms to fetch trades for
 * @param {int} [limit] the maximum number of trades to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=trade-structure}
 */
func (this *Bitfinex) FetchOrderTrades(id string, options ...FetchOrderTradesOptions) ([]Trade, error) {

	opts := FetchOrderTradesOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchOrderTradesAsync(id, symbol, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewTradeArray(res), nil
}

/**
 * @method
 * @name bitfinex#fetchMyTrades
 * @description fetch all trades made by the user
 * @see https://docs.bitfinex.com/reference/rest-auth-trades
 * @see https://docs.bitfinex.com/reference/rest-auth-trades-by-symbol
 * @param {string} symbol unified market symbol
 * @param {int} [since] the earliest time in ms to fetch trades for
 * @param {int} [limit] the maximum number of trades structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {Trade[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=trade-structure}
 */
func (this *Bitfinex) FetchMyTrades(options ...FetchMyTradesOptions) ([]Trade, error) {

	opts := FetchMyTradesOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchMyTradesAsync(symbol, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewTradeArray(res), nil
}

/**
 * @method
 * @name bitfinex#createDepositAddress
 * @description create a currency deposit address
 * @see https://docs.bitfinex.com/reference/rest-auth-deposit-address
 * @param {string} code unified currency code of the currency for the deposit address
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [address structure]{@link https://docs.ccxt.com/?id=address-structure}
 */
func (this *Bitfinex) CreateDepositAddress(code string, options ...CreateDepositAddressOptions) (DepositAddress, error) {

	opts := CreateDepositAddressOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var params *map[string]any = opts.Params
	res := <-this.CreateDepositAddressAsync(code, params)
	if IsError(res) {
		return DepositAddress{}, CreateReturnError(res)
	}
	return NewDepositAddress(res), nil
}

/**
 * @method
 * @name bitfinex#fetchDepositAddress
 * @description fetch the deposit address for a currency associated with this account
 * @see https://docs.bitfinex.com/reference/rest-auth-deposit-address
 * @param {string} code unified currency code
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [address structure]{@link https://docs.ccxt.com/?id=address-structure}
 */
func (this *Bitfinex) FetchDepositAddress(code string, options ...FetchDepositAddressOptions) (DepositAddress, error) {

	opts := FetchDepositAddressOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var params *map[string]any = opts.Params
	res := <-this.FetchDepositAddressAsync(code, params)
	if IsError(res) {
		return DepositAddress{}, CreateReturnError(res)
	}
	return NewDepositAddress(res), nil
}

/**
 * @method
 * @name bitfinex#fetchTradingFees
 * @description fetch the trading fees for multiple markets
 * @see https://docs.bitfinex.com/reference/rest-auth-summary
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a dictionary of [fee structures]{@link https://docs.ccxt.com/?id=fee-structure} indexed by market symbols
 */
func (this *Bitfinex) FetchTradingFees(params ...any) (TradingFees, error) {
	res := <-this.FetchTradingFeesAsync(params...)
	if IsError(res) {
		return TradingFees{}, CreateReturnError(res)
	}
	return NewTradingFees(res), nil
}

/**
 * @method
 * @name bitfinex#fetchDepositsWithdrawals
 * @description fetch history of deposits and withdrawals
 * @see https://docs.bitfinex.com/reference/movement-info
 * @see https://docs.bitfinex.com/reference/rest-auth-movements
 * @param {string} [code] unified currency code for the currency of the deposit/withdrawals, default is undefined
 * @param {int} [since] timestamp in ms of the earliest deposit/withdrawal, default is undefined
 * @param {int} [limit] max number of deposit/withdrawals to return, default is undefined
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a list of [transaction structure]{@link https://docs.ccxt.com/?id=transaction-structure}
 */
func (this *Bitfinex) FetchDepositsWithdrawals(options ...FetchDepositsWithdrawalsOptions) ([]Transaction, error) {

	opts := FetchDepositsWithdrawalsOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var code *string = opts.Code

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchDepositsWithdrawalsAsync(code, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewTransactionArray(res), nil
}

/**
 * @method
 * @name bitfinex#withdraw
 * @description make a withdrawal
 * @see https://docs.bitfinex.com/reference/rest-auth-withdraw
 * @param {string} code unified currency code
 * @param {float} amount the amount to withdraw
 * @param {string} address the address to withdraw to
 * @param {string} tag
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [transaction structure]{@link https://docs.ccxt.com/?id=transaction-structure}
 */
func (this *Bitfinex) Withdraw(code string, amount float64, address string, options ...WithdrawOptions) (Transaction, error) {

	opts := WithdrawOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var tag *string = opts.Tag

	var params *map[string]any = opts.Params
	res := <-this.WithdrawAsync(code, amount, address, tag, params)
	if IsError(res) {
		return Transaction{}, CreateReturnError(res)
	}
	return NewTransaction(res), nil
}

/**
 * @method
 * @name bitfinex#fetchPositions
 * @description fetch all open positions
 * @see https://docs.bitfinex.com/reference/rest-auth-positions
 * @param {string[]|undefined} symbols list of unified market symbols
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [position structure]{@link https://docs.ccxt.com/?id=position-structure}
 */
func (this *Bitfinex) FetchPositions(options ...FetchPositionsOptions) ([]Position, error) {

	opts := FetchPositionsOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbols *[]string = opts.Symbols

	var params *map[string]any = opts.Params
	res := <-this.FetchPositionsAsync(symbols, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewPositionArray(res), nil
}

/**
 * @method
 * @name bitfinex#fetchLedger
 * @description fetch the history of changes, actions done by the user or operations that altered the balance of the user
 * @see https://docs.bitfinex.com/reference/rest-auth-ledgers
 * @param {string} [code] unified currency code, default is undefined
 * @param {int} [since] timestamp in ms of the earliest ledger entry, default is undefined
 * @param {int} [limit] max number of ledger entries to return, default is undefined, max is 2500
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] timestamp in ms of the latest ledger entry
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns {object} a [ledger structure]{@link https://docs.ccxt.com/?id=ledger-entry-structure}
 */
func (this *Bitfinex) FetchLedger(options ...FetchLedgerOptions) ([]LedgerEntry, error) {

	opts := FetchLedgerOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var code *string = opts.Code

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchLedgerAsync(code, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewLedgerEntryArray(res), nil
}

/**
 * @method
 * @name bitfinex#fetchFundingRates
 * @description fetch the current funding rate for multiple symbols
 * @see https://docs.bitfinex.com/reference/rest-public-derivatives-status
 * @param {string[]} symbols list of unified market symbols
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [funding rate structures]{@link https://docs.ccxt.com/?id=funding-rate-structure}
 */
func (this *Bitfinex) FetchFundingRates(options ...FetchFundingRatesOptions) (FundingRates, error) {

	opts := FetchFundingRatesOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbols *[]string = opts.Symbols

	var params *map[string]any = opts.Params
	res := <-this.FetchFundingRatesAsync(symbols, params)
	if IsError(res) {
		return FundingRates{}, CreateReturnError(res)
	}
	return NewFundingRates(res), nil
}

/**
 * @method
 * @name bitfinex#fetchFundingRateHistory
 * @description fetches historical funding rate prices
 * @see https://docs.bitfinex.com/reference/rest-public-derivatives-status-history
 * @param {string} symbol unified market symbol
 * @param {int} [since] timestamp in ms of the earliest funding rate entry
 * @param {int} [limit] max number of funding rate entrys to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] timestamp in ms of the latest funding rate
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns {object} a [funding rate structure]{@link https://docs.ccxt.com/?id=funding-rate-structure}
 */
func (this *Bitfinex) FetchFundingRateHistory(options ...FetchFundingRateHistoryOptions) ([]FundingRateHistory, error) {

	opts := FetchFundingRateHistoryOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchFundingRateHistoryAsync(symbol, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewFundingRateHistoryArray(res), nil
}

/**
 * @method
 * @name bitfinex#fetchOpenInterests
 * @description Retrieves the open interest for a list of symbols
 * @see https://docs.bitfinex.com/reference/rest-public-derivatives-status
 * @param {string[]} [symbols] a list of unified CCXT market symbols
 * @param {object} [params] exchange specific parameters
 * @returns {object[]} a list of [open interest structures]{@link https://docs.ccxt.com/?id=open-interest-structure}
 */
func (this *Bitfinex) FetchOpenInterests(options ...FetchOpenInterestsOptions) (OpenInterests, error) {

	opts := FetchOpenInterestsOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbols *[]string = opts.Symbols

	var params *map[string]any = opts.Params
	res := <-this.FetchOpenInterestsAsync(symbols, params)
	if IsError(res) {
		return OpenInterests{}, CreateReturnError(res)
	}
	return NewOpenInterests(res), nil
}

/**
 * @method
 * @name bitfinex#fetchOpenInterest
 * @description retrieves the open interest of a contract trading pair
 * @see https://docs.bitfinex.com/reference/rest-public-derivatives-status
 * @param {string} symbol unified CCXT market symbol
 * @param {object} [params] exchange specific parameters
 * @returns {object} an [open interest structure]{@link https://docs.ccxt.com/?id=open-interest-structure}
 */
func (this *Bitfinex) FetchOpenInterest(symbol string, options ...FetchOpenInterestOptions) (OpenInterest, error) {

	opts := FetchOpenInterestOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var params *map[string]any = opts.Params
	res := <-this.FetchOpenInterestAsync(symbol, params)
	if IsError(res) {
		return OpenInterest{}, CreateReturnError(res)
	}
	return NewOpenInterest(res), nil
}

/**
 * @method
 * @name bitfinex#fetchOpenInterestHistory
 * @description retrieves the open interest history of a currency
 * @see https://docs.bitfinex.com/reference/rest-public-derivatives-status-history
 * @param {string} symbol unified CCXT market symbol
 * @param {string} timeframe the time period of each row of data, not used by bitfinex
 * @param {int} [since] the time in ms of the earliest record to retrieve as a unix timestamp
 * @param {int} [limit] the number of records in the response
 * @param {object} [params] exchange specific parameters
 * @param {int} [params.until] the time in ms of the latest record to retrieve as a unix timestamp
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns An array of [open interest structures]{@link https://docs.ccxt.com/?id=open-interest-structure}
 */
func (this *Bitfinex) FetchOpenInterestHistory(symbol string, options ...FetchOpenInterestHistoryOptions) ([]OpenInterest, error) {

	opts := FetchOpenInterestHistoryOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var timeframe *string = opts.Timeframe

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchOpenInterestHistoryAsync(symbol, timeframe, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewOpenInterestArray(res), nil
}

/**
 * @method
 * @name bitfinex#fetchLiquidations
 * @description retrieves the public liquidations of a trading pair
 * @see https://docs.bitfinex.com/reference/rest-public-liquidations
 * @param {string} symbol unified CCXT market symbol
 * @param {int} [since] the earliest time in ms to fetch liquidations for
 * @param {int} [limit] the maximum number of liquidation structures to retrieve
 * @param {object} [params] exchange specific parameters
 * @param {int} [params.until] timestamp in ms of the latest liquidation
 * @param {boolean} [params.paginate] default false, when true will automatically paginate by calling this endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
 * @returns {object} an array of [liquidation structures]{@link https://docs.ccxt.com/?id=liquidation-structure}
 */
func (this *Bitfinex) FetchLiquidations(symbol string, options ...FetchLiquidationsOptions) ([]Liquidation, error) {

	opts := FetchLiquidationsOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchLiquidationsAsync(symbol, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewLiquidationArray(res), nil
}

/**
 * @method
 * @name bitfinex#setMargin
 * @description either adds or reduces margin in a swap position in order to set the margin to a specific value
 * @see https://docs.bitfinex.com/reference/rest-auth-deriv-pos-collateral-set
 * @param {string} symbol unified market symbol of the market to set margin in
 * @param {float} amount the amount to set the margin to
 * @param {object} [params] parameters specific to the exchange API endpoint
 * @returns {object} A [margin structure]{@link https://github.com/ccxt/ccxt/wiki/Manual#add-margin-structure}
 */
func (this *Bitfinex) SetMargin(symbol string, amount float64, options ...SetMarginOptions) (MarginModification, error) {

	opts := SetMarginOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var params *map[string]any = opts.Params
	res := <-this.SetMarginAsync(symbol, amount, params)
	if IsError(res) {
		return MarginModification{}, CreateReturnError(res)
	}
	return NewMarginModification(res), nil
}

/**
 * @method
 * @name bitfinex#fetchOrder
 * @description fetches information on an order made by the user
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders
 * @see https://docs.bitfinex.com/reference/rest-auth-retrieve-orders-by-symbol
 * @param {string} id the order id
 * @param {string} [symbol] unified symbol of the market the order was made in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) FetchOrder(id string, options ...FetchOrderOptions) (Order, error) {

	opts := FetchOrderOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var params *map[string]any = opts.Params
	res := <-this.FetchOrderAsync(id, symbol, params)
	if IsError(res) {
		return Order{}, CreateReturnError(res)
	}
	return NewOrder(res), nil
}

/**
 * @method
 * @name bitfinex#editOrder
 * @description edit a trade order
 * @see https://docs.bitfinex.com/reference/rest-auth-update-order
 * @param {string} id edit order id
 * @param {string} symbol unified symbol of the market to edit an order in
 * @param {string} type 'market' or 'limit'
 * @param {string} side 'buy' or 'sell'
 * @param {float} amount how much you want to trade in units of the base currency
 * @param {float} [price] the price at which the order is to be fulfilled, in units of the quote currency, ignored in market orders
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {float} [params.triggerPrice] the price that triggers a trigger order
 * @param {boolean} [params.postOnly] set to true if you want to make a post only order
 * @param {boolean} [params.reduceOnly] indicates that the order is to reduce the size of a position
 * @param {int} [params.flags] additional order parameters: 4096 (Post Only), 1024 (Reduce Only), 16384 (OCO), 64 (Hidden), 512 (Close), 524288 (No Var Rates)
 * @param {int} [params.leverage] leverage for a derivative order, supported by derivative symbol orders only, the value should be between 1 and 100 inclusive
 * @param {int} [params.clientOrderId] a unique client order id for the order
 * @param {float} [params.trailingAmount] *swap only* the quote amount to trail away from the current market price
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *Bitfinex) EditOrder(id string, symbol string, typeVar string, side string, options ...EditOrderOptions) (Order, error) {

	opts := EditOrderOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var amount *float64 = opts.Amount

	var price *float64 = opts.Price

	var params *map[string]any = opts.Params
	res := <-this.EditOrderAsync(id, symbol, typeVar, side, amount, price, params)
	if IsError(res) {
		return Order{}, CreateReturnError(res)
	}
	return NewOrder(res), nil
}

// missing typed methods from base
// nolint
func (this *Bitfinex) LoadMarkets(params ...any) (map[string]MarketInterface, error) {
	return this.exchangeTyped.LoadMarkets(params...)
}
func (this *Bitfinex) CancelOrdersWithClientOrderIds(clientOrderIds []string, options ...CancelOrdersWithClientOrderIdsOptions) ([]Order, error) {
	return this.exchangeTyped.CancelOrdersWithClientOrderIds(clientOrderIds, options...)
}
func (this *Bitfinex) CancelAllOrdersAfter(timeout int64, options ...CancelAllOrdersAfterOptions) (map[string]any, error) {
	return this.exchangeTyped.CancelAllOrdersAfter(timeout, options...)
}
func (this *Bitfinex) CancelOrderWithClientOrderId(clientOrderId string, options ...CancelOrderWithClientOrderIdOptions) (Order, error) {
	return this.exchangeTyped.CancelOrderWithClientOrderId(clientOrderId, options...)
}
func (this *Bitfinex) CancelOrdersForSymbols(orders []CancellationRequest, options ...CancelOrdersForSymbolsOptions) ([]Order, error) {
	return this.exchangeTyped.CancelOrdersForSymbols(orders, options...)
}
func (this *Bitfinex) CreateConvertTrade(id string, fromCode string, toCode string, options ...CreateConvertTradeOptions) (Conversion, error) {
	return this.exchangeTyped.CreateConvertTrade(id, fromCode, toCode, options...)
}
func (this *Bitfinex) CreateLimitBuyOrder(symbol string, amount float64, price float64, options ...CreateLimitBuyOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateLimitBuyOrder(symbol, amount, price, options...)
}
func (this *Bitfinex) CreateLimitOrder(symbol string, side string, amount float64, price float64, options ...CreateLimitOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateLimitOrder(symbol, side, amount, price, options...)
}
func (this *Bitfinex) CreateLimitSellOrder(symbol string, amount float64, price float64, options ...CreateLimitSellOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateLimitSellOrder(symbol, amount, price, options...)
}
func (this *Bitfinex) CreateMarketBuyOrder(symbol string, amount float64, options ...CreateMarketBuyOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketBuyOrder(symbol, amount, options...)
}
func (this *Bitfinex) CreateMarketBuyOrderWithCost(symbol string, cost float64, options ...CreateMarketBuyOrderWithCostOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketBuyOrderWithCost(symbol, cost, options...)
}
func (this *Bitfinex) CreateMarketOrder(symbol string, side string, amount float64, options ...CreateMarketOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketOrder(symbol, side, amount, options...)
}
func (this *Bitfinex) CreateMarketOrderWithCost(symbol string, side string, cost float64, options ...CreateMarketOrderWithCostOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketOrderWithCost(symbol, side, cost, options...)
}
func (this *Bitfinex) CreateMarketSellOrder(symbol string, amount float64, options ...CreateMarketSellOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketSellOrder(symbol, amount, options...)
}
func (this *Bitfinex) CreateMarketSellOrderWithCost(symbol string, cost float64, options ...CreateMarketSellOrderWithCostOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketSellOrderWithCost(symbol, cost, options...)
}
func (this *Bitfinex) CreateOrderWithTakeProfitAndStopLoss(symbol string, typeVar string, side string, amount float64, options ...CreateOrderWithTakeProfitAndStopLossOptions) (Order, error) {
	return this.exchangeTyped.CreateOrderWithTakeProfitAndStopLoss(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreatePostOnlyOrder(symbol string, typeVar string, side string, amount float64, options ...CreatePostOnlyOrderOptions) (Order, error) {
	return this.exchangeTyped.CreatePostOnlyOrder(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateReduceOnlyOrder(symbol string, typeVar string, side string, amount float64, options ...CreateReduceOnlyOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateReduceOnlyOrder(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateStopLimitOrder(symbol string, side string, amount float64, price float64, triggerPrice float64, options ...CreateStopLimitOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateStopLimitOrder(symbol, side, amount, price, triggerPrice, options...)
}
func (this *Bitfinex) CreateStopLossOrder(symbol string, typeVar string, side string, amount float64, options ...CreateStopLossOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateStopLossOrder(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateStopMarketOrder(symbol string, side string, amount float64, triggerPrice float64, options ...CreateStopMarketOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateStopMarketOrder(symbol, side, amount, triggerPrice, options...)
}
func (this *Bitfinex) CreateStopOrder(symbol string, typeVar string, side string, amount float64, options ...CreateStopOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateStopOrder(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateTakeProfitOrder(symbol string, typeVar string, side string, amount float64, options ...CreateTakeProfitOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateTakeProfitOrder(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateTrailingAmountOrder(symbol string, typeVar string, side string, amount float64, options ...CreateTrailingAmountOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateTrailingAmountOrder(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateTrailingPercentOrder(symbol string, typeVar string, side string, amount float64, options ...CreateTrailingPercentOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateTrailingPercentOrder(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateTriggerOrder(symbol string, typeVar string, side string, amount float64, options ...CreateTriggerOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateTriggerOrder(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) EditLimitBuyOrder(id string, symbol string, amount float64, options ...EditLimitBuyOrderOptions) (Order, error) {
	return this.exchangeTyped.EditLimitBuyOrder(id, symbol, amount, options...)
}
func (this *Bitfinex) EditLimitOrder(id string, symbol string, side string, amount float64, options ...EditLimitOrderOptions) (Order, error) {
	return this.exchangeTyped.EditLimitOrder(id, symbol, side, amount, options...)
}
func (this *Bitfinex) EditLimitSellOrder(id string, symbol string, amount float64, options ...EditLimitSellOrderOptions) (Order, error) {
	return this.exchangeTyped.EditLimitSellOrder(id, symbol, amount, options...)
}
func (this *Bitfinex) EditOrderWithClientOrderId(clientOrderId string, symbol string, typeVar string, side string, options ...EditOrderWithClientOrderIdOptions) (Order, error) {
	return this.exchangeTyped.EditOrderWithClientOrderId(clientOrderId, symbol, typeVar, side, options...)
}
func (this *Bitfinex) EditOrders(orders []OrderRequest, options ...EditOrdersOptions) ([]Order, error) {
	return this.exchangeTyped.EditOrders(orders, options...)
}
func (this *Bitfinex) FetchAccounts(params ...any) ([]Account, error) {
	return this.exchangeTyped.FetchAccounts(params...)
}
func (this *Bitfinex) FetchAllGreeks(options ...FetchAllGreeksOptions) (AllGreeks, error) {
	return this.exchangeTyped.FetchAllGreeks(options...)
}
func (this *Bitfinex) FetchBidsAsks(options ...FetchBidsAsksOptions) (Tickers, error) {
	return this.exchangeTyped.FetchBidsAsks(options...)
}
func (this *Bitfinex) FetchBorrowInterest(options ...FetchBorrowInterestOptions) ([]BorrowInterest, error) {
	return this.exchangeTyped.FetchBorrowInterest(options...)
}
func (this *Bitfinex) FetchBorrowRate(code string, amount float64, options ...FetchBorrowRateOptions) (map[string]any, error) {
	return this.exchangeTyped.FetchBorrowRate(code, amount, options...)
}
func (this *Bitfinex) FetchCanceledAndClosedOrders(options ...FetchCanceledAndClosedOrdersOptions) ([]Order, error) {
	return this.exchangeTyped.FetchCanceledAndClosedOrders(options...)
}
func (this *Bitfinex) FetchConvertCurrencies(params ...any) (Currencies, error) {
	return this.exchangeTyped.FetchConvertCurrencies(params...)
}
func (this *Bitfinex) FetchConvertQuote(fromCode string, toCode string, options ...FetchConvertQuoteOptions) (Conversion, error) {
	return this.exchangeTyped.FetchConvertQuote(fromCode, toCode, options...)
}
func (this *Bitfinex) FetchConvertTrade(id string, options ...FetchConvertTradeOptions) (Conversion, error) {
	return this.exchangeTyped.FetchConvertTrade(id, options...)
}
func (this *Bitfinex) FetchConvertTradeHistory(options ...FetchConvertTradeHistoryOptions) ([]Conversion, error) {
	return this.exchangeTyped.FetchConvertTradeHistory(options...)
}
func (this *Bitfinex) FetchCrossBorrowRate(code string, options ...FetchCrossBorrowRateOptions) (CrossBorrowRate, error) {
	return this.exchangeTyped.FetchCrossBorrowRate(code, options...)
}
func (this *Bitfinex) FetchCrossBorrowRates(params ...any) (CrossBorrowRates, error) {
	return this.exchangeTyped.FetchCrossBorrowRates(params...)
}
func (this *Bitfinex) FetchDepositAddresses(options ...FetchDepositAddressesOptions) ([]DepositAddress, error) {
	return this.exchangeTyped.FetchDepositAddresses(options...)
}
func (this *Bitfinex) FetchDepositAddressesByNetwork(code string, options ...FetchDepositAddressesByNetworkOptions) (DepositAddresses, error) {
	return this.exchangeTyped.FetchDepositAddressesByNetwork(code, options...)
}
func (this *Bitfinex) FetchDeposits(options ...FetchDepositsOptions) ([]Transaction, error) {
	return this.exchangeTyped.FetchDeposits(options...)
}
func (this *Bitfinex) FetchDepositWithdrawFee(code string, options ...FetchDepositWithdrawFeeOptions) (DepositWithdrawFee, error) {
	return this.exchangeTyped.FetchDepositWithdrawFee(code, options...)
}
func (this *Bitfinex) FetchDepositWithdrawFees(options ...FetchDepositWithdrawFeesOptions) (DepositWithdrawFees, error) {
	return this.exchangeTyped.FetchDepositWithdrawFees(options...)
}
func (this *Bitfinex) FetchFreeBalance(params ...any) (Balance, error) {
	return this.exchangeTyped.FetchFreeBalance(params...)
}
func (this *Bitfinex) FetchFundingHistory(options ...FetchFundingHistoryOptions) ([]FundingHistory, error) {
	return this.exchangeTyped.FetchFundingHistory(options...)
}
func (this *Bitfinex) FetchFundingInterval(symbol string, options ...FetchFundingIntervalOptions) (FundingRate, error) {
	return this.exchangeTyped.FetchFundingInterval(symbol, options...)
}
func (this *Bitfinex) FetchFundingIntervals(options ...FetchFundingIntervalsOptions) (FundingRates, error) {
	return this.exchangeTyped.FetchFundingIntervals(options...)
}
func (this *Bitfinex) FetchFundingRate(symbol string, options ...FetchFundingRateOptions) (FundingRate, error) {
	return this.exchangeTyped.FetchFundingRate(symbol, options...)
}
func (this *Bitfinex) FetchGreeks(symbol string, options ...FetchGreeksOptions) (Greeks, error) {
	return this.exchangeTyped.FetchGreeks(symbol, options...)
}
func (this *Bitfinex) FetchIndexOHLCV(symbol string, options ...FetchIndexOHLCVOptions) ([]OHLCV, error) {
	return this.exchangeTyped.FetchIndexOHLCV(symbol, options...)
}
func (this *Bitfinex) FetchIsolatedBorrowRate(symbol string, options ...FetchIsolatedBorrowRateOptions) (IsolatedBorrowRate, error) {
	return this.exchangeTyped.FetchIsolatedBorrowRate(symbol, options...)
}
func (this *Bitfinex) FetchIsolatedBorrowRates(params ...any) (IsolatedBorrowRates, error) {
	return this.exchangeTyped.FetchIsolatedBorrowRates(params...)
}
func (this *Bitfinex) FetchLastPrices(options ...FetchLastPricesOptions) (LastPrices, error) {
	return this.exchangeTyped.FetchLastPrices(options...)
}
func (this *Bitfinex) FetchLedgerEntry(id string, options ...FetchLedgerEntryOptions) (LedgerEntry, error) {
	return this.exchangeTyped.FetchLedgerEntry(id, options...)
}
func (this *Bitfinex) FetchLeverage(symbol string, options ...FetchLeverageOptions) (Leverage, error) {
	return this.exchangeTyped.FetchLeverage(symbol, options...)
}
func (this *Bitfinex) FetchLeverages(options ...FetchLeveragesOptions) (Leverages, error) {
	return this.exchangeTyped.FetchLeverages(options...)
}
func (this *Bitfinex) FetchLeverageTiers(options ...FetchLeverageTiersOptions) (LeverageTiers, error) {
	return this.exchangeTyped.FetchLeverageTiers(options...)
}
func (this *Bitfinex) FetchLongShortRatio(symbol string, options ...FetchLongShortRatioOptions) (LongShortRatio, error) {
	return this.exchangeTyped.FetchLongShortRatio(symbol, options...)
}
func (this *Bitfinex) FetchLongShortRatioHistory(options ...FetchLongShortRatioHistoryOptions) ([]LongShortRatio, error) {
	return this.exchangeTyped.FetchLongShortRatioHistory(options...)
}
func (this *Bitfinex) FetchMarginAdjustmentHistory(options ...FetchMarginAdjustmentHistoryOptions) ([]MarginModification, error) {
	return this.exchangeTyped.FetchMarginAdjustmentHistory(options...)
}
func (this *Bitfinex) FetchMarginMode(symbol string, options ...FetchMarginModeOptions) (MarginMode, error) {
	return this.exchangeTyped.FetchMarginMode(symbol, options...)
}
func (this *Bitfinex) FetchMarginModes(options ...FetchMarginModesOptions) (MarginModes, error) {
	return this.exchangeTyped.FetchMarginModes(options...)
}
func (this *Bitfinex) FetchMarketLeverageTiers(symbol string, options ...FetchMarketLeverageTiersOptions) ([]LeverageTier, error) {
	return this.exchangeTyped.FetchMarketLeverageTiers(symbol, options...)
}
func (this *Bitfinex) FetchMarkOHLCV(symbol string, options ...FetchMarkOHLCVOptions) ([]OHLCV, error) {
	return this.exchangeTyped.FetchMarkOHLCV(symbol, options...)
}
func (this *Bitfinex) FetchMarkPrice(symbol string, options ...FetchMarkPriceOptions) (Ticker, error) {
	return this.exchangeTyped.FetchMarkPrice(symbol, options...)
}
func (this *Bitfinex) FetchMarkPrices(options ...FetchMarkPricesOptions) (Tickers, error) {
	return this.exchangeTyped.FetchMarkPrices(options...)
}
func (this *Bitfinex) FetchMyLiquidations(options ...FetchMyLiquidationsOptions) ([]Liquidation, error) {
	return this.exchangeTyped.FetchMyLiquidations(options...)
}
func (this *Bitfinex) FetchOption(symbol string, options ...FetchOptionOptions) (Option, error) {
	return this.exchangeTyped.FetchOption(symbol, options...)
}
func (this *Bitfinex) FetchOptionChain(code string, options ...FetchOptionChainOptions) (OptionChain, error) {
	return this.exchangeTyped.FetchOptionChain(code, options...)
}
func (this *Bitfinex) FetchOrderWithClientOrderId(clientOrderId string, options ...FetchOrderWithClientOrderIdOptions) (Order, error) {
	return this.exchangeTyped.FetchOrderWithClientOrderId(clientOrderId, options...)
}
func (this *Bitfinex) FetchOrderBooks(options ...FetchOrderBooksOptions) (OrderBooks, error) {
	return this.exchangeTyped.FetchOrderBooks(options...)
}
func (this *Bitfinex) FetchOrders(options ...FetchOrdersOptions) ([]Order, error) {
	return this.exchangeTyped.FetchOrders(options...)
}
func (this *Bitfinex) FetchOrderStatus(id string, options ...FetchOrderStatusOptions) (string, error) {
	return this.exchangeTyped.FetchOrderStatus(id, options...)
}
func (this *Bitfinex) FetchPaymentMethods(params ...any) (map[string]any, error) {
	return this.exchangeTyped.FetchPaymentMethods(params...)
}
func (this *Bitfinex) FetchPosition(symbol string, options ...FetchPositionOptions) (Position, error) {
	return this.exchangeTyped.FetchPosition(symbol, options...)
}
func (this *Bitfinex) FetchPositionHistory(symbol string, options ...FetchPositionHistoryOptions) ([]Position, error) {
	return this.exchangeTyped.FetchPositionHistory(symbol, options...)
}
func (this *Bitfinex) FetchPositionMode(options ...FetchPositionModeOptions) (PositionModeInfo, error) {
	return this.exchangeTyped.FetchPositionMode(options...)
}
func (this *Bitfinex) FetchPositionsForSymbol(symbol string, options ...FetchPositionsForSymbolOptions) ([]Position, error) {
	return this.exchangeTyped.FetchPositionsForSymbol(symbol, options...)
}
func (this *Bitfinex) FetchPositionsHistory(options ...FetchPositionsHistoryOptions) ([]Position, error) {
	return this.exchangeTyped.FetchPositionsHistory(options...)
}
func (this *Bitfinex) FetchPositionsRisk(options ...FetchPositionsRiskOptions) ([]Position, error) {
	return this.exchangeTyped.FetchPositionsRisk(options...)
}
func (this *Bitfinex) FetchPremiumIndexOHLCV(symbol string, options ...FetchPremiumIndexOHLCVOptions) ([]OHLCV, error) {
	return this.exchangeTyped.FetchPremiumIndexOHLCV(symbol, options...)
}
func (this *Bitfinex) FetchTime(params ...any) (int64, error) {
	return this.exchangeTyped.FetchTime(params...)
}
func (this *Bitfinex) FetchTradingFee(symbol string, options ...FetchTradingFeeOptions) (TradingFeeInterface, error) {
	return this.exchangeTyped.FetchTradingFee(symbol, options...)
}
func (this *Bitfinex) FetchTradingLimits(options ...FetchTradingLimitsOptions) (map[string]any, error) {
	return this.exchangeTyped.FetchTradingLimits(options...)
}
func (this *Bitfinex) FetchTransactionFee(code string, options ...FetchTransactionFeeOptions) (map[string]any, error) {
	return this.exchangeTyped.FetchTransactionFee(code, options...)
}
func (this *Bitfinex) FetchTransactionFees(options ...FetchTransactionFeesOptions) (map[string]any, error) {
	return this.exchangeTyped.FetchTransactionFees(options...)
}
func (this *Bitfinex) FetchTransactions(options ...FetchTransactionsOptions) ([]Transaction, error) {
	return this.exchangeTyped.FetchTransactions(options...)
}
func (this *Bitfinex) FetchTransfer(id string, options ...FetchTransferOptions) (TransferEntry, error) {
	return this.exchangeTyped.FetchTransfer(id, options...)
}
func (this *Bitfinex) FetchTransfers(options ...FetchTransfersOptions) ([]TransferEntry, error) {
	return this.exchangeTyped.FetchTransfers(options...)
}
func (this *Bitfinex) FetchWithdrawals(options ...FetchWithdrawalsOptions) ([]Transaction, error) {
	return this.exchangeTyped.FetchWithdrawals(options...)
}
func (this *Bitfinex) SetMarginMode(marginMode string, options ...SetMarginModeOptions) (map[string]any, error) {
	return this.exchangeTyped.SetMarginMode(marginMode, options...)
}
func (this *Bitfinex) SetPositionMode(hedged bool, options ...SetPositionModeOptions) (map[string]any, error) {
	return this.exchangeTyped.SetPositionMode(hedged, options...)
}
func (this *Bitfinex) CancelAllOrdersWs(options ...CancelAllOrdersWsOptions) ([]Order, error) {
	return this.exchangeTyped.CancelAllOrdersWs(options...)
}
func (this *Bitfinex) CancelOrdersWs(ids []string, options ...CancelOrdersWsOptions) ([]Order, error) {
	return this.exchangeTyped.CancelOrdersWs(ids, options...)
}
func (this *Bitfinex) CancelOrderWs(id string, options ...CancelOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CancelOrderWs(id, options...)
}
func (this *Bitfinex) CreateLimitBuyOrderWs(symbol string, amount float64, price float64, options ...CreateLimitBuyOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateLimitBuyOrderWs(symbol, amount, price, options...)
}
func (this *Bitfinex) CreateLimitOrderWs(symbol string, side string, amount float64, price float64, options ...CreateLimitOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateLimitOrderWs(symbol, side, amount, price, options...)
}
func (this *Bitfinex) CreateLimitSellOrderWs(symbol string, amount float64, price float64, options ...CreateLimitSellOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateLimitSellOrderWs(symbol, amount, price, options...)
}
func (this *Bitfinex) CreateMarketBuyOrderWs(symbol string, amount float64, options ...CreateMarketBuyOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketBuyOrderWs(symbol, amount, options...)
}
func (this *Bitfinex) CreateMarketOrderWithCostWs(symbol string, side string, cost float64, options ...CreateMarketOrderWithCostWsOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketOrderWithCostWs(symbol, side, cost, options...)
}
func (this *Bitfinex) CreateMarketOrderWs(symbol string, side string, amount float64, options ...CreateMarketOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketOrderWs(symbol, side, amount, options...)
}
func (this *Bitfinex) CreateMarketSellOrderWs(symbol string, amount float64, options ...CreateMarketSellOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketSellOrderWs(symbol, amount, options...)
}
func (this *Bitfinex) CreateOrdersWs(orders []OrderRequest, options ...CreateOrdersWsOptions) ([]Order, error) {
	return this.exchangeTyped.CreateOrdersWs(orders, options...)
}
func (this *Bitfinex) CreateOrderWithTakeProfitAndStopLossWs(symbol string, typeVar string, side string, amount float64, options ...CreateOrderWithTakeProfitAndStopLossWsOptions) (Order, error) {
	return this.exchangeTyped.CreateOrderWithTakeProfitAndStopLossWs(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreatePostOnlyOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreatePostOnlyOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreatePostOnlyOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateReduceOnlyOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateReduceOnlyOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateReduceOnlyOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateStopLimitOrderWs(symbol string, side string, amount float64, price float64, triggerPrice float64, options ...CreateStopLimitOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateStopLimitOrderWs(symbol, side, amount, price, triggerPrice, options...)
}
func (this *Bitfinex) CreateStopLossOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateStopLossOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateStopLossOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateStopMarketOrderWs(symbol string, side string, amount float64, triggerPrice float64, options ...CreateStopMarketOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateStopMarketOrderWs(symbol, side, amount, triggerPrice, options...)
}
func (this *Bitfinex) CreateStopOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateStopOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateStopOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateTakeProfitOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateTakeProfitOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateTakeProfitOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateTrailingAmountOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateTrailingAmountOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateTrailingAmountOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateTrailingPercentOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateTrailingPercentOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateTrailingPercentOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) CreateTriggerOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateTriggerOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateTriggerOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Bitfinex) EditOrderWs(id string, symbol string, typeVar string, side string, options ...EditOrderWsOptions) (Order, error) {
	return this.exchangeTyped.EditOrderWs(id, symbol, typeVar, side, options...)
}
func (this *Bitfinex) FetchBalanceWs(params ...any) (Balances, error) {
	return this.exchangeTyped.FetchBalanceWs(params...)
}
func (this *Bitfinex) FetchClosedOrdersWs(options ...FetchClosedOrdersWsOptions) ([]Order, error) {
	return this.exchangeTyped.FetchClosedOrdersWs(options...)
}
func (this *Bitfinex) FetchDepositsWs(options ...FetchDepositsWsOptions) ([]Transaction, error) {
	return this.exchangeTyped.FetchDepositsWs(options...)
}
func (this *Bitfinex) FetchMyTradesWs(options ...FetchMyTradesWsOptions) ([]Trade, error) {
	return this.exchangeTyped.FetchMyTradesWs(options...)
}
func (this *Bitfinex) FetchOHLCVWs(symbol string, options ...FetchOHLCVWsOptions) ([]OHLCV, error) {
	return this.exchangeTyped.FetchOHLCVWs(symbol, options...)
}
func (this *Bitfinex) FetchOpenOrdersWs(options ...FetchOpenOrdersWsOptions) ([]Order, error) {
	return this.exchangeTyped.FetchOpenOrdersWs(options...)
}
func (this *Bitfinex) FetchOrderBookWs(symbol string, options ...FetchOrderBookWsOptions) (OrderBook, error) {
	return this.exchangeTyped.FetchOrderBookWs(symbol, options...)
}
func (this *Bitfinex) FetchOrdersByStatusWs(status string, options ...FetchOrdersByStatusWsOptions) ([]Order, error) {
	return this.exchangeTyped.FetchOrdersByStatusWs(status, options...)
}
func (this *Bitfinex) FetchOrdersWs(options ...FetchOrdersWsOptions) ([]Order, error) {
	return this.exchangeTyped.FetchOrdersWs(options...)
}
func (this *Bitfinex) FetchOrderWs(id string, options ...FetchOrderWsOptions) (Order, error) {
	return this.exchangeTyped.FetchOrderWs(id, options...)
}
func (this *Bitfinex) FetchPositionsForSymbolWs(symbol string, options ...FetchPositionsForSymbolWsOptions) ([]Position, error) {
	return this.exchangeTyped.FetchPositionsForSymbolWs(symbol, options...)
}
func (this *Bitfinex) FetchPositionsWs(options ...FetchPositionsWsOptions) ([]Position, error) {
	return this.exchangeTyped.FetchPositionsWs(options...)
}
func (this *Bitfinex) FetchPositionWs(symbol string, options ...FetchPositionWsOptions) ([]Position, error) {
	return this.exchangeTyped.FetchPositionWs(symbol, options...)
}
func (this *Bitfinex) FetchTickersWs(options ...FetchTickersWsOptions) (Tickers, error) {
	return this.exchangeTyped.FetchTickersWs(options...)
}
func (this *Bitfinex) FetchTickerWs(symbol string, options ...FetchTickerWsOptions) (Ticker, error) {
	return this.exchangeTyped.FetchTickerWs(symbol, options...)
}
func (this *Bitfinex) FetchTradesWs(symbol string, options ...FetchTradesWsOptions) ([]Trade, error) {
	return this.exchangeTyped.FetchTradesWs(symbol, options...)
}
func (this *Bitfinex) FetchTradingFeesWs(params ...any) (TradingFees, error) {
	return this.exchangeTyped.FetchTradingFeesWs(params...)
}
func (this *Bitfinex) FetchWithdrawalsWs(options ...FetchWithdrawalsWsOptions) ([]Transaction, error) {
	return this.exchangeTyped.FetchWithdrawalsWs(options...)
}
func (this *Bitfinex) UnWatchBidsAsks(options ...UnWatchBidsAsksOptions) (any, error) {
	return this.exchangeTyped.UnWatchBidsAsks(options...)
}
func (this *Bitfinex) UnWatchMyTrades(options ...UnWatchMyTradesOptions) (any, error) {
	return this.exchangeTyped.UnWatchMyTrades(options...)
}
func (this *Bitfinex) UnWatchOHLCV(symbol string, options ...UnWatchOHLCVOptions) (any, error) {
	return this.exchangeTyped.UnWatchOHLCV(symbol, options...)
}
func (this *Bitfinex) UnWatchOHLCVForSymbols(symbolsAndTimeframes [][]string, options ...UnWatchOHLCVForSymbolsOptions) (any, error) {
	return this.exchangeTyped.UnWatchOHLCVForSymbols(symbolsAndTimeframes, options...)
}
func (this *Bitfinex) UnWatchOrderBook(symbol string, options ...UnWatchOrderBookOptions) (any, error) {
	return this.exchangeTyped.UnWatchOrderBook(symbol, options...)
}
func (this *Bitfinex) UnWatchOrderBookForSymbols(symbols []string, options ...UnWatchOrderBookForSymbolsOptions) (any, error) {
	return this.exchangeTyped.UnWatchOrderBookForSymbols(symbols, options...)
}
func (this *Bitfinex) UnWatchOrders(options ...UnWatchOrdersOptions) (any, error) {
	return this.exchangeTyped.UnWatchOrders(options...)
}
func (this *Bitfinex) UnWatchTicker(symbol string, options ...UnWatchTickerOptions) (any, error) {
	return this.exchangeTyped.UnWatchTicker(symbol, options...)
}
func (this *Bitfinex) UnWatchTickers(options ...UnWatchTickersOptions) (any, error) {
	return this.exchangeTyped.UnWatchTickers(options...)
}
func (this *Bitfinex) UnWatchTrades(symbol string, options ...UnWatchTradesOptions) (any, error) {
	return this.exchangeTyped.UnWatchTrades(symbol, options...)
}
func (this *Bitfinex) UnWatchTradesForSymbols(symbols []string, options ...UnWatchTradesForSymbolsOptions) (any, error) {
	return this.exchangeTyped.UnWatchTradesForSymbols(symbols, options...)
}
func (this *Bitfinex) WatchBalance(params ...any) (Balances, error) {
	return this.exchangeTyped.WatchBalance(params...)
}
func (this *Bitfinex) WatchBidsAsks(options ...WatchBidsAsksOptions) (Tickers, error) {
	return this.exchangeTyped.WatchBidsAsks(options...)
}
func (this *Bitfinex) WatchLiquidations(symbol string, options ...WatchLiquidationsOptions) ([]Liquidation, error) {
	return this.exchangeTyped.WatchLiquidations(symbol, options...)
}
func (this *Bitfinex) WatchMarkPrice(symbol string, options ...WatchMarkPriceOptions) (Ticker, error) {
	return this.exchangeTyped.WatchMarkPrice(symbol, options...)
}
func (this *Bitfinex) WatchMarkPrices(options ...WatchMarkPricesOptions) (Tickers, error) {
	return this.exchangeTyped.WatchMarkPrices(options...)
}
func (this *Bitfinex) WatchMyLiquidations(symbol string, options ...WatchMyLiquidationsOptions) ([]Liquidation, error) {
	return this.exchangeTyped.WatchMyLiquidations(symbol, options...)
}
func (this *Bitfinex) WatchMyLiquidationsForSymbols(symbols []string, options ...WatchMyLiquidationsForSymbolsOptions) ([]Liquidation, error) {
	return this.exchangeTyped.WatchMyLiquidationsForSymbols(symbols, options...)
}
func (this *Bitfinex) WatchMyTrades(options ...WatchMyTradesOptions) ([]Trade, error) {
	return this.exchangeTyped.WatchMyTrades(options...)
}
func (this *Bitfinex) WatchOHLCV(symbol string, options ...WatchOHLCVOptions) ([]OHLCV, error) {
	return this.exchangeTyped.WatchOHLCV(symbol, options...)
}
func (this *Bitfinex) WatchOHLCVForSymbols(symbolsAndTimeframes [][]string, options ...WatchOHLCVForSymbolsOptions) (map[string]map[string][]OHLCV, error) {
	return this.exchangeTyped.WatchOHLCVForSymbols(symbolsAndTimeframes, options...)
}
func (this *Bitfinex) WatchOrderBook(symbol string, options ...WatchOrderBookOptions) (OrderBook, error) {
	return this.exchangeTyped.WatchOrderBook(symbol, options...)
}
func (this *Bitfinex) WatchOrderBookForSymbols(symbols []string, options ...WatchOrderBookForSymbolsOptions) (OrderBook, error) {
	return this.exchangeTyped.WatchOrderBookForSymbols(symbols, options...)
}
func (this *Bitfinex) WatchOrders(options ...WatchOrdersOptions) ([]Order, error) {
	return this.exchangeTyped.WatchOrders(options...)
}
func (this *Bitfinex) WatchOrdersForSymbols(symbols []string, options ...WatchOrdersForSymbolsOptions) ([]Order, error) {
	return this.exchangeTyped.WatchOrdersForSymbols(symbols, options...)
}
func (this *Bitfinex) WatchPosition(options ...WatchPositionOptions) (Position, error) {
	return this.exchangeTyped.WatchPosition(options...)
}
func (this *Bitfinex) WatchPositions(options ...WatchPositionsOptions) ([]Position, error) {
	return this.exchangeTyped.WatchPositions(options...)
}
func (this *Bitfinex) WatchTicker(symbol string, options ...WatchTickerOptions) (Ticker, error) {
	return this.exchangeTyped.WatchTicker(symbol, options...)
}
func (this *Bitfinex) WatchTickers(options ...WatchTickersOptions) (Tickers, error) {
	return this.exchangeTyped.WatchTickers(options...)
}
func (this *Bitfinex) WatchTrades(symbol string, options ...WatchTradesOptions) ([]Trade, error) {
	return this.exchangeTyped.WatchTrades(symbol, options...)
}
func (this *Bitfinex) WatchTradesForSymbols(symbols []string, options ...WatchTradesForSymbolsOptions) ([]Trade, error) {
	return this.exchangeTyped.WatchTradesForSymbols(symbols, options...)
}
func (this *Bitfinex) WithdrawWs(code string, amount float64, address string, options ...WithdrawWsOptions) (Transaction, error) {
	return this.exchangeTyped.WithdrawWs(code, amount, address, options...)
}
