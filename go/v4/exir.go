package ccxt

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

type exir struct {
    Exchange

}

func NewExirCore() *exir {
    p := &exir{}
    setDefaults(p)
    return p
}

func  (this *exir) Describe() interface{}  {
    return this.DeepExtend(this.Exchange.Describe(), map[string]interface{} {
        "id": "exir",
        "name": "Exir",
        "countries": []interface{}{"IR"},
        "rateLimit": 1000,
        "version": "1",
        "certified": false,
        "pro": false,
        "has": map[string]interface{} {
            "CORS": nil,
            "spot": true,
            "margin": false,
            "swap": false,
            "future": false,
            "option": false,
            "addMargin": false,
            "cancelAllOrders": false,
            "cancelOrder": false,
            "cancelOrders": false,
            "createDepositAddress": false,
            "createOrder": false,
            "createStopLimitOrder": false,
            "createStopMarketOrder": false,
            "createStopOrder": false,
            "editOrder": false,
            "fetchBalance": false,
            "fetchBorrowInterest": false,
            "fetchBorrowRateHistories": false,
            "fetchBorrowRateHistory": false,
            "fetchClosedOrders": false,
            "fetchCrossBorrowRate": false,
            "fetchCrossBorrowRates": false,
            "fetchCurrencies": false,
            "fetchDepositAddress": false,
            "fetchDeposits": false,
            "fetchFundingHistory": false,
            "fetchFundingRate": false,
            "fetchFundingRateHistory": false,
            "fetchFundingRates": false,
            "fetchIndexOHLCV": false,
            "fetchIsolatedBorrowRate": false,
            "fetchIsolatedBorrowRates": false,
            "fetchL2OrderBook": false,
            "fetchLedger": false,
            "fetchLedgerEntry": false,
            "fetchLeverageTiers": false,
            "fetchMarkets": true,
            "fetchMarkOHLCV": false,
            "fetchMyTrades": false,
            "fetchOHLCV": true,
            "fetchOpenInterestHistory": false,
            "fetchOpenOrders": false,
            "fetchOrder": false,
            "fetchOrderBook": true,
            "fetchOrders": false,
            "fetchOrderTrades": "emulated",
            "fetchPositions": false,
            "fetchPremiumIndexOHLCV": false,
            "fetchTicker": true,
            "fetchTickers": true,
            "fetchTime": false,
            "fetchTrades": false,
            "fetchTradingFee": false,
            "fetchTradingFees": false,
            "fetchWithdrawals": false,
            "setLeverage": false,
            "setMarginMode": false,
            "transfer": false,
            "withdraw": false,
        },
        "comment": "This comment is optional",
        "urls": map[string]interface{} {
            "logo": "https://cdn.arz.digital/cr-odin/img/exchanges/exir/64x64.png",
            "api": map[string]interface{} {
                "public": "https://api.exir.io",
            },
            "www": "https://www.exir.io/",
            "doc": []interface{}{"https://apidocs.exir.io"},
        },
        "timeframes": map[string]interface{} {
            "15m": "15",
            "1h": "60",
            "4h": "240",
            "1d": "1D",
            "1w": "1W",
        },
        "api": map[string]interface{} {
            "public": map[string]interface{} {
                "get": map[string]interface{} {
                    "v2/tickers": 1,
                    "v2/ticker": 1,
                    "v2/chart": 1,
                    "v2/orderbook": 1,
                },
            },
        },
        "fees": map[string]interface{} {
            "trading": map[string]interface{} {
                "tierBased": false,
                "percentage": true,
                "maker": this.ParseNumber("0.001"),
                "taker": this.ParseNumber("0.001"),
            },
        },
    })
}
func  (this *exir) FetchMarkets(optionalArgs ...interface{}) <- chan interface{} {
            ch := make(chan interface{})
            go func() interface{} {
                defer close(ch)
                defer ReturnPanicError(ch)
                    /**
            * @method
            * @name exir#fetchMarkets
            * @description retrieves data on all markets for exir
            * @see https://apidocs.exir.io/#tickers
            * @param {object} [params] extra parameters specific to the exchange API endpoint
            * @returns {object[]} an array of objects representing market data
            */
            params := GetArg(optionalArgs, 0, map[string]interface{} {})
            _ = params
        
            response:= (<-this.PublicGetV2Tickers())
            PanicOnError(response)
            var marketKeys interface{} = ObjectKeys(response)
            var result interface{} = []interface{}{}
            for i := 0; IsLessThan(i, GetArrayLength(marketKeys)); i++ {
                var symbol interface{} = GetValue(marketKeys, i)
                AddElementToObject(GetValue(response, symbol), "symbol", symbol)
                var market interface{} = this.ParseMarket(GetValue(response, symbol))
                AppendToArray(&result, market)
            }
        
            ch <- result
            return nil
        
            }()
            return ch
        }
func  (this *exir) ParseMarket(market interface{}) interface{}  {
    //        {
    // symbol: btc-usdt
    // isClosed: false,
    // bestSell: "39659550020",
    // bestBuy: "39650000000",
    // volumeSrc: "11.6924501388",
    // volumeDst: "464510376461.05263193275",
    // latest: "39659550020",
    // mark: "39817678220",
    // dayLow: "38539978000",
    // dayHigh: "40809999990",
    // dayOpen: "38553149810",
    // dayClose: "39659550020",
    // dayChange: "2.87"
    // },
    var symbol interface{} = this.SafeValue(market, "symbol")
    var id interface{} = symbol
    baseIdquoteIdVariable := Split(symbol, "-");
    baseId := GetValue(baseIdquoteIdVariable,0);
    quoteId := GetValue(baseIdquoteIdVariable,1)
    var base interface{} = this.SafeCurrencyCode(baseId)
    var quote interface{} = this.SafeCurrencyCode(quoteId)
    baseId = ToLower(baseId)
    quoteId = ToLower(quoteId)
    return map[string]interface{} {
        "id": id,
        "symbol": Add(Add(base, "/"), quote),
        "base": base,
        "quote": quote,
        "settle": nil,
        "baseId": baseId,
        "quoteId": quoteId,
        "settleId": nil,
        "type": "spot",
        "spot": true,
        "margin": false,
        "swap": false,
        "future": false,
        "option": false,
        "active": true,
        "contract": false,
        "linear": nil,
        "inverse": nil,
        "contractSize": nil,
        "expiry": nil,
        "expiryDatetime": nil,
        "strike": nil,
        "optionType": nil,
        "precision": map[string]interface{} {
            "amount": nil,
            "price": nil,
        },
        "limits": map[string]interface{} {
            "leverage": map[string]interface{} {
                "min": nil,
                "max": nil,
            },
            "amount": map[string]interface{} {
                "min": nil,
                "max": nil,
            },
            "price": map[string]interface{} {
                "min": nil,
                "max": nil,
            },
            "cost": map[string]interface{} {
                "min": nil,
                "max": nil,
            },
        },
        "created": nil,
        "info": market,
    }
}
func  (this *exir) FetchTickers(optionalArgs ...interface{}) <- chan interface{} {
            ch := make(chan interface{})
            go func() interface{} {
                defer close(ch)
                defer ReturnPanicError(ch)
                    /**
            * @method
            * @name exir#fetchTickers
            * @description fetches price tickers for multiple markets, statistical information calculated over the past 24 hours for each market
            * @see https://apidocs.exir.io/#tickers
            * @param {string[]|undefined} symbols unified symbols of the markets to fetch the ticker for, all market tickers are returned if not assigned
            * @param {object} [params] extra parameters specific to the exchange API endpoint
            * @returns {object} a dictionary of [ticker structures]{@link https://docs.ccxt.com/#/?id=ticker-structure}
            */
            symbols := GetArg(optionalArgs, 0, nil)
            _ = symbols
            params := GetArg(optionalArgs, 1, map[string]interface{} {})
            _ = params
        
            retRes2298 := (<-this.LoadMarkets())
            PanicOnError(retRes2298)
            if IsTrue(!IsEqual(symbols, nil)) {
                symbols = this.MarketSymbols(symbols)
            }
        
            response:= (<-this.PublicGetV2Tickers())
            PanicOnError(response)
            var marketKeys interface{} = ObjectKeys(response)
            var result interface{} = []interface{}{}
            for i := 0; IsLessThan(i, GetArrayLength(marketKeys)); i++ {
                var symbol interface{} = GetValue(marketKeys, i)
                AddElementToObject(GetValue(response, symbol), "symbol", symbol)
                var ticker interface{} = this.ParseTicker(GetValue(response, symbol))
                symbol = GetValue(ticker, "symbol")
                AddElementToObject(result, symbol, ticker)
            }
        
            ch <- this.FilterByArrayTickers(result, "symbol", symbols)
            return nil
        
            }()
            return ch
        }
func  (this *exir) FetchTicker(symbol interface{}, optionalArgs ...interface{}) <- chan interface{} {
            ch := make(chan interface{})
            go func() interface{} {
                defer close(ch)
                defer ReturnPanicError(ch)
                    /**
            * @method
            * @name exir#fetchTicker
            * @description fetches a price ticker, a statistical calculation with the information calculated over the past 24 hours for a specific market
            * @see https://apidocs.exir.io/#ticker
            * @param {string} symbol unified symbol of the market to fetch the ticker for
            * @param {object} [params] extra parameters specific to the exchange API endpoint
            * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/#/?id=ticker-structure}
            */
            params := GetArg(optionalArgs, 0, map[string]interface{} {})
            _ = params
        
            retRes2568 := (<-this.LoadMarkets())
            PanicOnError(retRes2568)
            var market interface{} = this.Market(symbol)
            var request interface{} = map[string]interface{} {
                "symbol": GetValue(market, "id"),
            }
        
            response:= (<-this.PublicGetV2Ticker(request))
            PanicOnError(response)
            AddElementToObject(response, "symbol", GetValue(market, "id"))
            AddElementToObject(response, "time", GetValue(response, "timestamp"))
            var ticker interface{} = this.ParseTicker(response)
        
            ch <- ticker
            return nil
        
            }()
            return ch
        }
func  (this *exir) ParseTicker(ticker interface{}, optionalArgs ...interface{}) interface{}  {
    //  {
    //     'time': '2024-05-26T08:40:02.305Z',
    //     'open': 26125,
    //     'close': 26298,
    //     'high': 26939,
    //     'low': 25791,
    //     'last': 26298,
    //     'volume': 32167,
    //     'symbol': 'ada-irt',
    // },
    market := GetArg(optionalArgs, 0, nil)
    _ = market
    var marketType interface{} = "spot"
    var symbol interface{} = this.SafeValue(ticker, "symbol")
    var marketId interface{} = symbol
    symbol = this.SafeSymbol(marketId, market, nil, marketType)
    var high interface{} = this.SafeFloat(ticker, "high")
    var low interface{} = this.SafeFloat(ticker, "low")
    var bid interface{} = this.SafeFloat(ticker, "last")
    var ask interface{} = this.SafeFloat(ticker, "last")
    var open interface{} = this.SafeFloat(ticker, "open")
    var close interface{} = this.SafeFloat(ticker, "close")
    var last interface{} = this.SafeFloat(ticker, "last")
    var quoteVolume interface{} = this.SafeFloat(ticker, "volume")
    var datetime interface{} = this.SafeString(ticker, "time")
    return this.SafeTicker(map[string]interface{} {
        "symbol": symbol,
        "timestamp": this.Parse8601(datetime),
        "datetime": datetime,
        "high": high,
        "low": low,
        "bid": this.SafeFloat(bid, 0),
        "bidVolume": nil,
        "ask": this.SafeFloat(ask, 0),
        "askVolume": nil,
        "vwap": nil,
        "open": open,
        "close": close,
        "last": last,
        "previousClose": nil,
        "change": nil,
        "percentage": nil,
        "average": nil,
        "baseVolume": nil,
        "quoteVolume": quoteVolume,
        "info": ticker,
    }, market)
}
func  (this *exir) FetchOHLCV(symbol interface{}, optionalArgs ...interface{}) <- chan interface{} {
            ch := make(chan interface{})
            go func() interface{} {
                defer close(ch)
                defer ReturnPanicError(ch)
                    /**
            * @method
            * @name exir#fetchOHLCV
            * @description fetches historical candlestick data containing the open, high, low, and close price, and the volume of a market
            * @see https://apidocs.exir.io/#chart
            * @param {string} symbol unified symbol of the market to fetch OHLCV data for
            * @param {string} timeframe the length of time each candle represents
            * @param {int} [since] timestamp in ms of the earliest candle to fetch
            * @param {int} [limit] the maximum amount of candles to fetch
            * @param {object} [params] extra parameters specific to the exchange API endpoint
            * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
            */
            timeframe := GetArg(optionalArgs, 0, "1h")
            _ = timeframe
            since := GetArg(optionalArgs, 1, nil)
            _ = since
            limit := GetArg(optionalArgs, 2, nil)
            _ = limit
            params := GetArg(optionalArgs, 3, map[string]interface{} {})
            _ = params
        
            retRes3298 := (<-this.LoadMarkets())
            PanicOnError(retRes3298)
            var market interface{} = this.Market(symbol)
            var endTime interface{} = DateNow()
            var request interface{} = map[string]interface{} {
                "symbol": GetValue(market, "id"),
                "from": Subtract((Divide(endTime, 1000)), (Multiply(Multiply(24, 60), 60))),
                "to": Divide(endTime, 1000),
                "resolution": this.SafeString(this.Timeframes, timeframe, timeframe),
            }
            if IsTrue(!IsEqual(since, nil)) {
                AddElementToObject(request, "from", Divide(since, 1000))
            }
            AddElementToObject(request, "from", this.SafeInteger(request, "from"))
            AddElementToObject(request, "to", this.SafeInteger(request, "to"))
            if IsTrue(!IsEqual(timeframe, nil)) {
                AddElementToObject(request, "resolution", this.SafeString(this.Timeframes, timeframe, timeframe))
            }
        
            response:= (<-this.PublicGetV2Chart(request))
            PanicOnError(response)
            var ohlcvs interface{} = []interface{}{}
            for i := 0; IsLessThan(i, GetArrayLength(response)); i++ {
                var candle interface{} = GetValue(response, i)
                var ts interface{} = this.SafeTimestamp(candle, "time")
                var open interface{} = this.SafeFloat(candle, "open")
                var high interface{} = this.SafeFloat(candle, "high")
                var low interface{} = this.SafeFloat(candle, "low")
                var close interface{} = this.SafeFloat(candle, "close")
                var volume interface{} = this.SafeFloat(candle, "volume")
                AppendToArray(&ohlcvs, []interface{}{ts, open, high, low, close, volume})
            }
        
            ch <- this.ParseOHLCVs(ohlcvs, market, timeframe, since, limit)
            return nil
        
            }()
            return ch
        }
func  (this *exir) FetchOrderBook(symbol interface{}, optionalArgs ...interface{}) <- chan interface{} {
            ch := make(chan interface{})
            go func() interface{} {
                defer close(ch)
                defer ReturnPanicError(ch)
                    /**
            * @method
            * @name exir#fetchOrderBooks
            * @description fetches information on open orders with bid (buy) and ask (sell) prices, volumes and other data for multiple markets
            * @see https://apidocs.exir.io/#orderbook
            * @param {string[]|undefined} symbols list of unified market symbols, all symbols fetched if undefined, default is undefined
            * @param {int} [limit] max number of entries per orderbook to return, default is undefined
            * @param {object} [params] extra parameters specific to the exchange API endpoint
            * @returns {object} a dictionary of [order book structures]{@link https://docs.ccxt.com/#/?id=order-book-structure} indexed by market symbol
            */
            limit := GetArg(optionalArgs, 0, nil)
            _ = limit
            params := GetArg(optionalArgs, 1, map[string]interface{} {})
            _ = params
        
            retRes3798 := (<-this.LoadMarkets())
            PanicOnError(retRes3798)
            var market interface{} = this.Market(symbol)
            var request interface{} = map[string]interface{} {
                "symbol": GetValue(market, "id"),
            }
        
            response:= (<-this.PublicGetV2Orderbook(request))
            PanicOnError(response)
            var timestamp interface{} = Divide(this.SafeTimestamp(GetValue(response, GetValue(market, "id")), "timestamp"), 1000)
        
            ch <- this.ParseOrderBook(GetValue(response, GetValue(market, "id")), symbol, timestamp)
            return nil
        
            }()
            return ch
        }
func  (this *exir) Sign(path interface{}, optionalArgs ...interface{}) interface{}  {
    api := GetArg(optionalArgs, 0, "public")
    _ = api
    method := GetArg(optionalArgs, 1, "GET")
    _ = method
    params := GetArg(optionalArgs, 2, map[string]interface{} {})
    _ = params
    headers := GetArg(optionalArgs, 3, nil)
    _ = headers
    body := GetArg(optionalArgs, 4, nil)
    _ = body
    var query interface{} = this.Omit(params, this.ExtractParams(path))
    var url interface{} = Add(Add(GetValue(GetValue(this.Urls, "api"), "public"), "/"), path)
    if IsTrue(IsEqual(path, "v2/ticker")) {
        url = Add(Add(Add(Add(GetValue(GetValue(this.Urls, "api"), "public"), "/"), path), "?"), this.Urlencode(query))
    }
    if IsTrue(IsEqual(path, "v2/chart")) {
        url = Add(Add(url, "?"), this.Urlencode(query))
    }
    if IsTrue(IsEqual(path, "v2/orderbook")) {
        url = Add(Add(url, "?"), this.Urlencode(query))
    }
    headers = map[string]interface{} {
        "Content-Type": "application/json",
    }
    return map[string]interface{} {
        "url": url,
        "method": method,
        "body": body,
        "headers": headers,
    }
}


func (this *exir) Init(userConfig map[string]interface{}) {
    this.Exchange = Exchange{}
    this.Exchange.DerivedExchange = this
    this.Exchange.InitParent(userConfig, this.Describe().(map[string]interface{}), this)
}
