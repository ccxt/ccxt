package ccxtpro

import ccxt "github.com/ccxt/ccxt/go/v4"

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

import "strconv"
import "strings"

type Bittrade struct {
	*ccxt.Bittrade
	base *ccxt.Bittrade
}

func newBittrade() *Bittrade {
	p := &Bittrade{}
	base := &ccxt.Bittrade{}
	p.base = base
	p.Bittrade = base
	ccxt.SetDefaults(p)
	return p
}

func (this *Bittrade) Describe() any {
	return this.DeepExtend(this.base.Describe(), map[string]any{
		"has": map[string]any{
			"ws":                    true,
			"watchOrderBook":        true,
			"watchTickers":          false,
			"watchTicker":           true,
			"watchTrades":           true,
			"watchTradesForSymbols": false,
			"watchBalance":          false,
			"watchOHLCV":            true,
		},
		"urls": map[string]any{
			"api": map[string]any{
				"ws": map[string]any{
					"api": map[string]any{
						"public":  "wss://{hostname}/ws",
						"private": "wss://{hostname}/ws/v2",
					},
				},
			},
		},
		"options": map[string]any{
			"tradesLimit": 1000,
			"OHLCVLimit":  1000,
			"api":         "api",
			"ws": map[string]any{
				"gunzip": true,
			},
		},
	})
}
func (this *Bittrade) RequestId() string {
	this.LockId()
	var requestId int64 = this.Sum(this.SafeInteger(this.Options, "requestId", 0), 1).(int64)
	this.Options.Store("requestId", requestId)
	this.UnlockId()
	return strconv.FormatInt(requestId, 10)
}

/**
 * @method
 * @name bittrade#watchTicker
 * @description watches a price ticker, a statistical calculation with the information calculated over the past 24 hours for a specific market
 * @param {string} symbol unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
func (this *Bittrade) WatchTickerAsync(symbol string, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.watchTickerBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Bittrade) watchTickerBody(ch chan any, symbol string, optionalArgs ...any) any {
	defer close(ch)
	defer ccxt.ReturnPanicError(ch)
	var params map[string]any = ccxt.GetArgMap(optionalArgs, 0, map[string]any{})
	_ = params
	if this.Markets == nil {

		ccxt.PanicOnError((<-this.LoadMarketsAsync()))
	}
	var market map[string]any = this.Market(symbol)
	var symbolValue *string = ccxt.SafeStringPtr(market["symbol"])
	// only supports a limit of 150 at this time
	var messageHash *string = ccxt.SafeStringPtr(ccxt.Add(ccxt.Add("market.", market["id"]), ".detail"))
	var api *string = this.SafeString(this.Options, "api", "api")
	var hostname map[string]any = map[string]any{
		"hostname": this.Hostname,
	}
	var url string = this.ImplodeParams(ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(this.Urls, "api"), "ws"), api), "public"), hostname)
	var requestId string = this.RequestId()
	var request map[string]any = map[string]any{
		"sub": messageHash,
		"id":  requestId,
	}
	var subscription map[string]any = map[string]any{
		"id":          requestId,
		"messageHash": messageHash,
		"symbol":      symbolValue,
		"params":      params,
	}

	ch <- ccxt.PanicOnError((<-this.Watch(url, messageHash, this.Extend(request, params), messageHash, subscription)))
	return nil
}
func (this *Bittrade) HandleTicker(client any, message map[string]any) any {
	//
	//     {
	//         "ch": "market.btcusdt.detail",
	//         "ts": 1583494163784,
	//         "tick": {
	//             "id": 209988464418,
	//             "low": 8988,
	//             "high": 9155.41,
	//             "open": 9078.91,
	//             "close": 9136.46,
	//             "vol": 237813910.5928412,
	//             "amount": 26184.202558551195,
	//             "version": 209988464418,
	//             "count": 265673
	//         }
	//     }
	//
	var tick map[string]any = ccxt.MapTyped(this.SafeDict(message, "tick", map[string]any{}))
	var ch *string = this.SafeString(message, "ch")
	if ch == nil {
		return message
	}
	var parts []string = strings.Split(*ch, ".")
	var marketId *string = this.SafeString(parts, 1)
	var market map[string]any = this.SafeMarket(marketId)
	var ticker map[string]any = ccxt.MapTyped(this.ParseTicker(tick, market))
	var timestamp *int64 = this.SafeInteger(message, "ts")
	ticker["timestamp"] = timestamp
	ticker["datetime"] = this.Iso8601(timestamp)
	var symbol *string = ccxt.SafeStringPtr(ticker["symbol"])
	ccxt.AddElementToObject(this.Tickers, symbol, ticker)
	client.(ccxt.ClientInterface).Resolve(ticker, ch)
	return message
}

/**
 * @method
 * @name bittrade#watchTrades
 * @description get the list of most recent trades for a particular symbol
 * @param {string} symbol unified symbol of the market to fetch trades for
 * @param {int} [since] timestamp in ms of the earliest trade to fetch
 * @param {int} [limit] the maximum amount of trades to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=public-trades}
 */
func (this *Bittrade) WatchTradesAsync(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.watchTradesBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Bittrade) watchTradesBody(ch chan any, symbol any, optionalArgs ...any) any {
	defer close(ch)
	defer ccxt.ReturnPanicError(ch)
	var since *int64 = ccxt.GetArgInt64Ptr(optionalArgs, 0, nil)
	_ = since
	var limit *int64 = ccxt.GetArgInt64Ptr(optionalArgs, 1, nil)
	_ = limit
	var params map[string]any = ccxt.GetArgMap(optionalArgs, 2, map[string]any{})
	_ = params
	if this.Markets == nil {

		ccxt.PanicOnError((<-this.LoadMarketsAsync()))
	}
	var market map[string]any = this.Market(symbol)
	var symbolValue *string = ccxt.SafeStringPtr(market["symbol"])
	// only supports a limit of 150 at this time
	var messageHash *string = ccxt.SafeStringPtr(ccxt.Add(ccxt.Add("market.", market["id"]), ".trade.detail"))
	var api *string = this.SafeString(this.Options, "api", "api")
	var hostname map[string]any = map[string]any{
		"hostname": this.Hostname,
	}
	var url string = this.ImplodeParams(ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(this.Urls, "api"), "ws"), api), "public"), hostname)
	var requestId string = this.RequestId()
	var request map[string]any = map[string]any{
		"sub": messageHash,
		"id":  requestId,
	}
	var subscription map[string]any = map[string]any{
		"id":          requestId,
		"messageHash": messageHash,
		"symbol":      symbolValue,
		"params":      params,
	}

	var trades ccxt.ArrayCacheInterface = ccxt.AsArrayCache(ccxt.PanicOnError((<-this.Watch(url, messageHash, this.Extend(request, params), messageHash, subscription))))
	var limitResolved *int64 = limit
	if this.NewUpdates {
		limitResolved = ccxt.ToGetsLimit(trades).GetLimit(symbolValue, limit)
	}

	ch <- this.FilterBySinceLimit(trades, since, limitResolved, "timestamp", true)
	return nil
}
func (this *Bittrade) HandleTrades(client any, message map[string]any) any {
	//
	//     {
	//         "ch": "market.btcusdt.trade.detail",
	//         "ts": 1583495834011,
	//         "tick": {
	//             "id": 105004645372,
	//             "ts": 1583495833751,
	//             "data": [
	//                 {
	//                     "id": 1.050046453727319e+22,
	//                     "ts": 1583495833751,
	//                     "tradeId": 102090727790,
	//                     "amount": 0.003893,
	//                     "price": 9150.01,
	//                     "direction": "sell"
	//                 }
	//             ]
	//         }
	//     }
	//
	var tick map[string]any = ccxt.SafeMapTyped(message, "tick")
	var data []any = ccxt.SafeListTyped(tick, "data")
	var ch *string = this.SafeString(message, "ch")
	if ch == nil {
		return message
	}
	var parts []string = strings.Split(*ch, ".")
	var marketId *string = this.SafeString(parts, 1)
	var market map[string]any = this.SafeMarket(marketId)
	var symbol *string = ccxt.SafeStringPtr(market["symbol"])
	var tradesCache any = this.SafeValue(this.Trades, symbol)
	if ccxt.IsEqual(tradesCache, nil) {
		var limit *int64 = this.SafeInteger(this.Options, "tradesLimit", 1000)
		tradesCache = ccxt.NewArrayCache(limit)
		ccxt.AddElementToObject(this.Trades, symbol, tradesCache)
	}
	for i := 0; i < len(data); i++ {
		var trade map[string]any = ccxt.MapTyped(this.ParseTrade(func() any {
			if i >= 0 && i < len(data) {
				return ccxt.DerefScalar(data[i])
			}
			return nil
		}(), market))
		tradesCache.(ccxt.Appender).Append(trade)
	}
	client.(ccxt.ClientInterface).Resolve(tradesCache, ch)
	return message
}

/**
 * @method
 * @name bittrade#watchOHLCV
 * @description watches historical candlestick data containing the open, high, low, and close price, and the volume of a market
 * @param {string} symbol unified symbol of the market to fetch ccxt.OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum amount of candles to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 */
func (this *Bittrade) WatchOHLCVAsync(symbol string, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.watchOHLCVBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Bittrade) watchOHLCVBody(ch chan any, symbol string, optionalArgs ...any) any {
	defer close(ch)
	defer ccxt.ReturnPanicError(ch)
	var timeframe string = ccxt.GetArgString(optionalArgs, 0, "1m")
	_ = timeframe
	var since *int64 = ccxt.GetArgInt64Ptr(optionalArgs, 1, nil)
	_ = since
	var limit *int64 = ccxt.GetArgInt64Ptr(optionalArgs, 2, nil)
	_ = limit
	var params map[string]any = ccxt.GetArgMap(optionalArgs, 3, map[string]any{})
	_ = params
	if this.Markets == nil {

		ccxt.PanicOnError((<-this.LoadMarketsAsync()))
	}
	var market map[string]any = this.Market(symbol)
	var symbolValue *string = ccxt.SafeStringPtr(market["symbol"])
	var interval *string = this.SafeString(this.Timeframes, timeframe, timeframe)
	var messageHash *string = ccxt.SafeStringPtr(ccxt.Add(ccxt.Add(ccxt.Add("market.", market["id"]), ".kline."), interval))
	var api *string = this.SafeString(this.Options, "api", "api")
	var hostname map[string]any = map[string]any{
		"hostname": this.Hostname,
	}
	var url string = this.ImplodeParams(ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(this.Urls, "api"), "ws"), api), "public"), hostname)
	var requestId string = this.RequestId()
	var request map[string]any = map[string]any{
		"sub": messageHash,
		"id":  requestId,
	}
	var subscription map[string]any = map[string]any{
		"id":          requestId,
		"messageHash": messageHash,
		"symbol":      symbolValue,
		"timeframe":   timeframe,
		"params":      params,
	}

	var ohlcv ccxt.ArrayCacheInterface = ccxt.AsArrayCache(ccxt.PanicOnError((<-this.Watch(url, messageHash, this.Extend(request, params), messageHash, subscription))))
	var limitResolved *int64 = limit
	if this.NewUpdates {
		limitResolved = ccxt.ToGetsLimit(ohlcv).GetLimit(symbolValue, limit)
	}

	ch <- this.FilterBySinceLimit(ohlcv, since, limitResolved, 0, true)
	return nil
}
func (this *Bittrade) HandleOHLCV(client any, message map[string]any) {
	//
	//     {
	//         "ch": "market.btcusdt.kline.1min",
	//         "ts": 1583501786794,
	//         "tick": {
	//             "id": 1583501760,
	//             "open": 9094.5,
	//             "close": 9094.51,
	//             "low": 9094.5,
	//             "high": 9094.51,
	//             "amount": 0.44639786263800907,
	//             "vol": 4059.76919054,
	//             "count": 16
	//         }
	//     }
	//
	var ch *string = this.SafeString(message, "ch")
	if ch == nil {
		return
	}
	var parts []string = strings.Split(*ch, ".")
	var marketId *string = this.SafeString(parts, 1)
	var market map[string]any = this.SafeMarket(marketId)
	var symbol *string = ccxt.SafeStringPtr(market["symbol"])
	var interval *string = this.SafeString(parts, 3)
	var timeframe *string = this.FindTimeframe(interval)
	ccxt.AddElementToObject(this.Ohlcvs, symbol, this.SafeDict(this.Ohlcvs, symbol, map[string]any{}))
	var stored any = this.SafeValue(ccxt.GetValue(this.Ohlcvs, symbol), timeframe)
	if ccxt.IsEqual(stored, nil) {
		var limit *int64 = this.SafeInteger(this.Options, "OHLCVLimit", 1000)
		stored = ccxt.NewArrayCacheByTimestamp(limit)
		ccxt.AddElementToObject(ccxt.GetValue(this.Ohlcvs, symbol), timeframe, stored)
	}
	var tick map[string]any = ccxt.SafeMapTyped(message, "tick")
	var parsed any = this.ParseOHLCV(tick, market)
	stored.(ccxt.Appender).Append(parsed)
	client.(ccxt.ClientInterface).Resolve(stored, ch)
}

/**
 * @method
 * @name bittrade#watchOrderBook
 * @description watches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
 * @param {string} symbol unified symbol of the market to fetch the order book for
 * @param {int} [limit] the maximum amount of order book entries to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order book structure]{@link https://docs.ccxt.com/?id=order-book-structure}
 */
func (this *Bittrade) WatchOrderBookAsync(symbol string, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.watchOrderBookBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Bittrade) watchOrderBookBody(ch chan any, symbol string, optionalArgs ...any) any {
	defer close(ch)
	defer ccxt.ReturnPanicError(ch)
	limit := ccxt.GetArg(optionalArgs, 0, nil)
	_ = limit
	var params map[string]any = ccxt.GetArgMap(optionalArgs, 1, map[string]any{})
	_ = params
	if (limit != nil) && (!ccxt.IsEqual(limit, 150)) {
		panic(ccxt.ExchangeError(this.Id + " watchOrderBook accepts limit = 150 only"))
	}
	if this.Markets == nil {

		ccxt.PanicOnError((<-this.LoadMarketsAsync()))
	}
	var market map[string]any = this.Market(symbol)
	var symbolValue *string = ccxt.SafeStringPtr(market["symbol"])
	// only supports a limit of 150 at this time
	var limitValue any = func() any {
		if limit == nil {
			return 150
		}
		return limit
	}()
	var messageHash *string = ccxt.SafeStringPtr(ccxt.Add(ccxt.Add(ccxt.Add("market.", market["id"]), ".mbp."), ccxt.ToString(limitValue)))
	var api *string = this.SafeString(this.Options, "api", "api")
	var hostname map[string]any = map[string]any{
		"hostname": this.Hostname,
	}
	var url string = this.ImplodeParams(ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(this.Urls, "api"), "ws"), api), "public"), hostname)
	var requestId string = this.RequestId()
	var request map[string]any = map[string]any{
		"sub": messageHash,
		"id":  requestId,
	}
	var subscription map[string]any = map[string]any{
		"id":          requestId,
		"messageHash": messageHash,
		"symbol":      symbolValue,
		"limit":       limitValue,
		"params":      params,
		"method":      this.HandleOrderBookSubscription,
	}

	var orderbook ccxt.OrderBookInterface = ccxt.PanicOnError((<-this.Watch(url, messageHash, this.Extend(request, params), messageHash, subscription))).(ccxt.OrderBookInterface)

	ch <- orderbook.(ccxt.OrderBookInterface).Limit()
	return nil
}
func (this *Bittrade) HandleOrderBookSnapshot(client any, message map[string]any, subscription map[string]any) {
	//
	//     {
	//         "id": 1583473663565,
	//         "rep": "market.btcusdt.mbp.150",
	//         "ts": 1774979531056,
	//         "status": "ok",
	//         "data": {
	//             "seqNum": 104999417756,
	//             "bids": [
	//                 [9058.27, 0],
	//                 [9058.43, 0],
	//                 [9058.99, 0],
	//             ],
	//             "asks": [
	//                 [9084.27, 0.2],
	//                 [9085.69, 0],
	//                 [9085.81, 0],
	//             ]
	//         }
	//     }
	//
	var symbol *string = this.SafeString(subscription, "symbol")
	var messageHash *string = this.SafeString(subscription, "messageHash")
	var timestamp *int64 = this.SafeInteger(message, "ts")
	var orderbook any = ccxt.GetValue(this.Orderbooks, symbol)
	var data map[string]any = ccxt.SafeMapTyped(message, "data")
	var snapshot map[string]any = this.ParseOrderBook(data, symbol)
	snapshot["nonce"] = this.SafeInteger(data, "seqNum")
	snapshot["timestamp"] = timestamp
	snapshot["datetime"] = this.Iso8601(timestamp)
	orderbook.(ccxt.OrderBookInterface).Reset(snapshot)
	// unroll the accumulated deltas
	var messages any = orderbook.(ccxt.OrderBookInterface).GetCache()
	for i := 0; i < ccxt.GetArrayLength(messages); i++ {
		this.HandleOrderBookMessage(client, ccxt.GetValue(messages, i), orderbook)
	}
	ccxt.AddElementToObject(this.Orderbooks, symbol, orderbook)
	client.(ccxt.ClientInterface).Resolve(orderbook, messageHash)
}
func (this *Bittrade) WatchOrderBookSnapshotAsync(client any, message any, subscription any) <-chan any {
	ch := make(chan any, 1)
	go this.watchOrderBookSnapshotBody(ch, client, message, subscription)
	return ch
}
func (this *Bittrade) watchOrderBookSnapshotBody(ch chan any, client any, message any, subscription any) any {
	defer close(ch)
	defer ccxt.ReturnPanicError(ch)
	chSent := false
	_ = chSent
	var messageHash *string = this.SafeString(subscription, "messageHash")

	{
		func(this *Bittrade) (ret_ any) {
			defer func() {
				if e := recover(); e != nil {
					if e == "break" {
						return
					}
					ret_ = func(this *Bittrade) any {
						// catch block:
						ccxt.Remove(client.(ccxt.ClientInterface).GetSubscriptions(), messageHash)
						client.(ccxt.ClientInterface).Reject(e, messageHash)
						return nil
					}(this)
				}
			}()
			// try block:
			var symbol *string = this.SafeString(subscription, "symbol")
			var limit *int64 = this.SafeInteger(subscription, "limit")
			var params map[string]any = ccxt.SafeMapTyped(subscription, "params")
			var api *string = this.SafeString(this.Options, "api", "api")
			var hostname map[string]any = map[string]any{
				"hostname": this.Hostname,
			}
			var url string = this.ImplodeParams(ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(this.Urls, "api"), "ws"), api), "public"), hostname)
			var requestId string = this.RequestId()
			var request map[string]any = map[string]any{
				"req": messageHash,
				"id":  requestId,
			}
			// this is a temporary subscription by a specific requestId
			// it has a very short lifetime until the snapshot is received over ws
			var snapshotSubscription map[string]any = map[string]any{
				"id":          requestId,
				"messageHash": messageHash,
				"symbol":      symbol,
				"limit":       limit,
				"params":      params,
				"method":      this.HandleOrderBookSnapshot,
			}

			var orderbook ccxt.OrderBookInterface = ccxt.PanicOnError((<-this.Watch(url, requestId, request, requestId, snapshotSubscription))).(ccxt.OrderBookInterface)

			ch <- orderbook.(ccxt.OrderBookInterface).Limit()
			chSent = true
			return nil

		}(this)
		if chSent {
			return nil
		}

	}

	return nil
}
func (this *Bittrade) HandleDelta(bookside any, delta any) {
	var price *float64 = this.SafeFloat(delta, 0)
	var amount *float64 = this.SafeFloat(delta, 1)
	bookside.(ccxt.IOrderBookSide).Store(price, amount)
}
func (this *Bittrade) HandleDeltas(bookside any, deltas any) {
	for i := 0; i < ccxt.GetArrayLength(deltas); i++ {
		this.HandleDelta(bookside, ccxt.GetValue(deltas, i))
	}
}
func (this *Bittrade) HandleOrderBookMessage(client any, message any, orderbook any) any {
	//
	//     {
	//         "ch": "market.btcusdt.mbp.150",
	//         "ts": 1583472025885,
	//         "tick": {
	//             "seqNum": 104998984994,
	//             "prevSeqNum": 104998984977,
	//             "bids": [
	//                 [9058.27, 0],
	//                 [9058.43, 0],
	//                 [9058.99, 0],
	//             ],
	//             "asks": [
	//                 [9084.27, 0.2],
	//                 [9085.69, 0],
	//                 [9085.81, 0],
	//             ]
	//         }
	//     }
	//
	var tick map[string]any = ccxt.SafeMapTyped(message, "tick")
	var seqNum *int64 = this.SafeInteger(tick, "seqNum")
	var prevSeqNum *int64 = this.SafeInteger(tick, "prevSeqNum")
	if (prevSeqNum == nil) || (seqNum == nil) {
		return orderbook
	}
	if (ccxt.IsLessThanOrEqual(prevSeqNum, ccxt.GetValue(orderbook, "nonce"))) && (ccxt.IsGreaterThan(seqNum, ccxt.GetValue(orderbook, "nonce"))) {
		var asks []any = ccxt.SafeListTypedDefault(tick, "asks", []any{})
		var bids []any = ccxt.SafeListTypedDefault(tick, "bids", []any{})
		this.HandleDeltas(ccxt.GetValue(orderbook, "asks"), asks)
		this.HandleDeltas(ccxt.GetValue(orderbook, "bids"), bids)
		ccxt.AddElementToObject(orderbook, "nonce", seqNum)
		var timestamp *int64 = this.SafeInteger(message, "ts")
		ccxt.AddElementToObject(orderbook, "timestamp", timestamp)
		ccxt.AddElementToObject(orderbook, "datetime", this.Iso8601(timestamp))
	}
	return orderbook
}
func (this *Bittrade) HandleOrderBook(client any, message map[string]any) {
	//
	// deltas
	//
	//     {
	//         "ch": "market.btcusdt.mbp.150",
	//         "ts": 1583472025885,
	//         "tick": {
	//             "seqNum": 104998984994,
	//             "prevSeqNum": 104998984977,
	//             "bids": [
	//                 [9058.27, 0],
	//                 [9058.43, 0],
	//                 [9058.99, 0],
	//             ],
	//             "asks": [
	//                 [9084.27, 0.2],
	//                 [9085.69, 0],
	//                 [9085.81, 0],
	//             ]
	//         }
	//     }
	//
	var messageHash *string = this.SafeString(message, "ch")
	var ch any = this.SafeValue(message, "ch")
	var parts []string = ccxt.Split(ch, ".")
	var marketId *string = this.SafeString(parts, 1)
	var symbol *string = this.SafeSymbol(marketId)
	var orderbook any = ccxt.GetValue(this.Orderbooks, symbol)
	if ccxt.IsEqual(ccxt.GetValue(orderbook, "nonce"), nil) {
		ccxt.AppendToArray(orderbook.(ccxt.OrderBookInterface).GetCache(), message)
	} else {
		this.HandleOrderBookMessage(client, message, orderbook)
		client.(ccxt.ClientInterface).Resolve(orderbook, messageHash)
	}
}
func (this *Bittrade) HandleOrderBookSubscription(client any, message map[string]any, subscription map[string]any) {
	var symbol *string = this.SafeString(subscription, "symbol")
	if symbol == nil {
		return
	}
	var limit *int64 = this.SafeInteger(subscription, "limit")
	if ccxt.InOp(this.Orderbooks, symbol) {
		ccxt.Remove(this.Orderbooks, symbol)
	}
	ccxt.AddElementToObject(this.Orderbooks, symbol, this.OrderBook(map[string]any{}, limit))
	// watch the snapshot in a separate async call
	this.Spawn(this.WatchOrderBookSnapshotAsync, client, message, subscription)
}
func (this *Bittrade) HandleSubscriptionStatus(client any, message any) any {
	//
	//     {
	//         "id": 1583414227,
	//         "status": "ok",
	//         "subbed": "market.btcusdt.mbp.150",
	//         "ts": 1583414229143
	//     }
	//
	var id *string = this.SafeString(message, "id")
	if id == nil {
		return message
	}
	var subscriptionsById map[string]any = this.IndexBy(client.(ccxt.ClientInterface).GetSubscriptions(), "id")
	var subscription any = this.SafeDict(subscriptionsById, id)
	if subscription != nil {
		var method any = this.SafeValue(subscription, "method")
		if !ccxt.IsEqual(method, nil) {
			return ccxt.CallDynamically(method, client, message, subscription)
		}
		// clean up
		if ccxt.InOp(client.(ccxt.ClientInterface).GetSubscriptions(), id) {
			ccxt.Remove(client.(ccxt.ClientInterface).GetSubscriptions(), id)
		}
	}
	return message
}
func (this *Bittrade) HandleSystemStatus(client any, message map[string]any) any {
	//
	// todo: answer the question whether handleSystemStatus should be renamed
	// and unified as handleStatus for any usage pattern that
	// involves system status and maintenance updates
	//
	//     {
	//         "id": "1578090234088", // connectId
	//         "type": "welcome",
	//     }
	//
	return message
}
func (this *Bittrade) HandleSubject(client any, message any) {
	//
	//     {
	//         "ch": "market.btcusdt.mbp.150",
	//         "ts": 1583472025885,
	//         "tick": {
	//             "seqNum": 104998984994,
	//             "prevSeqNum": 104998984977,
	//             "bids": [
	//                 [9058.27, 0],
	//                 [9058.43, 0],
	//                 [9058.99, 0],
	//             ],
	//             "asks": [
	//                 [9084.27, 0.2],
	//                 [9085.69, 0],
	//                 [9085.81, 0],
	//             ]
	//         }
	//     }
	//
	var ch any = this.SafeValue(message, "ch")
	var parts []string = ccxt.Split(ch, ".")
	var typeVar *string = this.SafeString(parts, 0)
	if typeVar != nil && *typeVar == "market" {
		var methodName *string = this.SafeString(parts, 2)
		var methods map[string]any = map[string]any{
			"mbp":    this.HandleOrderBook,
			"detail": this.HandleTicker,
			"trade":  this.HandleTrades,
			"kline":  this.HandleOHLCV,
		}
		var method any = this.SafeValue(methods, methodName)
		if !ccxt.IsEqual(method, nil) {
			ccxt.CallDynamically(method, client, message)
		}
	}
}
func (this *Bittrade) PongAsync(client any, message any) <-chan any {
	ch := make(chan any, 1)
	go this.pongBody(ch, client, message)
	return ch
}
func (this *Bittrade) pongBody(ch chan any, client any, message any) any {
	defer close(ch)
	defer ccxt.ReturnPanicError(ch)
	//
	//     { ping: 1583491673714 }
	//

	ccxt.PanicOnError((<-client.(ccxt.ClientInterface).SendAsync(map[string]any{
		"pong": this.SafeInteger(message, "ping"),
	})))
	return nil
}
func (this *Bittrade) HandlePing(client any, message any) {
	this.Spawn(this.PongAsync, client, message)
}
func (this *Bittrade) HandleErrorMessage(client any, message any) bool {
	//
	//     {
	//         "ts": 1586323747018,
	//         "status": "error",
	//         'err-code': "bad-request",
	//         'err-msg': "invalid mbp.150.symbol linkusdt",
	//         "id": "2"
	//     }
	//
	var status *string = this.SafeString(message, "status")
	if status != nil && *status == "error" {
		var id *string = this.SafeString(message, "id")
		if id == nil {
			return false
		}
		var subscriptionsById map[string]any = this.IndexBy(client.(ccxt.ClientInterface).GetSubscriptions(), "id")
		var subscription any = this.SafeDict(subscriptionsById, id)
		if subscription != nil {
			var errorCode *string = this.SafeString(message, "err-code")

			{
				func(this *Bittrade) (ret_ any) {
					defer func() {
						if e := recover(); e != nil {
							if e == "break" {
								return
							}
							ret_ = func(this *Bittrade) any {
								// catch block:
								var messageHash *string = this.SafeString(subscription, "messageHash")
								client.(ccxt.ClientInterface).Reject(e, messageHash)
								client.(ccxt.ClientInterface).Reject(e, id)
								if ccxt.InOp(client.(ccxt.ClientInterface).GetSubscriptions(), id) {
									ccxt.Remove(client.(ccxt.ClientInterface).GetSubscriptions(), id)
								}
								return nil
							}(this)
						}
					}()
					// try block:
					this.ThrowExactlyMatchedException(this.Exceptions["exact"], errorCode, this.Json(message))
					return nil
				}(this)

			}
		}
		return false
	}
	return true
}
func (this *Bittrade) HandleMessage(client any, message any) {
	if ccxt.IsEqual(this.HandleErrorMessage(client, message), true) {
		//
		//     {"id":1583414227,"status":"ok","subbed":"market.btcusdt.mbp.150","ts":1583414229143}
		//
		//           ________________________
		//
		// sometimes bittrade responds with half of a JSON response like
		//
		//     " {"ch":"market.ethbtc.m "
		//
		// this is passed to handleMessage as a string since it failed to be decoded as JSON
		//
		if this.SafeString(message, "id") != nil {
			this.HandleSubscriptionStatus(client, message)
		} else if this.SafeString(message, "ch") != nil {
			// route by channel aka topic aka subject
			this.HandleSubject(client, message)
		} else if this.SafeString(message, "ping") != nil {
			this.HandlePing(client, message)
		}
	}
}

func NewBittrade(userConfig map[string]any) *Bittrade {
	p := newBittrade()
	p.Init(userConfig)
	return p
}

func (this *Bittrade) Init(userConfig map[string]any) {
	this.base.Init(this.DeepExtend(this.Describe(), userConfig))
	this.Itf = this
	this.Exchange.DerivedExchange = this
}

// typed methods

/**
 * @method
 * @name bittrade#watchTicker
 * @description watches a price ticker, a statistical calculation with the information calculated over the past 24 hours for a specific market
 * @param {string} symbol unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/?id=ticker-structure}
 */
func (this *Bittrade) WatchTicker(symbol string, options ...ccxt.WatchTickerOptions) (ccxt.Ticker, error) {

	opts := ccxt.WatchTickerOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}
	var res ccxt.AsyncResult[ccxt.Ticker] = ccxt.AwaitResult(ccxt.NewTicker, this.WatchTickerAsync(symbol, opts.Params))
	if res.Err != nil {
		return ccxt.Ticker{}, res.Err
	}
	return res.Value, nil
}

/**
 * @method
 * @name bittrade#watchTrades
 * @description get the list of most recent trades for a particular symbol
 * @param {string} symbol unified symbol of the market to fetch trades for
 * @param {int} [since] timestamp in ms of the earliest trade to fetch
 * @param {int} [limit] the maximum amount of trades to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/?id=public-trades}
 */
func (this *Bittrade) WatchTrades(symbol string, options ...ccxt.WatchTradesOptions) ([]ccxt.Trade, error) {

	opts := ccxt.WatchTradesOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}
	var res ccxt.AsyncResult[[]ccxt.Trade] = ccxt.AwaitResult(ccxt.NewTradeArray, this.WatchTradesAsync(symbol, opts.Since, opts.Limit, opts.Params))
	if res.Err != nil {
		return nil, res.Err
	}
	return res.Value, nil
}

/**
 * @method
 * @name bittrade#watchOHLCV
 * @description watches historical candlestick data containing the open, high, low, and close price, and the volume of a market
 * @param {string} symbol unified symbol of the market to fetch ccxt.OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum amount of candles to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 */
func (this *Bittrade) WatchOHLCV(symbol string, options ...ccxt.WatchOHLCVOptions) ([]ccxt.OHLCV, error) {

	opts := ccxt.WatchOHLCVOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}
	var res ccxt.AsyncResult[[]ccxt.OHLCV] = ccxt.AwaitResult(ccxt.NewOHLCVArray, this.WatchOHLCVAsync(symbol, opts.Timeframe, opts.Since, opts.Limit, opts.Params))
	if res.Err != nil {
		return nil, res.Err
	}
	return res.Value, nil
}

/**
 * @method
 * @name bittrade#watchOrderBook
 * @description watches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
 * @param {string} symbol unified symbol of the market to fetch the order book for
 * @param {int} [limit] the maximum amount of order book entries to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order book structure]{@link https://docs.ccxt.com/?id=order-book-structure}
 */
func (this *Bittrade) WatchOrderBook(symbol string, options ...ccxt.WatchOrderBookOptions) (ccxt.OrderBook, error) {

	opts := ccxt.WatchOrderBookOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}
	var res ccxt.AsyncResult[ccxt.OrderBook] = ccxt.AwaitResult(ccxt.NewOrderBookFromWs, this.WatchOrderBookAsync(symbol, opts.Limit, opts.Params))
	if res.Err != nil {
		return ccxt.OrderBook{}, res.Err
	}
	return res.Value, nil
}
