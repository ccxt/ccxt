package ccxtpro

import ccxt "github.com/ccxt/ccxt/go/v4"

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

type NadoCore struct {
	*ccxt.NadoCore
	base *ccxt.NadoCore
}

func NewNadoCore() *NadoCore {
	p := &NadoCore{}
	base := &ccxt.NadoCore{}
	p.base = base
	p.NadoCore = base
	ccxt.SetDefaults(p)
	return p
}

func (this *NadoCore) Describe() any {
	return this.DeepExtend(this.base.Describe(), map[string]any{
		"has": map[string]any{
			"ws":                          true,
			"cancelAllOrdersWs":           true,
			"cancelOrderWs":               true,
			"cancelOrdersWs":              true,
			"createOrderWs":               true,
			"editOrderWs":                 true,
			"watchBalance":                false,
			"watchBidsAsks":               true,
			"watchFundingRate":            false,
			"watchFundingRates":           false,
			"watchLiquidations":           false,
			"watchLiquidationsForSymbols": false,
			"watchMyTrades":               true,
			"unWatchBidsAsks":             true,
			"unWatchMyTrades":             true,
			"unWatchOHLCV":                true,
			"unWatchOHLCVForSymbols":      true,
			"unWatchOrderBook":            true,
			"unWatchOrderBookForSymbols":  true,
			"unWatchOrders":               true,
			"unWatchPositions":            true,
			"unWatchTicker":               true,
			"unWatchTickers":              true,
			"unWatchTrades":               true,
			"unWatchTradesForSymbols":     true,
			"watchOHLCV":                  true,
			"watchOHLCVForSymbols":        true,
			"watchOrderBook":              true,
			"watchOrderBookForSymbols":    true,
			"watchOrders":                 true,
			"watchPositions":              true,
			"watchTicker":                 true,
			"watchTickers":                true,
			"watchTrades":                 true,
			"watchTradesForSymbols":       true,
		},
		"streaming": map[string]any{
			"ping":      this.Ping,
			"keepAlive": 30000,
		},
		"options": map[string]any{
			"tradesLimit": 1000,
			"requestId":   0,
		},
		"urls": map[string]any{
			"api": map[string]any{
				"ws": map[string]any{
					"gateway":       "wss://gateway.prod.nado.xyz/ws/v2",
					"subscriptions": "wss://gateway.prod.nado.xyz/v1/subscribe",
				},
			},
			"test": map[string]any{
				"ws": map[string]any{
					"gateway":       "wss://gateway.test.nado.xyz/ws/v2",
					"subscriptions": "wss://gateway.test.nado.xyz/v1/subscribe",
				},
			},
		},
	})
}
func (this *NadoCore) RequestId() any {
	var requestId any = this.Sum(this.SafeInteger(this.Options, "requestId", 0), 1)
	ccxt.AddElementToObject(this.Options, "requestId", requestId)
	return requestId
}

/**
 * @method
 * @name nado#watchTrades
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description watches information on multiple trades made in a market
 * @param {string} symbol unified symbol of the market to fetch trades for
 * @param {int} [since] timestamp in ms of the earliest trade to fetch
 * @param {int} [limit] the maximum number of trades to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {ccxt.Trade[]} a list of [trade structures]{@link https://docs.ccxt.com/#/?id=public-trades}
 */
func (this *NadoCore) WatchTrades(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		since := ccxt.GetArg(optionalArgs, 0, nil)
		_ = since
		limit := ccxt.GetArg(optionalArgs, 1, nil)
		_ = limit
		params := ccxt.GetArg(optionalArgs, 2, map[string]any{})
		_ = params

		retRes958 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes958)
		var market any = this.Market(symbol)
		var messageHash any = ccxt.Add("trade:", ccxt.GetValue(market, "symbol"))

		trades := (<-this.WatchPublic("trade", market, messageHash, params))
		ccxt.PanicOnError(trades)
		if ccxt.IsTrue(this.NewUpdates) {
			limit = ccxt.ToGetsLimit(trades).GetLimit(ccxt.GetValue(market, "symbol"), limit)
		}

		ch <- this.FilterBySinceLimit(trades, since, limit, "timestamp", true)
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#unWatchTrades
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description unWatches information on multiple trades made in a market
 * @param {string} symbol unified symbol of the market to unwatch trades for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} the exchange response
 */
func (this *NadoCore) UnWatchTrades(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		params := ccxt.GetArg(optionalArgs, 0, map[string]any{})
		_ = params

		retRes1158 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes1158)

		retRes11615 := (<-this.UnWatchTradesForSymbols([]any{symbol}, params))
		ccxt.PanicOnError(retRes11615)
		ch <- retRes11615
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#watchTradesForSymbols
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description get the list of most recent trades for a list of symbols
 * @param {string[]} symbols unified symbols of the markets to fetch trades for
 * @param {int} [since] timestamp in ms of the earliest trade to fetch
 * @param {int} [limit] the maximum number of trades to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {ccxt.Trade[]} a list of [trade structures]{@link https://docs.ccxt.com/#/?id=public-trades}
 */
func (this *NadoCore) WatchTradesForSymbols(symbols any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		since := ccxt.GetArg(optionalArgs, 0, nil)
		_ = since
		limit := ccxt.GetArg(optionalArgs, 1, nil)
		_ = limit
		params := ccxt.GetArg(optionalArgs, 2, map[string]any{})
		_ = params

		retRes1318 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes1318)
		var symbolsLength any = ccxt.GetArrayLength(symbols)
		if ccxt.IsTrue(ccxt.IsEqual(symbolsLength, 0)) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " watchTradesForSymbols() requires a non-empty array of symbols")))
		}
		symbols = this.MarketSymbols(symbols, nil, false, true, true)
		var markets any = []any{}
		var messageHashes any = []any{}
		for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(symbols)); i++ {
			var market any = this.Market(ccxt.GetValue(symbols, i))
			ccxt.AppendToArray(&markets, market)
			ccxt.AppendToArray(&messageHashes, ccxt.Add("trade:", ccxt.GetValue(market, "symbol")))
		}

		trades := (<-this.WatchPublicMultiple("trade", markets, messageHashes, params))
		ccxt.PanicOnError(trades)
		if ccxt.IsTrue(this.NewUpdates) {
			var first any = this.SafeDict(trades, 0)
			var tradeSymbol any = this.SafeString(first, "symbol")
			limit = ccxt.ToGetsLimit(trades).GetLimit(tradeSymbol, limit)
		}

		ch <- this.FilterBySinceLimit(trades, since, limit, "timestamp", true)
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#unWatchTradesForSymbols
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description unWatches information on multiple trades made in a list of markets
 * @param {string[]} symbols unified symbols of the markets to unwatch trades for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} the exchange response
 */
func (this *NadoCore) UnWatchTradesForSymbols(symbols any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		params := ccxt.GetArg(optionalArgs, 0, map[string]any{})
		_ = params

		retRes1638 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes1638)
		var symbolsLength any = ccxt.GetArrayLength(symbols)
		if ccxt.IsTrue(ccxt.IsEqual(symbolsLength, 0)) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " unWatchTradesForSymbols() requires a non-empty array of symbols")))
		}
		symbols = this.MarketSymbols(symbols, nil, false, true, true)
		var markets any = []any{}
		var messageHashes any = []any{}
		for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(symbols)); i++ {
			var market any = this.Market(ccxt.GetValue(symbols, i))
			ccxt.AppendToArray(&markets, market)
			ccxt.AppendToArray(&messageHashes, ccxt.Add("trade:", ccxt.GetValue(market, "symbol")))
		}

		retRes17615 := (<-this.UnWatchPublicMultiple("trade", markets, messageHashes, params))
		ccxt.PanicOnError(retRes17615)
		ch <- retRes17615
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#watchOrderBook
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description watches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
 * @param {string} symbol unified symbol of the market to fetch the order book for
 * @param {int} [limit] the maximum amount of order book entries to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {ccxt.OrderBook} an [order book structure]{@link https://docs.ccxt.com/?id=order-book-structure}
 */
func (this *NadoCore) WatchOrderBook(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		limit := ccxt.GetArg(optionalArgs, 0, nil)
		_ = limit
		params := ccxt.GetArg(optionalArgs, 1, map[string]any{})
		_ = params

		retRes1908 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes1908)
		var market any = this.Market(symbol)
		var messageHash any = ccxt.Add("orderbook:", ccxt.GetValue(market, "symbol"))
		if !ccxt.IsTrue((ccxt.InOp(this.Orderbooks, ccxt.GetValue(market, "symbol")))) {

			snapshot := (<-this.FetchOrderBook(symbol, limit))
			ccxt.PanicOnError(snapshot)
			ccxt.AddElementToObject(this.Orderbooks, ccxt.GetValue(market, "symbol"), this.OrderBook(snapshot, limit))
		}

		orderbook := (<-this.WatchPublic("book_depth", market, messageHash, params))
		ccxt.PanicOnError(orderbook)

		ch <- orderbook.(ccxt.OrderBookInterface).Limit()
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#unWatchOrderBook
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description unWatches information on open orders with bid (buy) and ask (sell) prices, volumes and other data
 * @param {string} symbol unified symbol of the market to unwatch the order book for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} the exchange response
 */
func (this *NadoCore) UnWatchOrderBook(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		params := ccxt.GetArg(optionalArgs, 0, map[string]any{})
		_ = params

		retRes2118 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes2118)

		retRes21215 := (<-this.UnWatchOrderBookForSymbols([]any{symbol}, params))
		ccxt.PanicOnError(retRes21215)
		ch <- retRes21215
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#watchOrderBookForSymbols
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description watches information on open orders with bid (buy) and ask (sell) prices, volumes and other data for a list of symbols
 * @param {string[]} symbols unified symbols of the markets to fetch the order book for
 * @param {int} [limit] the maximum amount of order book entries to return
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {ccxt.OrderBook} an [order book structure]{@link https://docs.ccxt.com/#/?id=order-book-structure}
 */
func (this *NadoCore) WatchOrderBookForSymbols(symbols any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		limit := ccxt.GetArg(optionalArgs, 0, nil)
		_ = limit
		params := ccxt.GetArg(optionalArgs, 1, map[string]any{})
		_ = params

		retRes2268 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes2268)
		var symbolsLength any = ccxt.GetArrayLength(symbols)
		if ccxt.IsTrue(ccxt.IsEqual(symbolsLength, 0)) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " watchOrderBookForSymbols() requires a non-empty array of symbols")))
		}
		symbols = this.MarketSymbols(symbols, nil, false, true, true)
		var markets any = []any{}
		var messageHashes any = []any{}
		for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(symbols)); i++ {
			var symbol any = ccxt.GetValue(symbols, i)
			var market any = this.Market(symbol)
			var messageHash any = ccxt.Add("orderbook:", ccxt.GetValue(market, "symbol"))
			ccxt.AppendToArray(&markets, market)
			ccxt.AppendToArray(&messageHashes, messageHash)
			if !ccxt.IsTrue((ccxt.InOp(this.Orderbooks, ccxt.GetValue(market, "symbol")))) {

				snapshot := (<-this.FetchOrderBook(symbol, limit))
				ccxt.PanicOnError(snapshot)
				ccxt.AddElementToObject(this.Orderbooks, ccxt.GetValue(market, "symbol"), this.OrderBook(snapshot, limit))
			}
		}

		orderbook := (<-this.WatchPublicMultiple("book_depth", markets, messageHashes, params))
		ccxt.PanicOnError(orderbook)

		ch <- orderbook.(ccxt.OrderBookInterface).Limit()
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#unWatchOrderBookForSymbols
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description unWatches information on open orders with bid (buy) and ask (sell) prices, volumes and other data for a list of symbols
 * @param {string[]} symbols unified symbols of the markets to unwatch the order book for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} the exchange response
 */
func (this *NadoCore) UnWatchOrderBookForSymbols(symbols any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		params := ccxt.GetArg(optionalArgs, 0, map[string]any{})
		_ = params

		retRes2598 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes2598)
		var symbolsLength any = ccxt.GetArrayLength(symbols)
		if ccxt.IsTrue(ccxt.IsEqual(symbolsLength, 0)) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " unWatchOrderBookForSymbols() requires a non-empty array of symbols")))
		}
		symbols = this.MarketSymbols(symbols, nil, false, true, true)
		var markets any = []any{}
		var messageHashes any = []any{}
		for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(symbols)); i++ {
			var market any = this.Market(ccxt.GetValue(symbols, i))
			ccxt.AppendToArray(&markets, market)
			ccxt.AppendToArray(&messageHashes, ccxt.Add("orderbook:", ccxt.GetValue(market, "symbol")))
		}

		retRes27215 := (<-this.UnWatchPublicMultiple("book_depth", markets, messageHashes, params))
		ccxt.PanicOnError(retRes27215)
		ch <- retRes27215
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#watchOHLCV
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description watches historical candlestick data containing the open, high, low, and close price, and the volume of a market
 * @param {string} symbol unified symbol of the market to fetch ccxt.OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum amount of candles to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 */
func (this *NadoCore) WatchOHLCV(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		timeframe := ccxt.GetArg(optionalArgs, 0, "1m")
		_ = timeframe
		since := ccxt.GetArg(optionalArgs, 1, nil)
		_ = since
		limit := ccxt.GetArg(optionalArgs, 2, nil)
		_ = limit
		params := ccxt.GetArg(optionalArgs, 3, map[string]any{})
		_ = params

		retRes2888 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes2888)
		var market any = this.Market(symbol)
		var messageHash any = ccxt.Add(ccxt.Add(ccxt.Add("ohlcv:", timeframe), ":"), ccxt.GetValue(market, "symbol"))
		var request any = map[string]any{
			"granularity": this.SafeInteger(this.Timeframes, timeframe, this.ParseTimeframe(timeframe)),
		}

		result := (<-this.WatchPublic("latest_candlestick", market, messageHash, this.Extend(request, params)))
		ccxt.PanicOnError(result)
		var stored any = ccxt.GetValue(result, 2)
		if ccxt.IsTrue(this.NewUpdates) {
			limit = ccxt.ToGetsLimit(stored).GetLimit(ccxt.GetValue(market, "symbol"), limit)
		}

		ch <- this.FilterBySinceLimit(stored, since, limit, 0, true)
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#watchOHLCVForSymbols
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description watches historical candlestick data containing the open, high, low, and close price, and the volume of multiple markets
 * @param {string[][]} symbolsAndTimeframes array of arrays containing unified symbols and timeframes to watch ccxt.OHLCV data for, example [['BTC/USDT0:USDT0', '1m'], ['ETH/USDT0:USDT0', '5m']]
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum amount of candles to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} A dictionary of {@link https://docs.ccxt.com/#/?id=ohlcv-structure ccxt.OHLCV} structures indexed by market symbols
 */
func (this *NadoCore) WatchOHLCVForSymbols(symbolsAndTimeframes any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		since := ccxt.GetArg(optionalArgs, 0, nil)
		_ = since
		limit := ccxt.GetArg(optionalArgs, 1, nil)
		_ = limit
		params := ccxt.GetArg(optionalArgs, 2, map[string]any{})
		_ = params
		var symbolsLength any = ccxt.GetArrayLength(symbolsAndTimeframes)
		if ccxt.IsTrue(ccxt.IsTrue(ccxt.IsEqual(symbolsLength, 0)) || !ccxt.IsTrue(ccxt.IsArray(ccxt.GetValue(symbolsAndTimeframes, 0)))) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " watchOHLCVForSymbols() requires a an array of symbols and timeframes, like  [['BTC/USDT0:USDT0', '1m'], ['ETH/USDT0:USDT0', '5m']]")))
		}

		retRes3188 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes3188)
		var markets any = []any{}
		var messageHashes any = []any{}
		var subscriptionParams any = []any{}
		for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(symbolsAndTimeframes)); i++ {
			var symbolAndTimeframe any = ccxt.GetValue(symbolsAndTimeframes, i)
			var marketSymbol any = this.SafeString(symbolAndTimeframe, 0)
			var timeframe any = this.SafeString(symbolAndTimeframe, 1, "1m")
			var market any = this.Market(marketSymbol)
			ccxt.AppendToArray(&markets, market)
			ccxt.AppendToArray(&messageHashes, ccxt.Add(ccxt.Add(ccxt.Add("ohlcv:", timeframe), ":"), ccxt.GetValue(market, "symbol")))
			ccxt.AppendToArray(&subscriptionParams, this.Extend(map[string]any{
				"granularity": this.SafeInteger(this.Timeframes, timeframe, this.ParseTimeframe(timeframe)),
			}, params))
		}
		resultSymbolresultTimeframestoredVariable := (<-this.WatchPublicMultiple("latest_candlestick", markets, messageHashes, params, subscriptionParams))
		resultSymbol := ccxt.GetValue(resultSymbolresultTimeframestoredVariable, 0)
		resultTimeframe := ccxt.GetValue(resultSymbolresultTimeframestoredVariable, 1)
		stored := ccxt.GetValue(resultSymbolresultTimeframestoredVariable, 2)
		if ccxt.IsTrue(this.NewUpdates) {
			limit = ccxt.ToGetsLimit(stored).GetLimit(resultSymbol, limit)
		}
		var filtered any = this.FilterBySinceLimit(stored, since, limit, 0, true)

		ch <- this.CreateOHLCVObject(resultSymbol, resultTimeframe, filtered)
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#unWatchOHLCV
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description unWatches historical candlestick data containing the open, high, low, and close price, and the volume of a market
 * @param {string} symbol unified symbol of the market to unwatch ccxt.OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} the exchange response
 */
func (this *NadoCore) UnWatchOHLCV(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		timeframe := ccxt.GetArg(optionalArgs, 0, "1m")
		_ = timeframe
		params := ccxt.GetArg(optionalArgs, 1, map[string]any{})
		_ = params

		retRes3528 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes3528)

		retRes35315 := (<-this.UnWatchOHLCVForSymbols([]any{[]any{symbol, timeframe}}, params))
		ccxt.PanicOnError(retRes35315)
		ch <- retRes35315
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#unWatchOHLCVForSymbols
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description unWatches historical candlestick data containing the open, high, low, and close price, and the volume of multiple markets
 * @param {string[][]} symbolsAndTimeframes array of arrays containing unified symbols and timeframes to unwatch ccxt.OHLCV data for, example [['BTC/USDT0:USDT0', '1m'], ['ETH/USDT0:USDT0', '5m']]
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} the exchange response
 */
func (this *NadoCore) UnWatchOHLCVForSymbols(symbolsAndTimeframes any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		params := ccxt.GetArg(optionalArgs, 0, map[string]any{})
		_ = params
		var symbolsLength any = ccxt.GetArrayLength(symbolsAndTimeframes)
		if ccxt.IsTrue(ccxt.IsTrue(ccxt.IsEqual(symbolsLength, 0)) || !ccxt.IsTrue(ccxt.IsArray(ccxt.GetValue(symbolsAndTimeframes, 0)))) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " unWatchOHLCVForSymbols() requires a an array of symbols and timeframes, like  [['BTC/USDT0:USDT0', '1m'], ['ETH/USDT0:USDT0', '5m']]")))
		}

		retRes3708 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes3708)
		var markets any = []any{}
		var messageHashes any = []any{}
		var subscriptionParams any = []any{}
		for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(symbolsAndTimeframes)); i++ {
			var symbolAndTimeframe any = ccxt.GetValue(symbolsAndTimeframes, i)
			var marketSymbol any = this.SafeString(symbolAndTimeframe, 0)
			var timeframe any = this.SafeString(symbolAndTimeframe, 1, "1m")
			var market any = this.Market(marketSymbol)
			ccxt.AppendToArray(&markets, market)
			ccxt.AppendToArray(&messageHashes, ccxt.Add(ccxt.Add(ccxt.Add("ohlcv:", timeframe), ":"), ccxt.GetValue(market, "symbol")))
			ccxt.AppendToArray(&subscriptionParams, this.Extend(map[string]any{
				"granularity": this.SafeInteger(this.Timeframes, timeframe, this.ParseTimeframe(timeframe)),
			}, params))
		}

		retRes38515 := (<-this.UnWatchPublicMultiple("latest_candlestick", markets, messageHashes, params, subscriptionParams))
		ccxt.PanicOnError(retRes38515)
		ch <- retRes38515
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#watchTicker
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description watches a price ticker with the best bid and ask for a specific market
 * @param {string} symbol unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/#/?id=ticker-structure}
 */
func (this *NadoCore) WatchTicker(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		params := ccxt.GetArg(optionalArgs, 0, map[string]any{})
		_ = params

		retRes3988 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes3988)
		symbol = this.Symbol(symbol)

		tickers := (<-this.WatchTickers([]any{symbol}, params))
		ccxt.PanicOnError(tickers)

		ch <- ccxt.GetValue(tickers, symbol)
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#unWatchTicker
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description unWatches a price ticker with the best bid and ask for a specific market
 * @param {string} symbol unified symbol of the market to unwatch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} the exchange response
 */
func (this *NadoCore) UnWatchTicker(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		params := ccxt.GetArg(optionalArgs, 0, map[string]any{})
		_ = params

		retRes4148 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes4148)

		retRes41515 := (<-this.UnWatchTickers([]any{symbol}, params))
		ccxt.PanicOnError(retRes41515)
		ch <- retRes41515
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#watchTickers
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description watches price tickers with the best bid and ask for all markets of a specific list
 * @param {string[]} [symbols] unified symbols of the markets to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a dictionary of [ticker structures]{@link https://docs.ccxt.com/#/?id=ticker-structure}
 */
func (this *NadoCore) WatchTickers(optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		symbols := ccxt.GetArg(optionalArgs, 0, nil)
		_ = symbols
		params := ccxt.GetArg(optionalArgs, 1, map[string]any{})
		_ = params

		retRes4288 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes4288)
		symbols = this.MarketSymbols(symbols, nil, true, true, true)
		var market any = nil
		var messageHash any = "ticker"
		var streamType any = "all_bbo"
		if ccxt.IsTrue(!ccxt.IsEqual(symbols, nil)) {
			var symbolsLength any = ccxt.GetArrayLength(symbols)
			if ccxt.IsTrue(ccxt.IsEqual(symbolsLength, 1)) {
				market = this.Market(ccxt.GetValue(symbols, 0))
				messageHash = ccxt.Add("ticker:", ccxt.GetValue(market, "symbol"))
				streamType = "best_bid_offer"
			}
		}

		ticker := (<-this.WatchPublic(streamType, market, messageHash, params))
		ccxt.PanicOnError(ticker)
		if ccxt.IsTrue(this.NewUpdates) {
			if ccxt.IsTrue(ccxt.IsEqual(messageHash, "ticker")) {

				ch <- this.FilterByArray(ticker, "symbol", symbols)
				return nil
			}
			var tickers any = map[string]any{}
			ccxt.AddElementToObject(tickers, ccxt.GetValue(ticker, "symbol"), ticker)

			ch <- tickers
			return nil
		}

		ch <- this.FilterByArray(this.Tickers, "symbol", symbols)
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#unWatchTickers
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description unWatches price tickers with the best bid and ask for all markets of a specific list
 * @param {string[]} [symbols] unified symbols of the markets to unwatch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} the exchange response
 */
func (this *NadoCore) UnWatchTickers(optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		symbols := ccxt.GetArg(optionalArgs, 0, nil)
		_ = symbols
		params := ccxt.GetArg(optionalArgs, 1, map[string]any{})
		_ = params

		retRes4638 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes4638)
		symbols = this.MarketSymbols(symbols, nil, true, true, true)
		var market any = nil
		var messageHash any = "ticker"
		var streamType any = "all_bbo"
		if ccxt.IsTrue(!ccxt.IsEqual(symbols, nil)) {
			var symbolsLength any = ccxt.GetArrayLength(symbols)
			if ccxt.IsTrue(ccxt.IsEqual(symbolsLength, 1)) {
				market = this.Market(ccxt.GetValue(symbols, 0))
				messageHash = ccxt.Add("ticker:", ccxt.GetValue(market, "symbol"))
				streamType = "best_bid_offer"
			}
		}

		retRes47615 := (<-this.UnWatchPublic(streamType, market, messageHash, params))
		ccxt.PanicOnError(retRes47615)
		ch <- retRes47615
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#watchBidsAsks
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description watches best bid & ask for symbols
 * @param {string[]} symbols unified symbols of the markets to fetch the bids and asks for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/#/?id=ticker-structure}
 */
func (this *NadoCore) WatchBidsAsks(optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		symbols := ccxt.GetArg(optionalArgs, 0, nil)
		_ = symbols
		params := ccxt.GetArg(optionalArgs, 1, map[string]any{})
		_ = params

		retRes4898 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes4898)
		symbols = this.MarketSymbols(symbols, nil, true, true, true)
		var market any = nil
		var messageHash any = "bidask"
		var streamType any = "all_bbo"
		if ccxt.IsTrue(!ccxt.IsEqual(symbols, nil)) {
			var symbolsLength any = ccxt.GetArrayLength(symbols)
			if ccxt.IsTrue(ccxt.IsEqual(symbolsLength, 1)) {
				market = this.Market(ccxt.GetValue(symbols, 0))
				messageHash = ccxt.Add("bidask:", ccxt.GetValue(market, "symbol"))
				streamType = "best_bid_offer"
			}
		}

		ticker := (<-this.WatchPublic(streamType, market, messageHash, params))
		ccxt.PanicOnError(ticker)
		if ccxt.IsTrue(this.NewUpdates) {
			if ccxt.IsTrue(ccxt.IsEqual(messageHash, "bidask")) {

				ch <- this.FilterByArray(ticker, "symbol", symbols)
				return nil
			}
			var tickers any = map[string]any{}
			ccxt.AddElementToObject(tickers, ccxt.GetValue(ticker, "symbol"), ticker)

			ch <- tickers
			return nil
		}

		ch <- this.FilterByArray(this.Bidsasks, "symbol", symbols)
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#unWatchBidsAsks
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description unWatches best bid & ask for symbols
 * @param {string[]} symbols unified symbols of the markets to unwatch the bids and asks for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} the exchange response
 */
func (this *NadoCore) UnWatchBidsAsks(optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		symbols := ccxt.GetArg(optionalArgs, 0, nil)
		_ = symbols
		params := ccxt.GetArg(optionalArgs, 1, map[string]any{})
		_ = params

		retRes5248 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes5248)
		symbols = this.MarketSymbols(symbols, nil, true, true, true)
		var market any = nil
		var messageHash any = "bidask"
		var streamType any = "all_bbo"
		if ccxt.IsTrue(!ccxt.IsEqual(symbols, nil)) {
			var symbolsLength any = ccxt.GetArrayLength(symbols)
			if ccxt.IsTrue(ccxt.IsEqual(symbolsLength, 1)) {
				market = this.Market(ccxt.GetValue(symbols, 0))
				messageHash = ccxt.Add("bidask:", ccxt.GetValue(market, "symbol"))
				streamType = "best_bid_offer"
			}
		}

		retRes53715 := (<-this.UnWatchPublic(streamType, market, messageHash, params))
		ccxt.PanicOnError(retRes53715)
		ch <- retRes53715
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#watchOrders
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/authentication
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/events
 * @description watches information on multiple orders made by the user
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [order structures]{@link https://docs.ccxt.com/#/?id=order-structure}
 */
func (this *NadoCore) WatchOrders(optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		symbol := ccxt.GetArg(optionalArgs, 0, nil)
		_ = symbol
		since := ccxt.GetArg(optionalArgs, 1, nil)
		_ = since
		limit := ccxt.GetArg(optionalArgs, 2, nil)
		_ = limit
		params := ccxt.GetArg(optionalArgs, 3, map[string]any{})
		_ = params
		this.CheckRequiredCredentials()

		retRes5558 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes5558)

		retRes5568 := (<-this.Authenticate(this.Extend(map[string]any{}, params)))
		ccxt.PanicOnError(retRes5568)
		var market any = nil
		var messageHash any = "orders"
		var productId any = nil
		if ccxt.IsTrue(!ccxt.IsEqual(symbol, nil)) {
			market = this.Market(symbol)
			symbol = ccxt.GetValue(market, "symbol")
			messageHash = ccxt.Add(messageHash, ccxt.Add(":", symbol))
			productId = this.ParseToInt(ccxt.GetValue(market, "id"))
		}
		var subaccount any = nil
		subaccountparamsVariable := this.HandleOptionAndParams(params, "watchOrders", "subaccount", "default")
		subaccount = ccxt.GetValue(subaccountparamsVariable, 0)
		params = ccxt.GetValue(subaccountparamsVariable, 1)
		var sender any = this.CreateSubaccount(this.WalletAddress, subaccount)
		var stream any = map[string]any{
			"type":       "order_update",
			"subaccount": sender,
			"product_id": productId,
		}

		orders := (<-this.WatchPrivate("order_update", stream, messageHash, params))
		ccxt.PanicOnError(orders)
		if ccxt.IsTrue(this.NewUpdates) {
			limit = ccxt.ToGetsLimit(orders).GetLimit(symbol, limit)
		}

		ch <- this.FilterBySymbolSinceLimit(orders, symbol, since, limit, true)
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#unWatchOrders
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/authentication
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description unWatches information on multiple orders made by the user
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} the exchange response
 */
func (this *NadoCore) UnWatchOrders(optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		symbol := ccxt.GetArg(optionalArgs, 0, nil)
		_ = symbol
		params := ccxt.GetArg(optionalArgs, 1, map[string]any{})
		_ = params
		this.CheckRequiredCredentials()

		retRes5938 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes5938)

		retRes5948 := (<-this.Authenticate(this.Extend(map[string]any{}, params)))
		ccxt.PanicOnError(retRes5948)
		var market any = nil
		var messageHash any = "orders"
		var productId any = nil
		if ccxt.IsTrue(!ccxt.IsEqual(symbol, nil)) {
			market = this.Market(symbol)
			symbol = ccxt.GetValue(market, "symbol")
			messageHash = ccxt.Add(messageHash, ccxt.Add(":", symbol))
			productId = this.ParseToInt(ccxt.GetValue(market, "id"))
		}
		var subaccount any = nil
		subaccountparamsVariable := this.HandleOptionAndParams(params, "unWatchOrders", "subaccount", "default")
		subaccount = ccxt.GetValue(subaccountparamsVariable, 0)
		params = ccxt.GetValue(subaccountparamsVariable, 1)
		var sender any = this.CreateSubaccount(this.WalletAddress, subaccount)
		var stream any = map[string]any{
			"type":       "order_update",
			"subaccount": sender,
			"product_id": productId,
		}

		retRes61215 := (<-this.UnWatchPrivate(stream, messageHash, params))
		ccxt.PanicOnError(retRes61215)
		ch <- retRes61215
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#watchMyTrades
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/authentication
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/events
 * @description watches information on multiple trades made by the user
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch trades for
 * @param {int} [limit] the maximum number of trade structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [trade structures]{@link https://docs.ccxt.com/#/?id=trade-structure}
 */
func (this *NadoCore) WatchMyTrades(optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		symbol := ccxt.GetArg(optionalArgs, 0, nil)
		_ = symbol
		since := ccxt.GetArg(optionalArgs, 1, nil)
		_ = since
		limit := ccxt.GetArg(optionalArgs, 2, nil)
		_ = limit
		params := ccxt.GetArg(optionalArgs, 3, map[string]any{})
		_ = params
		this.CheckRequiredCredentials()

		retRes6308 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes6308)

		retRes6318 := (<-this.Authenticate(this.Extend(map[string]any{}, params)))
		ccxt.PanicOnError(retRes6318)
		var market any = nil
		var messageHash any = "myTrades"
		var productId any = nil
		if ccxt.IsTrue(!ccxt.IsEqual(symbol, nil)) {
			market = this.Market(symbol)
			symbol = ccxt.GetValue(market, "symbol")
			messageHash = ccxt.Add(messageHash, ccxt.Add(":", symbol))
			productId = this.ParseToInt(ccxt.GetValue(market, "id"))
		}
		var subaccount any = nil
		subaccountparamsVariable := this.HandleOptionAndParams(params, "watchMyTrades", "subaccount", "default")
		subaccount = ccxt.GetValue(subaccountparamsVariable, 0)
		params = ccxt.GetValue(subaccountparamsVariable, 1)
		var sender any = this.CreateSubaccount(this.WalletAddress, subaccount)
		var stream any = map[string]any{
			"type":       "fill",
			"subaccount": sender,
			"product_id": productId,
		}

		trades := (<-this.WatchPrivate("fill", stream, messageHash, params))
		ccxt.PanicOnError(trades)
		if ccxt.IsTrue(this.NewUpdates) {
			limit = ccxt.ToGetsLimit(trades).GetLimit(symbol, limit)
		}

		ch <- this.FilterBySymbolSinceLimit(trades, symbol, since, limit, true)
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#unWatchMyTrades
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/authentication
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description unWatches information on multiple trades made by the user
 * @param {string} symbol unified market symbol of the market orders were made in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} the exchange response
 */
func (this *NadoCore) UnWatchMyTrades(optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		symbol := ccxt.GetArg(optionalArgs, 0, nil)
		_ = symbol
		params := ccxt.GetArg(optionalArgs, 1, map[string]any{})
		_ = params
		this.CheckRequiredCredentials()

		retRes6688 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes6688)

		retRes6698 := (<-this.Authenticate(this.Extend(map[string]any{}, params)))
		ccxt.PanicOnError(retRes6698)
		var market any = nil
		var messageHash any = "myTrades"
		var productId any = nil
		if ccxt.IsTrue(!ccxt.IsEqual(symbol, nil)) {
			market = this.Market(symbol)
			symbol = ccxt.GetValue(market, "symbol")
			messageHash = ccxt.Add(messageHash, ccxt.Add(":", symbol))
			productId = this.ParseToInt(ccxt.GetValue(market, "id"))
		}
		var subaccount any = nil
		subaccountparamsVariable := this.HandleOptionAndParams(params, "unWatchMyTrades", "subaccount", "default")
		subaccount = ccxt.GetValue(subaccountparamsVariable, 0)
		params = ccxt.GetValue(subaccountparamsVariable, 1)
		var sender any = this.CreateSubaccount(this.WalletAddress, subaccount)
		var stream any = map[string]any{
			"type":       "fill",
			"subaccount": sender,
			"product_id": productId,
		}

		retRes68715 := (<-this.UnWatchPrivate(stream, messageHash, params))
		ccxt.PanicOnError(retRes68715)
		ch <- retRes68715
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#watchPositions
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/authentication
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/events
 * @description watches information on user positions
 * @param {string[]} [symbols] unified market symbols
 * @param {int} [since] the earliest time in ms to fetch positions for
 * @param {int} [limit] the maximum number of position structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} a list of [position structures]{@link https://docs.ccxt.com/#/?id=position-structure}
 */
func (this *NadoCore) WatchPositions(optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		symbols := ccxt.GetArg(optionalArgs, 0, nil)
		_ = symbols
		since := ccxt.GetArg(optionalArgs, 1, nil)
		_ = since
		limit := ccxt.GetArg(optionalArgs, 2, nil)
		_ = limit
		params := ccxt.GetArg(optionalArgs, 3, map[string]any{})
		_ = params
		this.CheckRequiredCredentials()

		retRes7058 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes7058)

		retRes7068 := (<-this.Authenticate(this.Extend(map[string]any{}, params)))
		ccxt.PanicOnError(retRes7068)
		symbols = this.MarketSymbols(symbols, nil, false, true, true)
		var messageHash any = "positions"
		var productId any = nil
		if ccxt.IsTrue(!ccxt.IsEqual(symbols, nil)) {
			var symbolsLength any = ccxt.GetArrayLength(symbols)
			if ccxt.IsTrue(ccxt.IsEqual(symbolsLength, 1)) {
				var market any = this.Market(ccxt.GetValue(symbols, 0))
				messageHash = ccxt.Add(messageHash, ccxt.Add(":", ccxt.GetValue(market, "symbol")))
				productId = this.ParseToInt(ccxt.GetValue(market, "id"))
			}
		}
		var subaccount any = nil
		subaccountparamsVariable := this.HandleOptionAndParams(params, "watchPositions", "subaccount", "default")
		subaccount = ccxt.GetValue(subaccountparamsVariable, 0)
		params = ccxt.GetValue(subaccountparamsVariable, 1)
		var sender any = this.CreateSubaccount(this.WalletAddress, subaccount)
		var stream any = map[string]any{
			"type":       "position_change",
			"subaccount": sender,
			"product_id": productId,
		}

		positions := (<-this.WatchPrivate("position_change", stream, messageHash, params))
		ccxt.PanicOnError(positions)
		if ccxt.IsTrue(this.NewUpdates) {

			ch <- positions
			return nil
		}

		ch <- this.FilterBySymbolsSinceLimit(this.Positions, symbols, since, limit, true)
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#unWatchPositions
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/authentication
 * @see https://docs.nado.xyz/developer-resources/api/subscriptions/streams
 * @description unWatches information on user positions
 * @param {string[]} [symbols] unified market symbols
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} the exchange response
 */
func (this *NadoCore) UnWatchPositions(optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		symbols := ccxt.GetArg(optionalArgs, 0, nil)
		_ = symbols
		params := ccxt.GetArg(optionalArgs, 1, map[string]any{})
		_ = params
		this.CheckRequiredCredentials()

		retRes7458 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes7458)

		retRes7468 := (<-this.Authenticate(this.Extend(map[string]any{}, params)))
		ccxt.PanicOnError(retRes7468)
		symbols = this.MarketSymbols(symbols, nil, false, true, true)
		var messageHash any = "positions"
		var productId any = nil
		if ccxt.IsTrue(!ccxt.IsEqual(symbols, nil)) {
			var symbolsLength any = ccxt.GetArrayLength(symbols)
			if ccxt.IsTrue(ccxt.IsEqual(symbolsLength, 1)) {
				var market any = this.Market(ccxt.GetValue(symbols, 0))
				messageHash = ccxt.Add(messageHash, ccxt.Add(":", ccxt.GetValue(market, "symbol")))
				productId = this.ParseToInt(ccxt.GetValue(market, "id"))
			}
		}
		var subaccount any = nil
		subaccountparamsVariable := this.HandleOptionAndParams(params, "unWatchPositions", "subaccount", "default")
		subaccount = ccxt.GetValue(subaccountparamsVariable, 0)
		params = ccxt.GetValue(subaccountparamsVariable, 1)
		var sender any = this.CreateSubaccount(this.WalletAddress, subaccount)
		var stream any = map[string]any{
			"type":       "position_change",
			"subaccount": sender,
			"product_id": productId,
		}

		retRes76615 := (<-this.UnWatchPrivate(stream, messageHash, params))
		ccxt.PanicOnError(retRes76615)
		ch <- retRes76615
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#createOrderWs
 * @description create a trade order over the v2 gateway WebSocket
 * @see https://docs.nado.xyz/developer-resources/api/gateway/websocket-v2
 * @see https://docs.nado.xyz/developer-resources/api/gateway/executes/place-order
 * @param {string} symbol unified symbol of the market to create an order in
 * @param {string} type must be 'limit'
 * @param {string} side 'buy' or 'sell'
 * @param {float} amount how much of currency you want to trade in units of base currency
 * @param {float} [price] the price at which the order is to be fulfilled, in units of the quote currency
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.subaccount] the 12-byte subaccount identifier, defaults to 'default'
 * @param {string|int} [params.expiration] order expiration timestamp in seconds, defaults to 4294967295
 * @param {string|int} [params.appendix] pre-encoded order appendix
 * @param {boolean} [params.reduceOnly] true if the order should only reduce position
 * @param {boolean} [params.postOnly] true to create a post-only order
 * @param {string} [params.timeInForce] 'GTC', 'IOC', 'FOK', or 'PO'
 * @param {boolean} [params.spotLeverage] whether leverage should be used for spot, defaults to true, exchange-specific alias params.spot_leverage
 * @param {int} [params.id] client-provided request id used to correlate the out-of-order v2 response, autogenerated when omitted
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/#/?id=order-structure}
 */
func (this *NadoCore) CreateOrderWs(symbol any, typeVar any, side any, amount any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		price := ccxt.GetArg(optionalArgs, 0, nil)
		_ = price
		params := ccxt.GetArg(optionalArgs, 1, map[string]any{})
		_ = params
		this.CheckRequiredCredentials()

		retRes7938 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes7938)
		var market any = this.Market(symbol)
		params = this.Extend(map[string]any{
			"id": this.RequestId(),
		}, params)
		var requestIdString any = this.SafeString(params, "id")
		if ccxt.IsTrue(ccxt.IsEqual(requestIdString, nil)) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " ws execute requires params.id")))
		}

		request := (<-this.CreateOrderRequest(symbol, typeVar, side, amount, price, params))
		ccxt.PanicOnError(request)
		var placeOrder any = this.SafeDict(request, "place_order", map[string]any{})
		if ccxt.IsTrue(ccxt.InOp(placeOrder, "trigger")) {
			panic(ccxt.NotSupported(ccxt.Add(this.Id, " createOrderWs() does not support trigger orders, use createOrder() instead")))
		}
		if ccxt.IsTrue(ccxt.IsEqual(requestIdString, nil)) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " requires params.id")))
		}

		response := (<-this.WatchExecuteRequest(requestIdString, request))
		ccxt.PanicOnError(response)

		//
		//     {
		//         "status": "success",
		//         "signature": "0x...",
		//         "data": {
		//             "digest": "0x..."
		//         },
		//         "request_type": "execute_place_order",
		//         "id": 100
		//     }
		//
		ch <- this.ParseOrder(this.Extend(map[string]any{
			"place_order": placeOrder,
		}, response), market)
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#editOrderWs
 * @description edit a trade order over the v2 gateway WebSocket
 * @see https://docs.nado.xyz/developer-resources/api/gateway/websocket-v2
 * @see https://docs.nado.xyz/developer-resources/api/gateway/executes/cancel-and-place
 * @param {string} id order id
 * @param {string} symbol unified symbol of the market to edit an order in
 * @param {string} type must be 'limit'
 * @param {string} side 'buy' or 'sell'
 * @param {float} amount how much of currency you want to trade in units of base currency
 * @param {float} [price] the price at which the order is to be fulfilled, in units of the quote currency
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.subaccount] the 12-byte subaccount identifier, defaults to 'default'
 * @param {string|int} [params.expiration] order expiration timestamp in seconds, defaults to 4294967295
 * @param {string|int} [params.appendix] pre-encoded order appendix
 * @param {boolean} [params.reduceOnly] true if the order should only reduce position
 * @param {boolean} [params.postOnly] true to create a post-only order
 * @param {string} [params.timeInForce] 'GTC', 'IOC', 'FOK', or 'PO'
 * @param {boolean} [params.spotLeverage] whether leverage should be used for spot, defaults to true, exchange-specific alias params.spot_leverage
 * @param {boolean} [params.placeRequiresUnfilled] when true, aborts the new order if the canceled order had partial fills or the cancel failed, exchange-specific alias params.place_requires_unfilled, defaults to true
 * @param {int} [params.id] client-provided request id used to correlate the out-of-order v2 response, autogenerated when omitted
 * @returns {object} an [order structure]{@link https://docs.ccxt.com/#/?id=order-structure}
 */
func (this *NadoCore) EditOrderWs(id any, symbol any, typeVar any, side any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		amount := ccxt.GetArg(optionalArgs, 0, nil)
		_ = amount
		price := ccxt.GetArg(optionalArgs, 1, nil)
		_ = price
		params := ccxt.GetArg(optionalArgs, 2, map[string]any{})
		_ = params
		this.CheckRequiredCredentials()

		retRes8498 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes8498)
		var market any = this.Market(symbol)
		// for cancel_and_place the request id is echoed from the nested place_order object
		params = this.Extend(map[string]any{
			"id": this.RequestId(),
		}, params)
		var requestIdString any = this.SafeString(params, "id")
		if ccxt.IsTrue(ccxt.IsEqual(requestIdString, nil)) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " ws execute requires params.id")))
		}

		request := (<-this.EditOrderRequest(id, symbol, typeVar, side, amount, price, params))
		ccxt.PanicOnError(request)
		if ccxt.IsTrue(ccxt.IsEqual(requestIdString, nil)) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " requires params.id")))
		}

		response := (<-this.WatchExecuteRequest(requestIdString, request))
		ccxt.PanicOnError(response)
		//
		//     {
		//         "status": "success",
		//         "signature": "0x...",
		//         "data": {
		//             "digest": "0x..."
		//         },
		//         "request_type": "execute_cancel_and_place",
		//         "id": 100
		//     }
		//
		var cancelAndPlace any = this.SafeDict(request, "cancel_and_place", map[string]any{})
		var placeOrder any = this.SafeDict(cancelAndPlace, "place_order", map[string]any{})

		ch <- this.ParseOrder(this.Extend(map[string]any{
			"place_order": placeOrder,
		}, response), market)
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#cancelOrderWs
 * @description cancels an open order over the v2 gateway WebSocket
 * @see https://docs.nado.xyz/developer-resources/api/gateway/websocket-v2
 * @see https://docs.nado.xyz/developer-resources/api/gateway/executes/cancel-orders
 * @param {string} id order id
 * @param {string} symbol unified symbol of the market the order was made in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.subaccount] the 12-byte subaccount identifier, defaults to 'default'
 * @param {string} [params.requiredUnfilledAmount] cancel only if the order's absolute remaining unfilled amount matches this amount, exchange-specific raw x18 alias params.required_unfilled_amount
 * @param {int} [params.id] client-provided request id used to correlate the out-of-order v2 response, autogenerated when omitted
 * @returns {object} An [order structure]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *NadoCore) CancelOrderWs(id any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		symbol := ccxt.GetArg(optionalArgs, 0, nil)
		_ = symbol
		params := ccxt.GetArg(optionalArgs, 1, map[string]any{})
		_ = params

		orders := (<-this.CancelOrdersWs([]any{id}, symbol, params))
		ccxt.PanicOnError(orders)

		ch <- this.SafeDict(orders, 0)
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#cancelOrdersWs
 * @description cancel multiple orders over the v2 gateway WebSocket
 * @see https://docs.nado.xyz/developer-resources/api/gateway/websocket-v2
 * @see https://docs.nado.xyz/developer-resources/api/gateway/executes/cancel-orders
 * @param {string[]} ids order ids
 * @param {string} symbol unified market symbol
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.subaccount] the 12-byte subaccount identifier, defaults to 'default'
 * @param {string} [params.requiredUnfilledAmount] cancel only if the order's absolute remaining unfilled amount matches this amount, exchange-specific raw x18 alias params.required_unfilled_amount
 * @param {int} [params.id] client-provided request id used to correlate the out-of-order v2 response, autogenerated when omitted
 * @returns {object[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *NadoCore) CancelOrdersWs(ids any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		symbol := ccxt.GetArg(optionalArgs, 0, nil)
		_ = symbol
		params := ccxt.GetArg(optionalArgs, 1, map[string]any{})
		_ = params
		this.CheckRequiredCredentials()
		if ccxt.IsTrue(ccxt.IsEqual(symbol, nil)) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " cancelOrdersWs() requires a symbol argument")))
		}

		retRes9168 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes9168)
		var market any = this.Market(symbol)
		var trigger any = this.SafeBool2(params, "stop", "trigger")
		if ccxt.IsTrue(trigger) {
			panic(ccxt.NotSupported(ccxt.Add(this.Id, " cancelOrdersWs() does not support trigger orders, use cancelOrders() instead")))
		}
		params = this.Extend(map[string]any{
			"id": this.RequestId(),
		}, params)
		var requestIdString any = this.SafeString(params, "id")
		if ccxt.IsTrue(ccxt.IsEqual(requestIdString, nil)) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " ws execute requires params.id")))
		}

		request := (<-this.CancelOrdersRequest(ids, symbol, params))
		ccxt.PanicOnError(request)
		if ccxt.IsTrue(ccxt.IsEqual(requestIdString, nil)) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " requires params.id")))
		}

		response := (<-this.WatchExecuteRequest(requestIdString, request))
		ccxt.PanicOnError(response)
		//
		//     {
		//         "status": "success",
		//         "signature": "0x...",
		//         "data": {
		//             "cancelled_orders": []
		//         },
		//         "request_type": "execute_cancel_orders",
		//         "id": 100
		//     }
		//
		var data any = this.SafeDict(response, "data", map[string]any{})
		var cancelledOrders any = this.SafeList(data, "cancelled_orders", []any{})
		var result any = []any{}
		for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(cancelledOrders)); i++ {
			ccxt.AppendToArray(&result, this.ParseOrder(this.Extend(map[string]any{
				"status": "canceled",
			}, ccxt.GetValue(cancelledOrders, i)), market))
		}

		ch <- result
		return nil

	}()
	return ch
}

/**
 * @method
 * @name nado#cancelAllOrdersWs
 * @description cancel all open orders over the v2 gateway WebSocket
 * @see https://docs.nado.xyz/developer-resources/api/gateway/websocket-v2
 * @see https://docs.nado.xyz/developer-resources/api/gateway/executes/cancel-product-orders
 * @param {string} [symbol] unified market symbol, when undefined all orders for all products are canceled
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.subaccount] the 12-byte subaccount identifier, defaults to 'default'
 * @param {int} [params.id] client-provided request id used to correlate the out-of-order v2 response, autogenerated when omitted
 * @returns {object[]} a list of [order structures]{@link https://docs.ccxt.com/?id=order-structure}
 */
func (this *NadoCore) CancelAllOrdersWs(optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		symbol := ccxt.GetArg(optionalArgs, 0, nil)
		_ = symbol
		params := ccxt.GetArg(optionalArgs, 1, map[string]any{})
		_ = params
		this.CheckRequiredCredentials()

		retRes9668 := (<-this.LoadMarkets())
		ccxt.PanicOnError(retRes9668)
		var market any = nil
		if ccxt.IsTrue(!ccxt.IsEqual(symbol, nil)) {
			market = this.Market(symbol)
		}
		var trigger any = this.SafeBool2(params, "stop", "trigger")
		if ccxt.IsTrue(trigger) {
			panic(ccxt.NotSupported(ccxt.Add(this.Id, " cancelAllOrdersWs() does not support trigger orders, use cancelAllOrders() instead")))
		}
		params = this.Extend(map[string]any{
			"id": this.RequestId(),
		}, params)
		var requestIdString any = this.SafeString(params, "id")
		if ccxt.IsTrue(ccxt.IsEqual(requestIdString, nil)) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " ws execute requires params.id")))
		}

		request := (<-this.CancelAllOrdersRequest(symbol, params))
		ccxt.PanicOnError(request)
		if ccxt.IsTrue(ccxt.IsEqual(requestIdString, nil)) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " requires params.id")))
		}

		response := (<-this.WatchExecuteRequest(requestIdString, request))
		ccxt.PanicOnError(response)
		var data any = this.SafeDict(response, "data", map[string]any{})
		var cancelledOrders any = this.SafeList(data, "cancelled_orders", []any{})
		var result any = []any{}
		for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(cancelledOrders)); i++ {
			ccxt.AppendToArray(&result, this.ParseOrder(this.Extend(map[string]any{
				"status": "canceled",
			}, ccxt.GetValue(cancelledOrders, i)), market))
		}

		ch <- result
		return nil

	}()
	return ch
}
func (this *NadoCore) WatchExecuteRequest(requestIdString any, request any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		// the v2 gateway dispatches requests concurrently, so responses arrive
		// in completion order, not send order — every execute carries a unique
		// request id and its response is correlated by the echoed id
		if ccxt.IsTrue(ccxt.IsEqual(requestIdString, nil)) {
			panic(ccxt.ArgumentsRequired(ccxt.Add(this.Id, " watchExecuteRequest() requires requestIdString")))
		}
		var url any = ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(this.Urls, "api"), "ws"), "gateway")
		var messageHash any = ccxt.Add("execute:", requestIdString)

		retRes100315 := (<-this.Watch(url, messageHash, request, messageHash))
		ccxt.PanicOnError(retRes100315)
		ch <- retRes100315
		return nil

	}()
	return ch
}
func (this *NadoCore) WatchPublic(streamType any, market any, messageHash any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		params := ccxt.GetArg(optionalArgs, 0, map[string]any{})
		_ = params
		var url any = ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(this.Urls, "api"), "ws"), "subscriptions")
		var stream any = map[string]any{
			"type": streamType,
		}
		if ccxt.IsTrue(!ccxt.IsEqual(market, nil)) {
			ccxt.AddElementToObject(stream, "product_id", this.ParseToInt(ccxt.GetValue(market, "id")))
		}
		var request any = map[string]any{
			"method": "subscribe",
			"stream": this.DeepExtend(stream, params),
			"id":     this.RequestId(),
		}
		var subscribeHash any = ccxt.Add("subscribe:", this.Json(ccxt.GetValue(request, "stream")))
		var subscription any = map[string]any{
			"streamType": streamType,
			"symbol":     this.SafeString(market, "symbol"),
		}
		var client any = this.Client(url)
		var clientSubscription any = this.SafeValue(client.(ccxt.ClientInterface).GetSubscriptions(), subscribeHash)
		if ccxt.IsTrue(ccxt.IsEqual(clientSubscription, nil)) {
			var id any = this.SafeString(request, "id")
			ccxt.AddElementToObject(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("subscription:", id), map[string]any{
				"subscribeHash": subscribeHash,
			})
			this.WatchMultiple(url, []any{subscribeHash}, request, []any{subscribeHash}, subscription)
		}

		retRes103315 := (<-this.Watch(url, messageHash))
		ccxt.PanicOnError(retRes103315)
		ch <- retRes103315
		return nil

	}()
	return ch
}
func (this *NadoCore) WatchPrivate(streamType any, stream any, messageHash any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		params := ccxt.GetArg(optionalArgs, 0, map[string]any{})
		_ = params
		var url any = ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(this.Urls, "api"), "ws"), "subscriptions")
		var client any = this.Client(url)
		var clientSubscription any = this.SafeValue(client.(ccxt.ClientInterface).GetSubscriptions(), messageHash)
		if ccxt.IsTrue(!ccxt.IsEqual(clientSubscription, nil)) {

			retRes104119 := (<-this.Watch(url, messageHash))
			ccxt.PanicOnError(retRes104119)
			ch <- retRes104119
			return nil
		}
		var id any = this.RequestId()
		var subscribeHash any = ccxt.Add("subscribe:", messageHash)
		var request any = map[string]any{
			"method": "subscribe",
			"stream": this.DeepExtend(stream, params),
			"id":     id,
		}
		var subscription any = map[string]any{
			"streamType": streamType,
		}
		ccxt.AddElementToObject(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("subscription:", this.NumberToString(id)), map[string]any{
			"subscribeHash": subscribeHash,
		})
		this.WatchMultiple(url, []any{subscribeHash}, request, []any{messageHash}, subscription)

		retRes105715 := (<-this.Watch(url, messageHash))
		ccxt.PanicOnError(retRes105715)
		ch <- retRes105715
		return nil

	}()
	return ch
}
func (this *NadoCore) UnWatchPrivate(stream any, messageHash any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		params := ccxt.GetArg(optionalArgs, 0, map[string]any{})
		_ = params
		var url any = ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(this.Urls, "api"), "ws"), "subscriptions")
		var id any = this.RequestId()
		var unsubscribeHash any = ccxt.Add("unsubscribe:", messageHash)
		var request any = map[string]any{
			"method": "unsubscribe",
			"stream": this.DeepExtend(stream, params),
			"id":     id,
		}
		var subscription any = map[string]any{
			"id":          id,
			"messageHash": messageHash,
		}
		var client any = this.Client(url)
		ccxt.AddElementToObject(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("unsubscription:", this.NumberToString(id)), map[string]any{
			"messageHash":     messageHash,
			"unsubscribeHash": unsubscribeHash,
		})

		retRes107815 := (<-this.Watch(url, unsubscribeHash, request, unsubscribeHash, subscription))
		ccxt.PanicOnError(retRes107815)
		ch <- retRes107815
		return nil

	}()
	return ch
}
func (this *NadoCore) Authenticate(optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		params := ccxt.GetArg(optionalArgs, 0, map[string]any{})
		_ = params
		this.CheckRequiredCredentials()
		var url any = ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(this.Urls, "api"), "ws"), "subscriptions")
		var client any = this.Client(url)
		var messageHash any = "authenticated"
		var authenticated any = this.SafeValue(client.(ccxt.ClientInterface).GetSubscriptions(), messageHash)
		if ccxt.IsTrue(!ccxt.IsEqual(authenticated, nil)) {
			var future any = this.SafeValue(client.(ccxt.ClientInterface).GetFutures(), messageHash)
			if ccxt.IsTrue(!ccxt.IsEqual(future, nil)) {

				retRes109023 := <-future.(*ccxt.Future).Await()
				ccxt.PanicOnError(retRes109023)
				ch <- retRes109023
				return nil
			}

			ch <- authenticated
			return nil
		}
		var recvWindow any = nil
		recvWindowparamsVariable := this.HandleOptionAndParams(params, "authenticate", "recvWindow", 5000)
		recvWindow = ccxt.GetValue(recvWindowparamsVariable, 0)
		params = ccxt.GetValue(recvWindowparamsVariable, 1)
		var subaccount any = nil
		subaccountparamsVariable := this.HandleOptionAndParams(params, "authenticate", "subaccount", "default")
		subaccount = ccxt.GetValue(subaccountparamsVariable, 0)
		params = ccxt.GetValue(subaccountparamsVariable, 1)
		var id any = this.RequestId()
		var sender any = this.CreateSubaccount(this.WalletAddress, subaccount)
		var expiration any = this.Sum(this.Milliseconds(), recvWindow)
		var tx any = map[string]any{
			"sender":     sender,
			"expiration": this.NumberToString(expiration),
		}

		contracts := (<-this.QueryContracts())
		ccxt.PanicOnError(contracts)
		var chainId any = this.SafeString(contracts, "chain_id")
		var endpointAddress any = this.SafeString(contracts, "endpoint_addr")
		if ccxt.IsTrue(ccxt.IsEqual(endpointAddress, nil)) {
			panic(ccxt.ExchangeError(ccxt.Add(this.Id, " authenticate() requires endpoint_addr from contracts query")))
		}
		var signature any = this.SignStreamAuthentication(tx, chainId, endpointAddress)
		var request any = map[string]any{
			"method":    "authenticate",
			"id":        id,
			"tx":        tx,
			"signature": signature,
		}
		ccxt.AddElementToObject(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("authentication:", this.NumberToString(id)), messageHash)

		retRes111915 := (<-this.Watch(url, messageHash, this.Extend(request, params), messageHash))
		ccxt.PanicOnError(retRes111915)
		ch <- retRes111915
		return nil

	}()
	return ch
}
func (this *NadoCore) SignStreamAuthentication(tx any, chainId any, endpointAddress any) any {
	var domain any = map[string]any{
		"name":              "Nado",
		"version":           "0.0.1",
		"chainId":           chainId,
		"verifyingContract": endpointAddress,
	}
	var messageTypes any = map[string]any{
		"StreamAuthentication": []any{map[string]any{
			"name": "sender",
			"type": "bytes32",
		}, map[string]any{
			"name": "expiration",
			"type": "uint64",
		}},
	}
	var encoded any = this.EthEncodeStructuredData(domain, messageTypes, tx)
	var hash any = ccxt.Add("0x", this.Hash(encoded, ccxt.Keccak, "hex"))
	return this.SignHash(hash, this.PrivateKey)
}
func (this *NadoCore) CreatePublicSubscriptionRequest(method any, streamType any, optionalArgs ...any) any {
	market := ccxt.GetArg(optionalArgs, 0, nil)
	_ = market
	id := ccxt.GetArg(optionalArgs, 1, nil)
	_ = id
	params := ccxt.GetArg(optionalArgs, 2, map[string]any{})
	_ = params
	var stream any = map[string]any{
		"type": streamType,
	}
	if ccxt.IsTrue(!ccxt.IsEqual(market, nil)) {
		ccxt.AddElementToObject(stream, "product_id", this.ParseToInt(ccxt.GetValue(market, "id")))
	}
	return map[string]any{
		"method": method,
		"stream": this.DeepExtend(stream, params),
		"id":     id,
	}
}
func (this *NadoCore) WatchPublicMultiple(streamType any, markets any, messageHashes any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		params := ccxt.GetArg(optionalArgs, 0, map[string]any{})
		_ = params
		subscriptionParams := ccxt.GetArg(optionalArgs, 1, nil)
		_ = subscriptionParams
		var url any = ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(this.Urls, "api"), "ws"), "subscriptions")
		var client any = this.Client(url)
		for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(messageHashes)); i++ {
			var messageHash any = ccxt.GetValue(messageHashes, i)
			var clientSubscription any = this.SafeValue(client.(ccxt.ClientInterface).GetSubscriptions(), messageHash)
			if ccxt.IsTrue(ccxt.IsEqual(clientSubscription, nil)) {
				var market any = ccxt.GetValue(markets, i)
				var id any = this.RequestId()
				var requestParams any = ccxt.Ternary(ccxt.IsTrue((ccxt.IsEqual(subscriptionParams, nil))), params, ccxt.GetValue(subscriptionParams, i))
				var request any = this.CreatePublicSubscriptionRequest("subscribe", streamType, market, id, requestParams)
				var subscribeHash any = ccxt.Add("subscribe:", this.Json(ccxt.GetValue(request, "stream")))
				var streamSubscription any = this.SafeValue(client.(ccxt.ClientInterface).GetSubscriptions(), subscribeHash)
				if ccxt.IsTrue(ccxt.IsEqual(streamSubscription, nil)) {
					var subscription any = map[string]any{
						"streamType": streamType,
						"symbol":     this.SafeString(market, "symbol"),
					}
					ccxt.AddElementToObject(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("subscription:", this.NumberToString(id)), map[string]any{
						"subscribeHash": subscribeHash,
					})
					this.WatchMultiple(url, []any{subscribeHash}, request, []any{subscribeHash}, subscription)
				}
			}
		}

		retRes117915 := (<-this.WatchMultiple(url, messageHashes, nil, messageHashes))
		ccxt.PanicOnError(retRes117915)
		ch <- retRes117915
		return nil

	}()
	return ch
}
func (this *NadoCore) UnWatchPublic(streamType any, market any, messageHash any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		params := ccxt.GetArg(optionalArgs, 0, map[string]any{})
		_ = params
		var url any = ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(this.Urls, "api"), "ws"), "subscriptions")
		var id any = this.RequestId()
		var request any = this.CreatePublicSubscriptionRequest("unsubscribe", streamType, market, id, params)
		var subscription any = map[string]any{
			"id":          id,
			"messageHash": messageHash,
		}
		var unsubscribeHash any = ccxt.Add("unsubscribe:", messageHash)
		var client any = this.Client(url)
		ccxt.AddElementToObject(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("unsubscription:", this.NumberToString(id)), map[string]any{
			"messageHash":     messageHash,
			"unsubscribeHash": unsubscribeHash,
		})

		retRes119615 := (<-this.Watch(url, unsubscribeHash, request, unsubscribeHash, subscription))
		ccxt.PanicOnError(retRes119615)
		ch <- retRes119615
		return nil

	}()
	return ch
}
func (this *NadoCore) UnWatchPublicMultiple(streamType any, markets any, messageHashes any, optionalArgs ...any) <-chan any {
	ch := make(chan any)
	go func() any {
		defer close(ch)
		defer ccxt.ReturnPanicError(ch)
		params := ccxt.GetArg(optionalArgs, 0, map[string]any{})
		_ = params
		subscriptionParams := ccxt.GetArg(optionalArgs, 1, nil)
		_ = subscriptionParams
		var url any = ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(this.Urls, "api"), "ws"), "subscriptions")
		var client any = this.Client(url)
		var results any = []any{}
		for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(messageHashes)); i++ {
			var messageHash any = ccxt.GetValue(messageHashes, i)
			var id any = this.RequestId()
			var unsubscribeHash any = ccxt.Add("unsubscribe:", messageHash)
			var requestParams any = ccxt.Ternary(ccxt.IsTrue((ccxt.IsEqual(subscriptionParams, nil))), params, ccxt.GetValue(subscriptionParams, i))
			var request any = this.CreatePublicSubscriptionRequest("unsubscribe", streamType, ccxt.GetValue(markets, i), id, requestParams)
			var subscription any = map[string]any{
				"id":          id,
				"messageHash": messageHash,
			}
			ccxt.AddElementToObject(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("unsubscription:", this.NumberToString(id)), map[string]any{
				"messageHash":     messageHash,
				"unsubscribeHash": unsubscribeHash,
			})
			ccxt.AppendToArray(&results, (<-this.WatchMultiple(url, []any{unsubscribeHash}, request, []any{unsubscribeHash}, subscription)))
		}

		ch <- results
		return nil

	}()
	return ch
}
func (this *NadoCore) ParseWsTimestamp(message any, key any) any {
	var value any = this.SafeString(message, key)
	if ccxt.IsTrue(ccxt.IsEqual(value, nil)) {
		return nil
	}
	var length any = ccxt.GetLength(value)
	if ccxt.IsTrue(ccxt.IsGreaterThan(length, 13)) {
		return this.ParseToInt(ccxt.Slice(value, 0, ccxt.Subtract(length, 6)))
	}
	return this.SafeInteger(message, key)
}
func (this *NadoCore) ParseWsTrade(trade any, optionalArgs ...any) any {
	//
	//     {
	//         "type": "trade",
	//         "timestamp": "1676151190656903000",
	//         "product_id": 1,
	//         "price": "25000000000000000000000",
	//         "taker_qty": "1000000000000000000",
	//         "maker_qty": "1000000000000000000",
	//         "is_taker_buyer": true
	//     }
	//
	market := ccxt.GetArg(optionalArgs, 0, nil)
	_ = market
	var marketId any = this.SafeString(trade, "product_id")
	market = this.SafeMarket(marketId, market)
	var timestamp any = this.ParseWsTimestamp(trade, "timestamp")
	var isTakerBuyer any = this.SafeBool(trade, "is_taker_buyer")
	var side any = nil
	if ccxt.IsTrue(!ccxt.IsEqual(isTakerBuyer, nil)) {
		side = ccxt.Ternary(ccxt.IsTrue(isTakerBuyer), "buy", "sell")
	}
	return this.SafeTrade(map[string]any{
		"info":         trade,
		"id":           nil,
		"timestamp":    timestamp,
		"datetime":     this.Iso8601(timestamp),
		"symbol":       ccxt.GetValue(market, "symbol"),
		"order":        nil,
		"type":         nil,
		"side":         side,
		"takerOrMaker": "taker",
		"price":        this.ParseX18(this.SafeString(trade, "price")),
		"amount":       this.ParseX18(this.SafeString(trade, "taker_qty")),
		"cost":         nil,
		"fee":          nil,
	}, market)
}
func (this *NadoCore) ParseWsMyTrade(trade any, optionalArgs ...any) any {
	//
	//     {
	//         "type": "fill",
	//         "timestamp": "1695081920633151000",
	//         "product_id": 1,
	//         "subaccount": "0x...",
	//         "order_digest": "0x...",
	//         "appendix": "1",
	//         "filled_qty": "18000000000000000",
	//         "remaining_qty": "82000000000000000",
	//         "original_qty": "100000000000000000",
	//         "price": "25000000000000000000000",
	//         "is_taker": true,
	//         "is_bid": true,
	//         "fee": "4500000000000000",
	//         "submission_idx": 1,
	//         "id": 100
	//     }
	//
	market := ccxt.GetArg(optionalArgs, 0, nil)
	_ = market
	var marketId any = this.SafeString(trade, "product_id")
	market = this.SafeMarket(marketId, market)
	var timestamp any = this.ParseWsTimestamp(trade, "timestamp")
	var isBid any = this.SafeBool(trade, "is_bid")
	var side any = nil
	if ccxt.IsTrue(!ccxt.IsEqual(isBid, nil)) {
		side = ccxt.Ternary(ccxt.IsTrue(isBid), "buy", "sell")
	}
	var isTaker any = this.SafeBool(trade, "is_taker")
	var takerOrMaker any = nil
	if ccxt.IsTrue(!ccxt.IsEqual(isTaker, nil)) {
		takerOrMaker = ccxt.Ternary(ccxt.IsTrue(isTaker), "taker", "maker")
	}
	var feeCost any = this.ParseX18(this.SafeString(trade, "fee"))
	var fee any = nil
	if ccxt.IsTrue(!ccxt.IsEqual(feeCost, nil)) {
		fee = map[string]any{
			"cost":     feeCost,
			"currency": ccxt.GetValue(market, "quote"),
		}
	}
	return this.SafeTrade(map[string]any{
		"info":         trade,
		"id":           this.SafeString2(trade, "id", "submission_idx"),
		"timestamp":    timestamp,
		"datetime":     this.Iso8601(timestamp),
		"symbol":       ccxt.GetValue(market, "symbol"),
		"order":        this.SafeString(trade, "order_digest"),
		"type":         nil,
		"side":         side,
		"takerOrMaker": takerOrMaker,
		"price":        this.ParseX18(this.SafeString(trade, "price")),
		"amount":       this.ParseX18(this.SafeString(trade, "filled_qty")),
		"cost":         nil,
		"fee":          fee,
	}, market)
}
func (this *NadoCore) HandleTrade(client any, message any) {
	var marketId any = this.SafeString(message, "product_id")
	var market any = this.SafeMarket(marketId)
	var symbol any = ccxt.GetValue(market, "symbol")
	var messageHash any = ccxt.Add("trade:", symbol)
	var trades any = this.SafeValue(this.Trades, symbol)
	if ccxt.IsTrue(ccxt.IsEqual(trades, nil)) {
		var limit any = this.SafeInteger(this.Options, "tradesLimit", 1000)
		trades = ccxt.NewArrayCache(limit)
		ccxt.AddElementToObject(this.Trades, symbol, trades)
	}
	var trade any = this.ParseWsTrade(message, market)
	trades.(ccxt.Appender).Append(trade)
	client.(ccxt.ClientInterface).Resolve(trades, messageHash)
}
func (this *NadoCore) HandleMyTrade(client any, message any) {
	var trade any = this.ParseWsMyTrade(message)
	if ccxt.IsTrue(ccxt.IsEqual(this.MyTrades, nil)) {
		var limit any = this.SafeInteger(this.Options, "tradesLimit", 1000)
		this.MyTrades = ccxt.NewArrayCacheBySymbolById(limit)
	}
	var trades any = this.MyTrades
	trades.(ccxt.Appender).Append(trade)
	var symbol any = ccxt.GetValue(trade, "symbol")
	client.(ccxt.ClientInterface).Resolve(trades, "myTrades")
	client.(ccxt.ClientInterface).Resolve(trades, ccxt.Add("myTrades:", symbol))
}
func (this *NadoCore) HandleOHLCV(client any, message any) {
	//
	//     {
	//         "type": "latest_candlestick",
	//         "timestamp": "1782179760",
	//         "product_id": 2,
	//         "granularity": 60,
	//         "open_x18": "64148000000000000000000",
	//         "high_x18": "64148000000000000000000",
	//         "low_x18": "64148000000000000000000",
	//         "close_x18": "64148000000000000000000",
	//         "volume": "24250000000000000"
	//     }
	//
	var marketId any = this.SafeString(message, "product_id")
	var market any = this.SafeMarket(marketId)
	var symbol any = ccxt.GetValue(market, "symbol")
	var granularity any = this.SafeInteger(message, "granularity")
	var timeframe any = this.FindTimeframe(granularity)
	if ccxt.IsTrue(ccxt.IsEqual(timeframe, nil)) {
		return
	}
	if !ccxt.IsTrue((ccxt.InOp(this.Ohlcvs, symbol))) {
		ccxt.AddElementToObject(this.Ohlcvs, symbol, map[string]any{})
	}
	var stored any = this.SafeValue(ccxt.GetValue(this.Ohlcvs, symbol), timeframe)
	if ccxt.IsTrue(ccxt.IsEqual(stored, nil)) {
		var limit any = this.SafeInteger(this.Options, "OHLCVLimit", 1000)
		stored = ccxt.NewArrayCacheByTimestamp(limit)
		ccxt.AddElementToObject(ccxt.GetValue(this.Ohlcvs, symbol), timeframe, stored)
	}
	var parsed any = this.ParseOHLCV(message, market)
	stored.(ccxt.Appender).Append(parsed)
	var messageHash any = ccxt.Add(ccxt.Add(ccxt.Add("ohlcv:", timeframe), ":"), symbol)
	client.(ccxt.ClientInterface).Resolve([]any{symbol, timeframe, stored}, messageHash)
}
func (this *NadoCore) ParseWsOrder(order any, optionalArgs ...any) any {
	//
	//     {
	//         "type": "order_update",
	//         "timestamp": "1695081920633151000",
	//         "product_id": 1,
	//         "digest": "0xf7712b63ccf70358db8f201e9bf33977423e7a63f6a16f6dab180bdd580f7c6c",
	//         "amount": "82000000000000000",
	//         "reason": "filled",
	//         "filled_qty": "18000000000000000",
	//         "filled_price": "25000000000000000000000",
	//         "id": 100
	//     }
	//
	market := ccxt.GetArg(optionalArgs, 0, nil)
	_ = market
	var marketId any = this.SafeString(order, "product_id")
	market = this.SafeMarket(marketId, market)
	var timestamp any = this.ParseWsTimestamp(order, "timestamp")
	var id any = this.SafeString(order, "digest")
	var amountString any = this.SafeString(order, "amount")
	var remaining any = nil
	if ccxt.IsTrue(!ccxt.IsEqual(amountString, nil)) {
		remaining = this.ParseX18(amountString)
	}
	var filled any = this.ParseX18(this.SafeString(order, "filled_qty"))
	var average any = this.ParseX18(this.SafeString(order, "filled_price"))
	var reason any = this.SafeString(order, "reason")
	var status any = nil
	if ccxt.IsTrue(ccxt.IsEqual(reason, "placed")) {
		status = "open"
	} else if ccxt.IsTrue(ccxt.IsEqual(reason, "filled")) {
		status = "open"
		if ccxt.IsTrue(ccxt.IsTrue((!ccxt.IsEqual(amountString, nil))) && ccxt.IsTrue(ccxt.Precise.StringEq(amountString, "0"))) {
			status = "closed"
		}
	} else if ccxt.IsTrue(ccxt.IsEqual(reason, "cancelled")) {
		status = "canceled"
	}
	return this.SafeOrder(map[string]any{
		"info":                order,
		"id":                  id,
		"clientOrderId":       nil,
		"timestamp":           timestamp,
		"datetime":            this.Iso8601(timestamp),
		"lastTradeTimestamp":  ccxt.Ternary(ccxt.IsTrue((ccxt.IsEqual(filled, nil))), nil, timestamp),
		"lastUpdateTimestamp": timestamp,
		"symbol":              ccxt.GetValue(market, "symbol"),
		"type":                nil,
		"timeInForce":         nil,
		"postOnly":            nil,
		"side":                nil,
		"price":               nil,
		"stopPrice":           nil,
		"triggerPrice":        nil,
		"amount":              nil,
		"cost":                nil,
		"average":             average,
		"filled":              filled,
		"remaining":           remaining,
		"status":              status,
		"fee":                 nil,
		"trades":              nil,
	}, market)
}
func (this *NadoCore) HandleOrder(client any, message any) {
	var order any = this.ParseWsOrder(message)
	if ccxt.IsTrue(ccxt.IsEqual(this.Orders, nil)) {
		var limit any = this.SafeInteger(this.Options, "ordersLimit", 1000)
		this.Orders = ccxt.NewArrayCacheBySymbolById(limit)
	}
	var orders any = this.Orders
	orders.(ccxt.Appender).Append(order)
	var symbol any = ccxt.GetValue(order, "symbol")
	client.(ccxt.ClientInterface).Resolve(orders, "orders")
	client.(ccxt.ClientInterface).Resolve(orders, ccxt.Add("orders:", symbol))
}
func (this *NadoCore) ParseWsPosition(position any, optionalArgs ...any) any {
	//
	//     {
	//         "type": "position_change",
	//         "timestamp": "1695081920633151000",
	//         "product_id": 2,
	//         "subaccount": "0x15f43d1f2dee81424afd891943262aa90f22cc2a64656661756c740000000000",
	//         "isolated": false,
	//         "amount": "100000000000000000",
	//         "v_quote_amount": "-3033500000000000000000",
	//         "reason": "match_orders"
	//     }
	//
	market := ccxt.GetArg(optionalArgs, 0, nil)
	_ = market
	var marketId any = this.SafeString(position, "product_id")
	market = this.SafeMarket(marketId, market)
	var timestamp any = this.ParseWsTimestamp(position, "timestamp")
	var amountString any = this.SafeString(position, "amount")
	var vQuoteAmount any = this.SafeString(position, "v_quote_amount")
	var side any = nil
	var contracts any = nil
	var entryPrice any = nil
	if ccxt.IsTrue(!ccxt.IsEqual(amountString, nil)) {
		if ccxt.IsTrue(ccxt.Precise.StringGt(amountString, "0")) {
			side = "long"
		} else if ccxt.IsTrue(ccxt.Precise.StringLt(amountString, "0")) {
			side = "short"
		}
		var absoluteAmount any = ccxt.Precise.StringAbs(amountString)
		contracts = this.ParseX18(absoluteAmount)
		if ccxt.IsTrue(ccxt.IsTrue((!ccxt.IsEqual(vQuoteAmount, nil))) && !ccxt.IsTrue(ccxt.Precise.StringEquals(absoluteAmount, "0"))) {
			entryPrice = this.ParseNumber(ccxt.Precise.StringDiv(ccxt.Precise.StringAbs(vQuoteAmount), absoluteAmount))
		}
	}
	return this.SafePosition(map[string]any{
		"info":                        position,
		"id":                          nil,
		"symbol":                      ccxt.GetValue(market, "symbol"),
		"timestamp":                   timestamp,
		"datetime":                    this.Iso8601(timestamp),
		"isolated":                    this.SafeBool(position, "isolated"),
		"hedged":                      false,
		"side":                        side,
		"contracts":                   contracts,
		"contractSize":                this.SafeNumber(market, "contractSize"),
		"entryPrice":                  entryPrice,
		"markPrice":                   nil,
		"notional":                    nil,
		"leverage":                    nil,
		"collateral":                  nil,
		"initialMargin":               nil,
		"initialMarginPercentage":     nil,
		"maintenanceMargin":           nil,
		"maintenanceMarginPercentage": nil,
		"unrealizedPnl":               nil,
		"liquidationPrice":            nil,
		"marginMode":                  nil,
		"marginRatio":                 nil,
		"percentage":                  nil,
	})
}
func (this *NadoCore) HandlePosition(client any, message any) {
	var marketId any = this.SafeString(message, "product_id")
	var market any = this.SafeMarket(marketId)
	if !ccxt.IsTrue(this.SafeBool(market, "contract", false)) {
		return
	}
	var position any = this.ParseWsPosition(message, market)
	if ccxt.IsTrue(ccxt.IsEqual(this.Positions, nil)) {
		this.Positions = ccxt.NewArrayCacheBySymbolBySide()
	}
	var positions any = this.Positions
	var side any = this.SafeString(position, "side")
	if ccxt.IsTrue(ccxt.IsEqual(side, nil)) {
		var longPosition any = this.Extend(map[string]any{}, position)
		ccxt.AddElementToObject(longPosition, "side", "long")
		positions.(ccxt.Appender).Append(longPosition)
		var shortPosition any = this.Extend(map[string]any{}, position)
		ccxt.AddElementToObject(shortPosition, "side", "short")
		positions.(ccxt.Appender).Append(shortPosition)
	} else {
		positions.(ccxt.Appender).Append(position)
	}
	var symbol any = ccxt.GetValue(position, "symbol")
	client.(ccxt.ClientInterface).Resolve(positions, "positions")
	client.(ccxt.ClientInterface).Resolve(positions, ccxt.Add("positions:", symbol))
}
func (this *NadoCore) ParseWsBidAsk(bidask any, optionalArgs ...any) any {
	//
	//     {
	//         "type": "best_bid_offer",
	//         "timestamp": "1676151190656903000",
	//         "product_id": 1,
	//         "bid_price": "24990000000000000000000",
	//         "bid_qty": "5000000000000000000",
	//         "ask_price": "25010000000000000000000",
	//         "ask_qty": "3000000000000000000"
	//     }
	//
	market := ccxt.GetArg(optionalArgs, 0, nil)
	_ = market
	var marketId any = this.SafeString(bidask, "product_id")
	market = this.SafeMarket(marketId, market)
	var timestamp any = this.ParseWsTimestamp(bidask, "timestamp")
	return this.SafeTicker(map[string]any{
		"symbol":    ccxt.GetValue(market, "symbol"),
		"timestamp": timestamp,
		"datetime":  this.Iso8601(timestamp),
		"ask":       this.ParseX18(this.SafeString(bidask, "ask_price")),
		"askVolume": this.ParseX18(this.SafeString(bidask, "ask_qty")),
		"bid":       this.ParseX18(this.SafeString(bidask, "bid_price")),
		"bidVolume": this.ParseX18(this.SafeString(bidask, "bid_qty")),
		"info":      bidask,
	}, market)
}
func (this *NadoCore) HandleBidAsk(client any, message any) {
	var ticker any = this.ParseWsBidAsk(message)
	var symbol any = this.SafeString(ticker, "symbol")
	if ccxt.IsTrue(ccxt.IsEqual(symbol, nil)) {
		return
	}
	ccxt.AddElementToObject(this.Bidsasks, symbol, ticker)
	ccxt.AddElementToObject(this.Tickers, symbol, ticker)
	var tickers any = map[string]any{}
	ccxt.AddElementToObject(tickers, symbol, ticker)
	client.(ccxt.ClientInterface).Resolve(ticker, ccxt.Add("bidask:", symbol))
	client.(ccxt.ClientInterface).Resolve(ticker, ccxt.Add("ticker:", symbol))
	client.(ccxt.ClientInterface).Resolve(tickers, "bidask")
	client.(ccxt.ClientInterface).Resolve(tickers, "ticker")
}
func (this *NadoCore) ParseWsAllBidsAsks(message any) any {
	//
	//     {
	//         "type": "all_bbo",
	//         "time": "1781750134714",
	//         "bbos": {
	//             "2": { "bid": "64924000000000000000000", "ask": "64935000000000000000000" }
	//         }
	//     }
	//
	var timestamp any = this.SafeInteger(message, "time")
	var bbos any = this.SafeDict(message, "bbos", map[string]any{})
	var marketIds any = ccxt.ObjectKeys(bbos)
	var result any = map[string]any{}
	for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(marketIds)); i++ {
		var marketId any = ccxt.GetValue(marketIds, i)
		var market any = this.SafeMarket(marketId)
		var bbo any = this.SafeDict(bbos, marketId, map[string]any{})
		var bid any = this.SafeString(bbo, "bid")
		var ask any = this.SafeString(bbo, "ask")
		var maxPrice any = "170141183460469231731687303715884105727"
		if ccxt.IsTrue(ccxt.IsTrue(ccxt.IsTrue(ccxt.IsTrue(ccxt.Precise.StringGt(bid, "0")) && ccxt.IsTrue(ccxt.Precise.StringGt(ask, "0"))) && !ccxt.IsTrue(ccxt.Precise.StringEquals(bid, maxPrice))) && !ccxt.IsTrue(ccxt.Precise.StringEquals(ask, maxPrice))) {
			var ticker any = this.SafeTicker(map[string]any{
				"symbol":    ccxt.GetValue(market, "symbol"),
				"timestamp": timestamp,
				"datetime":  this.Iso8601(timestamp),
				"ask":       this.ParseX18(ask),
				"bid":       this.ParseX18(bid),
				"info":      bbo,
			}, market)
			var symbol any = ccxt.GetValue(market, "symbol")
			ccxt.AddElementToObject(result, symbol, ticker)
		}
	}
	return result
}
func (this *NadoCore) HandleAllBidsAsks(client any, message any) {
	var tickers any = this.ParseWsAllBidsAsks(message)
	var symbols any = ccxt.ObjectKeys(tickers)
	for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(symbols)); i++ {
		var symbol any = ccxt.GetValue(symbols, i)
		var ticker any = ccxt.GetValue(tickers, symbol)
		ccxt.AddElementToObject(this.Bidsasks, symbol, ticker)
		ccxt.AddElementToObject(this.Tickers, symbol, ticker)
		client.(ccxt.ClientInterface).Resolve(ticker, ccxt.Add("bidask:", symbol))
		client.(ccxt.ClientInterface).Resolve(ticker, ccxt.Add("ticker:", symbol))
	}
	client.(ccxt.ClientInterface).Resolve(tickers, "bidask")
	client.(ccxt.ClientInterface).Resolve(tickers, "ticker")
}
func (this *NadoCore) HandleDelta(bookside any, delta any) {
	var bidAsk any = []any{this.ParseX18(this.SafeString(delta, 0)), this.ParseX18(this.SafeString(delta, 1))}
	bookside.(ccxt.IOrderBookSide).StoreArray(bidAsk)
}
func (this *NadoCore) HandleOrderBook(client any, message any) {
	//
	//     {
	//         "type": "book_depth",
	//         "min_timestamp": "1683805381879572835",
	//         "max_timestamp": "1683805381879572835",
	//         "last_max_timestamp": "1683805381771464799",
	//         "product_id": 1,
	//         "bids": [["21594490000000000000000", "51007390115411548"]],
	//         "asks": [["21694490000000000000000", "0"]]
	//     }
	//
	var marketId any = this.SafeString(message, "product_id")
	var market any = this.SafeMarket(marketId)
	var symbol any = ccxt.GetValue(market, "symbol")
	if !ccxt.IsTrue((ccxt.InOp(this.Orderbooks, symbol))) {
		return
	}
	var orderbook any = ccxt.GetValue(this.Orderbooks, symbol)
	var messageHash any = ccxt.Add("orderbook:", symbol)
	var maxTimestamp any = this.SafeString(orderbook, "maxTimestamp")
	var lastMaxTimestamp any = this.SafeString(message, "last_max_timestamp")
	if ccxt.IsTrue(ccxt.IsTrue(ccxt.IsTrue((!ccxt.IsEqual(maxTimestamp, nil))) && ccxt.IsTrue((!ccxt.IsEqual(lastMaxTimestamp, nil)))) && ccxt.IsTrue((!ccxt.IsEqual(maxTimestamp, lastMaxTimestamp)))) {
		var subscriptions any = ccxt.ObjectKeys(client.(ccxt.ClientInterface).GetSubscriptions())
		for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(subscriptions)); i++ {
			var subscriptionHash any = ccxt.GetValue(subscriptions, i)
			var subscription any = this.SafeDict(client.(ccxt.ClientInterface).GetSubscriptions(), subscriptionHash)
			var streamType any = this.SafeString(subscription, "streamType")
			var subscriptionSymbol any = this.SafeString(subscription, "symbol")
			if ccxt.IsTrue(ccxt.IsTrue((ccxt.IsEqual(streamType, "book_depth"))) && ccxt.IsTrue((ccxt.IsEqual(subscriptionSymbol, symbol)))) {
				ccxt.Remove(client.(ccxt.ClientInterface).GetSubscriptions(), subscriptionHash)
			}
		}
		ccxt.Remove(client.(ccxt.ClientInterface).GetSubscriptions(), messageHash)
		ccxt.Remove(this.Orderbooks, symbol)
		error := ccxt.InvalidNonce(ccxt.Add(this.Id, " watchOrderBook received invalid nonce"))
		client.(ccxt.ClientInterface).Reject(error, messageHash)
		return
	}
	var asks any = this.SafeList(message, "asks", []any{})
	var bids any = this.SafeList(message, "bids", []any{})
	this.HandleDeltas(ccxt.GetValue(orderbook, "asks"), asks)
	this.HandleDeltas(ccxt.GetValue(orderbook, "bids"), bids)
	var timestamp any = this.ParseWsTimestamp(message, "max_timestamp")
	ccxt.AddElementToObject(orderbook, "symbol", symbol)
	ccxt.AddElementToObject(orderbook, "timestamp", timestamp)
	ccxt.AddElementToObject(orderbook, "datetime", this.Iso8601(timestamp))
	ccxt.AddElementToObject(orderbook, "maxTimestamp", this.SafeString(message, "max_timestamp"))
	client.(ccxt.ClientInterface).Resolve(orderbook, messageHash)
}
func (this *NadoCore) HandleExecuteResponse(client any, message any) {
	//
	//     {
	//         "status": "success",
	//         "signature": "0x...",
	//         "data": {
	//             "digest": "0x..."
	//         },
	//         "request_type": "execute_place_order",
	//         "id": 100
	//     }
	//
	var id any = this.SafeString(message, "id")
	if ccxt.IsTrue(ccxt.IsEqual(id, nil)) {
		return
	}
	var messageHash any = ccxt.Add("execute:", id)
	var subscription any = this.SafeValue(client.(ccxt.ClientInterface).GetSubscriptions(), messageHash)
	if ccxt.IsTrue(!ccxt.IsEqual(subscription, nil)) {
		ccxt.Remove(client.(ccxt.ClientInterface).GetSubscriptions(), messageHash)
	}
	client.(ccxt.ClientInterface).Resolve(message, messageHash)
}
func (this *NadoCore) HandleSubscription(client any, message any) {
	var id any = this.SafeString(message, "id")
	var subscription any = this.SafeDict(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("subscription:", id))
	if ccxt.IsTrue(!ccxt.IsEqual(subscription, nil)) {
		var subscribeHash any = this.SafeString(subscription, "subscribeHash")
		ccxt.Remove(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("subscription:", id))
		client.(ccxt.ClientInterface).Resolve(message, subscribeHash)
	}
}
func (this *NadoCore) HandleAuthentication(client any, message any) {
	var id any = this.SafeString(message, "id")
	var messageHash any = this.SafeString(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("authentication:", id))
	if ccxt.IsTrue(!ccxt.IsEqual(messageHash, nil)) {
		ccxt.Remove(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("authentication:", id))
		ccxt.AddElementToObject(client.(ccxt.ClientInterface).GetSubscriptions(), messageHash, true)
		client.(ccxt.ClientInterface).Resolve(message, messageHash)
	}
}
func (this *NadoCore) HandleUnsubscription(client any, message any) {
	var id any = this.SafeString(message, "id")
	var unsubscription any = this.SafeDict(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("unsubscription:", id))
	if ccxt.IsTrue(!ccxt.IsEqual(unsubscription, nil)) {
		var messageHash any = this.SafeString(unsubscription, "messageHash")
		var unsubscribeHash any = this.SafeString(unsubscription, "unsubscribeHash")
		ccxt.Remove(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("unsubscription:", id))
		if ccxt.IsTrue(!ccxt.IsEqual(messageHash, nil)) {
			this.CleanUnsubscription(client.(*ccxt.Client), messageHash, unsubscribeHash)
			this.HandleUnsubscriptionCache(messageHash)
		}
		client.(ccxt.ClientInterface).Resolve(message, unsubscribeHash)
		return
	}
	var subscriptions any = ccxt.ObjectKeys(client.(ccxt.ClientInterface).GetSubscriptions())
	for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(subscriptions)); i++ {
		var unsubscribeHash any = ccxt.GetValue(subscriptions, i)
		var subscription any = ccxt.GetValue(client.(ccxt.ClientInterface).GetSubscriptions(), unsubscribeHash)
		var subscriptionId any = this.SafeString(subscription, "id")
		if ccxt.IsTrue(!ccxt.IsEqual(subscriptionId, id)) {
			continue
		}
		var messageHash any = this.SafeString(subscription, "messageHash")
		if ccxt.IsTrue(!ccxt.IsEqual(messageHash, nil)) {
			this.CleanUnsubscription(client.(*ccxt.Client), messageHash, unsubscribeHash)
			this.HandleUnsubscriptionCache(messageHash)
		}
		client.(ccxt.ClientInterface).Resolve(message, unsubscribeHash)
		return
	}
}
func (this *NadoCore) HandleUnsubscriptionCache(messageHash any) {
	if ccxt.IsTrue(ccxt.IsEqual(messageHash, nil)) {
		return
	}
	if ccxt.IsTrue(ccxt.IsEqual(ccxt.GetIndexOf(messageHash, "trade:"), 0)) {
		var symbol any = ccxt.Replace(messageHash, "trade:", "")
		if ccxt.IsTrue(ccxt.InOp(this.Trades, symbol)) {
			ccxt.Remove(this.Trades, symbol)
		}
	} else if ccxt.IsTrue(ccxt.IsEqual(ccxt.GetIndexOf(messageHash, "orderbook:"), 0)) {
		var symbol any = ccxt.Replace(messageHash, "orderbook:", "")
		if ccxt.IsTrue(ccxt.InOp(this.Orderbooks, symbol)) {
			ccxt.Remove(this.Orderbooks, symbol)
		}
	} else if ccxt.IsTrue(ccxt.IsEqual(ccxt.GetIndexOf(messageHash, "ohlcv:"), 0)) {
		var parts any = ccxt.Split(messageHash, ":")
		var timeframe any = this.SafeString(parts, 1)
		var symbol any = this.SafeString(parts, 2)
		if ccxt.IsTrue(ccxt.IsTrue(ccxt.IsTrue(ccxt.IsTrue((!ccxt.IsEqual(symbol, nil))) && ccxt.IsTrue((!ccxt.IsEqual(timeframe, nil)))) && ccxt.IsTrue((ccxt.InOp(this.Ohlcvs, symbol)))) && ccxt.IsTrue((ccxt.InOp(ccxt.GetValue(this.Ohlcvs, symbol), timeframe)))) {
			ccxt.Remove(ccxt.GetValue(this.Ohlcvs, symbol), timeframe)
		}
	} else if ccxt.IsTrue(ccxt.IsEqual(ccxt.GetIndexOf(messageHash, "ticker:"), 0)) {
		var symbol any = ccxt.Replace(messageHash, "ticker:", "")
		if ccxt.IsTrue(ccxt.InOp(this.Tickers, symbol)) {
			ccxt.Remove(this.Tickers, symbol)
		}
	} else if ccxt.IsTrue(ccxt.IsEqual(messageHash, "ticker")) {
		var symbols any = ccxt.ObjectKeys(this.Tickers)
		for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(symbols)); i++ {
			ccxt.Remove(this.Tickers, ccxt.GetValue(symbols, i))
		}
	} else if ccxt.IsTrue(ccxt.IsEqual(ccxt.GetIndexOf(messageHash, "bidask:"), 0)) {
		var symbol any = ccxt.Replace(messageHash, "bidask:", "")
		if ccxt.IsTrue(ccxt.InOp(this.Bidsasks, symbol)) {
			ccxt.Remove(this.Bidsasks, symbol)
		}
	} else if ccxt.IsTrue(ccxt.IsEqual(messageHash, "bidask")) {
		var symbols any = ccxt.ObjectKeys(this.Bidsasks)
		for i := 0; ccxt.IsLessThan(i, ccxt.GetArrayLength(symbols)); i++ {
			ccxt.Remove(this.Bidsasks, ccxt.GetValue(symbols, i))
		}
	} else if ccxt.IsTrue(ccxt.IsEqual(ccxt.GetIndexOf(messageHash, "orders"), 0)) {
		this.Orders = nil
	} else if ccxt.IsTrue(ccxt.IsEqual(ccxt.GetIndexOf(messageHash, "myTrades"), 0)) {
		this.MyTrades = nil
	} else if ccxt.IsTrue(ccxt.IsEqual(ccxt.GetIndexOf(messageHash, "positions"), 0)) {
		this.Positions = nil
	}
}
func (this *NadoCore) Ping(client any) any {
	var gatewayUrl any = ccxt.GetValue(ccxt.GetValue(ccxt.GetValue(this.Urls, "api"), "ws"), "gateway")
	if ccxt.IsTrue(ccxt.IsEqual(client.(ccxt.ClientInterface).GetUrl(), gatewayUrl)) {
		// the v2 gateway is kept alive with protocol-level ping frames,
		// returning undefined makes the client send one instead of a message
		return nil
	}
	return map[string]any{
		"method":      "ping",
		"id":          this.RequestId(),
		"client_time": this.NumberToString(this.Milliseconds()),
	}
}
func (this *NadoCore) HandlePong(client any, message any) any {
	//
	//     {
	//         "result": {
	//             "method": "pong",
	//             "server_time": "1780000000123",
	//             "client_time": "1780000000000"
	//         },
	//         "id": 10
	//     }
	//
	var result any = this.SafeDict(message, "result", map[string]any{})
	client.(ccxt.ClientInterface).SetLastPong(this.SafeInteger(result, "server_time", this.Milliseconds()))
	return message
}
func (this *NadoCore) HandleErrorMessage(client any, message any) any {
	var error any = this.SafeValue(message, "error")
	var status any = this.SafeString(message, "status")
	if ccxt.IsTrue(ccxt.IsTrue((ccxt.IsEqual(error, nil))) && ccxt.IsTrue((!ccxt.IsEqual(status, "failure")))) {
		return false
	}
	feedback := ccxt.ExchangeError(ccxt.Add(ccxt.Add(this.Id, " "), this.Json(message)))
	var id any = this.SafeString(message, "id")
	if ccxt.IsTrue(!ccxt.IsEqual(id, nil)) {
		var executeHash any = ccxt.Add("execute:", id)
		var executeSubscription any = this.SafeValue(client.(ccxt.ClientInterface).GetSubscriptions(), executeHash)
		if ccxt.IsTrue(!ccxt.IsEqual(executeSubscription, nil)) {
			ccxt.Remove(client.(ccxt.ClientInterface).GetSubscriptions(), executeHash)
			client.(ccxt.ClientInterface).Reject(feedback, executeHash)
			return true
		}
	}
	var subscription any = this.SafeDict(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("subscription:", id))
	if ccxt.IsTrue(!ccxt.IsEqual(subscription, nil)) {
		var subscribeHash any = this.SafeString(subscription, "subscribeHash")
		ccxt.Remove(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("subscription:", id))
		client.(ccxt.ClientInterface).Reject(feedback, subscribeHash)
	} else {
		client.(ccxt.ClientInterface).Reject(feedback)
	}
	return true
}
func (this *NadoCore) HandleMessage(client any, message any) {
	if ccxt.IsTrue(this.HandleErrorMessage(client, message)) {
		return
	}
	var id any = this.SafeString(message, "id")
	var hasResult any = (ccxt.InOp(message, "result"))
	var result any = this.SafeValue(message, "result")
	var method any = this.SafeString(result, "method")
	if ccxt.IsTrue(ccxt.IsEqual(method, "pong")) {
		// pong replies carry both 'id' and 'result' so they must be routed
		// before the subscription-ack branch below swallows them
		this.HandlePong(client, message)
		return
	}
	var requestType any = this.SafeString(message, "request_type")
	if ccxt.IsTrue(!ccxt.IsEqual(requestType, nil)) {
		// v2 gateway execute responses carry 'request_type' and the echoed request id
		this.HandleExecuteResponse(client, message)
		return
	}
	if ccxt.IsTrue(ccxt.IsTrue((!ccxt.IsEqual(id, nil))) && ccxt.IsTrue(hasResult)) {
		var authentication any = this.SafeValue(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("authentication:", id))
		if ccxt.IsTrue(!ccxt.IsEqual(authentication, nil)) {
			this.HandleAuthentication(client, message)
			return
		}
		var subscription any = this.SafeValue(client.(ccxt.ClientInterface).GetSubscriptions(), ccxt.Add("subscription:", id))
		if ccxt.IsTrue(!ccxt.IsEqual(subscription, nil)) {
			this.HandleSubscription(client, message)
			return
		}
		if ccxt.IsTrue(ccxt.IsEqual(result, nil)) {
			this.HandleUnsubscription(client, message)
			return
		}
		this.HandleSubscription(client, message)
		return
	}
	var typeVar any = this.SafeString(message, "type")
	var methods any = map[string]any{
		"trade":              this.HandleTrade,
		"all_bbo":            this.HandleAllBidsAsks,
		"best_bid_offer":     this.HandleBidAsk,
		"book_depth":         this.HandleOrderBook,
		"fill":               this.HandleMyTrade,
		"latest_candlestick": this.HandleOHLCV,
		"order_update":       this.HandleOrder,
		"position_change":    this.HandlePosition,
	}
	var handler any = this.SafeValue(methods, typeVar)
	if ccxt.IsTrue(!ccxt.IsEqual(handler, nil)) {
		ccxt.CallDynamically(handler, client, message)
	}
}

func (this *NadoCore) Init(userConfig map[string]any) {
	this.base.Init(this.DeepExtend(this.Describe(), userConfig))
	this.Itf = this
	this.Exchange.DerivedExchange = this
}
