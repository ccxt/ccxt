package ccxt

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

type Mudrex struct {
	Exchange
	exchangeTyped *ExchangeTyped
}

func newMudrex() *Mudrex {
	p := &Mudrex{}
	setDefaults(p)
	return p
}

func (this *Mudrex) Describe() any {
	return this.DeepExtend(this.Exchange.Describe(), map[string]any{
		"id":        "mudrex",
		"name":      "Mudrex",
		"countries": []any{"IN"},
		"rateLimit": 100,
		"version":   "v1",
		"pro":       true,
		"certified": false,
		"dex":       false,
		"hostname":  "trade.mudrex.com",
		"has": map[string]any{
			"CORS":                                 nil,
			"spot":                                 false,
			"margin":                               false,
			"swap":                                 true,
			"future":                               false,
			"option":                               false,
			"addMargin":                            true,
			"cancelOrder":                          true,
			"closePosition":                        true,
			"createMarketOrder":                    true,
			"createOrder":                          true,
			"createOrderWithTakeProfitAndStopLoss": true,
			"createReduceOnlyOrder":                true,
			"editOrder":                            true,
			"fetchBalance":                         true,
			"fetchClosedOrders":                    true,
			"fetchFundingRate":                     false,
			"fetchFundingRateHistory":              false,
			"fetchFundingRates":                    false,
			"fetchIndexOHLCV":                      false,
			"fetchLeverage":                        true,
			"fetchMarkets":                         true,
			"fetchMarkOHLCV":                       true,
			"fetchMyTrades":                        true,
			"fetchOHLCV":                           true,
			"fetchOpenInterest":                    false,
			"fetchOpenInterests":                   false,
			"fetchOpenOrders":                      true,
			"fetchOrder":                           true,
			"fetchOrderBook":                       false,
			"fetchOrders":                          true,
			"fetchPositions":                       true,
			"fetchPositionsHistory":                true,
			"fetchTicker":                          true,
			"fetchTickers":                         true,
			"fetchTrades":                          false,
			"reduceMargin":                         true,
			"setLeverage":                          true,
			"transfer":                             true,
			"watchOHLCV":                           true,
			"watchTicker":                          true,
			"watchTickers":                         true,
		},
		"timeframes": map[string]any{
			"1m":  "1m",
			"3m":  "3t",
			"5m":  "5t",
			"10m": "10t",
			"15m": "15t",
			"30m": "30t",
			"1h":  "1h",
			"4h":  "4h",
			"6h":  "6h",
			"12h": "12h",
			"1d":  "1d",
			"1w":  "1w",
			"1M":  "1mth",
		},
		"urls": map[string]any{
			"logo": "https://github.com/user-attachments/assets/72368864-84ed-43eb-8c75-d4fb77023b42",
			"api": map[string]any{
				"public":  "https://trade.mudrex.com/fapi/v1",
				"private": "https://trade.mudrex.com/fapi/v1",
				"market":  "https://trade.mudrex.com/fapi/v1",
			},
			"www":  "https://mudrex.com",
			"doc":  "https://docs.trade.mudrex.com/docs",
			"fees": "https://docs.trade.mudrex.com",
		},
		"api": map[string]any{
			"market": map[string]any{
				"get": map[string]any{
					"price/kline": map[string]any{
						"cost": 1,
					},
					"price/mark-kline": map[string]any{
						"cost": 1,
					},
				},
			},
			"public": map[string]any{
				"get": map[string]any{},
			},
			"private": map[string]any{
				"get": map[string]any{
					"futures": map[string]any{
						"cost": 1,
					},
					"futures/{asset_id}": map[string]any{
						"cost": 1,
					},
					"wallet/funds": map[string]any{
						"cost": 5,
					},
					"futures/funds": map[string]any{
						"cost": 5,
					},
					"futures/transactions": map[string]any{
						"cost": 1,
					},
					"futures/orders": map[string]any{
						"cost": 1,
					},
					"futures/orders/history": map[string]any{
						"cost": 1,
					},
					"futures/orders/{order_id}": map[string]any{
						"cost": 1,
					},
					"futures/positions": map[string]any{
						"cost": 1,
					},
					"futures/positions/history": map[string]any{
						"cost": 1,
					},
					"futures/fee/history": map[string]any{
						"cost": 1,
					},
					"futures/{asset_id}/leverage": map[string]any{
						"cost": 2,
					},
					"futures/positions/{position_id}/liq-price": map[string]any{
						"cost": 1,
					},
				},
				"post": map[string]any{
					"wallet/futures/transfer": map[string]any{
						"cost": 5,
					},
					"futures/transfers/inr": map[string]any{
						"cost": 5,
					},
					"futures/{asset_id}/order": map[string]any{
						"cost": 2,
					},
					"futures/positions/{position_id}/close": map[string]any{
						"cost": 2,
					},
					"futures/positions/{position_id}/close/partial": map[string]any{
						"cost": 2,
					},
					"futures/positions/{position_id}/reverse": map[string]any{
						"cost": 2,
					},
					"futures/positions/{position_id}/add-margin": map[string]any{
						"cost": 2,
					},
					"futures/positions/{position_id}/riskorder": map[string]any{
						"cost": 2,
					},
					"futures/{asset_id}/leverage": map[string]any{
						"cost": 2,
					},
				},
				"patch": map[string]any{
					"futures/orders/{order_id}": map[string]any{
						"cost": 1,
					},
					"futures/positions/{position_id}/riskorder": map[string]any{
						"cost": 2,
					},
				},
				"delete": map[string]any{
					"futures/orders/{order_id}": map[string]any{
						"cost": 2,
					},
				},
			},
		},
		"requiredCredentials": map[string]any{
			"apiKey": false,
			"secret": true,
		},
		"fees": map[string]any{
			"trading": map[string]any{
				"tierBased":  false,
				"percentage": true,
				"taker":      this.ParseNumber("0.00059"),
				"maker":      this.ParseNumber("0.00023"),
			},
		},
		"options": map[string]any{
			"defaultType": "swap",
			"broker":      "42ce8902-8585-448c-a1e8-0371a6ca7ca8",
		},
		"exceptions": map[string]any{
			"exact": map[string]any{
				"400 Invalid trade currency": BadRequest,
			},
			"broad": map[string]any{
				"Invalid trade currency":                       BadRequest,
				"Params error":                                 BadRequest,
				"invalid trigger type":                         BadRequest,
				"invalid order type":                           BadRequest,
				"order price out of permissible range":         BadRequest,
				"quantity not a multiple of the quantity step": BadRequest,
				"leverage out of permissible range":            BadRequest,
				"insufficient balance":                         InsufficientFunds,
				"asset not found":                              BadSymbol,
				"leverage not found":                           OrderNotFound,
				"order not found":                              OrderNotFound,
				"Rate limit exceeded":                          RateLimitExceeded,
			},
		},
	})
}
func (this *Mudrex) Sign(path any, optionalArgs ...any) any {
	api := GetArg(optionalArgs, 0, "public")
	_ = api
	method := GetArg(optionalArgs, 1, "GET")
	_ = method
	params := GetArg(optionalArgs, 2, map[string]any{})
	_ = params
	headers := GetArg(optionalArgs, 3, nil)
	_ = headers
	body := GetArg(optionalArgs, 4, nil)
	_ = body
	var apiUrls any = this.SafeDict(this.Urls, "api", map[string]any{})
	var base any = this.SafeString(apiUrls, api)
	if IsTrue(IsEqual(base, nil)) {
		panic(ExchangeError(Add(Add(this.Id, " unknown API namespace: "), api)))
	}
	var url any = Add(Add(base, "/"), this.ImplodeParams(path, params))
	var query any = this.Omit(params, this.ExtractParams(path))
	var requestHeaders map[string]any = map[string]any{}
	if IsTrue(!IsEqual(headers, nil)) {
		requestHeaders = this.Extend(map[string]any{}, headers)
	}
	var brokerId any = this.SafeString(this.Options, "broker")
	if IsTrue(!IsEqual(brokerId, nil)) {
		AddElementToObject(requestHeaders, "Partner-Id", brokerId)
	}
	var methodUpper string = ToUpper(method)
	if IsTrue(IsEqual(api, "private")) {
		this.CheckRequiredCredentials()
		AddElementToObject(requestHeaders, "X-Authentication", this.Secret)
		if IsTrue(IsTrue(IsTrue(IsEqual(methodUpper, "POST")) || IsTrue(IsEqual(methodUpper, "PATCH"))) || IsTrue(IsEqual(methodUpper, "DELETE"))) {
			AddElementToObject(requestHeaders, "Content-Type", "application/json")
			// is_symbol is a query-string flag even on write requests
			var isSymbol any = this.SafeString(query, "is_symbol")
			if IsTrue(!IsEqual(isSymbol, nil)) {
				query = this.Omit(query, "is_symbol")
				url = Add(url, Add("?", this.Urlencode(map[string]any{
					"is_symbol": isSymbol,
				})))
			}
			if IsTrue(IsTrue((IsEqual(methodUpper, "DELETE"))) && IsTrue(this.IsEmpty(query))) {
				return map[string]any{
					"url":     url,
					"method":  methodUpper,
					"body":    nil,
					"headers": requestHeaders,
				}
			}
			var bodyStr any = this.Json(query)
			return map[string]any{
				"url":     url,
				"method":  methodUpper,
				"body":    bodyStr,
				"headers": requestHeaders,
			}
		}
	}
	if IsTrue(IsGreaterThan(GetArrayLength(ObjectKeys(query)), 0)) {
		url = Add(url, Add("?", this.Urlencode(query)))
	}
	return map[string]any{
		"url":     url,
		"method":  methodUpper,
		"body":    nil,
		"headers": requestHeaders,
	}
}
func (this *Mudrex) HandleErrors(code any, reason any, url any, method any, headers any, body any, response any, requestHeaders any, requestBody any) any {
	if IsTrue(IsTrue(IsEqual(response, nil)) || IsTrue(!IsObject(response))) {
		return nil
	}
	var success any = this.SafeBool(response, "success", true)
	if IsTrue(!IsEqual(success, true)) {
		var errors any = this.SafeList(response, "errors", []any{})
		var first any = this.SafeDict(errors, 0, map[string]any{})
		var text any = this.SafeString(first, "text", this.Json(response))
		var errCode any = this.SafeString(first, "code")
		this.ThrowExactlyMatchedException(GetValue(this.Exceptions, "exact"), text, Add(Add(this.Id, " "), text))
		this.ThrowExactlyMatchedException(GetValue(this.Exceptions, "exact"), errCode, Add(Add(this.Id, " "), text))
		this.ThrowBroadlyMatchedException(GetValue(this.Exceptions, "broad"), text, Add(Add(this.Id, " "), text))
		var msg any = Add(Add(this.Id, " "), text)
		var low string = ToLower(text)
		if IsTrue(IsTrue(IsEqual(code, 401)) || IsTrue(IsGreaterThanOrEqual(GetIndexOf(low, "auth"), 0))) {
			panic(AuthenticationError(msg))
		}
		if IsTrue(IsTrue(IsEqual(code, 429)) || IsTrue(IsGreaterThanOrEqual(GetIndexOf(low, "rate"), 0))) {
			panic(RateLimitExceeded(msg))
		}
		if IsTrue(IsGreaterThanOrEqual(GetIndexOf(low, "insufficient"), 0)) {
			panic(InsufficientFunds(msg))
		}
		if IsTrue(IsEqual(code, 400)) {
			panic(BadRequest(msg))
		}
		panic(ExchangeError(msg))
	}
	return nil
}
func (this *Mudrex) ParseOHLCV(ohlcv any, optionalArgs ...any) any {
	//
	//     [ 1782984660, 60681, 60797.6, 60671.8, 60693.3, 275.741 ]
	//     [ timestampInSeconds, open, high, low, close, volume ]
	//
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	return []any{this.SafeTimestamp(ohlcv, 0), this.SafeNumber(ohlcv, 1), this.SafeNumber(ohlcv, 2), this.SafeNumber(ohlcv, 3), this.SafeNumber(ohlcv, 4), this.SafeNumber(ohlcv, 5)}
}

/**
 * @method
 * @name mudrex#fetchOHLCV
 * @description fetches historical candlestick data containing the open, high, low, and close price, and the volume of a market
 * @see https://docs.trade.mudrex.com/docs/historical-kline
 * @param {string} symbol unified symbol of the market to fetch OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum amount of candles to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] timestamp in ms of the latest candle to fetch
 * @param {string} [params.price] "mark" to fetch mark price candles
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 */
func (this *Mudrex) FetchOHLCVAsync(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchOHLCVBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchOHLCVBody(ch chan any, symbol any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	timeframe := GetArg(optionalArgs, 0, "1m")
	_ = timeframe
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes28512 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes28512)
	}
	var market any = this.Market(symbol)
	var priceType any = this.SafeString(params, "price")
	params = this.Omit(params, "price")
	// the endpoint expects the pair in "BASE/QUOTE" format (comma-separated for multiple)
	var assetPair any = Add(Add(GetValue(market, "baseId"), "/"), GetValue(market, "quoteId"))
	var request map[string]any = map[string]any{
		"assets":      assetPair,
		"aggregation": this.SafeString(this.Timeframes, timeframe, timeframe),
	}
	// the endpoint requires an explicit time window (in seconds)
	var duration any = this.ParseTimeframe(timeframe)
	var requestLimit any = limit
	if IsTrue(IsEqual(requestLimit, nil)) {
		requestLimit = 500
	}
	var now int64 = this.Seconds()
	var startTime any = nil
	if IsTrue(!IsEqual(since, nil)) {
		startTime = this.ParseToInt(Divide(since, 1000))
	} else {
		startTime = Subtract(now, Multiply(duration, requestLimit))
	}
	if IsTrue(IsEqual(startTime, nil)) {
		panic(ExchangeError(Add(this.Id, " fetchOHLCV() missing startTime")))
	}
	var endTime any = Add(startTime, Multiply(duration, requestLimit))
	var until any = this.SafeInteger(params, "until")
	if IsTrue(!IsEqual(until, nil)) {
		params = this.Omit(params, "until")
		endTime = this.ParseToInt(Divide(until, 1000))
	} else if IsTrue(IsGreaterThan(endTime, now)) {
		endTime = now
	}
	AddElementToObject(request, "start_time", startTime)
	AddElementToObject(request, "end_time", endTime)
	var response any = nil
	if IsTrue(IsEqual(priceType, "mark")) {

		response = (<-this.MarketGetPriceMarkKline(this.Extend(request, params)))
		PanicOnError(response)
	} else {

		response = (<-this.MarketGetPriceKline(this.Extend(request, params)))
		PanicOnError(response)
	}
	//
	//     {
	//         "success": true,
	//         "data": {
	//             "asset_ticks": {
	//                 "btc/usdt": [ [ 1782984660, 60681, 60797.6, 60671.8, 60693.3, 275.741 ] ]
	//             }
	//         }
	//     }
	//
	var data any = this.SafeDict(response, "data", map[string]any{})
	var assetTicks any = this.SafeDict(data, "asset_ticks", map[string]any{})
	var ohlcvs any = this.SafeList(assetTicks, ToLower(assetPair), []any{})

	ch <- this.ParseOHLCVs(ohlcvs, market, timeframe, since, limit)
	return nil
}

/**
 * @method
 * @name mudrex#fetchMarkOHLCV
 * @description fetches historical mark price candlestick data containing the open, high, low, and close price of a market
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} symbol unified symbol of the market to fetch OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum amount of candles to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 */
func (this *Mudrex) FetchMarkOHLCVAsync(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchMarkOHLCVBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchMarkOHLCVBody(ch chan any, symbol any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	timeframe := GetArg(optionalArgs, 0, "1m")
	_ = timeframe
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params

	retRes35715 := (<-this.FetchOHLCVAsync(symbol, timeframe, since, limit, this.Extend(params, map[string]any{
		"price": "mark",
	})))
	PanicOnError(retRes35715)
	ch <- retRes35715
	return nil
}

/**
 * @method
 * @name mudrex#fetchTicker
 * @description fetches a price ticker, a statistical calculation with the information calculated over the past 24 hours for a specific market
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} symbol unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure](https://docs.ccxt.com/#/?id=ticker-structure)
 */
func (this *Mudrex) FetchTickerAsync(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchTickerBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchTickerBody(ch chan any, symbol any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes37112 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes37112)
	}
	var market any = this.Market(symbol)
	var request map[string]any = map[string]any{
		"asset_id":  GetValue(market, "id"),
		"is_symbol": 1,
	}

	response := (<-this.PrivateGetFuturesAssetId(this.Extend(request, params)))
	PanicOnError(response)
	var data any = this.SafeDict(response, "data", map[string]any{})

	ch <- this.ParseTicker(data, market)
	return nil
}

/**
 * @method
 * @name mudrex#fetchTickers
 * @description fetches price tickers for multiple markets, statistical information calculated over the past 24 hours for each market
 * @see https://docs.trade.mudrex.com/docs
 * @param {string[]} [symbols] unified symbols of the markets to fetch the ticker for, all market tickers are returned if not assigned
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a dictionary of [ticker structures](https://docs.ccxt.com/#/?id=ticker-structure)
 */
func (this *Mudrex) FetchTickersAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchTickersBody(ch, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchTickersBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbols := GetArg(optionalArgs, 0, nil)
	_ = symbols
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes39412 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes39412)
	}
	var request map[string]any = map[string]any{}

	response := (<-this.PrivateGetFutures(this.Extend(request, params)))
	PanicOnError(response)
	var data any = this.SafeValue(response, "data", []any{})
	var rows any = Ternary(IsTrue(IsArray(data)), data, this.SafeList(data, "items", []any{}))
	var resultTickers map[string]any = map[string]any{}
	for i := 0; IsLessThan(i, GetArrayLength(rows)); i++ {
		var t any = GetValue(rows, i)
		var sym any = this.SafeString(t, "symbol")
		if IsTrue(IsEqual(sym, nil)) {
			continue
		}
		var m any = this.SafeMarket(sym)
		var symbol any = GetValue(m, "symbol")
		if IsTrue(IsTrue(!IsEqual(symbols, nil)) && !IsTrue(this.InArray(symbol, symbols))) {
			continue
		}
		AddElementToObject(resultTickers, symbol, this.ParseTicker(t, m))
	}

	ch <- this.FilterByArrayTickers(resultTickers, "symbol", symbols)
	return nil
}
func (this *Mudrex) ParseTicker(ticker any, optionalArgs ...any) any {
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	var ms any = this.SafeString(ticker, "symbol")
	market = this.SafeMarket(ms, market)
	var symbol any = GetValue(market, "symbol")
	var pct any = this.SafeNumber(ticker, "change_perc")
	return this.SafeTicker(map[string]any{
		"symbol":        symbol,
		"timestamp":     nil,
		"datetime":      nil,
		"high":          nil,
		"low":           nil,
		"bid":           nil,
		"bidVolume":     nil,
		"ask":           nil,
		"askVolume":     nil,
		"vwap":          nil,
		"open":          this.SafeNumber(ticker, "last_day_price"),
		"close":         this.SafeNumber(ticker, "price"),
		"last":          this.SafeNumber(ticker, "price"),
		"previousClose": nil,
		"change":        nil,
		"percentage":    pct,
		"average":       nil,
		"baseVolume":    nil,
		"quoteVolume":   this.SafeNumber(ticker, "volume"),
		"info":          ticker,
	}, market)
}

/**
 * @method
 * @name mudrex#fetchMarkets
 * @description retrieves data on all markets for the exchange
 * @see https://docs.trade.mudrex.com/docs
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} an array of objects representing market data
 */
func (this *Mudrex) FetchMarketsAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchMarketsBody(ch, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchMarketsBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	var aggregated any = []any{}
	var offset any = 0
	var pageLimit int = 100
	var paging bool = true
	for IsEqual(paging, true) {
		var q map[string]any = this.Extend(map[string]any{
			"limit":  pageLimit,
			"offset": offset,
		}, params)

		response := (<-this.PrivateGetFutures(q))
		PanicOnError(response)
		var data any = this.SafeValue(response, "data", []any{})
		var items any = []any{}
		if IsTrue(IsTrue(IsObject(data)) && !IsTrue(IsArray(data))) {
			items = this.SafeList(data, "items", []any{})
			// hoisted - inline length reads within conditionals become strlen for php, fatal on arrays
			var itemsLength int = GetArrayLength(items)
			if IsTrue(IsTrue((IsEqual(itemsLength, nil))) || IsTrue((IsEqual(itemsLength, 0)))) {
				items = this.SafeList(data, "results", []any{})
				itemsLength = GetArrayLength(items)
			}
			if IsTrue(IsTrue((IsEqual(itemsLength, 0))) && IsTrue((InOp(data, "symbol")))) {
				items = []any{data}
			}
		} else {
			items = this.ToArray(data)
		}
		var numItems int = GetArrayLength(items)
		if IsTrue(IsTrue((IsEqual(numItems, nil))) || IsTrue((IsEqual(numItems, 0)))) {
			paging = false
			break
		}
		for i := 0; IsLessThan(i, numItems); i++ {
			AppendToArray(&aggregated, GetValue(items, i))
		}
		if IsTrue(IsLessThan(numItems, pageLimit)) {
			paging = false
		} else {
			// this.sum keeps the offset numeric across the php transpile, see https://github.com/ccxt/ccxt/pull/29684
			offset = this.Sum(offset, pageLimit)
		}
	}
	var result any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(aggregated)); i++ {
		AppendToArray(&result, this.ParseMarket(GetValue(aggregated, i)))
	}

	ch <- result
	return nil
}
func (this *Mudrex) ParseMarket(asset any) any {
	var ms any = this.SafeString(asset, "symbol")
	var base any = ms
	if IsTrue(IsTrue(!IsEqual(ms, nil)) && IsTrue(EndsWith(ms, "USDT"))) {
		base = Slice(ms, 0, OpNeg(4))
	}
	var quote string = "USDT"
	var settle string = "USDT"
	var symbol any = nil
	if IsTrue(!IsEqual(base, nil)) {
		symbol = Add(Add(Add(Add(base, "/"), quote), ":"), settle)
	}
	var priceStep any = this.SafeString(asset, "price_step", "0.01")
	var qtyStep any = this.SafeString(asset, "quantity_step", "0.001")
	return this.SafeMarketStructure(map[string]any{
		"id":             ms,
		"lowercaseId":    nil,
		"symbol":         symbol,
		"base":           base,
		"quote":          quote,
		"settle":         settle,
		"baseId":         base,
		"quoteId":        "USDT",
		"settleId":       "USDT",
		"type":           "swap",
		"spot":           false,
		"margin":         false,
		"swap":           true,
		"future":         false,
		"option":         false,
		"active":         true,
		"contract":       true,
		"linear":         true,
		"inverse":        false,
		"taker":          this.SafeNumber(GetValue(this.Fees, "trading"), "taker"),
		"maker":          this.SafeNumber(GetValue(this.Fees, "trading"), "maker"),
		"contractSize":   this.SafeNumber(asset, "contract_size", 1),
		"expiry":         nil,
		"expiryDatetime": nil,
		"strike":         nil,
		"optionType":     nil,
		"precision": map[string]any{
			"amount": this.ParseNumber(qtyStep),
			"price":  this.ParseNumber(priceStep),
		},
		"limits": map[string]any{
			"amount": map[string]any{
				"min": this.SafeNumber(asset, "min_contract"),
				"max": this.SafeNumber(asset, "max_contract"),
			},
			"price": map[string]any{
				"min": this.SafeNumber(asset, "min_price"),
				"max": this.SafeNumber(asset, "max_price"),
			},
			"cost": map[string]any{
				"min": this.SafeNumber(asset, "min_notional_value"),
				"max": nil,
			},
		},
		"info":    asset,
		"created": nil,
	})
}

/**
 * @method
 * @name mudrex#fetchBalance
 * @description query for balance and get the amount of funds available for trading or funds locked in orders
 * @see https://docs.trade.mudrex.com/docs
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.type] 'swap' (default) or 'spot' - which wallet balance to fetch
 * @param {string} [params.trade_currency] the settlement currency to query the balance for
 * @returns {object} a [balance structure](https://docs.ccxt.com/#/?id=balance-structure)
 */
func (this *Mudrex) FetchBalanceAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchBalanceBody(ch, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchBalanceBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes57612 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes57612)
	}
	var typeVar any = nil
	typeVarparamsVariable := this.HandleMarketTypeAndParams("fetchBalance", nil, params, "swap")
	typeVar = GetValue(typeVarparamsVariable, 0)
	params = GetValue(typeVarparamsVariable, 1)
	var requested any = this.SafeStringN(params, []any{"trade_currency", "tradeCurrency", "currency"})
	params = this.Omit(params, []any{"trade_currency", "tradeCurrency", "currency"})
	var request map[string]any = map[string]any{}
	var response any = nil
	if IsTrue(IsEqual(typeVar, "spot")) {
		if IsTrue(!IsEqual(requested, nil)) {
			AddElementToObject(request, "currency", requested)
		}

		response = (<-this.PrivateGetWalletFunds(this.Extend(request, params)))
		PanicOnError(response)
	} else {
		if IsTrue(!IsEqual(requested, nil)) {
			AddElementToObject(request, "trade_currency", requested)
		}

		response = (<-this.PrivateGetFuturesFunds(this.Extend(request, params)))
		PanicOnError(response)
	}
	var currency any = requested
	if IsTrue(IsEqual(currency, nil)) {
		currency = "USDT"
	}
	if IsTrue(IsEqual(response, nil)) {
		panic(NullResponse(Add(this.Id, " fetchBalance() returned empty response")))
	}
	AddElementToObject(response, "currency", currency)

	ch <- this.ParseBalance(response)
	return nil
}
func (this *Mudrex) ParseBalance(response any) any {
	var data any = this.SafeDict(response, "data", map[string]any{})
	var currency any = this.SafeString(response, "currency", "USDT")
	var result map[string]any = map[string]any{
		"info": response,
	}
	var account any = this.Account()
	var futuresBalance any = this.SafeString(data, "balance")
	if IsTrue(!IsEqual(futuresBalance, nil)) {
		// futures wallet: balance is the free/available margin, locked_amount is used, safeBalance derives total
		AddElementToObject(account, "free", futuresBalance)
		AddElementToObject(account, "used", this.SafeString(data, "locked_amount"))
	} else {
		// spot wallet: total is the total, withdrawable is free, safeBalance derives used
		AddElementToObject(account, "total", this.SafeString(data, "total"))
		AddElementToObject(account, "free", this.SafeString(data, "withdrawable"))
	}
	AddElementToObject(result, currency, account)
	return this.SafeBalance(result)
}

/**
 * @method
 * @name mudrex#fetchLeverage
 * @description fetch the set leverage for a market
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} symbol unified market symbol
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [leverage structure](https://docs.ccxt.com/#/?id=leverage-structure)
 */
func (this *Mudrex) FetchLeverageAsync(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchLeverageBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchLeverageBody(ch chan any, symbol any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes63812 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes63812)
	}
	var market any = this.Market(symbol)
	var request map[string]any = map[string]any{
		"asset_id":  GetValue(market, "id"),
		"is_symbol": 1,
	}

	response := (<-this.PrivateGetFuturesAssetIdLeverage(this.Extend(request, params)))
	PanicOnError(response)
	var data any = this.SafeDict(response, "data", map[string]any{})

	ch <- map[string]any{
		"info":          response,
		"symbol":        symbol,
		"marginMode":    this.SafeStringLower(data, "margin_type"),
		"longLeverage":  this.SafeNumber(data, "leverage"),
		"shortLeverage": this.SafeNumber(data, "leverage"),
	}
	return nil
}

/**
 * @method
 * @name mudrex#setLeverage
 * @description set the level of leverage for a market
 * @see https://docs.trade.mudrex.com/docs
 * @param {float} leverage the rate of leverage
 * @param {string} symbol unified market symbol
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.marginType] 'ISOLATED' (default) or 'CROSSED'
 * @returns {object} response from the exchange
 */
func (this *Mudrex) SetLeverageAsync(leverage any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.setLeverageBody(ch, leverage, optionalArgs...)
	return ch
}
func (this *Mudrex) setLeverageBody(ch chan any, leverage any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(symbol, nil)) {
		panic(ArgumentsRequired(Add(this.Id, " setLeverage() requires a symbol")))
	}
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes67212 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes67212)
	}
	var market any = this.Market(symbol)
	var marginType any = this.SafeString(params, "marginType", "ISOLATED")
	var request map[string]any = map[string]any{
		"asset_id":    GetValue(market, "id"),
		"is_symbol":   1,
		"margin_type": marginType,
		"leverage":    leverage,
	}
	params = this.Omit(params, []any{"marginType"})

	response := (<-this.PrivatePostFuturesAssetIdLeverage(this.Extend(request, params)))
	PanicOnError(response)

	ch <- response
	return nil
}

/**
 * @method
 * @name mudrex#createOrder
 * @description create a trade order
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} symbol unified market symbol
 * @param {string} type 'market' or 'limit'
 * @param {string} side 'buy' or 'sell'
 * @param {float} amount how much you want to trade in units of the base currency
 * @param {float} [price] the price to fulfill the order, in units of the quote currency (also required for market orders on this exchange)
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.leverage] leverage for the order, required if setLeverage() was not called beforehand
 * @param {bool} [params.reduceOnly] true if the order is reduce only
 * @param {object} [params.takeProfit] *takeProfit object in params* containing the trigger price of the take-profit order attached to this order
 * @param {float} [params.takeProfit.triggerPrice] take profit trigger price
 * @param {object} [params.stopLoss] *stopLoss object in params* containing the trigger price of the stop-loss order attached to this order
 * @param {float} [params.stopLoss.triggerPrice] stop loss trigger price
 * @param {float} [params.takeProfitPrice] the trigger price for a standalone take-profit order on an existing position (requires params.positionId)
 * @param {float} [params.stopLossPrice] the trigger price for a standalone stop-loss order on an existing position (requires params.positionId)
 * @param {string} [params.positionId] the id of the position the standalone stopLossPrice/takeProfitPrice order is attached to
 * @param {string} [params.trade_currency] the settlement currency for the order
 * @returns {object} an [order structure](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) CreateOrderAsync(symbol any, typeVar any, side any, amount any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.createOrderBody(ch, symbol, typeVar, side, amount, optionalArgs...)
	return ch
}
func (this *Mudrex) createOrderBody(ch chan any, symbol any, typeVar any, side any, amount any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	price := GetArg(optionalArgs, 0, nil)
	_ = price
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes71212 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes71212)
	}
	var market any = this.Market(symbol)
	// standalone stop-loss / take-profit orders (stopLossPrice/takeProfitPrice) are attached to
	// an existing position through the riskorder endpoint, so a positionId is required
	var stopLossPrice any = this.SafeString(params, "stopLossPrice")
	var takeProfitPrice any = this.SafeString(params, "takeProfitPrice")
	if IsTrue(IsTrue((!IsEqual(stopLossPrice, nil))) || IsTrue((!IsEqual(takeProfitPrice, nil)))) {
		var positionId any = this.SafeString2(params, "positionId", "position_id")
		if IsTrue(IsEqual(positionId, nil)) {
			panic(ArgumentsRequired(Add(this.Id, " createOrder() requires a positionId parameter to place a stopLossPrice or takeProfitPrice order")))
		}
		params = this.Omit(params, []any{"stopLossPrice", "takeProfitPrice", "positionId", "position_id"})
		var riskRequest map[string]any = map[string]any{
			"position_id": positionId,
		}
		if IsTrue(!IsEqual(takeProfitPrice, nil)) {
			AddElementToObject(riskRequest, "is_takeprofit", true)
			AddElementToObject(riskRequest, "takeprofit_price", this.PriceToPrecision(symbol, takeProfitPrice))
		}
		if IsTrue(!IsEqual(stopLossPrice, nil)) {
			AddElementToObject(riskRequest, "is_stoploss", true)
			AddElementToObject(riskRequest, "stoploss_price", this.PriceToPrecision(symbol, stopLossPrice))
		}

		riskResponse := (<-this.PrivatePostFuturesPositionsPositionIdRiskorder(this.Extend(riskRequest, params)))
		PanicOnError(riskResponse)
		var riskData any = this.SafeDict(riskResponse, "data", riskResponse)

		ch <- this.ParseOrder(riskData, market)
		return nil
	}
	var lev any = this.SafeInteger(params, "leverage", 1)
	if IsTrue(IsTrue((IsEqual(typeVar, "market"))) && IsTrue((IsEqual(price, nil)))) {
		panic(ArgumentsRequired(Add(this.Id, " createOrder() requires a price argument for market orders")))
	}
	var request map[string]any = map[string]any{
		"asset_id":     GetValue(market, "id"),
		"is_symbol":    1,
		"leverage":     this.NumberToString(lev),
		"quantity":     this.AmountToPrecision(symbol, amount),
		"order_price":  this.PriceToPrecision(symbol, price),
		"order_type":   Ternary(IsTrue((IsEqual(side, "buy"))), "LONG", "SHORT"),
		"trigger_type": Ternary(IsTrue((IsEqual(typeVar, "market"))), "MARKET", "LIMIT"),
		"reduce_only":  this.SafeBool(params, "reduceOnly", false),
	}
	// mudrex only supports take-profit / stop-loss orders attached to the position-opening order
	var takeProfit any = this.SafeDict(params, "takeProfit")
	var stopLoss any = this.SafeDict(params, "stopLoss")
	if IsTrue(!IsEqual(takeProfit, nil)) {
		AddElementToObject(request, "is_takeprofit", true)
		AddElementToObject(request, "takeprofit_price", this.PriceToPrecision(symbol, this.SafeStringN(takeProfit, []any{"triggerPrice", "stopPrice", "price"})))
	}
	if IsTrue(!IsEqual(stopLoss, nil)) {
		AddElementToObject(request, "is_stoploss", true)
		AddElementToObject(request, "stoploss_price", this.PriceToPrecision(symbol, this.SafeStringN(stopLoss, []any{"triggerPrice", "stopPrice", "price"})))
	}
	params = this.Omit(params, []any{"leverage", "reduceOnly", "takeProfit", "stopLoss"})

	response := (<-this.PrivatePostFuturesAssetIdOrder(this.Extend(request, params)))
	PanicOnError(response)
	var data any = this.SafeDict(response, "data", response)
	// the create response omits the order/trigger type, so parse a merged copy - the base derivations, like timeInForce, need to see them - then keep the untouched raw payload under info
	var merged map[string]any = this.Extend(data, map[string]any{
		"order_type":   GetValue(request, "order_type"),
		"trigger_type": GetValue(request, "trigger_type"),
	})
	var order any = this.ParseOrder(merged, market)
	AddElementToObject(order, "info", data)

	ch <- order
	return nil
}

/**
 * @method
 * @name mudrex#editOrder
 * @description edit a trade order
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} id order id
 * @param {string} symbol unified symbol of the market to edit an order in
 * @param {string} type 'market' or 'limit'
 * @param {string} side 'buy' or 'sell'
 * @param {float} [amount] how much of the currency you want to trade in units of the base currency
 * @param {float} [price] the price at which the order is to be fulfilled, in units of the quote currency
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order structure](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) EditOrderAsync(id any, symbol any, typeVar any, side any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.editOrderBody(ch, id, symbol, typeVar, side, optionalArgs...)
	return ch
}
func (this *Mudrex) editOrderBody(ch chan any, id any, symbol any, typeVar any, side any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	amount := GetArg(optionalArgs, 0, nil)
	_ = amount
	price := GetArg(optionalArgs, 1, nil)
	_ = price
	params := GetArg(optionalArgs, 2, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes79112 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes79112)
	}
	var market any = nil
	if IsTrue(!IsEqual(symbol, nil)) {
		market = this.Market(symbol)
	}
	var request map[string]any = map[string]any{
		"order_id": id,
	}
	if IsTrue(!IsEqual(amount, nil)) {
		AddElementToObject(request, "quantity", this.AmountToPrecision(symbol, amount))
	}
	if IsTrue(!IsEqual(price, nil)) {
		AddElementToObject(request, "order_price", this.PriceToPrecision(symbol, price))
	}

	response := (<-this.PrivatePatchFuturesOrdersOrderId(this.Extend(request, params)))
	PanicOnError(response)
	var data any = this.SafeDict(response, "data", response)

	ch <- this.ParseOrder(data, market)
	return nil
}
func (this *Mudrex) ParseOrderStatus(status any) any {
	var statuses map[string]any = map[string]any{
		"open":             "open",
		"created":          "open",
		"new":              "open",
		"pending":          "open",
		"partially_filled": "open",
		"filled":           "closed",
		"completed":        "closed",
		"cancelled":        "canceled",
		"canceled":         "canceled",
		"rejected":         "rejected",
		"expired":          "expired",
	}
	return this.SafeString(statuses, status, status)
}
func (this *Mudrex) ParseOrder(order any, optionalArgs ...any) any {
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	var oms any = this.SafeString(order, "symbol")
	market = this.SafeMarket(oms, market)
	var oid any = this.SafeString2(order, "order_id", "id")
	var rawSide any = this.SafeStringUpper(order, "order_type")
	var side any = nil
	if IsTrue(IsEqual(rawSide, "LONG")) {
		side = "buy"
	} else if IsTrue(IsEqual(rawSide, "SHORT")) {
		side = "sell"
	}
	// stop-loss / take-profit rows attached to a position carry the trigger value under the "price" key
	var isRiskOrder bool = IsTrue((IsEqual(rawSide, "STOPLOSS"))) || IsTrue((IsEqual(rawSide, "TAKEPROFIT")))
	var priceString any = this.SafeString2(order, "price", "order_price")
	var orderPrice any = priceString
	var triggerPrice any = nil
	var stopLossPrice any = nil
	var takeProfitPrice any = nil
	if IsTrue(isRiskOrder) {
		triggerPrice = priceString
		orderPrice = nil
		if IsTrue(IsEqual(rawSide, "STOPLOSS")) {
			stopLossPrice = priceString
		} else {
			takeProfitPrice = priceString
		}
	}
	var trig any = this.SafeStringUpper(order, "trigger_type")
	var typ any = nil
	if IsTrue(IsEqual(trig, "MARKET")) {
		typ = "market"
	} else if IsTrue(IsEqual(trig, "LIMIT")) {
		typ = "limit"
	}
	var ts any = this.Parse8601(this.SafeString(order, "created_at"))
	var status any = this.ParseOrderStatus(this.SafeStringLower(order, "status"))
	var sym any = GetValue(market, "symbol")
	return this.SafeOrder(map[string]any{
		"info":                order,
		"id":                  oid,
		"clientOrderId":       nil,
		"timestamp":           ts,
		"datetime":            this.Iso8601(ts),
		"lastTradeTimestamp":  nil,
		"symbol":              sym,
		"type":                typ,
		"timeInForce":         nil,
		"postOnly":            nil,
		"side":                side,
		"price":               orderPrice,
		"triggerPrice":        triggerPrice,
		"stopLossPrice":       stopLossPrice,
		"takeProfitPrice":     takeProfitPrice,
		"amount":              this.SafeString2(order, "quantity", "amount"),
		"cost":                nil,
		"average":             this.SafeString(order, "filled_price"),
		"filled":              this.SafeString(order, "filled_quantity"),
		"remaining":           nil,
		"status":              status,
		"fee":                 nil,
		"trades":              []any{},
		"fees":                []any{},
		"lastUpdateTimestamp": this.Parse8601(this.SafeString(order, "updated_at")),
		"reduceOnly":          this.SafeBool(order, "reduce_only"),
	}, market)
}

/**
 * @method
 * @name mudrex#cancelOrder
 * @description cancels an open order
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} id order id
 * @param {string} [symbol] unified symbol of the market the order was made in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} An [order structure](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) CancelOrderAsync(id any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.cancelOrderBody(ch, id, optionalArgs...)
	return ch
}
func (this *Mudrex) cancelOrderBody(ch chan any, id any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes90712 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes90712)
	}
	var market any = nil
	if IsTrue(!IsEqual(symbol, nil)) {
		market = this.Market(symbol)
	}
	var request map[string]any = map[string]any{
		"order_id": id,
	}

	response := (<-this.PrivateDeleteFuturesOrdersOrderId(this.Extend(request, params)))
	PanicOnError(response)
	var data any = this.SafeDict(response, "data", response)

	ch <- this.ParseOrder(data, market)
	return nil
}

/**
 * @method
 * @name mudrex#fetchOrder
 * @description fetches information on an order made by the user
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} id the order id
 * @param {string} [symbol] unified symbol of the market the order was made in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} An [order structure](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) FetchOrderAsync(id any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchOrderBody(ch, id, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchOrderBody(ch chan any, id any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes93312 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes93312)
	}
	var market any = nil
	if IsTrue(!IsEqual(symbol, nil)) {
		market = this.Market(symbol)
	}
	var request map[string]any = map[string]any{
		"order_id": id,
	}

	response := (<-this.PrivateGetFuturesOrdersOrderId(this.Extend(request, params)))
	PanicOnError(response)
	var data any = this.SafeDict(response, "data", response)

	ch <- this.ParseOrder(data, market)
	return nil
}

/**
 * @method
 * @name mudrex#fetchOrdersByState
 * @ignore
 * @description fetches a list of orders filtered by their state
 * @param {string} state the state of the orders to fetch
 * @param {string} [symbol] unified market symbol
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {Order[]} a list of [order structures](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) FetchOrdersByStateAsync(state any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchOrdersByStateBody(ch, state, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchOrdersByStateBody(ch chan any, state any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes96112 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes96112)
	}
	var q map[string]any = map[string]any{}
	if IsTrue(!IsEqual(limit, nil)) {
		AddElementToObject(q, "limit", limit)
	}
	var request map[string]any = this.Extend(q, params)
	var response any = nil
	if IsTrue(IsEqual(state, "closed")) {

		response = (<-this.PrivateGetFuturesOrdersHistory(request))
		PanicOnError(response)
	} else {

		response = (<-this.PrivateGetFuturesOrders(request))
		PanicOnError(response)
	}
	var data any = this.SafeValue(response, "data", []any{})
	var rows []any = this.ToArray(data)
	var market any = nil
	if IsTrue(!IsEqual(symbol, nil)) {
		market = this.Market(symbol)
	}
	var orders any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(rows)); i++ {
		AppendToArray(&orders, this.ParseOrder(GetValue(rows, i), market))
	}

	ch <- this.FilterBySymbolSinceLimit(orders, symbol, since, limit)
	return nil
}

/**
 * @method
 * @name mudrex#fetchOrders
 * @description fetches information on multiple orders made by the user
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} [symbol] unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {Order[]} a list of [order structures](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) FetchOrdersAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchOrdersBody(ch, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchOrdersBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params

	retRes99915 := (<-this.FetchOrdersByStateAsync("closed", symbol, since, limit, params))
	PanicOnError(retRes99915)
	ch <- retRes99915
	return nil
}

/**
 * @method
 * @name mudrex#fetchOpenOrders
 * @description fetch all unfilled currently open orders
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} [symbol] unified market symbol
 * @param {int} [since] the earliest time in ms to fetch open orders for
 * @param {int} [limit] the maximum number of open order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {Order[]} a list of [order structures](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) FetchOpenOrdersAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchOpenOrdersBody(ch, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchOpenOrdersBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params

	retRes101415 := (<-this.FetchOrdersByStateAsync("open", symbol, since, limit, params))
	PanicOnError(retRes101415)
	ch <- retRes101415
	return nil
}

/**
 * @method
 * @name mudrex#fetchClosedOrders
 * @description fetches information on multiple closed orders made by the user
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} [symbol] unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {Order[]} a list of [order structures](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) FetchClosedOrdersAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchClosedOrdersBody(ch, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchClosedOrdersBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params

	retRes102915 := (<-this.FetchOrdersByStateAsync("closed", symbol, since, limit, params))
	PanicOnError(retRes102915)
	ch <- retRes102915
	return nil
}

/**
 * @method
 * @name mudrex#fetchPositions
 * @description fetch all open positions
 * @see https://docs.trade.mudrex.com/docs
 * @param {string[]} [symbols] list of unified market symbols
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.trade_currency] the settlement currency to query positions for
 * @returns {object[]} a list of [position structures](https://docs.ccxt.com/#/?id=position-structure)
 */
func (this *Mudrex) FetchPositionsAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchPositionsBody(ch, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchPositionsBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbols := GetArg(optionalArgs, 0, nil)
	_ = symbols
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes104412 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes104412)
	}
	var q map[string]any = map[string]any{}

	response := (<-this.PrivateGetFuturesPositions(this.Extend(q, params)))
	PanicOnError(response)
	var data any = this.SafeValue(response, "data", []any{})
	if IsTrue(IsEqual(data, nil)) {

		ch <- []any{}
		return nil
	}
	var rows []any = this.ToArray(data)
	var outPos any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(rows)); i++ {
		var p any = GetValue(rows, i)
		var symRaw any = this.SafeString(p, "symbol")
		var m any = this.SafeMarket(symRaw)
		var pos any = this.ParsePosition(p, m)
		AppendToArray(&outPos, pos)
	}

	ch <- this.FilterByArrayPositions(outPos, "symbol", symbols, false)
	return nil
}

/**
 * @method
 * @name mudrex#fetchPositionsHistory
 * @description fetches the history of closed positions
 * @see https://docs.trade.mudrex.com/docs/get-position-history
 * @param {string[]} [symbols] a list of unified market symbols
 * @param {int} [since] the earliest time in ms to fetch positions for
 * @param {int} [limit] the maximum number of position structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.trade_currency] the settlement currency to filter positions by
 * @returns {object[]} a list of [position structures](https://docs.ccxt.com/#/?id=position-structure)
 */
func (this *Mudrex) FetchPositionsHistoryAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchPositionsHistoryBody(ch, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchPositionsHistoryBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbols := GetArg(optionalArgs, 0, nil)
	_ = symbols
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes107812 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes107812)
	}
	symbols = this.MarketSymbols(symbols)
	var request map[string]any = map[string]any{}
	if IsTrue(!IsEqual(limit, nil)) {
		AddElementToObject(request, "limit", limit)
	}

	response := (<-this.PrivateGetFuturesPositionsHistory(this.Extend(request, params)))
	PanicOnError(response)
	//
	//     {
	//         "success": true,
	//         "data": [
	//             {
	//                 "id": "019f1ed6-...",
	//                 "position_type": "SHORT",
	//                 "status": "CLOSED",
	//                 "leverage": "3",
	//                 "entry_price": "1.3112",
	//                 "closed_price": "1.3395",
	//                 "quantity": "34",
	//                 "pnl": "-0.9622",
	//                 "created_at": "2026-07-01T10:18:57Z",
	//                 "updated_at": "2026-07-01T18:00:21Z",
	//                 "symbol": "CAKEUSDT",
	//                 "trade_currency": "USDT"
	//             }
	//         ]
	//     }
	//
	var data any = this.SafeList(response, "data", []any{})
	var positions any = this.ParsePositions(data, symbols)

	ch <- this.FilterBySinceLimit(positions, since, limit)
	return nil
}
func (this *Mudrex) ParsePosition(position any, optionalArgs ...any) any {
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	market = this.SafeMarket(nil, market)
	var ms any = this.SafeString(position, "symbol")
	var symbol any = this.SafeSymbol(ms, market)
	// open positions use "order_type", closed positions (history) use "position_type"
	var rawSide any = this.SafeStringUpper2(position, "order_type", "position_type")
	var side any = nil
	if IsTrue(IsEqual(rawSide, "LONG")) {
		side = "long"
	} else if IsTrue(IsEqual(rawSide, "SHORT")) {
		side = "short"
	}
	var ts any = this.Parse8601(this.SafeString(position, "updated_at"))
	if IsTrue(IsEqual(ts, nil)) {
		ts = this.Parse8601(this.SafeString(position, "created_at"))
	}
	var quantityString any = this.SafeString(position, "quantity")
	var entryPriceString any = this.SafeString(position, "entry_price")
	var contractSizeString any = this.SafeString(market, "contractSize", "1")
	var notional any = nil
	if IsTrue(IsTrue((!IsEqual(quantityString, nil))) && IsTrue((!IsEqual(entryPriceString, nil)))) {
		notional = this.ParseNumber(Precise.StringMul(Precise.StringMul(quantityString, entryPriceString), contractSizeString))
	}
	var initialMargin any = this.SafeString(position, "initial_margin")
	return map[string]any{
		"info":                        position,
		"id":                          this.SafeString(position, "id"),
		"symbol":                      symbol,
		"timestamp":                   ts,
		"datetime":                    this.Iso8601(ts),
		"isolated":                    true,
		"hedged":                      false,
		"side":                        side,
		"contracts":                   this.SafeNumber(position, "quantity"),
		"contractSize":                this.SafeNumber(market, "contractSize"),
		"entryPrice":                  this.SafeNumber(position, "entry_price"),
		"markPrice":                   nil,
		"lastPrice":                   this.SafeNumber(position, "closed_price"),
		"notional":                    notional,
		"leverage":                    this.SafeInteger(position, "leverage"),
		"collateral":                  this.ParseNumber(initialMargin),
		"initialMargin":               this.ParseNumber(initialMargin),
		"initialMarginPercentage":     nil,
		"maintenanceMargin":           this.SafeNumber(position, "maintenance_margin"),
		"maintenanceMarginPercentage": nil,
		"unrealizedPnl":               nil,
		"realizedPnl":                 this.SafeNumber(position, "pnl"),
		"liquidationPrice":            this.SafeNumber(position, "liquidation_price"),
		"marginMode":                  "isolated",
		"percentage":                  nil,
	}
}

/**
 * @method
 * @name mudrex#closePosition
 * @description closes an open position for a market
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} symbol unified CCXT market symbol
 * @param {string} [side] 'buy' or 'sell', not required by mudrex
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.position_id] the id of the position to close, resolved from the symbol if not provided
 * @param {float} [params.amount] the amount to close for a partial close, closes the whole position if not provided
 * @returns {object} an [order structure](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) ClosePositionAsync(symbol any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.closePositionBody(ch, symbol, optionalArgs...)
	return ch
}
func (this *Mudrex) closePositionBody(ch chan any, symbol any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	side := GetArg(optionalArgs, 0, nil)
	_ = side
	params := GetArg(optionalArgs, 1, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes117912 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes117912)
	}
	var positionId any = this.SafeString(params, "position_id")
	var amount any = this.SafeValue(params, "amount")
	if IsTrue(IsEqual(positionId, nil)) {
		var market any = this.Market(symbol)

		positions := (<-this.FetchPositionsAsync([]any{symbol}, params))
		PanicOnError(positions)
		for i := 0; IsLessThan(i, GetArrayLength(positions)); i++ {
			var p any = GetValue(positions, i)
			if IsTrue(IsTrue(!IsEqual(side, nil)) && IsTrue(!IsEqual(GetValue(p, "side"), side))) {
				continue
			}
			if IsTrue(IsEqual(GetValue(p, "symbol"), GetValue(market, "symbol"))) {
				positionId = this.SafeString(p, "id")
				break
			}
		}
	}
	if IsTrue(IsEqual(positionId, nil)) {
		panic(OrderNotFound(Add(this.Id, " closePosition() could not resolve position_id")))
	}
	var request map[string]any = map[string]any{
		"position_id": positionId,
	}
	if IsTrue(!IsEqual(amount, nil)) {
		var orderType any = this.SafeStringUpper(params, "order_type", "LIMIT")
		AddElementToObject(request, "order_type", orderType)
		AddElementToObject(request, "quantity", this.AmountToPrecision(symbol, amount))
		var lp any = this.SafeString(params, "limit_price")
		if IsTrue(IsTrue(IsEqual(orderType, "LIMIT")) && IsTrue(!IsEqual(lp, nil))) {
			AddElementToObject(request, "limit_price", lp)
		}
		params = this.Omit(params, []any{"order_type", "limit_price", "amount", "position_id"})

		partialResponse := (<-this.PrivatePostFuturesPositionsPositionIdClosePartial(this.Extend(request, params)))
		PanicOnError(partialResponse)

		ch <- partialResponse
		return nil
	}
	params = this.Omit(params, []any{"position_id"})

	response := (<-this.PrivatePostFuturesPositionsPositionIdClose(this.Extend(request, params)))
	PanicOnError(response)

	ch <- response
	return nil
}

/**
 * @method
 * @name mudrex#addMargin
 * @description add margin to a position
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} symbol unified market symbol
 * @param {float} amount amount of margin to add
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.position_id] the id of the position to add margin to, resolved from the symbol if not provided
 * @returns {object} a [margin structure](https://docs.ccxt.com/#/?id=add-margin-structure)
 */
func (this *Mudrex) AddMarginAsync(symbol any, amount any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.addMarginBody(ch, symbol, amount, optionalArgs...)
	return ch
}
func (this *Mudrex) addMarginBody(ch chan any, symbol any, amount any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes123312 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes123312)
	}
	var positionId any = this.SafeString(params, "position_id")
	if IsTrue(IsEqual(positionId, nil)) {

		positions := (<-this.FetchPositionsAsync([]any{symbol}, params))
		PanicOnError(positions)
		for i := 0; IsLessThan(i, GetArrayLength(positions)); i++ {
			var p any = GetValue(positions, i)
			if IsTrue(IsEqual(GetValue(p, "symbol"), symbol)) {
				positionId = this.SafeString(p, "id")
				break
			}
		}
	}
	if IsTrue(IsEqual(positionId, nil)) {
		panic(OrderNotFound(Add(this.Id, " addMargin() could not resolve position_id")))
	}
	var request map[string]any = map[string]any{
		"position_id": positionId,
		"margin":      this.CostToPrecision(symbol, amount),
	}
	params = this.Omit(params, []any{"position_id"})

	response := (<-this.PrivatePostFuturesPositionsPositionIdAddMargin(this.Extend(request, params)))
	PanicOnError(response)

	ch <- response
	return nil
}

/**
 * @method
 * @name mudrex#reduceMargin
 * @description remove margin from a position
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} symbol unified market symbol
 * @param {float} amount the amount of margin to remove
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [margin structure](https://docs.ccxt.com/#/?id=reduce-margin-structure)
 */
func (this *Mudrex) ReduceMarginAsync(symbol any, amount any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.reduceMarginBody(ch, symbol, amount, optionalArgs...)
	return ch
}
func (this *Mudrex) reduceMarginBody(ch chan any, symbol any, amount any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params

	retRes126915 := (<-this.AddMarginAsync(symbol, OpNeg(amount), params))
	PanicOnError(retRes126915)
	ch <- retRes126915
	return nil
}

/**
 * @method
 * @name mudrex#fetchMyTrades
 * @description fetch all trades made by the user, derived from the TRANSACTION rows of the fee history endpoint - FUNDING rows are excluded and each fill's REBATE row is netted into the trade fee
 * @see https://docs.trade.mudrex.com/docs/fees
 * @param {string} [symbol] unified market symbol, applied client-side because the endpoint has no symbol filter
 * @param {int} [since] the earliest time in ms to fetch trades for, applied client-side
 * @param {int} [limit] the maximum number of trade structures to retrieve, further pages are requested until the limit is satisfied, the history ends or the page cap is reached
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.trade_currency] the settlement currency to filter trades by, 'USDT' (default) or 'INR'
 * @param {int} [params.paginationCalls] the maximum number of pages to request (default 10) - a symbol with few or no recent fills can exhaust the cap and return fewer than limit trades
 * @returns {Trade[]} a list of [trade structures](https://docs.ccxt.com/#/?id=trade-structure)
 */
func (this *Mudrex) FetchMyTradesAsync(optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.fetchMyTradesBody(ch, optionalArgs...)
	return ch
}
func (this *Mudrex) fetchMyTradesBody(ch chan any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	symbol := GetArg(optionalArgs, 0, nil)
	_ = symbol
	since := GetArg(optionalArgs, 1, nil)
	_ = since
	limit := GetArg(optionalArgs, 2, nil)
	_ = limit
	params := GetArg(optionalArgs, 3, map[string]any{})
	_ = params
	if IsTrue(IsEqual(this.Markets, nil)) {

		retRes128712 := (<-this.LoadMarketsAsync())
		PanicOnError(retRes128712)
	}
	var market any = nil
	if IsTrue(!IsEqual(symbol, nil)) {
		market = this.Market(symbol)
	}
	var maxCalls any = nil
	maxCallsparamsVariable := this.HandleOptionAndParams(params, "fetchMyTrades", "paginationCalls", 10)
	maxCalls = GetValue(maxCallsparamsVariable, 0)
	params = GetValue(maxCallsparamsVariable, 1)
	var pageSize any = 0
	if IsTrue(!IsEqual(limit, nil)) {
		// every fill produces a TRANSACTION row plus a REBATE row and funding rows share the page, so over-request and paginate until the unified limit is satisfied
		pageSize = Multiply(limit, 2)
	}
	var allRows any = []any{}
	var transactionsCount any = 0
	var calls any = 0
	var offset any = 0
	var paging bool = true
	for IsEqual(paging, true) {
		var request map[string]any = map[string]any{}
		if IsTrue(IsGreaterThan(pageSize, 0)) {
			AddElementToObject(request, "limit", pageSize)
			AddElementToObject(request, "offset", offset)
		}

		response := (<-this.PrivateGetFuturesFeeHistory(this.Extend(request, params)))
		PanicOnError(response)
		var data any = this.SafeList(response, "data", []any{})
		var dataLength int = GetArrayLength(data)
		for i := 0; IsLessThan(i, dataLength); i++ {
			var entry any = GetValue(data, i)
			AppendToArray(&allRows, entry)
			if IsTrue(IsEqual(this.SafeString(entry, "fee_type"), "TRANSACTION")) {
				// count only rows the client-side symbol filter keeps, otherwise a symbol-filtered call under-returns
				if IsTrue(IsTrue((IsEqual(market, nil))) || IsTrue((IsEqual(this.SafeString(entry, "symbol"), GetValue(market, "id"))))) {
					transactionsCount = this.Sum(transactionsCount, 1)
				}
			}
		}
		calls = this.Sum(calls, 1)
		paging = false
		// the page cap bounds the walk when the requested symbol has few or no rows anywhere near the top of the history
		if IsTrue(IsTrue(IsTrue(IsTrue((!IsEqual(limit, nil))) && IsTrue((IsEqual(dataLength, pageSize)))) && IsTrue((IsLessThan(transactionsCount, limit)))) && IsTrue((IsLessThan(calls, maxCalls)))) {
			// this.sum keeps the offset numeric across the php transpile, see https://github.com/ccxt/ccxt/pull/29684
			offset = this.Sum(offset, pageSize)
			paging = true
		}
	}
	// a REBATE row is a partial refund of one fill's TRANSACTION fee, matched by symbol, time and notional - each rebate is consumed once, so equal fills sharing a key net exactly one refund apiece
	var rebateKeys any = []any{}
	var rebateAmounts any = []any{}
	var transactions any = []any{}
	var transactionKeys any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(allRows)); i++ {
		var entry any = GetValue(allRows, i)
		var feeType any = this.SafeString(entry, "fee_type")
		var pairKey any = Add(Add(Add(Add(this.SafeString(entry, "symbol", ""), ":"), this.SafeString(entry, "created_at", "")), ":"), this.SafeString(entry, "transaction_amount", ""))
		if IsTrue(IsEqual(feeType, "TRANSACTION")) {
			AppendToArray(&transactions, entry)
			AppendToArray(&transactionKeys, pairKey)
		} else if IsTrue(IsEqual(feeType, "REBATE")) {
			AppendToArray(&rebateKeys, pairKey)
			AppendToArray(&rebateAmounts, this.SafeString(entry, "fee_amount", "0"))
		}
	}
	var rows any = []any{}
	for i := 0; IsLessThan(i, GetArrayLength(transactions)); i++ {
		var rebate any = nil
		for j := 0; IsLessThan(j, GetArrayLength(rebateKeys)); j++ {
			if IsTrue(IsEqual(GetValue(rebateKeys, j), GetValue(transactionKeys, i))) {
				rebate = GetValue(rebateAmounts, j)
				// blank the consumed key so the next equal fill matches the next rebate, never the same one twice
				AddElementToObject(rebateKeys, j, nil)
				break
			}
		}
		if IsTrue(IsEqual(rebate, nil)) {
			AppendToArray(&rows, GetValue(transactions, i))
		} else {
			AppendToArray(&rows, this.Extend(GetValue(transactions, i), map[string]any{
				"rebate_amount": rebate,
			}))
		}
	}

	ch <- this.ParseTrades(rows, market, since, limit)
	return nil
}
func (this *Mudrex) ParseTrade(trade any, optionalArgs ...any) any {
	//
	//     {
	//         "id": "019f21e1-9093-7333-866d-31f19c1300ed",
	//         "symbol": "APTUSDT",
	//         "fee_amount": "0.02468116",
	//         "fee_perc": "0.05900003",
	//         "fee_type": "TRANSACTION",
	//         "created_at": "2026-07-02T08:10:58Z",
	//         "transaction_amount": "41.83245",
	//         "trade_currency": "USDT",
	//         "order_type": "LONG",
	//         "trigger_type": "MARKET",
	//         "gst_amount": "0.00376492"
	//     }
	//
	market := GetArg(optionalArgs, 0, nil)
	_ = market
	var ms any = this.SafeString(trade, "symbol")
	market = this.SafeMarket(ms, market)
	var symbol any = GetValue(market, "symbol")
	var ts any = this.Parse8601(this.SafeString(trade, "created_at"))
	// exit fills carry STOPLOSS / TAKEPROFIT markers without the closing direction, so their unified direction stays undefined
	var side any = this.SafeStringLower(trade, "order_type")
	var tradeSide any = nil
	if IsTrue(IsEqual(side, "long")) {
		tradeSide = "buy"
	} else if IsTrue(IsEqual(side, "short")) {
		tradeSide = "sell"
	}
	var trig any = this.SafeStringUpper(trade, "trigger_type")
	var takerOrMaker any = nil
	if IsTrue(IsEqual(trig, "MARKET")) {
		// a market execution always takes liquidity, a limit execution can be either
		takerOrMaker = "taker"
	}
	var fee any = nil
	var feeCostString any = this.SafeString(trade, "fee_amount")
	// rebate_amount is attached by fetchMyTrades from the fill's REBATE row - the reported fee is the net charge
	var rebateString any = this.SafeString(trade, "rebate_amount")
	if IsTrue(IsTrue((!IsEqual(feeCostString, nil))) && IsTrue((!IsEqual(rebateString, nil)))) {
		feeCostString = Precise.StringSub(feeCostString, rebateString)
	}
	if IsTrue(!IsEqual(feeCostString, nil)) {
		fee = map[string]any{
			"cost":     feeCostString,
			"currency": this.SafeString(trade, "trade_currency"),
		}
	}
	return this.SafeTrade(map[string]any{
		"info":         trade,
		"timestamp":    ts,
		"datetime":     this.Iso8601(ts),
		"symbol":       symbol,
		"id":           this.SafeString(trade, "id"),
		"order":        nil,
		"type":         this.SafeStringLower(trade, "trigger_type"),
		"side":         tradeSide,
		"takerOrMaker": takerOrMaker,
		"price":        nil,
		"amount":       nil,
		"cost":         this.SafeString(trade, "transaction_amount"),
		"fee":          fee,
	}, market)
}

/**
 * @method
 * @name mudrex#transfer
 * @description transfer currency internally between wallets on the same account
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} code unified currency code
 * @param {float} amount amount to transfer
 * @param {string} fromAccount 'spot' or 'futures'
 * @param {string} toAccount 'spot' or 'futures'
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [transfer structure](https://docs.ccxt.com/#/?id=transfer-structure)
 */
func (this *Mudrex) TransferAsync(code any, amount any, fromAccount any, toAccount any, optionalArgs ...any) <-chan any {
	ch := make(chan any, 1)
	go this.transferBody(ch, code, amount, fromAccount, toAccount, optionalArgs...)
	return ch
}
func (this *Mudrex) transferBody(ch chan any, code any, amount any, fromAccount any, toAccount any, optionalArgs ...any) any {
	defer close(ch)
	defer ReturnPanicError(ch)
	params := GetArg(optionalArgs, 0, map[string]any{})
	_ = params
	var mp map[string]any = map[string]any{
		"spot":    "SPOT",
		"SPOT":    "SPOT",
		"futures": "FUTURES",
		"future":  "FUTURES",
		"FUTURES": "FUTURES",
	}
	var fw any = this.SafeString(mp, fromAccount, ToUpper(fromAccount))
	var tw any = this.SafeString(mp, toAccount, ToUpper(toAccount))
	var body map[string]any = map[string]any{
		"from_wallet_type": fw,
		"to_wallet_type":   tw,
		"amount":           this.NumberToString(amount),
	}
	var useInr bool = false
	if IsTrue(IsEqual(code, "INR")) {
		useInr = true
	} else {
		// default USDT does not use the inr path
		var tradeCurrency any = this.SafeString2(params, "trade_currency", "tradeCurrency")
		if IsTrue(IsEqual(tradeCurrency, "INR")) {
			useInr = true
		}
	}
	var response any = nil
	if IsTrue(useInr) {

		response = (<-this.PrivatePostFuturesTransfersInr(this.Extend(body, params)))
		PanicOnError(response)
	} else {

		response = (<-this.PrivatePostWalletFuturesTransfer(this.Extend(body, params)))
		PanicOnError(response)
	}
	var data any = this.SafeDict(response, "data", response)

	ch <- map[string]any{
		"info":        response,
		"id":          this.SafeString(data, "id"),
		"timestamp":   nil,
		"datetime":    nil,
		"currency":    code,
		"amount":      amount,
		"fromAccount": fw,
		"toAccount":   tw,
		"status":      "ok",
	}
	return nil
}

func NewMudrex(userConfig map[string]any) *Mudrex {
	p := newMudrex()
	p.Init(userConfig)
	return p
}

func (this *Mudrex) Init(userConfig map[string]any) {
	this.Exchange = Exchange{}
	this.Exchange.DerivedExchange = this
	this.Exchange.InitParent(userConfig, this.Describe().(map[string]any), this)
	this.exchangeTyped = NewExchangeTyped(&this.Exchange)
}

// typed methods
/**
 * @method
 * @name mudrex#fetchOHLCV
 * @description fetches historical candlestick data containing the open, high, low, and close price, and the volume of a market
 * @see https://docs.trade.mudrex.com/docs/historical-kline
 * @param {string} symbol unified symbol of the market to fetch OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum amount of candles to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.until] timestamp in ms of the latest candle to fetch
 * @param {string} [params.price] "mark" to fetch mark price candles
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 */
func (this *Mudrex) FetchOHLCV(symbol string, options ...FetchOHLCVOptions) ([]OHLCV, error) {

	opts := FetchOHLCVOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var timeframe *string = opts.Timeframe

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchOHLCVAsync(symbol, timeframe, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewOHLCVArray(res), nil
}

/**
 * @method
 * @name mudrex#fetchMarkOHLCV
 * @description fetches historical mark price candlestick data containing the open, high, low, and close price of a market
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} symbol unified symbol of the market to fetch OHLCV data for
 * @param {string} timeframe the length of time each candle represents
 * @param {int} [since] timestamp in ms of the earliest candle to fetch
 * @param {int} [limit] the maximum amount of candles to fetch
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
 */
func (this *Mudrex) FetchMarkOHLCV(symbol string, options ...FetchMarkOHLCVOptions) ([]OHLCV, error) {

	opts := FetchMarkOHLCVOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var timeframe *string = opts.Timeframe

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchMarkOHLCVAsync(symbol, timeframe, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewOHLCVArray(res), nil
}

/**
 * @method
 * @name mudrex#fetchTicker
 * @description fetches a price ticker, a statistical calculation with the information calculated over the past 24 hours for a specific market
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} symbol unified symbol of the market to fetch the ticker for
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [ticker structure](https://docs.ccxt.com/#/?id=ticker-structure)
 */
func (this *Mudrex) FetchTicker(symbol string, options ...FetchTickerOptions) (Ticker, error) {

	opts := FetchTickerOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var params *map[string]any = opts.Params
	res := <-this.FetchTickerAsync(symbol, params)
	if IsError(res) {
		return Ticker{}, CreateReturnError(res)
	}
	return NewTicker(res), nil
}

/**
 * @method
 * @name mudrex#fetchTickers
 * @description fetches price tickers for multiple markets, statistical information calculated over the past 24 hours for each market
 * @see https://docs.trade.mudrex.com/docs
 * @param {string[]} [symbols] unified symbols of the markets to fetch the ticker for, all market tickers are returned if not assigned
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a dictionary of [ticker structures](https://docs.ccxt.com/#/?id=ticker-structure)
 */
func (this *Mudrex) FetchTickers(options ...FetchTickersOptions) (Tickers, error) {

	opts := FetchTickersOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbols *[]string = opts.Symbols

	var params *map[string]any = opts.Params
	res := <-this.FetchTickersAsync(symbols, params)
	if IsError(res) {
		return Tickers{}, CreateReturnError(res)
	}
	return NewTickers(res), nil
}

/**
 * @method
 * @name mudrex#fetchMarkets
 * @description retrieves data on all markets for the exchange
 * @see https://docs.trade.mudrex.com/docs
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object[]} an array of objects representing market data
 */
func (this *Mudrex) FetchMarkets(params ...any) ([]MarketInterface, error) {
	res := <-this.FetchMarketsAsync(params...)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewMarketInterfaceArray(res), nil
}

/**
 * @method
 * @name mudrex#fetchBalance
 * @description query for balance and get the amount of funds available for trading or funds locked in orders
 * @see https://docs.trade.mudrex.com/docs
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.type] 'swap' (default) or 'spot' - which wallet balance to fetch
 * @param {string} [params.trade_currency] the settlement currency to query the balance for
 * @returns {object} a [balance structure](https://docs.ccxt.com/#/?id=balance-structure)
 */
func (this *Mudrex) FetchBalance(params ...any) (Balances, error) {
	res := <-this.FetchBalanceAsync(params...)
	if IsError(res) {
		return Balances{}, CreateReturnError(res)
	}
	return NewBalances(res), nil
}

/**
 * @method
 * @name mudrex#fetchLeverage
 * @description fetch the set leverage for a market
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} symbol unified market symbol
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [leverage structure](https://docs.ccxt.com/#/?id=leverage-structure)
 */
func (this *Mudrex) FetchLeverage(symbol string, options ...FetchLeverageOptions) (Leverage, error) {

	opts := FetchLeverageOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var params *map[string]any = opts.Params
	res := <-this.FetchLeverageAsync(symbol, params)
	if IsError(res) {
		return Leverage{}, CreateReturnError(res)
	}
	return NewLeverage(res), nil
}

/**
 * @method
 * @name mudrex#setLeverage
 * @description set the level of leverage for a market
 * @see https://docs.trade.mudrex.com/docs
 * @param {float} leverage the rate of leverage
 * @param {string} symbol unified market symbol
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.marginType] 'ISOLATED' (default) or 'CROSSED'
 * @returns {object} response from the exchange
 */
func (this *Mudrex) SetLeverage(leverage int64, options ...SetLeverageOptions) (map[string]any, error) {

	opts := SetLeverageOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var params *map[string]any = opts.Params
	res := <-this.SetLeverageAsync(leverage, symbol, params)
	if IsError(res) {
		return map[string]any{}, CreateReturnError(res)
	}
	return res.(map[string]any), nil
}

/**
 * @method
 * @name mudrex#createOrder
 * @description create a trade order
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} symbol unified market symbol
 * @param {string} type 'market' or 'limit'
 * @param {string} side 'buy' or 'sell'
 * @param {float} amount how much you want to trade in units of the base currency
 * @param {float} [price] the price to fulfill the order, in units of the quote currency (also required for market orders on this exchange)
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {int} [params.leverage] leverage for the order, required if setLeverage() was not called beforehand
 * @param {bool} [params.reduceOnly] true if the order is reduce only
 * @param {object} [params.takeProfit] *takeProfit object in params* containing the trigger price of the take-profit order attached to this order
 * @param {float} [params.takeProfit.triggerPrice] take profit trigger price
 * @param {object} [params.stopLoss] *stopLoss object in params* containing the trigger price of the stop-loss order attached to this order
 * @param {float} [params.stopLoss.triggerPrice] stop loss trigger price
 * @param {float} [params.takeProfitPrice] the trigger price for a standalone take-profit order on an existing position (requires params.positionId)
 * @param {float} [params.stopLossPrice] the trigger price for a standalone stop-loss order on an existing position (requires params.positionId)
 * @param {string} [params.positionId] the id of the position the standalone stopLossPrice/takeProfitPrice order is attached to
 * @param {string} [params.trade_currency] the settlement currency for the order
 * @returns {object} an [order structure](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) CreateOrder(symbol string, typeVar string, side string, amount float64, options ...CreateOrderOptions) (Order, error) {

	opts := CreateOrderOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var price *float64 = opts.Price

	var params *map[string]any = opts.Params
	res := <-this.CreateOrderAsync(symbol, typeVar, side, amount, price, params)
	if IsError(res) {
		return Order{}, CreateReturnError(res)
	}
	return NewOrder(res), nil
}

/**
 * @method
 * @name mudrex#editOrder
 * @description edit a trade order
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} id order id
 * @param {string} symbol unified symbol of the market to edit an order in
 * @param {string} type 'market' or 'limit'
 * @param {string} side 'buy' or 'sell'
 * @param {float} [amount] how much of the currency you want to trade in units of the base currency
 * @param {float} [price] the price at which the order is to be fulfilled, in units of the quote currency
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} an [order structure](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) EditOrder(id string, symbol string, typeVar string, side string, options ...EditOrderOptions) (Order, error) {

	opts := EditOrderOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var amount *float64 = opts.Amount

	var price *float64 = opts.Price

	var params *map[string]any = opts.Params
	res := <-this.EditOrderAsync(id, symbol, typeVar, side, amount, price, params)
	if IsError(res) {
		return Order{}, CreateReturnError(res)
	}
	return NewOrder(res), nil
}

/**
 * @method
 * @name mudrex#cancelOrder
 * @description cancels an open order
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} id order id
 * @param {string} [symbol] unified symbol of the market the order was made in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} An [order structure](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) CancelOrder(id string, options ...CancelOrderOptions) (Order, error) {

	opts := CancelOrderOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var params *map[string]any = opts.Params
	res := <-this.CancelOrderAsync(id, symbol, params)
	if IsError(res) {
		return Order{}, CreateReturnError(res)
	}
	return NewOrder(res), nil
}

/**
 * @method
 * @name mudrex#fetchOrder
 * @description fetches information on an order made by the user
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} id the order id
 * @param {string} [symbol] unified symbol of the market the order was made in
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} An [order structure](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) FetchOrder(id string, options ...FetchOrderOptions) (Order, error) {

	opts := FetchOrderOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var params *map[string]any = opts.Params
	res := <-this.FetchOrderAsync(id, symbol, params)
	if IsError(res) {
		return Order{}, CreateReturnError(res)
	}
	return NewOrder(res), nil
}

/**
 * @method
 * @name mudrex#fetchOrdersByState
 * @ignore
 * @description fetches a list of orders filtered by their state
 * @param {string} state the state of the orders to fetch
 * @param {string} [symbol] unified market symbol
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {Order[]} a list of [order structures](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) FetchOrdersByState(state string, options ...FetchOrdersByStateOptions) ([]Order, error) {

	opts := FetchOrdersByStateOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchOrdersByStateAsync(state, symbol, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewOrderArray(res), nil
}

/**
 * @method
 * @name mudrex#fetchOrders
 * @description fetches information on multiple orders made by the user
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} [symbol] unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {Order[]} a list of [order structures](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) FetchOrders(options ...FetchOrdersOptions) ([]Order, error) {

	opts := FetchOrdersOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchOrdersAsync(symbol, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewOrderArray(res), nil
}

/**
 * @method
 * @name mudrex#fetchOpenOrders
 * @description fetch all unfilled currently open orders
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} [symbol] unified market symbol
 * @param {int} [since] the earliest time in ms to fetch open orders for
 * @param {int} [limit] the maximum number of open order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {Order[]} a list of [order structures](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) FetchOpenOrders(options ...FetchOpenOrdersOptions) ([]Order, error) {

	opts := FetchOpenOrdersOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchOpenOrdersAsync(symbol, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewOrderArray(res), nil
}

/**
 * @method
 * @name mudrex#fetchClosedOrders
 * @description fetches information on multiple closed orders made by the user
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} [symbol] unified market symbol of the market orders were made in
 * @param {int} [since] the earliest time in ms to fetch orders for
 * @param {int} [limit] the maximum number of order structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {Order[]} a list of [order structures](https://docs.ccxt.com/#/?id=order-structure)
 */
func (this *Mudrex) FetchClosedOrders(options ...FetchClosedOrdersOptions) ([]Order, error) {

	opts := FetchClosedOrdersOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchClosedOrdersAsync(symbol, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewOrderArray(res), nil
}

/**
 * @method
 * @name mudrex#fetchPositions
 * @description fetch all open positions
 * @see https://docs.trade.mudrex.com/docs
 * @param {string[]} [symbols] list of unified market symbols
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.trade_currency] the settlement currency to query positions for
 * @returns {object[]} a list of [position structures](https://docs.ccxt.com/#/?id=position-structure)
 */
func (this *Mudrex) FetchPositions(options ...FetchPositionsOptions) ([]Position, error) {

	opts := FetchPositionsOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbols *[]string = opts.Symbols

	var params *map[string]any = opts.Params
	res := <-this.FetchPositionsAsync(symbols, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewPositionArray(res), nil
}

/**
 * @method
 * @name mudrex#fetchPositionsHistory
 * @description fetches the history of closed positions
 * @see https://docs.trade.mudrex.com/docs/get-position-history
 * @param {string[]} [symbols] a list of unified market symbols
 * @param {int} [since] the earliest time in ms to fetch positions for
 * @param {int} [limit] the maximum number of position structures to retrieve
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.trade_currency] the settlement currency to filter positions by
 * @returns {object[]} a list of [position structures](https://docs.ccxt.com/#/?id=position-structure)
 */
func (this *Mudrex) FetchPositionsHistory(options ...FetchPositionsHistoryOptions) ([]Position, error) {

	opts := FetchPositionsHistoryOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbols *[]string = opts.Symbols

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchPositionsHistoryAsync(symbols, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewPositionArray(res), nil
}

/**
 * @method
 * @name mudrex#fetchMyTrades
 * @description fetch all trades made by the user, derived from the TRANSACTION rows of the fee history endpoint - FUNDING rows are excluded and each fill's REBATE row is netted into the trade fee
 * @see https://docs.trade.mudrex.com/docs/fees
 * @param {string} [symbol] unified market symbol, applied client-side because the endpoint has no symbol filter
 * @param {int} [since] the earliest time in ms to fetch trades for, applied client-side
 * @param {int} [limit] the maximum number of trade structures to retrieve, further pages are requested until the limit is satisfied, the history ends or the page cap is reached
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @param {string} [params.trade_currency] the settlement currency to filter trades by, 'USDT' (default) or 'INR'
 * @param {int} [params.paginationCalls] the maximum number of pages to request (default 10) - a symbol with few or no recent fills can exhaust the cap and return fewer than limit trades
 * @returns {Trade[]} a list of [trade structures](https://docs.ccxt.com/#/?id=trade-structure)
 */
func (this *Mudrex) FetchMyTrades(options ...FetchMyTradesOptions) ([]Trade, error) {

	opts := FetchMyTradesOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var symbol *string = opts.Symbol

	var since *int64 = opts.Since

	var limit *int64 = opts.Limit

	var params *map[string]any = opts.Params
	res := <-this.FetchMyTradesAsync(symbol, since, limit, params)
	if IsError(res) {
		return nil, CreateReturnError(res)
	}
	return NewTradeArray(res), nil
}

/**
 * @method
 * @name mudrex#transfer
 * @description transfer currency internally between wallets on the same account
 * @see https://docs.trade.mudrex.com/docs
 * @param {string} code unified currency code
 * @param {float} amount amount to transfer
 * @param {string} fromAccount 'spot' or 'futures'
 * @param {string} toAccount 'spot' or 'futures'
 * @param {object} [params] extra parameters specific to the exchange API endpoint
 * @returns {object} a [transfer structure](https://docs.ccxt.com/#/?id=transfer-structure)
 */
func (this *Mudrex) Transfer(code string, amount float64, fromAccount string, toAccount string, options ...TransferOptions) (TransferEntry, error) {

	opts := TransferOptionsStruct{}

	for _, opt := range options {
		opt(&opts)
	}

	var params *map[string]any = opts.Params
	res := <-this.TransferAsync(code, amount, fromAccount, toAccount, params)
	if IsError(res) {
		return TransferEntry{}, CreateReturnError(res)
	}
	return NewTransferEntry(res), nil
}

// missing typed methods from base
// nolint
func (this *Mudrex) LoadMarkets(params ...any) (map[string]MarketInterface, error) {
	return this.exchangeTyped.LoadMarkets(params...)
}
func (this *Mudrex) CancelOrders(ids []string, options ...CancelOrdersOptions) ([]Order, error) {
	return this.exchangeTyped.CancelOrders(ids, options...)
}
func (this *Mudrex) CancelOrdersWithClientOrderIds(clientOrderIds []string, options ...CancelOrdersWithClientOrderIdsOptions) ([]Order, error) {
	return this.exchangeTyped.CancelOrdersWithClientOrderIds(clientOrderIds, options...)
}
func (this *Mudrex) CancelAllOrders(options ...CancelAllOrdersOptions) ([]Order, error) {
	return this.exchangeTyped.CancelAllOrders(options...)
}
func (this *Mudrex) CancelAllOrdersAfter(timeout int64, options ...CancelAllOrdersAfterOptions) (map[string]any, error) {
	return this.exchangeTyped.CancelAllOrdersAfter(timeout, options...)
}
func (this *Mudrex) CancelOrderWithClientOrderId(clientOrderId string, options ...CancelOrderWithClientOrderIdOptions) (Order, error) {
	return this.exchangeTyped.CancelOrderWithClientOrderId(clientOrderId, options...)
}
func (this *Mudrex) CancelOrdersForSymbols(orders []CancellationRequest, options ...CancelOrdersForSymbolsOptions) ([]Order, error) {
	return this.exchangeTyped.CancelOrdersForSymbols(orders, options...)
}
func (this *Mudrex) CreateConvertTrade(id string, fromCode string, toCode string, options ...CreateConvertTradeOptions) (Conversion, error) {
	return this.exchangeTyped.CreateConvertTrade(id, fromCode, toCode, options...)
}
func (this *Mudrex) CreateDepositAddress(code string, options ...CreateDepositAddressOptions) (DepositAddress, error) {
	return this.exchangeTyped.CreateDepositAddress(code, options...)
}
func (this *Mudrex) CreateLimitBuyOrder(symbol string, amount float64, price float64, options ...CreateLimitBuyOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateLimitBuyOrder(symbol, amount, price, options...)
}
func (this *Mudrex) CreateLimitOrder(symbol string, side string, amount float64, price float64, options ...CreateLimitOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateLimitOrder(symbol, side, amount, price, options...)
}
func (this *Mudrex) CreateLimitSellOrder(symbol string, amount float64, price float64, options ...CreateLimitSellOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateLimitSellOrder(symbol, amount, price, options...)
}
func (this *Mudrex) CreateMarketBuyOrder(symbol string, amount float64, options ...CreateMarketBuyOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketBuyOrder(symbol, amount, options...)
}
func (this *Mudrex) CreateMarketBuyOrderWithCost(symbol string, cost float64, options ...CreateMarketBuyOrderWithCostOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketBuyOrderWithCost(symbol, cost, options...)
}
func (this *Mudrex) CreateMarketOrder(symbol string, side string, amount float64, options ...CreateMarketOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketOrder(symbol, side, amount, options...)
}
func (this *Mudrex) CreateMarketOrderWithCost(symbol string, side string, cost float64, options ...CreateMarketOrderWithCostOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketOrderWithCost(symbol, side, cost, options...)
}
func (this *Mudrex) CreateMarketSellOrder(symbol string, amount float64, options ...CreateMarketSellOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketSellOrder(symbol, amount, options...)
}
func (this *Mudrex) CreateMarketSellOrderWithCost(symbol string, cost float64, options ...CreateMarketSellOrderWithCostOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketSellOrderWithCost(symbol, cost, options...)
}
func (this *Mudrex) CreateOrders(orders []OrderRequest, options ...CreateOrdersOptions) ([]Order, error) {
	return this.exchangeTyped.CreateOrders(orders, options...)
}
func (this *Mudrex) CreateOrderWithTakeProfitAndStopLoss(symbol string, typeVar string, side string, amount float64, options ...CreateOrderWithTakeProfitAndStopLossOptions) (Order, error) {
	return this.exchangeTyped.CreateOrderWithTakeProfitAndStopLoss(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreatePostOnlyOrder(symbol string, typeVar string, side string, amount float64, options ...CreatePostOnlyOrderOptions) (Order, error) {
	return this.exchangeTyped.CreatePostOnlyOrder(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateReduceOnlyOrder(symbol string, typeVar string, side string, amount float64, options ...CreateReduceOnlyOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateReduceOnlyOrder(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateStopLimitOrder(symbol string, side string, amount float64, price float64, triggerPrice float64, options ...CreateStopLimitOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateStopLimitOrder(symbol, side, amount, price, triggerPrice, options...)
}
func (this *Mudrex) CreateStopLossOrder(symbol string, typeVar string, side string, amount float64, options ...CreateStopLossOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateStopLossOrder(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateStopMarketOrder(symbol string, side string, amount float64, triggerPrice float64, options ...CreateStopMarketOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateStopMarketOrder(symbol, side, amount, triggerPrice, options...)
}
func (this *Mudrex) CreateStopOrder(symbol string, typeVar string, side string, amount float64, options ...CreateStopOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateStopOrder(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateTakeProfitOrder(symbol string, typeVar string, side string, amount float64, options ...CreateTakeProfitOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateTakeProfitOrder(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateTrailingAmountOrder(symbol string, typeVar string, side string, amount float64, options ...CreateTrailingAmountOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateTrailingAmountOrder(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateTrailingPercentOrder(symbol string, typeVar string, side string, amount float64, options ...CreateTrailingPercentOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateTrailingPercentOrder(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateTriggerOrder(symbol string, typeVar string, side string, amount float64, options ...CreateTriggerOrderOptions) (Order, error) {
	return this.exchangeTyped.CreateTriggerOrder(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) EditLimitBuyOrder(id string, symbol string, amount float64, options ...EditLimitBuyOrderOptions) (Order, error) {
	return this.exchangeTyped.EditLimitBuyOrder(id, symbol, amount, options...)
}
func (this *Mudrex) EditLimitOrder(id string, symbol string, side string, amount float64, options ...EditLimitOrderOptions) (Order, error) {
	return this.exchangeTyped.EditLimitOrder(id, symbol, side, amount, options...)
}
func (this *Mudrex) EditLimitSellOrder(id string, symbol string, amount float64, options ...EditLimitSellOrderOptions) (Order, error) {
	return this.exchangeTyped.EditLimitSellOrder(id, symbol, amount, options...)
}
func (this *Mudrex) EditOrderWithClientOrderId(clientOrderId string, symbol string, typeVar string, side string, options ...EditOrderWithClientOrderIdOptions) (Order, error) {
	return this.exchangeTyped.EditOrderWithClientOrderId(clientOrderId, symbol, typeVar, side, options...)
}
func (this *Mudrex) EditOrders(orders []OrderRequest, options ...EditOrdersOptions) ([]Order, error) {
	return this.exchangeTyped.EditOrders(orders, options...)
}
func (this *Mudrex) FetchAccounts(params ...any) ([]Account, error) {
	return this.exchangeTyped.FetchAccounts(params...)
}
func (this *Mudrex) FetchAllGreeks(options ...FetchAllGreeksOptions) (AllGreeks, error) {
	return this.exchangeTyped.FetchAllGreeks(options...)
}
func (this *Mudrex) FetchBidsAsks(options ...FetchBidsAsksOptions) (Tickers, error) {
	return this.exchangeTyped.FetchBidsAsks(options...)
}
func (this *Mudrex) FetchBorrowInterest(options ...FetchBorrowInterestOptions) ([]BorrowInterest, error) {
	return this.exchangeTyped.FetchBorrowInterest(options...)
}
func (this *Mudrex) FetchBorrowRate(code string, amount float64, options ...FetchBorrowRateOptions) (map[string]any, error) {
	return this.exchangeTyped.FetchBorrowRate(code, amount, options...)
}
func (this *Mudrex) FetchCanceledAndClosedOrders(options ...FetchCanceledAndClosedOrdersOptions) ([]Order, error) {
	return this.exchangeTyped.FetchCanceledAndClosedOrders(options...)
}
func (this *Mudrex) FetchConvertCurrencies(params ...any) (Currencies, error) {
	return this.exchangeTyped.FetchConvertCurrencies(params...)
}
func (this *Mudrex) FetchConvertQuote(fromCode string, toCode string, options ...FetchConvertQuoteOptions) (Conversion, error) {
	return this.exchangeTyped.FetchConvertQuote(fromCode, toCode, options...)
}
func (this *Mudrex) FetchConvertTrade(id string, options ...FetchConvertTradeOptions) (Conversion, error) {
	return this.exchangeTyped.FetchConvertTrade(id, options...)
}
func (this *Mudrex) FetchConvertTradeHistory(options ...FetchConvertTradeHistoryOptions) ([]Conversion, error) {
	return this.exchangeTyped.FetchConvertTradeHistory(options...)
}
func (this *Mudrex) FetchCrossBorrowRate(code string, options ...FetchCrossBorrowRateOptions) (CrossBorrowRate, error) {
	return this.exchangeTyped.FetchCrossBorrowRate(code, options...)
}
func (this *Mudrex) FetchCrossBorrowRates(params ...any) (CrossBorrowRates, error) {
	return this.exchangeTyped.FetchCrossBorrowRates(params...)
}
func (this *Mudrex) FetchCurrencies(params ...any) (Currencies, error) {
	return this.exchangeTyped.FetchCurrencies(params...)
}
func (this *Mudrex) FetchDepositAddress(code string, options ...FetchDepositAddressOptions) (DepositAddress, error) {
	return this.exchangeTyped.FetchDepositAddress(code, options...)
}
func (this *Mudrex) FetchDepositAddresses(options ...FetchDepositAddressesOptions) ([]DepositAddress, error) {
	return this.exchangeTyped.FetchDepositAddresses(options...)
}
func (this *Mudrex) FetchDepositAddressesByNetwork(code string, options ...FetchDepositAddressesByNetworkOptions) (DepositAddresses, error) {
	return this.exchangeTyped.FetchDepositAddressesByNetwork(code, options...)
}
func (this *Mudrex) FetchDeposits(options ...FetchDepositsOptions) ([]Transaction, error) {
	return this.exchangeTyped.FetchDeposits(options...)
}
func (this *Mudrex) FetchDepositsWithdrawals(options ...FetchDepositsWithdrawalsOptions) ([]Transaction, error) {
	return this.exchangeTyped.FetchDepositsWithdrawals(options...)
}
func (this *Mudrex) FetchDepositWithdrawFee(code string, options ...FetchDepositWithdrawFeeOptions) (DepositWithdrawFee, error) {
	return this.exchangeTyped.FetchDepositWithdrawFee(code, options...)
}
func (this *Mudrex) FetchDepositWithdrawFees(options ...FetchDepositWithdrawFeesOptions) (DepositWithdrawFees, error) {
	return this.exchangeTyped.FetchDepositWithdrawFees(options...)
}
func (this *Mudrex) FetchFreeBalance(params ...any) (Balance, error) {
	return this.exchangeTyped.FetchFreeBalance(params...)
}
func (this *Mudrex) FetchFundingHistory(options ...FetchFundingHistoryOptions) ([]FundingHistory, error) {
	return this.exchangeTyped.FetchFundingHistory(options...)
}
func (this *Mudrex) FetchFundingInterval(symbol string, options ...FetchFundingIntervalOptions) (FundingRate, error) {
	return this.exchangeTyped.FetchFundingInterval(symbol, options...)
}
func (this *Mudrex) FetchFundingIntervals(options ...FetchFundingIntervalsOptions) (FundingRates, error) {
	return this.exchangeTyped.FetchFundingIntervals(options...)
}
func (this *Mudrex) FetchFundingRate(symbol string, options ...FetchFundingRateOptions) (FundingRate, error) {
	return this.exchangeTyped.FetchFundingRate(symbol, options...)
}
func (this *Mudrex) FetchFundingRateHistory(options ...FetchFundingRateHistoryOptions) ([]FundingRateHistory, error) {
	return this.exchangeTyped.FetchFundingRateHistory(options...)
}
func (this *Mudrex) FetchFundingRates(options ...FetchFundingRatesOptions) (FundingRates, error) {
	return this.exchangeTyped.FetchFundingRates(options...)
}
func (this *Mudrex) FetchGreeks(symbol string, options ...FetchGreeksOptions) (Greeks, error) {
	return this.exchangeTyped.FetchGreeks(symbol, options...)
}
func (this *Mudrex) FetchIndexOHLCV(symbol string, options ...FetchIndexOHLCVOptions) ([]OHLCV, error) {
	return this.exchangeTyped.FetchIndexOHLCV(symbol, options...)
}
func (this *Mudrex) FetchIsolatedBorrowRate(symbol string, options ...FetchIsolatedBorrowRateOptions) (IsolatedBorrowRate, error) {
	return this.exchangeTyped.FetchIsolatedBorrowRate(symbol, options...)
}
func (this *Mudrex) FetchIsolatedBorrowRates(params ...any) (IsolatedBorrowRates, error) {
	return this.exchangeTyped.FetchIsolatedBorrowRates(params...)
}
func (this *Mudrex) FetchLastPrices(options ...FetchLastPricesOptions) (LastPrices, error) {
	return this.exchangeTyped.FetchLastPrices(options...)
}
func (this *Mudrex) FetchLedger(options ...FetchLedgerOptions) ([]LedgerEntry, error) {
	return this.exchangeTyped.FetchLedger(options...)
}
func (this *Mudrex) FetchLedgerEntry(id string, options ...FetchLedgerEntryOptions) (LedgerEntry, error) {
	return this.exchangeTyped.FetchLedgerEntry(id, options...)
}
func (this *Mudrex) FetchLeverages(options ...FetchLeveragesOptions) (Leverages, error) {
	return this.exchangeTyped.FetchLeverages(options...)
}
func (this *Mudrex) FetchLeverageTiers(options ...FetchLeverageTiersOptions) (LeverageTiers, error) {
	return this.exchangeTyped.FetchLeverageTiers(options...)
}
func (this *Mudrex) FetchLiquidations(symbol string, options ...FetchLiquidationsOptions) ([]Liquidation, error) {
	return this.exchangeTyped.FetchLiquidations(symbol, options...)
}
func (this *Mudrex) FetchLongShortRatio(symbol string, options ...FetchLongShortRatioOptions) (LongShortRatio, error) {
	return this.exchangeTyped.FetchLongShortRatio(symbol, options...)
}
func (this *Mudrex) FetchLongShortRatioHistory(options ...FetchLongShortRatioHistoryOptions) ([]LongShortRatio, error) {
	return this.exchangeTyped.FetchLongShortRatioHistory(options...)
}
func (this *Mudrex) FetchMarginAdjustmentHistory(options ...FetchMarginAdjustmentHistoryOptions) ([]MarginModification, error) {
	return this.exchangeTyped.FetchMarginAdjustmentHistory(options...)
}
func (this *Mudrex) FetchMarginMode(symbol string, options ...FetchMarginModeOptions) (MarginMode, error) {
	return this.exchangeTyped.FetchMarginMode(symbol, options...)
}
func (this *Mudrex) FetchMarginModes(options ...FetchMarginModesOptions) (MarginModes, error) {
	return this.exchangeTyped.FetchMarginModes(options...)
}
func (this *Mudrex) FetchMarketLeverageTiers(symbol string, options ...FetchMarketLeverageTiersOptions) ([]LeverageTier, error) {
	return this.exchangeTyped.FetchMarketLeverageTiers(symbol, options...)
}
func (this *Mudrex) FetchMarkPrice(symbol string, options ...FetchMarkPriceOptions) (Ticker, error) {
	return this.exchangeTyped.FetchMarkPrice(symbol, options...)
}
func (this *Mudrex) FetchMarkPrices(options ...FetchMarkPricesOptions) (Tickers, error) {
	return this.exchangeTyped.FetchMarkPrices(options...)
}
func (this *Mudrex) FetchMyLiquidations(options ...FetchMyLiquidationsOptions) ([]Liquidation, error) {
	return this.exchangeTyped.FetchMyLiquidations(options...)
}
func (this *Mudrex) FetchOpenInterest(symbol string, options ...FetchOpenInterestOptions) (OpenInterest, error) {
	return this.exchangeTyped.FetchOpenInterest(symbol, options...)
}
func (this *Mudrex) FetchOpenInterestHistory(symbol string, options ...FetchOpenInterestHistoryOptions) ([]OpenInterest, error) {
	return this.exchangeTyped.FetchOpenInterestHistory(symbol, options...)
}
func (this *Mudrex) FetchOpenInterests(options ...FetchOpenInterestsOptions) (OpenInterests, error) {
	return this.exchangeTyped.FetchOpenInterests(options...)
}
func (this *Mudrex) FetchOption(symbol string, options ...FetchOptionOptions) (Option, error) {
	return this.exchangeTyped.FetchOption(symbol, options...)
}
func (this *Mudrex) FetchOptionChain(code string, options ...FetchOptionChainOptions) (OptionChain, error) {
	return this.exchangeTyped.FetchOptionChain(code, options...)
}
func (this *Mudrex) FetchOrderWithClientOrderId(clientOrderId string, options ...FetchOrderWithClientOrderIdOptions) (Order, error) {
	return this.exchangeTyped.FetchOrderWithClientOrderId(clientOrderId, options...)
}
func (this *Mudrex) FetchOrderBook(symbol string, options ...FetchOrderBookOptions) (OrderBook, error) {
	return this.exchangeTyped.FetchOrderBook(symbol, options...)
}
func (this *Mudrex) FetchOrderBooks(options ...FetchOrderBooksOptions) (OrderBooks, error) {
	return this.exchangeTyped.FetchOrderBooks(options...)
}
func (this *Mudrex) FetchOrderStatus(id string, options ...FetchOrderStatusOptions) (string, error) {
	return this.exchangeTyped.FetchOrderStatus(id, options...)
}
func (this *Mudrex) FetchOrderTrades(id string, options ...FetchOrderTradesOptions) ([]Trade, error) {
	return this.exchangeTyped.FetchOrderTrades(id, options...)
}
func (this *Mudrex) FetchPaymentMethods(params ...any) (map[string]any, error) {
	return this.exchangeTyped.FetchPaymentMethods(params...)
}
func (this *Mudrex) FetchPosition(symbol string, options ...FetchPositionOptions) (Position, error) {
	return this.exchangeTyped.FetchPosition(symbol, options...)
}
func (this *Mudrex) FetchPositionHistory(symbol string, options ...FetchPositionHistoryOptions) ([]Position, error) {
	return this.exchangeTyped.FetchPositionHistory(symbol, options...)
}
func (this *Mudrex) FetchPositionMode(options ...FetchPositionModeOptions) (PositionModeInfo, error) {
	return this.exchangeTyped.FetchPositionMode(options...)
}
func (this *Mudrex) FetchPositionsForSymbol(symbol string, options ...FetchPositionsForSymbolOptions) ([]Position, error) {
	return this.exchangeTyped.FetchPositionsForSymbol(symbol, options...)
}
func (this *Mudrex) FetchPositionsRisk(options ...FetchPositionsRiskOptions) ([]Position, error) {
	return this.exchangeTyped.FetchPositionsRisk(options...)
}
func (this *Mudrex) FetchPremiumIndexOHLCV(symbol string, options ...FetchPremiumIndexOHLCVOptions) ([]OHLCV, error) {
	return this.exchangeTyped.FetchPremiumIndexOHLCV(symbol, options...)
}
func (this *Mudrex) FetchStatus(params ...any) (Status, error) {
	return this.exchangeTyped.FetchStatus(params...)
}
func (this *Mudrex) FetchTime(params ...any) (int64, error) {
	return this.exchangeTyped.FetchTime(params...)
}
func (this *Mudrex) FetchTrades(symbol string, options ...FetchTradesOptions) ([]Trade, error) {
	return this.exchangeTyped.FetchTrades(symbol, options...)
}
func (this *Mudrex) FetchTradingFee(symbol string, options ...FetchTradingFeeOptions) (TradingFeeInterface, error) {
	return this.exchangeTyped.FetchTradingFee(symbol, options...)
}
func (this *Mudrex) FetchTradingFees(params ...any) (TradingFees, error) {
	return this.exchangeTyped.FetchTradingFees(params...)
}
func (this *Mudrex) FetchTradingLimits(options ...FetchTradingLimitsOptions) (map[string]any, error) {
	return this.exchangeTyped.FetchTradingLimits(options...)
}
func (this *Mudrex) FetchTransactionFee(code string, options ...FetchTransactionFeeOptions) (map[string]any, error) {
	return this.exchangeTyped.FetchTransactionFee(code, options...)
}
func (this *Mudrex) FetchTransactionFees(options ...FetchTransactionFeesOptions) (map[string]any, error) {
	return this.exchangeTyped.FetchTransactionFees(options...)
}
func (this *Mudrex) FetchTransactions(options ...FetchTransactionsOptions) ([]Transaction, error) {
	return this.exchangeTyped.FetchTransactions(options...)
}
func (this *Mudrex) FetchTransfer(id string, options ...FetchTransferOptions) (TransferEntry, error) {
	return this.exchangeTyped.FetchTransfer(id, options...)
}
func (this *Mudrex) FetchTransfers(options ...FetchTransfersOptions) ([]TransferEntry, error) {
	return this.exchangeTyped.FetchTransfers(options...)
}
func (this *Mudrex) FetchWithdrawals(options ...FetchWithdrawalsOptions) ([]Transaction, error) {
	return this.exchangeTyped.FetchWithdrawals(options...)
}
func (this *Mudrex) SetMargin(symbol string, amount float64, options ...SetMarginOptions) (MarginModification, error) {
	return this.exchangeTyped.SetMargin(symbol, amount, options...)
}
func (this *Mudrex) SetMarginMode(marginMode string, options ...SetMarginModeOptions) (map[string]any, error) {
	return this.exchangeTyped.SetMarginMode(marginMode, options...)
}
func (this *Mudrex) SetPositionMode(hedged bool, options ...SetPositionModeOptions) (map[string]any, error) {
	return this.exchangeTyped.SetPositionMode(hedged, options...)
}
func (this *Mudrex) Withdraw(code string, amount float64, address string, options ...WithdrawOptions) (Transaction, error) {
	return this.exchangeTyped.Withdraw(code, amount, address, options...)
}
func (this *Mudrex) CancelAllOrdersWs(options ...CancelAllOrdersWsOptions) ([]Order, error) {
	return this.exchangeTyped.CancelAllOrdersWs(options...)
}
func (this *Mudrex) CancelOrdersWs(ids []string, options ...CancelOrdersWsOptions) ([]Order, error) {
	return this.exchangeTyped.CancelOrdersWs(ids, options...)
}
func (this *Mudrex) CancelOrderWs(id string, options ...CancelOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CancelOrderWs(id, options...)
}
func (this *Mudrex) CreateLimitBuyOrderWs(symbol string, amount float64, price float64, options ...CreateLimitBuyOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateLimitBuyOrderWs(symbol, amount, price, options...)
}
func (this *Mudrex) CreateLimitOrderWs(symbol string, side string, amount float64, price float64, options ...CreateLimitOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateLimitOrderWs(symbol, side, amount, price, options...)
}
func (this *Mudrex) CreateLimitSellOrderWs(symbol string, amount float64, price float64, options ...CreateLimitSellOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateLimitSellOrderWs(symbol, amount, price, options...)
}
func (this *Mudrex) CreateMarketBuyOrderWs(symbol string, amount float64, options ...CreateMarketBuyOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketBuyOrderWs(symbol, amount, options...)
}
func (this *Mudrex) CreateMarketOrderWithCostWs(symbol string, side string, cost float64, options ...CreateMarketOrderWithCostWsOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketOrderWithCostWs(symbol, side, cost, options...)
}
func (this *Mudrex) CreateMarketOrderWs(symbol string, side string, amount float64, options ...CreateMarketOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketOrderWs(symbol, side, amount, options...)
}
func (this *Mudrex) CreateMarketSellOrderWs(symbol string, amount float64, options ...CreateMarketSellOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateMarketSellOrderWs(symbol, amount, options...)
}
func (this *Mudrex) CreateOrdersWs(orders []OrderRequest, options ...CreateOrdersWsOptions) ([]Order, error) {
	return this.exchangeTyped.CreateOrdersWs(orders, options...)
}
func (this *Mudrex) CreateOrderWithTakeProfitAndStopLossWs(symbol string, typeVar string, side string, amount float64, options ...CreateOrderWithTakeProfitAndStopLossWsOptions) (Order, error) {
	return this.exchangeTyped.CreateOrderWithTakeProfitAndStopLossWs(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreatePostOnlyOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreatePostOnlyOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreatePostOnlyOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateReduceOnlyOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateReduceOnlyOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateReduceOnlyOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateStopLimitOrderWs(symbol string, side string, amount float64, price float64, triggerPrice float64, options ...CreateStopLimitOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateStopLimitOrderWs(symbol, side, amount, price, triggerPrice, options...)
}
func (this *Mudrex) CreateStopLossOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateStopLossOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateStopLossOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateStopMarketOrderWs(symbol string, side string, amount float64, triggerPrice float64, options ...CreateStopMarketOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateStopMarketOrderWs(symbol, side, amount, triggerPrice, options...)
}
func (this *Mudrex) CreateStopOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateStopOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateStopOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateTakeProfitOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateTakeProfitOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateTakeProfitOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateTrailingAmountOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateTrailingAmountOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateTrailingAmountOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateTrailingPercentOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateTrailingPercentOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateTrailingPercentOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) CreateTriggerOrderWs(symbol string, typeVar string, side string, amount float64, options ...CreateTriggerOrderWsOptions) (Order, error) {
	return this.exchangeTyped.CreateTriggerOrderWs(symbol, typeVar, side, amount, options...)
}
func (this *Mudrex) EditOrderWs(id string, symbol string, typeVar string, side string, options ...EditOrderWsOptions) (Order, error) {
	return this.exchangeTyped.EditOrderWs(id, symbol, typeVar, side, options...)
}
func (this *Mudrex) FetchBalanceWs(params ...any) (Balances, error) {
	return this.exchangeTyped.FetchBalanceWs(params...)
}
func (this *Mudrex) FetchClosedOrdersWs(options ...FetchClosedOrdersWsOptions) ([]Order, error) {
	return this.exchangeTyped.FetchClosedOrdersWs(options...)
}
func (this *Mudrex) FetchDepositsWs(options ...FetchDepositsWsOptions) ([]Transaction, error) {
	return this.exchangeTyped.FetchDepositsWs(options...)
}
func (this *Mudrex) FetchMyTradesWs(options ...FetchMyTradesWsOptions) ([]Trade, error) {
	return this.exchangeTyped.FetchMyTradesWs(options...)
}
func (this *Mudrex) FetchOHLCVWs(symbol string, options ...FetchOHLCVWsOptions) ([]OHLCV, error) {
	return this.exchangeTyped.FetchOHLCVWs(symbol, options...)
}
func (this *Mudrex) FetchOpenOrdersWs(options ...FetchOpenOrdersWsOptions) ([]Order, error) {
	return this.exchangeTyped.FetchOpenOrdersWs(options...)
}
func (this *Mudrex) FetchOrderBookWs(symbol string, options ...FetchOrderBookWsOptions) (OrderBook, error) {
	return this.exchangeTyped.FetchOrderBookWs(symbol, options...)
}
func (this *Mudrex) FetchOrdersByStatusWs(status string, options ...FetchOrdersByStatusWsOptions) ([]Order, error) {
	return this.exchangeTyped.FetchOrdersByStatusWs(status, options...)
}
func (this *Mudrex) FetchOrdersWs(options ...FetchOrdersWsOptions) ([]Order, error) {
	return this.exchangeTyped.FetchOrdersWs(options...)
}
func (this *Mudrex) FetchOrderWs(id string, options ...FetchOrderWsOptions) (Order, error) {
	return this.exchangeTyped.FetchOrderWs(id, options...)
}
func (this *Mudrex) FetchPositionsForSymbolWs(symbol string, options ...FetchPositionsForSymbolWsOptions) ([]Position, error) {
	return this.exchangeTyped.FetchPositionsForSymbolWs(symbol, options...)
}
func (this *Mudrex) FetchPositionsWs(options ...FetchPositionsWsOptions) ([]Position, error) {
	return this.exchangeTyped.FetchPositionsWs(options...)
}
func (this *Mudrex) FetchPositionWs(symbol string, options ...FetchPositionWsOptions) ([]Position, error) {
	return this.exchangeTyped.FetchPositionWs(symbol, options...)
}
func (this *Mudrex) FetchTickersWs(options ...FetchTickersWsOptions) (Tickers, error) {
	return this.exchangeTyped.FetchTickersWs(options...)
}
func (this *Mudrex) FetchTickerWs(symbol string, options ...FetchTickerWsOptions) (Ticker, error) {
	return this.exchangeTyped.FetchTickerWs(symbol, options...)
}
func (this *Mudrex) FetchTradesWs(symbol string, options ...FetchTradesWsOptions) ([]Trade, error) {
	return this.exchangeTyped.FetchTradesWs(symbol, options...)
}
func (this *Mudrex) FetchTradingFeesWs(params ...any) (TradingFees, error) {
	return this.exchangeTyped.FetchTradingFeesWs(params...)
}
func (this *Mudrex) FetchWithdrawalsWs(options ...FetchWithdrawalsWsOptions) ([]Transaction, error) {
	return this.exchangeTyped.FetchWithdrawalsWs(options...)
}
func (this *Mudrex) UnWatchBidsAsks(options ...UnWatchBidsAsksOptions) (any, error) {
	return this.exchangeTyped.UnWatchBidsAsks(options...)
}
func (this *Mudrex) UnWatchMyTrades(options ...UnWatchMyTradesOptions) (any, error) {
	return this.exchangeTyped.UnWatchMyTrades(options...)
}
func (this *Mudrex) UnWatchOHLCV(symbol string, options ...UnWatchOHLCVOptions) (any, error) {
	return this.exchangeTyped.UnWatchOHLCV(symbol, options...)
}
func (this *Mudrex) UnWatchOHLCVForSymbols(symbolsAndTimeframes [][]string, options ...UnWatchOHLCVForSymbolsOptions) (any, error) {
	return this.exchangeTyped.UnWatchOHLCVForSymbols(symbolsAndTimeframes, options...)
}
func (this *Mudrex) UnWatchOrderBook(symbol string, options ...UnWatchOrderBookOptions) (any, error) {
	return this.exchangeTyped.UnWatchOrderBook(symbol, options...)
}
func (this *Mudrex) UnWatchOrderBookForSymbols(symbols []string, options ...UnWatchOrderBookForSymbolsOptions) (any, error) {
	return this.exchangeTyped.UnWatchOrderBookForSymbols(symbols, options...)
}
func (this *Mudrex) UnWatchOrders(options ...UnWatchOrdersOptions) (any, error) {
	return this.exchangeTyped.UnWatchOrders(options...)
}
func (this *Mudrex) UnWatchTicker(symbol string, options ...UnWatchTickerOptions) (any, error) {
	return this.exchangeTyped.UnWatchTicker(symbol, options...)
}
func (this *Mudrex) UnWatchTickers(options ...UnWatchTickersOptions) (any, error) {
	return this.exchangeTyped.UnWatchTickers(options...)
}
func (this *Mudrex) UnWatchTrades(symbol string, options ...UnWatchTradesOptions) (any, error) {
	return this.exchangeTyped.UnWatchTrades(symbol, options...)
}
func (this *Mudrex) UnWatchTradesForSymbols(symbols []string, options ...UnWatchTradesForSymbolsOptions) (any, error) {
	return this.exchangeTyped.UnWatchTradesForSymbols(symbols, options...)
}
func (this *Mudrex) WatchBalance(params ...any) (Balances, error) {
	return this.exchangeTyped.WatchBalance(params...)
}
func (this *Mudrex) WatchBidsAsks(options ...WatchBidsAsksOptions) (Tickers, error) {
	return this.exchangeTyped.WatchBidsAsks(options...)
}
func (this *Mudrex) WatchLiquidations(symbol string, options ...WatchLiquidationsOptions) ([]Liquidation, error) {
	return this.exchangeTyped.WatchLiquidations(symbol, options...)
}
func (this *Mudrex) WatchMarkPrice(symbol string, options ...WatchMarkPriceOptions) (Ticker, error) {
	return this.exchangeTyped.WatchMarkPrice(symbol, options...)
}
func (this *Mudrex) WatchMarkPrices(options ...WatchMarkPricesOptions) (Tickers, error) {
	return this.exchangeTyped.WatchMarkPrices(options...)
}
func (this *Mudrex) WatchMyLiquidations(symbol string, options ...WatchMyLiquidationsOptions) ([]Liquidation, error) {
	return this.exchangeTyped.WatchMyLiquidations(symbol, options...)
}
func (this *Mudrex) WatchMyLiquidationsForSymbols(symbols []string, options ...WatchMyLiquidationsForSymbolsOptions) ([]Liquidation, error) {
	return this.exchangeTyped.WatchMyLiquidationsForSymbols(symbols, options...)
}
func (this *Mudrex) WatchMyTrades(options ...WatchMyTradesOptions) ([]Trade, error) {
	return this.exchangeTyped.WatchMyTrades(options...)
}
func (this *Mudrex) WatchOHLCV(symbol string, options ...WatchOHLCVOptions) ([]OHLCV, error) {
	return this.exchangeTyped.WatchOHLCV(symbol, options...)
}
func (this *Mudrex) WatchOHLCVForSymbols(symbolsAndTimeframes [][]string, options ...WatchOHLCVForSymbolsOptions) (map[string]map[string][]OHLCV, error) {
	return this.exchangeTyped.WatchOHLCVForSymbols(symbolsAndTimeframes, options...)
}
func (this *Mudrex) WatchOrderBook(symbol string, options ...WatchOrderBookOptions) (OrderBook, error) {
	return this.exchangeTyped.WatchOrderBook(symbol, options...)
}
func (this *Mudrex) WatchOrderBookForSymbols(symbols []string, options ...WatchOrderBookForSymbolsOptions) (OrderBook, error) {
	return this.exchangeTyped.WatchOrderBookForSymbols(symbols, options...)
}
func (this *Mudrex) WatchOrders(options ...WatchOrdersOptions) ([]Order, error) {
	return this.exchangeTyped.WatchOrders(options...)
}
func (this *Mudrex) WatchOrdersForSymbols(symbols []string, options ...WatchOrdersForSymbolsOptions) ([]Order, error) {
	return this.exchangeTyped.WatchOrdersForSymbols(symbols, options...)
}
func (this *Mudrex) WatchPosition(options ...WatchPositionOptions) (Position, error) {
	return this.exchangeTyped.WatchPosition(options...)
}
func (this *Mudrex) WatchPositions(options ...WatchPositionsOptions) ([]Position, error) {
	return this.exchangeTyped.WatchPositions(options...)
}
func (this *Mudrex) WatchTicker(symbol string, options ...WatchTickerOptions) (Ticker, error) {
	return this.exchangeTyped.WatchTicker(symbol, options...)
}
func (this *Mudrex) WatchTickers(options ...WatchTickersOptions) (Tickers, error) {
	return this.exchangeTyped.WatchTickers(options...)
}
func (this *Mudrex) WatchTrades(symbol string, options ...WatchTradesOptions) ([]Trade, error) {
	return this.exchangeTyped.WatchTrades(symbol, options...)
}
func (this *Mudrex) WatchTradesForSymbols(symbols []string, options ...WatchTradesForSymbolsOptions) ([]Trade, error) {
	return this.exchangeTyped.WatchTradesForSymbols(symbols, options...)
}
func (this *Mudrex) WithdrawWs(code string, amount float64, address string, options ...WithdrawWsOptions) (Transaction, error) {
	return this.exchangeTyped.WithdrawWs(code, amount, address, options...)
}
