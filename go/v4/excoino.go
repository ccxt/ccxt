package ccxt

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

type excoino struct {
    Exchange

}

func NewExcoinoCore() *excoino {
    p := &excoino{}
    setDefaults(p)
    return p
}

func  (this *excoino) Describe() interface{}  {
    return this.DeepExtend(this.Exchange.Describe(), map[string]interface{} {
        "id": "excoino",
        "name": "Excoino",
        "countries": []interface{}{"IR"},
        "rateLimit": 1000,
        "version": "1",
        "certified": false,
        "pro": false,
        "has": map[string]interface{} {
            "CORS": nil,
            "spot": true,
            "margin": false,
            "swap": false,
            "future": false,
            "option": false,
            "addMargin": false,
            "cancelAllOrders": false,
            "cancelOrder": false,
            "cancelOrders": false,
            "createDepositAddress": false,
            "createOrder": false,
            "createStopLimitOrder": false,
            "createStopMarketOrder": false,
            "createStopOrder": false,
            "editOrder": false,
            "fetchBalance": false,
            "fetchBorrowInterest": false,
            "fetchBorrowRateHistories": false,
            "fetchBorrowRateHistory": false,
            "fetchClosedOrders": false,
            "fetchCrossBorrowRate": false,
            "fetchCrossBorrowRates": false,
            "fetchCurrencies": false,
            "fetchDepositAddress": false,
            "fetchDeposits": false,
            "fetchFundingHistory": false,
            "fetchFundingRate": false,
            "fetchFundingRateHistory": false,
            "fetchFundingRates": false,
            "fetchIndexOHLCV": false,
            "fetchIsolatedBorrowRate": false,
            "fetchIsolatedBorrowRates": false,
            "fetchL2OrderBook": false,
            "fetchLedger": false,
            "fetchLedgerEntry": false,
            "fetchLeverageTiers": false,
            "fetchMarkets": true,
            "fetchMarkOHLCV": false,
            "fetchMyTrades": false,
            "fetchOHLCV": true,
            "fetchOpenInterestHistory": false,
            "fetchOpenOrders": false,
            "fetchOrder": false,
            "fetchOrderBook": true,
            "fetchOrders": false,
            "fetchOrderTrades": "emulated",
            "fetchPositions": false,
            "fetchPremiumIndexOHLCV": false,
            "fetchTicker": true,
            "fetchTickers": true,
            "fetchTime": false,
            "fetchTrades": false,
            "fetchTradingFee": false,
            "fetchTradingFees": false,
            "fetchWithdrawals": false,
            "setLeverage": false,
            "setMarginMode": false,
            "transfer": false,
            "withdraw": false,
        },
        "comment": "This comment is optional",
        "urls": map[string]interface{} {
            "logo": "https://cdn.arz.digital/cr-odin/img/exchanges/excoino/64x64.png",
            "api": map[string]interface{} {
                "public": "https://market-api.excoino.com",
            },
            "www": "https://www.excoino.com/",
            "doc": []interface{}{"https://market-api.excoino.com"},
        },
        "timeframes": map[string]interface{} {
            "1m": "1",
            "5m": "5",
            "15m": "15",
            "30m": "30",
            "1h": "60",
            "4h": "240",
            "1d": "1D",
            "1W": "1W",
            "1M": "3M",
        },
        "api": map[string]interface{} {
            "public": map[string]interface{} {
                "get": map[string]interface{} {
                    "market/symbol-thumb-trend": 1,
                    "market/history": 1,
                    "market/exchange-plate-full": 1,
                },
            },
        },
        "commonCurrencies": map[string]interface{} {
            "BTCB": "BTC",
            "IRR": "IRT",
        },
        "fees": map[string]interface{} {
            "trading": map[string]interface{} {
                "tierBased": false,
                "percentage": true,
                "maker": this.ParseNumber("0.001"),
                "taker": this.ParseNumber("0.001"),
            },
        },
    })
}
func  (this *excoino) FetchMarkets(optionalArgs ...interface{}) <- chan interface{} {
            ch := make(chan interface{})
            go func() interface{} {
                defer close(ch)
                defer ReturnPanicError(ch)
                    /**
            * @method
            * @name excoino#fetchMarkets
            * @description retrieves data on all markets for excoino
            * @see https://apidocs.excoino.ir/#6ae2dae4a2
            * @param {object} [params] extra parameters specific to the exchange API endpoint
            * @returns {object[]} an array of objects representing market data
            */
            params := GetArg(optionalArgs, 0, map[string]interface{} {})
            _ = params
        
            response:= (<-this.PublicGetMarketSymbolThumbTrend())
            PanicOnError(response)
            var result interface{} = []interface{}{}
            for i := 0; IsLessThan(i, GetArrayLength(response)); i++ {
                var market interface{} = this.ParseMarket(GetValue(response, i))
                AppendToArray(&result, market)
            }
        
            ch <- result
            return nil
        
            }()
            return ch
        }
func  (this *excoino) ParseMarket(market interface{}) interface{}  {
    // {
    // coinType: "ORDINARY",
    // symbol: "USDT/IRR",
    // chg: -0.0065,
    // coinScale: 2,
    // displayName: "Tether",
    // change: -3807,
    // twentyFourHourVolume: 364506.8462,
    // oneHourTurnover: -3689066797218.638,
    // volume: 6455037.5749,
    // lastDayClose: 588715,
    // high: 590885,
    // coinUrl: "https://market-api.excoino.com/market/icons/currencies/USDT.png",
    // low: 584015,
    // zone: 0,
    // baseCoinScale: 0,
    // oneHourVolume: -6371368.6146,
    // usdRate: 1,
    // close: 584914,
    // turnover: 3738577518315.463,
    // baseUsdRate: 0.000001709653,
    // open: 588721,
    // twentyFourHourTurnover: 214737798378.2562
    // },
    baseIdquoteIdVariable := Split(this.SafeString(market, "symbol"), "/");
    baseId := GetValue(baseIdquoteIdVariable,0);
    quoteId := GetValue(baseIdquoteIdVariable,1)
    var id interface{} = this.SafeValue(market, "symbol")
    var base interface{} = this.SafeCurrencyCode(baseId)
    var quote interface{} = this.SafeCurrencyCode(quoteId)
    baseId = ToLower(baseId)
    quoteId = ToLower(quoteId)
    return map[string]interface{} {
        "id": id,
        "symbol": Add(Add(base, "/"), quote),
        "base": base,
        "quote": quote,
        "settle": nil,
        "baseId": baseId,
        "quoteId": quoteId,
        "settleId": nil,
        "type": "spot",
        "spot": true,
        "margin": false,
        "swap": false,
        "future": false,
        "option": false,
        "active": true,
        "contract": false,
        "linear": nil,
        "inverse": nil,
        "contractSize": nil,
        "expiry": nil,
        "expiryDatetime": nil,
        "strike": nil,
        "optionType": nil,
        "precision": map[string]interface{} {
            "amount": nil,
            "price": nil,
        },
        "limits": map[string]interface{} {
            "leverage": map[string]interface{} {
                "min": nil,
                "max": nil,
            },
            "amount": map[string]interface{} {
                "min": nil,
                "max": nil,
            },
            "price": map[string]interface{} {
                "min": nil,
                "max": nil,
            },
            "cost": map[string]interface{} {
                "min": nil,
                "max": nil,
            },
        },
        "created": nil,
        "info": market,
    }
}
func  (this *excoino) FetchTickers(optionalArgs ...interface{}) <- chan interface{} {
            ch := make(chan interface{})
            go func() interface{} {
                defer close(ch)
                defer ReturnPanicError(ch)
                    /**
            * @method
            * @name excoino#fetchTickers
            * @description fetches price tickers for multiple markets, statistical information calculated over the past 24 hours for each market
            * @see https://market-api.excoino.com
            * @param {string[]|undefined} symbols unified symbols of the markets to fetch the ticker for, all market tickers are returned if not assigned
            * @param {object} [params] extra parameters specific to the exchange API endpoint
            * @returns {object} a dictionary of [ticker structures]{@link https://docs.ccxt.com/#/?id=ticker-structure}
            */
            symbols := GetArg(optionalArgs, 0, nil)
            _ = symbols
            params := GetArg(optionalArgs, 1, map[string]interface{} {})
            _ = params
        
            retRes2418 := (<-this.LoadMarkets())
            PanicOnError(retRes2418)
            if IsTrue(!IsEqual(symbols, nil)) {
                symbols = this.MarketSymbols(symbols)
            }
        
            response:= (<-this.PublicGetMarketSymbolThumbTrend())
            PanicOnError(response)
            var result interface{} = []interface{}{}
            for i := 0; IsLessThan(i, GetArrayLength(response)); i++ {
                var ticker interface{} = this.ParseTicker(GetValue(response, i))
                var symbol interface{} = GetValue(ticker, "symbol")
                AddElementToObject(result, symbol, ticker)
            }
        
            ch <- this.FilterByArrayTickers(result, "symbol", symbols)
            return nil
        
            }()
            return ch
        }
func  (this *excoino) FetchTicker(symbol interface{}, optionalArgs ...interface{}) <- chan interface{} {
            ch := make(chan interface{})
            go func() interface{} {
                defer close(ch)
                defer ReturnPanicError(ch)
                    /**
            * @method
            * @name excoino#fetchTicker
            * @description fetches a price ticker, a statistical calculation with the information calculated over the past 24 hours for a specific market
            * @see https://market-api.excoino.com
            * @param {string} symbol unified symbol of the market to fetch the ticker for
            * @param {object} [params] extra parameters specific to the exchange API endpoint
            * @returns {object} a [ticker structure]{@link https://docs.ccxt.com/#/?id=ticker-structure}
            */
            params := GetArg(optionalArgs, 0, map[string]interface{} {})
            _ = params
        
            ticker:= (<-this.FetchTickers([]interface{}{symbol}))
            PanicOnError(ticker)
        
            ch <- GetValue(ticker, symbol)
            return nil
        
            }()
            return ch
        }
func  (this *excoino) ParseTicker(ticker interface{}, optionalArgs ...interface{}) interface{}  {
    // {
    // coinType: "ORDINARY",
    // symbol: "USDT/IRR",
    // chg: -0.0065,
    // coinScale: 2,
    // displayName: "Tether",
    // change: -3807,
    // twentyFourHourVolume: 364506.8462,
    // oneHourTurnover: -3689066797218.638,
    // volume: 6455037.5749,
    // lastDayClose: 588715,
    // high: 590885,
    // coinUrl: "https://market-api.excoino.com/market/icons/currencies/USDT.png",
    // low: 584015,
    // zone: 0,
    // baseCoinScale: 0,
    // oneHourVolume: -6371368.6146,
    // usdRate: 1,
    // close: 584914,
    // turnover: 3738577518315.463,
    // baseUsdRate: 0.000001709653,
    // open: 588721,
    // twentyFourHourTurnover: 214737798378.2562
    // },
    market := GetArg(optionalArgs, 0, nil)
    _ = market
    var marketType interface{} = "spot"
    var marketId interface{} = this.SafeValue(ticker, "symbol")
    var marketinfo interface{} = this.Market(marketId)
    var symbol interface{} = this.SafeSymbol(marketId, market, nil, marketType)
    var high interface{} = this.SafeFloat(ticker, "high", 0)
    var low interface{} = this.SafeFloat(ticker, "low", 0)
    var open interface{} = this.SafeFloat(ticker, "open", 0)
    var close interface{} = this.SafeFloat(ticker, "close", 0)
    var change interface{} = this.SafeFloat(ticker, "chg", 0)
    var last interface{} = this.SafeFloat(ticker, "close", 0)
    var quoteVolume interface{} = this.SafeFloat(ticker, "twentyFourHourTurnover", 0)
    if IsTrue(IsEqual(GetValue(marketinfo, "quote"), "IRT")) {
        high = Ternary(IsTrue(high), Divide(high, 10), 0)
        low = Ternary(IsTrue(low), Divide(low, 10), 0)
        open = Ternary(IsTrue(open), Divide(open, 10), 0)
        close = Ternary(IsTrue(close), Divide(close, 10), 0)
        last = Ternary(IsTrue(last), Divide(last, 10), 0)
        quoteVolume = Ternary(IsTrue(quoteVolume), Divide(quoteVolume, 10), 0)
    }
    return this.SafeTicker(map[string]interface{} {
        "symbol": symbol,
        "timestamp": nil,
        "datetime": nil,
        "high": high,
        "low": low,
        "bid": nil,
        "bidVolume": nil,
        "ask": nil,
        "askVolume": nil,
        "vwap": nil,
        "open": open,
        "close": close,
        "last": last,
        "previousClose": nil,
        "change": change,
        "percentage": nil,
        "average": nil,
        "baseVolume": nil,
        "quoteVolume": quoteVolume,
        "info": ticker,
    }, market)
}
func  (this *excoino) FetchOHLCV(symbol interface{}, optionalArgs ...interface{}) <- chan interface{} {
            ch := make(chan interface{})
            go func() interface{} {
                defer close(ch)
                defer ReturnPanicError(ch)
                    /**
            * @method
            * @name excoino#fetchOHLCV
            * @description fetches historical candlestick data containing the open, high, low, and close price, and the volume of a market
            * @see https://market-api.excoino.com
            * @param {string} symbol unified symbol of the market to fetch OHLCV data for
            * @param {string} timeframe the length of time each candle represents
            * @param {int} [since] timestamp in ms of the earliest candle to fetch
            * @param {int} [limit] the maximum amount of candles to fetch
            * @param {object} [params] extra parameters specific to the exchange API endpoint
            * @returns {int[][]} A list of candles ordered as timestamp, open, high, low, close, volume
            */
            timeframe := GetArg(optionalArgs, 0, "1h")
            _ = timeframe
            since := GetArg(optionalArgs, 1, nil)
            _ = since
            limit := GetArg(optionalArgs, 2, 200)
            _ = limit
            params := GetArg(optionalArgs, 3, map[string]interface{} {})
            _ = params
        
            retRes3508 := (<-this.LoadMarkets())
            PanicOnError(retRes3508)
            var market interface{} = this.Market(symbol)
            var endTime interface{} = DateNow()
            var request interface{} = map[string]interface{} {
                "symbol": GetValue(market, "id"),
                "from": Subtract(endTime, (Multiply(Multiply(Multiply(24, 60), 60), 1000))),
                "to": endTime,
                "resolution": this.SafeString(this.Timeframes, timeframe, timeframe),
                "size": limit,
            }
            if IsTrue(!IsEqual(since, nil)) {
                AddElementToObject(request, "from", since)
            }
            AddElementToObject(request, "from", this.SafeInteger(request, "from"))
            AddElementToObject(request, "to", this.SafeInteger(request, "to"))
            if IsTrue(!IsEqual(timeframe, nil)) {
                AddElementToObject(request, "resolution", this.SafeString(this.Timeframes, timeframe, timeframe))
            }
        
            response:= (<-this.PublicGetMarketHistory(request))
            PanicOnError(response)
            for i := 0; IsLessThan(i, GetArrayLength(response)); i++ {
                if IsTrue(IsEqual(GetValue(market, "quote"), "IRT")) {
                    AddElementToObject(GetValue(response, i), 1, Divide((IsTrue(GetValue(GetValue(response, i), 1)) || IsTrue(0)), 10))
                    AddElementToObject(GetValue(response, i), 2, Divide((IsTrue(GetValue(GetValue(response, i), 2)) || IsTrue(0)), 10))
                    AddElementToObject(GetValue(response, i), 3, Divide((IsTrue(GetValue(GetValue(response, i), 3)) || IsTrue(0)), 10))
                    AddElementToObject(GetValue(response, i), 4, Divide((IsTrue(GetValue(GetValue(response, i), 4)) || IsTrue(0)), 10))
                    AddElementToObject(GetValue(response, i), 5, Divide((IsTrue(GetValue(GetValue(response, i), 5)) || IsTrue(0)), 10))
                }
            }
        
            ch <- this.ParseOHLCVs(response, market, timeframe, since, limit)
            return nil
        
            }()
            return ch
        }
func  (this *excoino) FetchOrderBook(symbol interface{}, optionalArgs ...interface{}) <- chan interface{} {
            ch := make(chan interface{})
            go func() interface{} {
                defer close(ch)
                defer ReturnPanicError(ch)
                    /**
            * @method
            * @name excoino#fetchOrderBooks
            * @description fetches information on open orders with bid (buy) and ask (sell) prices, volumes and other data for multiple markets
            * @see https://market-api.excoino.com
            * @param {string[]|undefined} symbols list of unified market symbols, all symbols fetched if undefined, default is undefined
            * @param {int} [limit] max number of entries per orderbook to return, default is undefined
            * @param {object} [params] extra parameters specific to the exchange API endpoint
            * @returns {object} a dictionary of [order book structures]{@link https://docs.ccxt.com/#/?id=order-book-structure} indexed by market symbol
            */
            limit := GetArg(optionalArgs, 0, nil)
            _ = limit
            params := GetArg(optionalArgs, 1, map[string]interface{} {})
            _ = params
        
            retRes3928 := (<-this.LoadMarkets())
            PanicOnError(retRes3928)
            var market interface{} = this.Market(symbol)
            var request interface{} = map[string]interface{} {
                "symbol": GetValue(market, "id"),
            }
        
            response:= (<-this.PublicGetMarketExchangePlateFull(request))
            PanicOnError(response)
            var orderBook interface{} = map[string]interface{} {}
            var bids interface{} = this.SafeDict(response, "bid")
            var asks interface{} = this.SafeDict(response, "ask")
            if IsTrue(IsEqual(GetValue(market, "quote"), "IRT")) {
                bids = this.SafeDict(bids, "items")
                asks = this.SafeDict(asks, "items")
                for i := 0; IsLessThan(i, GetArrayLength(bids)); i++ {
                    AddElementToObject(GetValue(bids, i), "price", Divide((IsTrue(GetValue(GetValue(bids, i), "price")) || IsTrue(0)), 10))
                }
                for i := 0; IsLessThan(i, GetArrayLength(asks)); i++ {
                    AddElementToObject(GetValue(asks, i), "price", Divide((IsTrue(GetValue(GetValue(asks, i), "price")) || IsTrue(0)), 10))
                }
                AddElementToObject(orderBook, "bids", bids)
                AddElementToObject(orderBook, "asks", asks)
            } else {
                bids = this.SafeDict(bids, "items")
                asks = this.SafeDict(asks, "items")
                AddElementToObject(orderBook, "bids", bids)
                AddElementToObject(orderBook, "asks", asks)
            }
            var timestamp interface{} = DateNow()
        
            ch <- this.ParseOrderBook(orderBook, symbol, timestamp, "bids", "asks", "price", "amount")
            return nil
        
            }()
            return ch
        }
func  (this *excoino) Sign(path interface{}, optionalArgs ...interface{}) interface{}  {
    api := GetArg(optionalArgs, 0, "public")
    _ = api
    method := GetArg(optionalArgs, 1, "GET")
    _ = method
    params := GetArg(optionalArgs, 2, map[string]interface{} {})
    _ = params
    headers := GetArg(optionalArgs, 3, nil)
    _ = headers
    body := GetArg(optionalArgs, 4, nil)
    _ = body
    var query interface{} = this.Omit(params, this.ExtractParams(path))
    var url interface{} = Add(Add(GetValue(GetValue(this.Urls, "api"), "public"), "/"), path)
    if IsTrue(IsTrue(IsEqual(path, "market/history")) || IsTrue(IsEqual(path, "market/exchange-plate-full"))) {
        url = Add(Add(url, "?"), this.Urlencode(query))
    }
    headers = map[string]interface{} {
        "Content-Type": "application/json",
    }
    return map[string]interface{} {
        "url": url,
        "method": method,
        "body": body,
        "headers": headers,
    }
}


func (this *excoino) Init(userConfig map[string]interface{}) {
    this.Exchange = Exchange{}
    this.Exchange.DerivedExchange = this
    this.Exchange.InitParent(userConfig, this.Describe().(map[string]interface{}), this)
}
