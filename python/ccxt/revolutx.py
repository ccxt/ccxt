# -*- coding: utf-8 -*-

# PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
# https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

from ccxt.base.exchange import Exchange
from ccxt.abstract.revolutx import ImplicitAPI
import math
from ccxt.base.types import Balances, Currencies, Currency, Int, Market, Num, Order, OrderBook, OrderSide, OrderType, Str, Strings, Ticker, Tickers, Trade, MarketInterface
from ccxt.base.errors import ExchangeError
from ccxt.base.errors import PermissionDenied
from ccxt.base.errors import ArgumentsRequired
from ccxt.base.errors import BadRequest
from ccxt.base.errors import InsufficientFunds
from ccxt.base.errors import InvalidOrder
from ccxt.base.errors import OrderNotFound
from ccxt.base.errors import RateLimitExceeded
from ccxt.base.errors import InvalidNonce
from ccxt.base.decimal_to_precision import TICK_SIZE
from ccxt.base.precise import Precise


class revolutx(Exchange, ImplicitAPI):

    def describe(self) -> object:
        return self.deep_extend(super(revolutx, self).describe(), {
            'id': 'revolutx',
            'name': 'Revolut X',
            'countries': ['GB'],
            'rateLimit': 1000,
            'certified': False,
            'pro': False,
            'has': {
                'CORS': None,
                'spot': True,
                'margin': False,
                'swap': False,
                'future': False,
                'option': False,
                'cancelAllOrders': True,
                'cancelOrder': True,
                'createOrder': True,
                'editOrder': True,
                'fetchBalance': True,
                'fetchClosedOrders': True,
                'fetchCurrencies': True,
                'fetchMarkets': True,
                'fetchMyTrades': True,
                'fetchOHLCV': True,
                'fetchOpenOrders': True,
                'fetchOrder': True,
                'fetchOrderBook': True,
                'fetchOrders': True,
                'fetchTicker': True,
                'fetchTickers': True,
                'fetchTrades': True,
            },
            'timeframes': {
                '1m': 1,
                '5m': 5,
                '15m': 15,
                '30m': 30,
                '1h': 60,
                '4h': 240,
                '1d': 1440,
                '2d': 2880,
                '4d': 5760,
                '1w': 10080,
                '2w': 20160,
                '4w': 40320,
            },
            'urls': {
                'logo': 'https://github.com/user-attachments/assets/594ab1a5-3985-4f79-89f4-c40ef7c33867',
                'api': {
                    'public': 'https://revx.revolut.com/api',
                    'private': 'https://revx.revolut.com/api',
                },
                'www': 'https://exchange.revolut.com',
                'doc': 'https://developer.revolut.com/docs/api/revolut-x-crypto-exchange',
                'fees': 'https://exchange.revolut.com/fees',
            },
            'api': {
                'public': {
                    'get': {
                        '2.0/public/order-book/{symbol}': 1,
                        '1.0/public/tickers': 1,
                        '1.0/public/candles/{symbol}': 1,
                        '1.0/public/trades/all': 1,
                        '1.0/public/configuration/currencies': 1,
                        '1.0/public/configuration/pairs': 1,
                    },
                },
                'private': {
                    'get': {
                        '1.0/balances': 1,
                        '1.0/orders/active': 1,
                        '1.0/orders/historical': 1,
                        '1.0/orders/{venue_order_id}': 1,
                        '1.0/orders/fills/{venue_order_id}': 1,
                        '1.0/trades/private/{symbol}': 1,
                    },
                    'post': {
                        '1.0/orders': 1,
                    },
                    'put': {
                        '1.0/orders/{venue_order_id}': 1,
                    },
                    'delete': {
                        '1.0/orders': 1,
                        '1.0/orders/{venue_order_id}': 1,
                    },
                },
            },
            'requiredCredentials': {
                'apiKey': True,
                'secret': False,
                'privateKey': True,
            },
            'fees': {
                'trading': {
                    'tierBased': False,
                    'percentage': True,
                    'taker': self.parse_number('0.0009'),
                    'maker': self.parse_number('0'),
                },
            },
            'options': {
                'region': None,
            },
            'precisionMode': TICK_SIZE,
            'features': {
                'spot': {
                    'sandbox': False,
                    'createOrder': {
                        'marginMode': False,
                        'triggerPrice': False,
                        'triggerPriceType': None,
                        'triggerDirection': False,
                        'stopLossPrice': False,
                        'takeProfitPrice': False,
                        'attachedStopLossTakeProfit': None,
                        'timeInForce': {
                            'GTC': True,
                            'IOC': True,
                            'FOK': False,
                            'PO': False,
                            'GTD': False,
                        },
                        'hedged': False,
                        'trailing': False,
                        'leverage': False,
                        'marketBuyByCost': True,
                        'marketBuyRequiresPrice': False,
                        'selfTradePrevention': False,
                        'iceberg': False,
                    },
                    'createOrders': None,
                    'fetchMyTrades': {
                        'marginMode': False,
                        'limit': 1900,
                        'daysBack': 30,
                        'untilDays': None,
                        'symbolRequired': True,
                    },
                    'fetchOrder': {
                        'marginMode': False,
                        'trigger': False,
                        'trailing': False,
                        'symbolRequired': False,
                    },
                    'fetchOpenOrders': {
                        'marginMode': False,
                        'limit': 300,
                        'trigger': False,
                        'trailing': False,
                        'symbolRequired': False,
                    },
                    'fetchOrders': {
                        'marginMode': False,
                        'limit': 1900,
                        'daysBack': 30,
                        'untilDays': None,
                        'trigger': False,
                        'trailing': False,
                        'symbolRequired': False,
                    },
                    'fetchClosedOrders': {
                        'marginMode': False,
                        'limit': 1900,
                        'daysBack': 30,
                        'daysBackCanceled': None,
                        'untilDays': None,
                        'trigger': False,
                        'trailing': False,
                        'symbolRequired': False,
                    },
                    'fetchOHLCV': {
                        'limit': 50000,
                    },
                },
            },
            'httpExceptions': {
                '400': BadRequest,
                '403': PermissionDenied,
                '404': OrderNotFound,
                '409': ExchangeError,
                '429': RateLimitExceeded,
            },
            'exceptions': {
                'exact': {},
                'broad': {
                    'insufficient': InsufficientFunds,
                    'invalid order': InvalidOrder,
                    'not found': OrderNotFound,
                    'timestamp is in the future': InvalidNonce,
                },
            },
        })

    def sign(self, path: object, api: object = 'public', method='GET', params: dict = {}, headers: dict = None, body: Str = None) -> dict:
        implodedPath = self.implode_params(path, params)
        query = self.omit(params, self.extract_params(path))
        queryKeys = list(query.keys())
        queryLength = len(queryKeys)
        url = self.urls['api'][api] + '/' + implodedPath
        queryString = ''
        if api == 'private':
            self.check_required_credentials()
            timestamp = str(self.milliseconds())
            if method == 'GET':
                if queryLength > 0:
                    queryString = self.urlencode(query)
                    url += '?' + queryString
            elif method == 'DELETE':
                if queryLength > 0:
                    queryString = self.urlencode(query)
                    url += '?' + queryString
            else:
                body = self.json(query)
            requestPath = '/api/' + implodedPath
            bodyString = ''
            if body is not None:
                bodyString = body
            message = timestamp + method.upper() + requestPath + queryString + bodyString
            signature = self.eddsa(self.encode(message), self.privateKey, 'ed25519')
            headers = {
                'X-Revx-API-Key': self.apiKey,
                'X-Revx-Timestamp': timestamp,
                'X-Revx-Signature': signature,
            }
            if method == 'POST' or method == 'PUT':
                headers['Content-Type'] = 'application/json'
        else:
            if method == 'GET':
                if queryLength > 0:
                    queryString = self.urlencode(query)
                    url += '?' + queryString
            else:
                body = self.json(query)
                headers = {'Content-Type': 'application/json'}
        return {'url': url, 'method': method, 'body': body, 'headers': headers}

    def parse_market(self, market: dict) -> MarketInterface:
        """
        parses a market from the exchange's market data
 @ignore
        :param dict market: the raw market data from the exchange
        :returns dict: a `market structure <https://docs.ccxt.com/?id=market-structure>`
        """
        id = self.safe_string(market, 'id')
        base = self.safe_string(market, 'base', '')
        quote = self.safe_string(market, 'quote', '')
        baseId = base
        quoteId = quote
        baseStep = self.safe_string(market, 'base_step')
        quoteStep = self.safe_string(market, 'quote_step')
        minOrderSize = self.safe_string(market, 'min_order_size')
        maxOrderSize = self.safe_string(market, 'max_order_size')
        minOrderSizeQuote = self.safe_string(market, 'min_order_size_quote')
        status = self.safe_string(market, 'status')
        active = (status == 'active')
        symbol = base + '/' + quote
        return {
            'id': id,
            'symbol': symbol,
            'base': base,
            'quote': quote,
            'baseId': baseId,
            'quoteId': quoteId,
            'active': active,
            'type': 'spot',
            'spot': True,
            'margin': False,
            'swap': False,
            'future': False,
            'option': False,
            'contract': False,
            'settle': None,
            'settleId': None,
            'contractSize': None,
            'linear': None,
            'inverse': None,
            'expiry': None,
            'expiryDatetime': None,
            'strike': None,
            'optionType': None,
            'taker': self.parse_number('0.0009'),
            'maker': self.parse_number('0'),
            'percentage': True,
            'tierBased': False,
            'feeSide': 'get',
            'precision': {
                'amount': self.parse_number(baseStep),
                'price': self.parse_number(quoteStep),
            },
            'limits': {
                'amount': {
                    'min': self.parse_number(minOrderSize),
                    'max': self.parse_number(maxOrderSize),
                },
                'price': {
                    'min': None,
                    'max': None,
                },
                'cost': {
                    'min': self.parse_number(minOrderSizeQuote),
                    'max': None,
                },
                'leverage': {
                    'min': None,
                    'max': None,
                },
                'market': None,
            },
            'created': None,
            'info': market,
        }

    def fetch_markets(self, params={}) -> list[Market]:
        """
        retrieves all available markets on the exchange

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data

        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.region]: the region to filter markets by(e.g. EEA, UK)
        :returns dict[]: an array of `market structures <https://docs.ccxt.com/?id=market-structure>`
        """
        request = {}
        region = self.safe_string_2(params, 'region', 'region', self.options['region'])
        if region is not None:
            request['region'] = region
        response = self.publicGet10PublicConfigurationPairs(self.extend(request, params))
        #
        #     {
        #         "BTC/USD": {
        #             "base": "BTC", "quote": "USD",
        #             "base_step": "0.0000001", "quote_step": "0.01",
        #             "min_order_size": "0.0000001", "max_order_size": "1000",
        #             "min_order_size_quote": "0.01", "status": "active"
        #         }
        #     }
        #
        markets = self.safe_dict(response, 'data', response)
        keys = list(markets.keys())
        result = []
        for i in range(0, len(keys)):
            key = keys[i]
            market = self.safe_dict(markets, key, {})
            base = self.safe_string(market, 'base')
            quote = self.safe_string(market, 'quote')
            marketId = base + '-' + quote
            marketData = self.extend(market, {'id': marketId})
            result.append(self.parse_market(marketData))
        return result

    def parse_currency(self, currency: dict) -> Currency:
        """
        parses a currency from the exchange's currency data
 @ignore
        :param dict currency: the raw currency data from the exchange
        :returns dict: a `currency structure <https://docs.ccxt.com/?id=currency-structure>`
        """
        id = self.safe_string_2(currency, 'id', 'symbol', '')
        code = self.safe_currency_code(id)
        name = self.safe_string(currency, 'name')
        scale = self.safe_integer(currency, 'scale')
        status = self.safe_string(currency, 'status')
        active = (status == 'active')
        assetType = self.safe_string(currency, 'asset_type')
        type = 'crypto' if (assetType == 'crypto') else 'fiat'
        precision = math.pow(10, -scale) if (scale is not None) else None
        return {
            'info': currency,
            'id': id,
            'code': code,
            'name': name,
            'active': active,
            'type': type,
            'deposit': None,
            'withdraw': None,
            'precision': self.parse_number(precision),
            'fee': None,
            'limits': {
                'amount': {
                    'min': None,
                    'max': None,
                },
                'withdraw': {
                    'min': None,
                    'max': None,
                },
                'deposit': {
                    'min': None,
                    'max': None,
                },
            },
            'networks': {},
        }

    def fetch_currencies(self, params={}) -> Currencies:
        """
        fetches all available currencies on the exchange

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data

        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.region]: the region to filter currencies by
        :returns dict: a dictionary of `currency structures <https://docs.ccxt.com/?id=currency-structure>`
        """
        request = {}
        region = self.safe_string_2(params, 'region', 'region', self.options['region'])
        if region is not None:
            request['region'] = region
        response = self.publicGet10PublicConfigurationCurrencies(self.extend(request, params))
        #
        #     {
        #         "BTC": {"symbol": "BTC", "name": "Bitcoin", "scale": 8, "asset_type": "crypto", "status": "active"},
        #         "USD": {"symbol": "$", "name": "US Dollar", "scale": 2, "asset_type": "fiat", "status": "active"}
        #     }
        #
        currencies = self.safe_dict(response, 'data', response)
        keys = list(currencies.keys())
        result = {}
        for i in range(0, len(keys)):
            key = keys[i]
            currency = self.safe_dict(currencies, key, {})
            currencyData = self.extend(currency, {'id': key})
            parsed = self.parse_currency(currencyData)
            code = self.safe_string(parsed, 'code', '')
            if code == '':
                continue
            result[code] = parsed
        return result

    def parse_ticker(self, ticker: dict, market: Market = None) -> Ticker:
        """
        parses a ticker from the exchange's ticker data
 @ignore
        :param dict ticker: the raw ticker data from the exchange
        :param dict [market]: the market the ticker is for
        :returns dict: a `ticker structure <https://docs.ccxt.com/?id=ticker-structure>`
        """
        tickerSymbol = self.safe_string(ticker, 'symbol')
        symbol = self.safe_symbol(tickerSymbol, market, '/')
        bid = self.safe_string(ticker, 'bid')
        ask = self.safe_string(ticker, 'ask')
        last = self.safe_string(ticker, 'last_price')
        high = self.safe_string(ticker, 'high_24h')
        low = self.safe_string(ticker, 'low_24h')
        priceChange = self.safe_string(ticker, 'price_change_24h')
        baseVolume = self.safe_string(ticker, 'volume_24h')
        timestamp = self.safe_integer(ticker, 'timestamp')
        open = None
        if last is not None and priceChange is not None:
            open = Precise.string_sub(last, priceChange)
        percentage = None
        if open is not None and priceChange is not None:
            percentageString = Precise.string_div(priceChange, open, 8)
            percentage = self.parse_number(Precise.string_mul(percentageString, '100'))
        return self.safe_ticker({
            'symbol': symbol,
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'high': high,
            'low': low,
            'bid': bid,
            'bidVolume': None,
            'ask': ask,
            'askVolume': None,
            'vwap': None,
            'open': open,
            'close': last,
            'last': last,
            'previousClose': None,
            'change': priceChange,
            'percentage': percentage,
            'average': None,
            'baseVolume': baseVolume,
            'quoteVolume': None,
            'info': ticker,
        }, market)

    def fetch_tickers(self, symbols: Strings = None, params={}) -> Tickers:
        """
        fetches price tickers for multiple markets, statistical information calculated over the past 24 hours for each market

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data

        :param str[]|None symbols: unified symbols of the markets to fetch the ticker for, all market tickers are returned if not assigned
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.region]: the region to fetch tickers for(e.g. EEA, UK)
        :returns dict: a dictionary of `ticker structures <https://docs.ccxt.com/?id=ticker-structure>`
        """
        if self.markets is None:
            self.load_markets()
        request = {}
        if symbols is not None:
            marketIds = []
            for i in range(0, len(symbols)):
                symbol = symbols[i]
                market = self.market(symbol)
                marketIds.append(market['id'])
            request['symbols'] = ','.join(marketIds)
        region = self.safe_string_2(params, 'region', 'region', self.options['region'])
        if region is not None:
            request['region'] = region
        response = self.publicGet10PublicTickers(self.extend(request, params))
        #
        #     {
        #         "data": [
        #             {"symbol": "BTC/USD", "bid": "119950.00", "ask": "120050.00", "mid": "120000.00",
        #               "last_price": "119980.00", "low_24h": "115000.00", "high_24h": "122500.00",
        #               "price_change_24h": "2480.00", "volume_24h": "135.42000000", "region": "EEA"}
        #         ],
        #         "metadata": {"timestamp": 1785313433816}
        #     }
        #
        data = self.safe_list(response, 'data', [])
        metadata = self.safe_dict(response, 'metadata', {})
        timestamp = self.safe_integer(metadata, 'timestamp')
        result = {}
        for i in range(0, len(data)):
            tickerData = self.safe_dict(data, i, {})
            tickerData['timestamp'] = timestamp
            ticker = self.parse_ticker(tickerData)
            symbol = self.safe_string(ticker, 'symbol', '')
            if symbol == '':
                continue
            result[symbol] = ticker
        if symbols is not None:
            filtered = {}
            for i in range(0, len(symbols)):
                s = symbols[i]
                if s in result:
                    filtered[s] = result[s]
            return filtered
        return result

    def fetch_ticker(self, symbol: str, params={}) -> Ticker:
        """
        fetches a price ticker for a given market symbol

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data

        :param str symbol: unified symbol of the market to fetch the ticker for
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.region]: the region to fetch the ticker for
        :returns dict: a `ticker structure <https://docs.ccxt.com/?id=ticker-structure>`
        """
        if self.markets is None:
            self.load_markets()
        tickers = self.fetch_tickers([symbol], params)
        ticker = self.safe_dict(tickers, symbol)
        if ticker is None:
            raise ExchangeError(self.id + ' fetchTicker() could not find ticker for symbol ' + symbol)
        return ticker

    def fetch_order_book(self, symbol: str, limit: Int = None, params={}) -> OrderBook:
        """
        fetches the current order book snapshot for a given market symbol

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data

        :param str symbol: unified symbol of the market to fetch the order book for
        :param int [limit]: the maximum number of orders to return(1-50, default 50)
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.region]: the region to fetch the order book for
        :returns dict: an `order book structure <https://docs.ccxt.com/?id=order-book-structure>`
        """
        if self.markets is None:
            self.load_markets()
        market = self.market(symbol)
        request = {
            'symbol': market['id'],
        }
        if limit is not None:
            request['limit'] = limit
        region = self.safe_string_2(params, 'region', 'region', self.options['region'])
        if region is not None:
            request['region'] = region
        response = self.publicGet20PublicOrderBookSymbol(self.extend(request, params))
        #
        #     {
        #         "data": {
        #             "asks": [{"price": "4005.00", "quantity": "1.7000", "count": 3}],
        #             "bids": [{"price": "4000.00", "quantity": "0.25", "count": 1}]
        #         },
        #         "metadata": {"region": "UK", "timestamp": 1785313433816}
        #     }
        #
        data = self.safe_dict(response, 'data', {})
        metadata = self.safe_dict(response, 'metadata', {})
        timestamp = self.safe_integer(metadata, 'timestamp')
        return self.parse_order_book(data, symbol, timestamp, 'bids', 'asks', 'price', 'quantity')

    def parse_ohlcv(self, ohlcv: object, market: Market = None) -> list:
        """
        parses an OHLCV candle from the exchange's candle data
 @ignore
        :param dict ohlcv: the raw candle data from the exchange
        :param dict [market]: the market the candle is for
        :returns int[]: an `OHLCV structure <https://docs.ccxt.com/?id=ohlcv-structure>`
        """
        timestamp = self.safe_integer(ohlcv, 'start')
        open = self.safe_number(ohlcv, 'open')
        high = self.safe_number(ohlcv, 'high')
        low = self.safe_number(ohlcv, 'low')
        close = self.safe_number(ohlcv, 'close')
        volume = self.safe_number(ohlcv, 'volume')
        return [timestamp, open, high, low, close, volume]

    def fetch_ohlcv(self, symbol: str, timeframe: str = '1m', since: Int = None, limit: Int = None, params={}) -> list[list]:
        """
        fetches historical candlestick data for a given market symbol

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data

        :param str symbol: unified symbol of the market to fetch OHLCV data for
        :param str timeframe: the length of time each candle represents(e.g. 1m, 5m, 1h, 1d)
        :param int [since]: timestamp in ms of the earliest candle to fetch
        :param int [limit]: the maximum number of candles to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param int [params.until]: timestamp in ms of the latest candle to fetch
        :param str [params.region]: the region to fetch candles for
        :returns int[][]: a list of `OHLCV structures <https://docs.ccxt.com/?id=ohlcv-structure>`
        """
        if self.markets is None:
            self.load_markets()
        market = self.market(symbol)
        request = {
            'symbol': market['id'],
            'interval': self.safe_integer(self.timeframes, timeframe, 5),
        }
        if since is not None:
            request['since'] = since
        until = self.safe_integer_2(params, 'until', 'until')
        if until is not None:
            request['until'] = until
        else:
            request['until'] = self.milliseconds()
        region = self.safe_string_2(params, 'region', 'region', self.options['region'])
        if region is not None:
            request['region'] = region
        response = self.publicGet10PublicCandlesSymbol(self.extend(request, params))
        #
        #     {
        #         "data": [
        #             {"start": 1785309833816, "open": "119800.00", "high": "120100.00",
        #               "low": "119700.00", "close": "120000.00", "volume": "0.25000000"}
        #         ],
        #         "metadata": {"region": "EEA", "timestamp": 1785313433816}
        #     }
        #
        data = self.safe_list(response, 'data', [])
        return self.parse_ohlcvs(data, market, timeframe, since, limit)

    def parse_trade(self, trade: dict, market: Market = None) -> Trade:
        """
        parses a trade from the exchange's public trade data
 @ignore
        :param dict trade: the raw trade data from the exchange
        :param dict [market]: the market the trade was executed in
        :returns dict: a `trade structure <https://docs.ccxt.com/?id=trade-structure>`
        """
        id = self.safe_string(trade, 'id')
        tradeSymbol = self.safe_string(trade, 'symbol')
        symbol = self.safe_symbol(tradeSymbol, market, '/')
        price = self.safe_number(trade, 'price')
        amount = self.safe_number(trade, 'quantity')
        side = self.safe_string_lower(trade, 'side')
        timestamp = self.safe_integer(trade, 'timestamp')
        cost = None
        if price is not None and amount is not None:
            cost = price * amount
        return {
            'info': trade,
            'id': id,
            'order': None,
            'symbol': symbol,
            'side': side,
            'type': None,
            'takerOrMaker': None,
            'price': price,
            'amount': amount,
            'cost': cost,
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'fee': None,
            'fees': [],
        }

    def fetch_trades(self, symbol: str, since: Int = None, limit: Int = None, params={}) -> list[Trade]:
        """
        fetches the public trade history for a given market symbol

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data

        :param str symbol: unified symbol of the market to fetch trades for
        :param int [since]: timestamp in ms of the earliest trade to fetch
        :param int [limit]: the maximum number of trades to return(1-1900, default 1900)
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param int [params.until]: timestamp in ms of the latest trade to fetch
        :param str [params.cursor]: pagination cursor from the previous response
        :returns dict[]: a list of `trade structures <https://docs.ccxt.com/?id=trade-structure>`
        """
        if self.markets is None:
            self.load_markets()
        market = None
        if symbol is not None:
            market = self.market(symbol)
        request = {}
        if market is not None:
            request['symbol'] = market['id']
        if since is not None:
            request['start_date'] = since
        until = self.safe_integer_2(params, 'until', 'until')
        if until is not None:
            request['end_date'] = until
        elif since is not None:
            request['end_date'] = self.milliseconds()
        if limit is not None:
            request['limit'] = limit
        cursor = self.safe_string(params, 'cursor')
        if cursor is not None:
            request['cursor'] = cursor
        response = self.publicGet10PublicTradesAll(self.extend(request, params))
        #
        #     {
        #         "data": [
        #             {"id": "3b2b202b-7668-43cf-a6c8-b3354e7f4c52", "symbol": "BTC/USD",
        #               "price": "119980.00", "quantity": "0.01000000", "side": "sell", "timestamp": 1785313433816}
        #         ],
        #         "metadata": {"timestamp": 1785313433816, "next_cursor": "..."}
        #     }
        #
        data = self.safe_list(response, 'data', [])
        result = []
        for i in range(0, len(data)):
            trade = self.safe_dict(data, i, {})
            result.append(self.parse_trade(trade, market))
        return self.filter_by_symbol_since_limit(self.sort_by(result, 'timestamp'), symbol, since, limit)

    def fetch_balance(self, params={}) -> Balances:
        """
        fetches the current balance for the authenticated user

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data

        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a `balance structure <https://docs.ccxt.com/?id=balance-structure>`
        """
        if self.markets is None:
            self.load_markets()
        response = self.privateGet10Balances(params)
        #
        #     [
        #         {"currency": "BTC", "available": "1.25000000", "reserved": "0.10000000", "total": "1.35000000"},
        #         {"currency": "USD", "available": "50000.00", "reserved": "1000.00", "total": "51000.00", "staked": "32.00000000"}
        #     ]
        #
        data = response if isinstance(response, list) else self.safe_list(response, 'data', [])
        result = {'info': response}
        for i in range(0, len(data)):
            balance = self.safe_dict(data, i, {})
            currency = self.safe_string(balance, 'currency')
            code = self.safe_currency_code(currency)
            if code is None:
                continue
            account = self.account()
            account['free'] = self.safe_string(balance, 'available')
            reserved = self.safe_string(balance, 'reserved')
            staked = self.safe_string(balance, 'staked')
            used = reserved
            if staked is not None:
                used = staked if (reserved is None) else Precise.string_add(reserved, staked)
            account['used'] = used
            account['total'] = self.safe_string(balance, 'total')
            result[code] = account
        return self.safe_balance(result)

    def parse_order_status(self, status: Str):
        """
        parses the order status from the exchange's status to the unified ccxt status
 @ignore
        :param str status: the exchange-specific order status
        :returns str|None: the unified order status
        """
        statuses = {
            'pending_new': 'open',
            'new': 'open',
            'partially_filled': 'open',
            'filled': 'closed',
            'cancelled': 'canceled',
            'canceled': 'canceled',
            'rejected': 'rejected',
            'replaced': 'open',
        }
        return self.safe_string(statuses, status, status)

    def parse_order(self, order: dict, market: Market = None) -> Order:
        """
        parses an order from the exchange's order data
 @ignore
        :param dict order: the raw order data from the exchange
        :param dict [market]: the market the order was placed in
        :returns dict: an `order structure <https://docs.ccxt.com/?id=order-structure>`
        """
        orderId = self.safe_string_2(order, 'id', 'venue_order_id')
        clientOrderId = self.safe_string(order, 'client_order_id')
        orderSymbol = self.safe_string(order, 'symbol')
        symbol = self.safe_symbol(orderSymbol, market, '/')
        side = self.safe_string_lower(order, 'side')
        orderType = self.safe_string_lower(order, 'type')
        quantity = self.safe_string(order, 'quantity')
        filledQuantity = self.safe_string(order, 'filled_quantity')
        leavesQuantity = self.safe_string(order, 'leaves_quantity')
        price = self.safe_string(order, 'price')
        averageFillPrice = self.safe_string(order, 'average_fill_price')
        amount = self.safe_string(order, 'amount')
        filledAmount = self.safe_string(order, 'filled_amount')
        totalFee = self.safe_string(order, 'total_fee')
        feeCurrency = self.safe_string(order, 'fee_currency')
        status = self.parse_order_status(self.safe_string(order, 'status'))
        timeInForce = self.safe_string_upper(order, 'time_in_force')
        createdDate = self.safe_integer(order, 'created_date')
        updatedDate = self.safe_integer(order, 'updated_date')
        fee = None
        if totalFee is not None:
            fee = {
                'cost': self.parse_number(totalFee),
                'currency': feeCurrency,
            }
        amountValue = None
        if quantity is not None:
            amountValue = quantity
        elif amount is not None:
            amountValue = amount
        filledValue = None
        if filledQuantity is not None:
            filledValue = filledQuantity
        elif filledAmount is not None:
            filledValue = filledAmount
        remainingValue = None
        if leavesQuantity is not None:
            remainingValue = leavesQuantity
        return self.safe_order({
            'id': orderId,
            'clientOrderId': clientOrderId,
            'symbol': symbol,
            'side': side,
            'type': orderType,
            'price': self.parse_number(price),
            'average': self.parse_number(averageFillPrice),
            'amount': self.parse_number(amountValue),
            'filled': self.parse_number(filledValue),
            'remaining': self.parse_number(remainingValue),
            'status': status,
            'timeInForce': timeInForce,
            'timestamp': createdDate,
            'datetime': self.iso8601(createdDate),
            'lastUpdateTimestamp': updatedDate,
            'fee': fee,
            'info': order,
        }, market)

    def create_order(self, symbol: str, type: OrderType, side: OrderSide, amount: float, price: Num = None, params={}) -> Order:
        """
        create a trade order

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-trading

        :param str symbol: unified symbol of the market to create an order in
        :param str type: 'limit' or 'market'
        :param str side: 'buy' or 'sell'
        :param float amount: how much of the base currency you want to trade
        :param float [price]: the price at which the order is to be fulfilled, in units of the quote currency, ignored in market orders
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.clientOrderId]: a custom client order id(UUID)
        :param float [params.cost]: the order cost in units of the quote currency(alternative to amount)
        :param str [params.timeInForce]: 'gtc' or 'ioc' for limit orders
        :param str[] [params.executionInstructions]: limit order instructions, e.g. ['post_only'] or ['allow_taker']
        :returns dict: an `order structure <https://docs.ccxt.com/?id=order-structure>`
        """
        if self.markets is None:
            self.load_markets()
        market = self.market(symbol)
        clientOrderId = self.safe_string_2(params, 'clientOrderId', 'client_order_id', self.uuid())
        cost = self.safe_string_2(params, 'cost', 'quote_size')
        timeInForce = self.safe_string_lower_2(params, 'timeInForce', 'time_in_force')
        executionInstructions = self.safe_list(params, 'executionInstructions', self.safe_list(params, 'execution_instructions'))
        orderConfiguration = {}
        if type == 'limit':
            limitConfig = {}
            if cost is not None:
                limitConfig['quote_size'] = self.cost_to_precision(symbol, cost)
            else:
                limitConfig['base_size'] = self.amount_to_precision(symbol, amount)
            limitConfig['price'] = self.price_to_precision(symbol, price)
            if timeInForce is not None:
                limitConfig['time_in_force'] = timeInForce
            if executionInstructions is not None:
                limitConfig['execution_instructions'] = executionInstructions
            orderConfiguration['limit'] = limitConfig
        elif type == 'market':
            if timeInForce is not None:
                raise InvalidOrder(self.id + ' createOrder() timeInForce is only supported for limit orders')
            if executionInstructions is not None:
                raise InvalidOrder(self.id + ' createOrder() executionInstructions are only supported for limit orders')
            marketConfig = {}
            if cost is not None:
                marketConfig['quote_size'] = self.cost_to_precision(symbol, cost)
            else:
                marketConfig['base_size'] = self.amount_to_precision(symbol, amount)
            orderConfiguration['market'] = marketConfig
        else:
            raise InvalidOrder(self.id + ' createOrder() does not support order type ' + type)
        request = {
            'client_order_id': clientOrderId,
            'symbol': market['id'],
            'side': side,
            'order_configuration': orderConfiguration,
        }
        response = self.privatePost10Orders(self.extend(request, self.omit(params, ['cost', 'quote_size', 'clientOrderId', 'client_order_id', 'timeInForce', 'time_in_force', 'executionInstructions', 'execution_instructions'])))
        #
        #     {
        #         "data": [
        #             {"venue_order_id": "7a52e92e-8639-4fe1-abaa-68d3a2d5234b", "client_order_id": "...", "state": "new"}
        #         ]
        #     }
        #
        data = self.safe_value(response, 'data', {})
        orderData = self.safe_dict(data, 0, {}) if isinstance(data, list) else self.safe_dict(response, 'data', {})
        venueOrderId = self.safe_string(orderData, 'venue_order_id')
        state = self.safe_string(orderData, 'state')
        order = self.parse_order(self.extend(orderData, {
            'id': venueOrderId,
            'symbol': market['id'],
            'status': state,
            'side': side,
            'type': type,
        }), market)
        return order

    def cancel_order(self, id: str, symbol: Str = None, params={}) -> Order:
        """
        cancels an open order by its id

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-trading

        :param str id: the order id(venue order id)
        :param str symbol: not used by self exchange
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: an `order structure <https://docs.ccxt.com/?id=order-structure>`
        """
        if self.markets is None:
            self.load_markets()
        request = {
            'venue_order_id': id,
        }
        response = self.privateDelete10OrdersVenueOrderId(self.extend(request, params))
        return self.safe_order({
            'info': response,
            'id': id,
            'status': 'canceled',
        })

    def cancel_all_orders(self, symbol: Str = None, params={}) -> list[Order]:
        """
        cancels all open orders

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-trading

        :param str [symbol]: not used by self exchange
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: an empty `order structure <https://docs.ccxt.com/?id=order-structure>`
        """
        if self.markets is None:
            self.load_markets()
        self.privateDelete10Orders(params)
        return []

    def fetch_order(self, id: str, symbol: Str = None, params={}) -> Order:
        """
        fetches an order by its id

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data

        :param str id: the order id(venue order id)
        :param str symbol: unified symbol of the market the order was made in
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: an `order structure <https://docs.ccxt.com/?id=order-structure>`
        """
        if self.markets is None:
            self.load_markets()
        request = {
            'venue_order_id': id,
        }
        response = self.privateGet10OrdersVenueOrderId(self.extend(request, params))
        #
        #     {
        #         "data": {
        #             "id": "uuid", "client_order_id": "uuid", "symbol": "BTC/USD",
        #             "side": "buy", "type": "limit", "quantity": "0.002", "filled_quantity": "0",
        #             "leaves_quantity": "0.002", "amount": "240.00", "filled_amount": "0",
        #             "price": "120000.00", "average_fill_price": "0", "total_fee": "0",
        #             "fee_currency": "USD", "status": "new", "time_in_force": "gtc",
        #             "execution_instructions": ["allow_taker"],
        #             "created_date": 1785309833816, "updated_date": 1785313433816
        #         }
        #     }
        #
        data = self.safe_dict(response, 'data', {})
        market = None
        if symbol is not None:
            market = self.market(symbol)
        return self.parse_order(data, market)

    def fetch_open_orders(self, symbol: Str = None, since: Int = None, limit: Int = None, params={}) -> list[Order]:
        """
        fetches all open orders for the authenticated user

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data

        :param str|None symbol: unified symbol of the market to fetch open orders for
        :param int [since]: timestamp in ms of the earliest order to fetch
        :param int [limit]: the maximum number of orders to return(1-300, default 300)
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.cursor]: pagination cursor from the previous response
        :param str[] [params.orderStates]: filter by order states, e.g. ['new', 'partially_filled']
        :param str[] [params.orderTypes]: filter by order types, e.g. ['limit', 'market']
        :param str [params.side]: filter by side, 'buy' or 'sell'
        :returns dict[]: a list of `order structures <https://docs.ccxt.com/?id=order-structure>`
        """
        if self.markets is None:
            self.load_markets()
        request = {}
        if symbol is not None:
            market = self.market(symbol)
            request['symbols'] = market['id']
        if limit is not None:
            request['limit'] = limit
        cursor = self.safe_string(params, 'cursor')
        if cursor is not None:
            request['cursor'] = cursor
        orderStates = self.safe_list_2(params, 'orderStates', 'order_states')
        if orderStates is not None:
            request['order_states'] = ','.join(orderStates)
        orderTypes = self.safe_list_2(params, 'orderTypes', 'order_types')
        if orderTypes is not None:
            request['order_types'] = ','.join(orderTypes)
        side = self.safe_string(params, 'side')
        if side is not None:
            request['side'] = side
        response = self.privateGet10OrdersActive(self.extend(request, self.omit(params, ['cursor', 'orderStates', 'order_states', 'orderTypes', 'order_types', 'side'])))
        #
        #     {
        #         "data": [{"id": "uuid", "client_order_id": "uuid", "symbol": "BTC/USD", ...}],
        #         "metadata": {"timestamp": 1785313433816, "next_cursor": "..."}
        #     }
        #
        data = self.safe_list(response, 'data', [])
        result = []
        for i in range(0, len(data)):
            order = self.safe_dict(data, i, {})
            result.append(self.parse_order(order))
        return self.filter_by_symbol_since_limit(result, symbol, since, limit)

    def fetch_orders(self, symbol: Str = None, since: Int = None, limit: Int = None, params={}) -> list[Order]:
        """
        fetches historical orders for the authenticated user

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data

        :param str|None symbol: unified symbol of the market to fetch orders for
        :param int [since]: timestamp in ms of the earliest order to fetch, the lookup window is limited to 30 days
        :param int [limit]: the maximum number of orders to return(1-1900, default 1900)
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param int [params.until]: timestamp in ms of the latest order to fetch
        :param str [params.cursor]: pagination cursor from the previous response
        :param str[] [params.orderStates]: filter by order states, e.g. ['filled', 'cancelled', 'rejected']
        :param str[] [params.orderTypes]: filter by order types, e.g. ['limit', 'market']
        :returns dict[]: a list of `order structures <https://docs.ccxt.com/?id=order-structure>`
        """
        if self.markets is None:
            self.load_markets()
        request = {}
        if symbol is not None:
            market = self.market(symbol)
            request['symbols'] = market['id']
        thirtyDays = 2592000000
        until = self.safe_integer_2(params, 'until', 'until')
        if since is not None:
            request['start_date'] = since
        elif until is not None:
            request['start_date'] = until - thirtyDays
        if until is not None:
            request['end_date'] = until
        elif since is not None:
            now = self.milliseconds()
            defaultEnd = since + thirtyDays
            request['end_date'] = defaultEnd if (defaultEnd < now) else now
        if limit is not None:
            request['limit'] = limit
        cursor = self.safe_string(params, 'cursor')
        if cursor is not None:
            request['cursor'] = cursor
        orderStates = self.safe_list_2(params, 'orderStates', 'order_states')
        if orderStates is not None:
            request['order_states'] = ','.join(orderStates)
        orderTypes = self.safe_list_2(params, 'orderTypes', 'order_types')
        if orderTypes is not None:
            request['order_types'] = ','.join(orderTypes)
        response = self.privateGet10OrdersHistorical(self.extend(request, self.omit(params, ['until', 'cursor', 'orderStates', 'order_states', 'orderTypes', 'order_types'])))
        data = self.safe_list(response, 'data', [])
        result = []
        for i in range(0, len(data)):
            order = self.safe_dict(data, i, {})
            result.append(self.parse_order(order))
        return self.filter_by_symbol_since_limit(result, symbol, since, limit)

    def fetch_closed_orders(self, symbol: Str = None, since: Int = None, limit: Int = None, params={}) -> list[Order]:
        """
        fetches closed(filled, cancelled, rejected) orders for the authenticated user

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data

        :param str|None symbol: unified symbol of the market to fetch orders for
        :param int [since]: timestamp in ms of the earliest order to fetch, the lookup window is limited to 30 days
        :param int [limit]: the maximum number of orders to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of `order structures <https://docs.ccxt.com/?id=order-structure>`
        """
        orderStates = self.safe_list_2(params, 'orderStates', 'order_states', ['filled', 'cancelled', 'rejected', 'replaced'])
        requestParams = self.extend(self.omit(params, ['orderStates', 'order_states']), {
            'order_states': orderStates,
        })
        return self.fetch_orders(symbol, since, limit, requestParams)

    def parse_my_trade(self, trade: dict, market: Market = None) -> Trade:
        """
        parses a trade from the exchange's private trade format
 @ignore
        :param dict trade: the raw trade data from the exchange
        :param dict [market]: the market the trade was executed in
        :returns dict: a `trade structure <https://docs.ccxt.com/?id=trade-structure>`
        """
        id = self.safe_string(trade, 'tid')
        orderId = self.safe_string(trade, 'oid')
        price = self.safe_number(trade, 'p')
        amount = self.safe_number(trade, 'q')
        side = self.safe_string_lower(trade, 's')
        timestamp = self.safe_integer_2(trade, 'tdt', 'pdt')
        isMaker = self.safe_bool(trade, 'im', False)
        takerOrMaker = 'maker' if (isMaker) else 'taker'
        cost = None
        if price is not None and amount is not None:
            cost = price * amount
        symbol = self.safe_symbol(None, market)
        return {
            'info': trade,
            'id': id,
            'order': orderId,
            'symbol': symbol,
            'side': side,
            'type': None,
            'takerOrMaker': takerOrMaker,
            'price': price,
            'amount': amount,
            'cost': cost,
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'fee': None,
            'fees': [],
        }

    def fetch_my_trades(self, symbol: Str = None, since: Int = None, limit: Int = None, params={}) -> list[Trade]:
        """
        fetches the trade history for the authenticated user

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data

        :param str symbol: unified symbol of the market to fetch trades for
        :param int [since]: timestamp in ms of the earliest trade to fetch, the lookup window is limited to 30 days
        :param int [limit]: the maximum number of trades to return(1-1900, default 1900)
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param int [params.until]: timestamp in ms of the latest trade to fetch
        :param str [params.cursor]: pagination cursor from the previous response
        :returns dict[]: a list of `trade structures <https://docs.ccxt.com/?id=trade-structure>`
        """
        if self.markets is None:
            self.load_markets()
        if symbol is None:
            raise ArgumentsRequired(self.id + ' fetchMyTrades() requires a symbol parameter')
        market = self.market(symbol)
        request = {
            'symbol': market['id'],
        }
        thirtyDays = 2592000000
        until = self.safe_integer_2(params, 'until', 'until')
        if since is not None:
            request['start_date'] = since
        elif until is not None:
            request['start_date'] = until - thirtyDays
        if until is not None:
            request['end_date'] = until
        elif since is not None:
            now = self.milliseconds()
            defaultEnd = since + thirtyDays
            request['end_date'] = defaultEnd if (defaultEnd < now) else now
        if limit is not None:
            request['limit'] = limit
        cursor = self.safe_string(params, 'cursor')
        if cursor is not None:
            request['cursor'] = cursor
        response = self.privateGet10TradesPrivateSymbol(self.extend(request, self.omit(params, ['until'])))
        #
        #     {
        #         "data": [
        #             {"tdt": 1785309833816, "p": "119900.00", "q": "0.00100000",
        #               "tid": "ad3e8787ab623ba5a1dfea53819be6f9", "oid": "2affb2ac-4cf7-4bbf-b7b2-fc1e885bdc2c",
        #               "s": "buy", "im": False}
        #         ],
        #         "metadata": {"timestamp": 1785313433816, "next_cursor": "..."}
        #     }
        #
        data = self.safe_list(response, 'data', [])
        result = []
        for i in range(0, len(data)):
            trade = self.safe_dict(data, i, {})
            result.append(self.parse_my_trade(trade, market))
        return result

    def edit_order(self, id: str, symbol: str, type: OrderType, side: OrderSide, amount: Num = None, price: Num = None, params={}) -> Order:
        """
        replaces an existing order

        https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-trading

        :param str id: the order id(venue order id) to replace
        :param str symbol: unified symbol of the market
        :param str type: 'limit' or 'market'
        :param str side: 'buy' or 'sell'
        :param float amount: the new amount
        :param float [price]: the new price
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.clientOrderId]: a custom client order id(UUID)
        :param float [params.cost]: the new order cost in units of the quote currency
        :param str [params.timeInForce]: e.g. gtc
        :param str[] [params.executionInstructions]: e.g. ['post_only']
        :returns dict: an `order structure <https://docs.ccxt.com/?id=order-structure>`
        """
        # note: the exchange assigns a new venue_order_id on replace — the returned order carries the new id
        if self.markets is None:
            self.load_markets()
        market = self.market(symbol)
        clientOrderId = self.safe_string_2(params, 'clientOrderId', 'client_order_id', self.uuid())
        cost = self.safe_string_2(params, 'cost', 'quote_size')
        timeInForce = self.safe_string_lower_2(params, 'timeInForce', 'time_in_force')
        executionInstructions = self.safe_list(params, 'executionInstructions', self.safe_list(params, 'execution_instructions'))
        request = {
            'client_order_id': clientOrderId,
            'venue_order_id': id,
        }
        if cost is not None:
            request['quote_size'] = self.cost_to_precision(symbol, cost)
        elif amount is not None:
            request['base_size'] = self.amount_to_precision(symbol, amount)
        if price is not None:
            request['price'] = self.price_to_precision(symbol, price)
        if timeInForce is not None:
            request['time_in_force'] = timeInForce
        if executionInstructions is not None:
            request['execution_instructions'] = executionInstructions
        response = self.privatePut10OrdersVenueOrderId(self.extend(request, self.omit(params, ['clientOrderId', 'client_order_id', 'cost', 'quote_size', 'timeInForce', 'time_in_force', 'executionInstructions', 'execution_instructions'])))
        #
        #     {
        #         "data": [
        #             {"venue_order_id": "7a52e92e-8639-4fe1-abaa-68d3a2d5234b", "client_order_id": "...", "state": "new"}
        #         ]
        #     }
        #
        data = self.safe_value(response, 'data', {})
        orderData = self.safe_dict(data, 0, {}) if isinstance(data, list) else self.safe_dict(response, 'data', {})
        newVenueOrderId = self.safe_string(orderData, 'venue_order_id')
        state = self.safe_string(orderData, 'state')
        order = self.parse_order(self.extend(orderData, {
            'id': newVenueOrderId,
            'symbol': market['id'],
            'status': state,
            'side': side,
            'type': type,
        }), market)
        return order

    def handle_errors(self, code: int, reason: str, url: str, method: str, headers: dict, body: str, response: object, requestHeaders: object, requestBody: object):
        if code >= 400:
            if response is None:
                return None
            feedback = self.id + ' ' + body
            errorMessage = None
            if isinstance(response, dict):
                errorMessage = self.safe_string_2(response, 'message', 'error')
            if errorMessage is not None:
                self.throw_broadly_matched_exception(self.exceptions['broad'], errorMessage, feedback)
            return None
        return None
