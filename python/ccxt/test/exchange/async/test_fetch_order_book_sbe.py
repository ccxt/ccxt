import os
import sys

root = os.path.dirname(os.path.dirname(os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))))
sys.path.append(root)

# ----------------------------------------------------------------------------

# PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
# https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

# ----------------------------------------------------------------------------
# -*- coding: utf-8 -*-

from ccxt.test.exchange.base import test_order_book  # noqa E402

"""
 * Test for OKX's SBE (Simple Binary Encoding) orderbook endpoint
 * Note: This test requires a valid instIdCode which must be obtained from OKX API
 * The instIdCode is an integer identifier for each trading pair
"""
async def test_fetch_order_book_sbe(exchange, skipped_properties, symbol, inst_id_code):
    method = 'fetchOrderBookSbe'
    # Check if the method exists (OKX-specific)
    exchange_any = exchange
    if not exchange_any.fetch_order_book_sbe:
        print('fetchOrderBookSbe is not supported by', exchange.id)
        return True
    orderbook = await exchange.fetch_order_book_sbe(symbol, inst_id_code)
    test_order_book(exchange, skipped_properties, method, orderbook, symbol)
    # Additional SBE-specific validations
    # The orderbook should have the standard structure
    print('SBE Orderbook fetched successfully for', symbol)
    print('Bids:', len(orderbook.bids), 'Asks:', len(orderbook.asks))
    return True
