# -*- coding: utf-8 -*-

# PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
# https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

from ccxt.async_support.base.prediction_exchange import PredictionExchange
from ccxt.abstract.prediction.binance import ImplicitAPI
import hashlib
from ccxt.base.types import Any, Balances, Int, Market, Num, Str, Strings, PredictionEvent, fetchEventsParams, PredictionTicker, PredictionTickers, PredictionOrder, PredictionOrderBook, PredictionTrade, PredictionPosition
from typing import List
from ccxt.base.errors import ExchangeError
from ccxt.base.errors import AuthenticationError
from ccxt.base.errors import PermissionDenied
from ccxt.base.errors import ArgumentsRequired
from ccxt.base.errors import BadRequest
from ccxt.base.errors import BadSymbol
from ccxt.base.errors import InsufficientFunds
from ccxt.base.errors import InvalidOrder
from ccxt.base.errors import OrderNotFound
from ccxt.base.errors import NotSupported
from ccxt.base.errors import RateLimitExceeded
from ccxt.base.errors import InvalidNonce
from ccxt.base.decimal_to_precision import ROUND
from ccxt.base.decimal_to_precision import TRUNCATE
from ccxt.base.decimal_to_precision import DECIMAL_PLACES
from ccxt.base.precise import Precise


class binance(PredictionExchange, ImplicitAPI):

    def describe(self) -> Any:
        return self.deep_extend(super(binance, self).describe(), {
            'id': 'binance',
            'name': 'Binance',
            'countries': [],
            # all prediction endpoints weigh 200 against the 12000/min SAPI IP budget(5 ms per weight unit)
            'rateLimit': 5,
            'version': 'v1',
            'certified': False,
            'pro': False,
            'has': {
                'CORS': None,
                'spot': False,
                'margin': False,
                'swap': False,
                'future': False,
                'option': False,
                'cancelOrder': True,
                'cancelOrders': True,
                'createMarketOrderWithCost': True,
                'createOrder': True,
                'fetchBalance': True,
                'fetchEvent': True,
                'fetchEvents': True,
                'fetchMarkets': True,
                'fetchMyTrades': True,
                'fetchOpenOrders': True,
                'fetchOrderBook': True,
                'fetchOrders': True,
                'fetchPosition': True,
                'fetchPositions': True,
                'fetchTicker': True,
                'fetchTickers': True,
                'prediction': True,
            },
            'urls': {
                'logo': 'https://github.com/user-attachments/assets/e9419b93-ccb0-46aa-9bff-c883f096274b',
                'api': {
                    'sapi': 'https://api.binance.com/sapi/v1/w3w/wallet/prediction',
                },
                'www': 'https://www.binance.com/en/markets/prediction',
                'doc': [
                    'https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading',
                ],
            },
            'api': {
                'sapi': {
                    'private': {
                        'get': {
                            'category/list': {'cost': 200},
                            'market/list': {'cost': 200},
                            'market/search': {'cost': 200},
                            'market/detail': {'cost': 200},
                            'order-book': {'cost': 200},
                            'order-book/last-trade-price': {'cost': 200},
                            'wallet/list': {'cost': 200},
                            'balance/payment-options': {'cost': 200},
                            'quota/limit/status': {'cost': 200},
                            'pnl/portfolio': {'cost': 200},
                            'pnl/query': {'cost': 200},
                            'position/list': {'cost': 200},
                            'position/filter': {'cost': 200},
                            'position/token': {'cost': 200},
                            'position/settled-history': {'cost': 200},
                            'order/list': {'cost': 200},
                            'order/history': {'cost': 200},
                        },
                        'post': {
                            'trade/get-quote': {'cost': 200},
                            'trade/place-order-bundle': {'cost': 200},
                            'trade/batch-cancel': {'cost': 200},
                        },
                    },
                },
            },
            'requiredCredentials': {
                'apiKey': True,
                'secret': True,
            },
            'fees': {
                'trading': {
                    'tierBased': False,
                    'percentage': True,
                    'maker': 0,
                    'taker': 0.02,  # default feeRateBps of 200 applied per order
                },
            },
            'exceptions': {
                'exact': {
                    '-1003': RateLimitExceeded,
                    '-1021': InvalidNonce,  # timestamp outside recvWindow
                    '-1022': AuthenticationError,  # invalid signature
                    '-1102': BadRequest,  # mandatory parameter missing
                    '-1121': BadSymbol,
                    '-2008': AuthenticationError,  # invalid api-key id
                    '-2014': AuthenticationError,  # api-key format invalid
                    '-2015': PermissionDenied,  # invalid key, ip or permissions
                    '-2010': InsufficientFunds,
                },
                'broad': {
                    'insufficient balance': InsufficientFunds,
                    'Order does not exist': InvalidOrder,
                },
            },
            'options': {
                'defaultVendor': 'PREDICT_FUN',
                'fetchOutcomeSearchLimit': 10,
                'maxFetchEventsResults': 25,   # cap on topics fetched canonically when the caller gives no limit
                'marketsPageLimit': 100,       # market/list page size
                'maxFetchMarketsLimit': 200,   # cap on topics collected by an unscoped fetchMarkets
                'loadAllOutcomes': False,
                # the market listing is bounded(maxFetchEventsResults), so an unscoped
                # fetchEvents pages a capped listing instead of requiring a search scope
                'allowUnscopedFetchEvents': True,
                # venue-specific fetchEvents scope params accepted by requireEventQuery in
                # addition to the unified query/queries/tags/eventId/slug
                'eventScopeParams': ['l1Category', 'l2Category'],
            },
        })

    def nonce(self) -> float:
        return self.milliseconds()

    async def fetch_markets(self, params={}) -> List[Market]:
        """
        fetches binance prediction markets; with a query it resolves the query via the search endpoint and returns the matched topics' markets, otherwise it pages the market listing

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/market-data

        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.query]: a single search query resolved against the market search endpoint
        :param str[] [params.queries]: multiple search queries(alternative to query)
        :param str [params.l1Category]: filter the listing by a level-1 category id(see the category/list endpoint)
        :param str [params.l2Category]: filter the listing by a level-2 category id
        :param int [params.limit]: for an unscoped listing(no query), the max number of topics to collect(defaults to options.maxFetchMarketsLimit, 200)
        :returns dict[]: an array of objects representing market data
        """
        queries = self.parse_search_queries(params)
        queriesLength = len(queries)
        if queriesLength > 0:
            eventParams = self.omit(params, ['limit'])
            events = await self.fetch_events(eventParams)
            eventsLength = len(events)
            queryMarkets = []
            for ei in range(0, eventsLength):
                eventMarkets = self.safe_list(events[ei], 'markets', [])
                eventMarketsLength = len(eventMarkets)
                for mi in range(0, eventMarketsLength):
                    queryMarkets.append(eventMarkets[mi])
            return queryMarkets
        maxMarkets = self.safe_integer(params, 'limit', self.safe_integer(self.options, 'maxFetchMarketsLimit', 200))
        rest = self.omit(params, ['query', 'queries', 'limit'])
        rawTopics = await self.fetch_raw_topics(maxMarkets, rest)
        parsedEvents = []
        flatMarkets = []
        rawTopicsLength = len(rawTopics)
        for i in range(0, rawTopicsLength):
            parsedEvent = self.parse_event(rawTopics[i])
            parsedEvents.append(parsedEvent)
            eventMarkets = self.safe_list(parsedEvent, 'markets', [])
            eventMarketsLength = len(eventMarkets)
            for mi in range(0, eventMarketsLength):
                flatMarkets.append(eventMarkets[mi])
        self.set_events(parsedEvents)
        return flatMarkets

    async def fetch_raw_topics(self, maxTopics: Int, rest={}) -> List[Any]:
        """
 @ignore
        pages the market/list endpoint and returns up to `maxTopics` raw market topics

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/market-data#list-prediction-markets

        :param int maxTopics: stop collecting once self many topics are gathered
        :param dict [rest]: extra params forwarded verbatim to the listing endpoint(l1Category, l2Category, sortBy, orderBy)
        :returns dict[]: raw market topic objects
        """
        if maxTopics is None:
            maxTopics = self.safe_integer(self.options, 'maxFetchMarketsLimit', 200)
        pageLimit = self.safe_integer(self.options, 'marketsPageLimit', 100)
        if pageLimit > 100:
            pageLimit = 100
        collected = []
        offset = 0
        while(True):
            reqLimit = pageLimit
            collectedLength = len(collected)
            remaining = maxTopics - collectedLength
            if remaining < reqLimit:
                reqLimit = remaining
            if reqLimit <= 0:
                break
            request = {
                'offset': offset,
                'limit': reqLimit,
            }
            response = await self.sapiPrivateGetMarketList(self.extend(request, rest))
            #
            #     {
            #         "marketTopics": [
            #             {
            #                 "marketTopicId": 4229564,
            #                 "vendor": "PREDICT_FUN",
            #                 "chainId": "56",
            #                 "slug": "btc-price-1h-up-or-down",
            #                 "title": "BTC Price 1h Up or Down?",
            #                 "question": "Will BTC price go UP?",
            #                 "topicType": "FLAT",
            #                 "chartType": "CRYPTO_UP_DOWN",
            #                 "symbol": "BTCUSDT",
            #                 "participantCount": 3420,
            #                 "collateral": "USDT",
            #                 "feeRateBps": 200,
            #                 "slippageBps": 1200,
            #                 "tradeVolume": "158234.56",
            #                 "liquidity": "45000.00",
            #                 "publishedAt": 1748100000000,
            #                 "startDate": 1748131200000,
            #                 "endDate": 1748134800000,
            #                 "status": "REGISTERED",
            #                 "markets": []
            #             }
            #         ],
            #         "total": 128,
            #         "offset": 0,
            #         "limit": 20,
            #         "hasMore": True
            #     }
            #
            pageTopics = self.safe_list(response, 'marketTopics', [])
            pageTopicsLength = len(pageTopics)
            for i in range(0, pageTopicsLength):
                collected.append(pageTopics[i])
            hasMore = self.safe_bool(response, 'hasMore', False)
            if not hasMore or (pageTopicsLength < reqLimit):
                break
            offset = self.sum(offset, pageTopicsLength)
        return collected

    async def fetch_raw_topic_detail(self, topicId: str, params={}) -> Any:
        """
 @ignore
        fetches a single raw market topic(with nested markets and outcome tokens) by its id
        :param str topicId: the marketTopicId
        :param dict [params]: extra params forwarded verbatim to the detail endpoint
        :returns dict: the raw market topic object
        """
        request = {
            'marketTopicId': topicId,
        }
        return await self.sapiPrivateGetMarketDetail(self.extend(request, params))

    async def complete_raw_topics(self, rawTopics: List[Any]) -> List[Any]:
        """
 @ignore
        ensures each raw topic carries fully-populated nested markets(with outcome token ids), fetching the topic detail when the listing/search payload omitted them
        :param dict[] rawTopics: raw market topic objects
        :returns dict[]: raw market topic objects with usable nested markets
        """
        result = []
        rawTopicsLength = len(rawTopics)
        for i in range(0, rawTopicsLength):
            rawTopic = rawTopics[i]
            rawMarkets = self.safe_list(rawTopic, 'markets', [])
            rawMarketsLength = len(rawMarkets)
            hasOutcomes = False
            if rawMarketsLength > 0:
                firstMarket = self.safe_dict(rawMarkets, 0, {})
                firstOutcomes = self.safe_list(firstMarket, 'outcomes', [])
                firstOutcomesLength = len(firstOutcomes)
                hasOutcomes = (firstOutcomesLength > 0)
            if hasOutcomes:
                result.append(rawTopic)
            else:
                topicId = self.safe_string(rawTopic, 'marketTopicId')
                if topicId is not None:
                    detail = await self.fetch_raw_topic_detail(topicId)
                    result.append(detail)
        return result

    async def fetch_events(self, params: fetchEventsParams = {}) -> List[PredictionEvent]:
        """
        fetches prediction-market events(market topics); the call must be scoped by query/queries/tags, eventId, or an l1Category/l2Category listing filter

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/market-data

        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.query]: a free-text search resolved against the semantic market search endpoint
        :param str[] [params.queries]: multiple free-text searches(alternative to query)
        :param str[] [params.tags]: treated free-text searches(binance has no tag taxonomy)
        :param str [params.eventId]: a marketTopicId, fetched directly via the detail endpoint
        :param str [params.l1Category]: scope the listing server-side by a level-1 category id
        :param str [params.l2Category]: scope the listing server-side by a level-2 category id
        :param int [params.limit]: the maximum number of events to return
        :param str [params.sort]: 'volume' | 'liquidity' | 'newest'(client-side)
        :param str [params.status]: 'active' | 'closed' | 'all'(client-side)
        :param str [params.sortBy]: sort events by server side('RECOMMENDED' | 'VOLUME' | 'PARTICIPANTS' | 'CREATED_TIME' | 'END_DATE'), works when no queries and eventId provided
        :param str [params.orderBy]: order events by server side('ASC' | 'DESC'), works when no queries and eveitId provided
        :returns dict[]: a list of [prediction event structures](https://docs.ccxt.com/#/?id=prediction-event-structure)
        """
        allowUnscopedFetchEvents = self.safe_bool(self.options, 'allowUnscopedFetchEvents', False)
        if not allowUnscopedFetchEvents:
            self.require_event_query(params)
        queries = self.parse_search_queries(params)
        # binance has no tag taxonomy — resolve requested tags through the semantic search too
        tags = self.safe_list(params, 'tags', [])
        tagsLength = len(tags)
        allQueries = []
        for i in range(0, len(queries)):
            allQueries.append(queries[i])
        for i in range(0, tagsLength):
            allQueries.append(tags[i])
        allQueriesLength = len(allQueries)
        params = self.omit(params, ['query', 'queries'])
        userLimit = self.safe_integer(params, 'limit')
        fetchCap = self.safe_integer(self.options, 'maxFetchEventsResults', 100)
        if userLimit is not None:
            fetchCap = userLimit
        rest = self.omit(params, ['status', 'limit', 'sort', 'searchIn', 'eventId', 'slug', 'tags', 'l1Category', 'l2Category'])
        eventId = self.safe_string(params, 'eventId')
        l1Category = self.safe_string(params, 'l1Category')
        l2Category = self.safe_string(params, 'l2Category')
        if not self.markets:
            self.markets = self.create_safe_dictionary()
        rawTopics = []
        if allQueriesLength > 0:
            rawTopics = await self.fetch_events_by_query(allQueries, fetchCap, rest)
        elif eventId is not None:
            detail = await self.fetch_raw_topic_detail(eventId, rest)
            rawTopics = [detail]
        else:
            listingRequest = {}
            if l1Category is not None:
                listingRequest['l1Category'] = l1Category
            if l2Category is not None:
                listingRequest['l2Category'] = l2Category
            sortBy = self.safe_string_upper_2(params, 'sortBy', 'sort')
            if sortBy is not None:
                # map the unified sort values onto the server enum, one of RECOMMENDED,
                # VOLUME, PARTICIPANTS, CREATED_TIME or END_DATE — 'liquidity' has no
                # server-side equivalent and stays in params so the base
                # applyEventFetchParams sorts it client-side instead
                if sortBy == 'NEWEST':
                    sortBy = 'CREATED_TIME'
                elif sortBy == 'LIQUIDITY':
                    sortBy = None
                if sortBy is not None:
                    listingRequest['sortBy'] = sortBy
                    params = self.omit(params, ['sort', 'sortBy'])
            listed = await self.fetch_raw_topics(fetchCap, self.extend(listingRequest, rest))
            rawTopics = await self.complete_raw_topics(listed)
        rawTopicsLength = len(rawTopics)
        result = []
        for i in range(0, rawTopicsLength):
            parsedEvent = self.parse_event(rawTopics[i])
            result.append(parsedEvent)
            parsedMarkets = self.safe_list(parsedEvent, 'markets', [])
            parsedMarketsLength = len(parsedMarkets)
            for mi in range(0, parsedMarketsLength):
                m = parsedMarkets[mi]
                # prediction market rows are keyed by the unified 'market' handle
                handle = self.safe_string(m, 'market')
                if handle is not None:
                    self.markets[handle] = m
        self.populate_outcomes()
        # scoping already happened server-side: the tag filter needs an event-level tags field
        # binance topics lack, and the query filter would drop semantic-search matches whose
        # title uses different words than the query
        postParams = self.omit(params, ['tags', 'l1Category', 'l2Category'])
        return self.apply_event_fetch_params(result, postParams, [])

    async def fetch_events_by_query(self, queries: List[str], limit: Int, rest={}) -> List[Any]:
        """
 @ignore
        resolves free-text queries through the semantic market search endpoint, then completes the matched topics with their outcome tokens

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/market-data#market-search

        :param str[] queries: free-text search strings
        :param int [limit]: max number of topics to fetch
        :param dict [rest]: extra params forwarded verbatim to the search endpoint
        :returns dict[]: raw market topic objects with usable nested markets
        """
        seen = {}
        collected = []
        queriesLength = len(queries)
        if limit is None:
            limit = 20
        elif limit > 50:
            limit = 50
        for qi in range(0, queriesLength):
            request = {
                'query': queries[qi],
            }
            request['topK'] = limit
            response = await self.sapiPrivateGetMarketSearch(self.extend(request, rest))
            #
            #     [
            #         {
            #             "marketTopicId": 4229564,
            #             "vendor": "PREDICT_FUN",
            #             "slug": "btc-price-1h-up-or-down",
            #             "title": "BTC Price 1h Up or Down?",
            #             ...
            #             "markets": []
            #         }
            #     ]
            #
            responseLength = len(response)
            for i in range(0, responseLength):
                rawTopic = response[i]
                topicId = self.safe_string(rawTopic, 'marketTopicId')
                if topicId is not None:
                    already = self.safe_string(seen, topicId)
                    if already is None:
                        seen[topicId] = topicId
                        collected.append(rawTopic)
        capped = collected
        collectedLength = len(collected)
        if (limit is not None) and (collectedLength > limit):
            capped = self.array_slice(collected, 0, limit)
        return await self.complete_raw_topics(capped)

    async def fetch_event(self, id: str, params={}) -> PredictionEvent:
        """
        fetches a single prediction-market event(market topic) by its marketTopicId

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/market-data

        :param str id: the marketTopicId
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction event structure](https://docs.ccxt.com/#/?id=prediction-event-structure)
        """
        events = await self.fetch_events(self.extend({'eventId': id}, params))
        return self.safe_dict(events, 0)

    def parse_event(self, rawTopic: dict) -> Any:
        """
 @ignore
        parses a raw binance market topic(with nested markets) into the unified event shape
        :param dict rawTopic: the raw market topic object
        :returns dict: an event structure
        """
        #
        #     {
        #         "marketTopicId": 4229564,
        #         "vendor": "PREDICT_FUN",
        #         "chainId": "56",
        #         "slug": "btc-price-1h-up-or-down",
        #         "title": "BTC Price 1h Up or Down?",
        #         "question": "Will BTC price go UP?",
        #         "description": "Resolves YES if BTC spot price is higher than the starting price.",
        #         "imageUrl": "https://...",
        #         "topicType": "FLAT",
        #         "chartType": "CRYPTO_UP_DOWN",
        #         "symbol": "BTCUSDT",
        #         "variantData": {"type": "CRYPTO_UP_DOWN", "startPrice": "67890.12", "endPrice": null},
        #         "participantCount": 3420,
        #         "collateral": "USDT",
        #         "feeRateBps": 200,
        #         "slippageBps": 1200,
        #         "tradeVolume": "158234.56",
        #         "liquidity": "45000.00",
        #         "publishedAt": 1748100000000,
        #         "startDate": 1748131200000,
        #         "endDate": 1748134800000,
        #         "status": "REGISTERED",
        #         "timeline": [...],
        #         "markets": [{"marketId": 5567895, "title": "UP", "outcomes": [...]}]
        #     }
        #
        rawMarkets = self.safe_list(rawTopic, 'markets', [])
        marketsList = []
        anyActive = False
        rawMarketsLength = len(rawMarkets)
        for i in range(0, rawMarketsLength):
            parsed = self.parse_topic_market(rawMarkets[i], rawTopic)
            marketsList.append(parsed)
            if self.safe_bool(parsed, 'active', False):
                anyActive = True
        topicId = self.safe_string(rawTopic, 'marketTopicId')
        slug = self.safe_string(rawTopic, 'slug')
        title = self.safe_string(rawTopic, 'title')
        endDate = self.safe_integer(rawTopic, 'endDate')
        created = self.safe_integer_2(rawTopic, 'publishedAt', 'startDate')
        status = self.safe_string(rawTopic, 'status')
        active = anyActive
        if rawMarketsLength == 0:
            active = (status == 'REGISTERED') or (status == 'OPEN')
        resolved = None
        if status is not None:
            resolved = (status == 'RESOLVED') or (status == 'SETTLED')
        return {
            'id': topicId,
            'slug': slug,
            'event': self.shorten_slug(slug) if (slug is not None) else None,
            'title': title,
            'description': self.safe_string(rawTopic, 'description'),
            'markets': marketsList,
            'active': active,
            'volume': self.safe_number(rawTopic, 'tradeVolume'),
            'liquidity': self.safe_number(rawTopic, 'liquidity'),
            'url': None,
            'image': self.safe_string(rawTopic, 'imageUrl'),
            'created': created,
            'createdDatetime': self.iso8601(created),
            'end': endDate,
            'endDatetime': self.iso8601(endDate),
            'category': self.safe_string(rawTopic, 'chartType'),
            'resolved': resolved,
            'info': rawTopic,
        }

    def parse_topic_market(self, rawMarket: dict, rawTopic: dict) -> Market:
        """
 @ignore
        parses one nested market of a market topic into the unified market shape, building its outcome tokens
        :param dict rawMarket: the nested market object
        :param dict rawTopic: the enclosing raw market topic(carries slug/vendor/fees/dates)
        :returns dict: a market structure
        """
        #
        #     {
        #         "marketId": 5567895,
        #         "externalId": "ext_001",
        #         "title": "UP",
        #         "question": "Will BTC go UP?",
        #         "description": "Resolves YES if BTC price increases.",
        #         "conditionId": "0xabc123",
        #         "status": "REGISTERED",
        #         "tradingStatus": "OPEN",
        #         "tradeVolume": "90000.00",
        #         "liquidity": "25000.00",
        #         "decimalPrecision": 2,
        #         "outcomes": [
        #             {"name": "YES", "price": "0.52", "chance": "0.52", "index": 0, "tokenId": "112233"}
        #         ]
        #     }
        #
        marketId = self.safe_string(rawMarket, 'marketId')
        topicId = self.safe_string(rawTopic, 'marketTopicId')
        topicSlug = self.safe_string(rawTopic, 'slug')
        vendor = self.safe_string(rawTopic, 'vendor')
        collateral = self.safe_string(rawTopic, 'collateral', 'USDT')
        title = self.safe_string(rawMarket, 'title', marketId)
        marketSymbol = self.slug_to_market_symbol(topicSlug, title)
        tradingStatus = self.safe_string(rawMarket, 'tradingStatus')
        status = self.safe_string(rawMarket, 'status')
        active = (tradingStatus == 'OPEN')
        if tradingStatus is None:
            active = (status == 'REGISTERED') or (status == 'OPEN')
        resolved = (status == 'RESOLVED') or (status == 'SETTLED')
        endDate = self.safe_integer(rawTopic, 'endDate')
        feeRateBps = self.safe_string(rawTopic, 'feeRateBps', '200')
        feeRate = self.parse_number(Precise.string_div(feeRateBps, '10000'))
        decimalPrecision = self.safe_string(rawMarket, 'decimalPrecision', '2')
        pricePrecision = self.parse_number(self.parse_precision(decimalPrecision))
        precision = {
            'amount': 0.01,  # always be 2
            'price': pricePrecision,
        }
        volume = self.safe_number(rawMarket, 'tradeVolume')
        liquidity = self.safe_number(rawMarket, 'liquidity')
        rawOutcomes = self.safe_list(rawMarket, 'outcomes', [])
        outcomes = []
        resolvedOutcomeRaw = None
        rawOutcomesLength = len(rawOutcomes)
        for oi in range(0, rawOutcomesLength):
            rawOutcome = rawOutcomes[oi]
            label = self.safe_string_upper(rawOutcome, 'name')
            tokenId = self.safe_string(rawOutcome, 'tokenId')
            outcomeHandle = marketSymbol + ':' + label
            price = self.safe_string(rawOutcome, 'price')
            winnerRaw = None
            settleFractionRaw = None
            if resolved and (price is not None):
                winnerRaw = Precise.string_eq(price, '1')
                settleFractionRaw = 1 if (winnerRaw) else 0
                if winnerRaw:
                    resolvedOutcomeRaw = outcomeHandle
            winner = winnerRaw
            settleFraction = settleFractionRaw
            outcomes.append({
                'id': tokenId,
                'outcomeId': tokenId,
                'outcome': outcomeHandle,
                'market': marketSymbol,
                'label': label,
                'price': self.parse_number(price),
                'active': active,
                'winner': winner,
                'settleFraction': settleFraction,
                'precision': precision,
                'info': {
                    'tokenId': tokenId,
                    'marketId': marketId,
                    'marketTopicId': topicId,
                    'vendor': vendor,
                    'chainId': self.safe_string(rawTopic, 'chainId'),
                    'slug': topicSlug,
                    'marketTitle': title,
                    'outcomeLabel': label,
                    'index': self.safe_string(rawOutcome, 'index'),
                    'price': price,
                    'chance': self.safe_string(rawOutcome, 'chance'),
                    'collateral': collateral,
                    'feeRateBps': feeRateBps,
                    'slippageBps': self.safe_string(rawTopic, 'slippageBps'),
                    'conditionId': self.safe_string(rawMarket, 'conditionId'),
                    'externalId': self.safe_string(rawMarket, 'externalId'),
                },
            })
        resolvedOutcome = resolvedOutcomeRaw
        return {
            'id': marketId,
            'market': marketSymbol,
            'base': collateral,
            'quote': collateral,
            'settle': None,
            'baseId': marketId,
            'quoteId': collateral,
            'settleId': None,
            'type': 'prediction',
            'marketType': 'binary',
            'executionModel': 'clob',
            'collateral': collateral,
            'spot': False,
            'margin': False,
            'swap': False,
            'future': False,
            'option': False,
            'prediction': True,
            'active': active,
            'resolved': resolved,
            'resolvedOutcome': resolvedOutcome,
            'contract': False,
            'linear': None,
            'inverse': None,
            'contractSize': None,
            'expiry': endDate,
            'expiryDatetime': self.iso8601(endDate),
            'strike': None,
            'optionType': None,
            'taker': feeRate,
            'maker': 0,
            'percentage': True,
            'tierBased': False,
            'feeSide': 'get',
            'precision': precision,
            'limits': {
                'leverage': {'min': 1, 'max': 1},
                'amount': {'min': None, 'max': None},
                'price': {'min': 0.01, 'max': 0.99},
                'cost': {'min': 1.5, 'max': None},  # MARKET quotes require amountIn of at least ~1.5 USDT
            },
            'outcomes': outcomes,
            'info': self.extend(rawMarket, {
                'marketTopicId': topicId,
                'vendor': vendor,
                'slug': topicSlug,
                'volume': volume,
                'liquidity': liquidity,
            }),
            'created': self.safe_integer(rawTopic, 'publishedAt'),
        }

    async def fetch_ticker(self, outcome: Str, params={}) -> PredictionTicker:
        """
        fetches the last trade price for a single prediction outcome

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/market-data#query-last-trade-price

        :param str outcome: unified outcome handle like BTC_PRICE_1H_UP_DOWN_UP:YES, or an outcome token id
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a prediction [ticker structure](https://docs.ccxt.com/#/?id=ticker-structure)
        """
        await self.load_outcome(outcome)
        outcomeObj = self.outcome(outcome)
        info = self.safe_dict(outcomeObj, 'info', {})
        request = {
            'marketId': self.safe_string(info, 'marketId'),
        }
        response = await self.sapiPrivateGetOrderBookLastTradePrice(self.extend(request, params))
        #
        #     {"marketId": 5567895, "lastTradePrice": "0.52"}
        #
        return self.parse_prediction_ticker(response, outcomeObj)

    def parse_prediction_ticker(self, raw: dict, market: Market = None) -> PredictionTicker:
        """
 @ignore
        parses a last-trade-price response into a unified ticker object; the venue quotes the market's primary(YES) token, so a NO outcome mirrors - price
        :param dict raw: the raw last-trade-price object
        :param dict [market]: the outcome object the ticker belongs to
        :returns dict: a [ticker structure](https://docs.ccxt.com/#/?id=ticker-structure)
        """
        #
        #     {"marketId": 5567895, "lastTradePrice": "0.52"}
        #
        marketAny = market
        outcomeObj = self.safe_outcome(self.safe_string(marketAny, 'outcome'), marketAny)
        # the venue quotes the market's primary token(outcome index 0, e.g. YES or UP),
        # any other outcome of a binary market mirrors - price
        outcomeInfo = self.safe_dict(outcomeObj, 'info', {})
        outcomeIndex = self.safe_string(outcomeInfo, 'index')
        isMirrored = False
        if outcomeIndex is not None:
            isMirrored = (outcomeIndex != '0')
        else:
            label = self.safe_string_upper(outcomeObj, 'label', 'YES')
            isMirrored = (label == 'NO') or (label == 'DOWN')
        lastString = self.safe_string(raw, 'lastTradePrice')
        last = None
        if lastString is not None:
            if isMirrored:
                last = self.parse_number(Precise.string_sub('1', lastString))
            else:
                last = self.parse_number(lastString)
        now = self.milliseconds()
        return self.safe_prediction_ticker({
            'outcome': self.safe_string(outcomeObj, 'outcome'),
            'outcomeId': self.safe_string_2(outcomeObj, 'outcomeId', 'id'),
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string(outcomeObj, 'market'),
            'timestamp': now,
            'datetime': self.iso8601(now),
            'high': None,
            'low': None,
            'bid': None,
            'bidVolume': None,
            'ask': None,
            'askVolume': None,
            'vwap': None,
            'open': None,
            'close': last,
            'last': last,
            'previousClose': None,
            'change': None,
            'percentage': None,
            'average': None,
            'baseVolume': None,
            'quoteVolume': None,
            'info': raw,
        }, market)

    async def fetch_tickers(self, outcomes: Strings = None, params={}) -> PredictionTickers:
        """
        fetches last trade prices for multiple outcomes, one request per distinct underlying market

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/market-data#query-last-trade-price

        :param str[] outcomes: unified outcomes — required: the venue has no all-tickers endpoint
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a dictionary of prediction [ticker structures](https://docs.ccxt.com/#/?id=ticker-structure)
        """
        if outcomes is None:
            raise ArgumentsRequired(self.id + ' fetchTickers() requires an outcomes argument — the venue has no all-tickers endpoint; pass the outcome handles to fetch(discover them via fetchEvents())')
        await self.load_outcomes(outcomes)
        responsesByMarketId = {}
        result = {}
        outcomesLength = len(outcomes)
        for i in range(0, outcomesLength):
            outcomeObj = self.outcome(outcomes[i])
            info = self.safe_dict(outcomeObj, 'info', {})
            marketId = self.safe_string(info, 'marketId')
            if marketId is None:
                continue
            response = self.safe_dict(responsesByMarketId, marketId)
            if response is None:
                request = {
                    'marketId': marketId,
                }
                response = await self.sapiPrivateGetOrderBookLastTradePrice(self.extend(request, params))
                responsesByMarketId[marketId] = response
            ticker = self.parse_prediction_ticker(response, outcomeObj)
            symbolKey = self.safe_string(ticker, 'outcome', outcomes[i])
            result[symbolKey] = ticker
        return result

    async def fetch_order_book(self, outcome: Str, limit: Int = None, params={}) -> PredictionOrderBook:
        """
        fetches the order book for a single prediction outcome token

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/market-data#query-order-book

        :param str outcome: unified outcome handle, or an outcome token id
        :param int [limit]: not used by binance fetchOrderBook
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a prediction [order book structure](https://docs.ccxt.com/#/?id=order-book-structure)
        """
        await self.load_outcome(outcome)
        outcomeObj = self.outcome(outcome)
        info = self.safe_dict(outcomeObj, 'info', {})
        request = {
            'vendor': self.safe_string(info, 'vendor', self.safe_string(self.options, 'defaultVendor')),
            'marketId': self.safe_string(info, 'marketId'),
            'tokenId': self.safe_string_2(outcomeObj, 'outcomeId', 'id'),
        }
        response = await self.sapiPrivateGetOrderBook(self.extend(request, params))
        #
        #     {
        #         "outcome": "YES",
        #         "tokenId": "112233",
        #         "timestamp": 1748131800000,
        #         "bids": [{"price": "0.51", "size": "5000.00"}],
        #         "asks": [{"price": "0.52", "size": "3000.00"}]
        #     }
        #
        timestamp = self.safe_integer(response, 'timestamp')
        orderbook = self.parse_order_book(response, self.safe_outcome_symbol(outcome, outcomeObj), timestamp, 'bids', 'asks', 'price', 'size')
        return self.safe_prediction_order_book(orderbook, outcomeObj)

    async def fetch_balance(self, params={}) -> Balances:
        """
        query for balance and get the amount of funds available for trading or funds locked in orders

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/wallet#query-payment-option-balances

        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.type]: 'CeDefi', 'FUNDING', or 'SPOT'
        :returns dict: a `balance structure <https://docs.ccxt.com/?id=balance-structure>`
        """
        type = None
        type, params = self.handle_option_and_params(params, 'fetchBalance', 'type', 'SPOT')
        response = await self.sapiPrivateGetBalancePaymentOptions(params)
        #
        # {
        #     "items": [
        #         {
        #             "accountType": "SPOT",
        #             "availableBalanceDisplay": "1000.00",
        #             "enabled": True
        #         }
        #     ]
        # }
        #
        result = {
            'info': response,
        }
        balances = self.safe_list(response, 'items', [])
        for i in range(0, len(balances)):
            balance = balances[i]
            accountType = self.safe_string(balance, 'accountType')
            if accountType == type:
                free = self.safe_string(balance, 'availableBalanceDisplay')
                account = self.account()
                account['free'] = free
                result['USDT'] = account
        return self.safe_balance(result)

    def parse_prediction_order(self, order: dict, outcomeObj: Market = None) -> PredictionOrder:
        """
 @ignore
        parses a raw binance prediction order object into a unified order object
        :param dict order: the raw order object
        :param dict [outcomeObj]: the ourtome the order belongs to
        :returns dict: a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        #
        # {
        #     "orderId": "54124",
        #     "vendorOrderId": "0x1234abcd...",
        #     "vendor": "PREDICT_FUN",
        #     "marketTopicId": 4229564,
        #     "slug": "btc-price-1h-up-or-down",
        #     "marketTopicTitle": "BTC Price 1h Up or Down?",
        #     "marketId": 5567895,
        #     "marketTitle": "UP",
        #     "outcome": "YES",
        #     "outcomeIndex": 0,
        #     "status": "OPENING",
        #     "side": "BUY",
        #     "orderType": "LIMIT",
        #     "createTime": 1748131500000,
        #     "modifyTime": 1748131500000,
        #     "makerUsdtAmount": "1.00",
        #     "makerShareQty": "2000.00",
        #     "filledUsdtAmount": "0.00",
        #     "filledShareQty": "0.00",
        #     "fillPercentage": "0.00",
        #     "price": "0.50",
        #     "marketProviderFee": "0.02",
        #     "networkFee": "0.000001"
        # }
        #
        status = self.parse_order_status(self.safe_string(order, 'status'))
        if outcomeObj is None:
            marketId = self.safe_string(order, 'marketId')
            outcome = self.safe_string_upper(order, 'outcome')
            market = self.safe_market(marketId)
            outcomeName = self.safe_string(market, 'market')
            if outcomeName is None:
                outcomeName = marketId
            outcomeName += ':' + outcome
            outcomeObj = self.safe_outcome(outcomeName)
        side = self.safe_string_lower(order, 'side')
        timestamp = self.safe_integer(order, 'createTime')
        return self.safe_prediction_order({
            'id': self.safe_string(order, 'orderId'),
            'clientOrderId': None,
            'info': order,
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'lastTradeTimestamp': None,
            'status': status,
            'outcome': self.safe_string(outcomeObj, 'outcome'),
            'outcomeId': self.safe_string(outcomeObj, 'id'),
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string(outcomeObj, 'market'),
            'type': self.safe_string_lower(order, 'orderType'),
            'timeInForce': None,
            'postOnly': None,
            'reduceOnly': None,
            'side': side,
            'price': self.safe_number(order, 'price'),
            'triggerPrice': None,
            'amount': None,
            'cost': None,
            'average': None,
            'filled': None,
            'remaining': None,
            'fee': None,
            'trades': [],
        }, outcomeObj)

    def parse_order_status(self, status: Str) -> Str:
        statuses = {
            'OPENING': 'open',
            'FILLED': 'closed',
            # 'canceled': 'canceled',
            # 'rejected': 'rejected',
            # 'marginCanceled': 'canceled',
        }
        if status is None:
            return None
        if status.endswith('Rejected'):
            return 'rejected'
        if status.endswith('Canceled'):
            return 'canceled'
        return self.safe_string(statuses, status, status)

    async def fetch_open_orders(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> List[PredictionOrder]:
        """
        fetches currently open orders for the user

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/trade#query-active-orders

        :param str [outcome]: filter by outcome
        :param int [since]: only return orders updated since self timestamp in ms
        :param int [limit]: max number of orders to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.tradeSide]: Filter by trade side. Enum: BUY, SELL
        :param str [params.l1Category]: Filter by level-1 category
        :param boolean [params.paginate]: *spot only* default False, when True will automatically paginate by calling self endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
        :returns dict[]: a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        paginate = False
        paginate, params = self.handle_option_and_params(params, 'fetchOpenOrders', 'paginate')
        maxEntriesPerRequest = None
        maxEntriesPerRequest, params = self.handle_option_and_params(params, 'fetchOpenOrders', 'maxEntriesPerRequest', 100)
        pageKey = 'ccxtPageKey'
        if paginate:
            return await self.fetch_paginated_call_incremental('fetchOpenOrders', outcome, since, limit, params, pageKey, maxEntriesPerRequest)
        page = self.safe_integer(params, pageKey, 1) - 1
        request = {}
        offSet = self.safe_integer(params, 'offset', page * maxEntriesPerRequest)
        if offSet > 0:
            request['offset'] = offSet
        outcomeObj = None
        if outcome is not None:
            await self.load_outcome(outcome)
            outcomeObj = self.outcome(outcome)
            market = self.market(outcomeObj['market'])
            request['marketId'] = market['id']
        if limit is not None:
            request['limit'] = limit
        wallet = await self.fetch_wallet('fetchOpenOrders', params)
        request['walletAddress'] = wallet['walletAddress']
        response = await self.sapiPrivateGetOrderList(self.extend(request, params))
        #
        # {
        #     "total": 2,
        #     "offset": 0,
        #     "limit": 20,
        #     "orders": [
        #         {
        #             "orderId": "54124",
        #             "vendorOrderId": "0x1234abcd...",
        #             "vendor": "PREDICT_FUN",
        #             "marketTopicId": 4229564,
        #             "slug": "btc-price-1h-up-or-down",
        #             "marketTopicTitle": "BTC Price 1h Up or Down?",
        #             "marketId": 5567895,
        #             "marketTitle": "UP",
        #             "outcome": "YES",
        #             "outcomeIndex": 0,
        #             "status": "OPENING",
        #             "side": "BUY",
        #             "orderType": "LIMIT",
        #             "createTime": 1748131500000,
        #             "modifyTime": 1748131500000,
        #             "makerUsdtAmount": "1.00",
        #             "makerShareQty": "2000.00",
        #             "filledUsdtAmount": "0.00",
        #             "filledShareQty": "0.00",
        #             "fillPercentage": "0.00",
        #             "price": "0.50",
        #             "marketProviderFee": "0.02",
        #             "networkFee": "0.000001"
        #         }
        #     ]
        # }
        #
        orders = self.safe_list(response, 'orders', [])
        parsedOrders = self.parse_prediction_orders(orders, outcomeObj, since)
        return self.filter_by_outcome_since_limit(parsedOrders, outcome, since, limit)

    async def fetch_orders(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> List[PredictionOrder]:
        """
        fetches all historical orders for the user

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/trade#query-order-history

        :param str [outcome]: filter by outcome
        :param int [since]: only return orders updated since self timestamp in ms
        :param int [limit]: max number of orders to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.orderType]: Filter by order type. Enum: MARKET, LIMIT
        :param str [params.l1Category]: Filter by level-1 category
        :param str [params.status]: Filter by order status
        :param str [params.until]: end timestamp in ms
        :param boolean [params.paginate]: *spot only* default False, when True will automatically paginate by calling self endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
        :returns dict[]: a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        paginate = False
        paginate, params = self.handle_option_and_params(params, 'fetchOrders', 'paginate')
        maxEntriesPerRequest = None
        maxEntriesPerRequest, params = self.handle_option_and_params(params, 'fetchOrders', 'maxEntriesPerRequest', 100)
        pageKey = 'ccxtPageKey'
        if paginate:
            return await self.fetch_paginated_call_incremental('fetchOrders', outcome, since, limit, params, pageKey, maxEntriesPerRequest)
        page = self.safe_integer(params, pageKey, 1) - 1
        request = {}
        offSet = self.safe_integer(params, 'offset', page * maxEntriesPerRequest)
        if offSet > 0:
            request['offset'] = offSet
        outcomeObj = None
        if outcome is not None:
            await self.load_outcome(outcome)
            outcomeObj = self.outcome(outcome)
        if limit is not None:
            request['limit'] = limit
        if since is not None:
            request['startDate'] = self.yyyymmdd(since)
        until = self.safe_integer(params, 'until')
        params = self.omit(params, 'until')
        if until is not None:
            request['endDate'] = self.yyyymmdd(until)
        wallet = await self.fetch_wallet('fetchOrders', params)
        request['walletAddress'] = wallet['walletAddress']
        response = await self.sapiPrivateGetOrderHistory(self.extend(request, params))
        #
        # {
        #     "total": 15,
        #     "offset": 0,
        #     "limit": 20,
        #     "orders": [
        #         {
        #             "orderId": "54100",
        #             "vendorOrderId": "0xabcd5678...",
        #             "vendor": "PREDICT_FUN",
        #             "marketTopicId": 4229500,
        #             "slug": "btc-price-1h-up-or-down-prev",
        #             "marketTopicTitle": "BTC Price 1h Up or Down?",
        #             "marketId": 5567800,
        #             "marketTitle": "UP",
        #             "outcome": "YES",
        #             "outcomeIndex": 0,
        #             "status": "CLOSED",
        #             "side": "BUY",
        #             "orderType": "MARKET",
        #             "createTime": 1748045100000,
        #             "modifyTime": 1748045101000,
        #             "terminalTime": 1748045101000,
        #             "makerUsdtAmount": "1.00",
        #             "makerShareQty": "1923.07",
        #             "filledUsdtAmount": "1.00",
        #             "filledShareQty": "1923.07",
        #             "fillPercentage": "1.00",
        #             "price": "0.52",
        #             "marketProviderFee": "0.02",
        #             "networkFee": "0.000001"
        #         }
        #     ]
        # }
        #
        orders = self.safe_list(response, 'orders', [])
        parsedOrders = self.parse_prediction_orders(orders, outcomeObj, since)
        return self.filter_by_outcome_since_limit(parsedOrders, outcome, since, limit)

    async def fetch_positions(self, outcomes: Strings = None, params={}) -> List[PredictionPosition]:
        """
        fetches the user's outcome positions; outcome positions are spot token balances under the "+<encoding>" coin form(size and entry notional), the value/entry/mark price/pnl are computed from the current mid prices

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/position#query-positions

        :param str[] [outcomes]: filter by outcome ids or outcomes
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.tab]: Position status tab. Values from PositionQueryType. Default ONGOING
        :returns dict[]: a list of [prediction position structures](https://docs.ccxt.com/#/?id=prediction-position-structure)
        """
        await self.load_outcomes()
        requestedOutcomeSymbols = {}
        if outcomes is not None:
            for i in range(0, len(outcomes)):
                requested = outcomes[i]
                requestedOutcomeObj = self.safe_outcome(requested)
                requestedOutcome = self.safe_string(requestedOutcomeObj, 'outcome', requested)
                requestedOutcomeSymbols[requestedOutcome] = True
        wallet = await self.fetch_wallet('fetchPositions', params)
        request = {
            'walletAddress': wallet['walletAddress'],
        }
        response = await self.sapiPrivateGetPositionList(self.extend(request, params))
        #
        # {
        #     "summary": {
        #         "totalValue": "1523.45",
        #         "positionValue": "523.45",
        #         "walletBalance": "1000.00",
        #         "totalClaimableAmount": "50.00",
        #         "todayRealizedPnl": "15.30",
        #         "todayRealizedPnlPercent": "3.10",
        #         "todayTotalCost": "493.55"
        #     },
        #     "counts": {
        #         "ongoingCount": 3,
        #         "endedCount": 12,
        #         "pendingClaimCount": 1
        #     },
        #     "positions": [
        #         {
        #             "positionId": 1001,
        #             "vendor": "PREDICT_FUN",
        #             "chainId": "56",
        #             "tokenId": "112233",
        #             "collateralSymbol": "USDT",
        #             "topicType": "FLAT",
        #             "marketTopicId": 4229564,
        #             "marketId": 5567895,
        #             "marketTopicTitle": "BTC Price 1h Up or Down?",
        #             "marketTitle": "UP",
        #             "outcomeName": "YES",
        #             "outcomeIndex": 0,
        #             "shares": "1923.07",
        #             "avgPrice": "0.52",
        #             "totalCost": "1.00",
        #             "value": "1.06",
        #             "currentPrice": "0.55",
        #             "toWin": "1923.07",
        #             "positionStatus": "OPEN",
        #             "canClaim": False,
        #             "endDate": 1748134800000,
        #             "unrealizedPnl": "0.06",
        #             "unrealizedPnlPercent": "6.00",
        #             "realizedPnl": "0.00",
        #             "pnl": "0.06",
        #             "createdTime": 1748131500000,
        #             "updatedTime": 1748132000000
        #         }
        #     ]
        # }
        #
        data = self.safe_list(response, 'positions', [])
        positions = self.parse_prediction_positions(data)
        if outcomes is None:
            return positions
        filtered = []
        positionsLength = len(positions)
        for i in range(0, positionsLength):
            position = positions[i]
            positionOutcome = self.safe_string(position, 'outcome')
            if (positionOutcome is not None) and (positionOutcome in requestedOutcomeSymbols):
                filtered.append(position)
        return filtered

    async def fetch_position(self, outcome: str, params={}) -> PredictionPosition:
        """
        fetch data on an open position

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/position#query-positions-by-filter

        :param str [outcome]: filter by outcome
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction position structures](https://docs.ccxt.com/#/?id=prediction-position-structure)
        """
        request = {}
        outcomeObj = None
        if outcome is not None:
            await self.load_outcome(outcome)
            outcomeObj = self.outcome(outcome)
            market = self.market(outcomeObj['market'])
            request['marketTopicId'] = market['info']['marketTopicId']
        wallet = await self.fetch_wallet('fetchOrders', params)
        request['walletAddress'] = wallet['walletAddress']
        response = await self.sapiPrivateGetPositionFilter(self.extend(request, params))
        #
        #
        positions = self.safe_list(response, 'positions', [])
        parsedPositions = self.parse_prediction_positions(positions)
        filteredPositions = self.filter_by_outcome_since_limit(parsedPositions, outcome, None, None)
        return self.safe_dict(filteredPositions, 0)

    def parse_prediction_position(self, position: dict, outcomeObj: Market = None) -> PredictionPosition:
        """
 @ignore
        parses a spot balance entry for an outcome token into a unified position object
        :param dict position: the raw balance entry
        :param dict [outcomeObj]: the ourtome the position belongs to
        :returns dict: a [prediction position structure](https://docs.ccxt.com/#/?id=prediction-position-structure)
        """
        if outcomeObj is None:
            marketId = self.safe_string(position, 'marketId')
            outcome = self.safe_string_upper(position, 'outcomeName')
            market = self.safe_market(marketId)
            outcomeName = self.safe_string(market, 'market')
            if outcomeName is None:
                outcomeName = marketId
            outcomeName += ':' + outcome
            outcomeObj = self.safe_outcome(outcomeName)
        timestamp = self.safe_integer(position, 'createdTime')
        totalCost = self.parse_number(self.safe_string(position, 'totalCost'))
        return self.safe_prediction_position({
            'id': self.safe_integer(position, 'positionId'),
            'outcome': self.safe_string(outcomeObj, 'outcome'),
            'outcomeId': self.safe_string_2(outcomeObj, 'outcomeId', 'id'),
            'market': self.safe_string(outcomeObj, 'market'),
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'isolated': False,
            'hedged': None,
            'side': 'long',
            'contracts': None,
            'contractSize': self.parse_number(self.safe_string(position, 'shares')),
            'entryPrice': self.parse_number(self.safe_string(position, 'avgPrice')),
            'markPrice': None,
            'notional': self.parse_number(self.safe_string(position, 'value')),
            'leverage': None,
            'collateral': totalCost,
            'initialMargin': totalCost,
            'maintenanceMargin': None,
            'initialMarginPercentage': None,
            'maintenanceMarginPercentage': None,
            'unrealizedPnl': self.parse_number(self.safe_string(position, 'unrealizedPnl')),
            'realizedPnl': self.parse_number(self.safe_string(position, 'realizedPnl')),
            'liquidationPrice': None,
            'marginRatio': None,
            'marginMode': 'cross',
            'percentage': self.parse_number(self.safe_string(position, 'unrealizedPnlPercent')),
            'info': position,
        })

    async def fetch_my_trades(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> List[PredictionTrade]:
        """
        fetch all trades made by the user

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/trade#query-order-history

        :param str [outcome]: filter by outcome
        :param int [since]: only return orders updated since self timestamp in ms
        :param int [limit]: max number of orders to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.orderType]: Filter by order type. Enum: MARKET, LIMIT
        :param str [params.l1Category]: Filter by level-1 category
        :param str [params.status]: Filter by order status
        :param str [params.until]: end timestamp in ms
        :param boolean [params.paginate]: *spot only* default False, when True will automatically paginate by calling self endpoint multiple times. See in the docs all the [available parameters](https://github.com/ccxt/ccxt/wiki/Manual#pagination-params)
        :returns dict[]: a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        paginate = False
        paginate, params = self.handle_option_and_params(params, 'fetchMyTrades', 'paginate')
        maxEntriesPerRequest = None
        maxEntriesPerRequest, params = self.handle_option_and_params(params, 'fetchMyTrades', 'maxEntriesPerRequest', 100)
        pageKey = 'ccxtPageKey'
        if paginate:
            return await self.fetch_paginated_call_incremental('fetchMyTrades', outcome, since, limit, params, pageKey, maxEntriesPerRequest)
        page = self.safe_integer(params, pageKey, 1) - 1
        request = {
            'status': 'FILLED',
        }
        offSet = self.safe_integer(params, 'offset', page * maxEntriesPerRequest)
        if offSet > 0:
            request['offset'] = offSet
        outcomeObj = None
        if outcome is not None:
            await self.load_outcome(outcome)
            outcomeObj = self.outcome(outcome)
        if limit is not None:
            request['limit'] = limit
        if since is not None:
            request['startDate'] = self.yyyymmdd(since)
        until = self.safe_integer(params, 'until')
        params = self.omit(params, 'until')
        if until is not None:
            request['endDate'] = self.yyyymmdd(until)
        wallet = await self.fetch_wallet('fetchMyTrades', params)
        request['walletAddress'] = wallet['walletAddress']
        response = await self.sapiPrivateGetOrderHistory(self.extend(request, params))
        #
        # {
        #     "total": 15,
        #     "offset": 0,
        #     "limit": 20,
        #     "orders": [
        #         {
        #             "orderId": "54100",
        #             "vendorOrderId": "0xabcd5678...",
        #             "vendor": "PREDICT_FUN",
        #             "marketTopicId": 4229500,
        #             "slug": "btc-price-1h-up-or-down-prev",
        #             "marketTopicTitle": "BTC Price 1h Up or Down?",
        #             "marketId": 5567800,
        #             "marketTitle": "UP",
        #             "outcome": "YES",
        #             "outcomeIndex": 0,
        #             "status": "CLOSED",
        #             "side": "BUY",
        #             "orderType": "MARKET",
        #             "createTime": 1748045100000,
        #             "modifyTime": 1748045101000,
        #             "terminalTime": 1748045101000,
        #             "makerUsdtAmount": "1.00",
        #             "makerShareQty": "1923.07",
        #             "filledUsdtAmount": "1.00",
        #             "filledShareQty": "1923.07",
        #             "fillPercentage": "1.00",
        #             "price": "0.52",
        #             "marketProviderFee": "0.02",
        #             "networkFee": "0.000001"
        #         }
        #     ]
        # }
        #
        trades = self.safe_list(response, 'orders', [])
        parsedTrades = self.parse_prediction_trades(trades, outcomeObj)
        return self.filter_by_outcome_since_limit(parsedTrades, outcome, since, limit)

    def parse_prediction_trade(self, trade: dict, outcomeObj: Market = None) -> PredictionTrade:
        """
 @ignore
        parses a single binance fill into a unified trade object
        :param dict trade: the raw fill object
        :param dict [outcomeObj]: the outcome the trade belongs to
        :returns dict: a [prediction trade structure](https://docs.ccxt.com/#/?id=prediction-trade-structure)
        """
        #
        # {
        #     "orderId": "54124",
        #     "vendorOrderId": "0x1234abcd...",
        #     "vendor": "PREDICT_FUN",
        #     "marketTopicId": 4229564,
        #     "slug": "btc-price-1h-up-or-down",
        #     "marketTopicTitle": "BTC Price 1h Up or Down?",
        #     "marketId": 5567895,
        #     "marketTitle": "UP",
        #     "outcome": "YES",
        #     "outcomeIndex": 0,
        #     "status": "OPENING",
        #     "side": "BUY",
        #     "orderType": "LIMIT",
        #     "createTime": 1748131500000,
        #     "modifyTime": 1748131500000,
        #     "makerUsdtAmount": "1.00",
        #     "makerShareQty": "2000.00",
        #     "filledUsdtAmount": "0.00",
        #     "filledShareQty": "0.00",
        #     "fillPercentage": "0.00",
        #     "price": "0.50",
        #     "marketProviderFee": "0.02",
        #     "networkFee": "0.000001"
        # }
        #
        if outcomeObj is None:
            marketId = self.safe_string(trade, 'marketId')
            outcome = self.safe_string_upper(trade, 'outcome')
            market = self.safe_market(marketId)
            outcomeName = self.safe_string(market, 'market')
            if outcomeName is None:
                outcomeName = marketId
            outcomeName += ':' + outcome
            outcomeObj = self.safe_outcome(outcomeName)
        timestamp = self.safe_integer(trade, 'createTime')
        filled = self.safe_string(trade, 'filledShareQty')
        cost = self.safe_string(trade, 'filledUsdtAmount')
        price = self.safe_string(trade, 'price')
        orderType = self.safe_string_lower(trade, 'orderType')
        fee = None
        if (orderType == 'market') and (cost is not None) and (price is not None) and (filled is not None):
            # buys pay cost above price*filled, sells receive proceeds net of the fee —
            # either way the fee is the absolute difference
            feeCost = Precise.string_abs(Precise.string_sub(cost, Precise.string_mul(price, filled)))
            fee = {
                'currency': 'USDT',
                'cost': self.parse_number(feeCost),
            }
        return self.safe_prediction_trade({
            'id': None,
            'info': trade,
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'lastTradeTimestamp': self.safe_integer(trade, 'modifyTime'),
            'outcome': self.safe_string(outcomeObj, 'outcome'),
            'outcomeId': self.safe_string(outcomeObj, 'id'),
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string(outcomeObj, 'market'),
            'order': self.safe_string(trade, 'orderId'),
            'type': orderType,
            'side': self.safe_string_lower(trade, 'side'),
            'takerOrMaker': None,
            'price': price,
            'amount': self.safe_string(trade, 'makerShareQty'),
            'filled': filled,
            'cost': cost,
            'fee': fee,
        }, outcomeObj)

    async def fetch_wallet(self, methodName: str, params={}) -> Any:
        """
        fetch wallet for user and save the one match the walletAddress user provided

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/wallet#list-prediction-wallets

        :param str [methodName]: method name
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a wallet
        """
        cachedWallet = self.safe_dict(self.options, 'wallet')
        if cachedWallet is not None:
            return cachedWallet
        walletAddress = None
        walletAddress, params = self.handle_option_and_params(params, methodName, 'walletAddress', self.walletAddress)
        response = await self.sapiPrivateGetWalletList()
        #
        # {
        #     "wallets": [
        #         {
        #             "walletAddress": "0x12e32db8817e292508c34111cbc4b23340df542c",
        #             "walletId": "5b5c1ec3be4e4416a5872b21c1ca5d20",
        #             "registeredTime": 1748000000000
        #         }
        #     ]
        # }
        #
        wallets = self.safe_list(response, 'wallets', [])
        if walletAddress is None:
            cachedWallet = self.safe_dict(wallets, 0)
            self.options['wallet'] = cachedWallet
            return cachedWallet
        walletLength = len(wallets)
        for i in range(0, walletLength):
            w = self.safe_string(wallets[i], 'walletAddress', '')
            if w == walletAddress:
                cachedWallet = wallets[i]
                break
        if cachedWallet is None:
            raise NotSupported(self.id + 'fetchWallet could\'n find wallet ' + walletAddress)
        self.options['wallet'] = cachedWallet
        return cachedWallet

    async def fetch_quote(self, request: dict, params={}) -> Any:
        """
        request for quote from binance server

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/trade#get-quote

        :param dict [request]: request to the exchange API endpoint
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.chainId]: Chain ID. Default 56(BSC)
        :param integer [params.feeRateBps]: Fee rate in basis points. Default 200, range 1–10000
        :param str [params.fundingSource]: Funding source. Enum: MPC, CEX. Default MPC
        :param str [params.fundTransferAmount]: Auto-transfer amount before order(wei). Must be > 0 if provided
        :returns dict: a quote
        """
        response = await self.sapiPrivatePostTradeGetQuote(self.extend(request, params))
        #
        # {
        #     "quoteId": "q_20260525_abc123xyz",
        #     "tokenId": "112233",
        #     "chance": "0.52",
        #     "vendor": "PREDICT_FUN",
        #     "marketTitle": "UP",
        #     "marketExtId": "ext_001",
        #     "side": "BUY",
        #     "amountIn": "1000000000000000000",
        #     "amountOut": "1923070000000000000",
        #     "isMinAmountOut": False,
        #     "feeAmount": "20000000000000000",
        #     "feeDiscountBps": "0",
        #     "averagePrice": 0.52,
        #     "lastPrice": 0.52,
        #     "priceImpact": 0.001,
        #     "timestamp": 1748131500000,
        #     "chainId": "56",
        #     "userId": 100103755893,
        #     "walletAddress": "0x12e32db8817e292508c34111cbc4b23340df542c",
        #     "orderType": "MARKET",
        #     "slippageBps": 1200,
        #     "feeRateBps": 200,
        #     "minReceive": "1900000000000000000",
        #     "expireAt": 1748131800000,
        #     "priceLimit": null
        # }
        #
        return response

    def price_to_precision(self, outcome: Str, price: Any) -> str:
        market = self.market(outcome)
        prec = self.safe_number(self.safe_dict(market, 'precision', {}), 'price', 0.0001)
        decimals = 4
        if (prec is not None) and (prec > 0):
            decimals = self.precision_from_string(self.number_to_string(prec))
        return self.decimal_to_precision(price, ROUND, decimals, DECIMAL_PLACES, self.paddingMode)

    def amount_to_precision(self, outcome: Str, amount: Any) -> str:
        market = self.market(outcome)
        prec = self.safe_number(self.safe_dict(market, 'precision', {}), 'amount', 0.01)
        decimals = 2
        if (prec is not None) and (prec > 0):
            decimals = self.precision_from_string(self.number_to_string(prec))
        # amounts truncate so a rounded-up value can never exceed the caller's balance
        return self.decimal_to_precision(amount, TRUNCATE, decimals, DECIMAL_PLACES, self.paddingMode)

    async def create_order(self, outcome: str, type: str, side: str, amount: float, price: Num = None, params={}) -> PredictionOrder:
        """
        creates a limit or market order for an outcome market

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/trade#place-order

        :param str outcome: unified outcome
        :param str type: 'limit' or 'market'
        :param str side: 'buy' or 'sell'
        :param float amount: quantity of outcome tokens
        :param float [price]: limit price(0–1 range for prediction markets)
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.timeInForce]: Must match orderType: FOK for MARKET, GTC for LIMIT
        :param str [params.slippage]: slippage for market orders(default 5%)
        :param str [params.fundingSource]: Funding source. Enum: MPC, CEX. Default MPC
        :param str [params.fundTransferAmount]: Auto-transfer amount before order(wei). Must be > 0 if provided
        :param str [params.accountType]: Payment account type. Enum: SPOT, FUNDING
        :param str [params.feeRateBps]: Payment account type. Enum: SPOT, FUNDING
        :param str [params.cost]: Buy prediction market with USDT cost, only for buy side
        :returns dict: a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        await self.load_outcome(outcome)
        outcomeObj = self.outcome(outcome)
        # markets are keyed by the parent market outcome; the outcome handle("MARKET:LABEL")
        # is not a market id, so resolve the market and price/amount precision via outcomeObj['market']
        marketSymbol = self.safe_string(outcomeObj, 'market')
        market = self.market(marketSymbol)
        typeUpper = type.upper()
        sideUpper = side.upper()
        wallet = await self.fetch_wallet('createOrder', params)
        defaultSlippage = self.safe_string(self.options, 'defaultSlippage', '0.05')
        slippage = self.safe_string(params, 'slippage', defaultSlippage)
        cost = self.safe_string(params, 'cost')
        slippageBps = self.parse_to_int(Precise.string_mul(slippage, '10000'))
        commonRequest = {
            'walletAddress': wallet['walletAddress'],
            'orderType': typeUpper,
            'slippageBps': slippageBps,
        }
        amountStr = self.number_to_string(amount)
        priceStr = self.number_to_string(price)
        defaultTif = 'FOK'
        if typeUpper == 'LIMIT':
            if price is None:
                raise ArgumentsRequired(self.id + 'createOrder requires price for limit order')
            commonRequest['priceLimit'] = self.price_to_precision(marketSymbol, price)
            defaultTif = 'GTC'
        if sideUpper == 'BUY':
            if cost is not None:
                amountStr = cost
            else:
                # the amountIn represents USDT for buy order
                feeRateBps = self.safe_string(params, 'feeRateBps', '200')
                if typeUpper == 'LIMIT':
                    feeRateBps = '0'
                else:
                    if price is None:
                        raise ArgumentsRequired(self.id + ' createOrder requires price for ' + side + ' order')
                feeRate = Precise.string_div(feeRateBps, '10000')
                minPrice = Precise.string_min(priceStr, Precise.string_sub('1', priceStr))
                fee = Precise.string_mul(Precise.string_mul(feeRate, minPrice), amountStr)
                amountStr = Precise.string_add(Precise.string_mul(amountStr, priceStr), fee)
        timeInForce = self.safe_string_upper(params, 'timeInForce', defaultTif)
        accountType = self.safe_string(params, 'accountType')
        if accountType is None:
            raise ArgumentsRequired(self.id + ' createOrder requires accountType(SPOT, FUNDING)')
        params = self.omit(params, ['timeInForce', 'accountType', 'cost'])
        quoteRequest = self.extend(commonRequest, {
            'tokenId': outcomeObj['id'],
            'side': sideUpper,
            'amountIn': Precise.string_mul(self.amount_to_precision(marketSymbol, amountStr), '1000000000000000000'),
        })
        quote = await self.fetch_quote(quoteRequest, params)
        quoteId = self.safe_string(quote, 'quoteId')
        orderRequest = self.extend(commonRequest, {
            'walletId': wallet['walletId'],
            'quoteId': quoteId,
            'timeInForce': timeInForce,
            'accountType': accountType,
        })
        response = await self.sapiPrivatePostTradePlaceOrderBundle(self.extend(orderRequest, params))
        return self.safe_prediction_order({
            'id': self.safe_string(response, 'orderId'),
            'clientOrderId': None,
            'info': response,
            'timestamp': None,
            'datetime': None,
            'status': None,
            'outcome': self.safe_string(outcomeObj, 'outcome', outcome),
            'outcomeId': self.safe_string(outcomeObj, 'id'),
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string(outcomeObj, 'market'),
            'type': type,
            'side': side,
            'price': price,
            'amount': amount,
            'filled': None,
            'remaining': None,
            'cost': None,
            'fee': None,
            'trades': [],
        }, market)

    async def create_market_order_with_cost(self, symbol: str, side: str, cost: float, params={}):
        """
        create a market order by providing the symbol, side and cost

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/trade#place-order

        :param str symbol: unified symbol of the market to create an order in
        :param str side: 'buy' or 'sell'
        :param float cost: how much you want to trade in units of the quote currency
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: an `order structure <https://docs.ccxt.com/?id=order-structure>`
        """
        req = {
            'cost': cost,
        }
        return await self.create_order(symbol, 'market', side, cost, None, self.extend(req, params))

    async def cancel_order(self, id: str, outcome: Str = None, params={}) -> PredictionOrder:
        """
        cancels a single open order

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/trade#batch-cancel-orders

        :param str id: order id
        :param str [outcome]: unified outcome
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        orders = await self.cancel_orders([id], outcome, params)
        return self.safe_dict(orders, 0, {})

    async def cancel_orders(self, ids: List[str], outcome: Str = None, params={}) -> List[PredictionOrder]:
        """
        cancels multiple open orders

        https://developers.binance.com/en/docs/catalog/web3-wallet-prediction-trading/api/rest-api/trade#batch-cancel-orders

        :param str[] ids: order ids
        :param str [outcome]: unified outcome(required)
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        outcomeObj = None
        if outcome is not None:
            await self.load_outcome(outcome)
            outcomeObj = self.outcome(outcome)
        wallet = await self.fetch_wallet('cancelOrders', params)
        request = {
            'walletAddress': wallet['walletAddress'],
            'walletId': wallet['walletId'],
        }
        # flatten cancelInfoList to dot list, eg. cancelInfoList[o].orderId=1234
        for i in range(0, len(ids)):
            key = 'cancelInfoList[' + self.number_to_string(i) + '].orderId'
            request[key] = ids[i]
        response = await self.sapiPrivatePostTradeBatchCancel(self.extend(request, params))
        #
        # {
        #     "canceled": [
        #         "54124"
        #     ],
        #     "failed": [
        #         {
        #             "orderId": "54126",
        #             "reason": "ORDER_NOT_FOUND"
        #         }
        #     ]
        # }
        #
        canceledOrders = self.safe_list(response, 'canceled', [])
        outcomeSymbol = self.safe_string(outcomeObj, 'outcome', outcome)
        failedOrders = self.safe_list(response, 'failed', [])
        failedOrdersLength = len(failedOrders)
        for i in range(0, failedOrdersLength):
            failedOrder = failedOrders[i]
            error = self.safe_string(failedOrder, 'reason')
            raise OrderNotFound(self.id + ' cancelOrders() failed for ' + self.safe_string(failedOrder, 'orderId') + ': ' + error)
        orders = []
        canceledOrdersLength = len(canceledOrders)
        for i in range(0, canceledOrdersLength):
            status = canceledOrders[i]
            order = {
                'id': status,
                'clientOrderId': None,
                'info': status,
                'status': 'canceled',
                'outcome': outcomeSymbol,
                'outcomeId': self.safe_string(outcomeObj, 'id'),
                'label': self.safe_string(outcomeObj, 'label'),
                'market': self.safe_string(outcomeObj, 'market'),
                'timestamp': self.milliseconds(),
                'datetime': self.iso8601(self.milliseconds()),
            }
            orders.append(self.safe_prediction_order(order))
        return orders

    def handle_errors(self, code: int, reason: str, url: str, method: str, headers: dict, body: str, response: Any, requestHeaders: Any, requestBody: Any):
        if response is None:
            return None
        errorCode = self.safe_string(response, 'code')
        if (errorCode is not None) and Precise.string_lt(errorCode, '0'):
            message = self.safe_string(response, 'msg', '')
            feedback = self.id + ' ' + body
            self.throw_exactly_matched_exception(self.exceptions['exact'], errorCode, feedback)
            self.throw_broadly_matched_exception(self.exceptions['broad'], message, feedback)
            raise ExchangeError(feedback)
        return None

    def sign(self, path: Any, api: Any = 'sapi', method='GET', params={}, headers: Any = None, body: Any = None):
        """
 @ignore
        builds the request URL and attaches the standard binance SAPI HMAC-SHA256 signature — every prediction endpoint is signed
        :param str path: the endpoint path
        :param string|str[] [api]: the api group and access level
        :param str [method]: HTTP method
        :param dict [params]: request parameters
        :param dict [headers]: request headers
        :param dict [body]: request body
        :returns dict: a dictionary with url, method, body and headers
        """
        apiGroup = api if isinstance(api, str) else api[0]
        baseUrls = self.urls['api']
        baseUrl = self.safe_string(baseUrls, apiGroup, baseUrls['sapi'])
        url = baseUrl + '/' + self.implode_params(path, params)
        query = self.omit(params, self.extract_params(path))
        self.check_required_credentials()
        extendedParams = self.extend({
            'timestamp': self.nonce(),
        }, query)
        defaultRecvWindow = self.safe_integer(self.options, 'recvWindow')
        if defaultRecvWindow is not None:
            extendedParams['recvWindow'] = defaultRecvWindow
        querystring = self.urlencode_nested(extendedParams)
        querystring = querystring.replace('%5B', '[')
        querystring = querystring.replace('%5D', ']')
        signature = self.hmac(self.encode(querystring), self.encode(self.secret), hashlib.sha256)
        querystring = querystring + '&signature=' + signature
        headers = {
            'X-MBX-APIKEY': self.apiKey,
        }
        if (method == 'GET') or (method == 'DELETE'):
            url = url + '?' + querystring
        else:
            body = querystring
            headers['Content-Type'] = 'application/x-www-form-urlencoded'
        return {'url': url, 'method': method, 'body': body, 'headers': headers}
