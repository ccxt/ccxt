# -*- coding: utf-8 -*-

# PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
# https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

from ccxt.async_support.base.prediction_exchange import PredictionExchange
from ccxt.abstract.prediction.sxbet import ImplicitAPI
import json
from ccxt.async_support.base.ws.cache import ArrayCache, ArrayCacheByOutcomeById
from ccxt.base.types import Any, Balances, Int, Market, Num, Str, Strings, PredictionEvent, fetchEventsParams, PredictionTicker, PredictionTickers, PredictionOrder, PredictionOrderBook, PredictionTrade, PredictionPosition, PredictionSettlement
from ccxt.async_support.base.ws.client import Client
from ccxt.base.errors import ExchangeError
from ccxt.base.errors import AuthenticationError
from ccxt.base.errors import PermissionDenied
from ccxt.base.errors import AccountNotEnabled
from ccxt.base.errors import ArgumentsRequired
from ccxt.base.errors import BadRequest
from ccxt.base.errors import BadSymbol
from ccxt.base.errors import MarketClosed
from ccxt.base.errors import InsufficientFunds
from ccxt.base.errors import InvalidOrder
from ccxt.base.errors import OrderNotFound
from ccxt.base.errors import OrderNotFillable
from ccxt.base.errors import DuplicateOrderId
from ccxt.base.errors import NotSupported
from ccxt.base.decimal_to_precision import ROUND
from ccxt.base.decimal_to_precision import DECIMAL_PLACES
from ccxt.base.decimal_to_precision import TICK_SIZE
from ccxt.base.precise import Precise


class sxbet(PredictionExchange, ImplicitAPI):

    def describe(self) -> object:
        return self.deep_extend(super(sxbet, self).describe(), {
            'id': 'sxbet',
            'name': 'SX Bet',
            'countries': [],
            'rateLimit': 120,  # 500 req/min baseline; /orders is 5500/min, /trades is 200/min
            'certified': False,
            'pro': True,
            'has': {
                'CORS': None,
                'spot': False,
                'margin': False,
                'swap': False,
                'future': False,
                'option': False,
                'approve': True,
                'cancelAllOrders': True,
                'cancelOrder': True,
                'cancelOrders': True,
                'createOrder': True,
                'fetchBalance': True,
                'fetchClosedOrders': False,
                'fetchEvent': True,
                'fetchEvents': True,
                'fetchMarkets': True,
                'fetchMyTrades': True,
                'fetchOHLCV': False,
                'fetchOpenOrders': True,
                'fetchOrder': True,
                'fetchOrderBook': True,
                'fetchOrders': True,
                'fetchPositions': True,
                'fetchSettlements': True,
                'fetchTicker': True,
                'fetchTickers': True,
                'fetchTrades': True,
                'prediction': True,
                'watchMyTrades': True,
                'watchOrderBook': True,
                'watchOrders': True,
                'watchTicker': True,
                'watchTrades': True,
            },
            'urls': {
                'logo': 'https://github.com/user-attachments/assets/6d1d1e37-2b9c-4011-93c6-c9c25cd2c2f7',
                'api': {
                    'sxbet': 'https://api.sx.bet',
                    'ws': 'wss://realtime.sx.bet/connection/websocket',
                },
                'test': {
                    'sxbet': 'https://api.toronto.sx.bet',
                    'ws': 'wss://realtime.toronto.sx.bet/connection/websocket',
                },
                'www': 'https://sx.bet',
                'doc': ['https://docs.sx.bet'],
            },
            'api': {
                'sxbet': {
                    'public': {
                        'get': {
                            'metadata/obv3': 1,
                            'orderbook-v3/snapshot': 1,
                            'trades-v3/public': 1,
                            'markets/active': 1,
                            'markets/find': 1,
                            'markets/popular': 1,
                            'trades/consolidated': 1,
                            'trades/orders': 1,
                            'trades/portfolio/refunds': 1,
                            'fixture/active': 1,
                            'fixture/status': 1,
                            'sports': 1,
                            'leagues': 1,
                            'leagues/active': 1,
                            'teams': 1,
                            'live-scores': 1,
                        },
                    },
                    'private': {
                        'get': {
                            'user/realtime-token-v3/api-key': 1,
                            'user/proxy': 1,
                            'user/balance-v3': 1,
                            'user/transfer-to-proxy/pending': 1,
                            'user/transfer-to-proxy/status': 1,
                            'orders-v3': 1,
                            'orders-v3/{orderId}': 1,
                            'orders-v3/odds/best': 1,
                            'trades-v3': 1,
                            'fills-v3': 1,
                            'positions-v3': 1,
                        },
                        'delete': {
                            'orders-v3': 1,
                            'orders-v3/event': 1,
                            'orders-v3/all': 1,
                        },
                        'post': {
                            'orders-v3': 1,
                            'user/deploy-proxy': 1,
                            'user/transfer-to-proxy': 1,
                            'heartbeat/v3': 1,
                        },
                    },
                },
            },
            'requiredCredentials': {
                # apiKey is the x-sx-api-key header - every private v3 route authenticates with it;
                # trading additionally needs walletAddress (order 'maker' field) and privateKey
                'apiKey': True,
                'secret': False,
                'walletAddress': True,
                'privateKey': True,
            },
            'fees': {
                'trading': {
                    'tierBased': False,
                    'percentage': True,
                    # per-account fee rates live behind GET /user/fees-v3 and default to zero;
                    # there is no public venue-wide maker/taker schedule to mirror here
                    'maker': 0.0,
                    'taker': 0.0,
                },
            },
            'exceptions': {
                # sx.bet errors come as { "error": "Bad Request", "message": <CODE or [strings]>, "statusCode": 4xx };
                # per-order statuses inside a 200 createOrder response reuse the same codes
                'exact': {
                    'INSUFFICIENT_BALANCE': InsufficientFunds,   # maker token balance too low
                    'INVALID_MARKET': BadRequest,   # non-unique marketHashes in one request
                    'ORDERS_ALREADY_EXIST': DuplicateOrderId,
                    'TOO_MANY_DIFFERENT_MARKETS': BadRequest,   # more than 3 markets in a single request
                    'ORDERS_MUST_HAVE_IDENTICAL_MARKET': BadRequest,
                    'BAD_BASE_TOKEN': BadRequest,   # all orders must share one base token
                    'INSUFFICIENT_KYC': PermissionDenied,
                    'PROXY_NOT_DEPLOYED': AccountNotEnabled,   # live-verified v3: the account's obv3 proxy wallet is not deployed yet - call approve() (deploys and funds it)
                    'INVALID_USER': AuthenticationError,   # live-verified v3: the x-sx-api-key does not belong to a registered user on the target network
                    'BAD_AUTH': AuthenticationError,   # live-verified v3: missing or malformed x-sx-api-key
                    'ERC20_PERMIT_BAD_SPENDER': BadRequest,   # live-verified v3: transfer-to-proxy permit signed for the wrong executor
                    'BASE_TOKENS_NOT_SAME': BadRequest,
                    'MARKETS_NOT_SAME': BadRequest,
                    'DIRECTIONS_NOT_SAME': BadRequest,
                    'INVALID_ORDERS': OrderNotFillable,   # a targeted order is no longer active
                    'INVALID_ODDS': InvalidOrder,   # desiredOdds must be below 10^20
                    'PERCENTAGEODDS_UNDEFINED_OR_MALFORMED': InvalidOrder,   # live-verified: maker percentageOdds above the 10^20 cap (price > 1) or malformed
                    'TOTAL_BET_SIZE_TOO_LOW': InvalidOrder,   # live-verified: stake below the venue's minimum bet size
                    'MATCH_STATE_INVALID': MarketClosed,   # fixture not available for betting
                    'PROXY_ACCOUNT_INVALID': BadRequest,
                    'RATE_LIMIT_ORDER_REQUEST_MARKET_COUNT': BadRequest,   # more than 1000 marketHashes queried
                    'BOTH_SPORTXEVENTID_MARKETHASHES_PRESENT': BadRequest,
                    'BAD_MARKET_HASHES': BadRequest,   # invalid or more than 30 hashes on /markets/find
                    'Order not found': OrderNotFound,   # live-verified v3: GET /orders-v3/{orderId} 404s for a missing or foreign id
                },
                'broad': {
                    'no longer supported': NotSupported,   # e.g. "OrderBook V2 is no longer supported" (testnet V3 canary gate)
                    'must be': BadRequest,   # request validation messages like "orders must be an array"
                    'Insufficient available balance': InsufficientFunds,   # live-verified v3: the proxy wallet holds less than the order size
                },
            },
            'options': {
                'marketsPageSize': 100,   # markets per GET /markets/active page (cursor pagination)
                'maxMarketsPages': 50,    # safety cap on pages fetched per markets scan (100 * 50 = 5000 markets)
                # venue-specific fetchEvents scope params accepted by requireEventQuery
                'eventScopeParams': ['leagueId', 'sportId'],
                # per-chain RPC endpoint for the on-chain reads approve() needs (ERC20 name()/nonces());
                # sx.bet's own REST API has no RPC proxy, and /metadata carries no rpcUrl field
                'chains': {
                    '4162': {'rpcUrl': 'https://rpc-rollup.sx.technology'},
                    '79479957': {'rpcUrl': 'https://rpc-rollup.toronto.sx.technology'},
                },
                'approveDeadlineSeconds': 7200,
                # the venue rejects expiry 0 and past values - default resting orders to one day
                'defaultOrderExpirySeconds': 86400,
                # the venue caps GET /orders-v3/odds/best at 100 market hashes per request
                'bestOddsBatchSize': 100,
                # the venue caps DELETE /orders-v3 at maxCancelOrders (100, see /metadata/obv3) ids per request
                'cancelOrdersBatchSize': 100,
                'tradesLimit': 1000,
                'ordersLimit': 1000,
                'myTradesLimit': 1000,
            },
            'streaming': {
                # Centrifugo server pings arrive shaped like empty frames and are answered by pong();
                # no client-initiated keepAlive is needed
                'keepAlive': 30000,
            },
        })

    async def fetch_markets(self, params={}) -> list[Market]:
        """
        retrieves data on all active markets, each becomes one market with its two sides listed under the outcomes key

        https://docs.sx.bet/api-reference/get-markets-active

        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param int [params.limit]: max number of markets to collect(defaults to options.marketsPageSize * options.maxMarketsPages, 5000)
        :returns dict[]: an array of objects representing market data
        """
        rest = self.omit(params, ['limit'])
        userLimit = self.safe_integer(params, 'limit')
        rawMarkets = await self.fetch_raw_markets_paged(rest, userLimit)
        markets = []
        rawMarketsLength = len(rawMarkets)
        for i in range(0, rawMarketsLength):
            markets.append(self.parse_sxbet_market(rawMarkets[i]))
        marketsLength = len(markets)
        if (userLimit is not None) and (marketsLength > userLimit):
            return self.array_slice(markets, 0, userLimit)
        return markets

    async def fetch_raw_markets_paged(self, extra: dict = {}, userLimit: Int = None) -> list[object]:
        """
 @ignore
        pages through GET /markets/active(cursor-based via paginationKey/nextKey), stopping once options.maxMarketsPages pages or userLimit raw markets have been collected
        :param dict [extra]: extra request params merged into every page(e.g. leagueId, sportId, sportXeventId)
        :param int [userLimit]: stop collecting once self many raw markets have been gathered
        :returns dict[]: the raw(unparsed) sx.bet market objects
        """
        pageSize = self.safe_integer(self.options, 'marketsPageSize', 100)
        maxPages = self.safe_integer(self.options, 'maxMarketsPages', 50)
        rawMarkets = []
        paginationKey = None
        page = 0
        while(True):
            request = {'pageSize': pageSize}
            if paginationKey is not None:
                request['paginationKey'] = paginationKey
            response = await self.sxbetPublicGetMarketsActive(self.extend(request, extra))
            result = self.safe_dict(response, 'data', {})
            pageMarkets = self.safe_list(result, 'markets', [])
            pageMarketsLength = len(pageMarkets)
            for i in range(0, pageMarketsLength):
                rawMarkets.append(pageMarkets[i])
            paginationKey = self.safe_string(result, 'nextKey')
            page = self.sum(page, 1)
            collectedLength = len(rawMarkets)
            if (pageMarketsLength < pageSize) or (page >= maxPages) or (paginationKey is None) or ((userLimit is not None) and (collectedLength >= userLimit)):
                break
        return rawMarkets

    def filter_raw_markets_by_fixture(self, rawMarkets: list[object], sportXeventId: str) -> list[object]:
        """
 @ignore
        keeps only the raw markets belonging to one fixture — the venue's own sportXeventId filter on /markets/active is unreliable (observed live returning every fixture), so the scope is enforced client-side
        :param dict[] rawMarkets: the raw sx.bet market objects
        :param str sportXeventId: the fixture id to keep
        :returns dict[]: the raw markets of that fixture only
        """
        result = []
        rawMarketsLength = len(rawMarkets)
        for i in range(0, rawMarketsLength):
            raw = rawMarkets[i]
            if self.safe_string(raw, 'sportXeventId') == sportXeventId:
                result.append(raw)
        return result

    def parse_sxbet_market(self, raw: dict) -> Market:
        """
 @ignore
        converts a single raw sx.bet market into one ccxt market whose two sides become the outcomes
        :param dict raw: the raw sx.bet market object
        :returns dict: a [market structure](https://docs.ccxt.com/#/?id=market-structure)
        """
        #
        #     {
        #         "status": "ACTIVE",
        #         "marketHash": "0x7154aa3580267e276cccd0f4f826464a05e3efd8e1c81d7717bef6b5ae88b07d",
        #         "outcomeOneName": "Los Angeles Rams",
        #         "outcomeTwoName": "San Francisco 49ers",
        #         "outcomeVoidName": "NO_CONTEST",
        #         "teamOneName": "Los Angeles Rams",
        #         "teamTwoName": "San Francisco 49ers",
        #         "type": 226,
        #         "gameTime": 1789086900,
        #         "line": -2.5, // present on spread/total markets only
        #         "sportXeventId": "L18870109",
        #         "liveEnabled": true,
        #         "sportLabel": "Football",
        #         "sportId": 8,
        #         "leagueId": 243,
        #         "leagueLabel": "NFL",
        #         "mainLine": true,
        #         "isQuarterLineMarket": false
        #     }
        #
        marketHash = self.safe_string(raw, 'marketHash', '')
        teamOneName = self.safe_string(raw, 'teamOneName')
        teamTwoName = self.safe_string(raw, 'teamTwoName')
        outcomeOneName = self.safe_string(raw, 'outcomeOneName')
        outcomeTwoName = self.safe_string(raw, 'outcomeTwoName')
        eventSlug = self.shorten_slug(teamOneName + ' ' + teamTwoName)
        # one fixture carries many markets (moneyline, several spread/total lines, quarter/half
        # variants) whose outcomeOneName text can coincide or nearly coincide, so a text-only
        # slug isn't guaranteed unique. suffix with the market hash instead (always unique,
        # letters+digits only so it survives shortenSlug in one atomic word)
        # parseToInt-wrapped .length: the bare `const n = str.length;` statement is the php
        # transpiler's ARRAY hint (count()), which breaks on a string — this form emits
        # strlen()/len() correctly in both python and php
        hashLength = self.parse_to_int(len(marketHash))
        hashSuffix = marketHash[hashLength - 6:]
        marketSlug = self.shorten_slug(outcomeOneName) + '_' + hashSuffix
        marketSymbol = self.slug_to_market_symbol(eventSlug, marketSlug)
        status = self.safe_string_upper(raw, 'status')
        active = (status == 'ACTIVE')
        # guard against a zero sentinel for "no scheduled game time" - safeTimestamp would
        # turn it into the 1970 epoch
        gameTime = None
        if self.safe_integer(raw, 'gameTime', 0) != 0:
            gameTime = self.safe_timestamp(raw, 'gameTime')
        outcomeLabels = [outcomeOneName, outcomeTwoName]
        outcomeIds = [marketHash, marketHash + '-2']
        outcomes = []
        for oi in range(0, len(outcomeLabels)):
            label = outcomeLabels[oi]
            outcomeHandle = self.slug_to_outcome_symbol(eventSlug, marketSlug, label)
            outcomes.append({
                'id': outcomeIds[oi],
                'outcomeId': outcomeIds[oi],
                'outcome': outcomeHandle,
                'market': marketSymbol,
                'label': label,
                'active': active,
                'winner': None,
                'settleFraction': None,
                'info': raw,
            })
        return {
            'id': marketHash,
            'market': marketSymbol,
            'base': 'USDC',
            'quote': 'USDC',
            'settle': None,
            'baseId': marketHash,
            'quoteId': 'USDC',
            'settleId': None,
            'type': 'prediction',
            'marketType': 'binary',
            'executionModel': 'clob',
            'spot': False,
            'margin': False,
            'swap': False,
            'future': False,
            'option': False,
            'prediction': True,
            'active': active,
            'resolved': False,
            'resolvedOutcome': None,
            'contract': False,
            'linear': None,
            'inverse': None,
            'contractSize': None,
            'expiry': gameTime,
            'expiryDatetime': self.iso8601(gameTime),
            'strike': None,
            'optionType': None,
            # no trading-fee percentage found anywhere in the docs/metadata (oracleFees is a
            # resolution/oracle fee, not a per-trade maker/taker cut) - 0 until live data says otherwise
            'taker': 0,
            'maker': 0,
            'percentage': True,
            'tierBased': False,
            'feeSide': 'get',
            'precision': {
                # USDC stakes have 6 decimals; prices sit on the odds ladder (oddsLadderStepSize
                # 125 in 1e-5 units on both networks — see /metadata), i.e. a 0.00125 step
                'amount': 0.000001,
                'price': 0.00125,
            },
            'limits': {
                'leverage': {'min': 1, 'max': 1},
                # /metadata/obv3 publishes limits.orderSizeMinimum '1' (1 USDC, 1000000 base units)
                # on both networks - the stake and its cost share the same floor
                'amount': {'min': 1, 'max': None},
                'price': {'min': 0, 'max': 1},
                'cost': {'min': 1, 'max': None},
            },
            'outcomes': outcomes,
            'info': raw,
            'created': None,
        }

    async def fetch_events(self, params: fetchEventsParams = {}) -> list[PredictionEvent]:
        """
        fetches sx.bet fixtures(one fixture = one event, its markets are every moneyline/spread/total line on that fixture) scoped by eventId, leagueId, sportId or a free-text query/tags match against team and league names — always live from the API, never the local cache(it POPULATES the cache for later event()/outcome lookups). query/queries/tags are matched client-side over a bounded scan of /markets/active — the venue's GET /search covers team names only (not league or sport labels) and is not wired here yet

        https://docs.sx.bet/api-reference/get-markets-active

        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.eventId]: direct lookup by unified event id(the sx.bet sportXeventId, e.g. 'L18870109')
        :param str [params.query]: free-text search matched against team and league names
        :param str[] [params.queries]: multiple free-text searches(alternative to query, unioned)
        :param str[] [params.tags]: matched identically to query/queries(sx.bet has no tag taxonomy)
        :param int [params.leagueId]: sx.bet league id(e.g. 243 for NFL) — fetched server-side
        :param int [params.sportId]: sx.bet sport id(e.g. 8 for Football) — fetched server-side
        :param str [params.status]: 'active' | 'inactive' | 'closed' | 'all'
        :param int [params.limit]: max number of events to return
        :returns dict[]: an array of event structures
        """
        self.require_event_query(params)
        eventId = self.safe_string_2(params, 'eventId', 'slug')
        leagueId = self.safe_string(params, 'leagueId')
        sportId = self.safe_string(params, 'sportId')
        queries = self.parse_search_queries(params)
        tags = self.safe_list(params, 'tags', [])
        tagsLength = len(tags)
        for i in range(0, tagsLength):
            queries.append(tags[i])
        rest = self.omit(params, ['eventId', 'slug', 'leagueId', 'sportId', 'query', 'queries', 'tags', 'status', 'sort', 'searchIn', 'limit'])
        rawMarkets: list[Any]
        if eventId is not None:
            rawMarkets = await self.fetch_raw_markets_paged(self.extend({'sportXeventId': eventId}, rest), None)
            # the venue's sportXeventId filter on /markets/active is unreliable (observed live
            # returning every fixture) — enforce the scope client-side
            rawMarkets = self.filter_raw_markets_by_fixture(rawMarkets, eventId)
        elif leagueId is not None:
            rawMarkets = await self.fetch_raw_markets_paged(self.extend({'leagueId': leagueId}, rest), None)
        elif sportId is not None:
            rawMarkets = await self.fetch_raw_markets_paged(self.extend({'sportId': sportId}, rest), None)
        else:
            # no server-side scope left, only query/tags — full scan honoring the fetchMarkets
            # bound, then filter client-side (the venue's /search covers team names only)
            rawMarkets = await self.fetch_raw_markets_paged(rest, None)
        queriesLength = len(queries)
        if queriesLength > 0:
            filtered = []
            preFilterLength = len(rawMarkets)
            for i in range(0, preFilterLength):
                if self.matches_event_query(rawMarkets[i], queries):
                    filtered.append(rawMarkets[i])
            rawMarkets = filtered
        grouped = {}
        order = []
        rawMarketsLength = len(rawMarkets)
        for i in range(0, rawMarketsLength):
            raw = rawMarkets[i]
            sportXeventId = self.safe_string(raw, 'sportXeventId')
            if sportXeventId is None:
                continue
            if not (sportXeventId in grouped):
                grouped[sportXeventId] = []
                order.append(sportXeventId)
            grouped[sportXeventId].append(raw)
        if self.markets is None:
            self.markets = self.create_safe_dictionary()
        result = []
        orderLength = len(order)
        for i in range(0, orderLength):
            fixtureId = order[i]
            event = self.parse_event(fixtureId, grouped[fixtureId])
            evMarkets = self.safe_list(event, 'markets', [])
            evMarketsLength = len(evMarkets)
            for j in range(0, evMarketsLength):
                m = evMarkets[j]
                self.markets[m['market']] = m
            result.append(event)
        self.populate_outcomes()
        self.set_events(result)
        postParams = self.omit(params, ['leagueId', 'sportId', 'query', 'queries', 'tags'])
        return self.apply_event_fetch_params(result, postParams, [])

    async def fetch_event(self, id: str, params={}) -> PredictionEvent:
        """
        fetches a single sx.bet fixture(event) by its sportXeventId

        https://docs.sx.bet/api-reference/get-markets-active

        :param str id: the sx.bet sportXeventId, e.g. 'L18870109'
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction event structure](https://docs.ccxt.com/#/?id=prediction-event-structure)
        """
        rawMarkets = await self.fetch_raw_markets_paged(self.extend({'sportXeventId': id}, params), None)
        # enforce the fixture scope client-side — see filterRawMarketsByFixture
        rawMarkets = self.filter_raw_markets_by_fixture(rawMarkets, id)
        rawMarketsLength = len(rawMarkets)
        if rawMarketsLength == 0:
            raise BadSymbol(self.id + ' fetchEvent() could not find an active fixture ' + id)
        event = self.parse_event(id, rawMarkets)
        self.index_event_outcomes(event)
        self.set_events([event])
        return event

    def matches_event_query(self, raw: dict, queries: list[str]) -> bool:
        """
 @ignore
        checks a raw market's team/league names against a list of free-text queries, matching in either direction (a short user query inside a long name, or — since fetchEvents derives its fallback test query from an already-slugified handle — a long joined query containing a short name)
        :param dict raw: the raw sx.bet market object
        :param str[] queries: lowercase-insensitive free-text queries
        :returns boolean: whether any query matches any of the market's team/league/outcome names
        """
        fields = [
            self.safe_string(raw, 'teamOneName'),
            self.safe_string(raw, 'teamTwoName'),
            self.safe_string(raw, 'leagueLabel'),
            self.safe_string(raw, 'sportLabel'),
            self.safe_string(raw, 'outcomeOneName'),
            self.safe_string(raw, 'outcomeTwoName'),
        ]
        queriesLength = len(queries)
        for qi in range(0, queriesLength):
            query = queries[qi].lower()
            if query == '':
                continue
            for fi in range(0, len(fields)):
                field = fields[fi]
                if field is None:
                    continue
                fieldLower = field.lower()
                if (query.find(fieldLower) >= 0) or (fieldLower.find(query) >= 0):
                    return True
        return False

    def parse_event(self, fixtureId: str, rawMarkets: list[object]) -> object:
        """
 @ignore
        groups a fixture's raw markets (all sharing one sportXeventId) into one unified event structure
        :param str fixtureId: the sx.bet sportXeventId
        :param dict[] rawMarkets: the raw sx.bet market objects belonging to self fixture
        :returns dict: an event structure
        """
        marketsList = []
        anyActive = False
        earliestGameTime = None
        teamOneName = None
        teamTwoName = None
        leagueLabel = None
        rawMarketsLength = len(rawMarkets)
        for i in range(0, rawMarketsLength):
            raw = rawMarkets[i]
            parsed = self.parse_sxbet_market(raw)
            if parsed is None:
                raise ExchangeError(self.id + ' parseEvent() could not resolve parsed market')
            marketsList.append(parsed)
            if parsed['active'] is True:
                anyActive = True
            if teamOneName is None:
                teamOneName = self.safe_string(raw, 'teamOneName')
                teamTwoName = self.safe_string(raw, 'teamTwoName')
                leagueLabel = self.safe_string(raw, 'leagueLabel')
            gameTime = self.safe_string(raw, 'gameTime')
            # skip the zero sentinel for "no scheduled game time" so it can't become the epoch
            if (gameTime is not None) and (gameTime != '0') and ((earliestGameTime is None) or (gameTime < earliestGameTime)):
                earliestGameTime = gameTime
        end = self.safe_timestamp({'gameTime': earliestGameTime}, 'gameTime')
        # some fixtures carry no team names on their market rows - fall back to the fixture id
        # so the unified event handle is never empty or built from stringified nulls
        title = None
        eventSlug = None
        if (teamOneName is not None) and (teamTwoName is not None):
            title = teamOneName + ' vs ' + teamTwoName
            eventSlug = self.shorten_slug(teamOneName + ' ' + teamTwoName)
        else:
            eventSlug = self.shorten_slug(fixtureId)
        return {
            'id': fixtureId,
            'slug': fixtureId,
            'event': eventSlug,
            'title': title,
            'description': None,
            'category': leagueLabel,
            'tags': None,
            'markets': marketsList,
            'mutuallyExclusive': False,
            'active': anyActive,
            'resolved': None,
            'volume': None,
            'liquidity': None,
            'created': None,
            'createdDatetime': None,
            'end': end,
            'endDatetime': self.iso8601(end),
            'image': None,
            'url': None,
            'info': rawMarkets,
        }

    async def load_sx_obv3_metadata(self) -> dict:
        """
 @ignore
        fetches and caches GET /metadata/obv3 - the v3 orderbook metadata carrying the ready-made EIP-712 domain, the active base asset and the order size limits

        https://docs.sx.bet/api-reference/get-metadata-obv3

        :returns dict: the cached obv3 metadata data object
        """
        cached = self.safe_dict(self.options, 'sxObv3Metadata')
        if cached is not None:
            return cached
        response = await self.sxbetPublicGetMetadataObv3()
        data = self.safe_dict(response, 'data', {})
        self.options['sxObv3Metadata'] = data
        return data

    def round_odds_to_ladder(self, probability: Str, oddsLadderStepSize: str) -> str:
        """
 @ignore
        rounds an implied probability(0-1) to sx.bet's odds ladder (oddsLadderStepSize is in units of 1e-5, e.g. 125 -> a 0.125% step) — a maker's percentageOdds is rejected unless it lands exactly on the ladder
        :param str probability: the implied probability in decimal-string form(0-1)
        :param str oddsLadderStepSize: the raw oddsLadderStepSize from /metadata/obv3(e.g. '125')
        :returns str: the probability rounded to the nearest ladder step, in decimal-string form
        """
        tickSize = Precise.string_div(oddsLadderStepSize, '100000', 10)
        return self.decimal_to_precision(probability, ROUND, tickSize, TICK_SIZE)

    def hash_eip712_digest(self, encoded: object) -> str:
        """
 @ignore
        hashes an EIP-712 encoded payload(domainSeparator‖structHash, prefixed with 0x1901 by ethEncodeStructuredData) down to the final 32-byte digest to sign, used by sx.bet's taker-fill and cancel signatures
 @param {Uint8Array} encoded the output of self.eth_encode_structured_data(domain, types, message)
        :returns str: the 32-byte digest to ecdsa-sign, in '0x'-prefixed hex form
        """
        return '0x' + self.hash(encoded, 'keccak', 'hex')

    def sign_digest(self, digest: str, privateKey: str) -> str:
        """
 @ignore
        ecdsa-signs a 32-byte digest and assembles the r‖s‖v hex signature sx.bet expects
        :param str digest: the '0x'-prefixed 32-byte digest to sign
        :param str privateKey: the signer's private key
        :returns str: a '0x'-prefixed 65-byte hex signature(r‖s‖v)
        """
        signature = self.ecdsa(digest[-64:], privateKey[-64:], 'secp256k1', None)
        # assign to bare locals before padStart — the php transpiler's str_pad regex only
        # matches a simple identifier, an expression form leaks a raw padStart() call
        rRaw = signature['r']
        sRaw = signature['s']
        r = rRaw.rjust(64, '0')
        s = sRaw.rjust(64, '0')
        v = self.int_to_base16(self.sum(27, signature['v']))
        return '0x' + r + s + v

    async def fetch_erc20_name(self, rpcUrl: Str, tokenAddress: str) -> str:
        """
 @ignore
        reads an ERC20 token's name() via eth_call (needed for the Permit EIP-712 domain — must match the token's real on-chain name or the signature fails verification)
        :param str rpcUrl: the RPC endpoint to call
        :param str tokenAddress: the token contract address
        :returns str: the token's on-chain name
        """
        nameCallData = '0x06fdde03'  # name()
        result = await self.eth_rpc(rpcUrl, 'eth_call', [{'to': tokenAddress, 'data': nameCallData}, 'latest'])
        hex = self.remove0x_prefix(result)
        # dynamic ABI string return: [32-byte offset][32-byte length][utf8 bytes, right-padded]
        lengthHex = hex[64:128]
        length = self.hex_to_int(lengthHex)
        dataEnd = self.sum(128, length * 2)
        dataHex = hex[128:dataEnd]
        return self.decode(self.base16_to_binary(dataHex))

    def hex_to_int(self, hex: str) -> float:
        """
 @ignore
        parses a hex string(no '0x' prefix) into an integer — plain digit-by-digit, since JS's int (hex, 16) two-argument form has no portable equivalent across languages/transpilers. Only used for small values (byte lengths, permit nonces), safe well within the float-precision range
        :param str hex: the hex string, no '0x' prefix
        :returns int: the parsed integer
        """
        digits = '0123456789abcdef'
        lowerHex = hex.lower()
        hexLength = len(lowerHex)
        result = 0
        for i in range(0, hexLength):
            ch = lowerHex[i]
            digitValue = digits.find(ch)
            result = (result * 16) + digitValue
        return result

    async def fetch_sxbet_proxy(self) -> dict:
        """
 @ignore
        fetches the account's obv3 proxy wallet state

        https://docs.sx.bet/api-reference/get-user-proxy

        :returns dict} the raw proxy data({obv3ProxyWalletAddress, deployed, multisigSafeAddress:)
        """
        response = await self.sxbetPrivateGetUserProxy()
        return self.safe_dict(response, 'data', {})

    async def approve(self, params={}) -> object:
        """
        funds the account's obv3 proxy wallet - v3 trading capital must sit inside the proxy. Deploys the proxy first when absent, then moves USDC from the wallet into it via a gasless EIP-2612 Permit signature (POST /user/transfer-to-proxy)

        https://docs.sx.bet/api-reference/post-user-transfer-to-proxy
        https://docs.sx.bet/api-reference/post-user-deploy-proxy

        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param float [params.amount]: the USDC amount to move into the proxy(required)
        :param str [params.tokenAddress]: the token to transfer, defaults to the active base token
        :param str [params.spender]: the transfer executor granted the permit, defaults to options.transferToProxySpender or the obv3 transferToProxyExecutorAddress
        :param int [params.deadline]: unix seconds the permit signature expires at, defaults to options.approveDeadlineSeconds from now
        :param str [params.rpcUrl]: overrides the chain's default RPC endpoint (see options.chains)
        :returns dict: a dict with the raw response and the transfer sessionId
        """
        self.check_required_credentials()
        amount = self.safe_number(params, 'amount')
        if amount is None:
            raise ArgumentsRequired(self.id + ' approve() requires params.amount - the USDC amount to move into the proxy wallet')
        proxy = await self.fetch_sxbet_proxy()
        deployed = self.safe_bool(proxy, 'deployed', False)
        if deployed is not True:
            await self.sxbetPrivatePostUserDeployProxy()
            # deployment is asynchronous - poll until the proxy exists
            for i in range(0, 30):
                await self.sleep(2000)
                state = await self.fetch_sxbet_proxy()
                if self.safe_bool(state, 'deployed', False):
                    break
        obv3 = await self.load_sx_obv3_metadata()
        activeAsset = self.safe_dict(obv3, 'activeAsset', {})
        chainId = self.safe_integer(obv3, 'chainId')
        usdcAddress = self.safe_string(activeAsset, 'baseToken')
        # the permit spender is the venue's transfer-to-proxy executor, published in /metadata/obv3
        executorAddress = self.safe_string(obv3, 'transferToProxyExecutorAddress')
        tokenAddress = self.safe_string(params, 'tokenAddress', usdcAddress)
        if tokenAddress is None:
            raise BadRequest(self.id + ' approve() could not resolve the base token address from /metadata/obv3')
        spender = None
        spender, params = self.handle_option_and_params_2(params, 'approve', 'spender', 'transferToProxySpender', executorAddress)
        if spender is None:
            raise BadRequest(self.id + ' approve() could not resolve the transfer-to-proxy executor from /metadata/obv3 - pass params.spender')
        chains = self.safe_dict(self.options, 'chains', {})
        chainConfig = self.safe_dict(chains, self.number_to_string(chainId), {})
        rpcUrl = self.safe_string(params, 'rpcUrl', self.safe_string(chainConfig, 'rpcUrl'))
        if rpcUrl is None:
            raise ArgumentsRequired(self.id + ' approve() has no RPC endpoint configured for chainId ' + self.number_to_string(chainId) + ' - pass params.rpcUrl')
        owner = self.walletAddress
        nonceCallData = '0x7ecebe00' + self.pad_hex_address(owner)  # nonces(address)
        nonceResult = await self.eth_rpc(rpcUrl, 'eth_call', [{'to': tokenAddress, 'data': nonceCallData}, 'latest'])
        nonceHex = self.hex_to_rlp_bytes(nonceResult)
        nonce = '0' if (nonceHex == '') else self.number_to_string(self.hex_to_int(nonceHex))
        tokenName = await self.fetch_erc20_name(rpcUrl, tokenAddress)
        defaultDeadlineSeconds = self.safe_integer(self.options, 'approveDeadlineSeconds', 7200)
        deadline = self.safe_integer(params, 'deadline', self.sum(self.seconds(), defaultDeadlineSeconds))
        value = self.decimal_to_precision(Precise.string_mul(self.number_to_string(amount), '1000000'), ROUND, 0, DECIMAL_PLACES)
        domain = {'name': tokenName, 'version': '1', 'chainId': chainId, 'verifyingContract': tokenAddress}
        messageTypes = {
            'Permit': [
                {'name': 'owner', 'type': 'address'},
                {'name': 'spender', 'type': 'address'},
                {'name': 'value', 'type': 'uint256'},
                {'name': 'nonce', 'type': 'uint256'},
                {'name': 'deadline', 'type': 'uint256'},
            ],
        }
        messageData = {'owner': owner, 'spender': spender, 'value': value, 'nonce': nonce, 'deadline': deadline}
        encoded = self.eth_encode_structured_data(domain, messageTypes, messageData)
        digest = self.hash_eip712_digest(encoded)
        signature = self.sign_digest(digest, self.privateKey)
        request = {
            'owner': owner,
            'spender': spender,
            'tokenAddress': tokenAddress,
            'value': value,
            'deadline': self.number_to_string(deadline),
            'signature': signature,
        }
        rest = self.omit(params, ['amount', 'tokenAddress', 'deadline', 'rpcUrl'])
        response = await self.sxbetPrivatePostUserTransferToProxy(self.extend(request, rest))
        data = self.safe_dict(response, 'data', {})
        return {
            'info': response,
            'id': self.safe_string(data, 'sessionId'),
        }

    async def create_order(self, outcome: str, type: Str, side: Str, amount: Num, price: Num = None, params={}) -> PredictionOrder:
        """
        places an order on sx.bet's v3 unified orderbook - a 'limit' order rests with GTC time-in-force, a 'market' order fills immediately with IOC (or FOK via params.timeInForce). sx.bet has no shares - 'amount' is the USDC stake to risk, and 'price' is the implied probability (0-1) of the requested outcome. 'sell' bets the OPPOSITE outcome of the one requested (sx.bet is bilateral: there is no owned position to sell, only the complementary side of the same market)

        https://docs.sx.bet/api-reference/post-orders-v3

        :param str outcome: unified outcome or outcome token id
        :param str type: 'limit'(GTC resting order) or 'market'(IOC immediate fill)
        :param str side: 'buy' backs the requested outcome, 'sell' backs the complementary one
        :param float amount: the USDC amount to stake/risk
        :param float [price]: implied probability(0-1) of the requested outcome; required for both order types
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.timeInForce]: overrides the derived value - 'GTC', 'IOC' or 'FOK'
        :param int [params.expiry]: unix seconds the order expires at; must be in the future(zero and past values are rejected, so is anything inside the fixture's betting-delay window - /metadata/obv3 resolves the delay per sport/league, live vs pregame), defaults to options.defaultOrderExpirySeconds from now
        :param str [params.salt]: overrides the random salt differentiating self order
        :param str [params.clientOrderId]: caller-chosen id echoed back on reads(max 64 chars)
        :param boolean [params.waitForOutcome]: wait for the matching outcome inline(default True)
        :param boolean [params.useBetCredits]: fund the stake from bet credits instead of the proxy balance(IOC/FOK only)
        :param str [params.externalUserId]: partner attribution id echoed back on order, fill and trade reads
        :returns dict: a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        self.check_required_credentials()
        await self.load_outcome(outcome)
        outcomeObj = self.outcome(outcome)
        if (type != 'limit') and (type != 'market'):
            raise InvalidOrder(self.id + " createOrder() type must be 'limit' or 'market', got " + type)
        if price is None:
            raise ArgumentsRequired(self.id + ' createOrder() requires a price - the implied probability of the requested outcome')
        # the venue has no post-only or trigger mechanics - reject the unified params instead
        # of forwarding fields the exchange would silently ignore
        postOnly = self.safe_bool(params, 'postOnly', False)
        if postOnly is True:
            raise NotSupported(self.id + ' createOrder() does not support postOnly - GTC orders may cross on entry')
        triggerPrice = self.safe_string_n(params, ['triggerPrice', 'stopLossPrice', 'takeProfitPrice'])
        if triggerPrice is not None:
            raise NotSupported(self.id + ' createOrder() does not support trigger, stop-loss or take-profit orders')
        marketHash = self.safe_string(outcomeObj['info'], 'marketHash', '')
        outcomeId = self.safe_string(outcomeObj, 'outcomeId')
        isOutcomeOne = (outcomeId == marketHash)
        isBuy = (side == 'buy')
        # 'sell' bets the complementary outcome, mirroring the requested outcome's own probability -
        # matches the normalize-to-one-book convention used by other prediction venues
        isMakerBettingOutcomeOne = isOutcomeOne if (isBuy) else not isOutcomeOne
        priceStr = self.number_to_string(price)
        probability = priceStr if (isBuy) else Precise.string_sub('1', priceStr)
        obv3 = await self.load_sx_obv3_metadata()
        domain = self.safe_dict(obv3, 'domain', {})
        activeAsset = self.safe_dict(obv3, 'activeAsset', {})
        baseToken = self.safe_string(activeAsset, 'baseToken')
        oddsLadderStepSize = self.number_to_string(self.safe_integer(obv3, 'oddsLadderStepSize', 125))
        roundedProbability = self.round_odds_to_ladder(probability, oddsLadderStepSize)
        percentageOdds = self.decimal_to_precision(Precise.string_mul(roundedProbability, '100000000000000000000'), ROUND, 0, DECIMAL_PLACES)
        amountStr = self.number_to_string(amount)
        totalBetSize = self.decimal_to_precision(Precise.string_mul(amountStr, '1000000'), ROUND, 0, DECIMAL_PLACES)
        saltNumber = self.safe_string(params, 'salt', self.number_to_string(self.milliseconds()))
        # the request body carries the salt in 32-byte-hex form; the signed struct carries the
        # same value in uint256 form - both parse to one number server-side. assign to bare locals
        # before padStart - see the transpiler notes
        saltHexRaw = self.int_to_base16(self.parse_to_int(saltNumber))
        saltHexPadded = saltHexRaw.rjust(64, '0')
        saltHex = '0x' + saltHexPadded
        defaultExpirySeconds = self.safe_integer(self.options, 'defaultOrderExpirySeconds', 86400)
        expiry = self.safe_integer(params, 'expiry', self.sum(self.seconds(), defaultExpirySeconds))
        defaultTif = 'GTC' if (type == 'limit') else 'IOC'
        timeInForce = None
        timeInForce, params = self.handle_option_and_params(params, 'createOrder', 'timeInForce', defaultTif)
        # an explicit IOC/FOK on a 'limit' order is honored verbatim - the venue executes exactly
        # that time-in-force. only GTC on a 'market' order is refused: it would silently rest,
        # contradicting the immediate-fill semantics the type promises
        if (type == 'market') and (timeInForce == 'GTC'):
            raise InvalidOrder(self.id + " createOrder() market orders cannot be GTC - use type 'limit' for a resting order")
        maker = self.walletAddress
        messageTypes = {
            'Order': [
                {'name': 'marketHash', 'type': 'bytes32'},
                {'name': 'baseToken', 'type': 'address'},
                {'name': 'totalBetSize', 'type': 'uint256'},
                {'name': 'percentageOdds', 'type': 'uint256'},
                {'name': 'salt', 'type': 'uint256'},
                {'name': 'expiry', 'type': 'uint256'},
                {'name': 'maker', 'type': 'address'},
                {'name': 'isMakerBettingOutcomeOne', 'type': 'bool'},
            ],
        }
        messageData = {
            'marketHash': marketHash,
            'baseToken': baseToken,
            'totalBetSize': totalBetSize,
            'percentageOdds': percentageOdds,
            'salt': saltNumber,
            'expiry': expiry,
            'maker': maker,
            'isMakerBettingOutcomeOne': isMakerBettingOutcomeOne,
        }
        encoded = self.eth_encode_structured_data(domain, messageTypes, messageData)
        digest = self.hash_eip712_digest(encoded)
        orderSignature = self.sign_digest(digest, self.privateKey)
        orderItem = {
            'marketHash': marketHash,
            'maker': maker,
            'totalBetSize': totalBetSize,
            'percentageOdds': percentageOdds,
            'salt': saltHex,
            'expiry': expiry,
            'baseToken': baseToken,
            'isMakerBettingOutcomeOne': isMakerBettingOutcomeOne,
            'timeInForce': timeInForce,
            'orderSignature': orderSignature,
        }
        clientOrderId = self.safe_string(params, 'clientOrderId')
        if clientOrderId is not None:
            orderItem['clientOrderId'] = clientOrderId
        # useBetCredits and externalUserId are per-order fields - route them into the order item,
        # not the top-level body, where the venue would silently ignore them
        useBetCredits = self.safe_bool(params, 'useBetCredits')
        if useBetCredits is not None:
            orderItem['useBetCredits'] = useBetCredits
        externalUserId = self.safe_string(params, 'externalUserId')
        if externalUserId is not None:
            orderItem['externalUserId'] = externalUserId
        waitForOutcome = self.safe_bool(params, 'waitForOutcome', True)
        rest = self.omit(params, ['salt', 'expiry', 'clientOrderId', 'waitForOutcome', 'useBetCredits', 'externalUserId'])
        request = {'orders': [orderItem], 'waitForOutcome': waitForOutcome}
        response = await self.sxbetPrivatePostOrdersV3(self.extend(request, rest))
        data = self.safe_dict(response, 'data', {})
        results = self.safe_list(data, 'orders', [])
        first = self.safe_dict(results, 0, {})
        status = self.safe_string_upper(first, 'status')
        if status == 'FAILED':
            reason = self.safe_string(first, 'reason', 'FAILED')
            feedback = self.id + ' createOrder() rejected: ' + reason
            self.throw_exactly_matched_exception(self.exceptions['exact'], reason, feedback)
            raise InvalidOrder(feedback)
        # with waitForOutcome the venue reports the matching result inline:
        #     "outcome": { "state": "FULLY_FILLED", "fillAmount": "2000000", "remainingAmount": "0", "matchIds": [...] }
        # documented states: RESTED, FULLY_FILLED, PARTIAL_FILL_DONE, PARTIAL_FILL_RESTED, TIMEOUT and
        # CANCELLED with cancelReason NO_LIQUIDITY, INTERNAL_ERROR, ENGINE_SHUTDOWN, EXPIRED or INSUFFICIENT_BALANCE
        matchOutcome = self.safe_dict(first, 'outcome')
        usdcDecimals = '1000000'
        filled = None
        remaining = None
        orderStatus = None
        if matchOutcome is not None:
            fillAmountRaw = self.safe_string(matchOutcome, 'fillAmount')
            remainingRaw = self.safe_string(matchOutcome, 'remainingAmount')
            if fillAmountRaw is not None:
                filled = self.parse_number(Precise.string_div(fillAmountRaw, usdcDecimals, 6))
            if remainingRaw is not None:
                remaining = self.parse_number(Precise.string_div(remainingRaw, usdcDecimals, 6))
            state = self.safe_string_upper(matchOutcome, 'state')
            if (state == 'RESTED') or (state == 'PARTIAL_FILL_RESTED'):
                orderStatus = 'open'
            elif state == 'FULLY_FILLED':
                orderStatus = 'closed'
            elif state == 'PARTIAL_FILL_DONE':
                # the unmatched remainder of an IOC was cancelled by the venue
                orderStatus = 'canceled'
            elif state == 'CANCELLED':
                # the documented field is cancelReason; 'reason' is kept for older responses
                reason = self.safe_string_upper_2(matchOutcome, 'cancelReason', 'reason')
                orderStatus = 'expired' if (reason == 'EXPIRED') else 'canceled'
            # TIMEOUT: the wait elapsed before matching resolved - the order state is unknown here
        now = self.milliseconds()
        return self.safe_prediction_order({
            'id': self.safe_string_2(first, 'orderId', 'orderHash'),
            'clientOrderId': self.safe_string(first, 'clientOrderId', clientOrderId),
            'info': response,
            'timestamp': now,
            'datetime': self.iso8601(now),
            'status': orderStatus,
            'filled': filled,
            'remaining': remaining,
            'cost': filled,
            'outcome': self.safe_string(outcomeObj, 'outcome'),
            'outcomeId': outcomeId,
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string(outcomeObj, 'market'),
            'type': type,
            'timeInForce': timeInForce,
            'side': side,
            'price': self.parse_number(roundedProbability),
            'amount': amount,
            'fee': None,
            'trades': [],
        }, outcomeObj)

    def parse_sxbet_cancel_response(self, response: dict) -> list[PredictionOrder]:
        """
 @ignore
        parses the shared v3 cancel response({cancelled, notCancelled, unconfirmed}) into order structures
        :param dict response: the raw DELETE /orders-v3 response
        :returns dict[]: a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        data = self.safe_dict(response, 'data', {})
        result = []
        # by-id cancels report 'cancelled'; the /all and /event paths submit asynchronously and
        # report 'cancelledSubmitted' instead - treat both like cancelled
        cancelled = self.safe_list_2(data, 'cancelled', 'cancelledSubmitted', [])
        cancelledLength = len(cancelled)
        for i in range(0, cancelledLength):
            result.append(self.safe_prediction_order({
                'id': self.safe_string(cancelled[i], 'orderId'),
                'status': 'canceled',
                'info': response,
            }))
        notCancelled = self.safe_list(data, 'notCancelled', [])
        notCancelledLength = len(notCancelled)
        for i in range(0, notCancelledLength):
            # the venue reports why (e.g. NOT_FOUND) - the order was not cancelled, report it honestly
            result.append(self.safe_prediction_order({
                'id': self.safe_string(notCancelled[i], 'orderId'),
                'status': None,
                'info': response,
            }))
        unconfirmed = self.safe_list(data, 'unconfirmed', [])
        unconfirmedLength = len(unconfirmed)
        for i in range(0, unconfirmedLength):
            result.append(self.safe_prediction_order({
                'id': self.safe_string(unconfirmed[i], 'orderId'),
                'status': None,
                'info': response,
            }))
        return result

    async def cancel_order(self, id: str, outcome: Str = None, params={}) -> PredictionOrder:
        """
        cancels one resting maker order - v3 cancels are plain api-key-authenticated DELETE requests, no signature involved

        https://docs.sx.bet/api-reference/delete-orders-v3

        :param str id: the order id
        :param str [outcome]: not used by sxbet.cancelOrder
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        self.check_required_credentials()
        request = {'orders': [{'orderId': id}]}
        response = await self.sxbetPrivateDeleteOrdersV3(self.extend(request, params))
        orders = self.parse_sxbet_cancel_response(response)
        return self.safe_dict(orders, 0)

    async def cancel_orders(self, ids: list[str], outcome: Str = None, params={}) -> list[PredictionOrder]:
        """
        cancels multiple resting maker orders in one request - v3 cancels are plain api-key-authenticated DELETE requests, no signature involved

        https://docs.sx.bet/api-reference/delete-orders-v3

        :param str[] ids: the order ids to cancel
        :param str [outcome]: not used by sxbet.cancelOrders
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        self.check_required_credentials()
        idsLength = len(ids)
        if idsLength == 0:
            raise ArgumentsRequired(self.id + ' cancelOrders() requires a non-empty ids argument')
        # the venue caps one cancel request at maxCancelOrders ids (100, see /metadata/obv3) -
        # chunk larger batches instead of letting the whole request 400
        chunkSize = self.safe_integer(self.options, 'cancelOrdersBatchSize', 100)
        chunkCount = self.parse_to_int(self.sum(idsLength, chunkSize - 1) / chunkSize)
        result = []
        for c in range(0, chunkCount):
            start = c * chunkSize
            end = start + chunkSize
            if end > idsLength:
                end = idsLength
            orderItems = []
            for i in range(start, end):
                orderItems.append({'orderId': ids[i]})
            request = {'orders': orderItems}
            response = await self.sxbetPrivateDeleteOrdersV3(self.extend(request, params))
            result = self.array_concat(result, self.parse_sxbet_cancel_response(response))
        return result

    async def cancel_all_orders(self, outcome: Str = None, params={}) -> list[PredictionOrder]:
        """
        cancels every resting maker order of the account, or every order of one fixture via params.eventId - v3 cancels are plain api-key-authenticated DELETE requests, no signature involved

        https://docs.sx.bet/api-reference/delete-orders-v3-all
        https://docs.sx.bet/api-reference/delete-orders-v3-event

        :param str [outcome]: not used by sxbet.cancelAllOrders
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param str [params.eventId]: cancels every order across every market of self fixture instead of the account-wide path(params.sportXeventId is accepted too)
        :returns dict[]: a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        self.check_required_credentials()
        eventId = self.safe_string_2(params, 'eventId', 'sportXeventId')
        rest = self.omit(params, ['eventId', 'sportXeventId'])
        isEventScoped = (eventId is not None)
        response = None
        if isEventScoped:
            # the event route takes eventId in the QUERY string, not the body - sign() urlencodes
            # body-less DELETE params
            request = {'eventId': eventId}
            response = await self.sxbetPrivateDeleteOrdersV3Event(self.extend(request, rest))
        else:
            response = await self.sxbetPrivateDeleteOrdersV3All(rest)
        result = self.parse_sxbet_cancel_response(response)
        # both paths paginate their async submission - keep going on the SAME route while more remain
        hasMore = self.safe_bool(self.safe_dict(response, 'data', {}), 'hasMore', False)
        guard = 0
        while((hasMore is True) and (guard < 50)):
            nextResponse = None
            if isEventScoped:
                nextRequest = {'eventId': eventId}
                nextResponse = await self.sxbetPrivateDeleteOrdersV3Event(self.extend(nextRequest, rest))
            else:
                nextResponse = await self.sxbetPrivateDeleteOrdersV3All(rest)
            result = self.array_concat(result, self.parse_sxbet_cancel_response(nextResponse))
            hasMore = self.safe_bool(self.safe_dict(nextResponse, 'data', {}), 'hasMore', False)
            guard = self.sum(guard, 1)
        return result

    def parse_prediction_order(self, order: dict, market: Market = None) -> PredictionOrder:
        """
 @ignore
        parses one raw GET /orders row into a unified prediction order. every sx.bet maker order is a 'buy' of the outcome it bets on(isMakerBettingOutcomeOne selects the side), priced at the maker's own implied probability
        :param dict order: the raw sx.bet order object
        :param dict [market]: the outcome object the order belongs to(resolved from the cache by outcomeId when omitted)
        :returns dict: a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        #
        #     {
        #         "id": "0x112a4f19f20395aefcdb5d26f3f8aab0d96b1aff7ce346c0bfb13d9490340cdf",
        #         "marketHash": "0xbf06c6379c922d8118612d1d7493b20f6df6929437fbf6022904327f9516a2eb",
        #         "userAddress": "0xbcc6D643e4159A75ED1dB4e13330230B82F2AEe5",
        #         "wallet": "0x4361123dbdc1D812fdf7D27045aF358C9C8AA70A",
        #         "isBettingOutcomeOne": true,
        #         "percentageOdds": "40000000000000000000",
        #         "totalBetSize": "3000000",
        #         "remainingSize": "3000000",
        #         "expiry": null,
        #         "status": "ACTIVE",
        #         "inactiveReason": null,
        #         "eventId": "L12003787",
        #         "createdAt": "2026-07-31T18:45:42.636Z",
        #         "updatedAt": "2026-07-31T18:45:42.838Z"
        #     }
        #
        orderId = self.safe_string_2(order, 'id', 'orderId')
        marketHash = self.safe_string(order, 'marketHash', '')
        isBettingOutcomeOne = self.safe_bool(order, 'isBettingOutcomeOne', True)
        outcomeId = marketHash if (isBettingOutcomeOne) else (marketHash + '-2')
        outcomeObj = self.safe_outcome(outcomeId, market)
        oneDenom = '100000000000000000000'
        usdcDecimals = '1000000'
        percentageOdds = self.safe_string(order, 'percentageOdds')
        price = self.parse_number(Precise.string_div(percentageOdds, oneDenom)) if (percentageOdds is not None) else None
        totalBetSize = self.safe_string(order, 'totalBetSize', '0')
        remainingSize = self.safe_string(order, 'remainingSize', totalBetSize)
        filledRaw = Precise.string_sub(totalBetSize, remainingSize)
        amount = self.parse_number(Precise.string_div(totalBetSize, usdcDecimals, 6))
        filled = self.parse_number(Precise.string_div(filledRaw, usdcDecimals, 6))
        remaining = self.parse_number(Precise.string_div(remainingSize, usdcDecimals, 6))
        orderStatus = self.safe_string_upper(order, 'status')
        inactiveReason = self.safe_string_upper(order, 'inactiveReason')
        status = 'open'
        if orderStatus == 'INACTIVE':
            # documented inactiveReason enum: FILLED, USER_REQUESTED, EXPIRED, NO_LIQUIDITY,
            # INSUFFICIENT_BALANCE, EVENT_LIFECYCLE, MARKET_HALTED, HEARTBEAT_TIMEOUT, SYSTEM
            if inactiveReason == 'FILLED':
                status = 'closed'
            elif inactiveReason == 'EXPIRED':
                status = 'expired'
            else:
                status = 'canceled'
        timestamp = self.parse8601(self.safe_string(order, 'createdAt'))
        return self.safe_prediction_order({
            'id': orderId,
            'clientOrderId': self.safe_string(order, 'clientOrderId'),
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'lastTradeTimestamp': self.parse8601(self.safe_string(order, 'updatedAt')),
            'status': status,
            'type': 'limit',
            'timeInForce': None,
            'side': 'buy',
            'price': price,
            'average': None,
            'amount': amount,
            'filled': filled,
            'remaining': remaining,
            'cost': None,
            'fee': None,
            'reduceOnly': None,
            'postOnly': None,
            'trades': [],
            'outcome': self.safe_string(outcomeObj, 'outcome'),
            'outcomeId': self.safe_string(outcomeObj, 'outcomeId', outcomeId),
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string(outcomeObj, 'market'),
            'info': order,
        }, outcomeObj)

    def clamp_sxbet_per_page(self, limit: Int) -> Int:
        """
 @ignore
        clamps a user limit to the venue's hard perPage ceiling - every v3 listing route rejects perPage above 100, and deeper history is reachable through the nextKey cursor instead
        :param int limit: the user-requested limit
        :returns int: the limit clamped to at most 100
        """
        maxPerPage = 100
        if limit is None:
            return None
        return maxPerPage if (limit > maxPerPage) else limit

    async def fetch_open_orders(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> list[PredictionOrder]:
        """
        fetches the account's resting maker orders via the api-key-authenticated GET /orders-v3 (the route is hardcoded to ACTIVE orders and scoped to the key's account)

        https://docs.sx.bet/api-reference/get-orders-v3

        :param str [outcome]: unified outcome or outcomeId — narrows to that outcome's market
        :param int [since]: applied client-side(the route has no date filter; rows carry createdAt)
        :param int [limit]: the maximum number of orders to return(server-side perPage, max 100, default 50)
        :param dict [params]: extra parameters specific to the exchange API endpoint(e.g. eventId, sortBy, sortAsc, nextKey)
        :returns dict[]: a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        request = {}
        outcomeObj = None
        if outcome is not None:
            await self.load_outcome(outcome)
            outcomeObj = self.outcome(outcome)
            request['marketHash'] = self.safe_string(outcomeObj['info'], 'marketHash')
        if limit is not None:
            request['perPage'] = self.clamp_sxbet_per_page(limit)
        response = await self.sxbetPrivateGetOrdersV3(self.extend(request, params))
        data = self.safe_dict(response, 'data', {})
        rawOrders = self.safe_list(data, 'orders', [])
        return self.parse_prediction_orders(rawOrders, outcomeObj, since, limit)

    async def fetch_orders(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> list[PredictionOrder]:
        """
        fetches the account's maker orders. sx.bet's GET /orders-v3 listing is hardcoded to ACTIVE orders — filled/cancelled/expired orders leave the listing permanently(their history is only reconstructable from fills), so self returns the same set that fetchOpenOrders returns

        https://docs.sx.bet/api-reference/get-orders-v3

        :param str [outcome]: unified outcome or outcomeId — narrows to that outcome's market
        :param int [since]: applied client-side(the route has no date filter; rows carry createdAt)
        :param int [limit]: the maximum number of orders to return(server-side perPage, max 100, default 50)
        :param dict [params]: extra parameters specific to the exchange API endpoint(e.g. eventId, sortBy, sortAsc, nextKey)
        :returns dict[]: a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        return await self.fetch_open_orders(outcome, since, limit, params)

    async def fetch_order(self, id: Str, outcome: Str = None, params={}) -> PredictionOrder:
        """
        fetches a single maker order by its order hash - unlike the listing, GET /orders-v3/{orderId} also serves filled, cancelled and expired orders while they still exist. a missing or foreign id 404s with 'Order not found', surfaced through handleErrors's OrderNotFound mapping

        https://docs.sx.bet/api-reference/get-order-v3

        :param str id: the order hash
        :param str [outcome]: unified outcome or outcomeId(labelling hint only, the request needs just the id)
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        self.check_required_credentials()
        if id is None:
            raise ArgumentsRequired(self.id + ' fetchOrder() requires an id argument')
        outcomeObj = None
        if outcome is not None:
            await self.load_outcome(outcome)
            outcomeObj = self.outcome(outcome)
        request = {'orderId': id}
        response = await self.sxbetPrivateGetOrdersV3OrderId(self.extend(request, params))
        data = self.safe_dict(response, 'data', {})
        row = self.safe_dict(data, 'order', data)
        return self.parse_prediction_order(row, outcomeObj)

    async def fetch_trades(self, outcome: Str, since: Int = None, limit: Int = None, params={}) -> list[PredictionTrade]:
        """
        fetches the public trade tape of one outcome's market — every bettor's settled and in-flight bets on that market. the venue requires the trades listing to be scoped, so the outcome argument is mandatory

        https://docs.sx.bet/api-reference/get-trades-v3-public

        :param str outcome: unified outcome or outcomeId
        :param int [since]: timestamp in ms of the earliest trade to return — applied client-side over the newest page(the public tape serves newest-first and has no date filter; older pages are reachable through params.nextKey)
        :param int [limit]: the maximum number of trades to return(server-side perPage, max 100, default 50)
        :param dict [params]: extra parameters specific to the exchange API endpoint(e.g. eventId, nextKey)
        :returns dict[]: a list of [prediction trade structures](https://docs.ccxt.com/#/?id=prediction-trade-structure)
        """
        if outcome is None:
            raise ArgumentsRequired(self.id + ' fetchTrades() requires an outcome argument - the venue requires the trades listing to be scoped')
        await self.load_outcome(outcome)
        outcomeObj = self.outcome(outcome)
        request = {'marketHash': self.safe_string(outcomeObj['info'], 'marketHash')}
        if limit is not None:
            request['perPage'] = self.clamp_sxbet_per_page(limit)
        response = await self.sxbetPublicGetTradesV3Public(self.extend(request, params))
        data = self.safe_dict(response, 'data', {})
        rawTrades = self.safe_list(data, 'trades', [])
        trades = []
        rawTradesLength = len(rawTrades)
        for i in range(0, rawTradesLength):
            trades.append(self.parse_sxbet_v3_public_trade(rawTrades[i]))
        # the venue serves the tape newest-first - the unified contract is ascending by timestamp
        trades = self.sort_by(trades, 'timestamp')
        sym = self.safe_string(outcomeObj, 'outcome')
        return self.filter_by_value_since_limit(trades, 'outcome', sym, since, limit, 'timestamp', True)

    async def fetch_my_trades(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> list[PredictionTrade]:
        """
        fetches the account's fills (matched legs of its own orders, both taker and maker side) via the api-key-authenticated GET /fills-v3

        https://docs.sx.bet/api-reference/get-fills-v3

        :param str [outcome]: unified outcome or outcomeId — narrows to that outcome's market and drops the opposite side's legs
        :param int [since]: timestamp in ms of the earliest fill to fetch(server-side startDate)
        :param int [limit]: the maximum number of fills to return(server-side perPage, max 100, default 50)
        :param dict [params]: extra parameters specific to the exchange API endpoint(e.g. tradeId, orderId, endDate, sortAsc, nextKey)
        :returns dict[]: a list of [prediction trade structures](https://docs.ccxt.com/#/?id=prediction-trade-structure)
        """
        self.check_required_credentials()
        request = {}
        outcomeObj = None
        if outcome is not None:
            await self.load_outcome(outcome)
            outcomeObj = self.outcome(outcome)
        if since is not None:
            request['startDate'] = self.iso8601(since)
        if (limit is not None) and (outcome is None):
            # with an outcome filter the rows are narrowed client-side - a server-side page
            # size would truncate the page before the filter and under-fill the result
            request['perPage'] = self.clamp_sxbet_per_page(limit)
        response = await self.sxbetPrivateGetFillsV3(self.extend(request, params))
        data = self.safe_dict(response, 'data', {})
        rawFills = self.safe_list(data, 'fills', [])
        trades = []
        rawFillsLength = len(rawFills)
        for i in range(0, rawFillsLength):
            trades.append(self.parse_sxbet_v3_fill(rawFills[i]))
        trades = self.sort_by(trades, 'timestamp')
        sym = self.safe_string(outcomeObj, 'outcome') if (outcomeObj is not None) else None
        return self.filter_by_value_since_limit(trades, 'outcome', sym, since, limit, 'timestamp', True)

    def parse_sxbet_v3_fill(self, fill: dict, market: Market = None) -> PredictionTrade:
        """
 @ignore
        parses one GET /fills-v3 row into a unified trade - each fill is one maker/taker match of the wallet's own order
        :param dict fill: the raw fill row
        :param dict [market]: the outcome object labelling hint
        :returns dict: a [prediction trade structure](https://docs.ccxt.com/#/?id=prediction-trade-structure)
        """
        #
        #     {
        #         "id": "0xec8e...:0x02d2...:0x1fec...",
        #         "matchId": "0xec8e...", "orderId": "0x02d2...", "tradeId": "0xac6b...",
        #         "marketHash": "0x1fec...", "isParlay": false,
        #         "userAddress": "0xbcc6...", "wallet": "0x4361...",
        #         "fillAmount": "333334", "fillOdds": "40000000000000000000",
        #         "returnAmount": "833335", "isBettingOutcomeOne": false,
        #         "isMaker": false, "status": "LOCKED", "txHash": "0x6430...",
        #         "ceRefundAmount": "0", "ceRefundFeeAmount": "0",
        #         "usedBetCredits": false, "createdAt": "2026-07-31T18:47:09.586Z",
        #         "updatedAt": "2026-07-31T18:47:12.837Z", "settlement": null
        #     }
        #
        marketHash = self.safe_string(fill, 'marketHash', '')
        isBettingOutcomeOne = self.safe_bool(fill, 'isBettingOutcomeOne', True)
        outcomeId = marketHash if (isBettingOutcomeOne) else (marketHash + '-2')
        outcomeObj = self.safe_outcome(outcomeId, market)
        oneDenom = '100000000000000000000'
        usdcDecimals = '1000000'
        fillOdds = self.safe_string(fill, 'fillOdds')
        price = self.parse_number(Precise.string_div(fillOdds, oneDenom)) if (fillOdds is not None) else None
        fillAmount = self.safe_string(fill, 'fillAmount', '0')
        amount = self.parse_number(Precise.string_div(fillAmount, usdcDecimals, 6))
        timestamp = self.parse8601(self.safe_string(fill, 'createdAt'))
        # isMaker: true when the account's order was the resting one - absent on older rows
        isMaker = self.safe_bool(fill, 'isMaker')
        takerOrMaker = None
        if isMaker is not None:
            takerOrMaker = 'maker' if (isMaker is True) else 'taker'
        return self.safe_prediction_trade({
            'id': self.safe_string(fill, 'id'),
            'info': fill,
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'outcome': self.safe_string(outcomeObj, 'outcome'),
            'outcomeId': self.safe_string(outcomeObj, 'outcomeId', outcomeId),
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string(outcomeObj, 'market'),
            'order': self.safe_string(fill, 'orderId'),
            'type': None,
            'side': 'buy',
            'takerOrMaker': takerOrMaker,
            'price': price,
            'amount': amount,
            'cost': amount,
            'fee': None,
        }, market)

    async def fetch_balance(self, params={}) -> Balances:
        """
        fetches the account's order-spendable proxy balance from GET /user/balance-v3 - v3 trading capital sits inside the obv3 proxy wallet (funded via approve()), and GTC posting is checked against availableAmount. free is the spendable availableAmount, used the escrowedAmount locked behind open bets

        https://docs.sx.bet/api-reference/get-user-balance-v3

        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [balance structure](https://docs.ccxt.com/#/?id=balance-structure)
        """
        obv3 = await self.load_sx_obv3_metadata()
        activeAsset = self.safe_dict(obv3, 'activeAsset', {})
        usdcAddress = self.safe_string_lower(activeAsset, 'baseToken', '')
        response = await self.sxbetPrivateGetUserBalanceV3(params)
        #
        #     { "status": "success", "data": { "balances": [ {
        #         "userAddress": "0xC3f4...", "wallet": "0x19D1...",
        #         "tokenAddress": "0x1BC6...", "escrowAddress": "0x0Db3...",
        #         "availableAmount": "0", "pendingAvailableAmount": "0",
        #         "escrowedAmount": "0", "pendingEscrowAmount": "0" } ] } }
        #
        data = self.safe_dict(response, 'data', {})
        balances = self.safe_list(data, 'balances', [])
        result = {'info': response}
        usdcDecimals = '1000000'
        balancesLength = len(balances)
        for i in range(0, balancesLength):
            row = balances[i]
            tokenAddress = self.safe_string_lower(row, 'tokenAddress', '')
            # every sxbet market is denominated in the active base token, surfaced under 'USDC';
            # rows of any other token keep their contract address for the code
            code = self.safe_string(row, 'tokenAddress', '')
            if tokenAddress == usdcAddress:
                code = 'USDC'
            free = Precise.string_div(self.safe_string(row, 'availableAmount', '0'), usdcDecimals, 6)
            used = Precise.string_div(self.safe_string(row, 'escrowedAmount', '0'), usdcDecimals, 6)
            result[code] = {
                'free': self.parse_number(free),
                'used': self.parse_number(used),
                'total': self.parse_number(Precise.string_add(free, used)),
            }
        return self.safe_balance(result)

    async def fetch_positions(self, outcomes: Strings = None, params={}) -> list[PredictionPosition]:
        """
        fetches the account's open positions from the venue's per-market aggregates(GET /positions-v3, MATCHED and LOCKED bets by default; override with params.status - the enum is MATCHED, LOCKED, SETTLED, FAILED, and pnl is populated only when the filter is exclusively SETTLED). contracts is the total stake at risk, entryPrice the blended implied probability of the market's best-case outcome

        https://docs.sx.bet/api-reference/get-positions-v3

        :param str[] [outcomes]: filter by unified outcomes or outcomeIds
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction position structures](https://docs.ccxt.com/#/?id=prediction-position-structure)
        """
        self.check_required_credentials()
        # copy to a plain list so the transpilers and the strict null checks see one shape
        outcomesList = [] if (outcomes is None) else outcomes
        outcomesLength = len(outcomesList)
        wantedMarkets = {}
        if outcomesLength > 0:
            await self.load_outcomes(outcomesList)
            for i in range(0, outcomesLength):
                outcomeObj = self.outcome(outcomesList[i])
                hash = self.safe_string(outcomeObj['info'], 'marketHash', '')
                wantedMarkets[hash] = True
        # open exposure lives in MATCHED and LOCKED bets - the venue requires an explicit status
        # list drawn from MATCHED, LOCKED, SETTLED, FAILED
        request = {'status': self.safe_string(params, 'status', 'MATCHED,LOCKED')}
        rest = self.omit(params, ['status'])
        response = await self.sxbetPrivateGetPositionsV3(self.extend(request, rest))
        data = self.safe_dict(response, 'data', {})
        rawPositions = self.safe_list(data, 'positions', [])
        result = []
        rawPositionsLength = len(rawPositions)
        for i in range(0, rawPositionsLength):
            raw = rawPositions[i]
            marketHash = self.safe_string(raw, 'marketHash', '')
            if outcomesLength > 0:
                if self.safe_bool(wantedMarkets, marketHash) is None:
                    continue
            result.append(self.parse_sxbet_v3_position(raw))
        return result

    def parse_sxbet_v3_position(self, raw: dict) -> PredictionPosition:
        """
 @ignore
        parses one GET /positions-v3 row(per-market aggregate) into a unified position on the market's best-case outcome
        :param dict raw: the raw position row
        :returns dict: a [prediction position structure](https://docs.ccxt.com/#/?id=prediction-position-structure)
        """
        #
        #     {
        #         "marketHash": "0x8120...", "market": {...},
        #         "totalStake": "300000000", "maxWin": "200000000", "maxLoss": "-100000000",
        #         "isOutcomeOneMaxWin": true,
        #         "odds": {"outcomeOne": "40000000000000000000", "outcomeTwo": "50000000000000000000"},
        #         "pnl": null, "tradeCount": 2,
        #         "betTime": "2026-03-13T21:14:02.118Z", "updatedAt": "2026-03-13T22:03:41.502Z"
        #     }
        #
        marketHash = self.safe_string(raw, 'marketHash', '')
        isOutcomeOneMaxWin = self.safe_bool(raw, 'isOutcomeOneMaxWin', True)
        outcomeId = marketHash if (isOutcomeOneMaxWin) else (marketHash + '-2')
        outcomeObj = self.safe_outcome(outcomeId)
        oneDenom = '100000000000000000000'
        usdcDecimals = '1000000'
        odds = self.safe_dict(raw, 'odds', {})
        ownOdds = self.safe_string(odds, 'outcomeOne') if (isOutcomeOneMaxWin) else self.safe_string(odds, 'outcomeTwo')
        entryPrice = self.parse_number(Precise.string_div(ownOdds, oneDenom)) if (ownOdds is not None) else None
        totalStake = self.safe_string(raw, 'totalStake', '0')
        pnl = self.safe_string(raw, 'pnl')
        timestamp = self.parse8601(self.safe_string(raw, 'betTime'))
        return self.safe_prediction_position({
            'outcome': self.safe_string(outcomeObj, 'outcome'),
            'outcomeId': self.safe_string(outcomeObj, 'outcomeId', outcomeId),
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string(outcomeObj, 'market'),
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'lastUpdateTimestamp': self.parse8601(self.safe_string(raw, 'updatedAt')),
            'contracts': self.parse_number(Precise.string_div(totalStake, usdcDecimals, 6)),
            'entryPrice': entryPrice,
            'markPrice': None,
            'unrealizedPnl': None,
            'realizedPnl': self.parse_number(Precise.string_div(pnl, usdcDecimals, 6)) if (pnl is not None) else None,
            'side': 'long',
            'info': raw,
        })

    async def fetch_settlements(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> list[PredictionSettlement]:
        """
        fetches the account's settled bets — each settled GET /trades-v3 row becomes one settlement with the resolved winner, the payout (stake / odds when won, the stake back when the market voided, zero when lost) and the realized pnl

        https://docs.sx.bet/api-reference/get-trades-v3

        :param str [outcome]: filter to a single unified outcome or outcomeId
        :param int [since]: timestamp in ms of the earliest settlement to fetch(server-side startDate on the bet time)
        :param int [limit]: the maximum number of settlements to fetch(server-side perPage, max 100, default 50)
        :param dict [params]: extra parameters specific to the exchange API endpoint(e.g. eventId, endDate, sortAsc, nextKey)
        :returns dict[]: a list of prediction settlement structures
        """
        self.check_required_credentials()
        request = {'status': 'SETTLED'}
        wantedOutcomeId = None
        if outcome is not None:
            await self.load_outcome(outcome)
            outcomeObj = self.outcome(outcome)
            request['marketHash'] = self.safe_string(outcomeObj['info'], 'marketHash', '')
            wantedOutcomeId = self.safe_string(outcomeObj, 'outcomeId')
        if since is not None:
            request['startDate'] = self.iso8601(since)
        if (limit is not None) and (outcome is None):
            # the outcome scope narrows rows client-side (the venue filter is per market, not
            # per side) - a server-side page size would under-fill the filtered result
            request['perPage'] = self.clamp_sxbet_per_page(limit)
        response = await self.sxbetPrivateGetTradesV3(self.extend(request, params))
        data = self.safe_dict(response, 'data', {})
        rawTrades = self.safe_list(data, 'trades', [])
        result = []
        rawTradesLength = len(rawTrades)
        for i in range(0, rawTradesLength):
            settlement = self.parse_settlement(rawTrades[i])
            if (wantedOutcomeId is None) or (self.safe_string(settlement, 'outcomeId') == wantedOutcomeId):
                result.append(settlement)
        return result

    def parse_settlement(self, trade: dict, market: Market = None) -> object:
        """
 @ignore
        parses one settled GET /trades-v3 row into the unified prediction settlement shape. the raw `outcome` field carries the winner, 1 or 2, with 0 for a voided market, and the full payout of a winning bet is stake / odds(the implied probability the bettor received)
        :param dict trade: the raw settled sx.bet trade
        :param dict [market]: a resolved outcome/market hint
        :returns dict: a prediction settlement structure
        """
        #
        #     TradeV3 row with a settlement sub-object:
        #     {
        #         "tradeId": "0xac6b...", "marketHash": "0x1fec...",
        #         "isBettingOutcomeOne": false, "totalStake": "1000000", "totalReturn": "2500000",
        #         "weightedAverageOdds": "40000000000000000000", "status": "SETTLED",
        #         "betTime": "2026-07-31T18:47:09.577Z",
        #         "settlement": { "outcome": 2, "settleReturnAmount": "2500000",
        #                         "settleFeeAmount": "0", "settleCeRefundAmount": "0",
        #                         "settleDate": "2026-08-01T00:10:00.000Z" }
        #     }
        #
        marketHash = self.safe_string(trade, 'marketHash', '')
        isBettingOutcomeOne = self.safe_bool(trade, 'isBettingOutcomeOne', True)
        outcomeId = marketHash if (isBettingOutcomeOne) else (marketHash + '-2')
        outcomeObj = self.safe_outcome(outcomeId, market)
        settlement = self.safe_dict(trade, 'settlement', {})
        winner = self.safe_integer(settlement, 'outcome')
        isVoid = (winner == 0)
        heldNumber = 1 if (isBettingOutcomeOne) else 2
        won = None
        if (winner is not None) and not isVoid:
            won = (winner == heldNumber)
        usdcDecimals = '1000000'
        stake = Precise.string_div(self.safe_string(trade, 'totalStake', '0'), usdcDecimals, 6)
        payout = Precise.string_div(self.safe_string(settlement, 'settleReturnAmount', '0'), usdcDecimals, 6)
        pnl = Precise.string_sub(payout, stake)
        # resolve the winning side's human label through the cached market when available
        resultLabel = None
        if isVoid:
            resultLabel = 'VOID'
        elif winner is not None:
            info = self.safe_dict(outcomeObj, 'info', {})
            labelKey = 'outcomeOneName' if (winner == 1) else 'outcomeTwoName'
            resultLabel = self.safe_string(info, labelKey, self.number_to_string(winner))
        timestamp = self.parse8601(self.safe_string(settlement, 'settleDate'))
        settlePrice = None
        if won is not None:
            settlePrice = 1 if (won is True) else 0
        return {
            'info': trade,
            'id': self.safe_string(trade, 'tradeId'),
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'outcome': self.safe_string(outcomeObj, 'outcome', outcomeId),
            'outcomeId': self.safe_string(outcomeObj, 'outcomeId', outcomeId),
            'market': self.safe_string(outcomeObj, 'market'),
            'event': None,
            'result': resultLabel,
            'won': won,
            'amount': self.parse_number(stake),
            'price': settlePrice,
            'cost': self.parse_number(stake),
            'payout': self.parse_number(payout),
            'pnl': self.parse_number(pnl),
        }

    async def fetch_sxbet_book_snapshot(self, marketHash: Str) -> dict:
        """
 @ignore
        fetches the v3 aggregated order book snapshot of one market({outcomeOne, outcomeTwo, version} price levels)

        https://docs.sx.bet/api-reference/get-orderbook-snapshot

        :param str marketHash: the market hash
        :returns dict: the raw snapshot data
        """
        request = {'marketHash': marketHash}
        response = await self.sxbetPublicGetOrderbookV3Snapshot(request)
        return self.safe_dict(response, 'data', {})

    async def fetch_ticker(self, outcome: Str, params={}) -> PredictionTicker:
        """
        fetches the current best resting odds for a single sx.bet outcome. sx.bet is a peer-to-peer odds book(no matched-trade tape or candles), so bid/ask are the best(highest) percentageOdds resting on self outcome's own side and its mirror (1 - best percentageOdds resting on the opposite outcome)

        https://docs.sx.bet/api-reference/get-orderbook-snapshot

        :param str outcome: unified outcome handle or outcomeId(marketHash or marketHash + '-2')
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction ticker structure](https://docs.ccxt.com/#/?id=prediction-ticker-structure)
        """
        await self.load_outcome(outcome)
        outcomeObj = self.outcome(outcome)
        marketHash = self.safe_string(outcomeObj['info'], 'marketHash')
        # the book snapshot is public and carries the same top of book - the batched best-odds
        # route needs an apiKey, so it only pays off for the multi-market path
        snapshot = await self.fetch_sxbet_book_snapshot(marketHash)
        raw = self.parse_sxbet_snapshot_best_odds(snapshot)
        return self.parse_prediction_ticker(raw, outcomeObj)

    async def fetch_sxbet_best_odds(self, marketHashes: list[str], params={}) -> list[object]:
        """
 @ignore
        fetches the best resting level of both sides for a set of markets in one request

        https://docs.sx.bet/api-reference/get-best-odds-v3

        :param str[] marketHashes: the market hashes to query, at most 100 per call
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]} the raw bestOdds rows({marketHash, outcomeOne, outcomeTwo:)
        """
        request = {'marketHashes': ','.join(marketHashes)}
        response = await self.sxbetPrivateGetOrdersV3OddsBest(self.extend(request, params))
        data = self.safe_dict(response, 'data', {})
        return self.safe_list(data, 'bestOdds', [])

    def parse_sxbet_snapshot_best_odds(self, snapshot: dict) -> dict:
        """
 @ignore
        reduces a v3 book snapshot to the legacy best-odds shape the ticker parser consumes({marketHash, outcomeOne: {percentageOdds}, outcomeTwo: {percentageOdds}})
        :param dict snapshot: the raw snapshot data
        :returns dict: the best-odds shaped dict
        """
        # levels arrive sorted best-first; the best level's odds mirror the v2 best-odds fields
        oneLevels = self.safe_list(snapshot, 'outcomeOne', [])
        twoLevels = self.safe_list(snapshot, 'outcomeTwo', [])
        bestOne = self.safe_dict(oneLevels, 0, {})
        bestTwo = self.safe_dict(twoLevels, 0, {})
        return {
            'marketHash': self.safe_string(snapshot, 'marketHash'),
            'outcomeOne': {'percentageOdds': self.safe_string(bestOne, 'percentageOdds')},
            'outcomeTwo': {'percentageOdds': self.safe_string(bestTwo, 'percentageOdds')},
        }

    async def fetch_tickers(self, outcomes: Strings = None, params={}) -> PredictionTickers:
        """
        fetches the current best resting odds for multiple sx.bet outcomes, one book snapshot per market

        https://docs.sx.bet/api-reference/get-orderbook-snapshot

        :param str[] outcomes: unified outcomes - required: sx.bet has thousands of markets and no endpoint returning all of them at once
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a dictionary of [prediction ticker structures](https://docs.ccxt.com/#/?id=prediction-ticker-structure) indexed by outcome
        """
        if outcomes is None:
            raise ArgumentsRequired(self.id + ' fetchTickers() requires an outcomes argument - sx.bet has thousands of markets and serves best odds per market list')
        outcomesList = outcomes
        outcomesLength = len(outcomesList)
        hashesOrder = []
        seenHashes = {}
        for i in range(0, outcomesLength):
            await self.load_outcome(outcomesList[i])
            outcomeObj = self.outcome(outcomesList[i])
            marketHash = self.safe_string(outcomeObj['info'], 'marketHash', '')
            if self.safe_bool(seenHashes, marketHash) is None:
                seenHashes[marketHash] = True
                hashesOrder.append(marketHash)
        # both outcomes of a market share one row, and the venue takes at most 100 hashes per call.
        # the batched route is apiKey-authenticated, so fall back to the public per-market
        # snapshots when the instance carries no credentials
        rowsByHash = {}
        hashesLength = len(hashesOrder)
        hasApiKey = not self.is_empty_string(self.apiKey)
        if not hasApiKey:
            for i in range(0, hashesLength):
                marketHash = hashesOrder[i]
                snapshot = await self.fetch_sxbet_book_snapshot(marketHash)
                rowsByHash[marketHash] = self.parse_sxbet_snapshot_best_odds(snapshot)
            return self.parse_sxbet_tickers_by_hash(outcomesList, rowsByHash)
        chunkSize = self.safe_integer(self.options, 'bestOddsBatchSize', 100)
        chunkCount = self.parse_to_int(self.sum(hashesLength, chunkSize - 1) / chunkSize)
        for c in range(0, chunkCount):
            start = c * chunkSize
            end = start + chunkSize
            if end > hashesLength:
                end = hashesLength
            chunk = self.array_slice(hashesOrder, start, end)
            rows = await self.fetch_sxbet_best_odds(chunk, params)
            rowsLength = len(rows)
            for j in range(0, rowsLength):
                row = rows[j]
                rowHash = self.safe_string(row, 'marketHash')
                if rowHash is not None:
                    rowsByHash[rowHash] = row
        return self.parse_sxbet_tickers_by_hash(outcomesList, rowsByHash)

    def parse_sxbet_tickers_by_hash(self, outcomesList: list[str], rowsByHash: dict) -> PredictionTickers:
        """
 @ignore
        assembles the unified tickers dict from best-odds shaped rows keyed by market hash
        :param str[] outcomesList: the requested unified outcomes
        :param dict rowsByHash: best-odds rows indexed by market hash
        :returns dict: a dictionary of [prediction ticker structures](https://docs.ccxt.com/#/?id=prediction-ticker-structure) indexed by outcome
        """
        result = {}
        outcomesLength = len(outcomesList)
        for i in range(0, outcomesLength):
            outcomeObj = self.outcome(outcomesList[i])
            marketHash = self.safe_string(outcomeObj['info'], 'marketHash', '')
            raw = self.safe_dict(rowsByHash, marketHash)
            if raw is None:
                continue
            ticker = self.parse_prediction_ticker(raw, outcomeObj)
            sym = self.safe_string(ticker, 'outcome')
            if sym is not None:
                result[sym] = ticker
        return result

    def parse_prediction_ticker(self, raw: dict, market: Market = None) -> PredictionTicker:
        """
 @ignore
        parses one /orders/odds/best entry into a unified ticker for one side of the market
        :param dict raw: one bestOdds entry({marketHash, baseToken, outcomeOne: {percentageOdds, updatedAt}, outcomeTwo: {...}})
        :param dict [market]: the outcome object the ticker belongs to
        :returns dict: a [prediction ticker structure](https://docs.ccxt.com/#/?id=prediction-ticker-structure)
        """
        #
        #     {
        #         "marketHash": "0x48ce0ef287c59fb3bb3ca6deb80de0d5791eb761ea8e16d6f03c8e93ef28d1de",
        #         "baseToken": "0x6629Ce1Cf35Cc1329ebB4F63202F3f197b3F050B",
        #         "outcomeOne": { "percentageOdds": "50250000000000000000", "updatedAt": 1785697831141 },
        #         "outcomeTwo": { "percentageOdds": "48000000000000000000", "updatedAt": 1785699649869 }
        #     }
        #
        marketAny = market
        outcomeObj = self.safe_outcome(self.safe_string(marketAny, 'outcome'), marketAny)
        outcomeId = self.safe_string(outcomeObj, 'outcomeId')
        marketHash = self.safe_string(raw, 'marketHash')
        isOutcomeOne = (outcomeId == marketHash)
        outcomeOneOdds = self.safe_dict(raw, 'outcomeOne', {})
        outcomeTwoOdds = self.safe_dict(raw, 'outcomeTwo', {})
        ownOdds = outcomeOneOdds if (isOutcomeOne) else outcomeTwoOdds
        oppositeOdds = outcomeTwoOdds if (isOutcomeOne) else outcomeOneOdds
        # percentageOdds is the maker's own implied probability * 1e20 (sx.bet protocol format);
        # the opposite side's best resting maker mirrors into this outcome's ask via 1 - p
        oneDenom = '100000000000000000000'
        ownPercentage = self.safe_string(ownOdds, 'percentageOdds')
        oppositePercentage = self.safe_string(oppositeOdds, 'percentageOdds')
        bid = self.parse_number(Precise.string_div(ownPercentage, oneDenom)) if (ownPercentage is not None) else None
        ask = self.parse_number(Precise.string_sub('1', Precise.string_div(oppositePercentage, oneDenom))) if (oppositePercentage is not None) else None
        updatedAt = self.safe_integer(ownOdds, 'updatedAt')
        now = self.milliseconds()
        timestamp = updatedAt if (updatedAt is not None) else now
        average = None
        if (bid is not None) and (ask is not None):
            average = self.parse_number(Precise.string_div(Precise.string_add(self.number_to_string(bid), self.number_to_string(ask)), '2'))
        return self.safe_prediction_ticker({
            'outcome': self.safe_string(outcomeObj, 'outcome'),
            'outcomeId': outcomeId,
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string(outcomeObj, 'market'),
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'high': None,
            'low': None,
            'bid': bid,
            'bidVolume': None,
            'ask': ask,
            'askVolume': None,
            'open': None,
            'close': bid,
            'last': bid,
            'change': None,
            'percentage': None,
            'average': average,
            'baseVolume': None,
            'quoteVolume': None,
            'info': raw,
        }, market)

    async def fetch_order_book(self, outcome: Str, limit: Int = None, params={}) -> PredictionOrderBook:
        """
        fetches the resting maker order book for a single sx.bet outcome. bids are maker orders already betting on self outcome(priced at each maker's own implied probability, sized by their remaining stake); asks mirror the opposite outcome's maker orders(price = 1 - their implied probability, sized by how much a taker could bet against them, per sx.bet's remaining-taker-space formula) — the same YES/NO-style mirrored construction used across self codebase's other binary prediction venues

        https://docs.sx.bet/api-reference/get-orderbook-snapshot

        :param str outcome: unified outcome handle or outcomeId
        :param int [limit]: the maximum number of bids/asks to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction order book structure](https://docs.ccxt.com/#/?id=prediction-order-book-structure)
        """
        await self.load_outcome(outcome)
        outcomeObj = self.outcome(outcome)
        marketHash = self.safe_string(outcomeObj['info'], 'marketHash')
        outcomeId = self.safe_string(outcomeObj, 'outcomeId')
        isOutcomeOne = (outcomeId == marketHash)
        request = {'marketHash': marketHash}
        response = await self.sxbetPublicGetOrderbookV3Snapshot(self.extend(request, params))
        snapshot = self.safe_dict(response, 'data', {})
        sides = self.parse_sxbet_v3_book_sides(snapshot, isOutcomeOne)
        sortedBids = self.safe_list(sides, 'bids', [])
        sortedAsks = self.safe_list(sides, 'asks', [])
        if limit is not None:
            bidsLength = len(sortedBids)
            bidsEnd = limit
            if bidsEnd > bidsLength:
                bidsEnd = bidsLength
            sortedBids = self.array_slice(sortedBids, 0, bidsEnd)
            asksLength = len(sortedAsks)
            asksEnd = limit
            if asksEnd > asksLength:
                asksEnd = asksLength
            sortedAsks = self.array_slice(sortedAsks, 0, asksEnd)
        timestamp = self.milliseconds()
        return self.safe_prediction_order_book({
            'outcome': self.safe_string(outcomeObj, 'outcome', outcome),
            'bids': sortedBids,
            'asks': sortedAsks,
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'nonce': None,
        }, outcomeObj)

    def parse_sxbet_v3_book_sides(self, snapshot: dict, isOutcomeOne: bool) -> dict:
        """
 @ignore
        converts a v3 aggregated book snapshot into sorted [price, amount] bid/ask levels for one outcome; shared by fetchOrderBook and the websocket book handler
        :param dict snapshot: the raw snapshot({outcomeOne, outcomeTwo} level lists of {percentageOdds, size})
        :param boolean isOutcomeOne: whether the book is built for the market's outcome one
        :returns dict: a dict with sorted 'bids' and 'asks' lists
        """
        oneDenom = '100000000000000000000'
        usdcDecimals = '1000000'
        ownLevels = self.safe_list(snapshot, 'outcomeOne', []) if (isOutcomeOne) else self.safe_list(snapshot, 'outcomeTwo', [])
        oppositeLevels = self.safe_list(snapshot, 'outcomeTwo', []) if (isOutcomeOne) else self.safe_list(snapshot, 'outcomeOne', [])
        bids = []
        ownLevelsLength = len(ownLevels)
        for i in range(0, ownLevelsLength):
            level = ownLevels[i]
            percentageOdds = self.safe_string(level, 'percentageOdds')
            size = self.safe_string(level, 'size', '0')
            price = self.parse_number(Precise.string_div(percentageOdds, oneDenom))
            amount = self.parse_number(Precise.string_div(size, usdcDecimals, 6))
            bids.append([price, amount])
        asks = []
        oppositeLevelsLength = len(oppositeLevels)
        for i in range(0, oppositeLevelsLength):
            level = oppositeLevels[i]
            percentageOdds = self.safe_string(level, 'percentageOdds')
            size = self.safe_string(level, 'size', '0')
            # the opposite side's resting stake mirrors into this outcome's ask - the price is the
            # complement, and the takeable amount follows the remaining-taker-space formula
            price = self.parse_number(Precise.string_sub('1', Precise.string_div(percentageOdds, oneDenom)))
            ratio = Precise.string_div(oneDenom, percentageOdds, 12)
            remainingTaker = Precise.string_sub(Precise.string_mul(size, ratio), size)
            amount = self.parse_number(Precise.string_div(remainingTaker, usdcDecimals, 6))
            asks.append([price, amount])
        return {
            'bids': self.sort_by(bids, 0, True),
            'asks': self.sort_by(asks, 0),
        }

    def request_id(self, url: Str) -> float:
        existing = self.safe_value(self.options, 'requestId')
        if existing is None:
            self.options['requestId'] = self.create_safe_dictionary()
        options = self.options['requestId']
        previousValue = self.safe_integer(options, url, 0)
        newValue = self.sum(previousValue, 1)
        if url is not None:
            self.options['requestId'][url] = newValue
        return newValue

    def register_sxbet_ws_request(self, requestId: float, messageHash: str, subscription: str):
        """
 @ignore
        tracks an outgoing Centrifugo connect/subscribe command by its request id, so an error reply can be correlated back to the awaiting future and its registered subscription hash(see handleCentrifugoFrame)
        :param int requestId: the id sent with the command
        :param str messageHash: the future hash the caller awaits
        :param str subscription: the subscription hash registered by watch()('connect' or the channel name)
        """
        existing = self.safe_dict(self.options, 'wsPendingRequests')
        if existing is None:
            self.options['wsPendingRequests'] = self.create_safe_dictionary()
        requestIdString = self.number_to_string(requestId)
        self.options['wsPendingRequests'][requestIdString] = {'messageHash': messageHash, 'subscription': subscription}

    async def fetch_sxbet_realtime_token(self) -> Str:
        """
 @ignore
        fetches the short-lived Centrifugo connection token from the relayer; requires the apiKey credential(X-Api-Key header)

        https://docs.sx.bet/developers/realtime-initialization

        :returns str: the JWT connection token
        """
        if self.apiKey is None:
            raise ArgumentsRequired(self.id + ' websocket streaming requires the apiKey credential - the realtime token endpoint authenticates with the X-Api-Key header')
        response = await self.sxbetPrivateGetUserRealtimeTokenV3ApiKey()
        data = self.safe_dict(response, 'data', {})
        return self.safe_string_2(data, 'token', 'realtimeToken', self.safe_string(response, 'token'))

    async def connect_sxbet_centrifugo(self, url: Str) -> object:
        # Centrifugo requires a token-carrying connect command before any subscribe. This sends it once
        # per connection and resolves when the connect reply arrives (see handleCentrifugoFrame). The base
        # clears client.subscriptions on reconnect, so an absent 'connect' marker means a fresh handshake.
        client = self.client(url)
        connectSent = self.safe_value(client.subscriptions, 'connect')
        if connectSent is None:
            self.options['wsConnected'] = False
            token = await self.fetch_sxbet_realtime_token()
            requestId = self.request_id(url)
            self.register_sxbet_ws_request(requestId, 'centrifugoConnected', 'connect')
            connectMsg = {'connect': {'token': token, 'name': 'ccxt'}, 'id': requestId}
            return await self.watch(url, 'centrifugoConnected', connectMsg, 'connect')
        if self.safe_bool(self.options, 'wsConnected', False):
            # the connect reply already arrived on the current connection - safe to subscribe immediately
            return None
        # connect is in flight, sent by a concurrent subscribe - wait on the shared reply future
        return await client.future('centrifugoConnected')

    async def pong(self, client: Client, message: object = None):
        # Centrifugo server pings are empty frames; reply with the same empty frame to keep the link alive
        await client.send('{}')

    async def subscribe_sxbet_channel(self, messageHash: str, channel: str) -> object:
        url = self.safe_string(self.urls['api'], 'ws')
        # finish the connect handshake first so the subscribe frame follows the connect reply
        await self.connect_sxbet_centrifugo(url)
        requestId = self.request_id(url)
        self.register_sxbet_ws_request(requestId, messageHash, channel)
        subscribeMsg = {'subscribe': {'channel': channel}, 'id': requestId}
        return await self.watch(url, messageHash, subscribeMsg, channel)

    def handle_message(self, client: Client, message: object):
        # Centrifugo packs several commands per frame joined by newlines; a multi-command frame fails the
        # base JSON.parse and arrives here in raw-string form, a single command arrives already parsed
        if isinstance(message, str):
            lines = message.split('\n')
            linesLength = len(lines)
            for i in range(0, linesLength):
                line = lines[i]
                if len(line) > 0:
                    parsed = json.loads(line)
                    self.handle_centrifugo_frame(client, parsed)
            return
        self.handle_centrifugo_frame(client, message)

    def handle_centrifugo_frame(self, client: Client, msg: object):
        keys = list(msg.keys())
        keysLength = len(keys)
        if keysLength == 0:
            self.spawn(self.pong, client, msg)
            return
        requestIdString = self.safe_string(msg, 'id')
        pendingRequests = self.safe_dict(self.options, 'wsPendingRequests', {})
        pendingEntry = self.safe_dict(pendingRequests, requestIdString)
        errorReply = self.safe_dict(msg, 'error')
        if errorReply is not None:
            # a rejected connect (bad or expired realtime token) or subscribe (unauthorized or
            # unknown channel) - fail the awaiting future and clear the subscription hash that
            # watch() registered at send time, so the next call re-sends the command instead
            # of waiting forever behind a marker that will never be served
            error = ExchangeError(self.id + ' ' + self.json(msg))
            if pendingEntry is not None:
                failedHash = self.safe_string(pendingEntry, 'messageHash')
                subscription = self.safe_string(pendingEntry, 'subscription')
                if subscription == 'connect':
                    self.options['wsConnected'] = False
                if (subscription is not None) and (subscription in client.subscriptions):
                    del client.subscriptions[subscription]
                self.options['wsPendingRequests'] = self.omit(pendingRequests, requestIdString)
                client.reject(error, failedHash)
            else:
                # an error that cannot be correlated to one command fails the whole connection
                client.reject(error)
            return
        if pendingEntry is not None:
            # a successful command ack - the tracked request has served its purpose
            self.options['wsPendingRequests'] = self.omit(pendingRequests, requestIdString)
        connectReply = self.safe_dict(msg, 'connect')
        if connectReply is not None:
            # connect acknowledged - unblock connectSxbetCentrifugo so channel subscribes can be sent
            self.options['wsConnected'] = True
            client.resolve(True, 'centrifugoConnected')
            return
        push = self.safe_dict(msg, 'push')
        if push is None:
            return
        channel = self.safe_string(push, 'channel')
        if channel is None:
            return
        pub = self.safe_dict(push, 'pub', {})
        data = self.safe_value(pub, 'data')
        if data is None:
            return
        # rows arrive either shaped like one object or like an array of objects - normalize to a list
        rows = []
        if isinstance(data, list):
            rows = data
        else:
            rows = [data]
        parts = channel.split(':')
        channelType = self.safe_string(parts, 0)
        if channelType == 'orderbook_v3':
            self.handle_order_book(client, rows)
        elif channelType == 'best_odds_v3':
            self.handle_ticker(client, rows)
        elif channelType == 'recent_trades_v3':
            self.handle_trades(client, rows)
        elif channelType == 'account':
            # per-account channels: account:orders_v3_#addr / account:fills_v3_#addr
            if channel.find('orders_v3') >= 0:
                self.handle_order(client, rows)
            elif channel.find('fills_v3') >= 0:
                self.handle_my_trade(client, rows)

    async def watch_order_book(self, outcome: str, limit: Int = None, params={}) -> PredictionOrderBook:
        """
        streams the order book of an outcome - the v3 channel publishes the entire aggregated book on every update with a monotonic version, so each message replaces the held book

        https://docs.sx.bet/api-reference/channel-orderbook-v3

        :param str outcome: unified outcome or outcome token id
        :param int [limit]: the maximum number of order book entries to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction order book structure](https://docs.ccxt.com/#/?id=prediction-order-book-structure)
        """
        await self.load_outcome(outcome)
        outcomeObj = self.outcome(outcome)
        sym = self.safe_string(outcomeObj, 'outcome')
        marketHash = self.safe_string(outcomeObj['info'], 'marketHash')
        channel = 'orderbook_v3:' + marketHash
        messageHash = 'orderbook::' + sym
        url = self.safe_string(self.urls['api'], 'ws')
        await self.connect_sxbet_centrifugo(url)
        client = self.client(url)
        isNewSubscription = self.safe_value(client.subscriptions, channel) is None
        if self.safe_value(self.orderbooks, sym) is None:
            # seed from the REST snapshot so the book is served before the first publication
            watchedBooks = self.safe_dict(self.options, 'wsWatchedBooks')
            if watchedBooks is None:
                self.options['wsWatchedBooks'] = self.create_safe_dictionary()
            self.options['wsWatchedBooks'][sym] = marketHash
            snapshot = await self.fetch_sxbet_book_snapshot(marketHash)
            self.apply_sxbet_ws_snapshot(snapshot)
            if self.safe_value(self.orderbooks, sym) is None:
                emptyBook = self.order_book({})
                self.orderbooks[sym] = emptyBook
        requestId = self.request_id(url)
        self.register_sxbet_ws_request(requestId, messageHash, channel)
        subscribeMsg = {'subscribe': {'channel': channel}, 'id': requestId}
        future = self.watch(url, messageHash, subscribeMsg, channel)
        if isNewSubscription:
            # return the freshly-seeded book immediately instead of blocking until the next publication
            client.resolve(self.safe_value(self.orderbooks, sym), messageHash)
        orderbook = await future
        return orderbook.limit()

    def apply_sxbet_ws_snapshot(self, snapshot: dict) -> list[str]:
        """
 @ignore
        applies one full v3 book publication({marketHash, version, outcomeOne, outcomeTwo}) to every watched outcome of that market, honoring the monotonic version
        :param dict snapshot: the raw book publication
        :returns str[]: the outcome handles whose books were refreshed
        """
        marketHash = self.safe_string(snapshot, 'marketHash')
        version = self.safe_string(snapshot, 'version', '')
        refreshed = []
        if marketHash is None:
            return refreshed
        versionsAll = self.safe_dict(self.options, 'wsBookVersions')
        if versionsAll is None:
            self.options['wsBookVersions'] = self.create_safe_dictionary()
        held = self.safe_string(self.options['wsBookVersions'], marketHash, '')
        # versions are fixed-width numeric strings - replace only on a strictly newer publication
        if (held != '') and (version <= held):
            return refreshed
        self.options['wsBookVersions'][marketHash] = version
        watchedBooks = self.safe_dict(self.options, 'wsWatchedBooks', {})
        watchedSyms = list(watchedBooks.keys())
        timestamp = self.milliseconds()
        watchedSymsLength = len(watchedSyms)
        for i in range(0, watchedSymsLength):
            sym = watchedSyms[i]
            if self.safe_string(watchedBooks, sym) != marketHash:
                continue
            outcomeObj = self.outcome(sym)
            outcomeId = self.safe_string(outcomeObj, 'outcomeId')
            isOutcomeOne = (outcomeId == marketHash)
            sides = self.parse_sxbet_v3_book_sides(snapshot, isOutcomeOne)
            orderbook = self.safe_value(self.orderbooks, sym)
            if orderbook is None:
                orderbook = self.order_book({})
                self.orderbooks[sym] = orderbook
            bookSnapshot = {
                'bids': self.safe_list(sides, 'bids', []),
                'asks': self.safe_list(sides, 'asks', []),
                'timestamp': timestamp,
                'datetime': self.iso8601(timestamp),
                'nonce': None,
            }
            orderbook.reset(bookSnapshot)
            refreshed.append(sym)
        return refreshed

    def handle_order_book(self, client: Client, rows: list[object]):
        #
        #     {
        #         "marketHash": "0x...",
        #         "version": "00100000000000010015000",
        #         "outcomeOne": [ { "percentageOdds": "40000000000000000000", "size": "1000000" } ],
        #         "outcomeTwo": []
        #     }
        #
        rowsLength = len(rows)
        for i in range(0, rowsLength):
            refreshed = self.apply_sxbet_ws_snapshot(rows[i])
            refreshedLength = len(refreshed)
            for j in range(0, refreshedLength):
                sym = refreshed[j]
                client.resolve(self.safe_value(self.orderbooks, sym), 'orderbook::' + sym)

    async def watch_ticker(self, outcome: str, params={}) -> PredictionTicker:
        """
        streams best-odds updates of an outcome; the venue channel is global, entries are filtered down to the requested outcome's market

        https://docs.sx.bet/api-reference/channel-best-odds-v3

        :param str outcome: unified outcome or outcome token id
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction ticker structure](https://docs.ccxt.com/#/?id=prediction-ticker-structure)
        """
        await self.load_outcome(outcome)
        outcomeObj = self.outcome(outcome)
        sym = self.safe_string(outcomeObj, 'outcome')
        marketHash = self.safe_string(outcomeObj['info'], 'marketHash')
        messageHash = 'ticker::' + sym
        watchedTickers = self.safe_dict(self.options, 'wsWatchedTickers')
        if watchedTickers is None:
            self.options['wsWatchedTickers'] = self.create_safe_dictionary()
        self.options['wsWatchedTickers'][sym] = marketHash
        url = self.safe_string(self.urls['api'], 'ws')
        await self.connect_sxbet_centrifugo(url)
        client = self.client(url)
        channel = 'best_odds_v3:global'
        hydrated = False
        if self.safe_value(self.tickers, sym) is None:
            # hydrate from the REST snapshot so the first call does not hang until the
            # market's next top-of-book change - the channel is global, so the seed must fire
            # for every newly watched market, not only on a fresh subscription
            snapshot = await self.fetch_sxbet_book_snapshot(marketHash)
            raw = self.parse_sxbet_snapshot_best_odds(snapshot)
            ticker = self.parse_prediction_ticker(raw, outcomeObj)
            self.tickers[sym] = ticker
            hydrated = True
        requestId = self.request_id(url)
        self.register_sxbet_ws_request(requestId, messageHash, channel)
        subscribeMsg = {'subscribe': {'channel': channel}, 'id': requestId}
        future = self.watch(url, messageHash, subscribeMsg, channel)
        if hydrated:
            client.resolve(self.safe_value(self.tickers, sym), messageHash)
        tickerResult = await future
        return tickerResult

    def handle_ticker(self, client: Client, rows: list[object]):
        #
        #     {
        #         "marketHash": "0xbf06...16a2eb",
        #         "outcomeOne": { "percentageOdds": "40000000000000000000", "size": "1000000" },
        #         "outcomeTwo": null
        #     }
        #
        watchedTickers = self.safe_dict(self.options, 'wsWatchedTickers', {})
        watchedSyms = list(watchedTickers.keys())
        rowsLength = len(rows)
        for i in range(0, rowsLength):
            entry = rows[i]
            marketHash = self.safe_string(entry, 'marketHash')
            if marketHash is None:
                continue
            # every publication carries BOTH sides (null = no resting liquidity), so the legacy
            # best-odds shape rebuilds directly without any merge cache
            bestOne = self.safe_dict(entry, 'outcomeOne', {})
            bestTwo = self.safe_dict(entry, 'outcomeTwo', {})
            raw = {
                'marketHash': marketHash,
                'outcomeOne': {'percentageOdds': self.safe_string(bestOne, 'percentageOdds')},
                'outcomeTwo': {'percentageOdds': self.safe_string(bestTwo, 'percentageOdds')},
            }
            watchedSymsLength = len(watchedSyms)
            for j in range(0, watchedSymsLength):
                sym = watchedSyms[j]
                if self.safe_string(watchedTickers, sym) != marketHash:
                    continue
                outcomeObj = self.outcome(sym)
                ticker = self.parse_prediction_ticker(raw, outcomeObj)
                self.tickers[sym] = ticker
                client.resolve(ticker, 'ticker::' + sym)

    async def watch_trades(self, outcome: str, since: Int = None, limit: Int = None, params={}) -> list[PredictionTrade]:
        """
        streams public bets of an outcome; the venue channel is global, entries are filtered down to the requested outcome

        https://docs.sx.bet/api-reference/channel-recent-trades-v3

        :param str outcome: unified outcome or outcome token id
        :param int [since]: timestamp in ms of the earliest trade to return
        :param int [limit]: the maximum number of trades to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction trade structures](https://docs.ccxt.com/#/?id=prediction-trade-structure)
        """
        await self.load_outcome(outcome)
        outcomeObj = self.outcome(outcome)
        sym = self.safe_string(outcomeObj, 'outcome')
        messageHash = 'trades::' + sym
        trades = await self.subscribe_sxbet_channel(messageHash, 'recent_trades_v3:global')
        return self.filter_by_since_limit(trades, since, limit, 'timestamp', True)

    def parse_sxbet_v3_public_trade(self, trade: dict) -> PredictionTrade:
        """
 @ignore
        parses one recent_trades_v3 publication(public TradeV3 shape) into a unified trade
        :param dict trade: the raw public trade row
        :returns dict: a [prediction trade structure](https://docs.ccxt.com/#/?id=prediction-trade-structure)
        """
        marketHash = self.safe_string(trade, 'marketHash', '')
        isBettingOutcomeOne = self.safe_bool(trade, 'isBettingOutcomeOne', True)
        outcomeId = marketHash if (isBettingOutcomeOne) else (marketHash + '-2')
        outcomeObj = self.safe_outcome(outcomeId)
        oneDenom = '100000000000000000000'
        usdcDecimals = '1000000'
        odds = self.safe_string(trade, 'weightedAverageOdds')
        price = self.parse_number(Precise.string_div(odds, oneDenom)) if (odds is not None) else None
        stake = self.safe_string(trade, 'totalStake', '0')
        amount = self.parse_number(Precise.string_div(stake, usdcDecimals, 6))
        timestamp = self.parse8601(self.safe_string(trade, 'betTime'))
        return self.safe_prediction_trade({
            'id': self.safe_string(trade, 'tradeId'),
            'info': trade,
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'outcome': self.safe_string(outcomeObj, 'outcome'),
            'outcomeId': self.safe_string(outcomeObj, 'outcomeId', outcomeId),
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string(outcomeObj, 'market'),
            'order': None,
            'type': None,
            'side': 'buy',
            'takerOrMaker': None,
            'price': price,
            'amount': amount,
            'cost': amount,
            'fee': None,
        })

    def handle_trades(self, client: Client, rows: list[object]):
        #
        #     { "trade": { "tradeId": "0xac6b...", "marketHash": "0x1fec...", "isBettingOutcomeOne": true,
        #                  "totalStake": "1000000", "weightedAverageOdds": "40000000000000000000",
        #                  "betTime": "2026-07-31T18:22:41.000Z", ... } }
        #
        rowsLength = len(rows)
        for i in range(0, rowsLength):
            row = self.safe_dict(rows[i], 'trade', rows[i])
            trade = self.parse_sxbet_v3_public_trade(row)
            sym = self.safe_string(trade, 'outcome')
            if sym is None:
                # bet in a market absent from the loaded outcome cache
                continue
            if self.trades is None:
                self.trades = self.create_safe_dictionary()
            if self.safe_value(self.trades, sym) is None:
                tradesLimit = self.safe_integer(self.options, 'tradesLimit', 1000)
                self.trades[sym] = ArrayCache(tradesLimit)
            stored = self.trades[sym]
            stored.append(trade)
            client.resolve(stored, 'trades::' + sym)

    async def watch_my_trades(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> list[PredictionTrade]:
        """
        streams the authenticated wallet's fills over its per-account v3 channel

        https://docs.sx.bet/api-reference/channel-fills-v3

        :param str [outcome]: unified outcome or outcome token id to narrow the stream down to
        :param int [since]: timestamp in ms of the earliest trade to return
        :param int [limit]: the maximum number of trades to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction trade structures](https://docs.ccxt.com/#/?id=prediction-trade-structure)
        """
        if self.walletAddress is None:
            raise ArgumentsRequired(self.id + ' watchMyTrades() requires a walletAddress')
        messageHash = 'myTrades'
        if outcome is not None:
            await self.load_outcome(outcome)
            outcomeObj = self.outcome(outcome)
            sym = self.safe_string(outcomeObj, 'outcome')
            messageHash = 'myTrades::' + sym
        channel = 'account:fills_v3_#' + self.walletAddress
        trades = await self.subscribe_sxbet_channel(messageHash, channel)
        return self.filter_by_since_limit(trades, since, limit, 'timestamp', True)

    def handle_my_trade(self, client: Client, rows: list[object]):
        #
        #     FillV3 rows - identical to GET /fills-v3
        #
        rowsLength = len(rows)
        for i in range(0, rowsLength):
            row = self.safe_dict(rows[i], 'fill', rows[i])
            trade = self.parse_sxbet_v3_fill(row)
            sym = self.safe_string(trade, 'outcome')
            if sym is None:
                continue
            if self.myTrades is None:
                myTradesLimit = self.safe_integer(self.options, 'myTradesLimit', 1000)
                self.myTrades = ArrayCacheByOutcomeById(myTradesLimit)
            stored = self.myTrades
            stored.append(trade)
            client.resolve(stored, 'myTrades')
            client.resolve(stored, 'myTrades::' + sym)

    async def watch_orders(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> list[PredictionOrder]:
        """
        streams updates of the authenticated wallet's orders over its per-account v3 channel

        https://docs.sx.bet/api-reference/channel-orders-v3

        :param str [outcome]: unified outcome or outcome token id to narrow the stream down to
        :param int [since]: timestamp in ms of the earliest order to return
        :param int [limit]: the maximum number of orders to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        if self.walletAddress is None:
            raise ArgumentsRequired(self.id + ' watchOrders() requires a walletAddress')
        messageHash = 'orders'
        if outcome is not None:
            await self.load_outcome(outcome)
            outcomeObj = self.outcome(outcome)
            sym = self.safe_string(outcomeObj, 'outcome')
            messageHash = 'orders::' + sym
        channel = 'account:orders_v3_#' + self.walletAddress
        orders = await self.subscribe_sxbet_channel(messageHash, channel)
        return self.filter_by_since_limit(orders, since, limit, 'timestamp', True)

    def handle_order(self, client: Client, rows: list[object]):
        #
        #     OrderV3 rows - identical to GET /orders-v3 (plus INACTIVE states with inactiveReason)
        #
        rowsLength = len(rows)
        for i in range(0, rowsLength):
            row = self.safe_dict(rows[i], 'order', rows[i])
            order = self.parse_prediction_order(row)
            if self.orders is None:
                cacheLimit = self.safe_integer(self.options, 'ordersLimit', 1000)
                self.orders = ArrayCacheByOutcomeById(cacheLimit)
            stored = self.orders
            stored.append(order)
            client.resolve(stored, 'orders')
            sym = self.safe_string(order, 'outcome')
            if sym is not None:
                client.resolve(stored, 'orders::' + sym)

    def handle_errors(self, code: int, reason: str, url: str, method: str, headers: dict, body: str, response: object, requestHeaders: object, requestBody: object):
        # sx.bet returns { "error": "Bad Request", "message": <code or [strings]>, "statusCode": 4xx };
        # map the known codes to ccxt errors so callers can distinguish a rejected order or a
        # validation problem from a transport outage (the base otherwise maps a bare 4xx to the
        # exchange-not-available error). unmapped codes fall through to the base http-status handling
        if response is None:
            return None
        message = self.safe_value(response, 'message')
        if message is None:
            return None
        # validation failures arrive in string-array form, business errors in single-code-string form
        messageString = None
        if isinstance(message, list):
            messageString = ', '.join(message)
        else:
            messageString = message
        feedback = self.id + ' ' + body
        self.throw_exactly_matched_exception(self.exceptions['exact'], messageString, feedback)
        self.throw_broadly_matched_exception(self.exceptions['broad'], messageString, feedback)
        # a 400 is a client-side bad request (bad params, invalid order), not a transport outage —
        # throw BadRequest instead of letting the base map the bare 400 to a retryable network-unavailable error
        if code == 400:
            raise BadRequest(feedback)
        return None

    def sign(self, path: object, api: object = 'sxbet', method='GET', params={}, headers: object = None, body: object = None):
        """
 @ignore
        builds the request url and attaches the x-sx-api-key header; every private v3 route authenticates with the apiKey credential, so its absence fails fast instead of surfacing a raw 401
        :param str path: the endpoint path
        :param string|str[] api: the api group and access level
        :param str method: the http method
        :param dict params: the request parameters
        :param dict [headers]: request headers
        :param str [body]: the request body
        :returns dict: a dict with url, method, body and headers
        """
        apiGroup = api if isinstance(api, str) else api[0]
        accessLevel = 'public' if isinstance(api, str) else api[1]
        if (accessLevel == 'private') and (self.apiKey is None):
            raise AuthenticationError(self.id + ' ' + path + ' is a private endpoint and requires the apiKey credential (the x-sx-api-key header)')
        baseUrls = self.urls['api']
        baseUrl = self.safe_string(baseUrls, apiGroup, baseUrls['sxbet'])
        url = baseUrl + '/' + self.implode_params(path, params)
        query = self.omit(params, self.extract_params(path))
        existingHeaders = headers if (headers is not None) else {}
        headers = self.extend({
            'Accept': 'application/json',
            'Content-Type': 'application/json',
        }, existingHeaders)
        if self.apiKey is not None:
            headers['x-sx-api-key'] = self.apiKey
        # DELETE /orders-v3 carries its order ids in a JSON body; the other DELETE routes -
        # /orders-v3/all and /orders-v3/event - take query parameters, like every GET
        sendAsQuery = (method == 'GET')
        if method == 'DELETE':
            hasOrdersList = ('orders' in query)
            sendAsQuery = not hasOrdersList
        if sendAsQuery:
            querystring = self.urlencode(query)
            if querystring != '':
                url += '?' + querystring
        else:
            queryKeys = list(query.keys())
            queryKeysLength = len(queryKeys)
            if queryKeysLength > 0:
                body = self.json(query)
        return {'url': url, 'method': method, 'body': body, 'headers': headers}
