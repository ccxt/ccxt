# -*- coding: utf-8 -*-

# PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
# https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

from ccxt.async_support.base.prediction_exchange import PredictionExchange
from ccxt.abstract.prediction.opinion import ImplicitAPI
import asyncio
from ccxt.async_support.base.ws.cache import ArrayCache, ArrayCacheByOutcomeById
from ccxt.base.types import Any, Balances, Int, Market, Num, Str, Strings, PredictionEvent, fetchEventsParams, PredictionTicker, PredictionTickers, PredictionOrder, PredictionOrderBook, PredictionTrade, PredictionPosition
from typing import List
from ccxt.base.errors import ExchangeError
from ccxt.base.errors import AuthenticationError
from ccxt.base.errors import PermissionDenied
from ccxt.base.errors import AccountNotEnabled
from ccxt.base.errors import ArgumentsRequired
from ccxt.base.errors import BadRequest
from ccxt.base.errors import InsufficientFunds
from ccxt.base.errors import InvalidOrder
from ccxt.base.decimal_to_precision import ROUND
from ccxt.base.decimal_to_precision import TRUNCATE
from ccxt.base.decimal_to_precision import DECIMAL_PLACES
from ccxt.base.precise import Precise


class opinion(PredictionExchange, ImplicitAPI):

    def describe(self) -> Any:
        return self.deep_extend(super(opinion, self).describe(), {
            'id': 'opinion',
            'name': 'Opinion',
            'countries': ['HK'],
            'rateLimit': 67,  # 15 requests per second per API key
            'certified': False,
            'pro': True,
            'has': {
                'CORS': None,
                'spot': False,
                'margin': False,
                'swap': False,
                'future': False,
                'option': False,
                'cancelOrder': True,
                'createOrder': True,
                'fetchBalance': True,
                'fetchClosedOrders': True,
                'fetchEvent': True,
                'fetchEvents': True,
                'fetchMarkets': True,
                'fetchMyTrades': True,
                'fetchOHLCV': True,
                'fetchOpenOrders': True,
                'fetchOrder': True,
                'fetchOrderBook': True,
                'fetchOrders': True,
                'fetchPositions': True,
                'fetchTicker': True,
                'fetchTickers': True,
                'prediction': True,
                'watchMyTrades': True,
                'watchOrderBook': True,
                'watchOrders': True,
                'watchTicker': True,
                'watchTrades': True,
            },
            'timeframes': {
                # live-verified via GET /token/price-history: only 1h/1d are recognized,
                # any other interval value(including 1m/1w) silently falls back to 1d
                '1h': '1h',
                '1d': '1d',
            },
            'urls': {
                'logo': 'https://github.com/user-attachments/assets/f633496f-8d3d-4bc2-a59c-612dbbf23b11',
                'api': {
                    'opinion': 'https://openapi.opinion.trade/openapi',
                    'ws': 'wss://ws.opinion.trade',
                },
                'www': 'https://opinion.trade',
                'doc': ['https://docs.opinion.trade'],
            },
            'api': {
                'opinion': {
                    'public': {
                        'get': {
                            'market': 1,
                            'market/{marketId}': 1,
                            'market/categorical/{marketId}': 1,
                            'market/slug/{slug}': 1,
                            'label': 1,
                            'token/latest-price': 1,
                            'token/orderbook': 1,
                            'token/price-history': 1,
                            'quoteToken': 1,
                        },
                    },
                    'private': {
                        'get': {
                            'order': 1,
                            'order/{orderId}': 1,
                            'positions/user/{walletAddress}': 1,
                            'trade/user/{walletAddress}': 1,
                            'auth/api-key': 1,
                            'user/auth': 1,
                            'user/balance': 1,
                        },
                        'post': {
                            'auth/api-key': 1,
                            'order': 1,
                            'order/cancel': 1,
                        },
                        'delete': {
                            'auth/api-key': 1,
                        },
                    },
                },
            },
            'requiredCredentials': {
                'apiKey': True,
                'secret': False,
                'walletAddress': True,
                'privateKey': True,
            },
            'fees': {
                'trading': {
                    'tierBased': False,
                    'percentage': True,
                    'maker': -0.02,
                    'taker': 0.04,
                },
            },
            'exceptions': {
                'exact': {
                    '10014': AccountNotEnabled,  # "Please enable trading first" - live-verified: account onboarded but trading wallet not enabled yet
                    '10403': PermissionDenied,  # "API is not available to persons located in the United States, China, or persons located in restricted jurisdictions"
                    '11001': AuthenticationError,  # "This API Key has no related Opinion Login Wallet yet"
                    '11002': AuthenticationError,  # "Invalid API key"
                    '10605': InvalidOrder,  # "Depth not enough" - live-verified: a market order with no matching book depth
                    '10610': InvalidOrder,  # "postOnly is not allowed for market orders"
                    '11004': AuthenticationError,  # "Self-service key issuance is temporarily disabled"
                    '11005': AuthenticationError,  # "Wallet is not a registered Opinion account"
                    '11009': AuthenticationError,  # "API key already exists"
                    '11010': AuthenticationError,  # "No API key found for self wallet"
                    '11011': AuthenticationError,  # "Invalid signature"
                    '11012': AuthenticationError,  # "Signature expired"
                    '11013': AuthenticationError,  # "Signature already used"
                },
                'broad': {
                    'Insufficient token balance': InsufficientFunds,
                    'below the minimum required value': InvalidOrder,
                    'must be the current multi-signature wallet': InvalidOrder,
                },
            },
            'streaming': {
                # the venue closes idle sockets without an application-level HEARTBEAT(see ping)
                'keepAlive': 25000,
            },
            'options': {
                'tradesLimit': 1000,
                'ordersLimit': 1000,
                'myTradesLimit': 1000,
                'eventScopeParams': ['labelId'],
                'defaultFetchEventsLimit': 20,   # events page size for the paginated categorical listing
                'maxFetchEventsResults': 100,    # default cap on events fetched when the caller gives no limit
                'maxEventsPages': 50,            # safety cap on event pages fetched per call
                'marketsPageLimit': 20,
                'maxMarketsPages': 50,
            },
        })

    async def fetch_markets(self, params={}) -> List[Market]:
        """
        fetches every kind of opinion market
 categorical parents double unified "events" and are cached into self.events side effect

        https://docs.opinion.trade/developer-guide/opinion-open-api/market

        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param int [params.limit]: max number of markets to collect(defaults to options.marketsPageLimit * options.maxMarketsPages, 1000)
        :returns dict[]: an array of objects representing market data
        """
        rest = self.omit(params, ['limit'])
        userLimit = self.safe_integer(params, 'limit')
        pageLimit = self.safe_integer(self.options, 'marketsPageLimit', 20)
        maxPages = self.safe_integer(self.options, 'maxMarketsPages', 50)
        flatMarkets = []
        # seen-guard keyed by the event handle; the events themselves go through setEvents below
        # so the cache gets the base indexing(id + handle + slug) instead of a raw assignment
        seenEvents = {}
        eventsList = []
        page = 1
        fetchedRawCount = 0
        while(True):
            request = {
                'marketType': 2,  # all - both standalone binaries and categorical parents
                'limit': pageLimit,
                'page': page,
            }
            response = await self.opinionPublicGetMarket(self.extend(request, rest))
            result = self.safe_dict(response, 'result', {})
            rawMarkets = self.safe_list(result, 'list', [])
            rawMarketsLength = len(rawMarkets)
            fetchedRawCount = self.sum(fetchedRawCount, rawMarketsLength)
            # categorical parents expand into several flatMarkets entries each, so the raw,
            # unflattened row count in 'total' must be compared against fetchedRawCount, not
            # len(flatMarkets) - otherwise expansion makes the comparison meaningless
            total = self.safe_integer(result, 'total')
            for i in range(0, rawMarketsLength):
                raw = rawMarkets[i]
                marketType = self.safe_integer(raw, 'marketType')
                if marketType == 1:
                    event = self.parse_event(raw)
                    childMarkets = event['markets']
                    childMarketsLength = len(childMarkets)
                    for ci in range(0, childMarketsLength):
                        flatMarkets.append(childMarkets[ci])
                    eventKey = self.safe_string(event, 'event')
                    if (eventKey is not None) and (eventKey != '') and not (eventKey in seenEvents):
                        seenEvents[eventKey] = True
                        eventsList.append(event)
                else:
                    flatMarkets.append(self.parse_opinion_market(raw))
            collectedLength = len(flatMarkets)
            if (rawMarketsLength < pageLimit) or (page >= maxPages) or ((total is not None) and (fetchedRawCount >= total)) or ((userLimit is not None) and (collectedLength >= userLimit)):
                break
            page = self.sum(page, 1)
        self.set_events(eventsList)
        flatMarketsLength = len(flatMarkets)
        if (userLimit is not None) and (flatMarketsLength > userLimit):
            return self.array_slice(flatMarkets, 0, userLimit)
        return flatMarkets

    async def fetch_outcome(self, outcomeSymbol: str) -> Any:
        """
 @ignore
        resolves a single outcome; a bare numeric token id carries no search text for
 the base's fetchEvents-driven resolution(opinion has no per-id lookup endpoint, unlike
 kalshi/polymarket), so bulk-warm the outcome cache via loadOutcomes() first for id-form input
        :param str outcomeSymbol: the outcome handle or token id
        :returns dict: the outcome cache
        """
        if self.outcome_search_query(outcomeSymbol) is None:
            await self.load_outcomes()
            if self.has_outcome(outcomeSymbol):
                return self.safe_outcome(outcomeSymbol)
        return await super(opinion, self).fetch_outcome(outcomeSymbol)

    def parse_opinion_market(self, raw: dict, eventSlug: Str = None) -> Market:
        """
 @ignore
        converts a single raw opinion market into one ccxt market with yes/no outcomes
        :param dict raw: the raw opinion market object
        :param str [eventSlug]: the slug of the parent event
        :returns dict: a [market structure](https://docs.ccxt.com/#/?id=market-structure)
        """
        # {
        #     "chainId": "56",
        #     "conditionId": "469db44df1309dac7cf9fcaa142562f3c89719d47277e095d021c1561166539a",
        #     "createdAt": 1778750719,
        #     "cutoffAt": 0,
        #     "isResolvableByAI": True,
        #     "marketId": 16565,
        #     "marketTitle": "10–15s",
        #     "noLabel": "No",
        #     "noTokenId": "48641131337815600205538397357674754469573755318952418769815647071528778171134",
        #     "questionId": "63907f471c045499f69b5ea157bdea1942f9d53b6e5e767ae69f4d148aeeefae",
        #     "quoteToken": "0x55d398326f99059fF775485246999027B3197955",
        #     "rebate": {
        #         "maker": 0.5
        #     },
        #     "resolvedAt": 1779342061,
        #     "resultTokenId": "107063188116504514729209026208703521982564071792212276771696073817845504321279",
        #     "rules": "",
        #     "slug": "how-long-will-trump-and-xi-shake-hands-when-they-meet-10-15s",
        #     "status": 4,
        #     "statusEnum": "Resolved",
        #     "volume": "50",
        #     "yesLabel": "Yes",
        #     "yesTokenId": "107063188116504514729209026208703521982564071792212276771696073817845504321279"
        # }
        marketId = self.safe_string(raw, 'marketId')
        slug = self.safe_string(raw, 'slug')
        effectiveEventSlug = eventSlug
        if (eventSlug is not None) and (slug is not None) and (slug.find(eventSlug) == 0):
            effectiveEventSlug = None
        marketSymbol = self.slug_to_market_symbol(effectiveEventSlug, slug)
        statusEnum = self.safe_string(raw, 'statusEnum')
        active = (statusEnum == 'Activated')
        resolved = (statusEnum == 'Resolved')
        resultTokenId = self.safe_string(raw, 'resultTokenId')
        hasResult = resolved and (resultTokenId is not None) and (resultTokenId != '')
        outcomeLabels = [
            self.safe_string(raw, 'yesLabel', 'YES'),
            self.safe_string(raw, 'noLabel', 'NO'),
        ]
        outcomeTokenIds = [
            self.safe_string(raw, 'yesTokenId'),
            self.safe_string(raw, 'noTokenId'),
        ]
        outcomes = []
        resolvedOutcome = None
        for i in range(0, len(outcomeLabels)):
            label = outcomeLabels[i]
            tokenId = outcomeTokenIds[i]
            outcomeHandle = self.slug_to_outcome_symbol(effectiveEventSlug, slug, label)
            winner = None
            settleFraction = None
            if hasResult:
                winner = (tokenId == resultTokenId)
                settleFraction = 1 if winner else 0
                if winner:
                    resolvedOutcome = outcomeHandle
            outcomes.append({
                'id': tokenId,
                'outcomeId': tokenId,
                'outcome': outcomeHandle,
                'market': marketSymbol,
                'label': label,
                'active': active,
                'winner': winner,
                'settleFraction': settleFraction,
                'info': raw,
            })
        marketResolvedOutcome = resolvedOutcome
        # the venue sends cutoffAt 0 for markets without a scheduled cutoff - map it to
        # None instead of the epoch, same for the event-level end date
        expiryTimestamp = None
        if self.safe_integer(raw, 'cutoffAt', 0) != 0:
            expiryTimestamp = self.safe_timestamp(raw, 'cutoffAt')
        created = self.safe_timestamp(raw, 'createdAt')
        return {
            'id': marketId,
            'market': marketSymbol,
            'base': 'USDT',
            'quote': 'USDT',
            'settle': None,
            'baseId': marketId,
            'quoteId': 'USDT',
            'settleId': None,
            'type': 'prediction',
            'marketType': 'binary',
            'executionModel': 'clob',
            'spot': False,
            'margin': False,
            'swap': False,
            'future': False,
            'option': False,
            'prediction': True,
            'active': active,
            'resolved': resolved,
            'resolvedOutcome': marketResolvedOutcome,
            'contract': False,
            'linear': None,
            'inverse': None,
            'contractSize': None,
            'expiry': expiryTimestamp,
            'expiryDatetime': self.iso8601(expiryTimestamp),
            'strike': None,
            'optionType': None,
            'taker': self.fees['trading']['taker'],
            'maker': self.fees['trading']['maker'],
            'percentage': True,
            'tierBased': False,
            'feeSide': 'get',
            'precision': {
                'amount': None,
                'price': None,
            },
            'limits': {
                'leverage': {'min': 1, 'max': 1},
                'amount': {'min': None, 'max': None},
                'price': {'min': 0, 'max': 1},
                'cost': {'min': None, 'max': None},
            },
            'outcomes': outcomes,
            'info': raw,
            'created': created,
        }

    async def fetch_events(self, params: fetchEventsParams = {}) -> List[PredictionEvent]:
        """
        fetches Opinion's categorical markets - scope required via query/queries/tags/eventId/slug/labelId

        https://docs.opinion.trade/developer-guide/opinion-open-api/market

        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param int [params.labelId]: filter by opinion category label id
        :param int [params.limit]: max number of events to fetch(paginated server-side; defaults to options.maxFetchEventsResults, 100)
        :returns dict[]: an array of event structures
        """
        self.require_event_query(params)
        queries = self.parse_search_queries(params)
        eventId = self.safe_string(params, 'eventId')
        slug = self.safe_string(params, 'slug')
        if (eventId is not None) or (slug is not None):
            singleRest = self.omit(params, ['eventId', 'slug', 'query', 'queries', 'tags', 'status', 'sort', 'searchIn', 'limit'])
            singleResponse = None
            if slug is not None:
                singleResponse = await self.opinionPublicGetMarketSlugSlug(self.extend({'slug': slug}, singleRest))
            else:
                singleResponse = await self.opinionPublicGetMarketCategoricalMarketId(self.extend({'marketId': eventId}, singleRest))
            singleResult = self.safe_dict(singleResponse, 'result', {})
            singleData = self.safe_dict(singleResult, 'data', {})
            single = self.parse_event(singleData)
            self.index_event_outcomes(single)
            return self.apply_event_fetch_params([single], params, queries)
        rest = self.omit(params, ['query', 'queries', 'tags', 'status', 'sort', 'searchIn', 'limit'])
        pageLimit = self.safe_integer(self.options, 'defaultFetchEventsLimit', 20)
        userLimit = self.safe_integer(params, 'limit')
        # bound how many events are actually FETCHED: the user limit when given, otherwise
        # options.maxFetchEventsResults - the scope filters keep the listing narrow, but a broad
        # label can still hold more than one page
        fetchCap = self.safe_integer(self.options, 'maxFetchEventsResults', 100)
        if userLimit is not None:
            fetchCap = userLimit
        maxPages = self.safe_integer(self.options, 'maxEventsPages', 50)
        rawEvents = []
        page = 1
        fetchedRawCount = 0
        while(True):
            reqLimit = pageLimit
            remaining = fetchCap - fetchedRawCount
            if remaining < reqLimit:
                reqLimit = remaining
            if reqLimit <= 0:
                break
            request = {
                'marketType': 1,  # categorical only - these are the ones with childMarkets, our unified "event"
                'limit': reqLimit,
                'page': page,
            }
            response = await self.opinionPublicGetMarket(self.extend(request, rest))
            result = self.safe_dict(response, 'result', {})
            pageEvents = self.safe_list(result, 'list', [])
            pageEventsLength = len(pageEvents)
            fetchedRawCount = self.sum(fetchedRawCount, pageEventsLength)
            for i in range(0, pageEventsLength):
                rawEvents.append(pageEvents[i])
            total = self.safe_integer(result, 'total')
            if (pageEventsLength < reqLimit) or (page >= maxPages) or ((total is not None) and (fetchedRawCount >= total)) or (fetchedRawCount >= fetchCap):
                break
            page = self.sum(page, 1)
        rawEventsLength = len(rawEvents)
        parsedEvents = []
        if self.markets is None:
            self.markets = self.create_safe_dictionary()
        for i in range(0, rawEventsLength):
            event = self.parse_event(rawEvents[i])
            parsedEvents.append(event)
            # register the parsed markets so populateOutcomes can index their outcomes
            eventMarkets = self.safe_list(event, 'markets', [])
            eventMarketsLength = len(eventMarkets)
            for mi in range(0, eventMarketsLength):
                m = eventMarkets[mi]
                self.markets[m['market']] = m
        self.populate_outcomes()
        return self.apply_event_fetch_params(parsedEvents, params, queries)

    async def fetch_event(self, id: str, params={}) -> PredictionEvent:
        """
        fetches a single prediction-market event by its market id, or slug

        https://docs.opinion.trade/developer-guide/opinion-open-api/market

        :param str id: the numeric marketId, or the market slug
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction event structure](https://docs.ccxt.com/#/?id=prediction-event-structure)
        """
        isSlug = (id.find('-') >= 0)
        response = None
        if isSlug:
            response = await self.opinionPublicGetMarketSlugSlug(self.extend({'slug': id}, params))
        else:
            response = await self.opinionPublicGetMarketCategoricalMarketId(self.extend({'marketId': id}, params))
        result = self.safe_dict(response, 'result', {})
        data = self.safe_dict(result, 'data', {})
        event = self.parse_event(data)
        self.index_event_outcomes(event)
        return event

    def parse_event(self, rawEvent: dict) -> Any:
        """
 @ignore
        parses a raw opinion categorical market(with nested childMarkets) into the unified event shape
        :param dict rawEvent: the raw opinion categorical market object
        :returns dict: an event structure
        """
        # {
        #     "chainId": "56",
        #     "childMarkets": [
        #         {
        #             "chainId": "56",
        #             "conditionId": "469db44df1309dac7cf9fcaa142562f3c89719d47277e095d021c1561166539a",
        #             "createdAt": 1778750719,
        #             "cutoffAt": 0,
        #             "isResolvableByAI": True,
        #             "marketId": 16565,
        #             "marketTitle": "10–15s",
        #             "noLabel": "No",
        #             "noTokenId": "48641131337815600205538397357674754469573755318952418769815647071528778171134",
        #             "questionId": "63907f471c045499f69b5ea157bdea1942f9d53b6e5e767ae69f4d148aeeefae",
        #             "quoteToken": "0x55d398326f99059fF775485246999027B3197955",
        #             "rebate": {
        #                 "maker": 0.5
        #             },
        #             "resolvedAt": 1779342061,
        #             "resultTokenId": "107063188116504514729209026208703521982564071792212276771696073817845504321279",
        #             "rules": "",
        #             "slug": "how-long-will-trump-and-xi-shake-hands-when-they-meet-10-15s",
        #             "status": 4,
        #             "statusEnum": "Resolved",
        #             "volume": "50",
        #             "yesLabel": "Yes",
        #             "yesTokenId": "107063188116504514729209026208703521982564071792212276771696073817845504321279"
        #         },
        #         {
        #             "chainId": "56",
        #             "conditionId": "c4054e23e1afc96c93bbc05378414f8286b2f3e3107bb80944ba8a7581428812",
        #             "createdAt": 1778750720,
        #             "cutoffAt": 0,
        #             "isResolvableByAI": True,
        #             "marketId": 16566,
        #             "marketTitle": "15s+",
        #             "noLabel": "No",
        #             "noTokenId": "47010463515333704767569479601138504130354011266006912230152057656074556410652",
        #             "questionId": "3c78ac485d772b8241fd185f8b45ef9d7df9690d99ea59ed58927ade2f50b182",
        #             "quoteToken": "0x55d398326f99059fF775485246999027B3197955",
        #             "rebate": {
        #                 "maker": 0.5
        #             },
        #             "resolvedAt": 1779342061,
        #             "resultTokenId": "47010463515333704767569479601138504130354011266006912230152057656074556410652",
        #             "rules": "",
        #             "slug": "how-long-will-trump-and-xi-shake-hands-when-they-meet-15s-plus",
        #             "status": 4,
        #             "statusEnum": "Resolved",
        #             "volume": "50",
        #             "yesLabel": "Yes",
        #             "yesTokenId": "4219962057210945760892475434030882254029819241100485445656470801524365584613"
        #         }
        #     ],
        #     "conditionId": "",
        #     "coverUrl": "",
        #     "createdAt": 1778750719,
        #     "cutoffAt": 1778803200,
        #     "isResolvableByAI": False,
        #     "labelIds": [
        #         1
        #     ],
        #     "labels": [
        #         "Politics"
        #     ],
        #     "marketId": 788,
        #     "marketTitle": "How long will Trump and Xi shake hands when they meet?",
        #     "marketType": 1,
        #     "noLabel": "",
        #     "noTokenId": "",
        #     "questionId": "63907f471c045499f69b5ea157bdea1942f9d53b6e5e767ae69f4d148aeeefae",
        #     "quoteToken": "0x55d398326f99059fF775485246999027B3197955",
        #     "rebate": {
        #         "maker": 0.5
        #     },
        #     "resolvedAt": 0,
        #     "resultTokenId": "",
        #     "rules": "This market resolves based on the length of the longest qualifying handshake between Donald Trump and Xi Jinping on May 14, 2026(Beijing local time), the day of their bilateral meeting at the Great Hall of the People.\nBaseline already established: Video footage from the welcome ceremony confirms a qualifying handshake within the 10–15s range. The remaining question is whether any additional handshake on the same day produces a longer measured duration.\nOutcomes:\n\n10–15s — resolves YES if no qualifying handshake on May 14, 2026 exceeds 15 seconds.\n15s+ — resolves YES if any qualifying handshake on May 14, 2026 is measured at more than 15 seconds.\n\nMeasurement: Duration is measured from the exact moment hands make initial physical contact until the exact moment either party breaks contact. Where multiple video sources exist, the highest-resolution footage is used; where measurements differ, the consensus reading across major media is applied. A duration falling exactly on the 15s boundary resolves to 15s+.\nQualifying handshake: voluntary, intentional, in-person; direct hand-to-hand contact(gloves permitted); clearly visible on video from start to finish. Fist bumps, hugs, waves, back pats, and other non-handshake contact are excluded from the measured duration even if they occur during the same greeting.\nResolution window: All qualifying handshakes occurring on May 14, 2026 in Beijing local time are eligible, including those at arrival, bilateral sessions, signing ceremonies, state dinners, and departure. Handshakes on any other date do not count.\nResolution source: video footage from May 14, 2026.",
        #     "slug": "how-long-will-trump-and-xi-shake-hands-when-they-meet",
        #     "status": 1,
        #     "statusEnum": "Created",
        #     "thumbnailUrl": "https://images.opinion.trade/0xf988d66bd9c46b69d33e2703f7264d3c2267136c/0xdf583bdff183f389d7c5d9c5d099127cabda824afd05d66059b8027ca775763d",
        #     "volume": "100.00",
        #     "volume24h": "100.00",
        #     "volume7d": "100.00",
        #     "yesLabel": "",
        #     "yesTokenId": ""
        # }
        eventId = self.safe_string(rawEvent, 'marketId')
        slug = self.safe_string(rawEvent, 'slug')
        title = self.safe_string(rawEvent, 'marketTitle')
        eventHandle = self.shorten_slug(title) if (title is not None) else self.shorten_slug(slug)
        rawChildren = self.safe_list(rawEvent, 'childMarkets', [])
        rawChildrenLength = len(rawChildren)
        marketsList = []
        for i in range(0, rawChildrenLength):
            marketsList.append(self.parse_opinion_market(rawChildren[i], slug))
        statusEnum = self.safe_string(rawEvent, 'statusEnum')
        active = (statusEnum == 'Activated')
        resolved = (statusEnum == 'Resolved')
        end = None
        if self.safe_integer(rawEvent, 'cutoffAt', 0) != 0:
            end = self.safe_timestamp(rawEvent, 'cutoffAt')
        created = self.safe_timestamp(rawEvent, 'createdAt')
        labels = self.safe_list(rawEvent, 'labels', [])
        return self.extend({
            'id': eventId,
            'event': eventHandle,
            'title': title,
            'description': self.safe_string(rawEvent, 'rules'),
            'slug': slug,
            'category': self.safe_string(labels, 0),
            'tags': labels,
            'markets': marketsList,
            'active': active,
            'resolved': resolved,
            'volume': self.safe_number(rawEvent, 'volume'),
            'created': created,
            'createdDatetime': self.iso8601(created),
            'end': end,
            'endDatetime': self.iso8601(end),
            'image': self.safe_string_2(rawEvent, 'coverUrl', 'thumbnailUrl'),
            'info': rawEvent,
        })

    async def fetch_ticker(self, outcome: str, params={}) -> PredictionTicker:
        """
        fetches the latest trade price and top of book for a single outcome token

        https://docs.opinion.trade/developer-guide/opinion-open-api/token

        :param str outcome: unified outcome or outcome token id
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction ticker structure](https://docs.ccxt.com/#/?id=prediction-ticker-structure)
        """
        outcomeObj = await self.load_outcome(outcome)
        tokenId = outcomeObj['outcomeId']
        promises = [
            self.opinionPublicGetTokenLatestPrice(self.extend({'token_id': tokenId}, params)),
            self.opinionPublicGetTokenOrderbook(self.extend({'token_id': tokenId}, params)),
        ]
        priceResponse, bookResponse = await asyncio.gather(*promises)
        response = {'price': priceResponse, 'book': bookResponse}
        return self.parse_prediction_ticker(response, outcomeObj)

    def parse_prediction_ticker(self, ticker: dict, market: Market = None) -> PredictionTicker:
        """
 @ignore
        parses a raw opinion latest-price + orderbook pair into a unified ticker object
        :param dict ticker: a {price, book} dict of the two raw responses
        :param dict [market]: the outcome object the ticker belongs to
        :returns dict: a [prediction ticker structure](https://docs.ccxt.com/#/?id=prediction-ticker-structure)
        """
        #
        #     {
        #         "price": {
        #             "errmsg": "",
        #             "errno": 0,
        #             "result": {"price": "0.002", "side": "buy-limit", "size": "205.03", "timestamp": 1766844546000, "tokenId": "..."}
        #         },
        #         "book": {
        #             "errmsg": "",
        #             "errno": 0,
        #             "result": {"asks": [{"price": "0.999", "size": "5500"}], "bids": [], "market": "...", "timestamp": ..., "tokenId": "..."}
        #         }
        #     }
        #
        marketAny = market
        priceResponse = self.safe_dict(ticker, 'price', {})
        priceResult = self.safe_dict(priceResponse, 'result', {})
        bookResponse = self.safe_dict(ticker, 'book', {})
        bookResult = self.safe_dict(bookResponse, 'result', {})
        bids = self.safe_list(bookResult, 'bids', [])
        asks = self.safe_list(bookResult, 'asks', [])
        bestBid = self.safe_dict(bids, 0, {})
        bestAsk = self.safe_dict(asks, 0, {})
        last = self.safe_number(priceResult, 'price')
        timestamp = self.safe_integer(priceResult, 'timestamp', self.milliseconds())
        return self.safe_prediction_ticker({
            'outcome': self.safe_string(marketAny, 'outcome'),
            'outcomeId': self.safe_string_2(marketAny, 'outcomeId', 'id'),
            'label': self.safe_string(marketAny, 'label'),
            'market': self.safe_string_2(marketAny, 'market', 'outcome'),
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'high': None,
            'low': None,
            'bid': self.safe_number(bestBid, 'price'),
            'bidVolume': self.safe_number(bestBid, 'size'),
            'ask': self.safe_number(bestAsk, 'price'),
            'askVolume': self.safe_number(bestAsk, 'size'),
            'open': None,
            'close': last,
            'last': last,
            'change': None,
            'percentage': None,
            'average': None,
            'baseVolume': None,
            'quoteVolume': None,
            'info': ticker,
        }, market)

    async def fetch_tickers(self, outcomes: Strings = None, params={}) -> PredictionTickers:
        """
        fetches tickers for multiple outcome tokens - opinion has no all-tickers endpoint, each token needs its own latest-price + orderbook request

        https://docs.opinion.trade/developer-guide/opinion-open-api/token

        :param str[] outcomes: unified outcomes or outcome token ids - required, opinion has no all-tickers endpoint
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a dictionary of [prediction ticker structures](https://docs.ccxt.com/#/?id=prediction-ticker-structure) indexed by outcome
        """
        if outcomes is None:
            raise ArgumentsRequired(self.id + ' fetchTickers() requires an outcomes argument — the venue has no all-tickers endpoint; pass the outcome handles or token ids to fetch(discover them via fetchEvents())')
        await self.load_outcomes(outcomes)
        outcomesLength = len(outcomes)
        promises = []
        for i in range(0, outcomesLength):
            outcomeObj = self.outcome(outcomes[i])
            tokenId = outcomeObj['outcomeId']
            promises.append(self.opinionPublicGetTokenLatestPrice(self.extend({'token_id': tokenId}, params)))
            promises.append(self.opinionPublicGetTokenOrderbook(self.extend({'token_id': tokenId}, params)))
        responses = await asyncio.gather(*promises)
        result = {}
        for i in range(0, outcomesLength):
            outcomeObj = self.outcome(outcomes[i])
            priceIndex = i * 2
            priceResponse = responses[priceIndex]
            bookResponse = responses[self.sum(priceIndex, 1)]
            response = {'price': priceResponse, 'book': bookResponse}
            ticker = self.parse_prediction_ticker(response, outcomeObj)
            symbolKey = self.safe_string(ticker, 'outcome')
            if symbolKey is not None:
                result[symbolKey] = ticker
        return result

    async def fetch_order_book(self, outcome: Str, limit: Int = None, params={}) -> PredictionOrderBook:
        """
        fetches the order book for a single outcome token

        https://docs.opinion.trade/developer-guide/opinion-open-api/token

        :param str outcome: unified outcome or outcome token id
        :param int [limit]: not used by opinion fetchOrderBook
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction order book structure](https://docs.ccxt.com/#/?id=prediction-order-book-structure)
        """
        outcomeObj = await self.load_outcome(outcome)
        tokenId = outcomeObj['outcomeId']
        request = {
            'token_id': tokenId,
        }
        response = await self.opinionPublicGetTokenOrderbook(self.extend(request, params))
        #
        #     {
        #         "errmsg": "",
        #         "errno": 0,
        #         "result": {
        #             "asks": [
        #                 {"price": "0.999", "size": "5500"}
        #             ],
        #             "bids": [],
        #             "market": "ff7d2d935d0cce2922ea05a363e5a87439e1f8f86f01dacf7238d4c4cc542f6c",
        #             "timestamp": 1785488076901,
        #             "tokenId": "56915117085756475550546730127709511264652860289185956800398231821503615918119"
        #         }
        #     }
        #
        result = self.safe_dict(response, 'result', {})
        timestamp = self.safe_integer(result, 'timestamp')
        orderbook = self.parse_order_book(result, self.safe_outcome_symbol(outcome, outcomeObj), timestamp, 'bids', 'asks', 'price', 'size')
        return self.safe_prediction_order_book(orderbook, outcomeObj)

    async def fetch_ohlcv(self, outcome: str, timeframe='1d', since: Int = None, limit: Int = None, params={}) -> List[list]:
        """
        fetches historical candlestick data for an outcome token

        https://docs.opinion.trade/developer-guide/opinion-open-api/token

        :param str outcome: unified outcome or outcome token id
        :param str timeframe: the length of time each candle represents - only '1h' and '1d' are supported live
        :param int [since]: timestamp in ms of the earliest candle to fetch
        :param int [limit]: the maximum number of candles to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns int[][]: a list of candles ordered, open, high, low, close, volume
        """
        if not (timeframe in self.timeframes):
            supportedKeys = list(self.timeframes.keys())
            raise BadRequest(self.id + ' fetchOHLCV() unsupported timeframe ' + timeframe + ', supported timeframes are ' + ', '.join(supportedKeys))
        outcomeObj = await self.load_outcome(outcome)
        tokenId = outcomeObj['outcomeId']
        interval = self.safe_string(self.timeframes, timeframe)
        response = await self.opinionPublicGetTokenPriceHistory(self.extend({
            'token_id': tokenId,
            'interval': interval,
        }, params))
        #
        #     {
        #         "errmsg": "",
        #         "errno": 0,
        #         "result": {
        #             "history": [
        #                 {"p": "0.001", "t": 1785495600}
        #             ]
        #         }
        #     }
        #
        result = self.safe_dict(response, 'result', {})
        history = self.safe_list(result, 'history', [])
        candles = []
        historyLength = len(history)
        for i in range(0, historyLength):
            point = history[i]
            price = self.safe_number(point, 'p')
            timestamp = self.safe_timestamp(point, 't')
            if (price is not None) and (timestamp is not None):
                candles.append([timestamp, price, price, price, price, None])
        sorted = self.sort_by(candles, 0)
        return self.filter_by_since_limit(sorted, since, limit, 0)

    def parse_ohlcv(self, ohlcv: Any, market: Market = None) -> list:
        """
        parses a single opinion price-history point into a unified OHLCV candle
        :param dict ohlcv: the raw {p, t} point
        :param dict [market]: the outcome object the candle belongs to
        :returns int[]: a candle ordered, open, high, low, close, volume
        """
        # Unused: fetchOHLCV maps {p, t} points directly.
        #
        #     {"p": "0.001", "t": 1785495600}
        #
        price = self.safe_number(ohlcv, 'p')
        return [self.safe_timestamp(ohlcv, 't'), price, price, price, price, None]

    async def load_quote_token(self, quoteTokenAddress: Str) -> dict:
        """
 @ignore
        fetches and caches quote-token metadata needed to sign orders
        :param str quoteTokenAddress: the on-chain quote-token contract address, read from a 'quoteToken' field
        :returns dict: the matching quote-token entry
        """
        if quoteTokenAddress is None:
            raise ArgumentsRequired(self.id + ' loadQuoteToken() requires a quoteTokenAddress')
        cacheKey = quoteTokenAddress.lower()
        cached = self.safe_dict(self.options, 'quoteTokens', {})
        existing = self.safe_dict(cached, cacheKey)
        if existing is not None:
            return existing
        response = await self.opinionPublicGetQuoteToken({})
        result = self.safe_dict(response, 'result', {})
        list = self.safe_list(result, 'list', [])
        listLength = len(list)
        quoteTokens = {}
        for i in range(0, listLength):
            entry = list[i]
            address = self.safe_string_lower(entry, 'quoteTokenAddress')
            if address is not None:
                quoteTokens[address] = entry
        self.options['quoteTokens'] = quoteTokens
        quoteToken = self.safe_dict(quoteTokens, cacheKey)
        if quoteToken is None:
            raise ExchangeError(self.id + ' loadQuoteToken() could not find quote token ' + quoteTokenAddress)
        return quoteToken

    async def load_multi_sign_address(self) -> str:
        """
 @ignore
        fetches and caches the per-wallet multi-signature address that owns order assets
        :returns str: the multi-sig wallet address for self.walletAddress on chain 56, or self.walletAddress itself if none exists yet
        """
        cached = self.safe_string(self.options, 'multiSignAddress')
        if cached is not None:
            return cached
        response = await self.opinionPrivateGetUserAuth({})
        result = self.safe_dict(response, 'result', {})
        walletUsers = self.safe_dict(result, 'walletUsers', {})
        multiSignAddress = self.safe_string(walletUsers, '56', self.walletAddress)
        self.options['multiSignAddress'] = multiSignAddress
        return multiSignAddress

    def sign_opinion_order(self, order: dict, exchangeAddress: str) -> str:
        domain = {
            'name': 'OPINION CTF Exchange',
            'version': '1',
            'chainId': 56,
            'verifyingContract': exchangeAddress,
        }
        messageTypes = {
            'Order': [
                {'name': 'salt', 'type': 'uint256'},
                {'name': 'maker', 'type': 'address'},
                {'name': 'signer', 'type': 'address'},
                {'name': 'taker', 'type': 'address'},
                {'name': 'tokenId', 'type': 'uint256'},
                {'name': 'makerAmount', 'type': 'uint256'},
                {'name': 'takerAmount', 'type': 'uint256'},
                {'name': 'expiration', 'type': 'uint256'},
                {'name': 'nonce', 'type': 'uint256'},
                {'name': 'feeRateBps', 'type': 'uint256'},
                {'name': 'side', 'type': 'uint8'},
                {'name': 'signatureType', 'type': 'uint8'},
            ],
        }
        encoded = self.eth_encode_structured_data(domain, messageTypes, order)
        sig = self.sign_message(encoded, self.privateKey)
        return '0x' + self.remove0x_prefix(sig['r']) + self.remove0x_prefix(sig['s']) + self.int_to_base16(sig['v'])

    def opinion_order_raw_amounts(self, isMarket: bool, side: str, amount: Num, price: Num, decimals: float) -> dict:
        decimalsStr = '1'
        for i in range(0, decimals):
            decimalsStr = decimalsStr + '0'
        amountStr = self.number_to_string(amount)
        if isMarket and (side == 'BUY'):
            marketMakerAmountWei = self.decimal_to_precision(Precise.string_mul(amountStr, decimalsStr), TRUNCATE, 0, DECIMAL_PLACES)
            return {'makerAmount': marketMakerAmountWei, 'takerAmount': '0'}
        priceStr = self.decimal_to_precision(self.number_to_string(price), ROUND, 6, DECIMAL_PLACES)
        priceParts = priceStr.split('.')
        priceInt = self.safe_string(priceParts, 0, '0')
        priceFrac = self.safe_string(priceParts, 1, '')
        priceDenom = '1000000'
        priceNum = Precise.string_add(Precise.string_mul(priceInt, priceDenom), priceFrac.ljust(6, '0'))
        if priceNum == '0':
            raise InvalidOrder(self.id + ' createOrder() invalid price ' + priceStr)
        makerRaw = amountStr
        if side == 'BUY':
            makerRaw = Precise.string_mul(amountStr, priceStr)
        makerAmountWei = self.decimal_to_precision(Precise.string_mul(makerRaw, decimalsStr), TRUNCATE, 0, DECIMAL_PLACES)
        makerAmount: Str
        takerAmount: Str
        if side == 'BUY':
            k = Precise.string_div(makerAmountWei, priceNum, 0)
            makerAmount = Precise.string_mul(k, priceNum)
            takerAmount = Precise.string_mul(k, priceDenom)
        else:
            k = Precise.string_div(makerAmountWei, priceDenom, 0)
            makerAmount = Precise.string_mul(k, priceDenom)
            takerAmount = Precise.string_mul(k, priceNum)
        return {'makerAmount': makerAmount, 'takerAmount': takerAmount}

    async def create_order(self, outcome: str, type: Str, side: Str, amount: Num, price: Num = None, params={}) -> PredictionOrder:
        """
        places a limit or market order on the CLOB for the given outcome token

        https://docs.opinion.trade/developer-guide/opinion-open-api/order

        :param str outcome: unified outcome or outcome token id
        :param str type: 'market' or 'limit'
        :param str side: 'buy' or 'sell'
        :param float amount: for limit orders, the number of outcome shares to trade; for market orders, the quote(USDT) to spend on a BUY or the shares to sell on a SELL
        :param float [price]: the price per outcome token between 0 and 1; required for limit orders and market SELL orders(where it acts reference / worst acceptable price for the taker amount); ignored for market BUY orders(amount is already the quote to spend)
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :param bool [params.postOnly]: limit orders only - reject the order if it would cross the spread
        :returns dict: a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        await self.load_api_key()
        self.check_required_credentials()
        outcomeObj = await self.load_outcome(outcome)
        tokenId = outcomeObj['outcomeId']
        isMarket = (type == 'market')
        sideStr = side.upper()
        if price is None:
            if not isMarket:
                raise ArgumentsRequired(self.id + ' createOrder() requires a price for limit orders')
            if sideStr == 'SELL':
                # the reference(worst acceptable) price the taker amount is computed from
                raise ArgumentsRequired(self.id + ' createOrder() requires a price for market sell orders')
        marketOrderPrice = '0'
        if isMarket and (sideStr == 'SELL'):
            marketOrderPrice = self.number_to_string(price)
        info = self.safe_dict(outcomeObj, 'info', {})
        topicId = self.safe_integer(info, 'marketId')
        quoteTokenAddress = self.safe_string(info, 'quoteToken')
        quoteToken = await self.load_quote_token(quoteTokenAddress)
        exchangeAddress = self.safe_string(quoteToken, 'ctfExchangeAddress', '')
        decimals = self.safe_integer(quoteToken, 'decimal', 18)
        amounts = self.opinion_order_raw_amounts(isMarket, sideStr, amount, price, decimals)
        makerAmount = self.safe_string(amounts, 'makerAmount')
        takerAmount = self.safe_string(amounts, 'takerAmount')
        sideInt = 0 if (sideStr == 'BUY') else 1
        salt = self.number_to_string(self.milliseconds())
        postOnly = self.safe_bool(params, 'postOnly', False)
        rest = self.omit(params, ['postOnly'])
        maker = await self.load_multi_sign_address()
        # Ethereum addresses are case-insensitive - a checksummed multiSignAddress compared
        # against a differently-cased walletAddress with strict equality would pick the wrong
        # signatureType(0 EOA vs 2 Gnosis Safe) and break order signing/validation
        makerLower = maker.lower()
        walletAddressLower = self.walletAddress.lower()
        signatureType = 0 if (makerLower == walletAddressLower) else 2
        order = {
            'salt': salt,
            'maker': maker,
            'signer': self.walletAddress,
            'taker': '0x0000000000000000000000000000000000000000',
            'tokenId': tokenId,
            'makerAmount': makerAmount,
            'takerAmount': takerAmount,
            'expiration': '0',
            'nonce': '0',
            'feeRateBps': '0',
            'side': sideInt,
            'signatureType': signatureType,
        }
        signature = self.sign_opinion_order(order, exchangeAddress)
        signatureNo0x = self.remove0x_prefix(signature)
        orderBody = self.extend({
            'salt': salt,
            'maker': maker,
            'signer': self.walletAddress,
            'taker': '0x0000000000000000000000000000000000000000',
            'tokenId': tokenId,
            'makerAmount': makerAmount,
            'takerAmount': takerAmount,
            'expiration': '0',
            'nonce': '0',
            'feeRateBps': '0',
            'side': str(sideInt),
            'signatureType': str(signatureType),
            'signature': signature,
            'sign': signatureNo0x[0:64],
            'contractAddress': '',
            'currencyAddress': quoteTokenAddress,
            'topicId': topicId,
            'price': marketOrderPrice if isMarket else self.number_to_string(price),
            'tradingMethod': 1 if isMarket else 2,
            'timestamp': self.seconds(),
            'safeRate': '0',
            'orderExpTime': '0',
            'postOnly': postOnly,
        }, rest)
        response = await self.opinionPrivatePostOrder(orderBody)
        result = self.safe_dict(response, 'result', {})
        orderData = self.safe_dict(result, 'orderData', {})
        return self.parse_prediction_order(orderData, outcomeObj)

    async def cancel_order(self, id: Str, outcome: Str = None, params={}) -> PredictionOrder:
        """
        cancels a single open order by id

        https://docs.opinion.trade/developer-guide/opinion-open-api/order

        :param str id: the order id
        :param str [outcome]: not used by opinion cancelOrder
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        await self.load_api_key()
        request = {'orderId': id}
        response = await self.opinionPrivatePostOrderCancel(self.extend(request, params))
        result = self.safe_dict(response, 'result', {})
        canceled = self.safe_bool(result, 'result', False)
        # a False result does NOT mean the order is still open — it may already be filled,
        # already cancelled, or unknown; don't invent a status the venue didn't report.
        # error responses with an errno never reach self line, handleErrors throws on them
        status = 'canceled' if (canceled) else None
        return self.safe_prediction_order({'id': id, 'status': status, 'info': response})

    def parse_order_status(self, status: Str) -> Str:
        """
 @ignore
        maps an opinion order statusEnum string to the unified status vocabulary
        :param str status: the raw opinion order statusEnum
        :returns str: a unified order status
        """
        statuses = {
            'Pending': 'open',
            'Finished': 'closed',
            'Canceled': 'canceled',
            'Expired': 'canceled',
            'Failed': 'canceled',
        }
        return self.safe_string(statuses, status, status)

    def parse_prediction_order(self, order: dict, market: Market = None) -> PredictionOrder:
        """
        parses a raw opinion order object into a unified prediction order structure
        :param dict order: the raw opinion OrderData object
        :param dict [market]: the outcome object the order belongs to
        :returns dict: a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        #
        #     {
        #         "orderId": "...",
        #         "marketId": 1094,
        #         "side": 0,
        #         "sideEnum": "Buy",
        #         "tradingMethod": 2,
        #         "tradingMethodEnum": "Limit",
        #         "price": "0.01",
        #         "orderShares": "5",
        #         "orderAmount": "0.05",
        #         "filledShares": "0",
        #         "filledAmount": "0",
        #         "profit": "0",
        #         "status": 1,
        #         "statusEnum": "Pending",
        #         "createdAt": 1785500000,
        #         "expiresAt": 0,
        #         "postOnly": False
        #     }
        #
        id = self.safe_string(order, 'orderId')
        marketAny = market
        statusEnum = self.safe_string(order, 'statusEnum')
        status = self.parse_order_status(statusEnum)
        sideEnum = self.safe_string_lower(order, 'sideEnum')
        tradingMethodEnum = self.safe_string_lower(order, 'tradingMethodEnum')
        timestamp = self.safe_timestamp(order, 'createdAt')
        return self.safe_prediction_order({
            'id': id,
            'clientOrderId': None,
            'info': order,
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'lastTradeTimestamp': None,
            'status': status,
            'outcome': self.safe_string(marketAny, 'outcome'),
            'outcomeId': self.safe_string_2(marketAny, 'outcomeId', 'id'),
            'label': self.safe_string(marketAny, 'label'),
            'market': self.safe_string_2(marketAny, 'market', 'outcome'),
            'type': tradingMethodEnum,
            'side': sideEnum,
            'price': self.safe_number(order, 'price'),
            'amount': self.safe_number(order, 'orderShares'),
            # cost is the FILLED portion's collateral(unified cost = filled * price) — orderAmount
            # is the full requested orderShares * price, wrong for a partially filled order
            'cost': self.safe_number(order, 'filledAmount'),
            'filled': self.safe_number(order, 'filledShares'),
            'fee': None,
            'trades': [],
        }, market)

    async def fetch_orders(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> List[PredictionOrder]:
        """
        fetches all of the authenticated user's orders

        https://docs.opinion.trade/developer-guide/opinion-open-api/order

        :param str [outcome]: filter by unified outcome or outcome token id
        :param int [since]: timestamp in ms of the earliest order to fetch
        :param int [limit]: the maximum number of orders to fetch
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        await self.load_api_key()
        outcomeObj = None
        request = {}
        if outcome is not None:
            outcomeObj = await self.load_outcome(outcome)
            info = self.safe_dict(outcomeObj, 'info', {})
            request['marketId'] = self.safe_integer(info, 'marketId')
        response = await self.opinionPrivateGetOrder(self.extend(request, params))
        result = self.safe_dict(response, 'result', {})
        orders = self.safe_list(result, 'list', [])
        return self.parse_prediction_orders(orders, outcomeObj, since, limit)

    async def fetch_order(self, id: Str, outcome: Str = None, params={}) -> PredictionOrder:
        """
        fetches a single order by id

        https://docs.opinion.trade/developer-guide/opinion-open-api/order

        :param str id: the order id
        :param str [outcome]: unified outcome or outcome token id
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction order structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        await self.load_api_key()
        outcomeObj = None
        if outcome is not None:
            outcomeObj = await self.load_outcome(outcome)
        response = await self.opinionPrivateGetOrderOrderId(self.extend({'orderId': id}, params))
        result = self.safe_dict(response, 'result', {})
        orderData = self.safe_dict(result, 'orderData', {})
        return self.parse_prediction_order(orderData, outcomeObj)

    async def fetch_open_orders(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> List[PredictionOrder]:
        """
        fetches the authenticated user's open orders

        https://docs.opinion.trade/developer-guide/opinion-open-api/order

        :param str [outcome]: filter by unified outcome or outcome token id
        :param int [since]: timestamp in ms of the earliest order to fetch
        :param int [limit]: the maximum number of orders to fetch
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        # 1 = pending - open status
        request = {'status': '1'}
        return await self.fetch_orders(outcome, since, limit, self.extend(request, params))

    async def fetch_closed_orders(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> List[PredictionOrder]:
        """
        fetches the authenticated user's closed orders

        https://docs.opinion.trade/developer-guide/opinion-open-api/order

        :param str [outcome]: filter by unified outcome or outcome token id
        :param int [since]: timestamp in ms of the earliest order to fetch
        :param int [limit]: the maximum number of orders to fetch
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        # 2 = filled, 3 = canceled, 4 = expired, 5 = failed
        request = {'status': '2,3,4,5'}
        return await self.fetch_orders(outcome, since, limit, self.extend(request, params))

    async def fetch_my_trades(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> List[PredictionTrade]:
        """
        fetches the authenticated user's trades

        https://docs.opinion.trade/developer-guide/opinion-open-api/trade

        :param str [outcome]: filter by unified outcome or outcome token id
        :param int [since]: timestamp in ms of the earliest trade to fetch
        :param int [limit]: the maximum number of trades to fetch
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction trade structures](https://docs.ccxt.com/#/?id=prediction-trade-structure)
        """
        if self.walletAddress is None:
            raise ArgumentsRequired(self.id + ' fetchMyTrades() requires a walletAddress')
        await self.load_api_key()
        outcomeObj = None
        request = {'walletAddress': self.walletAddress}
        if outcome is not None:
            outcomeObj = await self.load_outcome(outcome)
            info = self.safe_dict(outcomeObj, 'info', {})
            request['marketId'] = self.safe_integer(info, 'marketId')
        response = await self.opinionPrivateGetTradeUserWalletAddress(self.extend(request, params))
        result = self.safe_dict(response, 'result', {})
        trades = self.safe_list(result, 'list', [])
        tradesLength = len(trades)
        for i in range(0, tradesLength):
            trade = trades[i]
            tokenId = self.safe_string(trade, 'tokenId')
            marketId = self.safe_integer(trade, 'marketId')
            if (tokenId is None) and (marketId is not None):
                tradeMarket = await self.load_trade_market(marketId)
                info = self.safe_dict(tradeMarket, 'info', {})
                isYes = (self.safe_string_lower(trade, 'outcomeSideEnum') == 'yes')
                trade['tokenId'] = self.safe_string(info, 'yesTokenId') if isYes else self.safe_string(info, 'noTokenId')
        return self.parse_prediction_trades(trades, outcomeObj, since, limit)

    async def load_trade_market(self, marketId: Int) -> Market:
        """
 @ignore
        fetches and caches a single market by its numeric marketId, and indexes its outcomes
        :param int marketId: the numeric market id
        :returns dict: the parsed market
        """
        if marketId is None:
            raise ArgumentsRequired(self.id + ' loadTradeMarket() requires a marketId')
        cacheKey = 'tradeMarketsById'
        cached = self.safe_dict(self.options, cacheKey, {})
        idStr = str(marketId)
        existing = self.safe_dict(cached, idStr)
        if existing is not None:
            return existing
        response = await self.opinionPublicGetMarketMarketId({'marketId': marketId})
        result = self.safe_dict(response, 'result', {})
        data = self.safe_dict(result, 'data', {})
        market = self.parse_opinion_market(data)
        if market is None:
            raise ExchangeError(self.id + ' loadTradeMarket() could not parse market ' + idStr)
        if self.markets is None:
            self.markets = self.create_safe_dictionary()
        marketHandle = self.safe_string(market, 'market', '')
        self.markets[marketHandle] = market
        self.index_market_outcomes(market)
        cached[idStr] = market
        self.options[cacheKey] = cached
        return market

    def parse_prediction_trade(self, trade: dict, market: Market = None) -> PredictionTrade:
        """
 @ignore
        parses a raw opinion trade object into a unified trade object
        :param dict trade: the raw opinion TradeData object
        :param dict [market]: the outcome object the trade belongs to
        :returns dict: a [prediction trade structure](https://docs.ccxt.com/#/?id=prediction-trade-structure)
        """
        tokenId = self.safe_string(trade, 'tokenId')
        outcomeObj = self.safe_outcome(tokenId, market)
        timestamp = self.safe_timestamp(trade, 'createdAt')
        side = self.safe_string_lower(trade, 'side')
        return self.safe_prediction_trade({
            'id': self.safe_string(trade, 'txHash'),
            'timestamp': timestamp,
            'side': side,
            'price': self.safe_number(trade, 'price'),
            'amount': self.safe_number(trade, 'shares'),
            'cost': self.safe_number(trade, 'amount'),
            'fee': {
                'cost': self.safe_number(trade, 'fee'),
                'currency': 'USDT',
            },
            'outcome': self.safe_string(outcomeObj, 'outcome'),
            'outcomeId': self.safe_string_2(outcomeObj, 'outcomeId', 'id'),
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string_2(outcomeObj, 'market', 'outcome'),
        })

    async def fetch_balance(self, params={}) -> Balances:
        """
        fetches the authenticated user's quote-token balances

        https://docs.opinion.trade/developer-guide/opinion-open-api/quote-token

        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [balance structure](https://docs.ccxt.com/#/?id=balance-structure)
        """
        await self.load_api_key()
        request = {'chain_id': '56'}
        response = await self.opinionPrivateGetUserBalance(self.extend(request, params))
        result = self.safe_dict(response, 'result', {})
        rawBalances = self.safe_list(result, 'balances', [])
        rawBalancesLength = len(rawBalances)
        for i in range(0, rawBalancesLength):
            rawBalance = rawBalances[i]
            quoteTokenAddress = self.safe_string(rawBalance, 'quoteToken')
            quoteToken = await self.load_quote_token(quoteTokenAddress)
            rawBalance['symbol'] = self.safe_string(quoteToken, 'symbol', 'USDT')
        return self.parse_balance(response)

    def parse_balance(self, response: Any) -> Balances:
        """
 @ignore
        parses an opinion user-balance response into a unified balances object
        :param dict response: the raw user-balance response
        :returns dict: a [balance structure](https://docs.ccxt.com/#/?id=balance-structure)
        """
        result = {'info': response}
        data = self.safe_dict(response, 'result', {})
        balances = self.safe_list(data, 'balances', [])
        balancesLength = len(balances)
        for i in range(0, balancesLength):
            balance = balances[i]
            code = self.safe_string(balance, 'symbol', 'USDT')
            result[code] = {
                'free': self.safe_number(balance, 'availableBalance'),
                'used': self.safe_number(balance, 'frozenBalance'),
                'total': self.safe_number(balance, 'totalBalance'),
            }
        return self.safe_balance(result)

    async def fetch_positions(self, outcomes: Strings = None, params={}) -> List[PredictionPosition]:
        """
        fetches the authenticated user's open positions

        https://docs.opinion.trade/developer-guide/opinion-open-api/position

        :param str[] [outcomes]: filter by unified outcomes or outcome token ids
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction position structures](https://docs.ccxt.com/#/?id=prediction-position-structure)
        """
        if self.walletAddress is None:
            raise ArgumentsRequired(self.id + ' fetchPositions() requires a walletAddress')
        await self.load_api_key()
        outcomesLength = 0
        if outcomes is not None:
            outcomesLength = len(outcomes)
            await self.load_outcomes(outcomes)
        request = {'walletAddress': self.walletAddress}
        response = await self.opinionPrivateGetPositionsUserWalletAddress(self.extend(request, params))
        result = self.safe_dict(response, 'result', {})
        positions = self.safe_list(result, 'list', [])
        parsed = self.parse_prediction_positions(positions)
        if outcomesLength == 0:
            return parsed
        wantedTokenIds = {}
        # copy to a plain list so the strict null checks see one shape
        outcomesList = [] if (outcomes is None) else outcomes
        for i in range(0, len(outcomesList)):
            outcomeObj = self.outcome(outcomesList[i])
            tokenId = self.safe_string(outcomeObj, 'outcomeId')
            if tokenId is not None:
                wantedTokenIds[tokenId] = True
        filtered = []
        for i in range(0, len(parsed)):
            position = parsed[i]
            info = self.safe_dict(position, 'info', {})
            tokenId = self.safe_string(info, 'tokenId')
            if (tokenId is not None) and (tokenId in wantedTokenIds):
                filtered.append(position)
        return filtered

    def parse_prediction_position(self, position: dict, market: Market = None) -> PredictionPosition:
        """
 @ignore
        parses a raw opinion position object into a unified position object
        :param dict position: the raw opinion PositionData object
        :param dict [market]: the outcome object the position belongs to
        :returns dict: a [prediction position structure](https://docs.ccxt.com/#/?id=prediction-position-structure)
        """
        tokenId = self.safe_string(position, 'tokenId')
        outcomeObj = self.safe_outcome(tokenId, market)
        outcomeSideEnum = self.safe_string_lower(position, 'outcomeSideEnum')
        return self.safe_prediction_position({
            'contracts': self.safe_number(position, 'sharesOwned'),
            'side': outcomeSideEnum,
            'unrealizedPnl': self.safe_number(position, 'unrealizedPnl'),
            'entryPrice': self.safe_number(position, 'avgEntryPrice'),
            'outcome': self.safe_string(outcomeObj, 'outcome'),
            'outcomeId': self.safe_string_2(outcomeObj, 'outcomeId', 'id'),
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string_2(outcomeObj, 'market', 'outcome'),
            'info': position,
        })

    def hash_message(self, message: Any) -> str:
        return '0x' + self.hash(message, 'keccak', 'hex')

    def sign_hash(self, hash: str, privateKey: str) -> dict:
        signature = self.ecdsa(hash[-64:], privateKey[-64:], 'secp256k1', None)
        # assign before padStart so the PHP str_pad regex matches
        rRaw = signature['r']
        sRaw = signature['s']
        r = rRaw.rjust(64, '0')
        s = sRaw.rjust(64, '0')
        return {
            'r': '0x' + r,
            's': '0x' + s,
            'v': self.sum(27, signature['v']),
        }

    def sign_message(self, message: Any, privateKey: str) -> dict:
        return self.sign_hash(self.hash_message(message), privateKey[-64:])

    def sign_api_key_auth(self, walletAddress: str, action: str, timestamp: str) -> str:
        # EIP-712 signature used to create/get/delete an API key(wallet-authenticated key management)
        domain = {
            'name': 'Opinion OpenAPI',
            'version': '1',
            'chainId': 56,
        }
        messageTypes = {
            'OpinionApiKeyAuth': [
                {'name': 'walletAddress', 'type': 'address'},
                {'name': 'action', 'type': 'string'},
                {'name': 'timestamp', 'type': 'string'},
            ],
        }
        messageData = {
            'walletAddress': walletAddress,
            'action': action,
            'timestamp': timestamp,
        }
        encoded = self.eth_encode_structured_data(domain, messageTypes, messageData)
        sig = self.sign_message(encoded, self.privateKey)
        return '0x' + self.remove0x_prefix(sig['r']) + self.remove0x_prefix(sig['s']) + self.int_to_base16(sig['v'])

    async def create_api_key(self, params={}) -> dict:
        """
        self-service creation of an Open API key linked to self.walletAddress via
 an EIP-712-signed request - there is no "generate key" button in the Opinion GUI, self is
 the only documented way to obtain a wallet-linked key

        https://docs.opinion.trade/developer-guide/opinion-open-api/authentication

        :param dict [params]: extra parameters
        :returns dict} the api credentials {apiKey, walletAddress:
        """
        response = await self.opinionPrivatePostAuthApiKey(params)
        result = self.safe_dict(response, 'result', {})
        return self.set_api_credentials(result)

    async def fetch_api_key(self, params={}) -> dict:
        """
        fetches the currently active Open API key for self.walletAddress

        https://docs.opinion.trade/developer-guide/opinion-open-api/authentication

        :param dict [params]: extra parameters
        :returns dict} the api credentials {apiKey, walletAddress:
        """
        response = await self.opinionPrivateGetAuthApiKey(params)
        result = self.safe_dict(response, 'result', {})
        return self.set_api_credentials(result)

    async def delete_api_key(self, params={}) -> dict:
        """
        revokes the Open API key for self.walletAddress

        https://docs.opinion.trade/developer-guide/opinion-open-api/authentication

        :param dict [params]: extra parameters
        :returns dict: raw response, result.deleted confirms revocation
        """
        response = await self.opinionPrivateDeleteAuthApiKey(params)
        self.options['apiKey'] = None
        # sign() prefers self.apiKey over options['apiKey'] - clear it too, or a directly-set
        # exchange.apiKey would keep being used for private calls after the key is revoked.
        # an empty string, not None: the strict base types the credential, and
        # sign() treats an empty key
        self.apiKey = ''
        return response

    async def load_api_key(self) -> Str:
        """
 @ignore
        ensures an apiKey is available before a private call - reuses a directly-set key, otherwise
 self-issues one from the walletAddress/privateKey via fetchApiKey(), falling back to createApiKey() when
 the wallet has no key yet; freshly created keys can take ~15 seconds to activate venue-side
        :returns str: the apiKey
        """
        hasDirectApiKey = not self.is_empty_string(self.apiKey)
        if hasDirectApiKey:
            return self.apiKey
        optionsKey = self.safe_string(self.options, 'apiKey')
        if optionsKey is not None:
            return optionsKey
        if (self.walletAddress is None) or (self.privateKey is None):
            raise AuthenticationError(self.id + ' private endpoints require an apiKey, or a walletAddress and privateKey to self-issue one')
        creds = None
        try:
            creds = await self.fetch_api_key()
        except Exception as e:
            # no key exists for self wallet yet(11010) - self-issue one; any other
            # failure(unregistered wallet, disabled issuance) surfaces from the create call
            creds = await self.create_api_key()
        return self.safe_string(creds, 'apiKey')

    def set_api_credentials(self, response: dict) -> dict:
        #
        #     {"apiKey": "...", "walletAddress": "..."}
        #
        creds = {
            'apiKey': self.safe_string(response, 'apiKey'),
            'walletAddress': self.safe_string(response, 'walletAddress'),
        }
        self.options['apiKey'] = creds['apiKey']
        # checkRequiredCredentials()(called by createOrder()) checks self.apiKey, not
        # options['apiKey'] - keep both in sync, same() clearing both
        self.apiKey = creds['apiKey']
        return creds

    def opinion_ws_url(self) -> str:
        """
 @ignore
        builds the websocket url - the venue authenticates the whole connection with the apiKey passed query parameter, for public and private channels alike
        :returns str: the websocket url
        """
        hasDirectApiKey = not self.is_empty_string(self.apiKey)
        apiKey = self.apiKey if (hasDirectApiKey) else self.safe_string(self.options, 'apiKey')
        if apiKey is None:
            raise AuthenticationError(self.id + ' websocket requires an apiKey - set it directly or call createApiKey()/fetchApiKey() first')
        wsUrl = self.safe_string(self.urls['api'], 'ws', '')
        return wsUrl + '?apikey=' + apiKey

    def ping(self, client: Any):
        # the venue keeps the socket open only while application-level heartbeats arrive
        return {'action': 'HEARTBEAT'}

    async def subscribe_opinion_channel(self, messageHash: str, channel: str, marketId: Int) -> Any:
        """
 @ignore
        subscribes to one venue channel scoped by the binary marketId and waits on the given message hash
        :param str messageHash: the internal hash the awaited payload resolves on
        :param str channel: the venue channel name(e.g. 'market.depth.diff')
        :param int marketId: the numeric binary market id
        :returns any: the first resolved payload
        """
        await self.load_api_key()
        url = self.opinion_ws_url()
        subscriptionKey = channel + ':' + self.number_to_string(marketId)
        subscribeMsg = {
            'action': 'SUBSCRIBE',
            'channel': channel,
            'marketId': marketId,
        }
        return await self.watch(url, messageHash, subscribeMsg, subscriptionKey)

    def handle_message(self, client: Any, message: Any):
        # every data payload carries its channel name in msgType; frames without one -
        # subscribe acks and heartbeat echoes - carry nothing to route
        msgType = self.safe_string(message, 'msgType')
        if msgType is None:
            return
        if msgType == 'market.depth.diff':
            self.handle_order_book(client, message)
        elif msgType == 'market.last.price':
            self.handle_ticker(client, message)
        elif msgType == 'market.last.trade':
            self.handle_trades(client, message)
        elif msgType == 'trade.order.update':
            self.handle_order(client, message)
        elif msgType == 'trade.record.new':
            self.handle_my_trade(client, message)

    def opinion_outcome_by_market_id_side(self, marketId: Int, outcomeSide: Int) -> Any:
        """
 @ignore
        resolves a cached outcome object from the numeric marketId + outcomeSide(1 yes / 2 no) the user channels report instead of a tokenId - cache-only, returns None on a cold cache
        :param int marketId: the numeric binary market id
        :param int outcomeSide: 1 for the yes token, 2 for the no token
        :returns dict: the outcome object, or None
        """
        if (marketId is None) or (self.markets is None):
            return None
        marketKeys = list(self.markets.keys())
        marketKeysLength = len(marketKeys)
        for i in range(0, marketKeysLength):
            market = self.markets[marketKeys[i]]
            info = self.safe_dict(market, 'info', {})
            if self.safe_integer(info, 'marketId') == marketId:
                outcomes = self.safe_list(market, 'outcomes', [])
                index = 1 if (outcomeSide == 2) else 0
                return self.safe_dict(outcomes, index)
        return None

    async def watch_order_book(self, outcome: str, limit: Int = None, params={}) -> PredictionOrderBook:
        """
        streams the order book of an outcome token; the channel is delta-only so the live book is seeded from the REST snapshot

        https://docs.opinion.trade/developer-guide/opinion-websocket/market-channels

        :param str outcome: unified outcome or outcome token id
        :param int [limit]: the maximum number of order book entries to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction order book structure](https://docs.ccxt.com/#/?id=prediction-order-book-structure)
        """
        outcomeObj = await self.load_outcome(outcome)
        info = self.safe_dict(outcomeObj, 'info', {})
        marketId = self.safe_integer(info, 'marketId')
        sym = self.safe_outcome_symbol(outcome, outcomeObj)
        channel = 'market.depth.diff'
        messageHash = 'orderbook::' + sym
        await self.load_api_key()
        url = self.opinion_ws_url()
        client = self.client(url)
        subscriptionKey = channel + ':' + self.number_to_string(marketId)
        isNewSubscription = self.safe_value(client.subscriptions, subscriptionKey) is None
        if isNewSubscription:
            await self.seed_order_book(outcome, sym, limit)
        subscribeMsg = {
            'action': 'SUBSCRIBE',
            'channel': channel,
            'marketId': marketId,
        }
        future = self.watch(url, messageHash, subscribeMsg, subscriptionKey)
        if isNewSubscription:
            # return the freshly-seeded book immediately instead of blocking until the next delta
            client.resolve(self.safe_value(self.orderbooks, sym), messageHash)
        orderbook = await future
        return orderbook.limit()

    async def seed_order_book(self, outcome: Str, sym: Str, limit: Int = None):
        # the depth channel streams single-level deltas only, so seed the live book from the REST snapshot
        snapshot = await self.fetch_order_book(outcome, limit)
        orderbook = self.order_book({})
        orderbook.reset(snapshot)
        self.orderbooks[sym] = orderbook

    def handle_order_book(self, client: Any, message: Any):
        #
        #     {
        #         "marketId": 2764,
        #         "tokenId": "19120407572139442221452465677574895365338028945317996490376653704877573103648",
        #         "outcomeSide": 1,
        #         "side": "bids",
        #         "price": "0.2",
        #         "size": "50",
        #         "msgType": "market.depth.diff"
        #     }
        #
        tokenId = self.safe_string(message, 'tokenId')
        outcomeObj = self.safe_dict(self.outcomes_by_id, tokenId)
        sym = self.safe_string(outcomeObj, 'outcome')
        if sym is None:
            return
        if self.safe_value(self.orderbooks, sym) is None:
            # the delta belongs to the market's other token, whose book is not being watched
            return
        orderbook = self.orderbooks[sym]
        sideStr = self.safe_string(message, 'side')
        bookSide = orderbook['bids'] if (sideStr == 'bids') else orderbook['asks']
        price = self.safe_number(message, 'price')
        size = self.safe_number(message, 'size')
        bookSide.storeArray([price, size])
        now = self.milliseconds()
        orderbook['timestamp'] = now
        orderbook['datetime'] = self.iso8601(now)
        client.resolve(orderbook, 'orderbook::' + sym)

    async def watch_ticker(self, outcome: str, params={}) -> PredictionTicker:
        """
        streams last-price updates of an outcome token

        https://docs.opinion.trade/developer-guide/opinion-websocket/market-channels

        :param str outcome: unified outcome or outcome token id
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict: a [prediction ticker structure](https://docs.ccxt.com/#/?id=prediction-ticker-structure)
        """
        outcomeObj = await self.load_outcome(outcome)
        info = self.safe_dict(outcomeObj, 'info', {})
        marketId = self.safe_integer(info, 'marketId')
        sym = self.safe_outcome_symbol(outcome, outcomeObj)
        messageHash = 'ticker::' + sym
        return await self.subscribe_opinion_channel(messageHash, 'market.last.price', marketId)

    def handle_ticker(self, client: Any, message: Any):
        #
        #     {
        #         "tokenId": "19120407572139442221452465677574895365338028945317996490376653704877573103648",
        #         "outcomeSide": 1,
        #         "price": "0.85",
        #         "marketId": 2764,
        #         "msgType": "market.last.price"
        #     }
        #
        tokenId = self.safe_string(message, 'tokenId')
        outcomeObj = self.safe_dict(self.outcomes_by_id, tokenId)
        sym = self.safe_string(outcomeObj, 'outcome')
        if sym is None:
            return
        now = self.milliseconds()
        last = self.safe_number(message, 'price')
        ticker = self.safe_prediction_ticker({
            'outcome': sym,
            'outcomeId': tokenId,
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string(outcomeObj, 'market'),
            'timestamp': now,
            'datetime': self.iso8601(now),
            'close': last,
            'last': last,
            'info': message,
        }, outcomeObj)
        self.tickers[sym] = ticker
        client.resolve(ticker, 'ticker::' + sym)

    async def watch_trades(self, outcome: str, since: Int = None, limit: Int = None, params={}) -> List[PredictionTrade]:
        """
        streams public trades of an outcome token

        https://docs.opinion.trade/developer-guide/opinion-websocket/market-channels

        :param str outcome: unified outcome or outcome token id
        :param int [since]: timestamp in ms of the earliest trade to return
        :param int [limit]: the maximum number of trades to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction trade structures](https://docs.ccxt.com/#/?id=prediction-trade-structure)
        """
        outcomeObj = await self.load_outcome(outcome)
        info = self.safe_dict(outcomeObj, 'info', {})
        marketId = self.safe_integer(info, 'marketId')
        sym = self.safe_outcome_symbol(outcome, outcomeObj)
        messageHash = 'trades::' + sym
        trades = await self.subscribe_opinion_channel(messageHash, 'market.last.trade', marketId)
        return self.filter_by_since_limit(trades, since, limit, 'timestamp', True)

    def handle_trades(self, client: Any, message: Any):
        #
        #     {
        #         "tokenId": "19120407572139442221452465677574895365338028945317996490376653704877573103648",
        #         "side": "Buy",
        #         "outcomeSide": 1,
        #         "price": "0.85",
        #         "shares": "10",
        #         "amount": "8.5",
        #         "marketId": 2764,
        #         "msgType": "market.last.trade"
        #     }
        #
        tokenId = self.safe_string(message, 'tokenId')
        outcomeObj = self.safe_dict(self.outcomes_by_id, tokenId)
        sym = self.safe_string(outcomeObj, 'outcome')
        if sym is None:
            return
        now = self.milliseconds()
        trade = self.safe_prediction_trade({
            'id': None,
            'info': message,
            'timestamp': now,
            'datetime': self.iso8601(now),
            'outcome': sym,
            'outcomeId': tokenId,
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string(outcomeObj, 'market'),
            'order': None,
            'type': None,
            'side': self.safe_string_lower(message, 'side'),
            'takerOrMaker': 'taker',
            'price': self.safe_number(message, 'price'),
            'amount': self.safe_number(message, 'shares'),
            'cost': self.safe_number(message, 'amount'),
            'fee': None,
        }, outcomeObj)
        if self.trades is None:
            self.trades = self.create_safe_dictionary()
        if self.safe_value(self.trades, sym) is None:
            tradesLimit = self.safe_integer(self.options, 'tradesLimit', 1000)
            self.trades[sym] = ArrayCache(tradesLimit)
        stored = self.trades[sym]
        stored.append(trade)
        client.resolve(stored, 'trades::' + sym)

    async def watch_orders(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> List[PredictionOrder]:
        """
        streams the authenticated user's order updates of one market - the venue channel is per-market, so the outcome argument is required

        https://docs.opinion.trade/developer-guide/opinion-websocket/user-channels

        :param str outcome: unified outcome or outcome token id whose market to watch
        :param int [since]: timestamp in ms of the earliest order to return
        :param int [limit]: the maximum number of orders to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction order structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
        """
        if outcome is None:
            raise ArgumentsRequired(self.id + ' watchOrders() requires an outcome(the order update channel is per-market)')
        outcomeObj = await self.load_outcome(outcome)
        info = self.safe_dict(outcomeObj, 'info', {})
        marketId = self.safe_integer(info, 'marketId')
        messageHash = 'orders'
        orders = await self.subscribe_opinion_channel(messageHash, 'trade.order.update', marketId)
        sym = self.safe_outcome_symbol(outcome, outcomeObj)
        return self.filter_by_value_since_limit(orders, 'outcome', sym, since, limit, 'timestamp', True)

    def parse_ws_order_status(self, status: Int) -> Str:
        """
 @ignore
        maps the numeric order status of the websocket order channel onto the unified vocabulary
        :param int status: the numeric order status
        :returns str: a unified order status, or None
        """
        # per the venue docs: 1 pending, 2 finished, 3 canceled, 4 expired, 5 failed
        if status == 1:
            return 'open'
        if status == 2:
            return 'closed'
        if status == 3:
            return 'canceled'
        if status == 4:
            return 'expired'
        if status == 5:
            return 'rejected'
        return None

    def handle_order(self, client: Any, message: Any):
        #
        #     {
        #         "orderUpdateType": "orderConfirm",
        #         "marketId": 2770,
        #         "rootMarketId": 122,
        #         "orderId": "a11ee07e-e22f-11f0-9714-0a58a9feac02",
        #         "side": 1,
        #         "outcomeSide": 1,
        #         "price": "0.150000000000000000",
        #         "shares": "66.66",
        #         "amount": "9.999000000000000000",
        #         "status": 1,
        #         "tradingMethod": 2,
        #         "quoteToken": "0x55d398326f99059fF775485246999027B3197955",
        #         "createdAt": 1766735464,
        #         "expiresAt": 0,
        #         "chainId": "56",
        #         "filledShares": "10.000000000000000000",
        #         "filledAmount": "1.500000000000000000",
        #         "msgType": "trade.order.update"
        #     }
        #
        marketId = self.safe_integer(message, 'marketId')
        outcomeSide = self.safe_integer(message, 'outcomeSide')
        outcomeObj = self.opinion_outcome_by_market_id_side(marketId, outcomeSide)
        timestamp = self.safe_timestamp(message, 'createdAt')
        # unlike the REST order body(0 buy / 1 sell), the websocket channel uses 1 buy / 2 sell
        # per the docs and confirmed live
        sideInt = self.safe_integer(message, 'side')
        side = 'buy' if (sideInt == 1) else 'sell'
        tradingMethod = self.safe_integer(message, 'tradingMethod')
        type = 'market' if (tradingMethod == 1) else 'limit'
        order = self.safe_prediction_order({
            'id': self.safe_string(message, 'orderId'),
            'clientOrderId': None,
            'info': message,
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'lastTradeTimestamp': None,
            'status': self.parse_ws_order_status(self.safe_integer(message, 'status')),
            'outcome': self.safe_string(outcomeObj, 'outcome'),
            'outcomeId': self.safe_string(outcomeObj, 'outcomeId'),
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string(outcomeObj, 'market'),
            'type': type,
            'side': side,
            'price': self.safe_number(message, 'price'),
            'amount': self.safe_number(message, 'shares'),
            'cost': self.safe_number(message, 'filledAmount'),
            'filled': self.safe_number(message, 'filledShares'),
            'fee': None,
            'trades': [],
        }, outcomeObj)
        if self.orders is None:
            limit = self.safe_integer(self.options, 'ordersLimit', 1000)
            self.orders = ArrayCacheByOutcomeById(limit)
        stored = self.orders
        stored.append(order)
        client.resolve(stored, 'orders')

    async def watch_my_trades(self, outcome: Str = None, since: Int = None, limit: Int = None, params={}) -> List[PredictionTrade]:
        """
        streams the authenticated user's executed trades of one market - the venue channel is per-market, so the outcome argument is required

        https://docs.opinion.trade/developer-guide/opinion-websocket/user-channels

        :param str outcome: unified outcome or outcome token id whose market to watch
        :param int [since]: timestamp in ms of the earliest trade to return
        :param int [limit]: the maximum number of trades to return
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: a list of [prediction trade structures](https://docs.ccxt.com/#/?id=prediction-trade-structure)
        """
        if outcome is None:
            raise ArgumentsRequired(self.id + ' watchMyTrades() requires an outcome(the trade record channel is per-market)')
        outcomeObj = await self.load_outcome(outcome)
        info = self.safe_dict(outcomeObj, 'info', {})
        marketId = self.safe_integer(info, 'marketId')
        messageHash = 'myTrades'
        trades = await self.subscribe_opinion_channel(messageHash, 'trade.record.new', marketId)
        sym = self.safe_outcome_symbol(outcome, outcomeObj)
        return self.filter_by_value_since_limit(trades, 'outcome', sym, since, limit, 'timestamp', True)

    def handle_my_trade(self, client: Any, message: Any):
        #
        #     {
        #         "orderId": "3c7af25f-e21f-11f0-9714-0a58a9feac02",
        #         "tradeNo": "e1403840-e22f-11f0-83af-0a58a9feac02",
        #         "marketId": 2770,
        #         "rootMarketId": 122,
        #         "txHash": "0x272c...4195",
        #         "side": "Buy",
        #         "outcomeSide": 2,
        #         "price": "0.100000000000000000",
        #         "shares": "9.44444",
        #         "amount": "0.944444",
        #         "profit": "0.000000000000000000",
        #         "status": 2,
        #         "quoteToken": "0x55d398326f99059fF775485246999027B3197955",
        #         "fee": "0.000000000000000000",
        #         "chainId": "56",
        #         "createdAt": 1766735571,
        #         "msgType": "trade.record.new"
        #     }
        #
        marketId = self.safe_integer(message, 'marketId')
        outcomeSide = self.safe_integer(message, 'outcomeSide')
        outcomeObj = self.opinion_outcome_by_market_id_side(marketId, outcomeSide)
        sym = self.safe_string(outcomeObj, 'outcome')
        timestamp = self.safe_timestamp(message, 'createdAt')
        trade = self.safe_prediction_trade({
            'id': self.safe_string(message, 'tradeNo'),
            'info': message,
            'timestamp': timestamp,
            'datetime': self.iso8601(timestamp),
            'outcome': sym,
            'outcomeId': self.safe_string(outcomeObj, 'outcomeId'),
            'label': self.safe_string(outcomeObj, 'label'),
            'market': self.safe_string(outcomeObj, 'market'),
            'order': self.safe_string(message, 'orderId'),
            'type': None,
            'side': self.safe_string_lower(message, 'side'),
            'takerOrMaker': None,
            'price': self.safe_number(message, 'price'),
            'amount': self.safe_number(message, 'shares'),
            'cost': self.safe_number(message, 'amount'),
            'fee': {
                'cost': self.safe_number(message, 'fee'),
                'currency': 'USDT',
            },
        }, outcomeObj)
        if self.myTrades is None:
            myTradesLimit = self.safe_integer(self.options, 'myTradesLimit', 1000)
            self.myTrades = ArrayCacheByOutcomeById(myTradesLimit)
        stored = self.myTrades
        stored.append(trade)
        client.resolve(stored, 'myTrades')

    def handle_errors(self, code: Int, reason: str, url: str, method: str, headers: dict, body: str, response: Any, requestHeaders: Any, requestBody: Any):
        if response is None:
            return None
        errno = self.safe_integer(response, 'errno')
        if (errno is not None) and (errno != 0):
            errmsg = self.safe_string(response, 'errmsg', '')
            feedback = self.id + ' ' + body
            self.throw_exactly_matched_exception(self.exceptions['exact'], self.number_to_string(errno), feedback)
            self.throw_broadly_matched_exception(self.exceptions['broad'], errmsg, feedback)
            raise ExchangeError(feedback)
        return None

    def sign(self, path: Any, api: Any = 'opinion', method='GET', params={}, headers: Any = None, body: Any = None):
        """
 @ignore
        builds the request url and attaches the apikey/EIP-712 authentication headers for private endpoints
        :param str path: the endpoint path
        :param string|str[] api: the api group and access level
        :param str method: the http method
        :param dict params: the request parameters
        :param dict [headers]: request headers
        :param str [body]: the request body
        :returns dict: a dict with url, method, body and headers
        """
        apiGroup = api if isinstance(api, str) else api[0]
        access = 'public' if isinstance(api, str) else api[1]
        baseUrls = self.urls['api']
        baseUrl = self.safe_string(baseUrls, apiGroup, baseUrls['opinion'])
        url = baseUrl + '/' + self.implode_params(path, params)
        query = self.omit(params, self.extract_params(path))
        existingHeaders = headers if (headers is not None) else {}
        headers = self.extend({
            'Accept': 'application/json',
            'Content-Type': 'application/json',
        }, existingHeaders)
        if access == 'private':
            if path == 'auth/api-key':
                # wallet-signature scheme: no apiKey involved, the signature itself is the credential
                if (self.walletAddress is None) or (self.privateKey is None):
                    raise ArgumentsRequired(self.id + ' ' + path + ' requires a walletAddress and privateKey')
                actionByMethod = {'POST': 'create', 'GET': 'get', 'DELETE': 'delete'}
                action = self.safe_string(actionByMethod, method, 'get')
                timestamp = self.number_to_string(self.seconds())
                headers['OPINION_ADDRESS'] = self.walletAddress
                headers['OPINION_SIGNATURE'] = self.sign_api_key_auth(self.walletAddress, action, timestamp)
                headers['OPINION_TIMESTAMP'] = timestamp
            else:
                # an empty self.apiKey counts - deleteApiKey clears it to ''(the
                # strict base types the credential, None can not be assigned)
                hasDirectApiKey = not self.is_empty_string(self.apiKey)
                apiKey = self.apiKey if (hasDirectApiKey) else self.safe_string(self.options, 'apiKey')
                if apiKey is None:
                    raise AuthenticationError(self.id + ' ' + path + ' requires an apiKey - set it directly or call createApiKey()/fetchApiKey() first')
                headers['apikey'] = apiKey
        if method == 'GET':
            if query:
                url += '?' + self.urlencode(query)
        else:
            body = self.json(query)
        return {'url': url, 'method': method, 'body': body, 'headers': headers}
