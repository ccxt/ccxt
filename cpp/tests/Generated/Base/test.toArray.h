#pragma once

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#include "../../BaseTest.Bridge.h"

// forward declarations - TS hoists function declarations, C++ does not
void testToArray();

void testToArray()
{
    ccxt::Exchange exchange = ccxt::Exchange(ccxt::dict {
        { std::string("id"), std::string("sampleexchange") },
    });
    std::any obj1 = ccxt::dict {
        { std::string("a"), 1 },
        { std::string("b"), 3 },
        { std::string("c"), 2 },
    };
    std::any obj2 = ccxt::dict {
        { std::string("a"), std::string("x") },
        { std::string("b"), 2 },
    };
    std::any result1 = exchange.toArray(obj1);
    std::any result2 = exchange.toArray(obj2);
    // we can't guarantee order of values in GO lang
    // assertDeepEqual (exchange, undefined, 'testToArray', exchange.toArray (obj1), [ 1, 3, 2 ]);
    // assertDeepEqual (exchange, undefined, 'testToArray', exchange.toArray (obj2), [ 'x', 2 ]);
    //
    assertTrue(isEqual(getArrayLength(result1), 3), std::string("testToArray: length of result1 should be 3"));
    assertTrue(isEqual(getArrayLength(result2), 2), std::string("testToArray: length of result2 should be 2"));
    assertTrue(isTrue(isTrue(exchange.inArray(1, result1)) && isTrue(exchange.inArray(3, result1))) && isTrue(exchange.inArray(2, result1)), std::string("testToArray: result1 should include 1, 3, and 2"));
    assertTrue(isTrue(exchange.inArray(std::string("x"), result2)) && isTrue(exchange.inArray(2, result2)), std::string("testToArray: result2 should include \"x\" and 2"));
}

