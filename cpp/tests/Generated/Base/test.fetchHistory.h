#pragma once

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#include "../../BaseTest.Bridge.h"

// forward declarations - TS hoists function declarations, C++ does not
std::shared_future<std::any> testFetchHistoryBase();
std::shared_future<std::any> testFetchHistory();

std::shared_future<std::any> testFetchHistoryBase()
{
    return std::async(std::launch::deferred, [=]() mutable -> std::any {
        ccxt::Exchange exchange = ccxt::Exchange(ccxt::dict {
            { std::string("id"), std::string("sampleexchange") },
            { std::string("fetchHistoryCacheSize"), 2 },
        });
        assertTrue(isEqual(exchangeProp(exchange, std::string("fetchHistoryCacheSize")), 2), std::string("fetchHistoryCacheSize should be 2"));
        std::any trueAssertion = isEqual(exchange.parseNumber(std::any{}), std::any{});
        try
        {
            awaitValue(exchange.fetch2(std::string("sample1")));
        } catch(const std::exception& error)
        {
            assertTrue(trueAssertion); // just skip
        }
        assertTrue(isEqual(getArrayLength((exchange.getFetchCache())), 1), std::string("fetchHistoryCache should be an array with 1 element"));
        try
        {
            awaitValue(exchange.fetch2(std::string("sample2")));
        } catch(const std::exception& error)
        {
            assertTrue(trueAssertion); // just skip
        }
        assertTrue(isEqual(getArrayLength((exchange.getFetchCache())), 2), std::string("fetchHistoryCache should be an array with 2 elements"));
        try
        {
            awaitValue(exchange.fetch2(std::string("sample3")));
        } catch(const std::exception& error)
        {
            assertTrue(trueAssertion); // just skip
        }
        assertTrue(isEqual(getArrayLength((exchange.getFetchCache())), 2), std::string("fetchHistoryCache should be an array with 2 elements"));
        assertTrue(isLessThan(add(1, 1), 3), std::string("sample assertion"));
        return std::any{};
    }).share();
}
// async function testFetchHistoryDerived () {
//     const exchange = new ccxt.coinbase ({
//         'id': 'sampleexchange',
//         'fetchHistoryCacheSize': 2,
//     });
//     // try 3 times
//     // first
//     await exchange.fetchTime (); // https://api.coinbase.com/api/v3/brokerage/time
//     assert ((exchange.getFetchCache ()).length === 1, 'fetchHistoryCache should be an array with 1 element');
//     // second
//     await exchange.fetchOrderBook ('BTC/USD'); // https://api.coinbase.com/api/v3/brokerage/market/product_book?product_id=BTC-USD
//     assert ((exchange.getFetchCache ()).length === 2, 'fetchHistoryCache should be an array with 2 elements');
//     // third
//     await exchange.fetchTrades ('BTC/USD'); // https://api.coinbase.com/api/v3/brokerage/market/products/BTC-USD/ticker
//     assert ((exchange.getFetchCache ()).length === 2, 'fetchHistoryCache should be an array with 2 elements');
//     const finalCache = exchange.getFetchCache ();
//     assert (finalCache[0]['request']['url'].toString () === 'https://api.coinbase.com/api/v3/brokerage/market/product_book?product_id=BTC-USD', 'The first element in fetchHistoryCache is : ' + finalCache[0]['request']['url']);
//     assert (finalCache[1]['request']['url'].toString () === 'https://api.coinbase.com/api/v3/brokerage/market/products/BTC-USD/ticker', 'The second element in fetchHistoryCache is : ' + finalCache[1]['request']['url']);
//     assert (1 + 1 < 3, 'sample assertion');
// }
std::shared_future<std::any> testFetchHistory()
{
    return std::async(std::launch::deferred, [=]() mutable -> std::any {
        awaitValue(testFetchHistoryBase());
        return std::any{};
    }).share();
}

