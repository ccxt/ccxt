#pragma once

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#include "../api/mudrex.h"
#include "../base/Exchange.h"

namespace ccxt {

class mudrex : public mudrexApi {
public:
  using mudrexApi::mudrexApi;
  ccxt::any describe() override {
    return this->deepExtend(
        Exchange::describe(),
        ccxt::dict{
            {std::string("id"), std::string("mudrex")},
            {std::string("name"), std::string("Mudrex")},
            {std::string("countries"), ccxt::list{std::string("IN")}},
            {std::string("rateLimit"), 100},
            {std::string("version"), std::string("v1")},
            {std::string("pro"), true},
            {std::string("certified"), false},
            {std::string("dex"), false},
            {std::string("hostname"), std::string("trade.mudrex.com")},
            {std::string("has"),
             ccxt::dict{
                 {std::string("CORS"), ccxt::any{}},
                 {std::string("spot"), false},
                 {std::string("margin"), false},
                 {std::string("swap"), true},
                 {std::string("future"), false},
                 {std::string("option"), false},
                 {std::string("addMargin"), true},
                 {std::string("cancelOrder"), true},
                 {std::string("closePosition"), true},
                 {std::string("createMarketOrder"), true},
                 {std::string("createOrder"), true},
                 {std::string("createOrderWithTakeProfitAndStopLoss"), true},
                 {std::string("createReduceOnlyOrder"), true},
                 {std::string("editOrder"), true},
                 {std::string("fetchBalance"), true},
                 {std::string("fetchClosedOrders"), true},
                 {std::string("fetchFundingRate"), false},
                 {std::string("fetchFundingRateHistory"), false},
                 {std::string("fetchFundingRates"), false},
                 {std::string("fetchIndexOHLCV"), false},
                 {std::string("fetchLeverage"), true},
                 {std::string("fetchMarkets"), true},
                 {std::string("fetchMarkOHLCV"), true},
                 {std::string("fetchMyTrades"), true},
                 {std::string("fetchOHLCV"), true},
                 {std::string("fetchOpenInterest"), false},
                 {std::string("fetchOpenInterests"), false},
                 {std::string("fetchOpenOrders"), true},
                 {std::string("fetchOrder"), true},
                 {std::string("fetchOrderBook"), false},
                 {std::string("fetchOrders"), true},
                 {std::string("fetchPositions"), true},
                 {std::string("fetchPositionsHistory"), true},
                 {std::string("fetchTicker"), true},
                 {std::string("fetchTickers"), true},
                 {std::string("fetchTrades"), false},
                 {std::string("reduceMargin"), true},
                 {std::string("setLeverage"), true},
                 {std::string("transfer"), true},
                 {std::string("watchOHLCV"), true},
                 {std::string("watchTicker"), true},
                 {std::string("watchTickers"), true},
             }},
            {std::string("timeframes"),
             ccxt::dict{
                 {std::string("1m"), std::string("1m")},
                 {std::string("3m"), std::string("3t")},
                 {std::string("5m"), std::string("5t")},
                 {std::string("10m"), std::string("10t")},
                 {std::string("15m"), std::string("15t")},
                 {std::string("30m"), std::string("30t")},
                 {std::string("1h"), std::string("1h")},
                 {std::string("4h"), std::string("4h")},
                 {std::string("6h"), std::string("6h")},
                 {std::string("12h"), std::string("12h")},
                 {std::string("1d"), std::string("1d")},
                 {std::string("1w"), std::string("1w")},
                 {std::string("1M"), std::string("1mth")},
             }},
            {std::string("urls"),
             ccxt::dict{
                 {std::string("logo"),
                  std::string("https://github.com/user-attachments/assets/"
                              "72368864-84ed-43eb-8c75-d4fb77023b42")},
                 {std::string("api"),
                  ccxt::dict{
                      {std::string("public"),
                       std::string("https://trade.mudrex.com/fapi/v1")},
                      {std::string("private"),
                       std::string("https://trade.mudrex.com/fapi/v1")},
                      {std::string("market"),
                       std::string("https://trade.mudrex.com/fapi/v1")},
                  }},
                 {std::string("www"), std::string("https://mudrex.com")},
                 {std::string("doc"),
                  std::string("https://docs.trade.mudrex.com/docs")},
                 {std::string("fees"),
                  std::string("https://docs.trade.mudrex.com")},
             }},
            {std::string("api"),
             ccxt::dict{
                 {std::string("market"),
                  ccxt::dict{
                      {std::string("get"),
                       ccxt::dict{
                           {std::string("price/kline"),
                            ccxt::dict{
                                {std::string("cost"), 1},
                            }},
                           {std::string("price/mark-kline"),
                            ccxt::dict{
                                {std::string("cost"), 1},
                            }},
                       }},
                  }},
                 {std::string("public"),
                  ccxt::dict{
                      {std::string("get"), ccxt::dict{}},
                  }},
                 {std::string("private"),
                  ccxt::dict{
                      {std::string("get"),
                       ccxt::dict{
                           {std::string("futures"),
                            ccxt::dict{
                                {std::string("cost"), 1},
                            }},
                           {std::string("futures/{asset_id}"),
                            ccxt::dict{
                                {std::string("cost"), 1},
                            }},
                           {std::string("wallet/funds"),
                            ccxt::dict{
                                {std::string("cost"), 5},
                            }},
                           {std::string("futures/funds"),
                            ccxt::dict{
                                {std::string("cost"), 5},
                            }},
                           {std::string("futures/orders"),
                            ccxt::dict{
                                {std::string("cost"), 1},
                            }},
                           {std::string("futures/orders/history"),
                            ccxt::dict{
                                {std::string("cost"), 1},
                            }},
                           {std::string("futures/orders/{order_id}"),
                            ccxt::dict{
                                {std::string("cost"), 1},
                            }},
                           {std::string("futures/positions"),
                            ccxt::dict{
                                {std::string("cost"), 1},
                            }},
                           {std::string("futures/positions/history"),
                            ccxt::dict{
                                {std::string("cost"), 1},
                            }},
                           {std::string("futures/fee/history"),
                            ccxt::dict{
                                {std::string("cost"), 1},
                            }},
                           {std::string("futures/{asset_id}/leverage"),
                            ccxt::dict{
                                {std::string("cost"), 2},
                            }},
                           {std::string(
                                "futures/positions/{position_id}/liq-price"),
                            ccxt::dict{
                                {std::string("cost"), 1},
                            }},
                       }},
                      {std::string("post"),
                       ccxt::dict{
                           {std::string("wallet/futures/transfer"),
                            ccxt::dict{
                                {std::string("cost"), 5},
                            }},
                           {std::string("futures/transfers/inr"),
                            ccxt::dict{
                                {std::string("cost"), 5},
                            }},
                           {std::string("futures/{asset_id}/order"),
                            ccxt::dict{
                                {std::string("cost"), 2},
                            }},
                           {std::string(
                                "futures/positions/{position_id}/close"),
                            ccxt::dict{
                                {std::string("cost"), 2},
                            }},
                           {std::string("futures/positions/{position_id}/close/"
                                        "partial"),
                            ccxt::dict{
                                {std::string("cost"), 2},
                            }},
                           {std::string(
                                "futures/positions/{position_id}/reverse"),
                            ccxt::dict{
                                {std::string("cost"), 2},
                            }},
                           {std::string(
                                "futures/positions/{position_id}/add-margin"),
                            ccxt::dict{
                                {std::string("cost"), 2},
                            }},
                           {std::string(
                                "futures/positions/{position_id}/riskorder"),
                            ccxt::dict{
                                {std::string("cost"), 2},
                            }},
                           {std::string("futures/{asset_id}/leverage"),
                            ccxt::dict{
                                {std::string("cost"), 2},
                            }},
                       }},
                      {std::string("patch"),
                       ccxt::dict{
                           {std::string("futures/orders/{order_id}"),
                            ccxt::dict{
                                {std::string("cost"), 1},
                            }},
                           {std::string(
                                "futures/positions/{position_id}/riskorder"),
                            ccxt::dict{
                                {std::string("cost"), 2},
                            }},
                       }},
                      {std::string("delete"),
                       ccxt::dict{
                           {std::string("futures/orders/{order_id}"),
                            ccxt::dict{
                                {std::string("cost"), 2},
                            }},
                       }},
                  }},
             }},
            {std::string("requiredCredentials"),
             ccxt::dict{
                 {std::string("apiKey"), false},
                 {std::string("secret"), true},
             }},
            {std::string("fees"),
             ccxt::dict{
                 {std::string("trading"),
                  ccxt::dict{
                      {std::string("tierBased"), false},
                      {std::string("percentage"), true},
                      {std::string("taker"),
                       this->parseNumber(std::string("0.00059"))},
                      {std::string("maker"),
                       this->parseNumber(std::string("0.00023"))},
                  }},
             }},
            {std::string("options"),
             ccxt::dict{
                 {std::string("defaultType"), std::string("swap")},
                 {std::string("broker"),
                  std::string("42ce8902-8585-448c-a1e8-0371a6ca7ca8")},
             }},
            {std::string("exceptions"),
             ccxt::dict{
                 {std::string("exact"),
                  ccxt::dict{
                      {std::string("400 Invalid trade currency"),
                       std::string("BadRequest")},
                  }},
                 {std::string("broad"),
                  ccxt::dict{
                      {std::string("Invalid trade currency"),
                       std::string("BadRequest")},
                      {std::string("Params error"), std::string("BadRequest")},
                      {std::string("invalid trigger type"),
                       std::string("BadRequest")},
                      {std::string("invalid order type"),
                       std::string("BadRequest")},
                      {std::string("order price out of permissible range"),
                       std::string("BadRequest")},
                      {std::string(
                           "quantity not a multiple of the quantity step"),
                       std::string("BadRequest")},
                      {std::string("leverage out of permissible range"),
                       std::string("BadRequest")},
                      {std::string("insufficient balance"),
                       std::string("InsufficientFunds")},
                      {std::string("asset not found"),
                       std::string("BadSymbol")},
                      {std::string("leverage not found"),
                       std::string("OrderNotFound")},
                      {std::string("order not found"),
                       std::string("OrderNotFound")},
                      {std::string("Rate limit exceeded"),
                       std::string("RateLimitExceeded")},
                  }},
             }},
        });
  }

  ccxt::any sign(ccxt::any path, ccxt::any api = std::string("public"),
                 ccxt::any method = std::string("GET"),
                 ccxt::any params = ccxt::dict{},
                 ccxt::any headers = ccxt::any{},
                 ccxt::any body = ccxt::any{}) override {
    ccxt::any apiUrls =
        this->safeDict(this->urls, std::string("api"), ccxt::dict{});
    ccxt::any base = this->safeString(apiUrls, api);
    if (isTrue(isEqual(base, ccxt::any{}))) {
      throw ExchangeError(toString(
          add(add(this->id, std::string(" unknown API namespace: ")), api)));
    }
    ccxt::any url =
        add(add(base, std::string("/")), this->implodeParams(path, params));
    ccxt::any query = this->omit(params, this->extractParams(path));
    ccxt::any requestHeaders = ccxt::dict{};
    if (isTrue(!isEqual(headers, ccxt::any{}))) {
      requestHeaders = this->extend(ccxt::dict{}, headers);
    }
    ccxt::any brokerId = this->safeString(this->options, std::string("broker"));
    if (isTrue(!isEqual(brokerId, ccxt::any{}))) {
      ::setValue(requestHeaders, std::string("Partner-Id"), brokerId);
    }
    ccxt::any methodUpper = toUpperCase(method);
    if (isTrue(isEqual(api, std::string("private")))) {
      this->checkRequiredCredentials();
      ::setValue(requestHeaders, std::string("X-Authentication"), this->secret);
      if (isTrue(isTrue(isTrue(isEqual(methodUpper, std::string("POST"))) ||
                        isTrue(isEqual(methodUpper, std::string("PATCH")))) ||
                 isTrue(isEqual(methodUpper, std::string("DELETE"))))) {
        ::setValue(requestHeaders, std::string("Content-Type"),
                   std::string("application/json"));
        // is_symbol is a query-string flag even on write requests
        ccxt::any isSymbol = this->safeString(query, std::string("is_symbol"));
        if (isTrue(!isEqual(isSymbol, ccxt::any{}))) {
          query = this->omit(query, std::string("is_symbol"));
          url = add(url, add(std::string("?"),
                             this->urlencode(ccxt::dict{
                                 {std::string("is_symbol"), isSymbol},
                             })));
        }
        if (isTrue(isTrue((isEqual(methodUpper, std::string("DELETE")))) &&
                   isTrue(this->isEmpty(query)))) {
          return ccxt::dict{
              {std::string("url"), url},
              {std::string("method"), methodUpper},
              {std::string("body"), ccxt::any{}},
              {std::string("headers"), requestHeaders},
          };
        }
        ccxt::any bodyStr = this->json(query);
        return ccxt::dict{
            {std::string("url"), url},
            {std::string("method"), methodUpper},
            {std::string("body"), bodyStr},
            {std::string("headers"), requestHeaders},
        };
      }
    }
    if (isTrue(isGreaterThan(getArrayLength(getObjectKeys(query)), 0))) {
      url = add(url, add(std::string("?"), this->urlencode(query)));
    }
    return ccxt::dict{
        {std::string("url"), url},
        {std::string("method"), methodUpper},
        {std::string("body"), ccxt::any{}},
        {std::string("headers"), requestHeaders},
    };
  }

  ccxt::any handleErrors(ccxt::any code, ccxt::any reason, ccxt::any url,
                         ccxt::any method, ccxt::any headers, ccxt::any body,
                         ccxt::any response, ccxt::any requestHeaders,
                         ccxt::any requestBody) override {
    if (isTrue(isTrue(isEqual(response, ccxt::any{})) ||
               isTrue(!isDictionary(response)))) {
      return ccxt::any{};
    }
    ccxt::any success = this->safeBool(response, std::string("success"), true);
    if (isTrue(!isEqual(success, true))) {
      ccxt::any errors =
          this->safeList(response, std::string("errors"), ccxt::list{});
      ccxt::any first = this->safeDict(errors, 0, ccxt::dict{});
      ccxt::any text =
          this->safeString(first, std::string("text"), this->json(response));
      ccxt::any errCode = this->safeString(first, std::string("code"));
      this->throwExactlyMatchedException(
          ::getValue(this->exceptions, std::string("exact")), text,
          add(add(this->id, std::string(" ")), text));
      this->throwExactlyMatchedException(
          ::getValue(this->exceptions, std::string("exact")), errCode,
          add(add(this->id, std::string(" ")), text));
      this->throwBroadlyMatchedException(
          ::getValue(this->exceptions, std::string("broad")), text,
          add(add(this->id, std::string(" ")), text));
      ccxt::any msg = add(add(this->id, std::string(" ")), text);
      ccxt::any low = toLowerCase(text);
      if (isTrue(isTrue(isEqual(code, 401)) ||
                 isTrue(isGreaterThanOrEqual(
                     getIndexOf(low, std::string("auth")), 0)))) {
        throw AuthenticationError(toString(msg));
      }
      if (isTrue(isTrue(isEqual(code, 429)) ||
                 isTrue(isGreaterThanOrEqual(
                     getIndexOf(low, std::string("rate")), 0)))) {
        throw RateLimitExceeded(toString(msg));
      }
      if (isTrue(isGreaterThanOrEqual(
              getIndexOf(low, std::string("insufficient")), 0))) {
        throw InsufficientFunds(toString(msg));
      }
      if (isTrue(isEqual(code, 400))) {
        throw BadRequest(toString(msg));
      }
      throw ExchangeError(toString(msg));
    }
    return ccxt::any{};
  }

  ccxt::any parseOHLCV(ccxt::any ohlcv,
                       ccxt::any market = ccxt::any{}) override {
    //
    //     [ 1782984660, 60681, 60797.6, 60671.8, 60693.3, 275.741 ]
    //     [ timestampInSeconds, open, high, low, close, volume ]
    //
    return ccxt::list{
        this->safeTimestamp(ohlcv, 0), this->safeNumber(ohlcv, 1),
        this->safeNumber(ohlcv, 2),    this->safeNumber(ohlcv, 3),
        this->safeNumber(ohlcv, 4),    this->safeNumber(ohlcv, 5)};
  }

  /**
   * @method
   * @name mudrex#fetchOHLCV
   * @description fetches historical candlestick data containing the open, high,
   * low, and close price, and the volume of a market
   * @see https://docs.trade.mudrex.com/docs/historical-kline
   * @param {string} symbol unified symbol of the market to fetch OHLCV data for
   * @param {string} timeframe the length of time each candle represents
   * @param {int} [since] timestamp in ms of the earliest candle to fetch
   * @param {int} [limit] the maximum amount of candles to fetch
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {int} [params.until] timestamp in ms of the latest candle to fetch
   * @param {string} [params.price] "mark" to fetch mark price candles
   * @returns {int[][]} A list of candles ordered as timestamp, open, high, low,
   * close, volume
   */
  std::shared_future<ccxt::any>
  fetchOHLCV(ccxt::any symbol, ccxt::any timeframe = std::string("1m"),
             ccxt::any since = ccxt::any{}, ccxt::any limit = ccxt::any{},
             ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = this->market(symbol);
                 ccxt::any priceType =
                     this->safeString(params, std::string("price"));
                 params = this->omit(params, std::string("price"));
                 // the endpoint expects the pair in "BASE/QUOTE" format
                 // (comma-separated for multiple)
                 ccxt::any assetPair =
                     add(add(::getValue(market, std::string("baseId")),
                             std::string("/")),
                         ::getValue(market, std::string("quoteId")));
                 ccxt::any request = ccxt::dict{
                     {std::string("assets"), assetPair},
                     {std::string("aggregation"),
                      this->safeString(this->timeframes, timeframe, timeframe)},
                 };
                 // the endpoint requires an explicit time window (in seconds)
                 ccxt::any duration = this->parseTimeframe(timeframe);
                 ccxt::any requestLimit = limit;
                 if (isTrue(isEqual(requestLimit, ccxt::any{}))) {
                   requestLimit = 500;
                 }
                 ccxt::any now = this->seconds();
                 ccxt::any startTime = ccxt::any{};
                 if (isTrue(!isEqual(since, ccxt::any{}))) {
                   startTime = this->parseToInt(divide(since, 1000));
                 } else {
                   startTime = subtract(now, multiply(duration, requestLimit));
                 }
                 if (isTrue(isEqual(startTime, ccxt::any{}))) {
                   throw ExchangeError(toString(
                       add(this->id,
                           std::string(" fetchOHLCV() missing startTime"))));
                 }
                 ccxt::any endTime =
                     add(startTime, multiply(duration, requestLimit));
                 ccxt::any until =
                     this->safeInteger(params, std::string("until"));
                 if (isTrue(!isEqual(until, ccxt::any{}))) {
                   params = this->omit(params, std::string("until"));
                   endTime = this->parseToInt(divide(until, 1000));
                 } else if (isTrue(isGreaterThan(endTime, now))) {
                   endTime = now;
                 }
                 ::setValue(request, std::string("start_time"), startTime);
                 ::setValue(request, std::string("end_time"), endTime);
                 ccxt::any response = ccxt::any{};
                 if (isTrue(isEqual(priceType, std::string("mark")))) {
                   response = awaitValue(this->marketGetPriceMarkKline(
                       this->extend(request, params)));
                 } else {
                   response = awaitValue(this->marketGetPriceKline(
                       this->extend(request, params)));
                 }
                 //
                 //     {
                 //         "success": true,
                 //         "data": {
                 //             "asset_ticks": {
                 //                 "btc/usdt": [ [ 1782984660, 60681, 60797.6,
                 //                 60671.8, 60693.3, 275.741 ] ]
                 //             }
                 //         }
                 //     }
                 //
                 ccxt::any data = this->safeDict(response, std::string("data"),
                                                 ccxt::dict{});
                 ccxt::any assetTicks = this->safeDict(
                     data, std::string("asset_ticks"), ccxt::dict{});
                 ccxt::any ohlcvs = this->safeList(
                     assetTicks, toLowerCase(assetPair), ccxt::list{});
                 return this->parseOHLCVs(ohlcvs, market, timeframe, since,
                                          limit);
               })
        .share();
  }

  /**
   * @method
   * @name mudrex#fetchMarkOHLCV
   * @description fetches historical mark price candlestick data containing the
   * open, high, low, and close price of a market
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} symbol unified symbol of the market to fetch OHLCV data for
   * @param {string} timeframe the length of time each candle represents
   * @param {int} [since] timestamp in ms of the earliest candle to fetch
   * @param {int} [limit] the maximum amount of candles to fetch
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {int[][]} A list of candles ordered as timestamp, open, high, low,
   * close, volume
   */
  std::shared_future<ccxt::any>
  fetchMarkOHLCV(ccxt::any symbol, ccxt::any timeframe = std::string("1m"),
                 ccxt::any since = ccxt::any{}, ccxt::any limit = ccxt::any{},
                 ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        return awaitValue(this->fetchOHLCV(
                            symbol, timeframe, since, limit,
                            this->extend(params, ccxt::dict{
                                                     {std::string("price"),
                                                      std::string("mark")},
                                                 })));
                      })
        .share();
  }

  /**
   * @method
   * @name mudrex#fetchTicker
   * @description fetches a price ticker, a statistical calculation with the
   * information calculated over the past 24 hours for a specific market
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} symbol unified symbol of the market to fetch the ticker for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [ticker
   * structure](https://docs.ccxt.com/#/?id=ticker-structure)
   */
  std::shared_future<ccxt::any>
  fetchTicker(ccxt::any symbol, ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                          awaitValue(this->loadMarkets());
                        }
                        ccxt::any market = this->market(symbol);
                        ccxt::any request = ccxt::dict{
                            {std::string("asset_id"),
                             ::getValue(market, std::string("id"))},
                            {std::string("is_symbol"), 1},
                        };
                        ccxt::any response =
                            awaitValue(this->privateGetFuturesAssetId(
                                this->extend(request, params)));
                        ccxt::any data = this->safeDict(
                            response, std::string("data"), ccxt::dict{});
                        return this->parseTicker(data, market);
                      })
        .share();
  }

  /**
   * @method
   * @name mudrex#fetchTickers
   * @description fetches price tickers for multiple markets, statistical
   * information calculated over the past 24 hours for each market
   * @see https://docs.trade.mudrex.com/docs
   * @param {string[]} [symbols] unified symbols of the markets to fetch the
   * ticker for, all market tickers are returned if not assigned
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a dictionary of [ticker
   * structures](https://docs.ccxt.com/#/?id=ticker-structure)
   */
  std::shared_future<ccxt::any>
  fetchTickers(ccxt::any symbols = ccxt::any{},
               ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any request = ccxt::dict{};
                 ccxt::any response = awaitValue(
                     this->privateGetFutures(this->extend(request, params)));
                 ccxt::any data = this->safeValue(response, std::string("data"),
                                                  ccxt::list{});
                 ccxt::any rows =
                     (isTrue(isArray(data))
                          ? ccxt::any(data)
                          : ccxt::any(this->safeList(data, std::string("items"),
                                                     ccxt::list{})));
                 ccxt::any resultTickers = ccxt::dict{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(rows));
                      postFixIncrement(i)) {
                   ccxt::any t = ::getValue(rows, i);
                   ccxt::any sym = this->safeString(t, std::string("symbol"));
                   if (isTrue(isEqual(sym, ccxt::any{}))) {
                     continue;
                   }
                   ccxt::any m = this->safeMarket(sym);
                   ccxt::any symbol = ::getValue(m, std::string("symbol"));
                   if (isTrue(isTrue(!isEqual(symbols, ccxt::any{})) &&
                              !isTrue(this->inArray(symbol, symbols)))) {
                     continue;
                   }
                   ::setValue(resultTickers, symbol, this->parseTicker(t, m));
                 }
                 return this->filterByArrayTickers(
                     resultTickers, std::string("symbol"), symbols);
               })
        .share();
  }

  ccxt::any parseTicker(ccxt::any ticker,
                        ccxt::any market = ccxt::any{}) override {
    ccxt::any ms = this->safeString(ticker, std::string("symbol"));
    market = this->safeMarket(ms, market);
    ccxt::any symbol = ::getValue(market, std::string("symbol"));
    ccxt::any ts = this->milliseconds();
    ccxt::any pct = this->safeNumber(ticker, std::string("change_perc"));
    return this->safeTicker(
        ccxt::dict{
            {std::string("symbol"), symbol},
            {std::string("timestamp"), ts},
            {std::string("datetime"), this->iso8601(ts)},
            {std::string("high"), ccxt::any{}},
            {std::string("low"), ccxt::any{}},
            {std::string("bid"), ccxt::any{}},
            {std::string("bidVolume"), ccxt::any{}},
            {std::string("ask"), ccxt::any{}},
            {std::string("askVolume"), ccxt::any{}},
            {std::string("vwap"), ccxt::any{}},
            {std::string("open"),
             this->safeNumber(ticker, std::string("last_day_price"))},
            {std::string("close"),
             this->safeNumber(ticker, std::string("price"))},
            {std::string("last"),
             this->safeNumber(ticker, std::string("price"))},
            {std::string("previousClose"), ccxt::any{}},
            {std::string("change"), ccxt::any{}},
            {std::string("percentage"), pct},
            {std::string("average"), ccxt::any{}},
            {std::string("baseVolume"), ccxt::any{}},
            {std::string("quoteVolume"),
             this->safeNumber(ticker, std::string("volume"))},
            {std::string("info"), ticker},
        },
        market);
  }

  /**
   * @method
   * @name mudrex#fetchMarkets
   * @description retrieves data on all markets for the exchange
   * @see https://docs.trade.mudrex.com/docs
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} an array of objects representing market data
   */
  std::shared_future<ccxt::any>
  fetchMarkets(ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 ccxt::any aggregated = ccxt::list{};
                 ccxt::any offset = 0;
                 ccxt::any pageLimit = 100;
                 ccxt::any paging = true;
                 while (isEqual(paging, true)) {
                   ccxt::any q = this->extend(
                       ccxt::dict{
                           {std::string("limit"), pageLimit},
                           {std::string("offset"), offset},
                       },
                       params);
                   ccxt::any response = awaitValue(this->privateGetFutures(q));
                   ccxt::any data = this->safeValue(
                       response, std::string("data"), ccxt::list{});
                   ccxt::any items = ccxt::list{};
                   if (isTrue(isTrue(isDictionary(data)) &&
                              !isTrue(isArray(data)))) {
                     items = this->safeList(data, std::string("items"),
                                            ccxt::list{});
                     // hoisted - inline length reads within conditionals become
                     // strlen for php, fatal on arrays
                     ccxt::any itemsLength = getArrayLength(items);
                     if (isTrue(isTrue((isEqual(itemsLength, ccxt::any{}))) ||
                                isTrue((isEqual(itemsLength, 0))))) {
                       items = this->safeList(data, std::string("results"),
                                              ccxt::list{});
                       itemsLength = getArrayLength(items);
                     }
                     if (isTrue(isTrue((isEqual(itemsLength, 0))) &&
                                isTrue((inOp(data, std::string("symbol")))))) {
                       items = ccxt::list{data};
                     }
                   } else {
                     items = this->toArray(data);
                   }
                   ccxt::any numItems = getArrayLength(items);
                   if (isTrue(isTrue((isEqual(numItems, ccxt::any{}))) ||
                              isTrue((isEqual(numItems, 0))))) {
                     paging = false;
                     break;
                   }
                   for (ccxt::any i = 0; isLessThan(i, numItems);
                        postFixIncrement(i)) {
                     arrayPush(aggregated, ::getValue(items, i));
                   }
                   if (isTrue(isLessThan(numItems, pageLimit))) {
                     paging = false;
                   } else {
                     // this.sum keeps the offset numeric across the php
                     // transpile, see https://github.com/ccxt/ccxt/pull/29684
                     offset = this->sum(offset, pageLimit);
                   }
                 }
                 ccxt::any result = ccxt::list{};
                 for (ccxt::any i = 0;
                      isLessThan(i, getArrayLength(aggregated));
                      postFixIncrement(i)) {
                   arrayPush(result,
                             this->parseMarket(::getValue(aggregated, i)));
                 }
                 return result;
               })
        .share();
  }

  ccxt::any parseMarket(ccxt::any asset) override {
    ccxt::any ms = this->safeString(asset, std::string("symbol"));
    ccxt::any base = ms;
    if (isTrue(isTrue(!isEqual(ms, ccxt::any{})) &&
               isTrue(endsWith(ms, std::string("USDT"))))) {
      base = slice(ms, 0, -4);
    }
    ccxt::any quote = std::string("USDT");
    ccxt::any settle = std::string("USDT");
    ccxt::any symbol = ccxt::any{};
    if (isTrue(!isEqual(base, ccxt::any{}))) {
      symbol =
          add(add(add(add(base, std::string("/")), quote), std::string(":")),
              settle);
    }
    ccxt::any priceStep =
        this->safeString(asset, std::string("price_step"), std::string("0.01"));
    ccxt::any qtyStep = this->safeString(asset, std::string("quantity_step"),
                                         std::string("0.001"));
    return this->safeMarketStructure(ccxt::dict{
        {std::string("id"), ms},
        {std::string("lowercaseId"), ccxt::any{}},
        {std::string("symbol"), symbol},
        {std::string("base"), base},
        {std::string("quote"), quote},
        {std::string("settle"), settle},
        {std::string("baseId"), base},
        {std::string("quoteId"), std::string("USDT")},
        {std::string("settleId"), std::string("USDT")},
        {std::string("type"), std::string("swap")},
        {std::string("spot"), false},
        {std::string("margin"), false},
        {std::string("swap"), true},
        {std::string("future"), false},
        {std::string("option"), false},
        {std::string("active"), true},
        {std::string("contract"), true},
        {std::string("linear"), true},
        {std::string("inverse"), false},
        {std::string("taker"),
         this->safeNumber(::getValue(this->fees, std::string("trading")),
                          std::string("taker"))},
        {std::string("maker"),
         this->safeNumber(::getValue(this->fees, std::string("trading")),
                          std::string("maker"))},
        {std::string("contractSize"),
         this->safeNumber(asset, std::string("contract_size"), 1)},
        {std::string("expiry"), ccxt::any{}},
        {std::string("expiryDatetime"), ccxt::any{}},
        {std::string("strike"), ccxt::any{}},
        {std::string("optionType"), ccxt::any{}},
        {std::string("precision"),
         ccxt::dict{
             {std::string("amount"), this->parseNumber(qtyStep)},
             {std::string("price"), this->parseNumber(priceStep)},
         }},
        {std::string("limits"),
         ccxt::dict{
             {std::string("amount"),
              ccxt::dict{
                  {std::string("min"),
                   this->safeNumber(asset, std::string("min_contract"))},
                  {std::string("max"),
                   this->safeNumber(asset, std::string("max_contract"))},
              }},
             {std::string("price"),
              ccxt::dict{
                  {std::string("min"),
                   this->safeNumber(asset, std::string("min_price"))},
                  {std::string("max"),
                   this->safeNumber(asset, std::string("max_price"))},
              }},
             {std::string("cost"),
              ccxt::dict{
                  {std::string("min"),
                   this->safeNumber(asset, std::string("min_notional_value"))},
                  {std::string("max"), ccxt::any{}},
              }},
         }},
        {std::string("info"), asset},
        {std::string("created"), ccxt::any{}},
    });
  }

  /**
   * @method
   * @name mudrex#fetchBalance
   * @description query for balance and get the amount of funds available for
   * trading or funds locked in orders
   * @see https://docs.trade.mudrex.com/docs
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.type] 'swap' (default) or 'spot' - which wallet
   * balance to fetch
   * @param {string} [params.trade_currency] the settlement currency to query
   * the balance for
   * @returns {object} a [balance
   * structure](https://docs.ccxt.com/#/?id=balance-structure)
   */
  std::shared_future<ccxt::any>
  fetchBalance(ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any type = ccxt::any{};
                 ccxt::any typeparamsVariable = this->handleMarketTypeAndParams(
                     std::string("fetchBalance"), ccxt::any{}, params,
                     std::string("swap"));
                 type = ::getValue(typeparamsVariable, 0);
                 params = ::getValue(typeparamsVariable, 1);
                 ccxt::any requested = this->safeStringN(
                     params, ccxt::list{std::string("trade_currency"),
                                        std::string("tradeCurrency"),
                                        std::string("currency")});
                 params = this->omit(params,
                                     ccxt::list{std::string("trade_currency"),
                                                std::string("tradeCurrency"),
                                                std::string("currency")});
                 ccxt::any request = ccxt::dict{};
                 ccxt::any response = ccxt::any{};
                 if (isTrue(isEqual(type, std::string("spot")))) {
                   if (isTrue(!isEqual(requested, ccxt::any{}))) {
                     ::setValue(request, std::string("currency"), requested);
                   }
                   response = awaitValue(this->privateGetWalletFunds(
                       this->extend(request, params)));
                 } else {
                   if (isTrue(!isEqual(requested, ccxt::any{}))) {
                     ::setValue(request, std::string("trade_currency"),
                                requested);
                   }
                   response = awaitValue(this->privateGetFuturesFunds(
                       this->extend(request, params)));
                 }
                 ccxt::any currency = requested;
                 if (isTrue(isEqual(currency, ccxt::any{}))) {
                   currency = std::string("USDT");
                 }
                 if (isTrue(isEqual(response, ccxt::any{}))) {
                   throw NullResponse(toString(
                       add(this->id,
                           std::string(
                               " fetchBalance() returned empty response"))));
                 }
                 ::setValue(response, std::string("currency"), currency);
                 return this->parseBalance(response);
               })
        .share();
  }

  ccxt::any parseBalance(ccxt::any response) override {
    ccxt::any data =
        this->safeDict(response, std::string("data"), ccxt::dict{});
    ccxt::any currency = this->safeString(response, std::string("currency"),
                                          std::string("USDT"));
    ccxt::any timestamp = this->milliseconds();
    ccxt::any result = ccxt::dict{
        {std::string("info"), response},
        {std::string("timestamp"), timestamp},
        {std::string("datetime"), this->iso8601(timestamp)},
    };
    ccxt::any account = this->account();
    ccxt::any futuresBalance = this->safeString(data, std::string("balance"));
    if (isTrue(!isEqual(futuresBalance, ccxt::any{}))) {
      // futures wallet: balance is the free/available margin, locked_amount is
      // used, safeBalance derives total
      ::setValue(account, std::string("free"), futuresBalance);
      ::setValue(account, std::string("used"),
                 this->safeString(data, std::string("locked_amount")));
    } else {
      // spot wallet: total is the total, withdrawable is free, safeBalance
      // derives used
      ::setValue(account, std::string("total"),
                 this->safeString(data, std::string("total")));
      ::setValue(account, std::string("free"),
                 this->safeString(data, std::string("withdrawable")));
    }
    ::setValue(result, currency, account);
    return this->safeBalance(result);
  }

  /**
   * @method
   * @name mudrex#fetchLeverage
   * @description fetch the set leverage for a market
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} symbol unified market symbol
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [leverage
   * structure](https://docs.ccxt.com/#/?id=leverage-structure)
   */
  std::shared_future<ccxt::any>
  fetchLeverage(ccxt::any symbol, ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = this->market(symbol);
                 ccxt::any request = ccxt::dict{
                     {std::string("asset_id"),
                      ::getValue(market, std::string("id"))},
                     {std::string("is_symbol"), 1},
                 };
                 ccxt::any response =
                     awaitValue(this->privateGetFuturesAssetIdLeverage(
                         this->extend(request, params)));
                 ccxt::any data = this->safeDict(response, std::string("data"),
                                                 ccxt::dict{});
                 return ccxt::dict{
                     {std::string("info"), response},
                     {std::string("symbol"), symbol},
                     {std::string("marginMode"),
                      this->safeStringLower(data, std::string("margin_type"))},
                     {std::string("longLeverage"),
                      this->safeNumber(data, std::string("leverage"))},
                     {std::string("shortLeverage"),
                      this->safeNumber(data, std::string("leverage"))},
                 };
               })
        .share();
  }

  /**
   * @method
   * @name mudrex#setLeverage
   * @description set the level of leverage for a market
   * @see https://docs.trade.mudrex.com/docs
   * @param {float} leverage the rate of leverage
   * @param {string} symbol unified market symbol
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.marginType] 'ISOLATED' (default) or 'CROSSED'
   * @returns {object} response from the exchange
   */
  std::shared_future<ccxt::any>
  setLeverage(ccxt::any leverage, ccxt::any symbol = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(symbol, ccxt::any{}))) {
                   throw ArgumentsRequired(toString(
                       add(this->id,
                           std::string(" setLeverage() requires a symbol"))));
                 }
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = this->market(symbol);
                 ccxt::any marginType =
                     this->safeString(params, std::string("marginType"),
                                      std::string("ISOLATED"));
                 ccxt::any request = ccxt::dict{
                     {std::string("asset_id"),
                      ::getValue(market, std::string("id"))},
                     {std::string("is_symbol"), 1},
                     {std::string("margin_type"), marginType},
                     {std::string("leverage"), leverage},
                 };
                 params =
                     this->omit(params, ccxt::list{std::string("marginType")});
                 ccxt::any response =
                     awaitValue(this->privatePostFuturesAssetIdLeverage(
                         this->extend(request, params)));
                 return response;
               })
        .share();
  }

  /**
   * @method
   * @name mudrex#createOrder
   * @description create a trade order
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} symbol unified market symbol
   * @param {string} type 'market' or 'limit'
   * @param {string} side 'buy' or 'sell'
   * @param {float} amount how much you want to trade in units of the base
   * currency
   * @param {float} [price] the price to fulfill the order, in units of the
   * quote currency (also required for market orders on this exchange)
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {int} [params.leverage] leverage for the order, required if
   * setLeverage() was not called beforehand
   * @param {bool} [params.reduceOnly] true if the order is reduce only
   * @param {object} [params.takeProfit] *takeProfit object in params*
   * containing the trigger price of the take-profit order attached to this
   * order
   * @param {float} [params.takeProfit.triggerPrice] take profit trigger price
   * @param {object} [params.stopLoss] *stopLoss object in params* containing
   * the trigger price of the stop-loss order attached to this order
   * @param {float} [params.stopLoss.triggerPrice] stop loss trigger price
   * @param {float} [params.takeProfitPrice] the trigger price for a standalone
   * take-profit order on an existing position (requires params.positionId)
   * @param {float} [params.stopLossPrice] the trigger price for a standalone
   * stop-loss order on an existing position (requires params.positionId)
   * @param {string} [params.positionId] the id of the position the standalone
   * stopLossPrice/takeProfitPrice order is attached to
   * @param {string} [params.trade_currency] the settlement currency for the
   * order
   * @returns {object} an [order
   * structure](https://docs.ccxt.com/#/?id=order-structure)
   */
  std::shared_future<ccxt::any>
  createOrder(ccxt::any symbol, ccxt::any type, ccxt::any side,
              ccxt::any amount, ccxt::any price = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = this->market(symbol);
                 // standalone stop-loss / take-profit orders
                 // (stopLossPrice/takeProfitPrice) are attached to an existing
                 // position through the riskorder endpoint, so a positionId is
                 // required
                 ccxt::any stopLossPrice =
                     this->safeString(params, std::string("stopLossPrice"));
                 ccxt::any takeProfitPrice =
                     this->safeString(params, std::string("takeProfitPrice"));
                 if (isTrue(isTrue((!isEqual(stopLossPrice, ccxt::any{}))) ||
                            isTrue((!isEqual(takeProfitPrice, ccxt::any{}))))) {
                   ccxt::any positionId =
                       this->safeString2(params, std::string("positionId"),
                                         std::string("position_id"));
                   if (isTrue(isEqual(positionId, ccxt::any{}))) {
                     throw ArgumentsRequired(toString(
                         add(this->id,
                             std::string(" createOrder() requires a positionId "
                                         "parameter to place a stopLossPrice "
                                         "or takeProfitPrice order"))));
                   }
                   params = this->omit(
                       params, ccxt::list{std::string("stopLossPrice"),
                                          std::string("takeProfitPrice"),
                                          std::string("positionId"),
                                          std::string("position_id")});
                   ccxt::any riskRequest = ccxt::dict{
                       {std::string("position_id"), positionId},
                   };
                   if (isTrue(!isEqual(takeProfitPrice, ccxt::any{}))) {
                     ::setValue(riskRequest, std::string("is_takeprofit"),
                                true);
                     ::setValue(
                         riskRequest, std::string("takeprofit_price"),
                         this->priceToPrecision(symbol, takeProfitPrice));
                   }
                   if (isTrue(!isEqual(stopLossPrice, ccxt::any{}))) {
                     ::setValue(riskRequest, std::string("is_stoploss"), true);
                     ::setValue(riskRequest, std::string("stoploss_price"),
                                this->priceToPrecision(symbol, stopLossPrice));
                   }
                   ccxt::any riskResponse = awaitValue(
                       this->privatePostFuturesPositionsPositionIdRiskorder(
                           this->extend(riskRequest, params)));
                   ccxt::any riskData = this->safeDict(
                       riskResponse, std::string("data"), riskResponse);
                   return this->parseOrder(riskData, market);
                 }
                 ccxt::any lev =
                     this->safeInteger(params, std::string("leverage"), 1);
                 if (isTrue(isTrue((isEqual(type, std::string("market")))) &&
                            isTrue((isEqual(price, ccxt::any{}))))) {
                   throw ArgumentsRequired(toString(add(
                       this->id, std::string(" createOrder() requires a price "
                                             "argument for market orders"))));
                 }
                 ccxt::any request = ccxt::dict{
                     {std::string("asset_id"),
                      ::getValue(market, std::string("id"))},
                     {std::string("is_symbol"), 1},
                     {std::string("leverage"), this->numberToString(lev)},
                     {std::string("quantity"),
                      this->amountToPrecision(symbol, amount)},
                     {std::string("order_price"),
                      this->priceToPrecision(symbol, price)},
                     {std::string("order_type"),
                      (isTrue((isEqual(side, std::string("buy"))))
                           ? ccxt::any(std::string("LONG"))
                           : ccxt::any(std::string("SHORT")))},
                     {std::string("trigger_type"),
                      (isTrue((isEqual(type, std::string("market"))))
                           ? ccxt::any(std::string("MARKET"))
                           : ccxt::any(std::string("LIMIT")))},
                     {std::string("reduce_only"),
                      this->safeBool(params, std::string("reduceOnly"), false)},
                 };
                 // mudrex only supports take-profit / stop-loss orders attached
                 // to the position-opening order
                 ccxt::any takeProfit =
                     this->safeDict(params, std::string("takeProfit"));
                 ccxt::any stopLoss =
                     this->safeDict(params, std::string("stopLoss"));
                 if (isTrue(!isEqual(takeProfit, ccxt::any{}))) {
                   ::setValue(request, std::string("is_takeprofit"), true);
                   ::setValue(
                       request, std::string("takeprofit_price"),
                       this->priceToPrecision(
                           symbol, this->safeStringN(
                                       takeProfit,
                                       ccxt::list{std::string("triggerPrice"),
                                                  std::string("stopPrice"),
                                                  std::string("price")})));
                 }
                 if (isTrue(!isEqual(stopLoss, ccxt::any{}))) {
                   ::setValue(request, std::string("is_stoploss"), true);
                   ::setValue(
                       request, std::string("stoploss_price"),
                       this->priceToPrecision(
                           symbol,
                           this->safeStringN(
                               stopLoss, ccxt::list{std::string("triggerPrice"),
                                                    std::string("stopPrice"),
                                                    std::string("price")})));
                 }
                 params =
                     this->omit(params, ccxt::list{std::string("leverage"),
                                                   std::string("reduceOnly"),
                                                   std::string("takeProfit"),
                                                   std::string("stopLoss")});
                 ccxt::any response =
                     awaitValue(this->privatePostFuturesAssetIdOrder(
                         this->extend(request, params)));
                 ccxt::any data =
                     this->safeDict(response, std::string("data"), response);
                 // the create response omits the order/trigger type, so restore
                 // them from the request
                 ::setValue(data, std::string("order_type"),
                            ::getValue(request, std::string("order_type")));
                 ::setValue(data, std::string("trigger_type"),
                            ::getValue(request, std::string("trigger_type")));
                 return this->parseOrder(data, market);
               })
        .share();
  }

  /**
   * @method
   * @name mudrex#editOrder
   * @description edit a trade order
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} id order id
   * @param {string} symbol unified symbol of the market to edit an order in
   * @param {string} type 'market' or 'limit'
   * @param {string} side 'buy' or 'sell'
   * @param {float} [amount] how much of the currency you want to trade in units
   * of the base currency
   * @param {float} [price] the price at which the order is to be fulfilled, in
   * units of the quote currency
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} an [order
   * structure](https://docs.ccxt.com/#/?id=order-structure)
   */
  std::shared_future<ccxt::any>
  editOrder(ccxt::any id, ccxt::any symbol, ccxt::any type, ccxt::any side,
            ccxt::any amount = ccxt::any{}, ccxt::any price = ccxt::any{},
            ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                          awaitValue(this->loadMarkets());
                        }
                        ccxt::any market = ccxt::any{};
                        if (isTrue(!isEqual(symbol, ccxt::any{}))) {
                          market = this->market(symbol);
                        }
                        ccxt::any request = ccxt::dict{
                            {std::string("order_id"), id},
                        };
                        if (isTrue(!isEqual(amount, ccxt::any{}))) {
                          ::setValue(request, std::string("quantity"),
                                     this->amountToPrecision(symbol, amount));
                        }
                        if (isTrue(!isEqual(price, ccxt::any{}))) {
                          ::setValue(request, std::string("order_price"),
                                     this->priceToPrecision(symbol, price));
                        }
                        ccxt::any response =
                            awaitValue(this->privatePatchFuturesOrdersOrderId(
                                this->extend(request, params)));
                        ccxt::any data = this->safeDict(
                            response, std::string("data"), response);
                        return this->parseOrder(data, market);
                      })
        .share();
  }

  virtual ccxt::any parseOrderStatus(ccxt::any status) {
    ccxt::any statuses = ccxt::dict{
        {std::string("open"), std::string("open")},
        {std::string("created"), std::string("open")},
        {std::string("new"), std::string("open")},
        {std::string("pending"), std::string("open")},
        {std::string("partially_filled"), std::string("open")},
        {std::string("filled"), std::string("closed")},
        {std::string("completed"), std::string("closed")},
        {std::string("cancelled"), std::string("canceled")},
        {std::string("canceled"), std::string("canceled")},
        {std::string("rejected"), std::string("rejected")},
        {std::string("expired"), std::string("expired")},
    };
    return this->safeString(statuses, status, status);
  }

  ccxt::any parseOrder(ccxt::any order,
                       ccxt::any market = ccxt::any{}) override {
    ccxt::any oms = this->safeString(order, std::string("symbol"));
    market = this->safeMarket(oms, market);
    ccxt::any oid =
        this->safeString2(order, std::string("order_id"), std::string("id"));
    ccxt::any rawSide = this->safeStringUpper(order, std::string("order_type"));
    ccxt::any side = ccxt::any{};
    if (isTrue(isEqual(rawSide, std::string("LONG")))) {
      side = std::string("buy");
    } else if (isTrue(isEqual(rawSide, std::string("SHORT")))) {
      side = std::string("sell");
    }
    ccxt::any trig = this->safeStringUpper(order, std::string("trigger_type"));
    ccxt::any typ = ccxt::any{};
    if (isTrue(isEqual(trig, std::string("MARKET")))) {
      typ = std::string("market");
    } else if (isTrue(isEqual(trig, std::string("LIMIT")))) {
      typ = std::string("limit");
    }
    ccxt::any ts =
        this->parse8601(this->safeString(order, std::string("created_at")));
    if (isTrue(isEqual(ts, ccxt::any{}))) {
      ts = this->milliseconds();
    }
    ccxt::any status = this->parseOrderStatus(
        this->safeStringLower(order, std::string("status")));
    ccxt::any sym = ::getValue(market, std::string("symbol"));
    return this->safeOrder(
        ccxt::dict{
            {std::string("info"), order},
            {std::string("id"), oid},
            {std::string("clientOrderId"), ccxt::any{}},
            {std::string("timestamp"), ts},
            {std::string("datetime"), this->iso8601(ts)},
            {std::string("lastTradeTimestamp"), ccxt::any{}},
            {std::string("symbol"), sym},
            {std::string("type"), typ},
            {std::string("timeInForce"), ccxt::any{}},
            {std::string("postOnly"), ccxt::any{}},
            {std::string("side"), side},
            {std::string("price"),
             this->safeNumber2(order, std::string("price"),
                               std::string("order_price"))},
            {std::string("stopPrice"), ccxt::any{}},
            {std::string("triggerPrice"), ccxt::any{}},
            {std::string("amount"),
             this->safeNumber2(order, std::string("quantity"),
                               std::string("amount"))},
            {std::string("cost"), ccxt::any{}},
            {std::string("average"), ccxt::any{}},
            {std::string("filled"), ccxt::any{}},
            {std::string("remaining"), ccxt::any{}},
            {std::string("status"), status},
            {std::string("fee"), ccxt::any{}},
            {std::string("trades"), ccxt::list{}},
            {std::string("fees"), ccxt::list{}},
            {std::string("lastUpdateTimestamp"), ccxt::any{}},
            {std::string("reduceOnly"),
             this->safeBool(order, std::string("reduce_only"))},
        },
        market);
  }

  /**
   * @method
   * @name mudrex#cancelOrder
   * @description cancels an open order
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} id order id
   * @param {string} [symbol] unified symbol of the market the order was made in
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} An [order
   * structure](https://docs.ccxt.com/#/?id=order-structure)
   */
  std::shared_future<ccxt::any>
  cancelOrder(ccxt::any id, ccxt::any symbol = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                          awaitValue(this->loadMarkets());
                        }
                        ccxt::any market = ccxt::any{};
                        if (isTrue(!isEqual(symbol, ccxt::any{}))) {
                          market = this->market(symbol);
                        }
                        ccxt::any request = ccxt::dict{
                            {std::string("order_id"), id},
                        };
                        ccxt::any response =
                            awaitValue(this->privateDeleteFuturesOrdersOrderId(
                                this->extend(request, params)));
                        ccxt::any data = this->safeDict(
                            response, std::string("data"), response);
                        return this->parseOrder(data, market);
                      })
        .share();
  }

  /**
   * @method
   * @name mudrex#fetchOrder
   * @description fetches information on an order made by the user
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} id the order id
   * @param {string} [symbol] unified symbol of the market the order was made in
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} An [order
   * structure](https://docs.ccxt.com/#/?id=order-structure)
   */
  std::shared_future<ccxt::any>
  fetchOrder(ccxt::any id, ccxt::any symbol = ccxt::any{},
             ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                          awaitValue(this->loadMarkets());
                        }
                        ccxt::any market = ccxt::any{};
                        if (isTrue(!isEqual(symbol, ccxt::any{}))) {
                          market = this->market(symbol);
                        }
                        ccxt::any request = ccxt::dict{
                            {std::string("order_id"), id},
                        };
                        ccxt::any response =
                            awaitValue(this->privateGetFuturesOrdersOrderId(
                                this->extend(request, params)));
                        ccxt::any data = this->safeDict(
                            response, std::string("data"), response);
                        return this->parseOrder(data, market);
                      })
        .share();
  }

  /**
   * @method
   * @name mudrex#fetchOrdersByState
   * @ignore
   * @description fetches a list of orders filtered by their state
   * @param {string} state the state of the orders to fetch
   * @param {string} [symbol] unified market symbol
   * @param {int} [since] the earliest time in ms to fetch orders for
   * @param {int} [limit] the maximum number of order structures to retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {Order[]} a list of [order
   * structures](https://docs.ccxt.com/#/?id=order-structure)
   */
  virtual std::shared_future<ccxt::any>
  fetchOrdersByState(ccxt::any state, ccxt::any symbol = ccxt::any{},
                     ccxt::any since = ccxt::any{},
                     ccxt::any limit = ccxt::any{},
                     ccxt::any params = ccxt::dict{}) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any q = ccxt::dict{};
                 if (isTrue(!isEqual(limit, ccxt::any{}))) {
                   ::setValue(q, std::string("limit"), limit);
                 }
                 ccxt::any request = this->extend(q, params);
                 ccxt::any response = ccxt::any{};
                 if (isTrue(isEqual(state, std::string("closed")))) {
                   response = awaitValue(
                       this->privateGetFuturesOrdersHistory(request));
                 } else {
                   response =
                       awaitValue(this->privateGetFuturesOrders(request));
                 }
                 ccxt::any data = this->safeValue(response, std::string("data"),
                                                  ccxt::list{});
                 ccxt::any rows = this->toArray(data);
                 ccxt::any market = ccxt::any{};
                 if (isTrue(!isEqual(symbol, ccxt::any{}))) {
                   market = this->market(symbol);
                 }
                 ccxt::any orders = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(rows));
                      postFixIncrement(i)) {
                   arrayPush(orders,
                             this->parseOrder(::getValue(rows, i), market));
                 }
                 return this->filterBySymbolSinceLimit(orders, symbol, since,
                                                       limit);
               })
        .share();
  }

  /**
   * @method
   * @name mudrex#fetchOrders
   * @description fetches information on multiple orders made by the user
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} [symbol] unified market symbol of the market orders were
   * made in
   * @param {int} [since] the earliest time in ms to fetch orders for
   * @param {int} [limit] the maximum number of order structures to retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {Order[]} a list of [order
   * structures](https://docs.ccxt.com/#/?id=order-structure)
   */
  std::shared_future<ccxt::any>
  fetchOrders(ccxt::any symbol = ccxt::any{}, ccxt::any since = ccxt::any{},
              ccxt::any limit = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 return awaitValue(this->fetchOrdersByState(
                     std::string("closed"), symbol, since, limit, params));
               })
        .share();
  }

  /**
   * @method
   * @name mudrex#fetchOpenOrders
   * @description fetch all unfilled currently open orders
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} [symbol] unified market symbol
   * @param {int} [since] the earliest time in ms to fetch open orders for
   * @param {int} [limit] the maximum number of open order structures to
   * retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {Order[]} a list of [order
   * structures](https://docs.ccxt.com/#/?id=order-structure)
   */
  std::shared_future<ccxt::any>
  fetchOpenOrders(ccxt::any symbol = ccxt::any{}, ccxt::any since = ccxt::any{},
                  ccxt::any limit = ccxt::any{},
                  ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        return awaitValue(this->fetchOrdersByState(
                            std::string("open"), symbol, since, limit, params));
                      })
        .share();
  }

  /**
   * @method
   * @name mudrex#fetchClosedOrders
   * @description fetches information on multiple closed orders made by the user
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} [symbol] unified market symbol of the market orders were
   * made in
   * @param {int} [since] the earliest time in ms to fetch orders for
   * @param {int} [limit] the maximum number of order structures to retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {Order[]} a list of [order
   * structures](https://docs.ccxt.com/#/?id=order-structure)
   */
  std::shared_future<ccxt::any> fetchClosedOrders(
      ccxt::any symbol = ccxt::any{}, ccxt::any since = ccxt::any{},
      ccxt::any limit = ccxt::any{}, ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 return awaitValue(this->fetchOrdersByState(
                     std::string("closed"), symbol, since, limit, params));
               })
        .share();
  }

  /**
   * @method
   * @name mudrex#fetchPositions
   * @description fetch all open positions
   * @see https://docs.trade.mudrex.com/docs
   * @param {string[]} [symbols] list of unified market symbols
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.trade_currency] the settlement currency to query
   * positions for
   * @returns {object[]} a list of [position
   * structures](https://docs.ccxt.com/#/?id=position-structure)
   */
  std::shared_future<ccxt::any>
  fetchPositions(ccxt::any symbols = ccxt::any{},
                 ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any q = ccxt::dict{};
                 ccxt::any response = awaitValue(
                     this->privateGetFuturesPositions(this->extend(q, params)));
                 ccxt::any data = this->safeValue(response, std::string("data"),
                                                  ccxt::list{});
                 if (isTrue(isEqual(data, ccxt::any{}))) {
                   return ccxt::list{};
                 }
                 ccxt::any rows = this->toArray(data);
                 ccxt::any outPos = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(rows));
                      postFixIncrement(i)) {
                   ccxt::any p = ::getValue(rows, i);
                   ccxt::any symRaw =
                       this->safeString(p, std::string("symbol"));
                   ccxt::any m = this->safeMarket(symRaw);
                   ccxt::any pos = this->parsePosition(p, m);
                   arrayPush(outPos, pos);
                 }
                 return this->filterByArrayPositions(
                     outPos, std::string("symbol"), symbols, false);
               })
        .share();
  }

  /**
   * @method
   * @name mudrex#fetchPositionsHistory
   * @description fetches the history of closed positions
   * @see https://docs.trade.mudrex.com/docs/get-position-history
   * @param {string[]} [symbols] a list of unified market symbols
   * @param {int} [since] the earliest time in ms to fetch positions for
   * @param {int} [limit] the maximum number of position structures to retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.trade_currency] the settlement currency to filter
   * positions by
   * @returns {object[]} a list of [position
   * structures](https://docs.ccxt.com/#/?id=position-structure)
   */
  std::shared_future<ccxt::any> fetchPositionsHistory(
      ccxt::any symbols = ccxt::any{}, ccxt::any since = ccxt::any{},
      ccxt::any limit = ccxt::any{}, ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 symbols = this->marketSymbols(symbols);
                 ccxt::any request = ccxt::dict{};
                 if (isTrue(!isEqual(limit, ccxt::any{}))) {
                   ::setValue(request, std::string("limit"), limit);
                 }
                 ccxt::any response =
                     awaitValue(this->privateGetFuturesPositionsHistory(
                         this->extend(request, params)));
                 //
                 //     {
                 //         "success": true,
                 //         "data": [
                 //             {
                 //                 "id": "019f1ed6-...",
                 //                 "position_type": "SHORT",
                 //                 "status": "CLOSED",
                 //                 "leverage": "3",
                 //                 "entry_price": "1.3112",
                 //                 "closed_price": "1.3395",
                 //                 "quantity": "34",
                 //                 "pnl": "-0.9622",
                 //                 "created_at": "2026-07-01T10:18:57Z",
                 //                 "updated_at": "2026-07-01T18:00:21Z",
                 //                 "symbol": "CAKEUSDT",
                 //                 "trade_currency": "USDT"
                 //             }
                 //         ]
                 //     }
                 //
                 ccxt::any data = this->safeList(response, std::string("data"),
                                                 ccxt::list{});
                 ccxt::any positions = this->parsePositions(data, symbols);
                 return this->filterBySinceLimit(positions, since, limit);
               })
        .share();
  }

  ccxt::any parsePosition(ccxt::any position,
                          ccxt::any market = ccxt::any{}) override {
    market = this->safeMarket(ccxt::any{}, market);
    ccxt::any ms = this->safeString(position, std::string("symbol"));
    ccxt::any symbol = this->safeSymbol(ms, market);
    // open positions use "order_type", closed positions (history) use
    // "position_type"
    ccxt::any rawSide = this->safeStringUpper2(
        position, std::string("order_type"), std::string("position_type"));
    ccxt::any side = ccxt::any{};
    if (isTrue(isEqual(rawSide, std::string("LONG")))) {
      side = std::string("long");
    } else if (isTrue(isEqual(rawSide, std::string("SHORT")))) {
      side = std::string("short");
    }
    ccxt::any ts =
        this->parse8601(this->safeString(position, std::string("updated_at")));
    if (isTrue(isEqual(ts, ccxt::any{}))) {
      ts = this->parse8601(
          this->safeString(position, std::string("created_at")));
    }
    ccxt::any quantityString =
        this->safeString(position, std::string("quantity"));
    ccxt::any entryPriceString =
        this->safeString(position, std::string("entry_price"));
    ccxt::any contractSizeString =
        this->safeString(market, std::string("contractSize"), std::string("1"));
    ccxt::any notional = ccxt::any{};
    if (isTrue(isTrue((!isEqual(quantityString, ccxt::any{}))) &&
               isTrue((!isEqual(entryPriceString, ccxt::any{}))))) {
      notional = this->parseNumber(ccxt::Precise::stringMul(
          ccxt::Precise::stringMul(quantityString, entryPriceString),
          contractSizeString));
    }
    ccxt::any initialMargin =
        this->safeString(position, std::string("initial_margin"));
    return ccxt::dict{
        {std::string("info"), position},
        {std::string("id"), this->safeString(position, std::string("id"))},
        {std::string("symbol"), symbol},
        {std::string("timestamp"), ts},
        {std::string("datetime"), this->iso8601(ts)},
        {std::string("isolated"), true},
        {std::string("hedged"), false},
        {std::string("side"), side},
        {std::string("contracts"),
         this->safeNumber(position, std::string("quantity"))},
        {std::string("contractSize"),
         this->safeNumber(market, std::string("contractSize"))},
        {std::string("entryPrice"),
         this->safeNumber(position, std::string("entry_price"))},
        {std::string("markPrice"), ccxt::any{}},
        {std::string("lastPrice"),
         this->safeNumber(position, std::string("closed_price"))},
        {std::string("notional"), notional},
        {std::string("leverage"),
         this->safeInteger(position, std::string("leverage"))},
        {std::string("collateral"), this->parseNumber(initialMargin)},
        {std::string("initialMargin"), this->parseNumber(initialMargin)},
        {std::string("initialMarginPercentage"), ccxt::any{}},
        {std::string("maintenanceMargin"),
         this->safeNumber(position, std::string("maintenance_margin"))},
        {std::string("maintenanceMarginPercentage"), ccxt::any{}},
        {std::string("unrealizedPnl"), ccxt::any{}},
        {std::string("realizedPnl"),
         this->safeNumber(position, std::string("pnl"))},
        {std::string("liquidationPrice"),
         this->safeNumber(position, std::string("liquidation_price"))},
        {std::string("marginMode"), std::string("isolated")},
        {std::string("percentage"), ccxt::any{}},
    };
  }

  /**
   * @method
   * @name mudrex#closePosition
   * @description closes an open position for a market
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} symbol unified CCXT market symbol
   * @param {string} [side] 'buy' or 'sell', not required by mudrex
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.position_id] the id of the position to close,
   * resolved from the symbol if not provided
   * @param {float} [params.amount] the amount to close for a partial close,
   * closes the whole position if not provided
   * @returns {object} an [order
   * structure](https://docs.ccxt.com/#/?id=order-structure)
   */
  std::shared_future<ccxt::any>
  closePosition(ccxt::any symbol, ccxt::any side = ccxt::any{},
                ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any positionId =
                     this->safeString(params, std::string("position_id"));
                 ccxt::any amount =
                     this->safeValue(params, std::string("amount"));
                 if (isTrue(isEqual(positionId, ccxt::any{}))) {
                   ccxt::any market = this->market(symbol);
                   ccxt::any positions = awaitValue(
                       this->fetchPositions(ccxt::list{symbol}, params));
                   for (ccxt::any i = 0;
                        isLessThan(i, getArrayLength(positions));
                        postFixIncrement(i)) {
                     ccxt::any p = ::getValue(positions, i);
                     if (isTrue(
                             isTrue(!isEqual(side, ccxt::any{})) &&
                             isTrue(!isEqual(::getValue(p, std::string("side")),
                                             side)))) {
                       continue;
                     }
                     if (isTrue(isEqual(
                             ::getValue(p, std::string("symbol")),
                             ::getValue(market, std::string("symbol"))))) {
                       positionId = this->safeString(p, std::string("id"));
                       break;
                     }
                   }
                 }
                 if (isTrue(isEqual(positionId, ccxt::any{}))) {
                   throw OrderNotFound(toString(add(
                       this->id,
                       std::string(
                           " closePosition() could not resolve position_id"))));
                 }
                 ccxt::any request = ccxt::dict{
                     {std::string("position_id"), positionId},
                 };
                 if (isTrue(!isEqual(amount, ccxt::any{}))) {
                   ccxt::any orderType = this->safeStringUpper(
                       params, std::string("order_type"), std::string("LIMIT"));
                   ::setValue(request, std::string("order_type"), orderType);
                   ::setValue(request, std::string("quantity"),
                              this->amountToPrecision(symbol, amount));
                   ccxt::any lp =
                       this->safeString(params, std::string("limit_price"));
                   if (isTrue(
                           isTrue(isEqual(orderType, std::string("LIMIT"))) &&
                           isTrue(!isEqual(lp, ccxt::any{})))) {
                     ::setValue(request, std::string("limit_price"), lp);
                   }
                   params = this->omit(params,
                                       ccxt::list{std::string("order_type"),
                                                  std::string("limit_price"),
                                                  std::string("amount"),
                                                  std::string("position_id")});
                   ccxt::any partialResponse = awaitValue(
                       this->privatePostFuturesPositionsPositionIdClosePartial(
                           this->extend(request, params)));
                   return partialResponse;
                 }
                 params =
                     this->omit(params, ccxt::list{std::string("position_id")});
                 ccxt::any response = awaitValue(
                     this->privatePostFuturesPositionsPositionIdClose(
                         this->extend(request, params)));
                 return response;
               })
        .share();
  }

  /**
   * @method
   * @name mudrex#addMargin
   * @description add margin to a position
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} symbol unified market symbol
   * @param {float} amount amount of margin to add
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.position_id] the id of the position to add margin
   * to, resolved from the symbol if not provided
   * @returns {object} a [margin
   * structure](https://docs.ccxt.com/#/?id=add-margin-structure)
   */
  std::shared_future<ccxt::any>
  addMargin(ccxt::any symbol, ccxt::any amount,
            ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any positionId =
                     this->safeString(params, std::string("position_id"));
                 if (isTrue(isEqual(positionId, ccxt::any{}))) {
                   ccxt::any positions = awaitValue(
                       this->fetchPositions(ccxt::list{symbol}, params));
                   for (ccxt::any i = 0;
                        isLessThan(i, getArrayLength(positions));
                        postFixIncrement(i)) {
                     ccxt::any p = ::getValue(positions, i);
                     if (isTrue(isEqual(::getValue(p, std::string("symbol")),
                                        symbol))) {
                       positionId = this->safeString(p, std::string("id"));
                       break;
                     }
                   }
                 }
                 if (isTrue(isEqual(positionId, ccxt::any{}))) {
                   throw OrderNotFound(toString(
                       add(this->id,
                           std::string(
                               " addMargin() could not resolve position_id"))));
                 }
                 ccxt::any request = ccxt::dict{
                     {std::string("position_id"), positionId},
                     {std::string("margin"),
                      this->costToPrecision(symbol, amount)},
                 };
                 params =
                     this->omit(params, ccxt::list{std::string("position_id")});
                 ccxt::any response = awaitValue(
                     this->privatePostFuturesPositionsPositionIdAddMargin(
                         this->extend(request, params)));
                 return response;
               })
        .share();
  }

  /**
   * @method
   * @name mudrex#reduceMargin
   * @description remove margin from a position
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} symbol unified market symbol
   * @param {float} amount the amount of margin to remove
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [margin
   * structure](https://docs.ccxt.com/#/?id=reduce-margin-structure)
   */
  std::shared_future<ccxt::any>
  reduceMargin(ccxt::any symbol, ccxt::any amount,
               ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        return awaitValue(this->addMargin(
                            symbol, prefixUnaryNeg(amount), params));
                      })
        .share();
  }

  /**
   * @method
   * @name mudrex#fetchMyTrades
   * @description fetch all trades made by the user
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} [symbol] unified market symbol
   * @param {int} [since] the earliest time in ms to fetch trades for
   * @param {int} [limit] the maximum number of trade structures to retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.trade_currency] the settlement currency to filter
   * trades by
   * @returns {Trade[]} a list of [trade
   * structures](https://docs.ccxt.com/#/?id=trade-structure)
   */
  std::shared_future<ccxt::any>
  fetchMyTrades(ccxt::any symbol = ccxt::any{}, ccxt::any since = ccxt::any{},
                ccxt::any limit = ccxt::any{},
                ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                          awaitValue(this->loadMarkets());
                        }
                        ccxt::any market = ccxt::any{};
                        if (isTrue(!isEqual(symbol, ccxt::any{}))) {
                          market = this->market(symbol);
                        }
                        ccxt::any request = ccxt::dict{};
                        if (isTrue(!isEqual(limit, ccxt::any{}))) {
                          ::setValue(request, std::string("limit"), limit);
                        }
                        ccxt::any response =
                            awaitValue(this->privateGetFuturesFeeHistory(
                                this->extend(request, params)));
                        ccxt::any data = this->safeValue(
                            response, std::string("data"), ccxt::list{});
                        ccxt::any rows = this->toArray(data);
                        return this->parseTrades(rows, market, since, limit);
                      })
        .share();
  }

  ccxt::any parseTrade(ccxt::any trade,
                       ccxt::any market = ccxt::any{}) override {
    ccxt::any ms = this->safeString(trade, std::string("symbol"));
    market = this->safeMarket(ms, market);
    ccxt::any symbol = ::getValue(market, std::string("symbol"));
    ccxt::any ts =
        this->parse8601(this->safeString(trade, std::string("created_at")));
    if (isTrue(isEqual(ts, ccxt::any{}))) {
      ts = this->safeInteger(trade, std::string("time"));
    }
    ccxt::any side = this->safeStringLower2(trade, std::string("side"),
                                            std::string("order_type"));
    ccxt::any tradeSide = ccxt::any{};
    if (isTrue(isTrue(isEqual(side, std::string("buy"))) ||
               isTrue(isEqual(side, std::string("long"))))) {
      tradeSide = std::string("buy");
    } else if (isTrue(isTrue(isEqual(side, std::string("sell"))) ||
                      isTrue(isEqual(side, std::string("short"))))) {
      tradeSide = std::string("sell");
    }
    ccxt::any feeType = this->safeStringUpper(trade, std::string("fee_type"));
    ccxt::any takerOrMaker = ccxt::any{};
    if (isTrue(isEqual(feeType, std::string("TRANSACTION")))) {
      takerOrMaker = std::string("taker");
    } else if (isTrue(isEqual(feeType, std::string("REBATE")))) {
      takerOrMaker = std::string("maker");
    }
    ccxt::any fee = ccxt::any{};
    ccxt::any feeCost = this->safeNumber(trade, std::string("fee_amount"));
    if (isTrue(!isEqual(feeCost, ccxt::any{}))) {
      fee = ccxt::dict{
          {std::string("cost"), feeCost},
          {std::string("currency"),
           this->safeString(trade, std::string("trade_currency"))},
      };
    }
    return this->safeTrade(
        ccxt::dict{
            {std::string("info"), trade},
            {std::string("timestamp"), ts},
            {std::string("datetime"), this->iso8601(ts)},
            {std::string("symbol"), symbol},
            {std::string("id"), this->safeString2(trade, std::string("execId"),
                                                  std::string("id"))},
            {std::string("order"),
             this->safeString(trade, std::string("order_id"))},
            {std::string("type"),
             this->safeStringLower(trade, std::string("trigger_type"))},
            {std::string("side"), tradeSide},
            {std::string("takerOrMaker"), takerOrMaker},
            {std::string("price"),
             this->safeNumber(trade, std::string("price"))},
            {std::string("amount"),
             this->safeNumber2(trade, std::string("size"),
                               std::string("quantity"))},
            {std::string("cost"),
             this->safeNumber(trade, std::string("transaction_amount"))},
            {std::string("fee"), fee},
        },
        market);
  }

  /**
   * @method
   * @name mudrex#transfer
   * @description transfer currency internally between wallets on the same
   * account
   * @see https://docs.trade.mudrex.com/docs
   * @param {string} code unified currency code
   * @param {float} amount amount to transfer
   * @param {string} fromAccount 'spot' or 'futures'
   * @param {string} toAccount 'spot' or 'futures'
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [transfer
   * structure](https://docs.ccxt.com/#/?id=transfer-structure)
   */
  std::shared_future<ccxt::any>
  transfer(ccxt::any code, ccxt::any amount, ccxt::any fromAccount,
           ccxt::any toAccount, ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 ccxt::any mp = ccxt::dict{
                     {std::string("spot"), std::string("SPOT")},
                     {std::string("SPOT"), std::string("SPOT")},
                     {std::string("futures"), std::string("FUTURES")},
                     {std::string("future"), std::string("FUTURES")},
                     {std::string("FUTURES"), std::string("FUTURES")},
                 };
                 ccxt::any fw = this->safeString(mp, fromAccount,
                                                 toUpperCase(fromAccount));
                 ccxt::any tw =
                     this->safeString(mp, toAccount, toUpperCase(toAccount));
                 ccxt::any body = ccxt::dict{
                     {std::string("from_wallet_type"), fw},
                     {std::string("to_wallet_type"), tw},
                     {std::string("amount"), this->numberToString(amount)},
                 };
                 ccxt::any useInr = false;
                 if (isTrue(isEqual(code, std::string("INR")))) {
                   useInr = true;
                 } else {
                   // default USDT does not use the inr path
                   ccxt::any tradeCurrency =
                       this->safeString2(params, std::string("trade_currency"),
                                         std::string("tradeCurrency"));
                   if (isTrue(isEqual(tradeCurrency, std::string("INR")))) {
                     useInr = true;
                   }
                 }
                 ccxt::any response = ccxt::any{};
                 if (isTrue(useInr)) {
                   response = awaitValue(this->privatePostFuturesTransfersInr(
                       this->extend(body, params)));
                 } else {
                   response = awaitValue(this->privatePostWalletFuturesTransfer(
                       this->extend(body, params)));
                 }
                 ccxt::any data =
                     this->safeDict(response, std::string("data"), response);
                 return ccxt::dict{
                     {std::string("info"), response},
                     {std::string("id"),
                      this->safeString(data, std::string("id"))},
                     {std::string("timestamp"), ccxt::any{}},
                     {std::string("datetime"), ccxt::any{}},
                     {std::string("currency"), code},
                     {std::string("amount"), amount},
                     {std::string("fromAccount"), fw},
                     {std::string("toAccount"), tw},
                     {std::string("status"), std::string("ok")},
                 };
               })
        .share();
  }
  // GENERATED dispatch table - see createDispatchTable in
  // build/cppTranspiler.ts
  virtual ccxt::any callMethod(ccxt::any name, ccxt::any args) override {
    const std::string which =
        ::toString(name).has_value()
            ? ccxt::any_cast<std::string>(::toString(name))
            : std::string();
    const long count =
        ccxt::isList(args)
            ? static_cast<long>(ccxt::any_cast<ccxt::list>(args).size())
            : 0;
    if (which == "describe") {
      if (true)
        return this->describe();
    }
    if (which == "sign") {
      if (count <= 1)
        return this->sign(::getValue(args, 0));
      if (count == 2)
        return this->sign(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2));
      if (count == 4)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3));
      if (count == 5)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3),
                          ::getValue(args, 4));
      if (count >= 6)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3),
                          ::getValue(args, 4), ::getValue(args, 5));
    }
    if (which == "handleErrors") {
      if (true)
        return this->handleErrors(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7), ::getValue(args, 8));
    }
    if (which == "parseOHLCV") {
      if (count <= 1)
        return this->parseOHLCV(::getValue(args, 0));
      if (count >= 2)
        return this->parseOHLCV(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "fetchOHLCV") {
      if (count <= 1)
        return awaitValue(this->fetchOHLCV(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOHLCV(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(
            this->fetchOHLCV(::getValue(args, 0), ::getValue(args, 1),
                             ::getValue(args, 2), ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->fetchOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "fetchMarkOHLCV") {
      if (count <= 1)
        return awaitValue(this->fetchMarkOHLCV(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchMarkOHLCV(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchMarkOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(
            this->fetchMarkOHLCV(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->fetchMarkOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "fetchTicker") {
      if (count <= 1)
        return awaitValue(this->fetchTicker(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->fetchTicker(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "fetchTickers") {
      if (count <= 0)
        return awaitValue(this->fetchTickers());
      if (count == 1)
        return awaitValue(this->fetchTickers(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->fetchTickers(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "parseTicker") {
      if (count <= 1)
        return this->parseTicker(::getValue(args, 0));
      if (count >= 2)
        return this->parseTicker(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "fetchMarkets") {
      if (count <= 0)
        return awaitValue(this->fetchMarkets());
      if (count >= 1)
        return awaitValue(this->fetchMarkets(::getValue(args, 0)));
    }
    if (which == "parseMarket") {
      if (true)
        return this->parseMarket(::getValue(args, 0));
    }
    if (which == "fetchBalance") {
      if (count <= 0)
        return awaitValue(this->fetchBalance());
      if (count >= 1)
        return awaitValue(this->fetchBalance(::getValue(args, 0)));
    }
    if (which == "parseBalance") {
      if (true)
        return this->parseBalance(::getValue(args, 0));
    }
    if (which == "fetchLeverage") {
      if (count <= 1)
        return awaitValue(this->fetchLeverage(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->fetchLeverage(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "setLeverage") {
      if (count <= 1)
        return awaitValue(this->setLeverage(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->setLeverage(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->setLeverage(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "createOrder") {
      if (count <= 4)
        return awaitValue(
            this->createOrder(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2), ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->createOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count >= 6)
        return awaitValue(this->createOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
    }
    if (which == "editOrder") {
      if (count <= 4)
        return awaitValue(
            this->editOrder(::getValue(args, 0), ::getValue(args, 1),
                            ::getValue(args, 2), ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->editOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->editOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count >= 7)
        return awaitValue(this->editOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
    }
    if (which == "parseOrderStatus") {
      if (true)
        return this->parseOrderStatus(::getValue(args, 0));
    }
    if (which == "parseOrder") {
      if (count <= 1)
        return this->parseOrder(::getValue(args, 0));
      if (count >= 2)
        return this->parseOrder(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "cancelOrder") {
      if (count <= 1)
        return awaitValue(this->cancelOrder(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->cancelOrder(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->cancelOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "fetchOrder") {
      if (count <= 1)
        return awaitValue(this->fetchOrder(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOrder(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->fetchOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "fetchOrdersByState") {
      if (count <= 1)
        return awaitValue(this->fetchOrdersByState(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOrdersByState(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchOrdersByState(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(
            this->fetchOrdersByState(::getValue(args, 0), ::getValue(args, 1),
                                     ::getValue(args, 2), ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->fetchOrdersByState(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "fetchOrders") {
      if (count <= 0)
        return awaitValue(this->fetchOrders());
      if (count == 1)
        return awaitValue(this->fetchOrders(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOrders(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchOrders(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "fetchOpenOrders") {
      if (count <= 0)
        return awaitValue(this->fetchOpenOrders());
      if (count == 1)
        return awaitValue(this->fetchOpenOrders(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOpenOrders(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchOpenOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchOpenOrders(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "fetchClosedOrders") {
      if (count <= 0)
        return awaitValue(this->fetchClosedOrders());
      if (count == 1)
        return awaitValue(this->fetchClosedOrders(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchClosedOrders(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchClosedOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchClosedOrders(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "fetchPositions") {
      if (count <= 0)
        return awaitValue(this->fetchPositions());
      if (count == 1)
        return awaitValue(this->fetchPositions(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->fetchPositions(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "fetchPositionsHistory") {
      if (count <= 0)
        return awaitValue(this->fetchPositionsHistory());
      if (count == 1)
        return awaitValue(this->fetchPositionsHistory(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchPositionsHistory(::getValue(args, 0),
                                                      ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchPositionsHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->fetchPositionsHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "parsePosition") {
      if (count <= 1)
        return this->parsePosition(::getValue(args, 0));
      if (count >= 2)
        return this->parsePosition(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "closePosition") {
      if (count <= 1)
        return awaitValue(this->closePosition(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->closePosition(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->closePosition(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "addMargin") {
      if (count <= 2)
        return awaitValue(
            this->addMargin(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->addMargin(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "reduceMargin") {
      if (count <= 2)
        return awaitValue(
            this->reduceMargin(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->reduceMargin(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "fetchMyTrades") {
      if (count <= 0)
        return awaitValue(this->fetchMyTrades());
      if (count == 1)
        return awaitValue(this->fetchMyTrades(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchMyTrades(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchMyTrades(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchMyTrades(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "parseTrade") {
      if (count <= 1)
        return this->parseTrade(::getValue(args, 0));
      if (count >= 2)
        return this->parseTrade(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "transfer") {
      if (count <= 4)
        return awaitValue(
            this->transfer(::getValue(args, 0), ::getValue(args, 1),
                           ::getValue(args, 2), ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->transfer(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "cleanRestData") {
      if (true) {
        this->cleanRestData();
        return ccxt::any{};
      }
    }
    if (which == "cleanWsData") {
      if (true) {
        this->cleanWsData();
        return ccxt::any{};
      }
    }
    if (which == "storeByKey") {
      if (true) {
        this->storeByKey(::getValue(args, 0), ::getValue(args, 1),
                         ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "handleDeltas") {
      if (true) {
        this->handleDeltas(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "handleDelta") {
      if (true) {
        this->handleDelta(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "handleDeltasWithKeys") {
      if (count <= 2) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
      if (count == 3) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
        return ccxt::any{};
      }
      if (count == 4) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
        return ccxt::any{};
      }
      if (count >= 5) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3),
                                   ::getValue(args, 4));
        return ccxt::any{};
      }
    }
    if (which == "checkConflictingProxies") {
      if (true) {
        this->checkConflictingProxies(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "setSandboxMode") {
      if (true) {
        this->setSandboxMode(::getValue(args, 0));
        return ccxt::any{};
      }
    }
    if (which == "enableDemoTrading") {
      if (true) {
        this->enableDemoTrading(::getValue(args, 0));
        return ccxt::any{};
      }
    }
    if (which == "afterConstruct") {
      if (true) {
        this->afterConstruct();
        return ccxt::any{};
      }
    }
    if (which == "initRestRateLimiter") {
      if (true) {
        this->initRestRateLimiter();
        return ccxt::any{};
      }
    }
    if (which == "featuresGenerator") {
      if (true) {
        this->featuresGenerator();
        return ccxt::any{};
      }
    }
    if (which == "createNetworksByIdObject") {
      if (true) {
        this->createNetworksByIdObject();
        return ccxt::any{};
      }
    }
    if (which == "throwExactlyMatchedException") {
      if (true) {
        this->throwExactlyMatchedException(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "throwBroadlyMatchedException") {
      if (true) {
        this->throwBroadlyMatchedException(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "checkRequiredArgument") {
      if (count <= 3) {
        this->checkRequiredArgument(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
        return ccxt::any{};
      }
      if (count >= 4) {
        this->checkRequiredArgument(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3));
        return ccxt::any{};
      }
    }
    if (which == "checkRequiredMarginArgument") {
      if (true) {
        this->checkRequiredMarginArgument(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "cleanUnsubscription") {
      if (count <= 3) {
        this->cleanUnsubscription(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2));
        return ccxt::any{};
      }
      if (count >= 4) {
        this->cleanUnsubscription(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3));
        return ccxt::any{};
      }
    }
    if (which == "cleanCache") {
      if (true) {
        this->cleanCache(::getValue(args, 0));
        return ccxt::any{};
      }
    }
    // not defined on this exchange: fall back to the TS parent class
    return Exchange::callMethod(name, args);
  }
};

} // namespace ccxt
