#pragma once

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#include "../api/kucoinfutures.h"
#include "../base/Exchange.h"

namespace ccxt {

class kucoinfutures : public kucoinfuturesApi {
public:
  using kucoinfuturesApi::kucoinfuturesApi;
  ccxt::any describe() override {
    return this->deepExtend(
        kucoin::describe(),
        ccxt::dict{
            {std::string("id"), std::string("kucoinfutures")},
            {std::string("name"), std::string("KuCoin Futures")},
            {std::string("urls"),
             ccxt::dict{
                 {std::string("logo"),
                  std::string(
                      "https://user-images.githubusercontent.com/1294454/"
                      "147508995-9e35030a-d046-43a1-a006-6fabd981b554.jpg")},
                 {std::string("www"),
                  std::string("https://futures.kucoin.com/")},
                 {std::string("referral"),
                  std::string("https://futures.kucoin.com/?rcode=E5wkqe")},
             }},
            {std::string("has"),
             ccxt::dict{
                 {std::string("CORS"), ccxt::any{}},
                 {std::string("spot"), false},
                 {std::string("margin"), false},
                 {std::string("swap"), true},
                 {std::string("future"), true},
                 {std::string("option"), ccxt::any{}},
                 {std::string("fetchBidsAsks"), true},
                 {std::string("transfer"), true},
             }},
            {std::string("options"),
             ccxt::dict{
                 {std::string("fetchMarkets"),
                  ccxt::dict{
                      {std::string("types"),
                       ccxt::list{std::string("swap"), std::string("future"),
                                  std::string("contract")}},
                      {std::string("fetchTickersFees"), false},
                  }},
                 {std::string("defaultType"), std::string("swap")},
                 {std::string("defaultAccountType"), std::string("contract")},
             }},
        });
  }

  /**
   * @method
   * @name kucoinfutures#fetchBidsAsks
   * @description fetches the bid and ask price and volume for multiple markets
   * @param {string[]} [symbols] unified symbols of the markets to fetch the
   * bids and asks for, all markets are returned if not assigned
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a dictionary of [ticker structures]{@link
   * https://docs.ccxt.com/?id=ticker-structure}
   */
  std::shared_future<ccxt::any>
  fetchBidsAsks(ccxt::any symbols = ccxt::any{},
                ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        ccxt::any request = ccxt::dict{
                            {std::string("method"),
                             std::string("futuresPublicGetAllTickers")},
                        };
                        ccxt::any extendedRequest =
                            this->extend(request, params);
                        return awaitValue(
                            this->fetchTickers(symbols, extendedRequest));
                      })
        .share();
  }

  /**
   * @method
   * @name kucoinfutures#transfer
   * @description transfer currency internally between wallets on the same
   * account
   * @param {string} code unified currency code
   * @param {float} amount amount to transfer
   * @param {string} fromAccount account to transfer from
   * @param {string} toAccount account to transfer to
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [transfer structure]{@link
   * https://docs.ccxt.com/?id=transfer-structure}
   */
  std::shared_future<ccxt::any>
  transfer(ccxt::any code, ccxt::any amount, ccxt::any fromAccount,
           ccxt::any toAccount, ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any currency = this->currency(code);
                 ccxt::any amountToPrecision =
                     this->currencyToPrecision(code, amount);
                 ccxt::any request = ccxt::dict{
                     {std::string("currency"),
                      this->safeString(currency, std::string("id"))},
                     {std::string("amount"), amountToPrecision},
                 };
                 ccxt::any toAccountString = this->parseTransferType(toAccount);
                 ccxt::any response = ccxt::any{};
                 if (isTrue(isTrue(isEqual(toAccountString,
                                           std::string("TRADE"))) ||
                            isTrue(isEqual(toAccountString,
                                           std::string("MAIN"))))) {
                   ::setValue(request, std::string("recAccountType"),
                              toAccountString);
                   response = awaitValue(this->futuresPrivatePostTransferOut(
                       this->extend(request, params)));
                 } else if (isTrue(
                                isTrue(isTrue(isEqual(toAccount,
                                                      std::string("future"))) ||
                                       isTrue(isEqual(toAccount,
                                                      std::string("swap")))) ||
                                isTrue(isEqual(toAccount,
                                               std::string("contract"))))) {
                   ::setValue(request, std::string("payAccountType"),
                              this->parseTransferType(fromAccount));
                   response = awaitValue(this->futuresPrivatePostTransferIn(
                       this->extend(request, params)));
                 } else {
                   throw BadRequest(toString(
                       add(this->id,
                           std::string(
                               " transfer() only supports transfers between "
                               "future/swap, spot and funding accounts"))));
                 }
                 ccxt::any data = this->safeDict(response, std::string("data"),
                                                 ccxt::dict{});
                 return this->extend(
                     this->parseTransfer(data, currency),
                     ccxt::dict{
                         {std::string("amount"),
                          this->parseNumber(amountToPrecision)},
                         {std::string("fromAccount"), fromAccount},
                         {std::string("toAccount"), toAccount},
                     });
               })
        .share();
  }

  virtual ccxt::any parseTransferType(ccxt::any transferType) {
    ccxt::any transferTypes = ccxt::dict{
        {std::string("spot"), std::string("TRADE")},
        {std::string("funding"), std::string("MAIN")},
    };
    return this->safeStringUpper(transferTypes, transferType, transferType);
  }
  // GENERATED dispatch table - see createDispatchTable in
  // build/cppTranspiler.ts
  virtual ccxt::any callMethod(ccxt::any name, ccxt::any args) override {
    const std::string which =
        ::toString(name).has_value()
            ? ccxt::any_cast<std::string>(::toString(name))
            : std::string();
    const long count =
        ccxt::isList(args)
            ? static_cast<long>(ccxt::any_cast<ccxt::list>(args).size())
            : 0;
    if (which == "describe") {
      if (true)
        return this->describe();
    }
    if (which == "fetchBidsAsks") {
      if (count <= 0)
        return awaitValue(this->fetchBidsAsks());
      if (count == 1)
        return awaitValue(this->fetchBidsAsks(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->fetchBidsAsks(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "transfer") {
      if (count <= 4)
        return awaitValue(
            this->transfer(::getValue(args, 0), ::getValue(args, 1),
                           ::getValue(args, 2), ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->transfer(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "parseTransferType") {
      if (true)
        return this->parseTransferType(::getValue(args, 0));
    }
    if (which == "cleanRestData") {
      if (true) {
        this->cleanRestData();
        return ccxt::any{};
      }
    }
    if (which == "cleanWsData") {
      if (true) {
        this->cleanWsData();
        return ccxt::any{};
      }
    }
    if (which == "storeByKey") {
      if (true) {
        this->storeByKey(::getValue(args, 0), ::getValue(args, 1),
                         ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "handleDeltas") {
      if (true) {
        this->handleDeltas(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "handleDelta") {
      if (true) {
        this->handleDelta(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "handleDeltasWithKeys") {
      if (count <= 2) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
      if (count == 3) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
        return ccxt::any{};
      }
      if (count == 4) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
        return ccxt::any{};
      }
      if (count >= 5) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3),
                                   ::getValue(args, 4));
        return ccxt::any{};
      }
    }
    if (which == "checkConflictingProxies") {
      if (true) {
        this->checkConflictingProxies(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "setSandboxMode") {
      if (true) {
        this->setSandboxMode(::getValue(args, 0));
        return ccxt::any{};
      }
    }
    if (which == "enableDemoTrading") {
      if (true) {
        this->enableDemoTrading(::getValue(args, 0));
        return ccxt::any{};
      }
    }
    if (which == "afterConstruct") {
      if (true) {
        this->afterConstruct();
        return ccxt::any{};
      }
    }
    if (which == "initRestRateLimiter") {
      if (true) {
        this->initRestRateLimiter();
        return ccxt::any{};
      }
    }
    if (which == "featuresGenerator") {
      if (true) {
        this->featuresGenerator();
        return ccxt::any{};
      }
    }
    if (which == "createNetworksByIdObject") {
      if (true) {
        this->createNetworksByIdObject();
        return ccxt::any{};
      }
    }
    if (which == "throwExactlyMatchedException") {
      if (true) {
        this->throwExactlyMatchedException(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "throwBroadlyMatchedException") {
      if (true) {
        this->throwBroadlyMatchedException(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "checkRequiredArgument") {
      if (count <= 3) {
        this->checkRequiredArgument(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
        return ccxt::any{};
      }
      if (count >= 4) {
        this->checkRequiredArgument(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3));
        return ccxt::any{};
      }
    }
    if (which == "checkRequiredMarginArgument") {
      if (true) {
        this->checkRequiredMarginArgument(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "cleanUnsubscription") {
      if (count <= 3) {
        this->cleanUnsubscription(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2));
        return ccxt::any{};
      }
      if (count >= 4) {
        this->cleanUnsubscription(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3));
        return ccxt::any{};
      }
    }
    if (which == "cleanCache") {
      if (true) {
        this->cleanCache(::getValue(args, 0));
        return ccxt::any{};
      }
    }
    // not defined on this exchange: fall back to the TS parent class
    return kucoin::callMethod(name, args);
  }
};

} // namespace ccxt
