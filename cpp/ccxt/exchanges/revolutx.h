#pragma once

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#include "../api/revolutx.h"
#include "../base/Exchange.h"

namespace ccxt {

class revolutx : public revolutxApi {
public:
  using revolutxApi::revolutxApi;
  ccxt::any describe() override {
    return this->deepExtend(
        Exchange::describe(),
        ccxt::dict{
            {std::string("id"), std::string("revolutx")},
            {std::string("name"), std::string("Revolut X")},
            {std::string("countries"), ccxt::list{std::string("GB")}},
            {std::string("rateLimit"), 1000},
            {std::string("certified"), false},
            {std::string("pro"), false},
            {std::string("has"),
             ccxt::dict{
                 {std::string("CORS"), ccxt::any{}},
                 {std::string("spot"), true},
                 {std::string("margin"), false},
                 {std::string("swap"), false},
                 {std::string("future"), false},
                 {std::string("option"), false},
                 {std::string("cancelAllOrders"), true},
                 {std::string("cancelOrder"), true},
                 {std::string("createOrder"), true},
                 {std::string("editOrder"), true},
                 {std::string("fetchBalance"), true},
                 {std::string("fetchClosedOrders"), true},
                 {std::string("fetchCurrencies"), true},
                 {std::string("fetchMarkets"), true},
                 {std::string("fetchMyTrades"), true},
                 {std::string("fetchOHLCV"), true},
                 {std::string("fetchOpenOrders"), true},
                 {std::string("fetchOrder"), true},
                 {std::string("fetchOrderBook"), true},
                 {std::string("fetchOrders"), true},
                 {std::string("fetchTicker"), true},
                 {std::string("fetchTickers"), true},
                 {std::string("fetchTrades"), true},
             }},
            {std::string("timeframes"),
             ccxt::dict{
                 {std::string("1m"), 1},
                 {std::string("5m"), 5},
                 {std::string("15m"), 15},
                 {std::string("30m"), 30},
                 {std::string("1h"), 60},
                 {std::string("4h"), 240},
                 {std::string("1d"), 1440},
                 {std::string("2d"), 2880},
                 {std::string("4d"), 5760},
                 {std::string("1w"), 10080},
                 {std::string("2w"), 20160},
                 {std::string("4w"), 40320},
             }},
            {std::string("urls"),
             ccxt::dict{
                 {std::string("logo"),
                  std::string("https://github.com/user-attachments/assets/"
                              "594ab1a5-3985-4f79-89f4-c40ef7c33867")},
                 {std::string("api"),
                  ccxt::dict{
                      {std::string("public"),
                       std::string("https://revx.revolut.com/api")},
                      {std::string("private"),
                       std::string("https://revx.revolut.com/api")},
                  }},
                 {std::string("www"),
                  std::string("https://exchange.revolut.com")},
                 {std::string("doc"),
                  std::string("https://developer.revolut.com/docs/api/"
                              "revolut-x-crypto-exchange")},
                 {std::string("fees"),
                  std::string("https://exchange.revolut.com/fees")},
             }},
            {std::string("api"),
             ccxt::dict{
                 {std::string("public"),
                  ccxt::dict{
                      {std::string("get"),
                       ccxt::dict{
                           {std::string("2.0/public/order-book/{symbol}"), 1},
                           {std::string("1.0/public/tickers"), 1},
                           {std::string("1.0/public/candles/{symbol}"), 1},
                           {std::string("1.0/public/trades/all"), 1},
                           {std::string("1.0/public/configuration/currencies"),
                            1},
                           {std::string("1.0/public/configuration/pairs"), 1},
                       }},
                  }},
                 {std::string("private"),
                  ccxt::dict{
                      {std::string("get"),
                       ccxt::dict{
                           {std::string("1.0/balances"), 1},
                           {std::string("1.0/orders/active"), 1},
                           {std::string("1.0/orders/historical"), 1},
                           {std::string("1.0/orders/{venue_order_id}"), 1},
                           {std::string("1.0/orders/fills/{venue_order_id}"),
                            1},
                           {std::string("1.0/trades/private/{symbol}"), 1},
                       }},
                      {std::string("post"),
                       ccxt::dict{
                           {std::string("1.0/orders"), 1},
                       }},
                      {std::string("put"),
                       ccxt::dict{
                           {std::string("1.0/orders/{venue_order_id}"), 1},
                       }},
                      {std::string("delete"),
                       ccxt::dict{
                           {std::string("1.0/orders"), 1},
                           {std::string("1.0/orders/{venue_order_id}"), 1},
                       }},
                  }},
             }},
            {std::string("requiredCredentials"),
             ccxt::dict{
                 {std::string("apiKey"), true},
                 {std::string("secret"), false},
                 {std::string("privateKey"), true},
             }},
            {std::string("fees"),
             ccxt::dict{
                 {std::string("trading"),
                  ccxt::dict{
                      {std::string("tierBased"), false},
                      {std::string("percentage"), true},
                      {std::string("taker"),
                       this->parseNumber(std::string("0.0009"))},
                      {std::string("maker"),
                       this->parseNumber(std::string("0"))},
                  }},
             }},
            {std::string("options"),
             ccxt::dict{
                 {std::string("region"), ccxt::any{}},
             }},
            {std::string("precisionMode"), TICK_SIZE},
            {std::string("features"),
             ccxt::dict{
                 {std::string("spot"),
                  ccxt::dict{
                      {std::string("sandbox"), false},
                      {std::string("createOrder"),
                       ccxt::dict{
                           {std::string("marginMode"), false},
                           {std::string("triggerPrice"), false},
                           {std::string("triggerPriceType"), ccxt::any{}},
                           {std::string("triggerDirection"), false},
                           {std::string("stopLossPrice"), false},
                           {std::string("takeProfitPrice"), false},
                           {std::string("attachedStopLossTakeProfit"),
                            ccxt::any{}},
                           {std::string("timeInForce"),
                            ccxt::dict{
                                {std::string("GTC"), true},
                                {std::string("IOC"), true},
                                {std::string("FOK"), false},
                                {std::string("PO"), false},
                                {std::string("GTD"), false},
                            }},
                           {std::string("hedged"), false},
                           {std::string("trailing"), false},
                           {std::string("leverage"), false},
                           {std::string("marketBuyByCost"), true},
                           {std::string("marketBuyRequiresPrice"), false},
                           {std::string("selfTradePrevention"), false},
                           {std::string("iceberg"), false},
                       }},
                      {std::string("createOrders"), ccxt::any{}},
                      {std::string("fetchMyTrades"),
                       ccxt::dict{
                           {std::string("marginMode"), false},
                           {std::string("limit"), 1900},
                           {std::string("daysBack"), 30},
                           {std::string("untilDays"), ccxt::any{}},
                           {std::string("symbolRequired"), true},
                       }},
                      {std::string("fetchOrder"),
                       ccxt::dict{
                           {std::string("marginMode"), false},
                           {std::string("trigger"), false},
                           {std::string("trailing"), false},
                           {std::string("symbolRequired"), false},
                       }},
                      {std::string("fetchOpenOrders"),
                       ccxt::dict{
                           {std::string("marginMode"), false},
                           {std::string("limit"), 300},
                           {std::string("trigger"), false},
                           {std::string("trailing"), false},
                           {std::string("symbolRequired"), false},
                       }},
                      {std::string("fetchOrders"),
                       ccxt::dict{
                           {std::string("marginMode"), false},
                           {std::string("limit"), 1900},
                           {std::string("daysBack"), 30},
                           {std::string("untilDays"), ccxt::any{}},
                           {std::string("trigger"), false},
                           {std::string("trailing"), false},
                           {std::string("symbolRequired"), false},
                       }},
                      {std::string("fetchClosedOrders"),
                       ccxt::dict{
                           {std::string("marginMode"), false},
                           {std::string("limit"), 1900},
                           {std::string("daysBack"), 30},
                           {std::string("daysBackCanceled"), ccxt::any{}},
                           {std::string("untilDays"), ccxt::any{}},
                           {std::string("trigger"), false},
                           {std::string("trailing"), false},
                           {std::string("symbolRequired"), false},
                       }},
                      {std::string("fetchOHLCV"),
                       ccxt::dict{
                           {std::string("limit"), 50000},
                       }},
                  }},
             }},
            {std::string("httpExceptions"),
             ccxt::dict{
                 {std::string("400"), std::string("BadRequest")},
                 {std::string("403"), std::string("PermissionDenied")},
                 {std::string("404"), std::string("OrderNotFound")},
                 {std::string("409"), std::string("ExchangeError")},
                 {std::string("429"), std::string("RateLimitExceeded")},
             }},
            {std::string("exceptions"),
             ccxt::dict{
                 {std::string("exact"), ccxt::dict{}},
                 {std::string("broad"),
                  ccxt::dict{
                      {std::string("insufficient"),
                       std::string("InsufficientFunds")},
                      {std::string("invalid order"),
                       std::string("InvalidOrder")},
                      {std::string("not found"), std::string("OrderNotFound")},
                      {std::string("timestamp is in the future"),
                       std::string("InvalidNonce")},
                  }},
             }},
        });
  }

  ccxt::any sign(ccxt::any path, ccxt::any api = std::string("public"),
                 ccxt::any method = std::string("GET"),
                 ccxt::any params = ccxt::dict{},
                 ccxt::any headers = ccxt::any{},
                 ccxt::any body = ccxt::any{}) override {
    ccxt::any implodedPath = this->implodeParams(path, params);
    ccxt::any query = this->omit(params, this->extractParams(path));
    ccxt::any queryKeys = getObjectKeys(query);
    ccxt::any queryLength = getArrayLength(queryKeys);
    ccxt::any url =
        add(add(::getValue(::getValue(this->urls, std::string("api")), api),
                std::string("/")),
            implodedPath);
    ccxt::any queryString = std::string("");
    if (isTrue(isEqual(api, std::string("private")))) {
      this->checkRequiredCredentials();
      ccxt::any timestamp = toString(this->milliseconds());
      if (isTrue(isEqual(method, std::string("GET")))) {
        if (isTrue(isGreaterThan(queryLength, 0))) {
          queryString = this->urlencode(query);
          url = add(url, add(std::string("?"), queryString));
        }
      } else if (isTrue(isEqual(method, std::string("DELETE")))) {
        if (isTrue(isGreaterThan(queryLength, 0))) {
          queryString = this->urlencode(query);
          url = add(url, add(std::string("?"), queryString));
        }
      } else {
        body = this->json(query);
      }
      ccxt::any requestPath = add(std::string("/api/"), implodedPath);
      ccxt::any bodyString = std::string("");
      if (isTrue(!isEqual(body, ccxt::any{}))) {
        bodyString = body;
      }
      ccxt::any message =
          add(add(add(add(timestamp, toUpperCase(method)), requestPath),
                  queryString),
              bodyString);
      ccxt::any signature =
          eddsa(this->encode(message), this->privateKey, ed25519);
      headers = ccxt::dict{
          {std::string("X-Revx-API-Key"), this->apiKey},
          {std::string("X-Revx-Timestamp"), timestamp},
          {std::string("X-Revx-Signature"), signature},
      };
      if (isTrue(isTrue(isEqual(method, std::string("POST"))) ||
                 isTrue(isEqual(method, std::string("PUT"))))) {
        ::setValue(headers, std::string("Content-Type"),
                   std::string("application/json"));
      }
    } else {
      if (isTrue(isEqual(method, std::string("GET")))) {
        if (isTrue(isGreaterThan(queryLength, 0))) {
          queryString = this->urlencode(query);
          url = add(url, add(std::string("?"), queryString));
        }
      } else {
        body = this->json(query);
        headers = ccxt::dict{
            {std::string("Content-Type"), std::string("application/json")},
        };
      }
    }
    return ccxt::dict{
        {std::string("url"), url},
        {std::string("method"), method},
        {std::string("body"), body},
        {std::string("headers"), headers},
    };
  }

  /**
   * @method
   * @name revolutx#parseMarket
   * @description parses a market from the exchange's market data
   * @ignore
   * @param {object} market the raw market data from the exchange
   * @returns {object} a [market structure]{@link
   * https://docs.ccxt.com/?id=market-structure}
   */
  ccxt::any parseMarket(ccxt::any market) override {
    ccxt::any id = this->safeString(market, std::string("id"));
    ccxt::any base =
        this->safeString(market, std::string("base"), std::string(""));
    ccxt::any quote =
        this->safeString(market, std::string("quote"), std::string(""));
    ccxt::any baseId = base;
    ccxt::any quoteId = quote;
    ccxt::any baseStep = this->safeString(market, std::string("base_step"));
    ccxt::any quoteStep = this->safeString(market, std::string("quote_step"));
    ccxt::any minOrderSize =
        this->safeString(market, std::string("min_order_size"));
    ccxt::any maxOrderSize =
        this->safeString(market, std::string("max_order_size"));
    ccxt::any minOrderSizeQuote =
        this->safeString(market, std::string("min_order_size_quote"));
    ccxt::any status = this->safeString(market, std::string("status"));
    ccxt::any active = (isEqual(status, std::string("active")));
    ccxt::any symbol = add(add(base, std::string("/")), quote);
    return ccxt::dict{
        {std::string("id"), id},
        {std::string("symbol"), symbol},
        {std::string("base"), base},
        {std::string("quote"), quote},
        {std::string("baseId"), baseId},
        {std::string("quoteId"), quoteId},
        {std::string("active"), active},
        {std::string("type"), std::string("spot")},
        {std::string("spot"), true},
        {std::string("margin"), false},
        {std::string("swap"), false},
        {std::string("future"), false},
        {std::string("option"), false},
        {std::string("contract"), false},
        {std::string("settle"), ccxt::any{}},
        {std::string("settleId"), ccxt::any{}},
        {std::string("contractSize"), ccxt::any{}},
        {std::string("linear"), ccxt::any{}},
        {std::string("inverse"), ccxt::any{}},
        {std::string("expiry"), ccxt::any{}},
        {std::string("expiryDatetime"), ccxt::any{}},
        {std::string("strike"), ccxt::any{}},
        {std::string("optionType"), ccxt::any{}},
        {std::string("taker"), this->parseNumber(std::string("0.0009"))},
        {std::string("maker"), this->parseNumber(std::string("0"))},
        {std::string("percentage"), true},
        {std::string("tierBased"), false},
        {std::string("feeSide"), std::string("get")},
        {std::string("precision"),
         ccxt::dict{
             {std::string("amount"), this->parseNumber(baseStep)},
             {std::string("price"), this->parseNumber(quoteStep)},
         }},
        {std::string("limits"),
         ccxt::dict{
             {std::string("amount"),
              ccxt::dict{
                  {std::string("min"), this->parseNumber(minOrderSize)},
                  {std::string("max"), this->parseNumber(maxOrderSize)},
              }},
             {std::string("price"),
              ccxt::dict{
                  {std::string("min"), ccxt::any{}},
                  {std::string("max"), ccxt::any{}},
              }},
             {std::string("cost"),
              ccxt::dict{
                  {std::string("min"), this->parseNumber(minOrderSizeQuote)},
                  {std::string("max"), ccxt::any{}},
              }},
             {std::string("leverage"),
              ccxt::dict{
                  {std::string("min"), ccxt::any{}},
                  {std::string("max"), ccxt::any{}},
              }},
             {std::string("market"), ccxt::any{}},
         }},
        {std::string("created"), ccxt::any{}},
        {std::string("info"), market},
    };
  }

  /**
   * @method
   * @name revolutx#fetchMarkets
   * @description retrieves all available markets on the exchange
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.region] the region to filter markets by (e.g. EEA,
   * UK)
   * @returns {object[]} an array of [market structures]{@link
   * https://docs.ccxt.com/?id=market-structure}
   */
  std::shared_future<ccxt::any>
  fetchMarkets(ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 ccxt::any request = ccxt::dict{};
                 ccxt::any region = this->safeString2(
                     params, std::string("region"), std::string("region"),
                     ::getValue(this->options, std::string("region")));
                 if (isTrue(!isEqual(region, ccxt::any{}))) {
                   ::setValue(request, std::string("region"), region);
                 }
                 ccxt::any response =
                     awaitValue(this->publicGet10PublicConfigurationPairs(
                         this->extend(request, params)));
                 //
                 //     {
                 //         "BTC/USD": {
                 //             "base": "BTC", "quote": "USD",
                 //             "base_step": "0.0000001", "quote_step": "0.01",
                 //             "min_order_size": "0.0000001", "max_order_size":
                 //             "1000", "min_order_size_quote": "0.01",
                 //             "status": "active"
                 //         }
                 //     }
                 //
                 ccxt::any markets =
                     this->safeDict(response, std::string("data"), response);
                 ccxt::any keys = getObjectKeys(markets);
                 ccxt::any result = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(keys));
                      postFixIncrement(i)) {
                   ccxt::any key = ::getValue(keys, i);
                   ccxt::any market =
                       this->safeDict(markets, key, ccxt::dict{});
                   ccxt::any base =
                       this->safeString(market, std::string("base"));
                   ccxt::any quote =
                       this->safeString(market, std::string("quote"));
                   ccxt::any marketId = add(add(base, std::string("-")), quote);
                   ccxt::any marketData =
                       this->extend(market, ccxt::dict{
                                                {std::string("id"), marketId},
                                            });
                   arrayPush(result, this->parseMarket(marketData));
                 }
                 return result;
               })
        .share();
  }

  /**
   * @method
   * @name revolutx#parseCurrency
   * @description parses a currency from the exchange's currency data
   * @ignore
   * @param {object} currency the raw currency data from the exchange
   * @returns {object} a [currency structure]{@link
   * https://docs.ccxt.com/?id=currency-structure}
   */
  ccxt::any parseCurrency(ccxt::any currency) override {
    ccxt::any id = this->safeString2(currency, std::string("id"),
                                     std::string("symbol"), std::string(""));
    ccxt::any code = this->safeCurrencyCode(id);
    ccxt::any name = this->safeString(currency, std::string("name"));
    ccxt::any scale = this->safeInteger(currency, std::string("scale"));
    ccxt::any status = this->safeString(currency, std::string("status"));
    ccxt::any active = (isEqual(status, std::string("active")));
    ccxt::any assetType = this->safeString(currency, std::string("asset_type"));
    ccxt::any type = (isTrue((isEqual(assetType, std::string("crypto"))))
                          ? ccxt::any(std::string("crypto"))
                          : ccxt::any(std::string("fiat")));
    ccxt::any precision = (isTrue((!isEqual(scale, ccxt::any{})))
                               ? ccxt::any(mathPow(10, prefixUnaryNeg(scale)))
                               : ccxt::any(ccxt::any{}));
    return ccxt::dict{
        {std::string("info"), currency},
        {std::string("id"), id},
        {std::string("code"), code},
        {std::string("name"), name},
        {std::string("active"), active},
        {std::string("type"), type},
        {std::string("deposit"), ccxt::any{}},
        {std::string("withdraw"), ccxt::any{}},
        {std::string("precision"), this->parseNumber(precision)},
        {std::string("fee"), ccxt::any{}},
        {std::string("limits"),
         ccxt::dict{
             {std::string("amount"),
              ccxt::dict{
                  {std::string("min"), ccxt::any{}},
                  {std::string("max"), ccxt::any{}},
              }},
             {std::string("withdraw"),
              ccxt::dict{
                  {std::string("min"), ccxt::any{}},
                  {std::string("max"), ccxt::any{}},
              }},
             {std::string("deposit"),
              ccxt::dict{
                  {std::string("min"), ccxt::any{}},
                  {std::string("max"), ccxt::any{}},
              }},
         }},
        {std::string("networks"), ccxt::dict{}},
    };
  }

  /**
   * @method
   * @name revolutx#fetchCurrencies
   * @description fetches all available currencies on the exchange
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.region] the region to filter currencies by
   * @returns {object} a dictionary of [currency structures]{@link
   * https://docs.ccxt.com/?id=currency-structure}
   */
  std::shared_future<ccxt::any>
  fetchCurrencies(ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 ccxt::any request = ccxt::dict{};
                 ccxt::any region = this->safeString2(
                     params, std::string("region"), std::string("region"),
                     ::getValue(this->options, std::string("region")));
                 if (isTrue(!isEqual(region, ccxt::any{}))) {
                   ::setValue(request, std::string("region"), region);
                 }
                 ccxt::any response =
                     awaitValue(this->publicGet10PublicConfigurationCurrencies(
                         this->extend(request, params)));
                 //
                 //     {
                 //         "BTC": { "symbol": "BTC", "name": "Bitcoin",
                 //         "scale": 8, "asset_type": "crypto", "status":
                 //         "active" }, "USD": { "symbol": "$", "name": "US
                 //         Dollar", "scale": 2, "asset_type": "fiat", "status":
                 //         "active" }
                 //     }
                 //
                 ccxt::any currencies =
                     this->safeDict(response, std::string("data"), response);
                 ccxt::any keys = getObjectKeys(currencies);
                 ccxt::any result = ccxt::dict{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(keys));
                      postFixIncrement(i)) {
                   ccxt::any key = ::getValue(keys, i);
                   ccxt::any currency =
                       this->safeDict(currencies, key, ccxt::dict{});
                   ccxt::any currencyData =
                       this->extend(currency, ccxt::dict{
                                                  {std::string("id"), key},
                                              });
                   ccxt::any parsed = this->parseCurrency(currencyData);
                   ccxt::any code = this->safeString(
                       parsed, std::string("code"), std::string(""));
                   if (isTrue(isEqual(code, std::string("")))) {
                     continue;
                   }
                   ::setValue(result, code, parsed);
                 }
                 return result;
               })
        .share();
  }

  /**
   * @method
   * @name revolutx#parseTicker
   * @description parses a ticker from the exchange's ticker data
   * @ignore
   * @param {object} ticker the raw ticker data from the exchange
   * @param {object} [market] the market the ticker is for
   * @returns {object} a [ticker structure]{@link
   * https://docs.ccxt.com/?id=ticker-structure}
   */
  ccxt::any parseTicker(ccxt::any ticker,
                        ccxt::any market = ccxt::any{}) override {
    ccxt::any tickerSymbol = this->safeString(ticker, std::string("symbol"));
    ccxt::any symbol = this->safeSymbol(tickerSymbol, market, std::string("/"));
    ccxt::any bid = this->safeString(ticker, std::string("bid"));
    ccxt::any ask = this->safeString(ticker, std::string("ask"));
    ccxt::any last = this->safeString(ticker, std::string("last_price"));
    ccxt::any high = this->safeString(ticker, std::string("high_24h"));
    ccxt::any low = this->safeString(ticker, std::string("low_24h"));
    ccxt::any priceChange =
        this->safeString(ticker, std::string("price_change_24h"));
    ccxt::any baseVolume = this->safeString(ticker, std::string("volume_24h"));
    ccxt::any timestamp = this->safeInteger(ticker, std::string("timestamp"));
    ccxt::any open = ccxt::any{};
    if (isTrue(isTrue(!isEqual(last, ccxt::any{})) &&
               isTrue(!isEqual(priceChange, ccxt::any{})))) {
      open = ccxt::Precise::stringSub(last, priceChange);
    }
    ccxt::any percentage = ccxt::any{};
    if (isTrue(isTrue(!isEqual(open, ccxt::any{})) &&
               isTrue(!isEqual(priceChange, ccxt::any{})))) {
      ccxt::any percentageString =
          ccxt::Precise::stringDiv(priceChange, open, 8);
      percentage = this->parseNumber(
          ccxt::Precise::stringMul(percentageString, std::string("100")));
    }
    return this->safeTicker(
        ccxt::dict{
            {std::string("symbol"), symbol},
            {std::string("timestamp"), timestamp},
            {std::string("datetime"), this->iso8601(timestamp)},
            {std::string("high"), high},
            {std::string("low"), low},
            {std::string("bid"), bid},
            {std::string("bidVolume"), ccxt::any{}},
            {std::string("ask"), ask},
            {std::string("askVolume"), ccxt::any{}},
            {std::string("vwap"), ccxt::any{}},
            {std::string("open"), open},
            {std::string("close"), last},
            {std::string("last"), last},
            {std::string("previousClose"), ccxt::any{}},
            {std::string("change"), priceChange},
            {std::string("percentage"), percentage},
            {std::string("average"), ccxt::any{}},
            {std::string("baseVolume"), baseVolume},
            {std::string("quoteVolume"), ccxt::any{}},
            {std::string("info"), ticker},
        },
        market);
  }

  /**
   * @method
   * @name revolutx#fetchTickers
   * @description fetches price tickers for multiple markets, statistical
   * information calculated over the past 24 hours for each market
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data
   * @param {string[]|undefined} symbols unified symbols of the markets to fetch
   * the ticker for, all market tickers are returned if not assigned
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.region] the region to fetch tickers for (e.g. EEA,
   * UK)
   * @returns {object} a dictionary of [ticker structures]{@link
   * https://docs.ccxt.com/?id=ticker-structure}
   */
  std::shared_future<ccxt::any>
  fetchTickers(ccxt::any symbols = ccxt::any{},
               ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any request = ccxt::dict{};
                 if (isTrue(!isEqual(symbols, ccxt::any{}))) {
                   ccxt::any marketIds = ccxt::list{};
                   for (ccxt::any i = 0; isLessThan(i, getArrayLength(symbols));
                        postFixIncrement(i)) {
                     ccxt::any symbol = ::getValue(symbols, i);
                     ccxt::any market = this->market(symbol);
                     arrayPush(marketIds,
                               ::getValue(market, std::string("id")));
                   }
                   ::setValue(request, std::string("symbols"),
                              join(marketIds, std::string(",")));
                 }
                 ccxt::any region = this->safeString2(
                     params, std::string("region"), std::string("region"),
                     ::getValue(this->options, std::string("region")));
                 if (isTrue(!isEqual(region, ccxt::any{}))) {
                   ::setValue(request, std::string("region"), region);
                 }
                 ccxt::any response = awaitValue(this->publicGet10PublicTickers(
                     this->extend(request, params)));
                 //
                 //     {
                 //         "data": [
                 //             { "symbol": "BTC/USD", "bid": "119950.00",
                 //             "ask": "120050.00", "mid": "120000.00",
                 //               "last_price": "119980.00", "low_24h":
                 //               "115000.00", "high_24h": "122500.00",
                 //               "price_change_24h": "2480.00", "volume_24h":
                 //               "135.42000000", "region": "EEA" }
                 //         ],
                 //         "metadata": { "timestamp": 1785313433816 }
                 //     }
                 //
                 ccxt::any data = this->safeList(response, std::string("data"),
                                                 ccxt::list{});
                 ccxt::any metadata = this->safeDict(
                     response, std::string("metadata"), ccxt::dict{});
                 ccxt::any timestamp =
                     this->safeInteger(metadata, std::string("timestamp"));
                 ccxt::any result = ccxt::dict{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(data));
                      postFixIncrement(i)) {
                   ccxt::any tickerData = this->safeDict(data, i, ccxt::dict{});
                   ::setValue(tickerData, std::string("timestamp"), timestamp);
                   ccxt::any ticker = this->parseTicker(tickerData);
                   ccxt::any symbol = this->safeString(
                       ticker, std::string("symbol"), std::string(""));
                   if (isTrue(isEqual(symbol, std::string("")))) {
                     continue;
                   }
                   ::setValue(result, symbol, ticker);
                 }
                 if (isTrue(!isEqual(symbols, ccxt::any{}))) {
                   ccxt::any filtered = ccxt::dict{};
                   for (ccxt::any i = 0; isLessThan(i, getArrayLength(symbols));
                        postFixIncrement(i)) {
                     ccxt::any s = ::getValue(symbols, i);
                     if (isTrue(inOp(result, s))) {
                       ::setValue(filtered, s, ::getValue(result, s));
                     }
                   }
                   return filtered;
                 }
                 return result;
               })
        .share();
  }

  /**
   * @method
   * @name revolutx#fetchTicker
   * @description fetches a price ticker for a given market symbol
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data
   * @param {string} symbol unified symbol of the market to fetch the ticker for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.region] the region to fetch the ticker for
   * @returns {object} a [ticker structure]{@link
   * https://docs.ccxt.com/?id=ticker-structure}
   */
  std::shared_future<ccxt::any>
  fetchTicker(ccxt::any symbol, ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any tickers =
                     awaitValue(this->fetchTickers(ccxt::list{symbol}, params));
                 ccxt::any ticker = this->safeDict(tickers, symbol);
                 if (isTrue(isEqual(ticker, ccxt::any{}))) {
                   throw ExchangeError(toString(add(
                       add(this->id, std::string(" fetchTicker() could not "
                                                 "find ticker for symbol ")),
                       symbol)));
                 }
                 return ticker;
               })
        .share();
  }

  /**
   * @method
   * @name revolutx#fetchOrderBook
   * @description fetches the current order book snapshot for a given market
   * symbol
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data
   * @param {string} symbol unified symbol of the market to fetch the order book
   * for
   * @param {int} [limit] the maximum number of orders to return (1-50, default
   * 50)
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.region] the region to fetch the order book for
   * @returns {object} an [order book structure]{@link
   * https://docs.ccxt.com/?id=order-book-structure}
   */
  std::shared_future<ccxt::any>
  fetchOrderBook(ccxt::any symbol, ccxt::any limit = ccxt::any{},
                 ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = this->market(symbol);
                 ccxt::any request = ccxt::dict{
                     {std::string("symbol"),
                      ::getValue(market, std::string("id"))},
                 };
                 if (isTrue(!isEqual(limit, ccxt::any{}))) {
                   ::setValue(request, std::string("limit"), limit);
                 }
                 ccxt::any region = this->safeString2(
                     params, std::string("region"), std::string("region"),
                     ::getValue(this->options, std::string("region")));
                 if (isTrue(!isEqual(region, ccxt::any{}))) {
                   ::setValue(request, std::string("region"), region);
                 }
                 ccxt::any response =
                     awaitValue(this->publicGet20PublicOrderBookSymbol(
                         this->extend(request, params)));
                 //
                 //     {
                 //         "data": {
                 //             "asks": [ { "price": "4005.00", "quantity":
                 //             "1.7000", "count": 3 } ], "bids": [ { "price":
                 //             "4000.00", "quantity": "0.25", "count": 1 } ]
                 //         },
                 //         "metadata": { "region": "UK", "timestamp":
                 //         1785313433816 }
                 //     }
                 //
                 ccxt::any data = this->safeDict(response, std::string("data"),
                                                 ccxt::dict{});
                 ccxt::any metadata = this->safeDict(
                     response, std::string("metadata"), ccxt::dict{});
                 ccxt::any timestamp =
                     this->safeInteger(metadata, std::string("timestamp"));
                 return this->parseOrderBook(
                     data, symbol, timestamp, std::string("bids"),
                     std::string("asks"), std::string("price"),
                     std::string("quantity"));
               })
        .share();
  }

  /**
   * @method
   * @name revolutx#parseOHLCV
   * @description parses an OHLCV candle from the exchange's candle data
   * @ignore
   * @param {object} ohlcv the raw candle data from the exchange
   * @param {object} [market] the market the candle is for
   * @returns {int[]} an [OHLCV structure]{@link
   * https://docs.ccxt.com/?id=ohlcv-structure}
   */
  ccxt::any parseOHLCV(ccxt::any ohlcv,
                       ccxt::any market = ccxt::any{}) override {
    ccxt::any timestamp = this->safeInteger(ohlcv, std::string("start"));
    ccxt::any open = this->safeNumber(ohlcv, std::string("open"));
    ccxt::any high = this->safeNumber(ohlcv, std::string("high"));
    ccxt::any low = this->safeNumber(ohlcv, std::string("low"));
    ccxt::any close = this->safeNumber(ohlcv, std::string("close"));
    ccxt::any volume = this->safeNumber(ohlcv, std::string("volume"));
    return ccxt::list{timestamp, open, high, low, close, volume};
  }

  /**
   * @method
   * @name revolutx#fetchOHLCV
   * @description fetches historical candlestick data for a given market symbol
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data
   * @param {string} symbol unified symbol of the market to fetch OHLCV data for
   * @param {string} timeframe the length of time each candle represents (e.g.
   * 1m, 5m, 1h, 1d)
   * @param {int} [since] timestamp in ms of the earliest candle to fetch
   * @param {int} [limit] the maximum number of candles to return
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {int} [params.until] timestamp in ms of the latest candle to fetch
   * @param {string} [params.region] the region to fetch candles for
   * @returns {int[][]} a list of [OHLCV structures]{@link
   * https://docs.ccxt.com/?id=ohlcv-structure}
   */
  std::shared_future<ccxt::any>
  fetchOHLCV(ccxt::any symbol, ccxt::any timeframe = std::string("1m"),
             ccxt::any since = ccxt::any{}, ccxt::any limit = ccxt::any{},
             ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                          awaitValue(this->loadMarkets());
                        }
                        ccxt::any market = this->market(symbol);
                        ccxt::any request = ccxt::dict{
                            {std::string("symbol"),
                             ::getValue(market, std::string("id"))},
                            {std::string("interval"),
                             this->safeInteger(this->timeframes, timeframe, 5)},
                        };
                        if (isTrue(!isEqual(since, ccxt::any{}))) {
                          ::setValue(request, std::string("since"), since);
                        }
                        ccxt::any until = this->safeInteger2(
                            params, std::string("until"), std::string("until"));
                        if (isTrue(!isEqual(until, ccxt::any{}))) {
                          ::setValue(request, std::string("until"), until);
                        } else {
                          ::setValue(request, std::string("until"),
                                     this->milliseconds());
                        }
                        ccxt::any region = this->safeString2(
                            params, std::string("region"),
                            std::string("region"),
                            ::getValue(this->options, std::string("region")));
                        if (isTrue(!isEqual(region, ccxt::any{}))) {
                          ::setValue(request, std::string("region"), region);
                        }
                        ccxt::any response =
                            awaitValue(this->publicGet10PublicCandlesSymbol(
                                this->extend(request, params)));
                        //
                        //     {
                        //         "data": [
                        //             { "start": 1785309833816, "open":
                        //             "119800.00", "high": "120100.00",
                        //               "low": "119700.00", "close":
                        //               "120000.00", "volume": "0.25000000" }
                        //         ],
                        //         "metadata": { "region": "EEA", "timestamp":
                        //         1785313433816 }
                        //     }
                        //
                        ccxt::any data = this->safeList(
                            response, std::string("data"), ccxt::list{});
                        return this->parseOHLCVs(data, market, timeframe, since,
                                                 limit);
                      })
        .share();
  }

  /**
   * @method
   * @name revolutx#parseTrade
   * @description parses a trade from the exchange's public trade data
   * @ignore
   * @param {object} trade the raw trade data from the exchange
   * @param {object} [market] the market the trade was executed in
   * @returns {object} a [trade structure]{@link
   * https://docs.ccxt.com/?id=trade-structure}
   */
  ccxt::any parseTrade(ccxt::any trade,
                       ccxt::any market = ccxt::any{}) override {
    ccxt::any id = this->safeString(trade, std::string("id"));
    ccxt::any tradeSymbol = this->safeString(trade, std::string("symbol"));
    ccxt::any symbol = this->safeSymbol(tradeSymbol, market, std::string("/"));
    ccxt::any price = this->safeNumber(trade, std::string("price"));
    ccxt::any amount = this->safeNumber(trade, std::string("quantity"));
    ccxt::any side = this->safeStringLower(trade, std::string("side"));
    ccxt::any timestamp = this->safeInteger(trade, std::string("timestamp"));
    ccxt::any cost = ccxt::any{};
    if (isTrue(isTrue(!isEqual(price, ccxt::any{})) &&
               isTrue(!isEqual(amount, ccxt::any{})))) {
      cost = multiply(price, amount);
    }
    return ccxt::dict{
        {std::string("info"), trade},
        {std::string("id"), id},
        {std::string("order"), ccxt::any{}},
        {std::string("symbol"), symbol},
        {std::string("side"), side},
        {std::string("type"), ccxt::any{}},
        {std::string("takerOrMaker"), ccxt::any{}},
        {std::string("price"), price},
        {std::string("amount"), amount},
        {std::string("cost"), cost},
        {std::string("timestamp"), timestamp},
        {std::string("datetime"), this->iso8601(timestamp)},
        {std::string("fee"), ccxt::any{}},
        {std::string("fees"), ccxt::list{}},
    };
  }

  /**
   * @method
   * @name revolutx#fetchTrades
   * @description fetches the public trade history for a given market symbol
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-public-market-data
   * @param {string} symbol unified symbol of the market to fetch trades for
   * @param {int} [since] timestamp in ms of the earliest trade to fetch
   * @param {int} [limit] the maximum number of trades to return (1-1900,
   * default 1900)
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {int} [params.until] timestamp in ms of the latest trade to fetch
   * @param {string} [params.cursor] pagination cursor from the previous
   * response
   * @returns {object[]} a list of [trade structures]{@link
   * https://docs.ccxt.com/?id=trade-structure}
   */
  std::shared_future<ccxt::any>
  fetchTrades(ccxt::any symbol, ccxt::any since = ccxt::any{},
              ccxt::any limit = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = ccxt::any{};
                 if (isTrue(!isEqual(symbol, ccxt::any{}))) {
                   market = this->market(symbol);
                 }
                 ccxt::any request = ccxt::dict{};
                 if (isTrue(!isEqual(market, ccxt::any{}))) {
                   ::setValue(request, std::string("symbol"),
                              ::getValue(market, std::string("id")));
                 }
                 if (isTrue(!isEqual(since, ccxt::any{}))) {
                   ::setValue(request, std::string("start_date"), since);
                 }
                 ccxt::any until = this->safeInteger2(
                     params, std::string("until"), std::string("until"));
                 if (isTrue(!isEqual(until, ccxt::any{}))) {
                   ::setValue(request, std::string("end_date"), until);
                 } else if (isTrue(!isEqual(since, ccxt::any{}))) {
                   ::setValue(request, std::string("end_date"),
                              this->milliseconds());
                 }
                 if (isTrue(!isEqual(limit, ccxt::any{}))) {
                   ::setValue(request, std::string("limit"), limit);
                 }
                 ccxt::any cursor =
                     this->safeString(params, std::string("cursor"));
                 if (isTrue(!isEqual(cursor, ccxt::any{}))) {
                   ::setValue(request, std::string("cursor"), cursor);
                 }
                 ccxt::any response =
                     awaitValue(this->publicGet10PublicTradesAll(
                         this->extend(request, params)));
                 //
                 //     {
                 //         "data": [
                 //             { "id": "3b2b202b-7668-43cf-a6c8-b3354e7f4c52",
                 //             "symbol": "BTC/USD",
                 //               "price": "119980.00", "quantity":
                 //               "0.01000000", "side": "sell", "timestamp":
                 //               1785313433816 }
                 //         ],
                 //         "metadata": { "timestamp": 1785313433816,
                 //         "next_cursor": "..." }
                 //     }
                 //
                 ccxt::any data = this->safeList(response, std::string("data"),
                                                 ccxt::list{});
                 ccxt::any result = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(data));
                      postFixIncrement(i)) {
                   ccxt::any trade = this->safeDict(data, i, ccxt::dict{});
                   arrayPush(result, this->parseTrade(trade, market));
                 }
                 return this->filterBySymbolSinceLimit(
                     this->sortBy(result, std::string("timestamp")), symbol,
                     since, limit);
               })
        .share();
  }

  /**
   * @method
   * @name revolutx#fetchBalance
   * @description fetches the current balance for the authenticated user
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [balance structure]{@link
   * https://docs.ccxt.com/?id=balance-structure}
   */
  std::shared_future<ccxt::any>
  fetchBalance(ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any response =
                     awaitValue(this->privateGet10Balances(params));
                 //
                 //     [
                 //         { "currency": "BTC", "available": "1.25000000",
                 //         "reserved": "0.10000000", "total": "1.35000000" },
                 //         { "currency": "USD", "available": "50000.00",
                 //         "reserved": "1000.00", "total": "51000.00",
                 //         "staked": "32.00000000" }
                 //     ]
                 //
                 ccxt::any data =
                     (isTrue(isArray(response))
                          ? ccxt::any(response)
                          : ccxt::any(this->safeList(
                                response, std::string("data"), ccxt::list{})));
                 ccxt::any result = ccxt::dict{
                     {std::string("info"), response},
                 };
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(data));
                      postFixIncrement(i)) {
                   ccxt::any balance = this->safeDict(data, i, ccxt::dict{});
                   ccxt::any currency =
                       this->safeString(balance, std::string("currency"));
                   ccxt::any code = this->safeCurrencyCode(currency);
                   if (isTrue(isEqual(code, ccxt::any{}))) {
                     continue;
                   }
                   ccxt::any account = this->account();
                   ::setValue(
                       account, std::string("free"),
                       this->safeString(balance, std::string("available")));
                   ccxt::any reserved =
                       this->safeString(balance, std::string("reserved"));
                   ccxt::any staked =
                       this->safeString(balance, std::string("staked"));
                   ccxt::any used = reserved;
                   if (isTrue(!isEqual(staked, ccxt::any{}))) {
                     used = (isTrue((isEqual(reserved, ccxt::any{})))
                                 ? ccxt::any(staked)
                                 : ccxt::any(ccxt::Precise::stringAdd(reserved,
                                                                      staked)));
                   }
                   ::setValue(account, std::string("used"), used);
                   ::setValue(account, std::string("total"),
                              this->safeString(balance, std::string("total")));
                   ::setValue(result, code, account);
                 }
                 return this->safeBalance(result);
               })
        .share();
  }

  /**
   * @method
   * @name revolutx#parseOrderStatus
   * @description parses the order status from the exchange's status to the
   * unified ccxt status
   * @ignore
   * @param {string} status the exchange-specific order status
   * @returns {string|undefined} the unified order status
   */
  virtual ccxt::any parseOrderStatus(ccxt::any status) {
    ccxt::any statuses = ccxt::dict{
        {std::string("pending_new"), std::string("open")},
        {std::string("new"), std::string("open")},
        {std::string("partially_filled"), std::string("open")},
        {std::string("filled"), std::string("closed")},
        {std::string("cancelled"), std::string("canceled")},
        {std::string("canceled"), std::string("canceled")},
        {std::string("rejected"), std::string("rejected")},
        {std::string("replaced"), std::string("open")},
    };
    return this->safeString(statuses, status, status);
  }

  /**
   * @method
   * @name revolutx#parseOrder
   * @description parses an order from the exchange's order data
   * @ignore
   * @param {object} order the raw order data from the exchange
   * @param {object} [market] the market the order was placed in
   * @returns {object} an [order structure]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  ccxt::any parseOrder(ccxt::any order,
                       ccxt::any market = ccxt::any{}) override {
    ccxt::any orderId = this->safeString2(order, std::string("id"),
                                          std::string("venue_order_id"));
    ccxt::any clientOrderId =
        this->safeString(order, std::string("client_order_id"));
    ccxt::any orderSymbol = this->safeString(order, std::string("symbol"));
    ccxt::any symbol = this->safeSymbol(orderSymbol, market, std::string("/"));
    ccxt::any side = this->safeStringLower(order, std::string("side"));
    ccxt::any orderType = this->safeStringLower(order, std::string("type"));
    ccxt::any quantity = this->safeString(order, std::string("quantity"));
    ccxt::any filledQuantity =
        this->safeString(order, std::string("filled_quantity"));
    ccxt::any leavesQuantity =
        this->safeString(order, std::string("leaves_quantity"));
    ccxt::any price = this->safeString(order, std::string("price"));
    ccxt::any averageFillPrice =
        this->safeString(order, std::string("average_fill_price"));
    ccxt::any amount = this->safeString(order, std::string("amount"));
    ccxt::any filledAmount =
        this->safeString(order, std::string("filled_amount"));
    ccxt::any totalFee = this->safeString(order, std::string("total_fee"));
    ccxt::any feeCurrency =
        this->safeString(order, std::string("fee_currency"));
    ccxt::any status =
        this->parseOrderStatus(this->safeString(order, std::string("status")));
    ccxt::any timeInForce =
        this->safeStringUpper(order, std::string("time_in_force"));
    ccxt::any createdDate =
        this->safeInteger(order, std::string("created_date"));
    ccxt::any updatedDate =
        this->safeInteger(order, std::string("updated_date"));
    ccxt::any fee = ccxt::any{};
    if (isTrue(!isEqual(totalFee, ccxt::any{}))) {
      fee = ccxt::dict{
          {std::string("cost"), this->parseNumber(totalFee)},
          {std::string("currency"), feeCurrency},
      };
    }
    ccxt::any amountValue = ccxt::any{};
    if (isTrue(!isEqual(quantity, ccxt::any{}))) {
      amountValue = quantity;
    } else if (isTrue(!isEqual(amount, ccxt::any{}))) {
      amountValue = amount;
    }
    ccxt::any filledValue = ccxt::any{};
    if (isTrue(!isEqual(filledQuantity, ccxt::any{}))) {
      filledValue = filledQuantity;
    } else if (isTrue(!isEqual(filledAmount, ccxt::any{}))) {
      filledValue = filledAmount;
    }
    ccxt::any remainingValue = ccxt::any{};
    if (isTrue(!isEqual(leavesQuantity, ccxt::any{}))) {
      remainingValue = leavesQuantity;
    }
    return this->safeOrder(
        ccxt::dict{
            {std::string("id"), orderId},
            {std::string("clientOrderId"), clientOrderId},
            {std::string("symbol"), symbol},
            {std::string("side"), side},
            {std::string("type"), orderType},
            {std::string("price"), this->parseNumber(price)},
            {std::string("average"), this->parseNumber(averageFillPrice)},
            {std::string("amount"), this->parseNumber(amountValue)},
            {std::string("filled"), this->parseNumber(filledValue)},
            {std::string("remaining"), this->parseNumber(remainingValue)},
            {std::string("status"), status},
            {std::string("timeInForce"), timeInForce},
            {std::string("timestamp"), createdDate},
            {std::string("datetime"), this->iso8601(createdDate)},
            {std::string("lastUpdateTimestamp"), updatedDate},
            {std::string("fee"), fee},
            {std::string("info"), order},
        },
        market);
  }

  /**
   * @method
   * @name revolutx#createOrder
   * @description create a trade order
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-trading
   * @param {string} symbol unified symbol of the market to create an order in
   * @param {string} type 'limit' or 'market'
   * @param {string} side 'buy' or 'sell'
   * @param {float} amount how much of the base currency you want to trade
   * @param {float} [price] the price at which the order is to be fulfilled, in
   * units of the quote currency, ignored in market orders
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.clientOrderId] a custom client order id (UUID)
   * @param {float} [params.cost] the order cost in units of the quote currency
   * (alternative to amount)
   * @param {string} [params.timeInForce] 'gtc' or 'ioc' for limit orders
   * @param {string[]} [params.executionInstructions] limit order instructions,
   * e.g. ['post_only'] or ['allow_taker']
   * @returns {object} an [order structure]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  createOrder(ccxt::any symbol, ccxt::any type, ccxt::any side,
              ccxt::any amount, ccxt::any price = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = this->market(symbol);
                 ccxt::any clientOrderId = this->safeString2(
                     params, std::string("clientOrderId"),
                     std::string("client_order_id"), this->uuid());
                 ccxt::any cost = this->safeString2(params, std::string("cost"),
                                                    std::string("quote_size"));
                 ccxt::any timeInForce =
                     this->safeStringLower2(params, std::string("timeInForce"),
                                            std::string("time_in_force"));
                 ccxt::any executionInstructions = this->safeList(
                     params, std::string("executionInstructions"),
                     this->safeList(params,
                                    std::string("execution_instructions")));
                 ccxt::any orderConfiguration = ccxt::dict{};
                 if (isTrue(isEqual(type, std::string("limit")))) {
                   ccxt::any limitConfig = ccxt::dict{};
                   if (isTrue(!isEqual(cost, ccxt::any{}))) {
                     ::setValue(limitConfig, std::string("quote_size"),
                                this->costToPrecision(symbol, cost));
                   } else {
                     ::setValue(limitConfig, std::string("base_size"),
                                this->amountToPrecision(symbol, amount));
                   }
                   ::setValue(limitConfig, std::string("price"),
                              this->priceToPrecision(symbol, price));
                   if (isTrue(!isEqual(timeInForce, ccxt::any{}))) {
                     ::setValue(limitConfig, std::string("time_in_force"),
                                timeInForce);
                   }
                   if (isTrue(!isEqual(executionInstructions, ccxt::any{}))) {
                     ::setValue(limitConfig,
                                std::string("execution_instructions"),
                                executionInstructions);
                   }
                   ::setValue(orderConfiguration, std::string("limit"),
                              limitConfig);
                 } else if (isTrue(isEqual(type, std::string("market")))) {
                   if (isTrue(!isEqual(timeInForce, ccxt::any{}))) {
                     throw InvalidOrder(toString(
                         add(this->id,
                             std::string(" createOrder() timeInForce is only "
                                         "supported for limit orders"))));
                   }
                   if (isTrue(!isEqual(executionInstructions, ccxt::any{}))) {
                     throw InvalidOrder(toString(add(
                         this->id,
                         std::string(" createOrder() executionInstructions are "
                                     "only supported for limit orders"))));
                   }
                   ccxt::any marketConfig = ccxt::dict{};
                   if (isTrue(!isEqual(cost, ccxt::any{}))) {
                     ::setValue(marketConfig, std::string("quote_size"),
                                this->costToPrecision(symbol, cost));
                   } else {
                     ::setValue(marketConfig, std::string("base_size"),
                                this->amountToPrecision(symbol, amount));
                   }
                   ::setValue(orderConfiguration, std::string("market"),
                              marketConfig);
                 } else {
                   throw InvalidOrder(toString(add(
                       add(this->id,
                           std::string(
                               " createOrder() does not support order type ")),
                       type)));
                 }
                 ccxt::any request = ccxt::dict{
                     {std::string("client_order_id"), clientOrderId},
                     {std::string("symbol"),
                      ::getValue(market, std::string("id"))},
                     {std::string("side"), side},
                     {std::string("order_configuration"), orderConfiguration},
                 };
                 ccxt::any response =
                     awaitValue(this->privatePost10Orders(this->extend(
                         request,
                         this->omit(
                             params,
                             ccxt::list{
                                 std::string("cost"), std::string("quote_size"),
                                 std::string("clientOrderId"),
                                 std::string("client_order_id"),
                                 std::string("timeInForce"),
                                 std::string("time_in_force"),
                                 std::string("executionInstructions"),
                                 std::string("execution_instructions")}))));
                 //
                 //     {
                 //         "data": [
                 //             { "venue_order_id":
                 //             "7a52e92e-8639-4fe1-abaa-68d3a2d5234b",
                 //             "client_order_id": "...", "state": "new" }
                 //         ]
                 //     }
                 //
                 ccxt::any data = this->safeValue(response, std::string("data"),
                                                  ccxt::dict{});
                 ccxt::any orderData =
                     (isTrue(isArray(data))
                          ? ccxt::any(this->safeDict(data, 0, ccxt::dict{}))
                          : ccxt::any(this->safeDict(
                                response, std::string("data"), ccxt::dict{})));
                 ccxt::any venueOrderId =
                     this->safeString(orderData, std::string("venue_order_id"));
                 ccxt::any state =
                     this->safeString(orderData, std::string("state"));
                 ccxt::any order = this->parseOrder(
                     this->extend(orderData,
                                  ccxt::dict{
                                      {std::string("id"), venueOrderId},
                                      {std::string("symbol"),
                                       ::getValue(market, std::string("id"))},
                                      {std::string("status"), state},
                                      {std::string("side"), side},
                                      {std::string("type"), type},
                                  }),
                     market);
                 return order;
               })
        .share();
  }

  /**
   * @method
   * @name revolutx#cancelOrder
   * @description cancels an open order by its id
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-trading
   * @param {string} id the order id (venue order id)
   * @param {string} symbol not used by this exchange
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} an [order structure]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  cancelOrder(ccxt::any id, ccxt::any symbol = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                          awaitValue(this->loadMarkets());
                        }
                        ccxt::any request = ccxt::dict{
                            {std::string("venue_order_id"), id},
                        };
                        ccxt::any response =
                            awaitValue(this->privateDelete10OrdersVenueOrderId(
                                this->extend(request, params)));
                        return this->safeOrder(ccxt::dict{
                            {std::string("info"), response},
                            {std::string("id"), id},
                            {std::string("status"), std::string("canceled")},
                        });
                      })
        .share();
  }

  /**
   * @method
   * @name revolutx#cancelAllOrders
   * @description cancels all open orders
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-trading
   * @param {string} [symbol] not used by this exchange
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} an empty [order structure]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  cancelAllOrders(ccxt::any symbol = ccxt::any{},
                  ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                          awaitValue(this->loadMarkets());
                        }
                        awaitValue(this->privateDelete10Orders(params));
                        return ccxt::list{};
                      })
        .share();
  }

  /**
   * @method
   * @name revolutx#fetchOrder
   * @description fetches an order by its id
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data
   * @param {string} id the order id (venue order id)
   * @param {string} symbol unified symbol of the market the order was made in
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} an [order structure]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  fetchOrder(ccxt::any id, ccxt::any symbol = ccxt::any{},
             ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                          awaitValue(this->loadMarkets());
                        }
                        ccxt::any request = ccxt::dict{
                            {std::string("venue_order_id"), id},
                        };
                        ccxt::any response =
                            awaitValue(this->privateGet10OrdersVenueOrderId(
                                this->extend(request, params)));
                        //
                        //     {
                        //         "data": {
                        //             "id": "uuid", "client_order_id": "uuid",
                        //             "symbol": "BTC/USD", "side": "buy",
                        //             "type": "limit", "quantity": "0.002",
                        //             "filled_quantity": "0",
                        //             "leaves_quantity": "0.002", "amount":
                        //             "240.00", "filled_amount": "0", "price":
                        //             "120000.00", "average_fill_price": "0",
                        //             "total_fee": "0", "fee_currency": "USD",
                        //             "status": "new", "time_in_force": "gtc",
                        //             "execution_instructions":
                        //             ["allow_taker"], "created_date":
                        //             1785309833816, "updated_date":
                        //             1785313433816
                        //         }
                        //     }
                        //
                        ccxt::any data = this->safeDict(
                            response, std::string("data"), ccxt::dict{});
                        ccxt::any market = ccxt::any{};
                        if (isTrue(!isEqual(symbol, ccxt::any{}))) {
                          market = this->market(symbol);
                        }
                        return this->parseOrder(data, market);
                      })
        .share();
  }

  /**
   * @method
   * @name revolutx#fetchOpenOrders
   * @description fetches all open orders for the authenticated user
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data
   * @param {string|undefined} symbol unified symbol of the market to fetch open
   * orders for
   * @param {int} [since] timestamp in ms of the earliest order to fetch
   * @param {int} [limit] the maximum number of orders to return (1-300, default
   * 300)
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.cursor] pagination cursor from the previous
   * response
   * @param {string[]} [params.orderStates] filter by order states, e.g. ['new',
   * 'partially_filled']
   * @param {string[]} [params.orderTypes] filter by order types, e.g. ['limit',
   * 'market']
   * @param {string} [params.side] filter by side, 'buy' or 'sell'
   * @returns {object[]} a list of [order structures]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  fetchOpenOrders(ccxt::any symbol = ccxt::any{}, ccxt::any since = ccxt::any{},
                  ccxt::any limit = ccxt::any{},
                  ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any request = ccxt::dict{};
                 if (isTrue(!isEqual(symbol, ccxt::any{}))) {
                   ccxt::any market = this->market(symbol);
                   ::setValue(request, std::string("symbols"),
                              ::getValue(market, std::string("id")));
                 }
                 if (isTrue(!isEqual(limit, ccxt::any{}))) {
                   ::setValue(request, std::string("limit"), limit);
                 }
                 ccxt::any cursor =
                     this->safeString(params, std::string("cursor"));
                 if (isTrue(!isEqual(cursor, ccxt::any{}))) {
                   ::setValue(request, std::string("cursor"), cursor);
                 }
                 ccxt::any orderStates =
                     this->safeList2(params, std::string("orderStates"),
                                     std::string("order_states"));
                 if (isTrue(!isEqual(orderStates, ccxt::any{}))) {
                   ::setValue(request, std::string("order_states"),
                              join(orderStates, std::string(",")));
                 }
                 ccxt::any orderTypes =
                     this->safeList2(params, std::string("orderTypes"),
                                     std::string("order_types"));
                 if (isTrue(!isEqual(orderTypes, ccxt::any{}))) {
                   ::setValue(request, std::string("order_types"),
                              join(orderTypes, std::string(",")));
                 }
                 ccxt::any side = this->safeString(params, std::string("side"));
                 if (isTrue(!isEqual(side, ccxt::any{}))) {
                   ::setValue(request, std::string("side"), side);
                 }
                 ccxt::any response =
                     awaitValue(this->privateGet10OrdersActive(this->extend(
                         request,
                         this->omit(params,
                                    ccxt::list{std::string("cursor"),
                                               std::string("orderStates"),
                                               std::string("order_states"),
                                               std::string("orderTypes"),
                                               std::string("order_types"),
                                               std::string("side")}))));
                 //
                 //     {
                 //         "data": [ { "id": "uuid", "client_order_id": "uuid",
                 //         "symbol": "BTC/USD", ... } ], "metadata": {
                 //         "timestamp": 1785313433816, "next_cursor": "..." }
                 //     }
                 //
                 ccxt::any data = this->safeList(response, std::string("data"),
                                                 ccxt::list{});
                 ccxt::any result = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(data));
                      postFixIncrement(i)) {
                   ccxt::any order = this->safeDict(data, i, ccxt::dict{});
                   arrayPush(result, this->parseOrder(order));
                 }
                 return this->filterBySymbolSinceLimit(result, symbol, since,
                                                       limit);
               })
        .share();
  }

  /**
   * @method
   * @name revolutx#fetchOrders
   * @description fetches historical orders for the authenticated user
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data
   * @param {string|undefined} symbol unified symbol of the market to fetch
   * orders for
   * @param {int} [since] timestamp in ms of the earliest order to fetch, the
   * lookup window is limited to 30 days
   * @param {int} [limit] the maximum number of orders to return (1-1900,
   * default 1900)
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {int} [params.until] timestamp in ms of the latest order to fetch
   * @param {string} [params.cursor] pagination cursor from the previous
   * response
   * @param {string[]} [params.orderStates] filter by order states, e.g.
   * ['filled', 'cancelled', 'rejected']
   * @param {string[]} [params.orderTypes] filter by order types, e.g. ['limit',
   * 'market']
   * @returns {object[]} a list of [order structures]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  fetchOrders(ccxt::any symbol = ccxt::any{}, ccxt::any since = ccxt::any{},
              ccxt::any limit = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any request = ccxt::dict{};
                 if (isTrue(!isEqual(symbol, ccxt::any{}))) {
                   ccxt::any market = this->market(symbol);
                   ::setValue(request, std::string("symbols"),
                              ::getValue(market, std::string("id")));
                 }
                 ccxt::any thirtyDays = 2592000000;
                 ccxt::any until = this->safeInteger2(
                     params, std::string("until"), std::string("until"));
                 if (isTrue(!isEqual(since, ccxt::any{}))) {
                   ::setValue(request, std::string("start_date"), since);
                 } else if (isTrue(!isEqual(until, ccxt::any{}))) {
                   ::setValue(request, std::string("start_date"),
                              subtract(until, thirtyDays));
                 }
                 if (isTrue(!isEqual(until, ccxt::any{}))) {
                   ::setValue(request, std::string("end_date"), until);
                 } else if (isTrue(!isEqual(since, ccxt::any{}))) {
                   ccxt::any now = this->milliseconds();
                   ccxt::any defaultEnd = add(since, thirtyDays);
                   ::setValue(request, std::string("end_date"),
                              (isTrue((isLessThan(defaultEnd, now)))
                                   ? ccxt::any(defaultEnd)
                                   : ccxt::any(now)));
                 }
                 if (isTrue(!isEqual(limit, ccxt::any{}))) {
                   ::setValue(request, std::string("limit"), limit);
                 }
                 ccxt::any cursor =
                     this->safeString(params, std::string("cursor"));
                 if (isTrue(!isEqual(cursor, ccxt::any{}))) {
                   ::setValue(request, std::string("cursor"), cursor);
                 }
                 ccxt::any orderStates =
                     this->safeList2(params, std::string("orderStates"),
                                     std::string("order_states"));
                 if (isTrue(!isEqual(orderStates, ccxt::any{}))) {
                   ::setValue(request, std::string("order_states"),
                              join(orderStates, std::string(",")));
                 }
                 ccxt::any orderTypes =
                     this->safeList2(params, std::string("orderTypes"),
                                     std::string("order_types"));
                 if (isTrue(!isEqual(orderTypes, ccxt::any{}))) {
                   ::setValue(request, std::string("order_types"),
                              join(orderTypes, std::string(",")));
                 }
                 ccxt::any response =
                     awaitValue(this->privateGet10OrdersHistorical(this->extend(
                         request,
                         this->omit(params,
                                    ccxt::list{std::string("until"),
                                               std::string("cursor"),
                                               std::string("orderStates"),
                                               std::string("order_states"),
                                               std::string("orderTypes"),
                                               std::string("order_types")}))));
                 ccxt::any data = this->safeList(response, std::string("data"),
                                                 ccxt::list{});
                 ccxt::any result = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(data));
                      postFixIncrement(i)) {
                   ccxt::any order = this->safeDict(data, i, ccxt::dict{});
                   arrayPush(result, this->parseOrder(order));
                 }
                 return this->filterBySymbolSinceLimit(result, symbol, since,
                                                       limit);
               })
        .share();
  }

  /**
   * @method
   * @name revolutx#fetchClosedOrders
   * @description fetches closed (filled, cancelled, rejected) orders for the
   * authenticated user
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data
   * @param {string|undefined} symbol unified symbol of the market to fetch
   * orders for
   * @param {int} [since] timestamp in ms of the earliest order to fetch, the
   * lookup window is limited to 30 days
   * @param {int} [limit] the maximum number of orders to return
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [order structures]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any> fetchClosedOrders(
      ccxt::any symbol = ccxt::any{}, ccxt::any since = ccxt::any{},
      ccxt::any limit = ccxt::any{}, ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 ccxt::any orderStates = this->safeList2(
                     params, std::string("orderStates"),
                     std::string("order_states"),
                     ccxt::list{std::string("filled"), std::string("cancelled"),
                                std::string("rejected"),
                                std::string("replaced")});
                 ccxt::any requestParams = this->extend(
                     this->omit(params,
                                ccxt::list{std::string("orderStates"),
                                           std::string("order_states")}),
                     ccxt::dict{
                         {std::string("order_states"), orderStates},
                     });
                 return awaitValue(
                     this->fetchOrders(symbol, since, limit, requestParams));
               })
        .share();
  }

  /**
   * @method
   * @name revolutx#parseMyTrade
   * @description parses a trade from the exchange's private trade format
   * @ignore
   * @param {object} trade the raw trade data from the exchange
   * @param {object} [market] the market the trade was executed in
   * @returns {object} a [trade structure]{@link
   * https://docs.ccxt.com/?id=trade-structure}
   */
  virtual ccxt::any parseMyTrade(ccxt::any trade,
                                 ccxt::any market = ccxt::any{}) {
    ccxt::any id = this->safeString(trade, std::string("tid"));
    ccxt::any orderId = this->safeString(trade, std::string("oid"));
    ccxt::any price = this->safeNumber(trade, std::string("p"));
    ccxt::any amount = this->safeNumber(trade, std::string("q"));
    ccxt::any side = this->safeStringLower(trade, std::string("s"));
    ccxt::any timestamp =
        this->safeInteger2(trade, std::string("tdt"), std::string("pdt"));
    ccxt::any isMaker = this->safeBool(trade, std::string("im"), false);
    ccxt::any takerOrMaker =
        (isTrue((isMaker)) ? ccxt::any(std::string("maker"))
                           : ccxt::any(std::string("taker")));
    ccxt::any cost = ccxt::any{};
    if (isTrue(isTrue(!isEqual(price, ccxt::any{})) &&
               isTrue(!isEqual(amount, ccxt::any{})))) {
      cost = multiply(price, amount);
    }
    ccxt::any symbol = this->safeSymbol(ccxt::any{}, market);
    return ccxt::dict{
        {std::string("info"), trade},
        {std::string("id"), id},
        {std::string("order"), orderId},
        {std::string("symbol"), symbol},
        {std::string("side"), side},
        {std::string("type"), ccxt::any{}},
        {std::string("takerOrMaker"), takerOrMaker},
        {std::string("price"), price},
        {std::string("amount"), amount},
        {std::string("cost"), cost},
        {std::string("timestamp"), timestamp},
        {std::string("datetime"), this->iso8601(timestamp)},
        {std::string("fee"), ccxt::any{}},
        {std::string("fees"), ccxt::list{}},
    };
  }

  /**
   * @method
   * @name revolutx#fetchMyTrades
   * @description fetches the trade history for the authenticated user
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-account-data
   * @param {string} symbol unified symbol of the market to fetch trades for
   * @param {int} [since] timestamp in ms of the earliest trade to fetch, the
   * lookup window is limited to 30 days
   * @param {int} [limit] the maximum number of trades to return (1-1900,
   * default 1900)
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {int} [params.until] timestamp in ms of the latest trade to fetch
   * @param {string} [params.cursor] pagination cursor from the previous
   * response
   * @returns {object[]} a list of [trade structures]{@link
   * https://docs.ccxt.com/?id=trade-structure}
   */
  std::shared_future<ccxt::any>
  fetchMyTrades(ccxt::any symbol = ccxt::any{}, ccxt::any since = ccxt::any{},
                ccxt::any limit = ccxt::any{},
                ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 if (isTrue(isEqual(symbol, ccxt::any{}))) {
                   throw ArgumentsRequired(toString(add(
                       this->id,
                       std::string(
                           " fetchMyTrades() requires a symbol parameter"))));
                 }
                 ccxt::any market = this->market(symbol);
                 ccxt::any request = ccxt::dict{
                     {std::string("symbol"),
                      ::getValue(market, std::string("id"))},
                 };
                 ccxt::any thirtyDays = 2592000000;
                 ccxt::any until = this->safeInteger2(
                     params, std::string("until"), std::string("until"));
                 if (isTrue(!isEqual(since, ccxt::any{}))) {
                   ::setValue(request, std::string("start_date"), since);
                 } else if (isTrue(!isEqual(until, ccxt::any{}))) {
                   ::setValue(request, std::string("start_date"),
                              subtract(until, thirtyDays));
                 }
                 if (isTrue(!isEqual(until, ccxt::any{}))) {
                   ::setValue(request, std::string("end_date"), until);
                 } else if (isTrue(!isEqual(since, ccxt::any{}))) {
                   ccxt::any now = this->milliseconds();
                   ccxt::any defaultEnd = add(since, thirtyDays);
                   ::setValue(request, std::string("end_date"),
                              (isTrue((isLessThan(defaultEnd, now)))
                                   ? ccxt::any(defaultEnd)
                                   : ccxt::any(now)));
                 }
                 if (isTrue(!isEqual(limit, ccxt::any{}))) {
                   ::setValue(request, std::string("limit"), limit);
                 }
                 ccxt::any cursor =
                     this->safeString(params, std::string("cursor"));
                 if (isTrue(!isEqual(cursor, ccxt::any{}))) {
                   ::setValue(request, std::string("cursor"), cursor);
                 }
                 ccxt::any response = awaitValue(
                     this->privateGet10TradesPrivateSymbol(this->extend(
                         request, this->omit(params, ccxt::list{std::string(
                                                         "until")}))));
                 //
                 //     {
                 //         "data": [
                 //             { "tdt": 1785309833816, "p": "119900.00", "q":
                 //             "0.00100000",
                 //               "tid": "ad3e8787ab623ba5a1dfea53819be6f9",
                 //               "oid": "2affb2ac-4cf7-4bbf-b7b2-fc1e885bdc2c",
                 //               "s": "buy", "im": false }
                 //         ],
                 //         "metadata": { "timestamp": 1785313433816,
                 //         "next_cursor": "..." }
                 //     }
                 //
                 ccxt::any data = this->safeList(response, std::string("data"),
                                                 ccxt::list{});
                 ccxt::any result = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(data));
                      postFixIncrement(i)) {
                   ccxt::any trade = this->safeDict(data, i, ccxt::dict{});
                   arrayPush(result, this->parseMyTrade(trade, market));
                 }
                 return result;
               })
        .share();
  }

  /**
   * @method
   * @name revolutx#editOrder
   * @description replaces an existing order
   * @see
   * https://developer.revolut.com/docs/api/revolut-x-crypto-exchange#tag-trading
   * @param {string} id the order id (venue order id) to replace
   * @param {string} symbol unified symbol of the market
   * @param {string} type 'limit' or 'market'
   * @param {string} side 'buy' or 'sell'
   * @param {float} amount the new amount
   * @param {float} [price] the new price
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.clientOrderId] a custom client order id (UUID)
   * @param {float} [params.cost] the new order cost in units of the quote
   * currency
   * @param {string} [params.timeInForce] e.g. gtc
   * @param {string[]} [params.executionInstructions] e.g. ['post_only']
   * @returns {object} an [order structure]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  editOrder(ccxt::any id, ccxt::any symbol, ccxt::any type, ccxt::any side,
            ccxt::any amount = ccxt::any{}, ccxt::any price = ccxt::any{},
            ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 // note: the exchange assigns a new venue_order_id on replace —
                 // the returned order carries the new id
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = this->market(symbol);
                 ccxt::any clientOrderId = this->safeString2(
                     params, std::string("clientOrderId"),
                     std::string("client_order_id"), this->uuid());
                 ccxt::any cost = this->safeString2(params, std::string("cost"),
                                                    std::string("quote_size"));
                 ccxt::any timeInForce =
                     this->safeStringLower2(params, std::string("timeInForce"),
                                            std::string("time_in_force"));
                 ccxt::any executionInstructions = this->safeList(
                     params, std::string("executionInstructions"),
                     this->safeList(params,
                                    std::string("execution_instructions")));
                 ccxt::any request = ccxt::dict{
                     {std::string("client_order_id"), clientOrderId},
                     {std::string("venue_order_id"), id},
                 };
                 if (isTrue(!isEqual(cost, ccxt::any{}))) {
                   ::setValue(request, std::string("quote_size"),
                              this->costToPrecision(symbol, cost));
                 } else if (isTrue(!isEqual(amount, ccxt::any{}))) {
                   ::setValue(request, std::string("base_size"),
                              this->amountToPrecision(symbol, amount));
                 }
                 if (isTrue(!isEqual(price, ccxt::any{}))) {
                   ::setValue(request, std::string("price"),
                              this->priceToPrecision(symbol, price));
                 }
                 if (isTrue(!isEqual(timeInForce, ccxt::any{}))) {
                   ::setValue(request, std::string("time_in_force"),
                              timeInForce);
                 }
                 if (isTrue(!isEqual(executionInstructions, ccxt::any{}))) {
                   ::setValue(request, std::string("execution_instructions"),
                              executionInstructions);
                 }
                 ccxt::any response = awaitValue(
                     this->privatePut10OrdersVenueOrderId(this->extend(
                         request,
                         this->omit(
                             params,
                             ccxt::list{
                                 std::string("clientOrderId"),
                                 std::string("client_order_id"),
                                 std::string("cost"), std::string("quote_size"),
                                 std::string("timeInForce"),
                                 std::string("time_in_force"),
                                 std::string("executionInstructions"),
                                 std::string("execution_instructions")}))));
                 //
                 //     {
                 //         "data": [
                 //             { "venue_order_id":
                 //             "7a52e92e-8639-4fe1-abaa-68d3a2d5234b",
                 //             "client_order_id": "...", "state": "new" }
                 //         ]
                 //     }
                 //
                 ccxt::any data = this->safeValue(response, std::string("data"),
                                                  ccxt::dict{});
                 ccxt::any orderData =
                     (isTrue(isArray(data))
                          ? ccxt::any(this->safeDict(data, 0, ccxt::dict{}))
                          : ccxt::any(this->safeDict(
                                response, std::string("data"), ccxt::dict{})));
                 ccxt::any newVenueOrderId =
                     this->safeString(orderData, std::string("venue_order_id"));
                 ccxt::any state =
                     this->safeString(orderData, std::string("state"));
                 ccxt::any order = this->parseOrder(
                     this->extend(orderData,
                                  ccxt::dict{
                                      {std::string("id"), newVenueOrderId},
                                      {std::string("symbol"),
                                       ::getValue(market, std::string("id"))},
                                      {std::string("status"), state},
                                      {std::string("side"), side},
                                      {std::string("type"), type},
                                  }),
                     market);
                 return order;
               })
        .share();
  }

  ccxt::any handleErrors(ccxt::any code, ccxt::any reason, ccxt::any url,
                         ccxt::any method, ccxt::any headers, ccxt::any body,
                         ccxt::any response, ccxt::any requestHeaders,
                         ccxt::any requestBody) override {
    if (isTrue(isGreaterThanOrEqual(code, 400))) {
      if (isTrue(isEqual(response, ccxt::any{}))) {
        return ccxt::any{};
      }
      ccxt::any feedback = add(add(this->id, std::string(" ")), body);
      ccxt::any errorMessage = ccxt::any{};
      if (isTrue(isDictionary(response))) {
        errorMessage = this->safeString2(response, std::string("message"),
                                         std::string("error"));
      }
      if (isTrue(!isEqual(errorMessage, ccxt::any{}))) {
        this->throwBroadlyMatchedException(
            ::getValue(this->exceptions, std::string("broad")), errorMessage,
            feedback);
      }
      return ccxt::any{};
    }
    return ccxt::any{};
  }
  // GENERATED dispatch table - see createDispatchTable in
  // build/cppTranspiler.ts
  virtual ccxt::any callMethod(ccxt::any name, ccxt::any args) override {
    const std::string which =
        ::toString(name).has_value()
            ? ccxt::any_cast<std::string>(::toString(name))
            : std::string();
    const long count =
        ccxt::isList(args)
            ? static_cast<long>(ccxt::any_cast<ccxt::list>(args).size())
            : 0;
    if (which == "describe") {
      if (true)
        return this->describe();
    }
    if (which == "sign") {
      if (count <= 1)
        return this->sign(::getValue(args, 0));
      if (count == 2)
        return this->sign(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2));
      if (count == 4)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3));
      if (count == 5)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3),
                          ::getValue(args, 4));
      if (count >= 6)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3),
                          ::getValue(args, 4), ::getValue(args, 5));
    }
    if (which == "parseMarket") {
      if (true)
        return this->parseMarket(::getValue(args, 0));
    }
    if (which == "fetchMarkets") {
      if (count <= 0)
        return awaitValue(this->fetchMarkets());
      if (count >= 1)
        return awaitValue(this->fetchMarkets(::getValue(args, 0)));
    }
    if (which == "parseCurrency") {
      if (true)
        return this->parseCurrency(::getValue(args, 0));
    }
    if (which == "fetchCurrencies") {
      if (count <= 0)
        return awaitValue(this->fetchCurrencies());
      if (count >= 1)
        return awaitValue(this->fetchCurrencies(::getValue(args, 0)));
    }
    if (which == "parseTicker") {
      if (count <= 1)
        return this->parseTicker(::getValue(args, 0));
      if (count >= 2)
        return this->parseTicker(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "fetchTickers") {
      if (count <= 0)
        return awaitValue(this->fetchTickers());
      if (count == 1)
        return awaitValue(this->fetchTickers(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->fetchTickers(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "fetchTicker") {
      if (count <= 1)
        return awaitValue(this->fetchTicker(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->fetchTicker(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "fetchOrderBook") {
      if (count <= 1)
        return awaitValue(this->fetchOrderBook(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOrderBook(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->fetchOrderBook(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "parseOHLCV") {
      if (count <= 1)
        return this->parseOHLCV(::getValue(args, 0));
      if (count >= 2)
        return this->parseOHLCV(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "fetchOHLCV") {
      if (count <= 1)
        return awaitValue(this->fetchOHLCV(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOHLCV(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(
            this->fetchOHLCV(::getValue(args, 0), ::getValue(args, 1),
                             ::getValue(args, 2), ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->fetchOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "parseTrade") {
      if (count <= 1)
        return this->parseTrade(::getValue(args, 0));
      if (count >= 2)
        return this->parseTrade(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "fetchTrades") {
      if (count <= 1)
        return awaitValue(this->fetchTrades(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchTrades(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchTrades(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchTrades(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "fetchBalance") {
      if (count <= 0)
        return awaitValue(this->fetchBalance());
      if (count >= 1)
        return awaitValue(this->fetchBalance(::getValue(args, 0)));
    }
    if (which == "parseOrderStatus") {
      if (true)
        return this->parseOrderStatus(::getValue(args, 0));
    }
    if (which == "parseOrder") {
      if (count <= 1)
        return this->parseOrder(::getValue(args, 0));
      if (count >= 2)
        return this->parseOrder(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "createOrder") {
      if (count <= 4)
        return awaitValue(
            this->createOrder(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2), ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->createOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count >= 6)
        return awaitValue(this->createOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
    }
    if (which == "cancelOrder") {
      if (count <= 1)
        return awaitValue(this->cancelOrder(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->cancelOrder(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->cancelOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "cancelAllOrders") {
      if (count <= 0)
        return awaitValue(this->cancelAllOrders());
      if (count == 1)
        return awaitValue(this->cancelAllOrders(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->cancelAllOrders(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "fetchOrder") {
      if (count <= 1)
        return awaitValue(this->fetchOrder(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOrder(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->fetchOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "fetchOpenOrders") {
      if (count <= 0)
        return awaitValue(this->fetchOpenOrders());
      if (count == 1)
        return awaitValue(this->fetchOpenOrders(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOpenOrders(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchOpenOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchOpenOrders(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "fetchOrders") {
      if (count <= 0)
        return awaitValue(this->fetchOrders());
      if (count == 1)
        return awaitValue(this->fetchOrders(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOrders(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchOrders(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "fetchClosedOrders") {
      if (count <= 0)
        return awaitValue(this->fetchClosedOrders());
      if (count == 1)
        return awaitValue(this->fetchClosedOrders(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchClosedOrders(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchClosedOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchClosedOrders(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "parseMyTrade") {
      if (count <= 1)
        return this->parseMyTrade(::getValue(args, 0));
      if (count >= 2)
        return this->parseMyTrade(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "fetchMyTrades") {
      if (count <= 0)
        return awaitValue(this->fetchMyTrades());
      if (count == 1)
        return awaitValue(this->fetchMyTrades(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchMyTrades(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchMyTrades(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchMyTrades(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "editOrder") {
      if (count <= 4)
        return awaitValue(
            this->editOrder(::getValue(args, 0), ::getValue(args, 1),
                            ::getValue(args, 2), ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->editOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->editOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count >= 7)
        return awaitValue(this->editOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
    }
    if (which == "handleErrors") {
      if (true)
        return this->handleErrors(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7), ::getValue(args, 8));
    }
    if (which == "cleanRestData") {
      if (true) {
        this->cleanRestData();
        return ccxt::any{};
      }
    }
    if (which == "cleanWsData") {
      if (true) {
        this->cleanWsData();
        return ccxt::any{};
      }
    }
    if (which == "storeByKey") {
      if (true) {
        this->storeByKey(::getValue(args, 0), ::getValue(args, 1),
                         ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "handleDeltas") {
      if (true) {
        this->handleDeltas(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "handleDelta") {
      if (true) {
        this->handleDelta(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "handleDeltasWithKeys") {
      if (count <= 2) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
      if (count == 3) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
        return ccxt::any{};
      }
      if (count == 4) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
        return ccxt::any{};
      }
      if (count >= 5) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3),
                                   ::getValue(args, 4));
        return ccxt::any{};
      }
    }
    if (which == "checkConflictingProxies") {
      if (true) {
        this->checkConflictingProxies(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "setSandboxMode") {
      if (true) {
        this->setSandboxMode(::getValue(args, 0));
        return ccxt::any{};
      }
    }
    if (which == "enableDemoTrading") {
      if (true) {
        this->enableDemoTrading(::getValue(args, 0));
        return ccxt::any{};
      }
    }
    if (which == "afterConstruct") {
      if (true) {
        this->afterConstruct();
        return ccxt::any{};
      }
    }
    if (which == "initRestRateLimiter") {
      if (true) {
        this->initRestRateLimiter();
        return ccxt::any{};
      }
    }
    if (which == "featuresGenerator") {
      if (true) {
        this->featuresGenerator();
        return ccxt::any{};
      }
    }
    if (which == "createNetworksByIdObject") {
      if (true) {
        this->createNetworksByIdObject();
        return ccxt::any{};
      }
    }
    if (which == "throwExactlyMatchedException") {
      if (true) {
        this->throwExactlyMatchedException(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "throwBroadlyMatchedException") {
      if (true) {
        this->throwBroadlyMatchedException(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "checkRequiredArgument") {
      if (count <= 3) {
        this->checkRequiredArgument(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
        return ccxt::any{};
      }
      if (count >= 4) {
        this->checkRequiredArgument(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3));
        return ccxt::any{};
      }
    }
    if (which == "checkRequiredMarginArgument") {
      if (true) {
        this->checkRequiredMarginArgument(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "cleanUnsubscription") {
      if (count <= 3) {
        this->cleanUnsubscription(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2));
        return ccxt::any{};
      }
      if (count >= 4) {
        this->cleanUnsubscription(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3));
        return ccxt::any{};
      }
    }
    if (which == "cleanCache") {
      if (true) {
        this->cleanCache(::getValue(args, 0));
        return ccxt::any{};
      }
    }
    // not defined on this exchange: fall back to the TS parent class
    return Exchange::callMethod(name, args);
  }
};

} // namespace ccxt
