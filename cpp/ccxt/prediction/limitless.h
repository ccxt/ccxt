#pragma once

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#include "../api/prediction/limitless.h"
#include "../base/PredictionExchange.h"
#include "PredictionFactory.h"

namespace ccxt {
namespace prediction {

class limitless : public limitlessApi {
public:
  using limitlessApi::limitlessApi;
  ccxt::any describe() override {
    return this->deepExtend(
        ccxt::PredictionExchange::describe(),
        ccxt::dict{
            {std::string("id"), std::string("limitless")},
            {std::string("name"), std::string("Limitless")},
            {std::string("countries"), ccxt::list{}},
            {std::string("rateLimit"), 200},
            {std::string("certified"), false},
            {std::string("pro"), false},
            {std::string("has"),
             ccxt::dict{
                 {std::string("CORS"), ccxt::any{}},
                 {std::string("spot"), false},
                 {std::string("margin"), false},
                 {std::string("swap"), false},
                 {std::string("future"), false},
                 {std::string("option"), false},
                 {std::string("approve"), true},
                 {std::string("cancelAllOrders"), true},
                 {std::string("cancelOrder"), true},
                 {std::string("cancelOrders"), true},
                 {std::string("createOrder"), true},
                 {std::string("fetchAccounts"), true},
                 {std::string("fetchBalance"), false},
                 {std::string("fetchClosedOrders"), true},
                 {std::string("fetchCurrencies"), false},
                 {std::string("fetchEvent"), true},
                 {std::string("fetchEvents"), true},
                 {std::string("fetchMarkets"), true},
                 {std::string("fetchMyTrades"), true},
                 {std::string("fetchOHLCV"), true},
                 {std::string("fetchOpenOrders"), true},
                 {std::string("fetchOrder"), true},
                 {std::string("fetchOrderBook"), true},
                 {std::string("fetchOrders"), true},
                 {std::string("fetchOrdersByIds"), true},
                 {std::string("fetchPositions"), true},
                 {std::string("fetchTicker"), true},
                 {std::string("fetchTickers"), true},
                 {std::string("fetchTrades"), true},
                 {std::string("prediction"), true},
                 {std::string("redeem"), true},
             }},
            {std::string("timeframes"),
             ccxt::dict{
                 {std::string("1h"), std::string("1h")},
                 {std::string("6h"), std::string("6h")},
                 {std::string("1d"), std::string("1d")},
                 {std::string("1w"), std::string("1w")},
                 {std::string("1M"), std::string("1m")},
             }},
            {std::string("urls"),
             ccxt::dict{
                 {std::string("logo"),
                  std::string("https://github.com/user-attachments/assets/"
                              "bb10c8b4-7b97-49f5-927c-057709dbaf7e")},
                 {std::string("api"),
                  ccxt::dict{
                      {std::string("limitless"),
                       std::string("https://api.limitless.exchange")},
                  }},
                 {std::string("www"),
                  std::string("https://limitless.exchange")},
                 {std::string("doc"),
                  ccxt::list{std::string("https://docs.limitless.exchange")}},
             }},
            {std::string("api"),
             ccxt::dict{
                 {std::string("limitless"),
                  ccxt::dict{
                      {std::string("public"),
                       ccxt::dict{
                           {std::string("get"),
                            ccxt::dict{
                                {std::string("markets/active"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("markets/active/{categoryId}"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("categories"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("markets/{addressOrSlug}"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("markets/categories/count"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("markets/active/slugs"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("markets/search"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("markets/{slug}/orderbook"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("markets/{slug}/historical-price"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("auth/signing-message"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string(
                                     "markets/{addressOrSlug}/oracle-candles"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("markets/{slug}/get-feed-events"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("markets/{slug}/events"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("markets/timeline"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("markets/{slug}/timeline"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("navigation"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("market-pages/by-path"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("market-pages/{id}/markets"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("property-keys"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("property-keys/{id}"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("property-keys/{id}/options"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string(
                                     "portfolio/{account}/traded-volume"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("portfolio/{account}/positions"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("portfolio/{account}/pnl-chart"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                            }},
                       }},
                      {std::string("private"),
                       ccxt::dict{
                           {std::string("get"),
                            ccxt::dict{
                                {std::string("auth/api-keys"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("profiles/partner-accounts"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("markets/{slug}/user-orders"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("portfolio/positions"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("portfolio/trades"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("markets/{slug}/locked-balance"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("profiles/me"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("profiles/{account}"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("portfolio/pnl-chart"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("portfolio/history"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("portfolio/points"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("portfolio/trading/allowance"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("auth/api-tokens/capabilities"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("auth/api-tokens"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("profiles/partner-accounts/"
                                             "{profileId}/allowances"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                            }},
                           {std::string("post"),
                            ccxt::dict{
                                {std::string("auth/logout"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("auth/api-keys"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("auth/login"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("orders"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("orders/cancel"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("orders/cancel-batch"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("orders/batch-cancel"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("orders/status/batch"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("portfolio/redeem"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("portfolio/withdraw"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("portfolio/withdrawal-addresses"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("auth/api-tokens/derive"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("profiles/partner-accounts"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("profiles/partner-accounts/"
                                             "{profileId}/allowances/retry"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                            }},
                           {std::string("delete"),
                            ccxt::dict{
                                {std::string("auth/api-keys"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("orders/{order_id}"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("orders/all/{slug}"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("auth/api-tokens/{tokenId}"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                                {std::string("portfolio/withdrawal-addresses/"
                                             "{address}"),
                                 ccxt::dict{
                                     {std::string("cost"), 1},
                                 }},
                            }},
                       }},
                  }},
             }},
            {std::string("requiredCredentials"),
             ccxt::dict{
                 {std::string("apiKey"), true},
                 {std::string("secret"), true},
                 {std::string("privateKey"), true},
             }},
            {std::string("fees"),
             ccxt::dict{
                 {std::string("trading"),
                  ccxt::dict{
                      {std::string("tierBased"), false},
                      {std::string("percentage"), true},
                      {std::string("maker"), 0.02},
                      {std::string("taker"), 0.02},
                  }},
             }},
            {std::string("options"),
             ccxt::dict{
                 {std::string("defaultFetchMarketsPages"), 5},
                 {std::string("marketsPageSize"), 25},
                 {std::string("usdcDecimals"), 6},
                 {std::string("warnOnCancelAllOrdersWithOutcome"), true},
                 {std::string("zeroAddress"),
                  std::string("0x0000000000000000000000000000000000000000")},
                 {std::string("chainId"), 8453},
                 {std::string("rpcUrl"),
                  std::string("https://mainnet.base.org")},
                 {std::string("collateralAddress"),
                  std::string("0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913")},
                 {std::string("exchangeAddress"),
                  std::string("0x05c748E2f4DcDe0ec9Fa8DDc40DE6b867f923fa5")},
                 {std::string("createMarketBuyOrderRequiresPrice"), true},
             }},
            {std::string("exceptions"),
             ccxt::dict{
                 {std::string("exact"), ccxt::dict{}},
                 {std::string("broad"),
                  ccxt::dict{
                      {std::string("Order not found"),
                       std::string("OrderNotFound")},
                      {std::string("Orders not found"),
                       std::string("OrderNotFound")},
                      {std::string("already canceled"),
                       std::string("OrderNotFound")},
                      {std::string("must be a UUIDv4"),
                       std::string("BadRequest")},
                      {std::string("Invalid signature"),
                       std::string("InvalidOrder")},
                  }},
             }},
        });
  }

  /**
   * @method
   * @name limitless#fetchMarkets
   * @description fetches all active limitless markets paginated and returns one
   * CCXT market per child market, each containing a list of outcome objects
   * (YES/NO)
   * @see
   * https://docs.limitless.exchange/api-reference/markets/get-active-markets
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.query] a single search query string to filter
   * markets by
   * @param {string[]} [params.queries] multiple search query strings
   * (alternative to query)
   * @param {int} [params.limit] max number of markets to collect (defaults to
   * options.fetchMarketsLimit, 1000); caps the pages fetched
   * @returns {object[]} an array of objects representing market data
   */
  std::shared_future<ccxt::any>
  fetchMarkets(ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 ccxt::any queries = this->parseSearchQueries(params);
                 ccxt::any rest =
                     this->omit(params, ccxt::list{std::string("query"),
                                                   std::string("queries"),
                                                   std::string("limit")});
                 // scope the listing: without a search query loadMarkets would
                 // otherwise page through every active limitless market. Cap
                 // the total number of markets collected.
                 ccxt::any maxMarkets = this->safeInteger(
                     params, std::string("limit"),
                     this->safeInteger(this->options,
                                       std::string("fetchMarketsLimit"), 1000));
                 ccxt::any allRaw = ccxt::list{};
                 ccxt::any queriesLength = getArrayLength(queries);
                 if (isTrue(isGreaterThan(queriesLength, 0))) {
                   ccxt::any requestedLimit =
                       this->safeInteger(params, std::string("limit"), 50);
                   // the search endpoint rejects limit > 50 - cap the per-query
                   // request and let maxMarkets bound the overall collection
                   ccxt::any limit = mathMin(requestedLimit, 50);
                   ccxt::any searchRest =
                       this->omit(rest, ccxt::list{std::string("limit")});
                   ccxt::any seen = ccxt::dict{};
                   for (ccxt::any i = 0; isLessThan(i, getArrayLength(queries));
                        postFixIncrement(i)) {
                     ccxt::any q = ::getValue(queries, i);
                     ccxt::any response = awaitValue(
                         this->limitlessPublicGetMarketsSearch(this->extend(
                             ccxt::dict{
                                 {std::string("query"), q},
                                 {std::string("limit"), limit},
                             },
                             searchRest)));
                     ccxt::any found = this->safeList(
                         response, std::string("markets"), ccxt::list{});
                     for (ccxt::any j = 0; isLessThan(j, getArrayLength(found));
                          postFixIncrement(j)) {
                       ccxt::any raw = ::getValue(found, j);
                       ccxt::any slug =
                           this->safeString(raw, std::string("slug"));
                       if (isTrue(isTrue((isTrue(!isEqual(slug, ccxt::any{})) &&
                                          isTrue(!isEqual(slug,
                                                          std::string(""))))) &&
                                  !isTrue((inOp(seen, slug))))) {
                         ::setValue(seen, slug, true);
                         arrayPush(allRaw, raw);
                       }
                     }
                   }
                 } else {
                   ccxt::any page = 1;
                   ccxt::any pageSize = this->safeInteger(
                       this->options, std::string("marketsPageSize"), 25);
                   ccxt::any request = ccxt::dict{
                       {std::string("page"), page},
                       {std::string("limit"), pageSize},
                   };
                   ccxt::any firstPageResponse =
                       awaitValue(this->limitlessPublicGetMarketsActive(
                           this->extend(request, rest)));
                   ccxt::any totalMarketsCount = this->safeInteger(
                       firstPageResponse, std::string("totalMarketsCount"));
                   ccxt::any firstData = this->safeList(
                       firstPageResponse, std::string("data"), ccxt::list{});
                   allRaw = this->arrayConcat(allRaw, firstData);
                   ccxt::any promises = ccxt::list{};
                   ccxt::any cappedPages =
                       mathCeil(divide(maxMarkets, pageSize));
                   ccxt::any knownTotal =
                       (isTrue((!isEqual(totalMarketsCount, ccxt::any{})))
                            ? ccxt::any(totalMarketsCount)
                            : ccxt::any(0));
                   ccxt::any allPages = mathCeil(divide(knownTotal, pageSize));
                   ccxt::any totalPages = mathMin(allPages, cappedPages);
                   for (ccxt::any i = 2; isLessThanOrEqual(i, totalPages);
                        postFixIncrement(i)) {
                     page = i;
                     ::setValue(request, std::string("page"), page);
                     arrayPush(promises, this->limitlessPublicGetMarketsActive(
                                             this->extend(request, rest)));
                   }
                   ccxt::any responses = awaitValue(promiseAll(promises));
                   ccxt::any length = getArrayLength(responses);
                   for (ccxt::any j = 0; isLessThan(j, length);
                        postFixIncrement(j)) {
                     ccxt::any response = this->safeDict(responses, j);
                     ccxt::any data = this->safeList(
                         response, std::string("data"), ccxt::list{});
                     allRaw = this->arrayConcat(allRaw, data);
                   }
                   ccxt::any lastPageResponse =
                       this->safeDict(responses, subtract(length, 1));
                   ccxt::any lastPageData = this->safeList(
                       lastPageResponse, std::string("data"), ccxt::list{});
                   ccxt::any lastPageLength = getArrayLength(lastPageData);
                   ccxt::any allRawLength = getArrayLength(allRaw);
                   if (isTrue(isTrue(isGreaterThanOrEqual(lastPageLength,
                                                          pageSize)) &&
                              isTrue(isLessThan(allRawLength, maxMarkets)))) {
                     while (true) {
                       page = this->sum(page, 1);
                       ::setValue(request, std::string("page"), page);
                       ccxt::any response =
                           awaitValue(this->limitlessPublicGetMarketsActive(
                               this->extend(request, rest)));
                       ccxt::any responseRows = ccxt::list{};
                       if (isTrue(isArray(response))) {
                         responseRows = response;
                       }
                       ccxt::any rawPageMarkets = this->safeList(
                           response, std::string("data"), responseRows);
                       ccxt::any page_markets =
                           (isTrue((!isEqual(rawPageMarkets, ccxt::any{})))
                                ? ccxt::any(rawPageMarkets)
                                : ccxt::any(ccxt::list{}));
                       ccxt::any pageMarketsLength =
                           getArrayLength(page_markets);
                       if (isTrue(isEqual(pageMarketsLength, 0))) {
                         break;
                       }
                       for (ccxt::any i = 0;
                            isLessThan(i, getArrayLength(page_markets));
                            postFixIncrement(i)) {
                         ccxt::any raw = ::getValue(page_markets, i);
                         arrayPush(allRaw, raw);
                       }
                       ccxt::any allRawCount = getArrayLength(allRaw);
                       if (isTrue(isTrue(isLessThan(pageMarketsLength,
                                                    pageSize)) ||
                                  isTrue(isGreaterThanOrEqual(allRawCount,
                                                              maxMarkets)))) {
                         break;
                       }
                     }
                   }
                 }
                 ccxt::any markets = ccxt::list{};
                 ccxt::any eventGroups = ccxt::dict{};
                 // group rows carry their tradeable children in a nested
                 // `markets` list — expand them into regular rows before
                 // parsing (a group row itself has no tokens)
                 ccxt::any expandedRaw = this->expandGroupRows(allRaw);
                 for (ccxt::any i = 0;
                      isLessThan(i, getArrayLength(expandedRaw));
                      postFixIncrement(i)) {
                   ccxt::any raw = ::getValue(expandedRaw, i);
                   ccxt::any groupId = this->safeStringN(
                       raw,
                       ccxt::list{std::string("groupSlug"),
                                  std::string("groupId")},
                       this->safeString(raw, std::string("slug")));
                   ccxt::any eventKey =
                       (isTrue((isTrue(!isEqual(groupId, ccxt::any{})) &&
                                isTrue(!isEqual(groupId, std::string("")))))
                            ? ccxt::any(this->shortenSlug(groupId))
                            : ccxt::any(ccxt::any{}));
                   ccxt::any m = this->parseMarket(raw);
                   arrayPush(markets, m);
                   if (isTrue(isTrue((!isEqual(eventKey, ccxt::any{}))) &&
                              isTrue((!isEqual(eventKey, std::string("")))))) {
                     if (!isTrue((inOp(eventGroups, eventKey)))) {
                       ::setValue(eventGroups, eventKey,
                                  ccxt::dict{
                                      {std::string("groupId"), groupId},
                                      {std::string("title"),
                                       this->safeString2(
                                           raw, std::string("groupTitle"),
                                           std::string("title"), groupId)},
                                      {std::string("raw"), raw},
                                      {std::string("markets"), ccxt::list{}},
                                  });
                     }
                     ccxt::any eventGroup = ::getValue(eventGroups, eventKey);
                     // push through a local and write the slice back — the go
                     // transpiler's AppendToArray reassigns only a local copy
                     // of a map-stored array, so a direct push on
                     // eventGroup['markets'] loses the element in go
                     ccxt::any groupMarkets =
                         ::getValue(eventGroup, std::string("markets"));
                     arrayPush(groupMarkets, m);
                     ::setValue(eventGroup, std::string("markets"),
                                groupMarkets);
                   }
                 }
                 ccxt::any eventsDict = ccxt::dict{};
                 ccxt::any eventKeys = getObjectKeys(eventGroups);
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(eventKeys));
                      postFixIncrement(i)) {
                   ccxt::any eventKey = ::getValue(eventKeys, i);
                   ccxt::any g = ::getValue(eventGroups, eventKey);
                   ::setValue(eventsDict, eventKey, this->parseEvent(g));
                 }
                 this->events = eventsDict;
                 ccxt::any marketsLength = getArrayLength(markets);
                 if (isTrue(isGreaterThan(marketsLength, maxMarkets))) {
                   return this->arraySlice(markets, 0, maxMarkets);
                 }
                 return markets;
               })
        .share();
  }

  ccxt::any parseMarket(ccxt::any raw) override {
    //
    // {
    //   "id":"36814",
    //   "automationType":"manual",
    //   "conditionId":"0x11287d02d8067ff3d3d8bd21b212ebcfdc20b638f7f6440e4115f649e6b57015",
    //   "negRiskRequestId":null,
    //   "description":"<p>This market will resolve to “Yes” if Donald Trump
    //   resigns or is removed as President or otherwise ceases to be the
    //   President of the United States for any period of time by December 31,
    //   2026, 11:59 PM ET. Otherwise, this market will resolve to
    //   “No”.</p><p>An announcement of Donald Trump's resignation/removal
    //   before this market's end date will immediately resolve this market to
    //   \\""Yes\\"", regardless of when the announced resignation/removal goes
    //   into effect.</p><p>Only permanent removal from office will qualify.
    //   Temporary removal (e.g. temporary invocation of the 25th Amendment
    //   under Section 3 or a Section 4 invocation not sustained by both Houses
    //   of Congress) or impeachment without removal will not count.</p><p>A
    //   sustained invocation of the Twenty-Fifth Amendment, Section 4 (i.e., if
    //   both Houses of Congress, by two-thirds vote, uphold the Vice President
    //   and Cabinet’s determination of presidential inability) will qualify for
    //   a \\""Yes\\"" resolution.</p><p>The resolution source for this market
    //   will be a consensus of credible reporting.</p>", "collateralToken":{
    //       "address":"0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913",
    //       "decimals":"6",
    //       "symbol":"USDC"
    //   },
    //   "title":"💎 Trump out as President before 2027?",
    //   "proxyTitle":null,
    //   "expirationDate":"Jan 1, 2027",
    //   "expirationTimestamp":"1798779540000",
    //   "createdAt":"2026-01-20T18:17:48.298Z",
    //   "updatedAt":"2026-02-24T17:00:11.833Z",
    //   "categories":[
    //       "Politics"
    //   ],
    //   "status":"FUNDED",
    //   "expired":false,
    //   "hidden":false,
    //   "creator":{
    //       "name":"Limitless",
    //       "imageURI":"https://limitless.exchange/assets/images/logo.svg",
    //       "link":"https://x.com/trylimitless"
    //   },
    //   "tags":[
    //       "Limitless"
    //   ],
    //   "volume":"290091252",
    //   "volumeFormatted":"290.091252",
    //   "tokens":{
    //       "yes":"56154308742753982686710750162015444986563701968079760676518531584453506363044",
    //       "no":"32572248812801208874557774576516861470423415416073401354576860825663488568217"
    //   },
    //   "prices":[
    //       0.164,
    //       0.836
    //   ],
    //   "isOther":false,
    //   "isRewardable":true,
    //   "slug":"trump-out-as-president-before-2027-1768933068297",
    //   "tradeType":"clob",
    //   "venue":{
    //       "exchange":"0x05c748E2f4DcDe0ec9Fa8DDc40DE6b867f923fa5",
    //       "adapter":null
    //   },
    //   "marketType":"single",
    //   "priorityIndex":"0",
    //   "winningOutcomeIndex":null,
    //   "metadata":{
    //       "fee":true,
    //       "isBannered":false,
    //       "isPolyArbitrage":true
    //   },
    //   "trends":{
    //       "hourly":{
    //           "value":"3",
    //           "rank":"395"
    //       }
    //   },
    //   "settings":{
    //       "minSize":"100000000",
    //       "maxSpread":"0.035",
    //       "dailyReward":"5",
    //       "rewardsEpoch":"0.003472222222222222",
    //       "c":"3",
    //       "rebateRate":"0"
    //   },
    //   "imageUrl":"https://cdn.limitless.exchange/markets-logo/36814/9daba01d-6bcd-4a2c-9187-f4264b7191da.png",
    //   "logo":"https://cdn.limitless.exchange/markets-logo/36814/9daba01d-6bcd-4a2c-9187-f4264b7191da.png"
    // }
    //
    ccxt::any slug = this->safeString(raw, std::string("slug"));
    ccxt::any address = this->safeString(raw, std::string("address"), slug);
    // groupSlug is stamped by expandGroupRows on children of a group row —
    // prefer it over the child's own numeric groupId so symbols/handles derive
    // from the readable slug
    ccxt::any groupId = this->safeStringN(
        raw, ccxt::list{std::string("groupSlug"), std::string("groupId")},
        slug);
    // CTF condition id — needed to redeem a resolved winning position
    ccxt::any conditionId = this->safeString(raw, std::string("conditionId"));
    ccxt::any tokens =
        this->safeValue(raw, std::string("tokens"), ccxt::dict{});
    // the listing exposes `expired` + `status` (FUNDED/RESOLVED/…), not an
    // `active` flag; a market is tradeable only while it is FUNDED and not yet
    // expired
    ccxt::any isExpired = this->safeBool(raw, std::string("expired"), false);
    ccxt::any marketStatus = this->safeString(raw, std::string("status"));
    ccxt::any active = isTrue((!isEqual(isExpired, true))) &&
                       isTrue((isEqual(marketStatus, std::string("FUNDED"))));
    // expiry is a ms timestamp string (`expirationTimestamp`);
    // `deadline`/`expiresAt` do not exist
    ccxt::any expiryTimestamp =
        this->safeInteger(raw, std::string("expirationTimestamp"));
    // limitless reports lifetime volume (human-readable in `volumeFormatted`),
    // not a 24h figure
    ccxt::any volume24h = this->safeNumber(raw, std::string("volumeFormatted"));
    // resolution: winningOutcomeIndex is null until the market resolves, then
    // the winning outcome index
    ccxt::any winningOutcomeIndex =
        this->safeInteger(raw, std::string("winningOutcomeIndex"));
    ccxt::any marketResolved = (!isEqual(winningOutcomeIndex, ccxt::any{}));
    ccxt::any resolvedOutcome = ccxt::any{};
    ccxt::any marketSymbol = this->slugToMarketSymbol(groupId, slug);
    // amount precision comes from the collateral token decimals (USDC, 6);
    // limitless does not expose a price tick, so 0.001 is the platform
    // convention
    ccxt::any collateralToken =
        this->safeDict(raw, std::string("collateralToken"), ccxt::dict{});
    ccxt::any collateralDecimals = this->safeInteger(
        collateralToken, std::string("decimals"),
        this->safeInteger(this->options, std::string("usdcDecimals"), 6));
    ccxt::any precision = ccxt::dict{
        {std::string("amount"), this->parseNumber(this->parsePrecision(
                                    this->numberToString(collateralDecimals)))},
        {std::string("price"), 0.001},
    };
    ccxt::any outcomes = ccxt::list{};
    ccxt::any tokenEntries = getObjectKeys(tokens);
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(tokenEntries));
         postFixIncrement(i)) {
      ccxt::any outcomeLabel = ::getValue(tokenEntries, i);
      ccxt::any tokenData = ::getValue(tokens, outcomeLabel);
      ccxt::any tokenId = tokenData;
      ccxt::any outcomeHandle =
          this->slugToOutcomeSymbol(groupId, slug, outcomeLabel);
      // winningOutcomeIndex indexes the API's canonical outcome order (yes=0,
      // no=1 for limitless's binary yes/no markets). Object.keys iteration
      // order is NOT stable across languages (Go randomizes map iteration), so
      // map the leg to its canonical index by label rather than by loop
      // position — otherwise Go/Java flag the wrong winner
      ccxt::any labelLower = toLowerCase(outcomeLabel);
      ccxt::any legIndex = i;
      if (isTrue(isEqual(labelLower, std::string("yes")))) {
        legIndex = 0;
      } else if (isTrue(isEqual(labelLower, std::string("no")))) {
        legIndex = 1;
      }
      ccxt::any winnerRaw = ccxt::any{};
      ccxt::any settleFractionRaw = ccxt::any{};
      if (isTrue(marketResolved)) {
        winnerRaw = (isEqual(legIndex, winningOutcomeIndex));
        settleFractionRaw = (isTrue(winnerRaw) ? ccxt::any(1) : ccxt::any(0));
        if (isTrue(winnerRaw)) {
          resolvedOutcome = outcomeHandle;
        }
      }
      // effectively-final copies for the object literal below (Java cannot
      // capture a reassigned local into the anonymous inner class it emits for
      // a map literal)
      ccxt::any winner = winnerRaw;
      ccxt::any settleFraction = settleFractionRaw;
      arrayPush(outcomes, ccxt::dict{
                              {std::string("outcome"), outcomeHandle},
                              {std::string("outcomeId"), tokenId},
                              {std::string("market"), marketSymbol},
                              {std::string("label"), outcomeLabel},
                              {std::string("active"), active},
                              {std::string("winner"), winner},
                              {std::string("settleFraction"), settleFraction},
                              {std::string("precision"), precision},
                              {std::string("info"),
                               ccxt::dict{
                                   {std::string("slug"), slug},
                                   {std::string("address"), address},
                                   {std::string("conditionId"), conditionId},
                                   {std::string("outcomeLabel"), outcomeLabel},
                                   {std::string("tokenId"), tokenId},
                                   {std::string("volume24h"), volume24h},
                               }},
                          });
    }
    ccxt::any outcomesLength = getArrayLength(outcomes);
    // effectively-final copy for the market object literal below (reassigned in
    // the loop)
    ccxt::any marketResolvedOutcome = resolvedOutcome;
    return ccxt::dict{
        {std::string("id"), slug},
        {std::string("market"), marketSymbol},
        {std::string("marketType"), (isTrue((isGreaterThan(outcomesLength, 2)))
                                         ? ccxt::any(std::string("categorical"))
                                         : ccxt::any(std::string("binary")))},
        {std::string("executionModel"), std::string("clob")},
        {std::string("collateral"), std::string("USDC")},
        {std::string("base"), slug},
        {std::string("quote"), std::string("USDC")},
        {std::string("settle"), ccxt::any{}},
        {std::string("baseId"), slug},
        {std::string("quoteId"), std::string("USDC")},
        {std::string("settleId"), ccxt::any{}},
        {std::string("type"), std::string("prediction")},
        {std::string("spot"), false},
        {std::string("margin"), false},
        {std::string("swap"), false},
        {std::string("future"), false},
        {std::string("option"), false},
        {std::string("prediction"), true},
        {std::string("active"), active},
        {std::string("resolved"), marketResolved},
        {std::string("resolvedOutcome"), marketResolvedOutcome},
        {std::string("contract"), false},
        {std::string("linear"), ccxt::any{}},
        {std::string("inverse"), ccxt::any{}},
        {std::string("contractSize"), ccxt::any{}},
        {std::string("expiry"), expiryTimestamp},
        {std::string("expiryDatetime"), this->iso8601(expiryTimestamp)},
        {std::string("strike"), ccxt::any{}},
        {std::string("optionType"), ccxt::any{}},
        {std::string("taker"), 0.02},
        {std::string("maker"), 0.02},
        {std::string("percentage"), true},
        {std::string("tierBased"), false},
        {std::string("feeSide"), std::string("get")},
        {std::string("precision"), precision},
        {std::string("limits"),
         ccxt::dict{
             {std::string("leverage"),
              ccxt::dict{
                  {std::string("min"), 1},
                  {std::string("max"), 1},
              }},
             {std::string("amount"),
              ccxt::dict{
                  {std::string("min"), 0},
                  {std::string("max"), ccxt::any{}},
              }},
             {std::string("price"),
              ccxt::dict{
                  {std::string("min"), 0.001},
                  {std::string("max"), 0.999},
              }},
             {std::string("cost"),
              ccxt::dict{
                  {std::string("min"), ccxt::any{}},
                  {std::string("max"), ccxt::any{}},
              }},
         }},
        {std::string("outcomes"), outcomes},
        {std::string("info"),
         this->extend(raw,
                      ccxt::dict{
                          {std::string("slug"), slug},
                          {std::string("address"), address},
                          {std::string("volume24h"), volume24h},
                      })},
        {std::string("created"), ccxt::any{}},
    };
  }

  /**
   * @method
   * @name limitless#fetchEvent
   * @description fetches a single prediction-market event by its market slug or
   * address
   * @see https://docs.limitless.exchange/api-reference/markets/get-market
   * @param {string} id the market slug or address
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [prediction event
   * structure](https://docs.ccxt.com/#/?id=prediction-event-structure)
   */
  std::shared_future<ccxt::any>
  fetchEvent(ccxt::any id, ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 ccxt::any request = ccxt::dict{
                     {std::string("addressOrSlug"), id},
                 };
                 ccxt::any response =
                     awaitValue(this->limitlessPublicGetMarketsAddressOrSlug(
                         this->extend(request, params)));
                 // a group response carries its tradeable children in `markets`
                 // (each a full market row with tokens) — expandGroupRows
                 // unwraps them; a single market has no nested markets and
                 // wraps as its own one-market event, which parseEvent's loop
                 // then parses
                 ccxt::any rows = this->expandGroupRows(ccxt::list{response});
                 ccxt::any wrapped =
                     this->extend(response, ccxt::dict{
                                                {std::string("markets"), rows},
                                            });
                 ccxt::any event = this->parseEvent(wrapped);
                 this->indexEventOutcomes(event);
                 return event;
               })
        .share();
  }

  /**
   * @ignore
   * @method
   * @name limitless#expandGroupRows
   * @description flattens listing rows — a 'group' row carries no tradeable
   * tokens itself and its children (each a full market row with tokens) appear
   * nowhere else in the listing, so each group row is replaced by its nested
   * markets, tagged with the group's slug and title (the child's own groupId is
   * an opaque numeric venue id) so they regroup under one readable event
   * @param {object[]} rawRows raw listing rows, single-market and group rows
   * mixed
   * @returns {object[]} raw single-market rows only
   */
  virtual ccxt::any expandGroupRows(ccxt::any rawRows) {
    ccxt::any result = ccxt::list{};
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(rawRows));
         postFixIncrement(i)) {
      ccxt::any raw = ::getValue(rawRows, i);
      ccxt::any rowType = this->safeString(raw, std::string("marketType"));
      ccxt::any nestedMarkets = this->safeList(raw, std::string("markets"));
      if (isTrue(isTrue((isEqual(rowType, std::string("group")))) &&
                 isTrue((!isEqual(nestedMarkets, ccxt::any{}))))) {
        ccxt::any groupSlug = this->safeString(raw, std::string("slug"));
        ccxt::any groupTitle =
            this->safeString(raw, std::string("title"), groupSlug);
        ccxt::any nestedMarketsLength = getArrayLength(nestedMarkets);
        for (ccxt::any j = 0; isLessThan(j, nestedMarketsLength);
             postFixIncrement(j)) {
          // extend copies — the raw child stays untouched
          ccxt::any tagged =
              this->extend(::getValue(nestedMarkets, j),
                           ccxt::dict{
                               {std::string("groupSlug"), groupSlug},
                               {std::string("groupTitle"), groupTitle},
                           });
          arrayPush(result, tagged);
        }
      } else {
        arrayPush(result, raw);
      }
    }
    return result;
  }

  virtual ccxt::any parseEvent(ccxt::any event) {
    // {
    //    "groupId":"trump-out-as-president-before-2027-1768933068297",
    //    "title":"💎 Trump out as President before 2027?",
    //    "raw":{
    //       "id":"36814",
    //       "automationType":"manual",
    //       "conditionId":"0x11287d02d8067ff3d3d8bd21b212ebcfdc20b638f7f6440e4115f649e6b57015",
    //       "negRiskRequestId":null,
    //       "description":"<p>This market will resolve to “Yes” if Donald Trump
    //       resigns or is removed as President or otherwise ceases to be the
    //       President of the United States for any period of time by December
    //       31, 2026, 11:59 PM ET. Otherwise, this market will resolve to
    //       “No”.</p><p>An announcement of Donald Trump's resignation/removal
    //       before this market's end date will immediately resolve this market
    //       to \\""Yes\\"", regardless of when the announced
    //       resignation/removal goes into effect.</p><p>Only permanent removal
    //       from office will qualify. Temporary removal (e.g. temporary
    //       invocation of the 25th Amendment under Section 3 or a Section 4
    //       invocation not sustained by both Houses of Congress) or impeachment
    //       without removal will not count.</p><p>A sustained invocation of the
    //       Twenty-Fifth Amendment, Section 4 (i.e., if both Houses of
    //       Congress, by two-thirds vote, uphold the Vice President and
    //       Cabinet’s determination of presidential inability) will qualify for
    //       a \\""Yes\\"" resolution.</p><p>The resolution source for this
    //       market will be a consensus of credible reporting.</p>",
    //       "collateralToken":{
    //          "address":"0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913",
    //          "decimals":"6",
    //          "symbol":"USDC"
    //       },
    //       "title":"💎 Trump out as President before 2027?",
    //       "proxyTitle":null,
    //       "expirationDate":"Jan 1, 2027",
    //       "expirationTimestamp":"1798779540000",
    //       "createdAt":"2026-01-20T18:17:48.298Z",
    //       "updatedAt":"2026-02-24T17:00:11.833Z",
    //       "categories":[
    //          "Politics"
    //       ],
    //       "status":"FUNDED",
    //       "expired":false,
    //       "hidden":false,
    //       "creator":{
    //          "name":"Limitless",
    //          "imageURI":"https://limitless.exchange/assets/images/logo.svg",
    //          "link":"https://x.com/trylimitless"
    //       },
    //       "tags":[
    //          "Limitless"
    //       ],
    //       "volume":"290091252",
    //       "volumeFormatted":"290.091252",
    //       "tokens":{
    //          "yes":"56154308742753982686710750162015444986563701968079760676518531584453506363044",
    //          "no":"32572248812801208874557774576516861470423415416073401354576860825663488568217"
    //       },
    //       "prices":[
    //          0.164,
    //          0.836
    //       ],
    //       "isOther":false,
    //       "isRewardable":true,
    //       "slug":"trump-out-as-president-before-2027-1768933068297",
    //       "tradeType":"clob",
    //       "venue":{
    //          "exchange":"0x05c748E2f4DcDe0ec9Fa8DDc40DE6b867f923fa5",
    //          "adapter":null
    //       },
    //       "marketType":"single",
    //       "priorityIndex":"0",
    //       "winningOutcomeIndex":null,
    //       "metadata":{
    //          "fee":true,
    //          "isBannered":false,
    //          "isPolyArbitrage":true
    //       },
    //       "trends":{
    //          "hourly":{
    //             "value":"3",
    //             "rank":"395"
    //          }
    //       },
    //       "settings":{
    //          "minSize":"100000000",
    //          "maxSpread":"0.035",
    //          "dailyReward":"5",
    //          "rewardsEpoch":"0.003472222222222222",
    //          "c":"3",
    //          "rebateRate":"0"
    //       },
    //       "imageUrl":"https://cdn.limitless.exchange/markets-logo/36814/9daba01d-6bcd-4a2c-9187-f4264b7191da.png",
    //       "logo":"https://cdn.limitless.exchange/markets-logo/36814/9daba01d-6bcd-4a2c-9187-f4264b7191da.png"
    //    },
    //    "markets":[
    //       {
    //          "id":"trump-out-as-president-before-2027-1768933068297",
    //          "symbol":"TRUMP_OUT_PRESIDENT_2027_1768933068297",
    //          "base":"trump-out-as-president-before-2027-1768933068297",
    //          "quote":"USDC",
    //          "baseId":"trump-out-as-president-before-2027-1768933068297",
    //          "quoteId":"USDC",
    //          "type":"prediction",
    //          "spot":false,
    //          "margin":false,
    //          "swap":false,
    //          "future":false,
    //          "option":false,
    //          "prediction":true,
    //          "active":true,
    //          "contract":false,
    //          "taker":0.02,
    //          "maker":0.02,
    //          "percentage":true,
    //          "tierBased":false,
    //          "feeSide":"get",
    //          "precision":{
    //             "amount":0.000001,
    //             "price":0.001
    //          },
    //          "limits":{
    //             "leverage":{
    //                "min":1,
    //                "max":1
    //             },
    //             "amount":{
    //                "min":0
    //             },
    //             "price":{
    //                "min":0.001,
    //                "max":0.999
    //             },
    //             "cost":{
    //             }
    //          },
    //          "outcomes":[
    //             {
    //                "id":"trump-out-as-president-before-2027-1768933068297/yes",
    //                "symbol":"TRUMP_OUT_PRESIDENT_2027_1768933068297:YES",
    //                "marketSymbol":"TRUMP_OUT_PRESIDENT_2027_1768933068297",
    //                "label":"yes",
    //                "active":true,
    //                "info":{
    //                   "slug":"trump-out-as-president-before-2027-1768933068297",
    //                   "address":"trump-out-as-president-before-2027-1768933068297",
    //                   "outcomeLabel":"yes",
    //                   "tokenId":"trump-out-as-president-before-2027-1768933068297/yes"
    //                }
    //             },
    //             {
    //                "id":"trump-out-as-president-before-2027-1768933068297/no",
    //                "symbol":"TRUMP_OUT_PRESIDENT_2027_1768933068297:NO",
    //                "marketSymbol":"TRUMP_OUT_PRESIDENT_2027_1768933068297",
    //                "label":"no",
    //                "active":true,
    //                "info":{
    //                   "slug":"trump-out-as-president-before-2027-1768933068297",
    //                   "address":"trump-out-as-president-before-2027-1768933068297",
    //                   "outcomeLabel":"no",
    //                   "tokenId":"trump-out-as-president-before-2027-1768933068297/no"
    //                }
    //             }
    //          ],
    //          "info":{
    //             "id":"36814",
    //             "automationType":"manual",
    //             "conditionId":"0x11287d02d8067ff3d3d8bd21b212ebcfdc20b638f7f6440e4115f649e6b57015",
    //             "negRiskRequestId":null,
    //             "description":"<p>This market will resolve to “Yes” if Donald
    //             Trump resigns or is removed as President or otherwise ceases
    //             to be the President of the United States for any period of
    //             time by December 31, 2026, 11:59 PM ET. Otherwise, this
    //             market will resolve to “No”.</p><p>An announcement of Donald
    //             Trump's resignation/removal before this market's end date
    //             will immediately resolve this market to \\""Yes\\"",
    //             regardless of when the announced resignation/removal goes
    //             into effect.</p><p>Only permanent removal from office will
    //             qualify. Temporary removal (e.g. temporary invocation of the
    //             25th Amendment under Section 3 or a Section 4 invocation not
    //             sustained by both Houses of Congress) or impeachment without
    //             removal will not count.</p><p>A sustained invocation of the
    //             Twenty-Fifth Amendment, Section 4 (i.e., if both Houses of
    //             Congress, by two-thirds vote, uphold the Vice President and
    //             Cabinet’s determination of presidential inability) will
    //             qualify for a \\""Yes\\"" resolution.</p><p>The resolution
    //             source for this market will be a consensus of credible
    //             reporting.</p>", "collateralToken":{
    //                "address":"0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913",
    //                "decimals":"6",
    //                "symbol":"USDC"
    //             },
    //             "title":"💎 Trump out as President before 2027?",
    //             "proxyTitle":null,
    //             "expirationDate":"Jan 1, 2027",
    //             "expirationTimestamp":"1798779540000",
    //             "createdAt":"2026-01-20T18:17:48.298Z",
    //             "updatedAt":"2026-02-24T17:00:11.833Z",
    //             "categories":[
    //                "Politics"
    //             ],
    //             "status":"FUNDED",
    //             "expired":false,
    //             "hidden":false,
    //             "creator":{
    //                "name":"Limitless",
    //                "imageURI":"https://limitless.exchange/assets/images/logo.svg",
    //                "link":"https://x.com/trylimitless"
    //             },
    //             "tags":[
    //                "Limitless"
    //             ],
    //             "volume":"290091252",
    //             "volumeFormatted":"290.091252",
    //             "tokens":{
    //                "yes":"56154308742753982686710750162015444986563701968079760676518531584453506363044",
    //                "no":"32572248812801208874557774576516861470423415416073401354576860825663488568217"
    //             },
    //             "prices":[
    //                0.164,
    //                0.836
    //             ],
    //             "isOther":false,
    //             "isRewardable":true,
    //             "slug":"trump-out-as-president-before-2027-1768933068297",
    //             "tradeType":"clob",
    //             "venue":{
    //                "exchange":"0x05c748E2f4DcDe0ec9Fa8DDc40DE6b867f923fa5",
    //                "adapter":null
    //             },
    //             "marketType":"single",
    //             "priorityIndex":"0",
    //             "winningOutcomeIndex":null,
    //             "metadata":{
    //                "fee":true,
    //                "isBannered":false,
    //                "isPolyArbitrage":true
    //             },
    //             "trends":{
    //                "hourly":{
    //                   "value":"3",
    //                   "rank":"395"
    //                }
    //             },
    //             "settings":{
    //                "minSize":"100000000",
    //                "maxSpread":"0.035",
    //                "dailyReward":"5",
    //                "rewardsEpoch":"0.003472222222222222",
    //                "c":"3",
    //                "rebateRate":"0"
    //             },
    //             "imageUrl":"https://cdn.limitless.exchange/markets-logo/36814/9daba01d-6bcd-4a2c-9187-f4264b7191da.png",
    //             "logo":"https://cdn.limitless.exchange/markets-logo/36814/9daba01d-6bcd-4a2c-9187-f4264b7191da.png",
    //             "address":"trump-out-as-president-before-2027-1768933068297"
    //          }
    //       }
    //    ]
    // }
    ccxt::any groupId = this->safeString(
        event, std::string("address"),
        this->safeString(event, std::string("groupId"),
                         this->safeString(event, std::string("slug"))));
    ccxt::any endDate =
        this->safeString(event, std::string("deadline"),
                         this->safeString(event, std::string("expiresAt")));
    ccxt::any title = this->safeString(event, std::string("title"), groupId);
    ccxt::any hasGroupId = isTrue((!isEqual(groupId, ccxt::any{}))) &&
                           isTrue((!isEqual(groupId, std::string(""))));
    ccxt::any eventSlug =
        (isTrue(hasGroupId) ? ccxt::any(this->shortenSlug(groupId))
                            : ccxt::any(ccxt::any{}));
    ccxt::any hasEndDate = isTrue((!isEqual(endDate, ccxt::any{}))) &&
                           isTrue((!isEqual(endDate, std::string(""))));
    ccxt::any endTimestamp =
        (isTrue(hasEndDate) ? ccxt::any(this->parse8601(endDate))
                            : ccxt::any(ccxt::any{}));
    ccxt::any markets = ccxt::list{};
    ccxt::any rawMarkets =
        this->safeList(event, std::string("markets"), ccxt::list{});
    // aggregate 24h volume across the markets so sort by volume works
    ccxt::any totalVolume = 0;
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(rawMarkets));
         postFixIncrement(i)) {
      ccxt::any rawMarket = ::getValue(rawMarkets, i);
      // an already-parsed ccxt market row carries the unified 'market' handle +
      // outcomes with 'symbol' kept as a legacy fallback — don't run it through
      // parseMarket again
      ccxt::any marketSymbol = this->safeString2(
          rawMarket, std::string("market"), std::string("symbol"));
      ccxt::any marketOutcomes =
          this->safeList(rawMarket, std::string("outcomes"));
      if (isTrue(isTrue(!isEqual(marketSymbol, ccxt::any{})) &&
                 isTrue(!isEqual(marketOutcomes, ccxt::any{})))) {
        arrayPush(markets, rawMarket);
      } else {
        arrayPush(markets, this->parseMarket(rawMarket));
      }
      ccxt::any marketInfo =
          this->safeDict(rawMarket, std::string("info"), rawMarket);
      // use volumeFormatted (human units) — the raw `volume` is 1e-6
      // fixed-point, which would make the event volume 1,000,000x too big and
      // useless for cross-venue ranking
      totalVolume = this->sum(
          totalVolume,
          this->safeNumber(marketInfo, std::string("volumeFormatted"), 0));
    }
    return this->extend(ccxt::dict{
        {std::string("id"), groupId},
        {std::string("slug"), groupId},
        {std::string("event"), eventSlug},
        {std::string("title"), title},
        {std::string("description"),
         this->safeString(event, std::string("description"))},
        {std::string("markets"), markets},
        {std::string("volume"), totalVolume},
        {std::string("liquidity"),
         this->safeNumber(event, std::string("liquidity"))},
        {std::string("url"), this->safeString(event, std::string("url"))},
        {std::string("image"),
         this->safeString(event, std::string("imageUrl"),
                          this->safeString(event, std::string("image")))},
        {std::string("active"),
         this->safeBool(event, std::string("active"), true)},
        {std::string("resolved"),
         this->safeBool(event, std::string("resolved"), false)},
        {std::string("category"),
         this->safeString(event, std::string("category"))},
        {std::string("tags"), this->safeList(event, std::string("tags"))},
        {std::string("created"),
         this->parse8601(this->safeString(event, std::string("createdAt")))},
        {std::string("createdDatetime"),
         this->safeString(event, std::string("createdAt"))},
        {std::string("end"), endTimestamp},
        {std::string("endDatetime"), endDate},
        {std::string("lastUpdatedAt"),
         this->parse8601(this->safeString(event, std::string("updatedAt")))},
        {std::string("resolutionSource"),
         this->safeString(event, std::string("resolutionSource"))},
        {std::string("info"), event},
    });
  }

  /**
   * @method
   * @name limitless#fetchTicker
   * @description fetches the current price and best bid/ask for a single
   * outcome token, combining the market detail and order book endpoints
   * @see https://docs.limitless.exchange/api-reference/markets/get-market
   * @see https://docs.limitless.exchange/api-reference/trading/orderbook
   * @param {string} outcome unified outcome like TRUMP_OUT_PRESIDENT_2027:YES
   * or an outcome token id
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [prediction ticker
   * structure](https://docs.ccxt.com/#/?id=prediction-ticker-structure)
   */
  std::shared_future<ccxt::any>
  fetchTicker(ccxt::any outcome, ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 awaitValue(this->loadOutcome(outcome));
                 ccxt::any outcomeObj = this->outcome(outcome);
                 ccxt::any slug = this->safeString(
                     ::getValue(outcomeObj, std::string("info")),
                     std::string("slug"));
                 ccxt::any request = ccxt::dict{
                     {std::string("addressOrSlug"), slug},
                 };
                 ccxt::any promises = ccxt::list{
                     this->limitlessPublicGetMarketsAddressOrSlug(
                         this->extend(request, params)),
                     this->limitlessPublicGetMarketsSlugOrderbook(ccxt::dict{
                         {std::string("slug"), slug},
                     })};
                 ccxt::any responses = awaitValue(promiseAll(promises));
                 ccxt::any response = ::getValue(responses, 0);
                 //
                 //     {
                 //         "id": "36814",
                 //         "automationType": "manual",
                 //         "conditionId":
                 //         "0x11287d02d8067ff3d3d8bd21b212ebcfdc20b638f7f6440e4115f649e6b57015",
                 //         "negRiskRequestId": null,
                 //         "description": "<p>This market will resolve to Yes
                 //         if Donald Trump resigns or is removed as President
                 //         or otherwise ceases to be the President of the
                 //         United States for any period of time by December 31,
                 //         2026, 11:59 PM ET. Otherwise, this market will
                 //         resolve to No.</p><p>An announcement of Donald
                 //         Trump's resignation/removal before this market's end
                 //         date will immediately resolve this market to Yes,
                 //         regardless of when the announced resignation/removal
                 //         goes into effect.</p><p>Only permanent removal from
                 //         office will qualify. Temporary removal (e.g.
                 //         temporary invocation of the 25th Amendment under
                 //         Section 3 or a Section 4 invocation not sustained by
                 //         both Houses of Congress) or impeachment without
                 //         removal will not count.</p><p>A sustained invocation
                 //         of the Twenty-Fifth Amendment, Section 4 (i.e., if
                 //         both Houses of Congress, by two-thirds vote, uphold
                 //         the Vice President and Cabinets determination of
                 //         presidential inability) will qualify for a Yes
                 //         resolution.</p><p>The resolution source for this
                 //         market will be a consensus of credible
                 //         reporting.</p>", "collateralToken": {
                 //             "address":
                 //             "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913",
                 //             "decimals": "6",
                 //             "symbol": "USDC"
                 //         },
                 //         "title": "Trump out as President before 2027?",
                 //         "proxyTitle": null,
                 //         "expirationDate": "Jan 1, 2027",
                 //         "expirationTimestamp": "1798779540000",
                 //         "createdAt": "2026-01-20T18:17:48.298Z",
                 //         "updatedAt": "2026-04-09T10:47:02.254Z",
                 //         "categories": [ "Politics" ],
                 //         "status": "FUNDED",
                 //         "expired": false,
                 //         "hidden": false,
                 //         "creator": {
                 //             "name": "Limitless",
                 //             "imageURI":
                 //             "https://limitless.exchange/assets/images/logo.svg",
                 //             "link": "https://x.com/trylimitless"
                 //         },
                 //         "tags": [ "Limitless" ],
                 //         "volume": "1032001807",
                 //         "volumeFormatted": "1032.001807",
                 //         "tokens": {
                 //             "yes":
                 //             "56154308742753982686710750162015444986563701968079760676518531584453506363044",
                 //             "no":
                 //             "32572248812801208874557774576516861470423415416073401354576860825663488568217"
                 //         },
                 //         "prices": [ 0.155, 0.845 ],
                 //         "tradePrices": {
                 //             "buy": { "market": [Array], "limit": [Array] },
                 //             "sell": { "market": [Array], "limit": [Array] }
                 //         },
                 //         "isOther": false,
                 //         "isRewardable": true,
                 //         "slug":
                 //         "trump-out-as-president-before-2027-1768933068297",
                 //         "tradeType": "clob",
                 //         "venue": {
                 //             "exchange":
                 //             "0x05c748E2f4DcDe0ec9Fa8DDc40DE6b867f923fa5",
                 //             "adapter": null
                 //         },
                 //         "marketType": "single",
                 //         "priorityIndex": "0",
                 //         "winningOutcomeIndex": null,
                 //         "metadata": { "fee": true, "isBannered": false,
                 //         "isPolyArbitrage": true }, "settings": {
                 //             "minSize": "100000000",
                 //             "maxSpread": "0.035",
                 //             "dailyReward": "5",
                 //             "rewardsEpoch": "0.003472222222222222",
                 //             "c": "3",
                 //             "rebateRate": "0"
                 //         },
                 //         "imageUrl":
                 //         "https://cdn.limitless.exchange/markets-logo/36814/9daba01d-6bcd-4a2c-9187-f4264b7191da.png",
                 //         "logo":
                 //         "https://cdn.limitless.exchange/markets-logo/36814/9daba01d-6bcd-4a2c-9187-f4264b7191da.png"
                 //     }
                 //
                 ccxt::any tickerInput = ccxt::dict{
                     {std::string("market"), response},
                     {std::string("book"), ::getValue(responses, 1)},
                 };
                 return this->parsePredictionTicker(tickerInput, outcomeObj);
               })
        .share();
  }

  /**
   * @ignore
   * @method
   * @name limitless#parsePredictionTicker
   * @description parses a raw market object, or a composite market + book dict,
   * into a unified ticker for the specified outcome token
   * @param {object} ticker a raw limitless market object or a dict with market
   * and book entries
   * @param {object} [market] the outcome object the ticker belongs to
   * @returns {object} a [prediction ticker
   * structure](https://docs.ccxt.com/#/?id=prediction-ticker-structure)
   */
  ccxt::any parsePredictionTicker(ccxt::any ticker,
                                  ccxt::any market = ccxt::any{}) override {
    //
    //     {
    //         "id": "36814",
    //         "automationType": "manual",
    //         "conditionId":
    //         "0x11287d02d8067ff3d3d8bd21b212ebcfdc20b638f7f6440e4115f649e6b57015",
    //         "negRiskRequestId": null,
    //         "description": "<p>This market will resolve to Yes if Donald
    //         Trump resigns or is removed as President or otherwise ceases to
    //         be the President of the United States for any period of time by
    //         December 31, 2026, 11:59 PM ET. Otherwise, this market will
    //         resolve to No.</p><p>An announcement of Donald Trump's
    //         resignation/removal before this market's end date will
    //         immediately resolve this market to Yes, regardless of when the
    //         announced resignation/removal goes into effect.</p><p>Only
    //         permanent removal from office will qualify. Temporary removal
    //         (e.g. temporary invocation of the 25th Amendment under Section 3
    //         or a Section 4 invocation not sustained by both Houses of
    //         Congress) or impeachment without removal will not count.</p><p>A
    //         sustained invocation of the Twenty-Fifth Amendment, Section 4
    //         (i.e., if both Houses of Congress, by two-thirds vote, uphold the
    //         Vice President and Cabinets determination of presidential
    //         inability) will qualify for a Yes resolution.</p><p>The
    //         resolution source for this market will be a consensus of credible
    //         reporting.</p>", "collateralToken": {
    //             "address": "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913",
    //             "decimals": "6",
    //             "symbol": "USDC"
    //         },
    //         "title": "Trump out as President before 2027?",
    //         "proxyTitle": null,
    //         "expirationDate": "Jan 1, 2027",
    //         "expirationTimestamp": "1798779540000",
    //         "createdAt": "2026-01-20T18:17:48.298Z",
    //         "updatedAt": "2026-04-09T10:47:02.254Z",
    //         "categories": [ "Politics" ],
    //         "status": "FUNDED",
    //         "expired": false,
    //         "hidden": false,
    //         "creator": {
    //             "name": "Limitless",
    //             "imageURI":
    //             "https://limitless.exchange/assets/images/logo.svg", "link":
    //             "https://x.com/trylimitless"
    //         },
    //         "tags": [ "Limitless" ],
    //         "volume": "1032001807",
    //         "volumeFormatted": "1032.001807",
    //         "tokens": {
    //             "yes":
    //             "56154308742753982686710750162015444986563701968079760676518531584453506363044",
    //             "no":
    //             "32572248812801208874557774576516861470423415416073401354576860825663488568217"
    //         },
    //         "prices": [ 0.155, 0.845 ],
    //         "tradePrices": {
    //             "buy": { "market": [Array], "limit": [Array] },
    //             "sell": { "market": [Array], "limit": [Array] }
    //         },
    //         "isOther": false,
    //         "isRewardable": true,
    //         "slug": "trump-out-as-president-before-2027-1768933068297",
    //         "tradeType": "clob",
    //         "venue": {
    //             "exchange": "0x05c748E2f4DcDe0ec9Fa8DDc40DE6b867f923fa5",
    //             "adapter": null
    //         },
    //         "marketType": "single",
    //         "priorityIndex": "0",
    //         "winningOutcomeIndex": null,
    //         "metadata": { "fee": true, "isBannered": false,
    //         "isPolyArbitrage": true }, "settings": {
    //             "minSize": "100000000",
    //             "maxSpread": "0.035",
    //             "dailyReward": "5",
    //             "rewardsEpoch": "0.003472222222222222",
    //             "c": "3",
    //             "rebateRate": "0"
    //         },
    //         "imageUrl":
    //         "https://cdn.limitless.exchange/markets-logo/36814/9daba01d-6bcd-4a2c-9187-f4264b7191da.png",
    //         "logo":
    //         "https://cdn.limitless.exchange/markets-logo/36814/9daba01d-6bcd-4a2c-9187-f4264b7191da.png"
    //     }
    //
    // ticker is either a plain raw market object, or a composite dict {
    // 'market': rawMarket, 'book': rawOrderbook }
    ccxt::any raw = ticker;
    ccxt::any book = ccxt::any{};
    if (isTrue(inOp(ticker, std::string("market")))) {
      raw = this->safeDict(ticker, std::string("market"), ccxt::dict{});
      book = this->safeDict(ticker, std::string("book"));
    }
    ccxt::any rawLabel =
        (isTrue((!isEqual(market, ccxt::any{})))
             ? ccxt::any(this->safeString(
                   market, std::string("label"),
                   this->safeString(::getValue(market, std::string("info")),
                                    std::string("outcomeLabel"),
                                    std::string("yes"))))
             : ccxt::any(std::string("yes")));
    ccxt::any isYes = !isEqual(toLowerCase(rawLabel), std::string("no"));
    ccxt::any bidStr = ccxt::any{};
    ccxt::any askStr = ccxt::any{};
    ccxt::any bidSizeStr = ccxt::any{};
    ccxt::any askSizeStr = ccxt::any{};
    ccxt::any lastStr = ccxt::any{};
    ccxt::any midStr = ccxt::any{};
    if (isTrue(!isEqual(book, ccxt::any{}))) {
      // the book endpoint is quoted in the yes token, the no side mirrors at 1
      // - price
      ccxt::any rawBids =
          this->safeList(book, std::string("bids"), ccxt::list{});
      ccxt::any rawAsks =
          this->safeList(book, std::string("asks"), ccxt::list{});
      ccxt::any rawBidsLength = getArrayLength(rawBids);
      ccxt::any rawAsksLength = getArrayLength(rawAsks);
      ccxt::any yesBestBid = (isTrue((isGreaterThan(rawBidsLength, 0)))
                                  ? ccxt::any(::getValue(rawBids, 0))
                                  : ccxt::any(ccxt::any{}));
      ccxt::any yesBestAsk = (isTrue((isGreaterThan(rawAsksLength, 0)))
                                  ? ccxt::any(::getValue(rawAsks, 0))
                                  : ccxt::any(ccxt::any{}));
      ccxt::any yesBidPrice =
          this->safeString(yesBestBid, std::string("price"));
      ccxt::any yesBidSize = this->safeString(yesBestBid, std::string("size"));
      ccxt::any yesAskPrice =
          this->safeString(yesBestAsk, std::string("price"));
      ccxt::any yesAskSize = this->safeString(yesBestAsk, std::string("size"));
      ccxt::any yesLast = this->safeString(book, std::string("lastTradePrice"));
      ccxt::any yesMid = this->safeString(book, std::string("midpoint"));
      if (isTrue(isYes)) {
        bidStr = yesBidPrice;
        bidSizeStr = yesBidSize;
        askStr = yesAskPrice;
        askSizeStr = yesAskSize;
        lastStr = yesLast;
        midStr = yesMid;
      } else {
        if (isTrue(!isEqual(yesAskPrice, ccxt::any{}))) {
          bidStr = ccxt::Precise::stringSub(std::string("1"), yesAskPrice);
        }
        bidSizeStr = yesAskSize;
        if (isTrue(!isEqual(yesBidPrice, ccxt::any{}))) {
          askStr = ccxt::Precise::stringSub(std::string("1"), yesBidPrice);
        }
        askSizeStr = yesBidSize;
        if (isTrue(!isEqual(yesLast, ccxt::any{}))) {
          lastStr = ccxt::Precise::stringSub(std::string("1"), yesLast);
        }
        if (isTrue(!isEqual(yesMid, ccxt::any{}))) {
          midStr = ccxt::Precise::stringSub(std::string("1"), yesMid);
        }
      }
    }
    ccxt::any prices = this->safeList(raw, std::string("prices"), ccxt::list{});
    ccxt::any pricesLength = getArrayLength(prices);
    if (isTrue(isTrue((isEqual(lastStr, ccxt::any{}))) &&
               isTrue((isGreaterThan(pricesLength, 0))))) {
      lastStr = (isTrue((isYes)) ? ccxt::any(this->safeString(prices, 0))
                                 : ccxt::any(this->safeString(prices, 1)));
    }
    // volume and book sizes are in USDC micro-units (6 decimals)
    ccxt::any rawVolume = this->safeString(raw, std::string("volume"));
    ccxt::any volumeStr = ccxt::any{};
    if (isTrue(!isEqual(rawVolume, ccxt::any{}))) {
      volumeStr = ccxt::Precise::stringDiv(rawVolume, std::string("1000000"));
    }
    if (isTrue(!isEqual(bidSizeStr, ccxt::any{}))) {
      bidSizeStr = ccxt::Precise::stringDiv(bidSizeStr, std::string("1000000"));
    }
    if (isTrue(!isEqual(askSizeStr, ccxt::any{}))) {
      askSizeStr = ccxt::Precise::stringDiv(askSizeStr, std::string("1000000"));
    }
    ccxt::any now = this->milliseconds();
    ccxt::any outcomeSymbol = this->safeOutcomeSymbol(ccxt::any{}, market);
    return this->safePredictionTicker(ccxt::dict{
        {std::string("outcome"), outcomeSymbol},
        {std::string("outcomeId"),
         this->safeString(market, std::string("outcomeId"))},
        {std::string("label"), this->safeString(market, std::string("label"))},
        {std::string("market"),
         this->safeString(market, std::string("market"))},
        {std::string("timestamp"), now},
        {std::string("datetime"), this->iso8601(now)},
        {std::string("high"), ccxt::any{}},
        {std::string("low"), ccxt::any{}},
        {std::string("bid"), this->parseNumber(bidStr)},
        {std::string("bidVolume"), this->parseNumber(bidSizeStr)},
        {std::string("ask"), this->parseNumber(askStr)},
        {std::string("askVolume"), this->parseNumber(askSizeStr)},
        {std::string("vwap"), ccxt::any{}},
        {std::string("open"), ccxt::any{}},
        {std::string("close"), this->parseNumber(lastStr)},
        {std::string("last"), this->parseNumber(lastStr)},
        {std::string("previousClose"), ccxt::any{}},
        {std::string("change"), ccxt::any{}},
        {std::string("percentage"), ccxt::any{}},
        {std::string("average"), this->parseNumber(midStr)},
        {std::string("baseVolume"), ccxt::any{}},
        {std::string("quoteVolume"), this->parseNumber(volumeStr)},
        {std::string("info"), ticker},
    });
  }

  /**
   * @method
   * @name limitless#fetchTickers
   * @description fetches tickers for multiple outcome tokens, grouping
   * requested outcomes by their parent market (two requests per market: detail
   * + order book)
   * @see https://docs.limitless.exchange/api-reference/markets/get-market
   * @see https://docs.limitless.exchange/api-reference/trading/orderbook
   * @param {string[]} outcomes unified outcomes or outcome token ids —
   * required: limitless has no endpoint returning all tickers at once, so an
   * unscoped call is not supported
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a dictionary of [prediction ticker
   * structures](https://docs.ccxt.com/#/?id=prediction-ticker-structure)
   * indexed by outcome
   */
  std::shared_future<ccxt::any>
  fetchTickers(ccxt::any outcomes = ccxt::any{},
               ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(outcomes, ccxt::any{}))) {
                   throw ArgumentsRequired(toString(
                       add(this->id,
                           std::string(
                               " fetchTickers() requires an outcomes argument "
                               "— the venue has no all-tickers endpoint; pass "
                               "the outcome handles to fetch (discover them "
                               "via fetchEvents ())"))));
                 }
                 ccxt::any result = ccxt::dict{};
                 // resolve the uncached outcomes first, then group by parent
                 // market to fetch each market and book only once
                 awaitValue(this->loadOutcomes(outcomes));
                 ccxt::any outcomesBySlug = ccxt::dict{};
                 ccxt::any slugs = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(outcomes));
                      postFixIncrement(i)) {
                   ccxt::any outcomeObj =
                       this->outcome(::getValue(outcomes, i));
                   ccxt::any slug = this->safeString(
                       ::getValue(outcomeObj, std::string("info")),
                       std::string("slug"));
                   if (isTrue(isEqual(slug, ccxt::any{}))) {
                     throw ExchangeError(toString(
                         add(this->id,
                             std::string(" fetchTickers() missing slug"))));
                   }
                   if (!isTrue((inOp(outcomesBySlug, slug)))) {
                     if (isTrue(!isEqual(slug, ccxt::any{}))) {
                       ::setValue(outcomesBySlug, slug, ccxt::list{});
                     }
                     arrayPush(slugs, slug);
                   }
                   // reassign after push, plain mutation through a local is
                   // lost in transpiled php (arrays are value types there)
                   ccxt::any grouped = this->safeValue(outcomesBySlug, slug);
                   arrayPush(grouped, outcomeObj);
                   if (isTrue(!isEqual(slug, ccxt::any{}))) {
                     ::setValue(outcomesBySlug, slug, grouped);
                   }
                 }
                 ccxt::any promises = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(slugs));
                      postFixIncrement(i)) {
                   ccxt::any slug = ::getValue(slugs, i);
                   arrayPush(promises,
                             this->limitlessPublicGetMarketsAddressOrSlug(
                                 this->extend(
                                     ccxt::dict{
                                         {std::string("addressOrSlug"), slug},
                                     },
                                     params)));
                   arrayPush(
                       promises,
                       this->limitlessPublicGetMarketsSlugOrderbook(ccxt::dict{
                           {std::string("slug"), slug},
                       }));
                 }
                 ccxt::any responses = awaitValue(promiseAll(promises));
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(slugs));
                      postFixIncrement(i)) {
                   ccxt::any slug = ::getValue(slugs, i);
                   ccxt::any detailIndex = multiply(i, 2);
                   ccxt::any detail = ::getValue(responses, detailIndex);
                   ccxt::any book =
                       ::getValue(responses, this->sum(detailIndex, 1));
                   ccxt::any tickerInput = ccxt::dict{
                       {std::string("market"), detail},
                       {std::string("book"), book},
                   };
                   ccxt::any grouped = ::getValue(outcomesBySlug, slug);
                   for (ccxt::any j = 0; isLessThan(j, getArrayLength(grouped));
                        postFixIncrement(j)) {
                     ccxt::any ticker = this->parsePredictionTicker(
                         tickerInput, ::getValue(grouped, j));
                     ccxt::any symbolKey =
                         this->safeString(ticker, std::string("outcome"));
                     if (isTrue(!isEqual(symbolKey, ccxt::any{}))) {
                       ::setValue(result, symbolKey, ticker);
                     }
                   }
                 }
                 return result;
               })
        .share();
  }

  /**
   * @method
   * @name limitless#fetchTrades
   * @description fetches recent public trades for a single outcome token from
   * the market events feed
   * @see https://docs.limitless.exchange/api-reference/trading/market-events
   * @param {string} outcome unified outcome like TRUMP_OUT_PRESIDENT_2027:YES
   * or an outcome token id
   * @param {int} [since] timestamp in ms of the earliest trade to fetch
   * @param {int} [limit] the maximum number of trades to return
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [prediction trade
   * structures](https://docs.ccxt.com/#/?id=prediction-trade-structure)
   */
  std::shared_future<ccxt::any>
  fetchTrades(ccxt::any outcome, ccxt::any since = ccxt::any{},
              ccxt::any limit = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 awaitValue(this->loadOutcome(outcome));
                 ccxt::any outcomeObj = this->outcome(outcome);
                 ccxt::any slug = this->safeString(
                     ::getValue(outcomeObj, std::string("info")),
                     std::string("slug"));
                 ccxt::any tokenId =
                     this->safeString(outcomeObj, std::string("outcomeId"));
                 ccxt::any request = ccxt::dict{
                     {std::string("slug"), slug},
                 };
                 if (isTrue(!isEqual(limit, ccxt::any{}))) {
                   ::setValue(request, std::string("limit"), limit);
                 }
                 ccxt::any response =
                     awaitValue(this->limitlessPublicGetMarketsSlugEvents(
                         this->extend(request, params)));
                 //
                 //     {
                 //         "events": [
                 //             {
                 //                 "createdAt": "2026-06-12T15:24:25.617Z",
                 //                 "makerAmount": "19996200",
                 //                 "matchedSize": "2500000",
                 //                 "price": 0.332,
                 //                 "profile": { "account":
                 //                 "0x0572B4Aa431e730d1d19cc7CFea7D6C0Bc07096f"
                 //                 }, "side": 0, "takerAmount": "830000",
                 //                 "title": "",
                 //                 "tokenId":
                 //                 "34504808738227095158010759634880534083339296329182698784451980951930485131851",
                 //                 "txHash":
                 //                 "0x432344fc63f26f37e3ffe3aca45bcfcba6b7addfc07d04de4410855c0a56a175"
                 //             }
                 //         ],
                 //         "limit": 30,
                 //         "page": 1,
                 //         "totalPages": 1,
                 //         "totalRows": 13
                 //     }
                 //
                 ccxt::any rows = this->safeList(
                     response, std::string("events"), ccxt::list{});
                 ccxt::any filtered = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(rows));
                      postFixIncrement(i)) {
                   ccxt::any row = ::getValue(rows, i);
                   ccxt::any rowTokenId =
                       this->safeString(row, std::string("tokenId"));
                   if (isTrue(isTrue(isTrue((!isEqual(tokenId, ccxt::any{}))) &&
                                     isTrue((
                                         !isEqual(rowTokenId, ccxt::any{})))) &&
                              isTrue((!isEqual(rowTokenId, tokenId))))) {
                     continue;
                   }
                   arrayPush(filtered, row);
                 }
                 return this->parsePredictionTrades(filtered, outcomeObj, since,
                                                    limit);
               })
        .share();
  }

  /**
   * @method
   * @name limitless#fetchOrderBook
   * @description fetches the order book for a single outcome token, converting
   * 6-decimal USDC sizes to whole units, no outcomes are quoted at 1 - price
   * with the sides swapped
   * @see https://docs.limitless.exchange/api-reference/trading/orderbook
   * @param {string} outcome unified outcome like TRUMP_OUT_PRESIDENT_2027:YES
   * or an outcome token id
   * @param {int} [limit] not used by limitless fetchOrderBook
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [prediction order book
   * structure](https://docs.ccxt.com/#/?id=prediction-order-book-structure)
   */
  std::shared_future<ccxt::any>
  fetchOrderBook(ccxt::any outcome, ccxt::any limit = ccxt::any{},
                 ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 awaitValue(this->loadOutcome(outcome));
                 ccxt::any outcomeObj = this->outcome(outcome);
                 ccxt::any slug = this->safeString(
                     ::getValue(outcomeObj, std::string("info")),
                     std::string("slug"));
                 ccxt::any request = ccxt::dict{
                     {std::string("slug"), slug},
                 };
                 ccxt::any response =
                     awaitValue(this->limitlessPublicGetMarketsSlugOrderbook(
                         this->extend(request, params)));
                 //
                 //     {
                 //         "bids": [
                 //             { "price": "0.14", "size": "12360330000",
                 //             "side": "BUY" }, { "price": "0.1", "size":
                 //             "1000000", "side": "BUY" }, { "price": "0.003",
                 //             "size": "500000000", "side": "BUY" }
                 //         ],
                 //         "asks": [
                 //             { "price": "0.161", "size": "222000000", "side":
                 //             "SELL" }, { "price": "0.996", "size":
                 //             "5555000000", "side": "SELL" }, { "price":
                 //             "0.997", "size": "500000000", "side": "SELL" }
                 //         ],
                 //         "tokenId":
                 //         "56154308742753982686710750162015444986563701968079760676518531584453506363044",
                 //         "adjustedMidpoint": "0.1505",
                 //         "midpoint": "0.1505",
                 //         "maxSpread": "0.035",
                 //         "minSize": "100000000",
                 //         "lastTradePrice": "0.161"
                 //     }
                 //
                 ccxt::any timestamp = this->milliseconds();
                 ccxt::any decimals = this->safeInteger(
                     this->options, std::string("usdcDecimals"), 6);
                 // sizes are scaled by 10^decimals, USDC uses 6 decimals
                 ccxt::any scaleStr = this->parsePrecision(
                     this->numberToString(prefixUnaryNeg(decimals)));
                 ccxt::any outcomeLabel = this->safeStringLower(
                     ::getValue(outcomeObj, std::string("info")),
                     std::string("outcomeLabel"), std::string("yes"));
                 ccxt::any isYes = !isEqual(outcomeLabel, std::string("no"));
                 ccxt::any rawBids = this->safeList(
                     response, std::string("bids"), ccxt::list{});
                 ccxt::any rawAsks = this->safeList(
                     response, std::string("asks"), ccxt::list{});
                 // the book endpoint is quoted in the yes token, the no side
                 // mirrors at 1 - price with bids and asks swapped
                 ccxt::any bidsSource = (isTrue((isYes)) ? ccxt::any(rawBids)
                                                         : ccxt::any(rawAsks));
                 ccxt::any asksSource = (isTrue((isYes)) ? ccxt::any(rawAsks)
                                                         : ccxt::any(rawBids));
                 ccxt::any bids = ccxt::list{};
                 ccxt::any asks = ccxt::list{};
                 for (ccxt::any bi = 0;
                      isLessThan(bi, getArrayLength(bidsSource));
                      postFixIncrement(bi)) {
                   ccxt::any priceStr = this->safeString(
                       ::getValue(bidsSource, bi), std::string("price"));
                   if (isTrue(!isTrue(isYes) &&
                              isTrue((!isEqual(priceStr, ccxt::any{}))))) {
                     priceStr =
                         ccxt::Precise::stringSub(std::string("1"), priceStr);
                   }
                   ccxt::any sizeStr = this->safeString(
                       ::getValue(bidsSource, bi), std::string("size"));
                   if (isTrue(!isEqual(sizeStr, ccxt::any{}))) {
                     sizeStr = ccxt::Precise::stringDiv(sizeStr, scaleStr);
                   }
                   arrayPush(bids, ccxt::list{this->parseNumber(priceStr),
                                              this->parseNumber(sizeStr)});
                 }
                 for (ccxt::any ai = 0;
                      isLessThan(ai, getArrayLength(asksSource));
                      postFixIncrement(ai)) {
                   ccxt::any priceStr = this->safeString(
                       ::getValue(asksSource, ai), std::string("price"));
                   if (isTrue(!isTrue(isYes) &&
                              isTrue((!isEqual(priceStr, ccxt::any{}))))) {
                     priceStr =
                         ccxt::Precise::stringSub(std::string("1"), priceStr);
                   }
                   ccxt::any sizeStr = this->safeString(
                       ::getValue(asksSource, ai), std::string("size"));
                   if (isTrue(!isEqual(sizeStr, ccxt::any{}))) {
                     sizeStr = ccxt::Precise::stringDiv(sizeStr, scaleStr);
                   }
                   arrayPush(asks, ccxt::list{this->parseNumber(priceStr),
                                              this->parseNumber(sizeStr)});
                 }
                 ccxt::any orderbook = ccxt::dict{
                     {std::string("outcome"),
                      this->safeOutcomeSymbol(outcome, outcomeObj)},
                     {std::string("bids"), this->sortBy(bids, 0, true)},
                     {std::string("asks"), this->sortBy(asks, 0)},
                     {std::string("timestamp"), timestamp},
                     {std::string("datetime"), this->iso8601(timestamp)},
                     {std::string("nonce"), ccxt::any{}},
                 };
                 return this->safePredictionOrderBook(orderbook, outcomeObj);
               })
        .share();
  }

  /**
   * @method
   * @name limitless#fetchOHLCV
   * @description fetches historical prices for a single limitless market
   * outcome and maps them to OHLCV format, uses the `interval` query parameter
   * and selects the YES/NO series that matches the requested outcome
   * @see https://docs.limitless.exchange/api-reference/trading/historical-price
   * @param {string} outcome outcome, e.g. "TRUMP_OUT:YES"
   * @param {string} timeframe the length of time each candle represents
   * @param {int} [since] timestamp in ms of the earliest candle to fetch
   * @param {int} [limit] the maximum number of candles to fetch
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {int[][]} a list of candles ordered as timestamp, open, high, low,
   * close, volume
   */
  std::shared_future<ccxt::any>
  fetchOHLCV(ccxt::any outcome, ccxt::any timeframe = std::string("1d"),
             ccxt::any since = ccxt::any{}, ccxt::any limit = ccxt::any{},
             ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 awaitValue(this->loadOutcome(outcome));
                 ccxt::any outcomeObj = this->outcome(outcome);
                 ccxt::any slug = this->safeString(
                     ::getValue(outcomeObj, std::string("info")),
                     std::string("slug"));
                 ccxt::any outcomeLabel = this->safeStringUpper(
                     ::getValue(outcomeObj, std::string("info")),
                     std::string("outcomeLabel"));
                 ccxt::any interval = this->safeString(
                     this->timeframes, timeframe, std::string("1d"));
                 ccxt::any response = awaitValue(
                     this->limitlessPublicGetMarketsSlugHistoricalPrice(
                         this->extend(
                             ccxt::dict{
                                 {std::string("slug"), slug},
                                 {std::string("interval"), interval},
                             },
                             params)));
                 //
                 //     {
                 //         "data": [
                 //             {
                 //                  "timestamp": 1705318200000,
                 //                  "price": 0.1655
                 //             },
                 //         ]
                 //     }
                 //
                 //
                 //     [
                 //         {
                 //             "title": "YES Token",
                 //             "prices": [
                 //                 {
                 //                     "price": 0.75,
                 //                     "timestamp": "2024-01-15T10:30:00Z"
                 //                 },
                 //             ]
                 //         },
                 //         {
                 //             "title": "NO Token",
                 //             "prices": [
                 //                 {
                 //                     "price": 0.25,
                 //                     "timestamp": "2024-01-15T10:30:00Z"
                 //                 }
                 //             ]
                 //         }
                 //     ]
                 //
                 ccxt::any responseRows = ccxt::list{};
                 if (isTrue(isArray(response))) {
                   responseRows = response;
                 }
                 ccxt::any rawHistoryList = this->safeList(
                     response, std::string("data"),
                     this->safeList(response, std::string("prices"),
                                    responseRows));
                 ccxt::any rawHistory =
                     (isTrue((!isEqual(rawHistoryList, ccxt::any{})))
                          ? ccxt::any(rawHistoryList)
                          : ccxt::any(ccxt::list{}));
                 ccxt::any history = rawHistory;
                 ccxt::any rawHistoryLength = getArrayLength(rawHistory);
                 if (isTrue(isGreaterThan(rawHistoryLength, 0))) {
                   ccxt::any first =
                       this->safeDict(rawHistory, 0, ccxt::dict{});
                   ccxt::any firstPrices =
                       this->safeList(first, std::string("prices"));
                   if (isTrue(!isEqual(firstPrices, ccxt::any{}))) {
                     ccxt::any selectedSeries = first;
                     for (ccxt::any i = 0;
                          isLessThan(i, getArrayLength(rawHistory));
                          postFixIncrement(i)) {
                       ccxt::any series =
                           this->safeDict(rawHistory, i, ccxt::dict{});
                       ccxt::any title = this->safeStringUpper(
                           series, std::string("title"), std::string(""));
                       if (isTrue(isEqual(title, ccxt::any{}))) {
                         throw ExchangeError(toString(
                             add(this->id,
                                 std::string(" fetchOHLCV() missing title"))));
                       }
                       if (isTrue(
                               isTrue((!isEqual(outcomeLabel, ccxt::any{}))) &&
                               isTrue((isGreaterThanOrEqual(
                                   getIndexOf(title, outcomeLabel), 0))))) {
                         selectedSeries = series;
                         break;
                       }
                     }
                     history = this->safeList(
                         selectedSeries, std::string("prices"), ccxt::list{});
                   }
                 }
                 // the endpoint returns raw price points, not candles - bucket
                 // them into timeframe-aligned candles (single points would
                 // carry unaligned timestamps)
                 ccxt::any pseudoTrades = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(history));
                      postFixIncrement(i)) {
                   ccxt::any point = ::getValue(history, i);
                   ccxt::any pointPrice =
                       this->safeNumber(point, std::string("price"));
                   ccxt::any pointTs =
                       this->safeInteger(point, std::string("timestamp"));
                   if (isTrue(isEqual(pointTs, ccxt::any{}))) {
                     ccxt::any tsString =
                         this->safeString(point, std::string("timestamp"));
                     pointTs =
                         (isTrue((isTrue(!isEqual(tsString, ccxt::any{})) &&
                                  isTrue(!isEqual(tsString, std::string("")))))
                              ? ccxt::any(this->parse8601(tsString))
                              : ccxt::any(ccxt::any{}));
                   } else if (isTrue(isLessThan(pointTs, 1000000000000))) {
                     // old responses may return unix seconds
                     pointTs = multiply(pointTs, 1000);
                   }
                   if (isTrue(isTrue((!isEqual(pointPrice, ccxt::any{}))) &&
                              isTrue((!isEqual(pointTs, ccxt::any{}))))) {
                     arrayPush(pseudoTrades,
                               ccxt::dict{
                                   {std::string("timestamp"), pointTs},
                                   {std::string("price"), pointPrice},
                                   {std::string("amount"), 0},
                               });
                   }
                 }
                 // the endpoint returns points NEWEST-first, so sort ascending
                 // by timestamp before bucketing — otherwise candles come back
                 // descending and open/close are inverted within each bucket —
                 // the first point seen would be the latest, not the earliest.
                 // sortBy is stable, so equal timestamps keep their relative
                 // order consistently across languages
                 ccxt::any sorted =
                     this->sortBy(pseudoTrades, std::string("timestamp"));
                 ccxt::any ms = multiply(this->parseTimeframe(timeframe), 1000);
                 ccxt::any candles = ccxt::dict{};
                 ccxt::any bucketOrder = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(sorted));
                      postFixIncrement(i)) {
                   ccxt::any point = ::getValue(sorted, i);
                   ccxt::any pTs =
                       this->safeInteger(point, std::string("timestamp"));
                   ccxt::any pPrice =
                       this->safeNumber(point, std::string("price"));
                   if (isTrue(isEqual(pTs, ccxt::any{}))) {
                     throw ExchangeError(toString(
                         add(this->id, std::string(" method() missing pTs"))));
                   }
                   ccxt::any bucket =
                       multiply(this->parseToInt(divide(pTs, ms)), ms);
                   ccxt::any key = toString(bucket);
                   if (!isTrue((inOp(candles, key)))) {
                     ::setValue(
                         candles, key,
                         ccxt::list{bucket, pPrice, pPrice, pPrice, pPrice, 0});
                     arrayPush(bucketOrder, key);
                   } else {
                     ccxt::any candle = ::getValue(candles, key);
                     ccxt::any pPriceOrZero =
                         (isTrue((isEqual(pPrice, ccxt::any{})))
                              ? ccxt::any(0)
                              : ccxt::any(pPrice));
                     ::setValue(candle, 2,
                                mathMax(::getValue(candle, 2), pPriceOrZero));
                     ccxt::any candleLow =
                         (isTrue((isEqual(::getValue(candle, 3), ccxt::any{})))
                              ? ccxt::any(pPrice)
                              : ccxt::any(::getValue(candle, 3)));
                     ccxt::any pPriceOrCandleLow =
                         (isTrue((isEqual(pPrice, ccxt::any{})))
                              ? ccxt::any(::getValue(candle, 3))
                              : ccxt::any(pPrice));
                     ::setValue(candle, 3,
                                mathMin(candleLow, pPriceOrCandleLow));
                     ::setValue(candle, 4, pPrice);
                     ::setValue(candles, key,
                                candle); // php arrays are value types - write
                                         // the mutation back
                   }
                 }
                 ccxt::any result = ccxt::list{};
                 for (ccxt::any i = 0;
                      isLessThan(i, getArrayLength(bucketOrder));
                      postFixIncrement(i)) {
                   arrayPush(result,
                             ::getValue(candles, ::getValue(bucketOrder, i)));
                 }
                 return this->filterBySinceLimit(result, since, limit, 0);
               })
        .share();
  }

  /**
   * @method
   * @name limitless#fetchOrders
   * @description fetches orders for the authenticated user for a single outcome
   * @see https://docs.limitless.exchange/api-reference/orders/get-user-orders
   * @param {string} [outcome] outcome, e.g. "TRUMP_OUT:YES"
   * @param {int} [since] the earliest time in ms to fetch orders for
   * @param {int} [limit] the maximum number of order structures to retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [prediction order
   * structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
   */
  std::shared_future<ccxt::any>
  fetchOrders(ccxt::any outcome = ccxt::any{}, ccxt::any since = ccxt::any{},
              ccxt::any limit = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(outcome, ccxt::any{}))) {
                   throw ArgumentsRequired(toString(
                       add(this->id,
                           std::string(
                               " fetchOrders requires an outcome argument"))));
                 }
                 awaitValue(this->loadOutcome(outcome));
                 ccxt::any outcomeObj = this->outcome(outcome);
                 ccxt::any info =
                     this->safeDict(outcomeObj, std::string("info"));
                 ccxt::any request = ccxt::dict{
                     {std::string("slug"),
                      this->safeString(info, std::string("slug"))},
                     {std::string("statuses"),
                      ccxt::list{std::string("LIVE"), std::string("MATCHED")}},
                 };
                 if (isTrue(!isEqual(limit, ccxt::any{}))) {
                   ::setValue(request, std::string("limit"), limit);
                 }
                 ccxt::any response =
                     awaitValue(this->limitlessPrivateGetMarketsSlugUserOrders(
                         this->extend(request, params)));
                 //
                 //     [
                 //         {
                 //             "createdAt": "2026-05-04T08:57:06.448Z",
                 //             "id": "c4b1a83a-219f-48db-a9be-1ddadf0bc14c",
                 //             "ownerId": 1315134,
                 //             "marketId": "112523",
                 //             "token":
                 //             "46235703925185836960484608024734446969378108670784413458211837874003718039438",
                 //             "type": "GTC",
                 //             "status": "LIVE",
                 //             "side": "BUY",
                 //             "makerAmount": "1000040",
                 //             "takerAmount": "10870000",
                 //             "price": "0.092",
                 //             "originalSize": "10870000",
                 //             "remainingSize": "10870000"
                 //         }
                 //     ]
                 //
                 // pass undefined as market: parsePredictionOrder sets outcome
                 // to the market outcome while the outcome lives under
                 // 'outcome', so the base outcome filter would drop every
                 // order; the per-slug endpoint already scopes results and
                 // parsePredictionOrder resolves the outcome via outcomes_by_id
                 return this->parsePredictionOrders(this->toArray(response),
                                                    ccxt::any{}, since, limit);
               })
        .share();
  }

  /**
   * @method
   * @name limitless#fetchOpenOrders
   * @description fetches open orders for the authenticated user for a single
   * outcome
   * @see https://docs.limitless.exchange/api-reference/orders/get-user-orders
   * @param {string} [outcome] outcome, e.g. "TRUMP_OUT:YES"
   * @param {int} [since] the earliest time in ms to fetch orders for
   * @param {int} [limit] the maximum number of order structures to retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [prediction order
   * structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
   */
  std::shared_future<ccxt::any>
  fetchOpenOrders(ccxt::any outcome = ccxt::any{},
                  ccxt::any since = ccxt::any{}, ccxt::any limit = ccxt::any{},
                  ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(outcome, ccxt::any{}))) {
                   throw ArgumentsRequired(toString(add(
                       this->id,
                       std::string(
                           " fetchOpenOrders requires an outcome argument"))));
                 }
                 awaitValue(this->loadOutcome(outcome));
                 params =
                     this->extend(params, ccxt::dict{
                                              {std::string("statuses"),
                                               ccxt::list{std::string("LIVE")}},
                                          });
                 return awaitValue(
                     this->fetchOrders(outcome, since, limit, params));
               })
        .share();
  }

  /**
   * @method
   * @name limitless#fetchClosedOrders
   * @description fetches closed orders for the authenticated user for a single
   * outcome
   * @see https://docs.limitless.exchange/api-reference/orders/get-user-orders
   * @param {string} [outcome] outcome, e.g. "TRUMP_OUT:YES"
   * @param {int} [since] the earliest time in ms to fetch orders for
   * @param {int} [limit] the maximum number of order structures to retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [prediction order
   * structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
   */
  std::shared_future<ccxt::any> fetchClosedOrders(
      ccxt::any outcome = ccxt::any{}, ccxt::any since = ccxt::any{},
      ccxt::any limit = ccxt::any{}, ccxt::any params = ccxt::dict{}) override {
    return std::
        async(std::launch::deferred,
              [=]() mutable -> ccxt::any {
                if (isTrue(isEqual(outcome, ccxt::any{}))) {
                  throw ArgumentsRequired(toString(add(
                      this->id,
                      std::string(
                          " fetchClosedOrders requires an outcome argument"))));
                }
                awaitValue(this->loadOutcome(outcome));
                params = this->extend(params,
                                      ccxt::dict{
                                          {std::string("statuses"),
                                           ccxt::list{std::string("MATCHED")}},
                                      });
                return awaitValue(
                    this->fetchOrders(outcome, since, limit, params));
              })
            .share();
  }

  /**
   * @method
   * @name limitless#fetchOrdersByIds
   * @description fetch orders by the list of order id
   * @see
   * https://docs.limitless.exchange/api-reference/trading/order-status-batch
   * @param {string[]} ids list of order id
   * @param {string} [outcome] market outcome, e.g. "TRUMP_OUT:YES"
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [prediction order
   * structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
   */
  virtual std::shared_future<ccxt::any>
  fetchOrdersByIds(ccxt::any ids, ccxt::any outcome = ccxt::any{},
                   ccxt::any params = ccxt::dict{}) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(!isEqual(outcome, ccxt::any{}))) {
                   awaitValue(this->loadOutcome(outcome));
                 }
                 ccxt::any length = getArrayLength(ids);
                 if (isTrue(isGreaterThan(length, 50))) {
                   throw BadRequest(toString(add(
                       this->id, std::string(" fetchOrdersByIds can only fetch "
                                             "up to 50 orders at a time"))));
                 }
                 ccxt::any items = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, length);
                      postFixIncrement(i)) {
                   ccxt::any id = this->safeString(ids, i);
                   ccxt::any item = ccxt::dict{
                       {std::string("orderId"), id},
                   };
                   arrayPush(items, item);
                 }
                 ccxt::any request = ccxt::dict{
                     {std::string("items"), items},
                 };
                 ccxt::any response =
                     awaitValue(this->limitlessPrivatePostOrdersStatusBatch(
                         this->extend(request, params)));
                 //
                 //     {
                 //         "results": [
                 //             {
                 //                 "index": 0,
                 //                 "status": "found",
                 //                 "orderId":
                 //                 "ff0dcbf1-f7de-43f0-b6f1-16b972a17f49",
                 //                 "data": {
                 //                     "order": {
                 //                         "createdAt":
                 //                         "2026-05-04T10:26:01.334Z", "id":
                 //                         "ff0dcbf1-f7de-43f0-b6f1-16b972a17f49",
                 //                         "makerAmount": "9999360",
                 //                         "takerAmount": "10752000",
                 //                         "expiration": null,
                 //                         "signatureType": 2,
                 //                         "salt": "277966495716",
                 //                         "maker":
                 //                         "0xAb2B9833FC8B8f55F4De7C4A0FAb8577EF0F7b36",
                 //                         "signer":
                 //                         "0x82a0f074C6C0C11aA370D7FBF077668c31fCc990",
                 //                         "taker":
                 //                         "0x0000000000000000000000000000000000000000",
                 //                         "tokenId":
                 //                         "46235703925185836960484608024734446969378108670784413458211837874003718039438",
                 //                         "side": 0,
                 //                         "feeRateBps": 300,
                 //                         "nonce": "0",
                 //                         "signature":
                 //                         "0x1640c8558d8c627017b2c6ac71a79770d177033afeb9be72842803afcd12938f23308c507935011ad6d10acc299e5367f76b4f806eb025c15cb05739de11260d1b",
                 //                         "orderType": "FAK",
                 //                         "price": "0.93",
                 //                         "marketId": 112523,
                 //                         "ownerId": 1315134,
                 //                         "market": {
                 //                             "id": 112523,
                 //                             "slug":
                 //                             "doge-above-dollar010859-on-may-4-2000-utc-1777838401426",
                 //                             "title": "DOGE above $0.10859 on
                 //                             May 4, 20:00 UTC?", "status":
                 //                             "FUNDED", "yesPositionId":
                 //                             "46235703925185836960484608024734446969378108670784413458211837874003718039438",
                 //                             "noPositionId":
                 //                             "101714389600295994108208140228744407174156865974966685440205519669272948152879"
                 //                         },
                 //                         "owner": {
                 //                             "id": 1315134,
                 //                             "account":
                 //                             "0x7CFF82f72b991B6B2b661e404389fD8a40bCD21B",
                 //                             "client": "eoa",
                 //                             "tradeWalletOption":
                 //                             "smartWallet", "smartWallet":
                 //                             "0xAb2B9833FC8B8f55F4De7C4A0FAb8577EF0F7b36",
                 //                             "points": 0,
                 //                             "referredUsersCount": 0
                 //                         }
                 //                     },
                 //                     "makerMatches": [
                 //                         {
                 //                             "id":
                 //                             "be44a183-ec56-4076-a69c-10283321abd6",
                 //                             "matchedSize": "10752000",
                 //                             "fillPrice": "0.93",
                 //                             "fillCost": "9999360",
                 //                             "orderId":
                 //                             "3255c786-3f30-4115-a99f-72be478f2e44",
                 //                             "order": {
                 //                                 "id":
                 //                                 "3255c786-3f30-4115-a99f-72be478f2e44",
                 //                                 "maker":
                 //                                 "0x8274600A3a9DC84747E6dEb49380F6B1DE2C505d",
                 //                                 "price": "0.07",
                 //                                 "side": 0,
                 //                                 "tokenId":
                 //                                 "101714389600295994108208140228744407174156865974966685440205519669272948152879",
                 //                                 "owner": {
                 //                                     "id": 202602,
                 //                                     "account":
                 //                                     "0x8274600A3a9DC84747E6dEb49380F6B1DE2C505d",
                 //                                     "client": "eoa",
                 //                                     "tradeWalletOption":
                 //                                     null, "smartWallet":
                 //                                     null, "username": null,
                 //                                     "displayName":
                 //                                     "0x8274600A3a9DC84747E6dEb49380F6B1DE2C505d",
                 //                                     "pfpUrl": null,
                 //                                     "socialUrl": null,
                 //                                     "points": 0,
                 //                                     "referredUsersCount": 0
                 //                                 }
                 //                             }
                 //                         }
                 //                     ],
                 //                     "execution": {
                 //                         "feeRateBps": 300,
                 //                         "effectiveFeeBps": 59,
                 //                         "matched": true,
                 //                         "settlementStatus": "MINED",
                 //                         "tradeEventId":
                 //                         "44c46a93-f5cb-40f5-a52f-bd55bb97641e",
                 //                         "txHash":
                 //                         "0x101cda4b605007440b382c35a27531605c7fc1b29a7c803b19237586a74c10e8",
                 //                         "totalsRaw": {
                 //                             "contractsGross": "10752000",
                 //                             "contractsFee": "63436",
                 //                             "contractsNet": "10688564",
                 //                             "usdGross": "9999360",
                 //                             "usdFee": "0",
                 //                             "usdNet": "9999360"
                 //                         }
                 //                     }
                 //                 }
                 //             }
                 //         ]
                 //     }
                 //
                 ccxt::any results = this->safeList(
                     response, std::string("results"), ccxt::list{});
                 ccxt::any found = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(results));
                      postFixIncrement(i)) {
                   ccxt::any item = this->safeDict(results, i, ccxt::dict{});
                   ccxt::any itemStatus =
                       this->safeString(item, std::string("status"));
                   if (isTrue(isEqual(itemStatus, std::string("found")))) {
                     arrayPush(found, item);
                   }
                 }
                 return this->parsePredictionOrders(found);
               })
        .share();
  }

  /**
   * @method
   * @name limitless#fetchOrder
   * @description fetches information on an order made by the user
   * @see
   * https://docs.limitless.exchange/api-reference/trading/order-status-batch
   * @param {string} id the order id
   * @param {string} [outcome] market outcome, e.g. "TRUMP_OUT:YES"
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [prediction order
   * structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
   */
  virtual std::shared_future<ccxt::any>
  fetchOrder(ccxt::any id, ccxt::any outcome = ccxt::any{},
             ccxt::any params = ccxt::dict{}) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(!isEqual(outcome, ccxt::any{}))) {
                   awaitValue(this->loadOutcome(outcome));
                 }
                 ccxt::any orders = awaitValue(
                     this->fetchOrdersByIds(ccxt::list{id}, outcome, params));
                 ccxt::any order = this->safeDict(orders, 0);
                 if (isTrue(isEqual(order, ccxt::any{}))) {
                   throw OrderNotFound(toString(add(
                       add(this->id,
                           std::string(" fetchOrder() could not find order ")),
                       id)));
                 }
                 return order;
               })
        .share();
  }

  /**
   * @ignore
   * @method
   * @name limitless#parsePredictionOrder
   * @description parses a raw limitless order object into a unified order
   * object
   * @param {object} order the raw order object
   * @param {object} [market] the outcome object the order belongs to
   * @returns {object} a [prediction order
   * structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
   */
  ccxt::any parsePredictionOrder(ccxt::any order,
                                 ccxt::any market = ccxt::any{}) override {
    //
    // fetchOrders, fetchOpenOrders, fetchClosedOrders
    //     {
    //         "createdAt": "2026-05-04T08:57:06.448Z",
    //         "id": "c4b1a83a-219f-48db-a9be-1ddadf0bc14c",
    //         "ownerId": 1315134,
    //         "marketId": "112523",
    //         "token":
    //         "46235703925185836960484608024734446969378108670784413458211837874003718039438",
    //         "type": "GTC",
    //         "status": "LIVE",
    //         "side": "BUY",
    //         "makerAmount": "1000040",
    //         "takerAmount": "10870000",
    //         "price": "0.092",
    //         "originalSize": "10870000",
    //         "remainingSize": "10870000"
    //     }
    //
    // fetchOrdersByIds, fetchOrder
    //     {
    //         "index": 0,
    //         "status": "found",
    //         "orderId": "ff0dcbf1-f7de-43f0-b6f1-16b972a17f49",
    //         "data": {
    //             "order": {
    //                 "createdAt": "2026-05-04T10:26:01.334Z",
    //                 "id": "ff0dcbf1-f7de-43f0-b6f1-16b972a17f49",
    //                 "makerAmount": "9999360",
    //                 "takerAmount": "10752000",
    //                 "expiration": null,
    //                 "signatureType": 2,
    //                 "salt": "277966495716",
    //                 "maker": "0xAb2B9833FC8B8f55F4De7C4A0FAb8577EF0F7b36",
    //                 "signer": "0x82a0f074C6C0C11aA370D7FBF077668c31fCc990",
    //                 "taker": "0x0000000000000000000000000000000000000000",
    //                 "tokenId":
    //                 "46235703925185836960484608024734446969378108670784413458211837874003718039438",
    //                 "side": 0,
    //                 "feeRateBps": 300,
    //                 "nonce": "0",
    //                 "signature":
    //                 "0x1640c8558d8c627017b2c6ac71a79770d177033afeb9be72842803afcd12938f23308c507935011ad6d10acc299e5367f76b4f806eb025c15cb05739de11260d1b",
    //                 "orderType": "FAK",
    //                 "price": "0.93",
    //                 "marketId": 112523,
    //                 "ownerId": 1315134,
    //                 "market": {
    //                     "id": 112523,
    //                     "slug":
    //                     "doge-above-dollar010859-on-may-4-2000-utc-1777838401426",
    //                     "title": "DOGE above $0.10859 on May 4, 20:00 UTC?",
    //                     "status": "FUNDED",
    //                     "yesPositionId":
    //                     "46235703925185836960484608024734446969378108670784413458211837874003718039438",
    //                     "noPositionId":
    //                     "101714389600295994108208140228744407174156865974966685440205519669272948152879"
    //                 },
    //                 "owner": {
    //                     "id": 1315134,
    //                     "account":
    //                     "0x7CFF82f72b991B6B2b661e404389fD8a40bCD21B",
    //                     "client": "eoa",
    //                     "tradeWalletOption": "smartWallet",
    //                     "smartWallet":
    //                     "0xAb2B9833FC8B8f55F4De7C4A0FAb8577EF0F7b36",
    //                     "points": 0,
    //                     "referredUsersCount": 0
    //                 }
    //             },
    //             "makerMatches": [
    //                 {
    //                     "id": "be44a183-ec56-4076-a69c-10283321abd6",
    //                     "matchedSize": "10752000",
    //                     "fillPrice": "0.93",
    //                     "fillCost": "9999360",
    //                     "orderId": "3255c786-3f30-4115-a99f-72be478f2e44",
    //                     "order": {
    //                         "id": "3255c786-3f30-4115-a99f-72be478f2e44",
    //                         "maker":
    //                         "0x8274600A3a9DC84747E6dEb49380F6B1DE2C505d",
    //                         "price": "0.07",
    //                         "side": 0,
    //                         "tokenId":
    //                         "101714389600295994108208140228744407174156865974966685440205519669272948152879",
    //                         "owner": {
    //                             "id": 202602,
    //                             "account":
    //                             "0x8274600A3a9DC84747E6dEb49380F6B1DE2C505d",
    //                             "client": "eoa",
    //                             "tradeWalletOption": null,
    //                             "smartWallet": null,
    //                             "username": null,
    //                             "displayName":
    //                             "0x8274600A3a9DC84747E6dEb49380F6B1DE2C505d",
    //                             "pfpUrl": null,
    //                             "socialUrl": null,
    //                             "points": 0,
    //                             "referredUsersCount": 0
    //                         }
    //                     }
    //                 }
    //             ],
    //             "execution": {
    //                 "feeRateBps": 300,
    //                 "effectiveFeeBps": 59,
    //                 "matched": true,
    //                 "settlementStatus": "MINED",
    //                 "tradeEventId": "44c46a93-f5cb-40f5-a52f-bd55bb97641e",
    //                 "txHash":
    //                 "0x101cda4b605007440b382c35a27531605c7fc1b29a7c803b19237586a74c10e8",
    //                 "totalsRaw": {
    //                     "contractsGross": "10752000",
    //                     "contractsFee": "63436",
    //                     "contractsNet": "10688564",
    //                     "usdGross": "9999360",
    //                     "usdFee": "0",
    //                     "usdNet": "9999360"
    //                 }
    //             }
    //         }
    //     }
    ccxt::any data = this->safeDict(order, std::string("data"));
    ccxt::any rawOrder = this->safeDict(data, std::string("order"), order);
    // createOrder returns the order nested under an 'order' key
    ccxt::any wrappedOrder = this->safeDict(rawOrder, std::string("order"));
    if (isTrue(!isEqual(wrappedOrder, ccxt::any{}))) {
      rawOrder = wrappedOrder;
    }
    ccxt::any id = this->safeString(rawOrder, std::string("id"));
    ccxt::any tokenId = this->safeString2(rawOrder, std::string("token"),
                                          std::string("tokenId"));
    ccxt::any mkt = this->safeOutcome(tokenId, market);
    ccxt::any outcomeSymbol = this->safeString(mkt, std::string("outcome"));
    ccxt::any rawSide = this->safeString(rawOrder, std::string("side"));
    ccxt::any side = this->parseOrderSide(rawSide);
    ccxt::any price = this->safeString(rawOrder, std::string("price"));
    ccxt::any amountKey =
        (isTrue((isEqual(side, std::string("buy"))))
             ? ccxt::any(std::string("takerAmount"))
             : ccxt::any(std::string("makerAmount"))); // todo check
    ccxt::any amount = this->safeString(rawOrder, amountKey);
    ccxt::any remaining =
        this->safeString(rawOrder, std::string("remainingSize"));
    ccxt::any datetime = this->safeString(rawOrder, std::string("createdAt"));
    ccxt::any ts = this->parse8601(datetime);
    ccxt::any timeInForce = this->safeString2(rawOrder, std::string("type"),
                                              std::string("orderType"));
    ccxt::any type = ccxt::any{};
    if (isTrue(isEqual(timeInForce, std::string("GTC")))) {
      type = std::string("limit");
    } else if (isTrue(isEqual(timeInForce, std::string("FAK")))) {
      type = std::string("market");
    }
    ccxt::any rawStatus = this->safeString(rawOrder, std::string("status"));
    ccxt::any execution = this->safeDict(data, std::string("execution"));
    ccxt::any fee = ccxt::any{};
    ccxt::any filled = ccxt::any{};
    ccxt::any cost = ccxt::any{};
    if (isTrue(!isEqual(execution, ccxt::any{}))) {
      rawStatus = this->safeString(execution, std::string("settlementStatus"));
      ccxt::any totals = this->safeDict(execution, std::string("totalsRaw"));
      cost = this->safeString(totals, std::string("usdGross"));
      filled = this->safeString(totals, std::string("contractsGross"));
      ccxt::any feeCurrency = std::string("USDC");
      ccxt::any feeCost = this->safeString(totals, std::string("usdFee"));
      if (isTrue(isEqual(side, std::string("buy")))) {
        feeCurrency = outcomeSymbol;
        feeCost = this->safeString(totals, std::string("contractsFee"));
      }
      fee = ccxt::dict{
          {std::string("cost"), this->parseNumber(this->applyScale(feeCost))},
          {std::string("currency"), feeCurrency},
      };
    }
    return this->safePredictionOrder(ccxt::dict{
        {std::string("id"), id},
        {std::string("clientOrderId"), ccxt::any{}},
        {std::string("info"), order},
        {std::string("timestamp"), ts},
        {std::string("datetime"), datetime},
        {std::string("lastTradeTimestamp"), ccxt::any{}},
        {std::string("status"), this->parseOrderStatus(rawStatus)},
        {std::string("outcome"), outcomeSymbol},
        {std::string("outcomeId"),
         this->safeString(mkt, std::string("outcomeId"))},
        {std::string("label"), this->safeString(mkt, std::string("label"))},
        {std::string("market"), this->safeString(mkt, std::string("market"))},
        {std::string("type"), type},
        {std::string("timeInForce"), this->parseOrderTimeInForce(timeInForce)},
        {std::string("postOnly"), ccxt::any{}},
        {std::string("side"), side},
        {std::string("price"), price},
        {std::string("stopPrice"), ccxt::any{}},
        {std::string("triggerPrice"), ccxt::any{}},
        {std::string("average"), ccxt::any{}},
        {std::string("amount"), this->applyScale(amount)},
        {std::string("cost"), this->applyScale(cost)},
        {std::string("filled"), this->applyScale(filled)},
        {std::string("remaining"), this->applyScale(remaining)},
        {std::string("fee"), fee},
        {std::string("trades"), ccxt::list{}},
    });
  }

  /**
   * @ignore
   * @method
   * @name limitless#parseOrderStatus
   * @description maps an order status string to the CCXT unified status
   * vocabulary
   * @param {string} status the raw limitless order status
   * @returns {string} the unified order status
   */
  virtual ccxt::any parseOrderStatus(ccxt::any status) {
    ccxt::any statuses = ccxt::dict{
        {std::string("LIVE"), std::string("open")},
        {std::string("MATCHED"), std::string("closed")},
        {std::string("PENDING"), std::string("pending")},
        {std::string("MINED"), std::string("closed")},
        {std::string("CONFIRMED"), std::string("closed")},
        {std::string("FAILED"), std::string("rejected")},
    };
    return this->safeString(statuses, status, status);
  }

  /**
   * @ignore
   * @method
   * @name limitless#parseOrderTimeInForce
   * @description maps an order time in force string to the CCXT unified type
   * vocabulary
   * @param {string} timeInForce the raw limitless time in force
   * @returns {string} the unified time in force
   */
  virtual ccxt::any parseOrderTimeInForce(ccxt::any timeInForce) {
    ccxt::any timeInForces = ccxt::dict{
        {std::string("FAK"), std::string("FOK")},
    };
    return this->safeString(timeInForces, timeInForce, timeInForce);
  }

  /**
   * @ignore
   * @method
   * @name limitless#parseOrderSide
   * @description maps an order side string to the CCXT unified side vocabulary
   * @param {string} side the raw limitless order side
   * @returns {string} the unified order side
   */
  virtual ccxt::any parseOrderSide(ccxt::any side) {
    ccxt::any sides = ccxt::dict{
        {std::string("BUY"), std::string("buy")},
        {std::string("SELL"), std::string("sell")},
        {std::string("0"), std::string("buy")},
        {std::string("1"), std::string("sell")},
    };
    return this->safeString(sides, side, side);
  }

  virtual ccxt::any applyScale(ccxt::any amount, ccxt::any multiply = false) {
    ccxt::any decimals =
        this->safeInteger(this->options, std::string("usdcDecimals"), 6);
    ccxt::any scale = this->numberToString(mathPow(10, decimals));
    if (isTrue(multiply)) {
      return ccxt::Precise::stringMul(amount, scale);
    } else {
      return ccxt::Precise::stringDiv(amount, scale);
    }
  }

  ccxt::any parseAccount(ccxt::any account) override {
    ccxt::any accountId = this->safeString(account, std::string("id"));
    return ccxt::dict{
        {std::string("id"), accountId},
        {std::string("type"), ccxt::any{}},
        {std::string("code"), ccxt::any{}},
        {std::string("info"), account},
    };
  }

  /**
   * @method
   * @name limitless#fetchAccounts
   * @description query for account id and info
   * @see
   * https://docs.limitless.exchange/api-reference/portfolio/get-current-profile
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [account structures]
   */
  std::shared_future<ccxt::any>
  fetchAccounts(ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        ccxt::any response = awaitValue(
                            this->limitlessPrivateGetProfilesMe(params));
                        ccxt::any responseList = ccxt::list{response};
                        return this->parseAccounts(responseList);
                      })
        .share();
  }

  /**
   * @method
   * @name limitless#createOrder
   * @description places a limit or market order on limitless for the given
   * outcome token
   * @see https://docs.limitless.exchange/api-reference/orders/create-order
   * @param {string} outcome outcome, e.g. "TRUMP_OUT:YES"
   * @param {string} type 'limit' or 'market'
   * @param {string} side 'buy' or 'sell'
   * @param {float} amount amount of outcome tokens
   * @param {float} [price] limit price (0–1 range)
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [prediction order
   * structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
   */
  std::shared_future<ccxt::any>
  createOrder(ccxt::any outcome, ccxt::any type, ccxt::any side,
              ccxt::any amount, ccxt::any price = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 ccxt::any accounts = awaitValue(this->loadAccounts());
                 awaitValue(this->loadOutcome(outcome));
                 ccxt::any outcomeObj = this->outcome(outcome);
                 ccxt::any account = this->safeDict(accounts, 0);
                 ccxt::any accountInfo =
                     this->safeDict(account, std::string("info"));
                 // the trade wallet is chosen by `tradeWalletOption`:
                 // 'smartWallet' profiles trade through the `smartWallet`
                 // address, plain 'eoa' profiles trade directly from `account`.
                 // the smartWallet field can stay populated after switching to
                 // eoa, so key off the option here
                 ccxt::any tradeWalletOption = this->safeString(
                     accountInfo, std::string("tradeWalletOption"));
                 ccxt::any usesSmartWallet =
                     (isEqual(tradeWalletOption, std::string("smartWallet")));
                 ccxt::any walletFromAccount =
                     (isTrue((usesSmartWallet))
                          ? ccxt::any(this->safeString(
                                accountInfo, std::string("smartWallet")))
                          : ccxt::any(this->safeString(
                                accountInfo, std::string("account"))));
                 ccxt::any maker =
                     (isTrue((!isEqual(this->walletAddress, std::string(""))))
                          ? ccxt::any(this->walletAddress)
                          : ccxt::any(walletFromAccount));
                 ccxt::any makerparamsVariable = this->handleOptionAndParams(
                     params, std::string("createOrder"), std::string("maker"),
                     maker);
                 maker = ::getValue(makerparamsVariable, 0);
                 params = ::getValue(makerparamsVariable, 1);
                 try {
                   this->checkAddress(maker);
                 } catch (const std::exception &e) {
                   throw InvalidAddress(toString(add(
                       this->id,
                       std::string(" createOrder requires a valid maker "
                                   "address. Set the \"maker\" parameter to a "
                                   "valid address or set the \"walletAddress\" "
                                   "property in the constructor options."))));
                 }
                 // when the profile trades through a smart wallet the order
                 // must be signedFlag by the linked embedded (owner) wallet,
                 // not by the smart wallet itself
                 ccxt::any embeddedAddress = this->safeString(
                     accountInfo, std::string("embeddedAccount"));
                 ccxt::any hasEmbedded =
                     (!isEqual(embeddedAddress, ccxt::any{}));
                 ccxt::any isSmartWallet =
                     isTrue(usesSmartWallet) && isTrue(hasEmbedded);
                 ccxt::any signer = maker;
                 if (isTrue(isSmartWallet)) {
                   signer = embeddedAddress;
                 }
                 ccxt::any signerparamsVariable = this->handleOptionAndParams(
                     params, std::string("createOrder"), std::string("signer"),
                     signer);
                 signer = ::getValue(signerparamsVariable, 0);
                 params = ::getValue(signerparamsVariable, 1);
                 try {
                   this->checkAddress(signer);
                 } catch (const std::exception &e) {
                   throw InvalidAddress(toString(add(
                       this->id,
                       std::string(" createOrder requires a valid signer "
                                   "address. Set the \"signer\" parameter to a "
                                   "valid address or set the \"walletAddress\" "
                                   "property in the constructor options."))));
                 }
                 ccxt::any taker = this->safeString(
                     this->options, std::string("nullAddress"),
                     std::string("0x0000000000000000000000000000000000000000"));
                 ccxt::any takerparamsVariable = this->handleOptionAndParams(
                     params, std::string("createOrder"), std::string("taker"),
                     taker);
                 taker = ::getValue(takerparamsVariable, 0);
                 params = ::getValue(takerparamsVariable, 1);
                 try {
                   this->checkAddress(taker);
                 } catch (const std::exception &e) {
                   throw InvalidAddress(toString(add(
                       this->id,
                       std::string(" createOrder requires a valid taker "
                                   "address. Set the \"taker\" parameter to a "
                                   "valid address or set the \"nullAddress\" "
                                   "property in the constructor options."))));
                 }
                 ccxt::any nonce = this->milliseconds();
                 ccxt::any sides = ccxt::dict{
                     {std::string("buy"), 0},
                     {std::string("sell"), 1},
                 };
                 if (isTrue(isEqual(side, ccxt::any{}))) {
                   throw ArgumentsRequired(toString(
                       add(this->id,
                           std::string(
                               " createOrder() requires a side argument"))));
                 }
                 ccxt::any sideValue =
                     this->safeInteger(sides, toLowerCase(side));
                 ccxt::any rank =
                     this->safeDict(accountInfo, std::string("rank"));
                 // signatureType: 0 = EOA, 2 = smart-wallet (the embedded owner
                 // signs on behalf of the safe)
                 ccxt::any signatureType =
                     (isTrue(isSmartWallet) ? ccxt::any(2) : ccxt::any(0));
                 ccxt::any signatureTypeparamsVariable =
                     this->handleOptionAndParams(
                         params, std::string("createOrder"),
                         std::string("signatureType"), signatureType);
                 signatureType = ::getValue(signatureTypeparamsVariable, 0);
                 params = ::getValue(signatureTypeparamsVariable, 1);
                 ccxt::any signRequest = ccxt::dict{
                     {std::string("salt"), nonce},
                     {std::string("maker"), maker},
                     {std::string("signer"), signer},
                     {std::string("taker"), taker},
                     {std::string("tokenId"),
                      ::getValue(outcomeObj, std::string("outcomeId"))},
                     {std::string("nonce"), 0},
                     {std::string("feeRateBps"),
                      this->safeInteger(rank, std::string("feeRateBps"), 0)},
                     {std::string("side"), sideValue},
                     {std::string("signatureType"), signatureType},
                 };
                 // the contract expects expiration as a uint256; non-zero
                 // values are rejected by the API (GTC orders use 0)
                 ccxt::any expirationInt =
                     this->safeInteger(params, std::string("expiration"));
                 if (isTrue(!isEqual(expirationInt, ccxt::any{}))) {
                   params = this->omit(params, std::string("expiration"));
                   ::setValue(signRequest, std::string("expiration"),
                              this->numberToString(expirationInt));
                 } else {
                   ::setValue(signRequest, std::string("expiration"),
                              std::string("0"));
                 }
                 ccxt::any amountString = this->numberToString(amount);
                 ccxt::any priceString = this->numberToString(price);
                 ccxt::any makerAmount = ccxt::any{};
                 ccxt::any takerAmount = ccxt::any{};
                 ccxt::any isMarket = isEqual(type, std::string("market"));
                 ccxt::any postOnly = false;
                 ccxt::any postOnlyparamsVariable =
                     this->handlePostOnly(isMarket, false, params);
                 postOnly = ::getValue(postOnlyparamsVariable, 0);
                 params = ::getValue(postOnlyparamsVariable, 1);
                 ccxt::any timeInForce =
                     this->safeString(params, std::string("timeInForce"));
                 params = this->omit(params, std::string("timeInForce"));
                 if (isTrue(isEqual(timeInForce, ccxt::any{}))) {
                   timeInForce =
                       (isTrue(isMarket) ? ccxt::any(std::string("FOK"))
                                         : ccxt::any(std::string("GTC")));
                 }
                 ccxt::any marketSymbol =
                     this->safeString(outcomeObj, std::string("market"));
                 if (isTrue(isTrue(isMarket) &&
                            isTrue((isEqual(side, std::string("buy")))))) {
                   ccxt::any createMarketBuyOrderRequiresPrice = true;
                   ccxt::any createMarketBuyOrderRequiresPriceparamsVariable =
                       this->handleOptionAndParams(
                           params, std::string("createOrder"),
                           std::string("createMarketBuyOrderRequiresPrice"),
                           true);
                   createMarketBuyOrderRequiresPrice = ::getValue(
                       createMarketBuyOrderRequiresPriceparamsVariable, 0);
                   params = ::getValue(
                       createMarketBuyOrderRequiresPriceparamsVariable, 1);
                   ccxt::any cost =
                       this->safeNumber(params, std::string("cost"));
                   params = this->omit(params, std::string("cost"));
                   if (isTrue(createMarketBuyOrderRequiresPrice)) {
                     if (isTrue(isTrue((isEqual(price, ccxt::any{}))) &&
                                isTrue((isEqual(cost, ccxt::any{}))))) {
                       throw InvalidOrder(toString(add(
                           this->id,
                           std::string(
                               " createOrder() requires the price argument for "
                               "market buy orders to calculate the total cost "
                               "to spend (amount * price), alternatively set "
                               "the createMarketBuyOrderRequiresPrice option "
                               "or param to false and pass the cost to spend "
                               "in the amount argument"))));
                     } else {
                       ccxt::any quoteAmount = this->parseToNumeric(
                           ccxt::Precise::stringMul(amountString, priceString));
                       ccxt::any costRequest =
                           (isTrue((!isEqual(cost, ccxt::any{})))
                                ? ccxt::any(cost)
                                : ccxt::any(quoteAmount));
                       makerAmount = this->costToPredictionPrecision(
                           outcome, costRequest);
                     }
                   } else {
                     makerAmount =
                         this->costToPredictionPrecision(outcome, amount);
                   }
                 } else if (isTrue(isMarket)) {
                   makerAmount =
                       this->amountToPredictionPrecision(outcome, amount);
                 } else {
                   ccxt::any calculatedCost =
                       ccxt::Precise::stringMul(amountString, priceString);
                   if (isTrue(isEqual(side, std::string("buy")))) {
                     makerAmount = this->costToPredictionPrecision(
                         outcome, calculatedCost);
                     takerAmount =
                         this->amountToPredictionPrecision(outcome, amount);
                   } else {
                     makerAmount =
                         this->amountToPredictionPrecision(outcome, amount);
                     takerAmount = this->costToPredictionPrecision(
                         outcome, calculatedCost);
                   }
                 }
                 // amounts must be integers (uint256): parseNumber yields a
                 // float that the Python EIP-712 encoder rejects
                 ::setValue(
                     signRequest, std::string("makerAmount"),
                     this->parseToInt(this->applyScale(makerAmount, true)));
                 ::setValue(signRequest, std::string("takerAmount"),
                            (isTrue(isMarket)
                                 ? ccxt::any(1)
                                 : ccxt::any(this->parseToInt(
                                       this->applyScale(takerAmount, true)))));
                 ccxt::any signature =
                     this->signOrderRequest(signRequest, marketSymbol);
                 ::setValue(signRequest, std::string("signature"), signature);
                 // price is an unsigned hint required by the API for GTC/FAK
                 // orders (not part of the EIP-712 struct)
                 if (isTrue(!isTrue(isMarket) &&
                            isTrue((!isEqual(price, ccxt::any{}))))) {
                   ::setValue(signRequest, std::string("price"),
                              this->parseNumber(priceString));
                 }
                 ccxt::any slug = this->safeString(
                     ::getValue(outcomeObj, std::string("info")),
                     std::string("slug"));
                 ccxt::any request = ccxt::dict{
                     {std::string("ownerId"),
                      this->safeInteger(account, std::string("id"))},
                     {std::string("order"), signRequest},
                     {std::string("marketSlug"), slug},
                     {std::string("orderType"), timeInForce},
                 };
                 if (isTrue(postOnly)) {
                   ::setValue(request, std::string("postOnly"), postOnly);
                 }
                 ccxt::any response =
                     awaitValue(this->limitlessPrivatePostOrders(
                         this->extend(request, params)));
                 ccxt::any parsedOrder =
                     this->parsePredictionOrder(response, outcomeObj);
                 // the create-order response omits a status field; a freshly
                 // accepted order is open
                 if (isTrue(
                         isEqual(::getValue(parsedOrder, std::string("status")),
                                 ccxt::any{}))) {
                   ::setValue(parsedOrder, std::string("status"),
                              std::string("open"));
                 }
                 return parsedOrder;
               })
        .share();
  }

  virtual ccxt::any signOrderRequest(ccxt::any signRequest,
                                     ccxt::any marketSymbol) {
    this->checkRequiredCredentials();
    if (isTrue(isEqual(this->privateKey, ccxt::any{}))) {
      throw ArgumentsRequired(toString(
          add(this->id,
              std::string(" createOrder() requires a privateKey (the "
                          "embedded/trading wallet key) to sign orders"))));
    }
    ccxt::any market = this->market(marketSymbol);
    ccxt::any info = this->safeDict(market, std::string("info"));
    ccxt::any venue = this->safeDict(info, std::string("venue"));
    ccxt::any exchange = this->safeString(venue, std::string("exchange"));
    ccxt::any domain = ccxt::dict{
        {std::string("chainId"), 8453},
        {std::string("name"), std::string("Limitless CTF Exchange")},
        {std::string("verifyingContract"), exchange},
        {std::string("version"), std::string("1")},
    };
    ccxt::any messageTypes = ccxt::dict{
        {std::string("Order"),
         ccxt::list{ccxt::dict{
                        {std::string("name"), std::string("salt")},
                        {std::string("type"), std::string("uint256")},
                    },
                    ccxt::dict{
                        {std::string("name"), std::string("maker")},
                        {std::string("type"), std::string("address")},
                    },
                    ccxt::dict{
                        {std::string("name"), std::string("signer")},
                        {std::string("type"), std::string("address")},
                    },
                    ccxt::dict{
                        {std::string("name"), std::string("taker")},
                        {std::string("type"), std::string("address")},
                    },
                    ccxt::dict{
                        {std::string("name"), std::string("tokenId")},
                        {std::string("type"), std::string("uint256")},
                    },
                    ccxt::dict{
                        {std::string("name"), std::string("makerAmount")},
                        {std::string("type"), std::string("uint256")},
                    },
                    ccxt::dict{
                        {std::string("name"), std::string("takerAmount")},
                        {std::string("type"), std::string("uint256")},
                    },
                    ccxt::dict{
                        {std::string("name"), std::string("expiration")},
                        {std::string("type"), std::string("uint256")},
                    },
                    ccxt::dict{
                        {std::string("name"), std::string("nonce")},
                        {std::string("type"), std::string("uint256")},
                    },
                    ccxt::dict{
                        {std::string("name"), std::string("feeRateBps")},
                        {std::string("type"), std::string("uint256")},
                    },
                    ccxt::dict{
                        {std::string("name"), std::string("side")},
                        {std::string("type"), std::string("uint8")},
                    },
                    ccxt::dict{
                        {std::string("name"), std::string("signatureType")},
                        {std::string("type"), std::string("uint8")},
                    }}},
    };
    ccxt::any msg =
        this->ethEncodeStructuredData(domain, messageTypes, signRequest);
    return this->signMessage(msg, this->privateKey);
  }

  virtual ccxt::any hashMessage(ccxt::any message) {
    return add(std::string("0x"),
               this->hash(message, keccak, std::string("hex")));
  }

  virtual ccxt::any signHash(ccxt::any hash, ccxt::any privateKey) {
    ccxt::any signature =
        ecdsa(slice(hash, -64, ccxt::any{}),
              slice(privateKey, -64, ccxt::any{}), secp256k1, ccxt::any{});
    ccxt::any r = ::getValue(signature, std::string("r"));
    ccxt::any s = ::getValue(signature, std::string("s"));
    ccxt::any v = this->intToBase16(
        this->sum(27, ::getValue(signature, std::string("v"))));
    ccxt::any rPadded = padStart(r, 64, std::string("0"));
    ccxt::any sPadded = padStart(s, 64, std::string("0"));
    ccxt::any result = add(add(add(std::string("0x"), rPadded), sPadded), v);
    return toLowerCase(result);
  }

  virtual ccxt::any signMessage(ccxt::any message, ccxt::any privateKey) {
    return this->signHash(this->hashMessage(message),
                          slice(privateKey, -64, ccxt::any{}));
  }

  ccxt::any signEvmTransaction(ccxt::any tx, ccxt::any privateKey) override {
    // builds and signs an EIP-1559 (type 0x02) transaction, returning the
    // signedFlag raw tx hex
    ccxt::any accessList = this->rlpEncodeList(ccxt::list{});
    ccxt::any fields = ccxt::list{
        this->rlpEncodeBytes(
            this->intToRlpHex(this->safeInteger(tx, std::string("chainId")))),
        this->rlpEncodeBytes(
            this->hexToRlpBytes(this->safeString(tx, std::string("nonce")))),
        this->rlpEncodeBytes(this->hexToRlpBytes(
            this->safeString(tx, std::string("maxPriorityFeePerGas")))),
        this->rlpEncodeBytes(this->hexToRlpBytes(
            this->safeString(tx, std::string("maxFeePerGas")))),
        this->rlpEncodeBytes(
            this->hexToRlpBytes(this->safeString(tx, std::string("gasLimit")))),
        this->rlpEncodeBytes(
            this->remove0xPrefix(this->safeString(tx, std::string("to")))),
        this->rlpEncodeBytes(this->hexToRlpBytes(
            this->safeString(tx, std::string("value"), std::string("0x0")))),
        this->rlpEncodeBytes(this->remove0xPrefix(
            this->safeString(tx, std::string("data"), std::string("0x")))),
        accessList};
    ccxt::any payload = add(std::string("02"), this->rlpEncodeList(fields));
    ccxt::any hashHex =
        this->hash(this->base16ToBinary(payload), keccak, std::string("hex"));
    ccxt::any signature = ecdsa(hashHex, this->remove0xPrefix(privateKey),
                                secp256k1, ccxt::any{});
    ccxt::any rHex = this->safeString(signature, std::string("r"));
    ccxt::any sHex = this->safeString(signature, std::string("s"));
    rHex = this->padHexToEven(rHex);
    sHex = this->padHexToEven(sHex);
    ccxt::any yParity = this->safeInteger(signature, std::string("v"));
    ccxt::any signedFields = ccxt::list{};
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(fields));
         postFixIncrement(i)) {
      arrayPush(signedFields, ::getValue(fields, i));
    }
    arrayPush(signedFields, this->rlpEncodeBytes(this->intToRlpHex(yParity)));
    arrayPush(signedFields, this->rlpEncodeBytes(rHex));
    arrayPush(signedFields, this->rlpEncodeBytes(sHex));
    return add(std::string("0x02"), this->rlpEncodeList(signedFields));
  }

  /**
   * @method
   * @name limitless#approve
   * @description sets the on-chain ERC20 collateral (USDC) allowance for the
   * limitless exchange contract on Base, which is required before an EOA maker
   * can place orders ("Insufficient collateral allowance" otherwise). Sends a
   * real on-chain transaction signedFlag with the privateKey and waits for the
   * receipt
   * @param {object} [params] extra parameters
   * @param {string} [params.token] the collateral token address (default USDC
   * on Base)
   * @param {string} [params.spender] the exchange contract to approve (default
   * the limitless CTF exchange); read from a market's venue when omitted
   * @param {string} [params.owner] the token holder address (default
   * this.walletAddress or the address derived from the privateKey)
   * @param {float} [params.amount] the allowance in USDC (default: unlimited /
   * maxUint256)
   * @param {string} [params.rpcUrl] the Base RPC url to broadcast through
   * @param {string} [params.gasLimit] gas limit hex for the approve tx (default
   * '0x186a0')
   * @returns {object} the transaction receipt
   */
  virtual std::shared_future<ccxt::any>
  approve(ccxt::any params = ccxt::dict{}) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 this->checkRequiredCredentials();
                 if (isTrue(isEqual(this->privateKey, ccxt::any{}))) {
                   throw ArgumentsRequired(toString(
                       add(this->id,
                           std::string(" approve() requires a privateKey to "
                                       "sign the on-chain transaction"))));
                 }
                 ccxt::any rpcUrl = this->safeString(
                     params, std::string("rpcUrl"),
                     this->safeString(this->options, std::string("rpcUrl")));
                 ccxt::any chainId = this->safeInteger(
                     this->options, std::string("chainId"), 8453);
                 ccxt::any token = this->safeString(
                     params, std::string("token"),
                     this->safeString(this->options,
                                      std::string("collateralAddress")));
                 ccxt::any spender = this->safeString(
                     params, std::string("spender"),
                     this->safeString(this->options,
                                      std::string("exchangeAddress")));
                 ccxt::any owner = this->safeString(
                     params, std::string("owner"), this->walletAddress);
                 if (isTrue(isEqual(owner, ccxt::any{}))) {
                   owner = this->ethGetAddressFromPrivateKey(this->privateKey);
                 }
                 ccxt::any gasLimit = this->safeString(
                     params, std::string("gasLimit"), std::string("0x186a0"));
                 ccxt::any maxUint =
                     std::string("fffffffffffffffffffffffffffffffffffffffffffff"
                                 "fffffffffffffffffff");
                 ccxt::any amountHex = maxUint;
                 ccxt::any amount =
                     this->safeString(params, std::string("amount"));
                 if (isTrue(!isEqual(amount, ccxt::any{}))) {
                   ccxt::any decimals = this->safeInteger(
                       this->options, std::string("usdcDecimals"), 6);
                   // scale the human USDC amount to base units (amount /
                   // 10^-decimals = amount * 10^decimals)
                   ccxt::any scaled = ccxt::Precise::stringDiv(
                       amount,
                       this->parsePrecision(this->numberToString(decimals)));
                   ccxt::any amountInt = this->parseToInt(scaled);
                   ccxt::any amountBase16 = this->intToBase16(amountInt);
                   amountHex = padStart(amountBase16, 64, std::string("0"));
                 }
                 // approve(spender, amount) -> selector 0x095ea7b3
                 ccxt::any approveData = add(add(std::string("0x095ea7b3"),
                                                 this->padHexAddress(spender)),
                                             amountHex);
                 ccxt::any txHash = awaitValue(this->sendEvmTransaction(
                     rpcUrl, chainId, owner, token, std::string("0x0"),
                     approveData, gasLimit));
                 return awaitValue(
                     this->waitForTransactionReceipt(rpcUrl, txHash));
               })
        .share();
  }

  /**
   * @method
   * @name limitless#cancelOrder
   * @description cancels a single open order by id
   * @see https://docs.limitless.exchange/api-reference/orders/cancel-order
   * @param {string} id order id
   * @param {string} [outcome] outcome, e.g. "TRUMP_OUT:YES"
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [prediction order
   * structure](https://docs.ccxt.com/#/?id=prediction-order-structure)
   */
  std::shared_future<ccxt::any>
  cancelOrder(ccxt::any id, ccxt::any outcome = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(!isEqual(outcome, ccxt::any{}))) {
                   awaitValue(this->loadOutcome(outcome));
                 }
                 ccxt::any request = ccxt::dict{
                     {std::string("order_id"), id},
                 };
                 ccxt::any response =
                     awaitValue(this->limitlessPrivateDeleteOrdersOrderId(
                         this->extend(request, params)));
                 // the delete response carries no order body, so backfill the
                 // id and the resulting status
                 ccxt::any order = this->parsePredictionOrder(response);
                 if (isTrue(isEqual(::getValue(order, std::string("id")),
                                    ccxt::any{}))) {
                   ::setValue(order, std::string("id"), id);
                 }
                 if (isTrue(isEqual(::getValue(order, std::string("status")),
                                    ccxt::any{}))) {
                   ::setValue(order, std::string("status"),
                              std::string("canceled"));
                 }
                 return order;
               })
        .share();
  }

  /**
   * @method
   * @name limitless#redeem
   * @description redeem a resolved winning position back to collateral (gasless
   * — the operator settles on-chain)
   * @see https://docs.limitless.exchange/api-reference/portfolio/redeem
   * @param {string} [outcome] a unified outcome on the resolved market to
   * redeem (used to resolve the market conditionId)
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.conditionId] the CTF condition id (bytes32 hex) to
   * redeem directly, instead of resolving it from an outcome
   * @returns {object} the raw redemption response
   */
  virtual std::shared_future<ccxt::any>
  redeem(ccxt::any outcome = ccxt::any{}, ccxt::any params = ccxt::dict{}) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 ccxt::any conditionId =
                     this->safeString2(params, std::string("conditionId"),
                                       std::string("condition_id"));
                 if (isTrue(isEqual(conditionId, ccxt::any{}))) {
                   if (isTrue(isEqual(outcome, ccxt::any{}))) {
                     throw ArgumentsRequired(toString(add(
                         this->id, std::string(" redeem() requires an outcome "
                                               "or a params.conditionId"))));
                   }
                   awaitValue(this->loadOutcome(outcome));
                   ccxt::any outcomeObj = this->outcome(outcome);
                   conditionId = this->safeString(
                       this->safeDict(outcomeObj, std::string("info"),
                                      ccxt::dict{}),
                       std::string("conditionId"));
                 }
                 if (isTrue(isEqual(conditionId, ccxt::any{}))) {
                   throw ArgumentsRequired(toString(
                       add(this->id,
                           std::string(" redeem() could not resolve the market "
                                       "conditionId - pass params.conditionId "
                                       "(a bytes32 hex string)"))));
                 }
                 ccxt::any request = ccxt::dict{
                     {std::string("conditionId"), conditionId},
                 };
                 ccxt::any rest = this->omit(
                     params, ccxt::list{std::string("conditionId"),
                                        std::string("condition_id")});
                 ccxt::any response =
                     awaitValue(this->limitlessPrivatePostPortfolioRedeem(
                         this->extend(request, rest)));
                 return ccxt::dict{
                     {std::string("info"), response},
                     {std::string("id"), conditionId},
                     {std::string("conditionId"), conditionId},
                 };
               })
        .share();
  }

  /**
   * @method
   * @name limitless#cancelOrders
   * @description cancel multiple orders at the same time
   * @see https://docs.limitless.exchange/api-reference/trading/cancel-batch
   * @param {string[]} ids order ids
   * @param {string} [outcome] unified market outcome, default is undefined
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [prediction order
   * structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
   */
  std::shared_future<ccxt::any>
  cancelOrders(ccxt::any ids, ccxt::any outcome = ccxt::any{},
               ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(!isEqual(outcome, ccxt::any{}))) {
                   awaitValue(this->loadOutcome(outcome));
                 }
                 ccxt::any request = ccxt::dict{
                     {std::string("orderIds"), ids},
                 };
                 ccxt::any response =
                     awaitValue(this->limitlessPrivatePostOrdersCancelBatch(
                         this->extend(request, params)));
                 ccxt::any canceled = this->safeList(
                     response, std::string("canceled"), ccxt::list{});
                 ccxt::any failed = this->safeList(
                     response, std::string("failed"), ccxt::list{});
                 ccxt::any failedLethgn = getArrayLength(failed);
                 if (isTrue(isGreaterThan(failedLethgn, 0))) {
                   ccxt::any message = this->json(response);
                   ccxt::any feedback =
                       add(add(this->id, std::string(" cancelOrders failed: ")),
                           message);
                   throw OrderNotFound(toString(feedback));
                 }
                 return this->parsePredictionOrders(canceled);
               })
        .share();
  }

  /**
   * @method
   * @name limitless#cancelAllOrders
   * @description cancels all open orders for one market slug
   * @see https://docs.limitless.exchange/api-reference/orders/cancel-all-orders
   * @param {string} [outcome] outcome, e.g. "TRUMP_OUT:YES"
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.slug] the market slug to cancel all orders for
   * @returns {object[]} a list of [prediction order
   * structures](https://docs.ccxt.com/#/?id=prediction-order-structure)
   */
  virtual std::shared_future<ccxt::any>
  cancelAllOrders(ccxt::any outcome = ccxt::any{},
                  ccxt::any params = ccxt::dict{}) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(!isEqual(outcome, ccxt::any{}))) {
                   ccxt::any warn = true;
                   ccxt::any warnparamsVariable = this->handleOptionAndParams(
                       params, std::string("cancelAllOrders"),
                       std::string("warnOnCancelAllOrdersWithOutcome"), warn);
                   warn = ::getValue(warnparamsVariable, 0);
                   params = ::getValue(warnparamsVariable, 1);
                   if (isTrue(warn)) {
                     throw BadRequest(toString(add(
                         this->id,
                         std::string(
                             " cancelAllOrders cancels all orders for entire "
                             "slug (both YES and NO outcomes). Please provide "
                             "params.slug to specify the slug, or set the "
                             "warnOnCancelAllOrdersWithOutcome option to false "
                             "to suppress this warning message."))));
                   }
                 }
                 ccxt::any request = ccxt::dict{};
                 ccxt::any slug = this->safeString(params, std::string("slug"));
                 if (isTrue(!isEqual(outcome, ccxt::any{}))) {
                   ccxt::any outcomeObj =
                       awaitValue(this->loadOutcome(outcome));
                   ::setValue(request, std::string("slug"),
                              this->safeString(
                                  ::getValue(outcomeObj, std::string("info")),
                                  std::string("slug")));
                 } else if (isTrue(isEqual(slug, ccxt::any{}))) {
                   throw ArgumentsRequired(toString(add(
                       this->id,
                       std::string(" cancelAllOrders requires either an "
                                   "outcome argument or a slug parameter"))));
                 }
                 ccxt::any response =
                     awaitValue(this->limitlessPrivateDeleteOrdersAllSlug(
                         this->extend(request, params)));
                 //
                 //     {
                 //         "message": "Orders canceled successfully"
                 //     }
                 //
                 return ccxt::list{this->safePredictionOrder(ccxt::dict{
                     {std::string("info"), response},
                 })};
               })
        .share();
  }

  /**
   * @method
   * @name limitless#fetchMyTrades
   * @description fetch all trades made by the user
   * @see https://docs.limitless.exchange/api-reference/trades/get-trades
   * @param {string} [outcome] outcome, e.g. "TRUMP_OUT:YES"
   * @param {int} [since] the earliest time in ms to fetch trades for
   * @param {int} [limit] the maximum number of trades structures to retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [prediction trade
   * structures](https://docs.ccxt.com/#/?id=prediction-trade-structure)
   */
  std::shared_future<ccxt::any>
  fetchMyTrades(ccxt::any outcome = ccxt::any{}, ccxt::any since = ccxt::any{},
                ccxt::any limit = ccxt::any{},
                ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 // resolve the handle for the final filter — the caller may
                 // have passed an outcomeId
                 ccxt::any outcomeSymbol = outcome;
                 if (isTrue(!isEqual(outcome, ccxt::any{}))) {
                   ccxt::any outcomeObj =
                       awaitValue(this->loadOutcome(outcome));
                   outcomeSymbol =
                       this->safeString(outcomeObj, std::string("outcome"));
                 }
                 ccxt::any paginate = false;
                 ccxt::any maxLimit = 100;
                 ccxt::any paginateparamsVariable = this->handleOptionAndParams(
                     params, std::string("fetchMyTrades"),
                     std::string("paginate"), paginate);
                 paginate = ::getValue(paginateparamsVariable, 0);
                 params = ::getValue(paginateparamsVariable, 1);
                 if (isTrue(paginate)) {
                   params = this->omit(params, std::string("paginate"));
                   return awaitValue(this->fetchPaginatedCallCursor(
                       std::string("fetchMyTrades"), outcome, since, limit,
                       params, std::string("nextCursor"), std::string("cursor"),
                       ccxt::any{}, maxLimit));
                 }
                 ccxt::any request = ccxt::dict{};
                 if (isTrue(!isEqual(limit, ccxt::any{}))) {
                   ::setValue(request, std::string("limit"),
                              mathMin(limit, maxLimit));
                 }
                 ccxt::any response =
                     awaitValue(this->limitlessPrivateGetPortfolioHistory(
                         this->extend(request, params)));
                 //
                 //     {
                 //         "data": [
                 //             {
                 //                 "action": "loss",
                 //                 "blockTimestamp": 1778144400,
                 //                 "collateralAmount": "2",
                 //                 "collateralSymbol": "USDC",
                 //                 "collateralToken": "7",
                 //                 "conditionId":
                 //                 "0x0c61db7449dd8f8c81cd856f53d4186cf30888e27eb025d10c7908fa94ba736e",
                 //                 "market": {
                 //                     "closed": true,
                 //                     "collateral": {
                 //                     "symbol": "USDC",
                 //                     "id": "7",
                 //                     "decimals": 6},
                 //                     "group": null,
                 //                     "condition_id":
                 //                     "0x0c61db7449dd8f8c81cd856f53d4186cf30888e27eb025d10c7908fa94ba736e",
                 //                     "conditionId":
                 //                     "0x0c61db7449dd8f8c81cd856f53d4186cf30888e27eb025d10c7908fa94ba736e",
                 //                     "funding": "0",
                 //                     "id": "117515",
                 //                     "slug":
                 //                     "doge-up-or-down-1-hour-1778140801775",
                 //                     "title": "DOGE Up or Down - 1 hour",
                 //                     "expirationDate":
                 //                     "2026-05-07T09:00:00.000Z"
                 //                 },
                 //                 "outcomeIndex": 1,
                 //                 "pnl": "-2000000",
                 //                 "title": "DOGE Up or Down - 1 hour"
                 //             },
                 //             {
                 //                 "blockTimestamp": 1778144137,
                 //                 "collateralAmount": "2",
                 //                 "market": {
                 //                     "closed": true,
                 //                     "collateral": {
                 //                         "symbol": "USDC",
                 //                         "id": "7",
                 //                         "decimals": 6
                 //                     },
                 //                     "group": null,
                 //                     "condition_id":
                 //                     "0x0c61db7449dd8f8c81cd856f53d4186cf30888e27eb025d10c7908fa94ba736e",
                 //                     "conditionId":
                 //                     "0x0c61db7449dd8f8c81cd856f53d4186cf30888e27eb025d10c7908fa94ba736e",
                 //                     "funding": "0",
                 //                     "id": "117515",
                 //                     "slug":
                 //                     "doge-up-or-down-1-hour-1778140801775",
                 //                     "title": "DOGE Up or Down - 1 hour",
                 //                      "expirationDate":
                 //                      "2026-05-07T09:00:00.000Z"
                 //                 },
                 //                 "outcomeTokenAmount": "10",
                 //                 "outcomeTokenAmounts": [
                 //                     "10",
                 //                     "0"
                 //                 ],
                 //                 "outcomeIndex": 0,
                 //                 "outcomeTokenPrice": "0.2",
                 //                 "strategy": "Limit Buy",
                 //                 "transactionHash":
                 //                 "0x1e1167f09bb65ad3037610ae4f2521b696f7109f535e148ea388d42fdb6e2a10"
                 //             }
                 //         ],
                 //         "nextCursor": null
                 //     }
                 //
                 ccxt::any data = this->safeList(response, std::string("data"),
                                                 ccxt::list{});
                 // response contains both trade, settlement, split and merge
                 // history we filter out the settlements here and only return
                 // the trades
                 ccxt::any trades = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(data));
                      postFixIncrement(i)) {
                   ccxt::any item = this->safeDict(data, i);
                   ccxt::any strategy =
                       this->safeStringLower(item, std::string("strategy"));
                   if (isTrue(!isEqual(strategy, ccxt::any{}))) {
                     ccxt::any buyIndex =
                         getIndexOf(strategy, std::string("buy"));
                     ccxt::any sellIndex =
                         getIndexOf(strategy, std::string("sell"));
                     if (isTrue(isTrue((isGreaterThanOrEqual(buyIndex, 0))) ||
                                isTrue((isGreaterThanOrEqual(sellIndex, 0))))) {
                       arrayPush(trades, item);
                     }
                   }
                 }
                 ccxt::any parsedTrades =
                     this->parsePredictionTrades(trades, ccxt::any{});
                 return this->filterByOutcomeSinceLimit(
                     parsedTrades, outcomeSymbol, since, limit);
               })
        .share();
  }

  /**
   * @ignore
   * @method
   * @name limitless#parsePredictionTrade
   * @description parses a raw trade from either the public market events feed
   * or the private portfolio history into a unified trade object
   * @param {object} trade the raw trade object
   * @param {object} [market] the outcome object the trade belongs to
   * @returns {object} a [prediction trade
   * structure](https://docs.ccxt.com/#/?id=prediction-trade-structure)
   */
  ccxt::any parsePredictionTrade(ccxt::any trade,
                                 ccxt::any market = ccxt::any{}) override {
    ccxt::any matchedSize = this->safeString(trade, std::string("matchedSize"));
    if (isTrue(!isEqual(matchedSize, ccxt::any{}))) {
      // public market events feed trade, see fetchTrades for the response
      // sample
      ccxt::any ts =
          this->parse8601(this->safeString(trade, std::string("createdAt")));
      ccxt::any sideRaw = this->safeString(trade, std::string("side"));
      ccxt::any feedSide = ccxt::any{};
      if (isTrue(isEqual(sideRaw, std::string("0")))) {
        feedSide = std::string("buy");
      } else if (isTrue(isEqual(sideRaw, std::string("1")))) {
        feedSide = std::string("sell");
      }
      ccxt::any amountStr =
          ccxt::Precise::stringDiv(matchedSize, std::string("1000000"));
      ccxt::any priceStr = this->safeString(trade, std::string("price"));
      ccxt::any costStr = ccxt::any{};
      if (isTrue(!isEqual(priceStr, ccxt::any{}))) {
        costStr = ccxt::Precise::stringMul(priceStr, amountStr);
      }
      ccxt::any feedOutcome = this->safeOutcomeSymbol(ccxt::any{}, market);
      return this->safePredictionTrade(ccxt::dict{
          {std::string("id"), this->safeString(trade, std::string("txHash"))},
          {std::string("info"), trade},
          {std::string("timestamp"), ts},
          {std::string("datetime"), this->iso8601(ts)},
          {std::string("outcome"), feedOutcome},
          {std::string("outcomeId"),
           this->safeString(market, std::string("outcomeId"))},
          {std::string("label"),
           this->safeString(market, std::string("label"))},
          {std::string("market"),
           this->safeString(market, std::string("market"))},
          {std::string("order"), ccxt::any{}},
          {std::string("type"), ccxt::any{}},
          {std::string("side"), feedSide},
          {std::string("takerOrMaker"), std::string("taker")},
          {std::string("price"), this->parseNumber(priceStr)},
          {std::string("amount"), this->parseNumber(amountStr)},
          {std::string("cost"), this->parseNumber(costStr)},
          {std::string("fee"), ccxt::any{}},
      });
    }
    //
    //     {
    //         "blockTimestamp": 1778144137,
    //         "collateralAmount": "2",
    //         "market": {
    //             "closed": true,
    //             "collateral": {
    //                 "symbol": "USDC",
    //                 "id": "7",
    //                 "decimals": 6
    //             },
    //             "group": null,
    //             "condition_id":
    //             "0x0c61db7449dd8f8c81cd856f53d4186cf30888e27eb025d10c7908fa94ba736e",
    //             "conditionId":
    //             "0x0c61db7449dd8f8c81cd856f53d4186cf30888e27eb025d10c7908fa94ba736e",
    //             "funding": "0",
    //             "id": "117515",
    //             "slug": "doge-up-or-down-1-hour-1778140801775",
    //             "title": "DOGE Up or Down - 1 hour",
    //             "expirationDate": "2026-05-07T09:00:00.000Z"
    //         },
    //         "outcomeTokenAmount": "10",
    //         "outcomeTokenAmounts": [
    //             "10",
    //             "0"
    //         ],
    //         "outcomeIndex": 0,
    //         "outcomeTokenPrice": "0.2",
    //         "strategy": "Limit Buy",
    //         "transactionHash":
    //         "0x1e1167f09bb65ad3037610ae4f2521b696f7109f535e148ea388d42fdb6e2a10"
    //     }
    //
    ccxt::any id = this->safeString(trade, std::string("transactionHash"));
    ccxt::any timestamp =
        this->safeIntegerProduct(trade, std::string("blockTimestamp"), 1000);
    ccxt::any price = this->safeString(trade, std::string("outcomeTokenPrice"));
    ccxt::any amount =
        this->safeString(trade, std::string("outcomeTokenAmount"));
    ccxt::any cost = this->safeString(trade, std::string("collateralAmount"));
    ccxt::any rawSide = this->safeStringLower(trade, std::string("strategy"));
    if (isTrue(isEqual(rawSide, ccxt::any{}))) {
      throw ExchangeError(toString(add(
          this->id, std::string(" parsePredictionTrade() missing rawSide"))));
    }
    ccxt::any sellIndex = getIndexOf(rawSide, std::string("sell"));
    ccxt::any side = (isTrue((isGreaterThanOrEqual(sellIndex, 0)))
                          ? ccxt::any(std::string("sell"))
                          : ccxt::any(std::string("buy")));
    ccxt::any type = ccxt::any{};
    ccxt::any takerOrMaker = ccxt::any{};
    if (isTrue(isEqual(rawSide, ccxt::any{}))) {
      throw ExchangeError(toString(add(
          this->id, std::string(" parsePredictionTrade() missing rawSide"))));
    }
    if (isTrue(isGreaterThanOrEqual(getIndexOf(rawSide, std::string("limit")),
                                    0))) {
      type = std::string("limit");
      takerOrMaker = std::string("maker");
      if (isTrue(isEqual(rawSide, ccxt::any{}))) {
        throw ExchangeError(
            toString(add(this->id, std::string(" method() missing rawSide"))));
      }
    } else if (isTrue(isGreaterThanOrEqual(
                   getIndexOf(rawSide, std::string("market")), 0))) {
      type = std::string("market");
      takerOrMaker = std::string("taker");
    }
    ccxt::any rawMarket =
        this->safeDict(trade, std::string("market"), ccxt::dict{});
    ccxt::any slug = this->safeString(rawMarket, std::string("slug"));
    ccxt::any outcomeIndex =
        this->safeInteger(trade, std::string("outcomeIndex"));
    ccxt::any label =
        (isTrue((isEqual(outcomeIndex, 0))) ? ccxt::any(std::string("yes"))
                                            : ccxt::any(std::string("no")));
    ccxt::any outcome = this->getOutcomeBySlugAndLabel(slug, label, market);
    ccxt::any tradeOutcome = this->safeString(outcome, std::string("outcome"));
    return this->safePredictionTrade(ccxt::dict{
        {std::string("id"), id},
        {std::string("info"), trade},
        {std::string("timestamp"), timestamp},
        {std::string("datetime"), this->iso8601(timestamp)},
        {std::string("outcome"), tradeOutcome},
        {std::string("outcomeId"),
         this->safeString(trade, std::string("asset"))},
        {std::string("label"), this->safeString(outcome, std::string("label"))},
        {std::string("market"),
         this->safeString(outcome, std::string("market"))},
        {std::string("order"), ccxt::any{}},
        {std::string("type"), type},
        {std::string("side"), side},
        {std::string("takerOrMaker"), takerOrMaker},
        {std::string("price"), price},
        {std::string("amount"), amount},
        {std::string("cost"), cost},
        {std::string("fee"), ccxt::any{}},
    });
  }

  virtual ccxt::any getOutcomeBySlugAndLabel(ccxt::any slug, ccxt::any label,
                                             ccxt::any market = ccxt::any{}) {
    ccxt::any mkt = this->safeMarket(slug, market);
    ccxt::any outcomes =
        this->safeList(mkt, std::string("outcomes"), ccxt::list{});
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(outcomes));
         postFixIncrement(i)) {
      ccxt::any outcome = this->safeDict(outcomes, i);
      ccxt::any outcomeLabel = this->safeString(outcome, std::string("label"));
      if (isTrue(isEqual(outcomeLabel, label))) {
        return outcome;
      }
    }
    return ccxt::any{};
  }

  /**
   * @method
   * @name limitless#fetchPositions
   * @description fetches open positions for the authenticated limitless user
   * from the portfolio endpoint
   * @see https://docs.limitless.exchange/api-reference/portfolio/get-positions
   * @param {string[]} [outcomes] filter by outcome ids or outcomes
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [prediction position
   * structures](https://docs.ccxt.com/#/?id=prediction-position-structure)
   */
  std::shared_future<ccxt::any>
  fetchPositions(ccxt::any outcomes = ccxt::any{},
                 ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 ccxt::any symbolsLength = 0;
                 if (isTrue(!isEqual(outcomes, ccxt::any{}))) {
                   symbolsLength = getArrayLength(outcomes);
                 }
                 if (isTrue(isGreaterThan(symbolsLength, 0))) {
                   awaitValue(this->loadOutcomes(outcomes));
                 }
                 // no bulk warm-up on the unfiltered path: the portfolio
                 // request is self-contained and labels resolve cache-only (raw
                 // slugs/labels stay available in info when the cache is cold)
                 ccxt::any response = awaitValue(
                     this->limitlessPrivateGetPortfolioPositions(params));
                 //
                 //     {
                 //         "rewards": {
                 //                 "todaysRewards": "0",
                 //                 "totalUnpaidRewards": "0",
                 //                 "totalUserRewardsLastEpoch": "0",
                 //                 "rewardsChartData": [],
                 //                 "rewardsByEpoch": []
                 //         },
                 //         "points": "0.00000000",
                 //         "accumulativePoints": "0.00000000",
                 //         "amm": [],
                 //         "group": [],
                 //         "clob": [
                 //             {
                 //                 "market": {
                 //                     "slug":
                 //                     "btc-above-dollar7982448-on-may-11-1000-utc-1777888806248",
                 //                     "status": "FUNDED",
                 //                     "title": "BTC Up or Down - 1 week",
                 //                     "conditionId":
                 //                     "0xdcd8264cd09a6c50fca35eca24cda13e70f705e8b9ca7df7edb1c53d5e14ef91",
                 //                     "id": 113280,
                 //                     "address": null,
                 //                     "closed": false,
                 //                     "expirationDate":
                 //                     "2026-05-11T10:00:00.000Z", "deadline":
                 //                     "2026-05-11T10:00:00.000Z",
                 //                     "negRiskRequestId": null,
                 //                     "winningOutcomeIndex": null,
                 //                     "yesPositionId":
                 //                     "93872239373494820196551522390839917813776244436120184186867916673676200558660",
                 //                     "noPositionId":
                 //                     "63415751165356883207530164011662686422066216695676262832702521399916548381548",
                 //                     "collateralToken": {
                 //                         "id": 7,
                 //                         "decimals": 6,
                 //                         "symbol": "USDC",
                 //                         "address":
                 //                         "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913"
                 //                     },
                 //                     "venue": {
                 //                         "exchange":
                 //                         "0x05c748E2f4DcDe0ec9Fa8DDc40DE6b867f923fa5",
                 //                         "adapter": null,
                 //                         "operator": null
                 //                     },
                 //                     "group": {}
                 //                 },
                 //                 "latestTrade": {
                 //                     "outcomeTokenPrice": 0.991,
                 //                     "latestNoPrice": 0.009,
                 //                     "latestYesPrice": 0.991
                 //                 },
                 //                 "orders": {
                 //                     "liveOrders": [],
                 //                     "totalCollateralLocked": "0"
                 //                 },
                 //                 "positions": {
                 //                     "no": {
                 //                         "cost": "0",
                 //                         "fillPrice": "0",
                 //                         "marketValue": "0",
                 //                         "realisedPnl": "0",
                 //                         "unrealizedPnl": "0"
                 //                     },
                 //                     "yes": {
                 //                         "cost": "995720",
                 //                         "fillPrice": "991000",
                 //                         "marketValue": "999919",
                 //                         "realisedPnl": "0",
                 //                         "unrealizedPnl": "0"
                 //                     }
                 //                 },
                 //                 "tokensBalance": {
                 //                     "no": "0",
                 //                     "yes": "1004763"
                 //                 },
                 //                 "rewards": {
                 //                     "isEarning": false,
                 //                     "epochs": []
                 //                 },
                 //                 "makerAddress":
                 //                 "0xAb2B9833FC8B8f55F4De7C4A0FAb8577EF0F7b36"
                 //             }
                 //         ]
                 //     }
                 //
                 ccxt::any clob = this->safeList(response, std::string("clob"),
                                                 ccxt::list{});
                 ccxt::any result = ccxt::list{};
                 ccxt::any labels =
                     ccxt::list{std::string("yes"), std::string("no")};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(clob));
                      postFixIncrement(i)) {
                   ccxt::any entry = this->safeDict(clob, i);
                   for (ccxt::any j = 0; isLessThan(j, getArrayLength(labels));
                        postFixIncrement(j)) {
                     ccxt::any label = this->safeString(labels, j);
                     ccxt::any position =
                         this->getPositionFromClobEntry(label, entry);
                     if (isTrue(!isEqual(position, ccxt::any{}))) {
                       arrayPush(result, position);
                     }
                   }
                 }
                 return result;
               })
        .share();
  }

  virtual ccxt::any getPositionFromClobEntry(ccxt::any label,
                                             ccxt::any entry = ccxt::any{}) {
    if (isTrue(isEqual(entry, ccxt::any{}))) {
      return ccxt::any{};
    }
    ccxt::any tokensBalance =
        this->safeDict(entry, std::string("tokensBalance"));
    ccxt::any contracts =
        this->omitZero(this->safeString(tokensBalance, label));
    if (isTrue(isEqual(contracts, ccxt::any{}))) {
      return ccxt::any{};
    }
    ccxt::any positions = this->safeDict(entry, std::string("positions"));
    ccxt::any position = this->safeDict(positions, label, ccxt::dict{});
    ccxt::any rawMarket = this->safeDict(entry, std::string("market"));
    ccxt::any slug = this->safeString(rawMarket, std::string("slug"));
    ccxt::any outcomeObj = this->getOutcomeBySlugAndLabel(slug, label);
    ccxt::any parsed = this->parsePredictionPosition(position, outcomeObj);
    ::setValue(parsed, std::string("contracts"),
               this->parseNumber(this->applyScale(contracts)));
    ccxt::any latestTrade = this->safeDict(entry, std::string("latestTrade"));
    ccxt::any key = std::string("latestYesPrice");
    if (isTrue(isEqual(label, std::string("no")))) {
      key = std::string("latestNoPrice");
    }
    ::setValue(parsed, std::string("markPrice"),
               this->safeNumber(latestTrade, key));
    ::setValue(parsed, std::string("info"), entry);
    return this->safePredictionPosition(parsed);
  }

  /**
   * @ignore
   * @method
   * @name limitless#parsePredictionPosition
   * @description parses a raw limitless portfolio position into a unified
   * position object
   * @param {object} position the raw position object
   * @param {object} [market] the outcome object the position belongs to
   * @returns {object} a [prediction position
   * structure](https://docs.ccxt.com/#/?id=prediction-position-structure)
   */
  ccxt::any parsePredictionPosition(ccxt::any position,
                                    ccxt::any market = ccxt::any{}) override {
    //
    //     {
    //         "cost": "995720",
    //         "fillPrice": "991000",
    //         "marketValue": "999919",
    //         "realisedPnl": "0",
    //         "unrealizedPnl": "0"
    //     }
    //
    ccxt::any outcomeSymbol = this->safeString(market, std::string("outcome"));
    ccxt::any notional = this->applyScale(
        this->safeString(position, std::string("marketValue")));
    ccxt::any unrealizedPnl = this->applyScale(
        this->safeString(position, std::string("unrealizedPnl")));
    ccxt::any realizedPnl = this->applyScale(
        this->safeString(position, std::string("realisedPnl")));
    ccxt::any collateral =
        this->applyScale(this->safeString(position, std::string("cost")));
    ccxt::any entryPrice =
        this->applyScale(this->safeString(position, std::string("fillPrice")));
    return ccxt::dict{
        {std::string("id"), ccxt::any{}},
        {std::string("outcome"), outcomeSymbol},
        {std::string("outcomeId"),
         this->safeString(market, std::string("outcomeId"))},
        {std::string("label"), this->safeString(market, std::string("label"))},
        {std::string("market"),
         this->safeString(market, std::string("market"))},
        {std::string("timestamp"), ccxt::any{}},
        {std::string("datetime"), ccxt::any{}},
        {std::string("contracts"), ccxt::any{}},
        {std::string("contractSize"), 1},
        {std::string("side"), std::string("long")},
        {std::string("notional"), this->parseNumber(notional)},
        {std::string("leverage"), 1},
        {std::string("unrealizedPnl"), this->parseNumber(unrealizedPnl)},
        {std::string("realizedPnl"), this->parseNumber(realizedPnl)},
        {std::string("collateral"), this->parseNumber(collateral)},
        {std::string("entryPrice"), this->parseNumber(entryPrice)},
        {std::string("markPrice"), ccxt::any{}},
        {std::string("liquidationPrice"), ccxt::any{}},
        {std::string("hedged"), false},
        {std::string("maintenanceMargin"), ccxt::any{}},
        {std::string("maintenanceMarginPercentage"), ccxt::any{}},
        {std::string("initialMargin"), ccxt::any{}},
        {std::string("initialMarginPercentage"), ccxt::any{}},
        {std::string("marginRatio"), ccxt::any{}},
        {std::string("marginMode"), std::string("cross")},
        {std::string("marginType"), std::string("cross")},
        {std::string("percentage"), ccxt::any{}},
        {std::string("info"), position},
    };
  }

  /**
   * @method
   * @name limitless#fetchEvents
   * @description fetches prediction-market events matching the given scope
   * (query/queries/tags/eventId/slug — required) and caches their markets and
   * outcomes on the instance
   * @see https://docs.limitless.exchange/api-reference/markets/search
   * @param {object} [params] extra exchange-specific parameters
   * @param {string} [params.query] a single search term; an eventId/slug does a
   * direct lookup and tags resolve to limitless categories, paging only those
   * categories' listings
   * @param {string[]} [params.queries] multiple search terms (alternative to
   * query)
   * @param {string[]} [params.tags] category names to scope by (matched against
   * GET /categories, e.g. ['crypto'])
   * @param {string} [params.eventId] direct lookup by market address or slug
   * @param {int} [params.limit] maximum number of markets per query, defaults
   * to 50
   * @returns {object[]} an array of event structures
   */
  std::shared_future<ccxt::any>
  fetchEvents(ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 this->requireEventQuery(params);
                 ccxt::any queries = this->parseSearchQueries(params);
                 if (isTrue(isEqual(queries, ccxt::any{}))) {
                   throw ExchangeError(toString(
                       add(this->id,
                           std::string(" fetchEvents() missing queries"))));
                 }
                 ccxt::any queriesLength = getArrayLength(queries);
                 ccxt::any rest = this->omit(
                     params,
                     ccxt::list{std::string("query"), std::string("queries"),
                                std::string("limit"), std::string("sort"),
                                std::string("searchIn"), std::string("eventId"),
                                std::string("slug"), std::string("status")});
                 ccxt::any eventId = this->safeString2(
                     params, std::string("eventId"), std::string("slug"));
                 // always fetch fresh from the API (never serve the
                 // possibly-cold cache): a query searches, an eventId/slug does
                 // a direct lookup, and any other scope (tags) pages the
                 // active-markets listing
                 ccxt::any rawMarkets = ccxt::list{};
                 if (isTrue(isGreaterThan(queriesLength, 0))) {
                   ccxt::any requestedLimit =
                       this->safeInteger(params, std::string("limit"), 50);
                   // the search endpoint rejects limit > 50 - cap the per-query
                   // request
                   ccxt::any limit = mathMin(requestedLimit, 50);
                   ccxt::any seen = ccxt::dict{};
                   for (ccxt::any i = 0; isLessThan(i, getArrayLength(queries));
                        postFixIncrement(i)) {
                     if (isTrue(isEqual(queries, ccxt::any{}))) {
                       throw ExchangeError(toString(
                           add(this->id,
                               std::string(" fetchEvents() missing queries"))));
                     }
                     ccxt::any q = ::getValue(queries, i);
                     ccxt::any response = awaitValue(
                         this->limitlessPublicGetMarketsSearch(this->extend(
                             ccxt::dict{
                                 {std::string("query"), q},
                                 {std::string("limit"), limit},
                             },
                             rest)));
                     ccxt::any found = this->safeList(
                         response, std::string("markets"), ccxt::list{});
                     for (ccxt::any j = 0; isLessThan(j, getArrayLength(found));
                          postFixIncrement(j)) {
                       ccxt::any raw = ::getValue(found, j);
                       ccxt::any rawSlug =
                           this->safeString(raw, std::string("slug"));
                       if (isTrue(
                               isTrue((isTrue(!isEqual(rawSlug, ccxt::any{})) &&
                                       isTrue(!isEqual(rawSlug,
                                                       std::string(""))))) &&
                               !isTrue((inOp(seen, rawSlug))))) {
                         ::setValue(seen, rawSlug, true);
                         arrayPush(rawMarkets, raw);
                       }
                     }
                   }
                 } else if (isTrue(!isEqual(eventId, ccxt::any{}))) {
                   ccxt::any response =
                       awaitValue(this->limitlessPublicGetMarketsAddressOrSlug(
                           this->extend(
                               ccxt::dict{
                                   {std::string("addressOrSlug"), eventId},
                               },
                               rest)));
                   arrayPush(rawMarkets, response);
                 } else {
                   // tags scope: resolve the tags to limitless categories and
                   // page only those categories' listings server-side — never
                   // the whole active listing
                   ccxt::any requestedTags = this->safeList(
                       params, std::string("tags"), ccxt::list{});
                   ccxt::any listRaw = awaitValue(
                       this->fetchRawMarketsByTags(requestedTags, params));
                   ccxt::any listRawLength = getArrayLength(listRaw);
                   for (ccxt::any i = 0; isLessThan(i, listRawLength);
                        postFixIncrement(i)) {
                     arrayPush(rawMarkets, ::getValue(listRaw, i));
                   }
                 }
                 if (isTrue(isEqual(this->events, ccxt::any{}))) {
                   this->events = ccxt::dict{};
                 }
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   this->markets = this->createSafeDictionary();
                 }
                 ccxt::any eventGroups = ccxt::dict{};
                 // group rows carry their tradeable children in a nested
                 // `markets` list — expand them into regular rows before
                 // parsing (a group row itself has no tokens)
                 ccxt::any expandedMarkets = this->expandGroupRows(rawMarkets);
                 ccxt::any rawMarketsLength = getArrayLength(expandedMarkets);
                 for (ccxt::any i = 0; isLessThan(i, rawMarketsLength);
                      postFixIncrement(i)) {
                   ccxt::any raw = ::getValue(expandedMarkets, i);
                   ccxt::any groupId = this->safeStringN(
                       raw,
                       ccxt::list{std::string("groupSlug"),
                                  std::string("groupId")},
                       this->safeString(raw, std::string("slug")));
                   ccxt::any eventKey =
                       (isTrue((isTrue(!isEqual(groupId, ccxt::any{})) &&
                                isTrue(!isEqual(groupId, std::string("")))))
                            ? ccxt::any(this->shortenSlug(groupId))
                            : ccxt::any(ccxt::any{}));
                   ccxt::any m = this->parseMarket(raw);
                   if (isTrue(isEqual(m, ccxt::any{}))) {
                     throw ExchangeError(toString(add(
                         this->id, std::string(" fetchEvents() missing m"))));
                   }
                   ::setValue(this->markets,
                              ::getValue(m, std::string("market")), m);
                   if (isTrue(isTrue((!isEqual(eventKey, ccxt::any{}))) &&
                              isTrue((!isEqual(eventKey, std::string("")))))) {
                     if (!isTrue((inOp(eventGroups, eventKey)))) {
                       ::setValue(eventGroups, eventKey,
                                  ccxt::dict{
                                      {std::string("groupId"), groupId},
                                      {std::string("title"),
                                       this->safeString2(
                                           raw, std::string("groupTitle"),
                                           std::string("title"), groupId)},
                                      {std::string("raw"), raw},
                                      {std::string("markets"), ccxt::list{}},
                                  });
                     }
                     ccxt::any eventGroup = ::getValue(eventGroups, eventKey);
                     // push through a local and write the slice back — the go
                     // transpiler's AppendToArray reassigns only a local copy
                     // of a map-stored array, so a direct push on
                     // eventGroup['markets'] loses the element in go
                     ccxt::any groupMarkets =
                         ::getValue(eventGroup, std::string("markets"));
                     arrayPush(groupMarkets, m);
                     ::setValue(eventGroup, std::string("markets"),
                                groupMarkets);
                   }
                 }
                 ccxt::any result = ccxt::list{};
                 ccxt::any eventKeys = getObjectKeys(eventGroups);
                 ccxt::any eventKeysLength = getArrayLength(eventKeys);
                 for (ccxt::any i = 0; isLessThan(i, eventKeysLength);
                      postFixIncrement(i)) {
                   ccxt::any g =
                       ::getValue(eventGroups, ::getValue(eventKeys, i));
                   ccxt::any ev = this->parseEvent(g);
                   arrayPush(result, ev);
                 }
                 // setEvents keys events by id/slug/handle; populateOutcomes
                 // rebuilds the outcome cache
                 this->setEvents(result);
                 this->populateOutcomes();
                 // the limitless search endpoint is FUZZY — it returns
                 // nearest-neighbour markets even for queries that match
                 // nothing — so default searchIn to 'both' to post-filter the
                 // results literally by title/description (an explicit
                 // params.searchIn still wins). tags were already applied
                 // server-side (category-scoped listing); strip them before the
                 // client-side pass — events built from raw markets carry venue
                 // tags, not category names, so the base tag filter would
                 // wrongly drop server-matched events
                 ccxt::any searchParams = this->extend(
                     ccxt::dict{
                         {std::string("searchIn"), std::string("both")},
                     },
                     params);
                 ccxt::any postParams =
                     this->omit(searchParams, ccxt::list{std::string("tags")});
                 return this->applyEventFetchParams(result, postParams,
                                                    queries);
               })
        .share();
  }

  /**
   * @ignore
   * @method
   * @name limitless#fetchRawActiveMarkets
   * @description pages the active-markets listing (or a single category's
   * listing), bounded by limit (or options.fetchMarketsLimit)
   * @param {object} [params] extra exchange-specific parameters
   * @param {int} [params.limit] max number of raw markets to collect
   * @param {string} [categoryId] a limitless category id — pages only that
   * category's listing
   * @returns {object[]} raw limitless market objects
   */
  virtual std::shared_future<ccxt::any>
  fetchRawActiveMarkets(ccxt::any params = ccxt::dict{},
                        ccxt::any categoryId = ccxt::any{}) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 ccxt::any maxMarkets = this->safeInteger(
                     params, std::string("limit"),
                     this->safeInteger(this->options,
                                       std::string("fetchMarketsLimit"), 1000));
                 ccxt::any pageSize = this->safeInteger(
                     this->options, std::string("marketsPageSize"), 25);
                 ccxt::any rest = this->omit(
                     params,
                     ccxt::list{std::string("query"), std::string("queries"),
                                std::string("limit"), std::string("sort"),
                                std::string("searchIn"), std::string("eventId"),
                                std::string("slug"), std::string("status"),
                                std::string("tags")});
                 ccxt::any allRaw = ccxt::list{};
                 ccxt::any page = 1;
                 ccxt::any collected = 0;
                 while (true) {
                   ccxt::any request = ccxt::dict{
                       {std::string("page"), page},
                       {std::string("limit"), pageSize},
                   };
                   ccxt::any response = ccxt::any{};
                   if (isTrue(!isEqual(categoryId, ccxt::any{}))) {
                     ::setValue(request, std::string("categoryId"), categoryId);
                     response = awaitValue(
                         this->limitlessPublicGetMarketsActiveCategoryId(
                             this->extend(request, rest)));
                   } else {
                     response =
                         awaitValue(this->limitlessPublicGetMarketsActive(
                             this->extend(request, rest)));
                   }
                   ccxt::any data = this->safeList(
                       response, std::string("data"), ccxt::list{});
                   ccxt::any dataLength = getArrayLength(data);
                   if (isTrue(isEqual(dataLength, 0))) {
                     break;
                   }
                   for (ccxt::any i = 0; isLessThan(i, dataLength);
                        postFixIncrement(i)) {
                     if (isTrue(isLessThan(collected, maxMarkets))) {
                       arrayPush(allRaw, ::getValue(data, i));
                       collected = this->sum(collected, 1);
                     }
                   }
                   page = this->sum(page, 1);
                   if (isTrue(isTrue(isLessThan(dataLength, pageSize)) ||
                              isTrue(isGreaterThanOrEqual(collected,
                                                          maxMarkets)))) {
                     break;
                   }
                 }
                 return allRaw;
               })
        .share();
  }

  /**
   * @ignore
   * @method
   * @name limitless#fetchRawMarketsByTags
   * @description resolves the requested tags to limitless categories via GET
   * /categories, then pages only those categories' active listings server-side
   * @param {string[]} tags tag/category names to match (case-insensitive
   * substring match on the category name)
   * @param {object} [params] extra exchange-specific parameters
   * @param {int} [params.limit] max number of raw markets to collect per
   * category
   * @returns {object[]} raw limitless market objects, deduped by slug
   */
  virtual std::shared_future<ccxt::any>
  fetchRawMarketsByTags(ccxt::any tags, ccxt::any params = ccxt::dict{}) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 ccxt::any categoriesResponse =
                     awaitValue(this->limitlessPublicGetCategories());
                 ccxt::any categories = ccxt::list{};
                 if (isTrue(isArray(categoriesResponse))) {
                   categories = categoriesResponse;
                 }
                 ccxt::any wanted = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(tags));
                      postFixIncrement(i)) {
                   arrayPush(wanted, toLowerCase(::getValue(tags, i)));
                 }
                 ccxt::any categoryIds = ccxt::list{};
                 ccxt::any categoriesLength = getArrayLength(categories);
                 for (ccxt::any i = 0; isLessThan(i, categoriesLength);
                      postFixIncrement(i)) {
                   ccxt::any category = ::getValue(categories, i);
                   ccxt::any name = this->safeStringLower(
                       category, std::string("name"), std::string(""));
                   ccxt::any categoryId =
                       this->safeString(category, std::string("id"));
                   ccxt::any matched = false;
                   for (ccxt::any wi = 0;
                        isLessThan(wi, getArrayLength(wanted));
                        postFixIncrement(wi)) {
                     if (isTrue(isEqual(name, ccxt::any{}))) {
                       throw ExchangeError(toString(
                           add(this->id,
                               std::string(
                                   " fetchRawMarketsByTags() missing name"))));
                     }
                     if (isTrue(isGreaterThanOrEqual(
                             getIndexOf(name, ::getValue(wanted, wi)), 0))) {
                       matched = true;
                       break;
                     }
                   }
                   if (isTrue(isTrue(matched) &&
                              isTrue((!isEqual(categoryId, ccxt::any{}))))) {
                     arrayPush(categoryIds, categoryId);
                   }
                 }
                 ccxt::any categoryIdsLength = getArrayLength(categoryIds);
                 if (isTrue(isEqual(categoryIdsLength, 0))) {
                   throw BadRequest(toString(add(
                       this->id,
                       std::string(" fetchEvents() could not match the "
                                   "requested tags to any limitless category — "
                                   "GET /categories lists the valid names"))));
                 }
                 ccxt::any seen = ccxt::dict{};
                 ccxt::any allRaw = ccxt::list{};
                 for (ccxt::any ci = 0; isLessThan(ci, categoryIdsLength);
                      postFixIncrement(ci)) {
                   ccxt::any categoryMarkets =
                       awaitValue(this->fetchRawActiveMarkets(
                           params, ::getValue(categoryIds, ci)));
                   ccxt::any categoryMarketsLength =
                       getArrayLength(categoryMarkets);
                   for (ccxt::any mi = 0; isLessThan(mi, categoryMarketsLength);
                        postFixIncrement(mi)) {
                     ccxt::any raw = ::getValue(categoryMarkets, mi);
                     ccxt::any slug =
                         this->safeString(raw, std::string("slug"));
                     if (isTrue(isTrue((!isEqual(slug, ccxt::any{}))) &&
                                !isTrue((inOp(seen, slug))))) {
                       ::setValue(seen, slug, true);
                       arrayPush(allRaw, raw);
                     }
                   }
                 }
                 return allRaw;
               })
        .share();
  }

  /**
   * @ignore
   * @method
   * @name limitless#sign
   * @description builds the request URL and attaches the lmts authentication
   * headers for private endpoints
   * @param {string} path the endpoint path
   * @param {string|string[]} [section] the api group and access level
   * @param {string} [method] HTTP method
   * @param {object} [params] request parameters
   * @param {object} [headers] request headers
   * @param {object} [body] request body
   * @returns {object} a dictionary with url, method, body and headers
   */
  ccxt::any sign(ccxt::any path, ccxt::any section = std::string("limitless"),
                 ccxt::any method = std::string("GET"),
                 ccxt::any params = ccxt::dict{},
                 ccxt::any headers = ccxt::any{},
                 ccxt::any body = ccxt::any{}) override {
    ccxt::any apiGroup =
        (isTrue(isString(section)) ? ccxt::any(section)
                                   : ccxt::any(::getValue(section, 0)));
    ccxt::any access =
        (isTrue(isString(section)) ? ccxt::any(std::string("public"))
                                   : ccxt::any(::getValue(section, 1)));
    ccxt::any baseUrls = ::getValue(this->urls, std::string("api"));
    ccxt::any baseUrl = this->safeString(
        baseUrls, apiGroup, ::getValue(baseUrls, std::string("limitless")));
    ccxt::any url = add(std::string("/"), this->implodeParams(path, params));
    ccxt::any query = this->omit(params, this->extractParams(path));
    ccxt::any querystring = this->urlencodeWithArrayRepeat(query);
    if (isTrue(isTrue(isEqual(method, std::string("GET"))) &&
               isTrue((!isEqual(querystring, std::string("")))))) {
      url = add(url, add(std::string("?"), querystring));
    }
    if (isTrue(isEqual(access, std::string("private")))) {
      ccxt::any bodyString = std::string("");
      if (isTrue(isEqual(headers, ccxt::any{}))) {
        headers = ccxt::dict{};
      }
      if (isTrue(isTrue(isEqual(method, std::string("POST"))) &&
                 isTrue((!isEqual(querystring, std::string("")))))) {
        bodyString = this->json(query);
        body = bodyString;
        ccxt::any headerDefaults = (isTrue((!isEqual(headers, ccxt::any{})))
                                        ? ccxt::any(headers)
                                        : ccxt::any(ccxt::dict{}));
        headers = this->extend(
            ccxt::dict{
                {std::string("Accept"), std::string("application/json")},
                {std::string("Content-Type"), std::string("application/json")},
            },
            headerDefaults);
      }
      this->checkRequiredCredentials();
      ccxt::any timestamp = this->iso8601(this->milliseconds());
      ccxt::any newline = std::string("\n"); // eslint-disable-line quotes
      ccxt::any payload =
          add(add(add(add(add(add(timestamp, newline), method), newline), url),
                  newline),
              bodyString);
      ccxt::any signature =
          this->hmac(this->encode(payload), this->base64ToBinary(this->secret),
                     sha256, std::string("base64"));
      headers =
          this->extend(headers, ccxt::dict{
                                    {std::string("lmts-api-key"), this->apiKey},
                                    {std::string("lmts-timestamp"), timestamp},
                                    {std::string("lmts-signature"), signature},
                                });
    }
    url = add(baseUrl, url);
    return ccxt::dict{
        {std::string("url"), url},
        {std::string("method"), method},
        {std::string("body"), body},
        {std::string("headers"), headers},
    };
  }

  /**
   * @ignore
   * @method
   * @name limitless#handleErrors
   * @description maps limitless error responses to ccxt exceptions
   */
  ccxt::any handleErrors(ccxt::any statusCode, ccxt::any statusText,
                         ccxt::any url, ccxt::any method,
                         ccxt::any responseHeaders, ccxt::any responseBody,
                         ccxt::any response, ccxt::any requestHeaders,
                         ccxt::any requestBody) override {
    if (isTrue(isEqual(response, ccxt::any{}))) {
      return ccxt::any{};
    }
    if (isTrue(isTrue((isGreaterThanOrEqual(statusCode, 200))) &&
               isTrue((isLessThan(statusCode, 300))))) {
      return ccxt::any{};
    }
    ccxt::any feedback = add(add(this->id, std::string(" ")), responseBody);
    // the API returns either a string message or an array of field-validation
    // errors
    ccxt::any message = this->safeString(response, std::string("message"));
    if (isTrue(!isEqual(message, ccxt::any{}))) {
      this->throwExactlyMatchedException(
          ::getValue(this->exceptions, std::string("exact")), message,
          feedback);
    }
    this->throwBroadlyMatchedException(
        ::getValue(this->exceptions, std::string("broad")), responseBody,
        feedback);
    // a 400 is a client-side bad request (bad params, or a business rule like
    // "market not resolved"), not a transport outage — throw BadRequest with
    // the exchange message instead of letting the base map the bare 400 to a
    // retryable network-unavailable error
    if (isTrue(isEqual(statusCode, 400))) {
      throw BadRequest(toString(feedback));
    }
    return ccxt::any{};
  }
  // GENERATED dispatch table - see createDispatchTable in
  // build/cppTranspiler.ts
  virtual ccxt::any callMethod(ccxt::any name, ccxt::any args) override {
    const std::string which =
        ::toString(name).has_value()
            ? ccxt::any_cast<std::string>(::toString(name))
            : std::string();
    const long count =
        ccxt::isList(args)
            ? static_cast<long>(ccxt::any_cast<ccxt::list>(args).size())
            : 0;
    if (which == "describe") {
      if (true)
        return this->describe();
    }
    if (which == "fetchMarkets") {
      if (count <= 0)
        return awaitValue(this->fetchMarkets());
      if (count >= 1)
        return awaitValue(this->fetchMarkets(::getValue(args, 0)));
    }
    if (which == "parseMarket") {
      if (true)
        return this->parseMarket(::getValue(args, 0));
    }
    if (which == "fetchEvent") {
      if (count <= 1)
        return awaitValue(this->fetchEvent(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->fetchEvent(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "expandGroupRows") {
      if (true)
        return this->expandGroupRows(::getValue(args, 0));
    }
    if (which == "parseEvent") {
      if (true)
        return this->parseEvent(::getValue(args, 0));
    }
    if (which == "fetchTicker") {
      if (count <= 1)
        return awaitValue(this->fetchTicker(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->fetchTicker(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "parsePredictionTicker") {
      if (count <= 1)
        return this->parsePredictionTicker(::getValue(args, 0));
      if (count >= 2)
        return this->parsePredictionTicker(::getValue(args, 0),
                                           ::getValue(args, 1));
    }
    if (which == "fetchTickers") {
      if (count <= 0)
        return awaitValue(this->fetchTickers());
      if (count == 1)
        return awaitValue(this->fetchTickers(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->fetchTickers(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "fetchTrades") {
      if (count <= 1)
        return awaitValue(this->fetchTrades(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchTrades(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchTrades(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchTrades(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "fetchOrderBook") {
      if (count <= 1)
        return awaitValue(this->fetchOrderBook(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOrderBook(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->fetchOrderBook(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "fetchOHLCV") {
      if (count <= 1)
        return awaitValue(this->fetchOHLCV(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOHLCV(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(
            this->fetchOHLCV(::getValue(args, 0), ::getValue(args, 1),
                             ::getValue(args, 2), ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->fetchOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "fetchOrders") {
      if (count <= 0)
        return awaitValue(this->fetchOrders());
      if (count == 1)
        return awaitValue(this->fetchOrders(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOrders(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchOrders(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "fetchOpenOrders") {
      if (count <= 0)
        return awaitValue(this->fetchOpenOrders());
      if (count == 1)
        return awaitValue(this->fetchOpenOrders(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOpenOrders(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchOpenOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchOpenOrders(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "fetchClosedOrders") {
      if (count <= 0)
        return awaitValue(this->fetchClosedOrders());
      if (count == 1)
        return awaitValue(this->fetchClosedOrders(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchClosedOrders(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchClosedOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchClosedOrders(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "fetchOrdersByIds") {
      if (count <= 1)
        return awaitValue(this->fetchOrdersByIds(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOrdersByIds(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->fetchOrdersByIds(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "fetchOrder") {
      if (count <= 1)
        return awaitValue(this->fetchOrder(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOrder(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->fetchOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "parsePredictionOrder") {
      if (count <= 1)
        return this->parsePredictionOrder(::getValue(args, 0));
      if (count >= 2)
        return this->parsePredictionOrder(::getValue(args, 0),
                                          ::getValue(args, 1));
    }
    if (which == "parseOrderStatus") {
      if (true)
        return this->parseOrderStatus(::getValue(args, 0));
    }
    if (which == "parseOrderTimeInForce") {
      if (true)
        return this->parseOrderTimeInForce(::getValue(args, 0));
    }
    if (which == "parseOrderSide") {
      if (true)
        return this->parseOrderSide(::getValue(args, 0));
    }
    if (which == "applyScale") {
      if (count <= 1)
        return this->applyScale(::getValue(args, 0));
      if (count >= 2)
        return this->applyScale(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseAccount") {
      if (true)
        return this->parseAccount(::getValue(args, 0));
    }
    if (which == "fetchAccounts") {
      if (count <= 0)
        return awaitValue(this->fetchAccounts());
      if (count >= 1)
        return awaitValue(this->fetchAccounts(::getValue(args, 0)));
    }
    if (which == "createOrder") {
      if (count <= 4)
        return awaitValue(
            this->createOrder(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2), ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->createOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count >= 6)
        return awaitValue(this->createOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
    }
    if (which == "signOrderRequest") {
      if (true)
        return this->signOrderRequest(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "hashMessage") {
      if (true)
        return this->hashMessage(::getValue(args, 0));
    }
    if (which == "signHash") {
      if (true)
        return this->signHash(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "signMessage") {
      if (true)
        return this->signMessage(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "signEvmTransaction") {
      if (true)
        return this->signEvmTransaction(::getValue(args, 0),
                                        ::getValue(args, 1));
    }
    if (which == "approve") {
      if (count <= 0)
        return awaitValue(this->approve());
      if (count >= 1)
        return awaitValue(this->approve(::getValue(args, 0)));
    }
    if (which == "cancelOrder") {
      if (count <= 1)
        return awaitValue(this->cancelOrder(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->cancelOrder(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->cancelOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "redeem") {
      if (count <= 0)
        return awaitValue(this->redeem());
      if (count == 1)
        return awaitValue(this->redeem(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->redeem(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "cancelOrders") {
      if (count <= 1)
        return awaitValue(this->cancelOrders(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->cancelOrders(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->cancelOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "cancelAllOrders") {
      if (count <= 0)
        return awaitValue(this->cancelAllOrders());
      if (count == 1)
        return awaitValue(this->cancelAllOrders(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->cancelAllOrders(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "fetchMyTrades") {
      if (count <= 0)
        return awaitValue(this->fetchMyTrades());
      if (count == 1)
        return awaitValue(this->fetchMyTrades(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchMyTrades(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchMyTrades(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchMyTrades(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "parsePredictionTrade") {
      if (count <= 1)
        return this->parsePredictionTrade(::getValue(args, 0));
      if (count >= 2)
        return this->parsePredictionTrade(::getValue(args, 0),
                                          ::getValue(args, 1));
    }
    if (which == "getOutcomeBySlugAndLabel") {
      if (count <= 2)
        return this->getOutcomeBySlugAndLabel(::getValue(args, 0),
                                              ::getValue(args, 1));
      if (count >= 3)
        return this->getOutcomeBySlugAndLabel(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "fetchPositions") {
      if (count <= 0)
        return awaitValue(this->fetchPositions());
      if (count == 1)
        return awaitValue(this->fetchPositions(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->fetchPositions(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "getPositionFromClobEntry") {
      if (count <= 1)
        return this->getPositionFromClobEntry(::getValue(args, 0));
      if (count >= 2)
        return this->getPositionFromClobEntry(::getValue(args, 0),
                                              ::getValue(args, 1));
    }
    if (which == "parsePredictionPosition") {
      if (count <= 1)
        return this->parsePredictionPosition(::getValue(args, 0));
      if (count >= 2)
        return this->parsePredictionPosition(::getValue(args, 0),
                                             ::getValue(args, 1));
    }
    if (which == "fetchEvents") {
      if (count <= 0)
        return awaitValue(this->fetchEvents());
      if (count >= 1)
        return awaitValue(this->fetchEvents(::getValue(args, 0)));
    }
    if (which == "fetchRawActiveMarkets") {
      if (count <= 0)
        return awaitValue(this->fetchRawActiveMarkets());
      if (count == 1)
        return awaitValue(this->fetchRawActiveMarkets(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(this->fetchRawActiveMarkets(::getValue(args, 0),
                                                      ::getValue(args, 1)));
    }
    if (which == "fetchRawMarketsByTags") {
      if (count <= 1)
        return awaitValue(this->fetchRawMarketsByTags(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(this->fetchRawMarketsByTags(::getValue(args, 0),
                                                      ::getValue(args, 1)));
    }
    if (which == "sign") {
      if (count <= 1)
        return this->sign(::getValue(args, 0));
      if (count == 2)
        return this->sign(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2));
      if (count == 4)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3));
      if (count == 5)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3),
                          ::getValue(args, 4));
      if (count >= 6)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3),
                          ::getValue(args, 4), ::getValue(args, 5));
    }
    if (which == "handleErrors") {
      if (true)
        return this->handleErrors(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7), ::getValue(args, 8));
    }
    // not defined on this exchange: fall back to the TS parent class
    return ccxt::PredictionExchange::callMethod(name, args);
  }
};

} // namespace prediction
} // namespace ccxt
