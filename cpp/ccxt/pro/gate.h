#pragma once

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#include "../base/Exchange.h"
#include "../exchanges/gate.h"
#include "ProExchangeFactory.h"

namespace ccxt {
namespace pro {

class gate : public ccxt::gate {
public:
  using ccxt::gate::gate;
  ccxt::any describe() override {
    ccxt::any superDescribe = ccxt::gate::describe();
    return this->deepExtend(superDescribe, this->describeData());
  }

  virtual ccxt::any describeData() {
    return ccxt::dict{
        {std::string("has"),
         ccxt::dict{
             {std::string("ws"), true},
             {std::string("cancelAllOrdersWs"), true},
             {std::string("cancelOrderWs"), true},
             {std::string("createMarketBuyOrderWithCostWs"), true},
             {std::string("createMarketOrderWs"), true},
             {std::string("createMarketOrderWithCostWs"), false},
             {std::string("createMarketSellOrderWithCostWs"), false},
             {std::string("createOrderWs"), true},
             {std::string("createOrdersWs"), true},
             {std::string("createPostOnlyOrderWs"), true},
             {std::string("createReduceOnlyOrderWs"), true},
             {std::string("createStopLimitOrderWs"), true},
             {std::string("createStopLossOrderWs"), true},
             {std::string("createStopMarketOrderWs"), false},
             {std::string("createStopOrderWs"), true},
             {std::string("createTakeProfitOrderWs"), true},
             {std::string("createTriggerOrderWs"), true},
             {std::string("editOrderWs"), true},
             {std::string("fetchOrderWs"), true},
             {std::string("fetchOrdersWs"), false},
             {std::string("fetchOpenOrdersWs"), true},
             {std::string("fetchClosedOrdersWs"), true},
             {std::string("watchOrderBook"), true},
             {std::string("watchBidsAsks"), true},
             {std::string("watchTicker"), true},
             {std::string("watchTickers"), true},
             {std::string("watchTrades"), true},
             {std::string("watchTradesForSymbols"), true},
             {std::string("watchMyTrades"), true},
             {std::string("watchOHLCV"), true},
             {std::string("watchBalance"), true},
             {std::string("watchOrders"), true},
             {std::string("watchLiquidations"), false},
             {std::string("watchLiquidationsForSymbols"), false},
             {std::string("watchMyLiquidations"), true},
             {std::string("watchMyLiquidationsForSymbols"), true},
             {std::string("watchPositions"), true},
             {std::string("unWatchTicker"), false},
             {std::string("unWatchTickers"), false},
             {std::string("unWatchOHLCV"), false},
             {std::string("unWatchOHLCVForSymbols"), false},
             {std::string("unWatchOrderBook"), true},
             {std::string("unWatchOrderBookForSymbols"), false},
             {std::string("unWatchTrades"), true},
             {std::string("unWatchTradesForSymbols"), true},
             {std::string("unWatchMyTrades"), false},
             {std::string("unWatchOrders"), false},
             {std::string("unWatchPositions"), false},
             {std::string("unWatchMarkPrices"), false},
             {std::string("unWatchMarkPrice"), false},
         }},
        {std::string("urls"),
         ccxt::dict{
             {std::string("api"),
              ccxt::dict{
                  {std::string("ws"), std::string("wss://ws.gate.io/v4")},
                  {std::string("spot"),
                   std::string("wss://api.gateio.ws/ws/v4/")},
                  {std::string("swap"),
                   ccxt::dict{
                       {std::string("usdt"),
                        std::string("wss://fx-ws.gateio.ws/v4/ws/usdt")},
                       {std::string("btc"),
                        std::string("wss://fx-ws.gateio.ws/v4/ws/btc")},
                   }},
                  {std::string("future"),
                   ccxt::dict{
                       {std::string("usdt"),
                        std::string(
                            "wss://fx-ws.gateio.ws/v4/ws/delivery/usdt")},
                       {std::string("btc"),
                        std::string(
                            "wss://fx-ws.gateio.ws/v4/ws/delivery/btc")},
                   }},
                  {std::string("option"),
                   ccxt::dict{
                       {std::string("usdt"),
                        std::string("wss://op-ws.gateio.live/v4/ws/usdt")},
                       {std::string("btc"),
                        std::string("wss://op-ws.gateio.live/v4/ws/btc")},
                   }},
              }},
             {std::string("test"),
              ccxt::dict{
                  {std::string("swap"),
                   ccxt::dict{
                       {std::string("usdt"),
                        std::string(
                            "wss://ws-testnet.gate.com/v4/ws/futures/usdt")},
                       {std::string("btc"),
                        std::string("wss://fx-ws-testnet.gateio.ws/v4/ws/btc")},
                   }},
                  {std::string("future"),
                   ccxt::dict{
                       {std::string("usdt"),
                        std::string("wss://fx-ws-testnet.gateio.ws/v4/ws/"
                                    "delivery/usdt")},
                       {std::string("btc"),
                        std::string("wss://fx-ws-testnet.gateio.ws/v4/ws/"
                                    "delivery/btc")},
                   }},
                  {std::string("option"),
                   ccxt::dict{
                       {std::string("usdt"),
                        std::string(
                            "wss://ws-testnet.gate.com/v4/ws/options/usdt")},
                       {std::string("btc"),
                        std::string(
                            "wss://ws-testnet.gate.com/v4/ws/options/btc")},
                   }},
              }},
         }},
        {std::string("options"),
         ccxt::dict{
             {std::string("tradesLimit"), 1000},
             {std::string("OHLCVLimit"), 1000},
             {std::string("watchTradesSubscriptions"), ccxt::dict{}},
             {std::string("watchTickerSubscriptions"), ccxt::dict{}},
             {std::string("watchOrderBookSubscriptions"), ccxt::dict{}},
             {std::string("watchTicker"),
              ccxt::dict{
                  {std::string("name"), std::string("tickers")},
              }},
             {std::string("watchOrderBook"),
              ccxt::dict{
                  {std::string("snapshotDelay"), 10},
                  {std::string("snapshotMaxRetries"), 3},
                  {std::string("checksum"), true},
              }},
             {std::string("watchBalance"),
              ccxt::dict{
                  {std::string("settle"), std::string("usdt")},
                  {std::string("spot"), std::string("spot.balances")},
              }},
             {std::string("watchPositions"),
              ccxt::dict{
                  {std::string("fetchPositionsSnapshot"), true},
                  {std::string("awaitPositionsSnapshot"), true},
              }},
         }},
        {std::string("exceptions"),
         ccxt::dict{
             {std::string("ws"),
              ccxt::dict{
                  {std::string("exact"),
                   ccxt::dict{
                       {std::string("1"), std::string("BadRequest")},
                       {std::string("2"), std::string("BadRequest")},
                       {std::string("4"), std::string("AuthenticationError")},
                       {std::string("6"), std::string("AuthenticationError")},
                       {std::string("11"), std::string("AuthenticationError")},
                   }},
                  {std::string("broad"), ccxt::dict{}},
              }},
         }},
    };
  }

  /**
   * @method
   * @name gate#createOrderWs
   * @see https://www.gate.io/docs/developers/apiv4/ws/en/#order-place
   * @see https://www.gate.io/docs/developers/futures/ws/en/#order-place
   * @description Create an order on the exchange
   * @param {string} symbol Unified CCXT market symbol
   * @param {string} type 'limit' or 'market' *"market" is contract only*
   * @param {string} side 'buy' or 'sell'
   * @param {float} amount the amount of currency to trade
   * @param {float} [price] *ignored in "market" orders* the price at which the
   * order is to be fulfilled at in units of the quote currency
   * @param {object} [params]  extra parameters specific to the exchange API
   * endpoint
   * @param {float} [params.stopPrice] The price at which a trigger order is
   * triggered at
   * @param {string} [params.timeInForce] "GTC", "IOC", or "PO"
   * @param {float} [params.stopLossPrice] The price at which a stop loss order
   * is triggered at
   * @param {float} [params.takeProfitPrice] The price at which a take profit
   * order is triggered at
   * @param {string} [params.marginMode] 'cross' or 'isolated' - marginMode for
   * margin trading if not provided ::getValue(this,
   * std::string("options"))['defaultMarginMode'] is used
   * @param {int} [params.iceberg] Amount to display for the iceberg order, Null
   * or 0 for normal orders, Set to -1 to hide the order completely
   * @param {string} [params.text] User defined information
   * @param {string} [params.account] *spot and margin only* "spot", "margin" or
   * "cross_margin"
   * @param {bool} [params.auto_borrow] *margin only* Used in margin or cross
   * margin trading to allow automatic loan of insufficient amount if balance is
   * not enough
   * @param {string} [params.settle] *contract only* Unified Currency Code for
   * settle currency
   * @param {bool} [params.reduceOnly] *contract only* Indicates if this order
   * is to reduce the size of a position
   * @param {bool} [params.close] *contract only* Set as true to close the
   * position, with size set to 0
   * @param {bool} [params.auto_size] *contract only* Set side to close
   * dual-mode position, close_long closes the long side, while close_short the
   * short one, size also needs to be set to 0
   * @param {int} [params.price_type] *contract only* 0 latest deal price, 1
   * mark price, 2 index price
   * @param {float} [params.cost] *spot market buy only* the quote quantity that
   * can be used as an alternative for the amount
   * @returns {object|undefined} [An order structure]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  createOrderWs(ccxt::any symbol, ccxt::any type, ccxt::any side, ccxt::any amount,
                ccxt::any price = ccxt::any{},
                ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                          awaitValue(this->loadMarkets());
                        }
                        ccxt::any market = this->market(symbol);
                        symbol = ::getValue(market, std::string("symbol"));
                        ccxt::any messageType = this->getTypeByMarket(market);
                        ccxt::any channel =
                            add(messageType, std::string(".order_place"));
                        ccxt::any url = this->getUrlByMarket(market);
                        ::setValue(params, std::string("textIsRequired"), true);
                        ccxt::any request = this->createOrderRequest(
                            symbol, type, side, amount, price, params);
                        awaitValue(this->authenticate(url, messageType));
                        ccxt::any rawOrder = awaitValue(
                            this->requestPrivate(url, request, channel));
                        ccxt::any order = this->parseOrder(rawOrder, market);
                        return order;
                      })
        .share();
  }

  /**
   * @method
   * @name gate#createOrdersWs
   * @description create a list of trade orders
   * @see https://www.gate.io/docs/developers/futures/ws/en/#order-batch-place
   * @param {Array} orders list of orders to create, each object should contain
   * the parameters required by createOrder, namely symbol, type, side, amount,
   * price and params
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} an [order structure]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  createOrdersWs(ccxt::any orders, ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any request = this->createOrdersRequest(orders, params);
                 ccxt::any firstOrder = ::getValue(orders, 0);
                 ccxt::any market = this->market(
                     ::getValue(firstOrder, std::string("symbol")));
                 if (isTrue(!isEqual(::getValue(market, std::string("swap")),
                                     true))) {
                   throw NotSupported(toString(add(
                       this->id, std::string(" createOrdersWs is not supported "
                                             "for swap markets"))));
                 }
                 // todo add swap support
                 ccxt::any messageType = this->getTypeByMarket(market);
                 ccxt::any channel =
                     add(messageType, std::string(".order_batch_place"));
                 ccxt::any url = this->getUrlByMarket(market);
                 awaitValue(this->authenticate(url, messageType));
                 ccxt::any rawOrders =
                     awaitValue(this->requestPrivate(url, request, channel));
                 return this->parseOrders(rawOrders, market);
               })
        .share();
  }

  /**
   * @method
   * @name gate#cancelAllOrdersWs
   * @description cancel all open orders
   * @see
   * https://www.gate.com/docs/developers/futures/ws/en/#cancel-matched-open-orders
   * @see
   * https://www.gate.io/docs/developers/apiv4/ws/en/#order-cancel-all-with-specified-currency-pair
   * @param {string} symbol unified market symbol, only orders in the market of
   * this symbol are cancelled when symbol is not undefined
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.channel] the channel to use, defaults to
   * spot.order_cancel_cp or futures.order_cancel_cp
   * @returns {object[]} a list of [order structures]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  cancelAllOrdersWs(ccxt::any symbol = ccxt::any{},
                    ccxt::any params = ccxt::dict{}) override {
    return std::
        async(std::launch::deferred,
              [=]() mutable -> ccxt::any {
                if (isTrue(isEqual(symbol, ccxt::any{}))) {
                  throw ArgumentsRequired(toString(add(
                      this->id,
                      std::string(
                          " cancelAllOrdersWs() requires a symbol argument"))));
                }
                if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                  awaitValue(this->loadMarkets());
                }
                ccxt::any market = (isTrue((isEqual(symbol, ccxt::any{})))
                                       ? ccxt::any(ccxt::any{})
                                       : ccxt::any(this->market(symbol)));
                ccxt::any trigger = this->safeBool2(params, std::string("stop"),
                                                   std::string("trigger"));
                ccxt::any messageType = this->getTypeByMarket(market);
                ccxt::any channel =
                    add(messageType, std::string(".order_cancel_cp"));
                ccxt::any channelparamsVariable = this->handleOptionAndParams(
                    params, std::string("cancelAllOrdersWs"),
                    std::string("channel"), channel);
                channel = ::getValue(channelparamsVariable, 0);
                params = ::getValue(channelparamsVariable, 1);
                ccxt::any url = this->getUrlByMarket(market);
                params = this->omit(params, ccxt::list{std::string("stop"),
                                                       std::string("trigger")});
                ccxt::any typequeryVariable = this->handleMarketTypeAndParams(
                    std::string("cancelAllOrders"), market, params);
                ccxt::any type = ::getValue(typequeryVariable, 0);
                ccxt::any query = ::getValue(typequeryVariable, 1);
                ccxt::any requestrequestParamsVariable =
                    (isTrue((isEqual(type, std::string("spot"))))
                         ? ccxt::any(this->multiOrderSpotPrepareRequest(
                               market, trigger, query))
                         : ccxt::any(this->prepareRequest(market, type, query)));
                ccxt::any request = ::getValue(requestrequestParamsVariable, 0);
                ccxt::any requestParams =
                    ::getValue(requestrequestParamsVariable, 1);
                awaitValue(this->authenticate(url, messageType));
                ccxt::any rawOrders = awaitValue(this->requestPrivate(
                    url, this->extend(request, requestParams), channel));
                return this->parseOrders(rawOrders, market);
              })
            .share();
  }

  /**
   * @method
   * @name gate#cancelOrderWs
   * @description Cancels an open order
   * @see https://www.gate.io/docs/developers/apiv4/ws/en/#order-cancel
   * @see https://www.gate.io/docs/developers/futures/ws/en/#order-cancel
   * @param {string} id Order id
   * @param {string} symbol Unified market symbol
   * @param {object} [params] Parameters specified by the exchange api
   * @param {bool} [params.trigger] True if the order to be cancelled is a
   * trigger order
   * @returns An [order structure]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  cancelOrderWs(ccxt::any id, ccxt::any symbol = ccxt::any{},
                ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = (isTrue((isEqual(symbol, ccxt::any{})))
                                        ? ccxt::any(ccxt::any{})
                                        : ccxt::any(this->market(symbol)));
                 ccxt::any trigger = this->safeValueN(
                     params,
                     ccxt::list{std::string("is_stop_order"),
                                std::string("stop"), std::string("trigger")},
                     false);
                 params =
                     this->omit(params, ccxt::list{std::string("is_stop_order"),
                                                   std::string("stop"),
                                                   std::string("trigger")});
                 ccxt::any typequeryVariable = this->handleMarketTypeAndParams(
                     std::string("cancelOrder"), market, params);
                 ccxt::any type = ::getValue(typequeryVariable, 0);
                 ccxt::any query = ::getValue(typequeryVariable, 1);
                 ccxt::any requestrequestParamsVariable =
                     (isTrue((isTrue(isEqual(type, std::string("spot"))) ||
                              isTrue(isEqual(type, std::string("margin")))))
                          ? ccxt::any(this->spotOrderPrepareRequest(
                                market, trigger, query))
                          : ccxt::any(
                                this->prepareRequest(market, type, query)));
                 ccxt::any request = ::getValue(requestrequestParamsVariable, 0);
                 ccxt::any requestParams =
                     ::getValue(requestrequestParamsVariable, 1);
                 ccxt::any messageType = this->getTypeByMarket(market);
                 ccxt::any channel =
                     add(messageType, std::string(".order_cancel"));
                 ccxt::any url = this->getUrlByMarket(market);
                 awaitValue(this->authenticate(url, messageType));
                 ::setValue(request, std::string("order_id"), toString(id));
                 ccxt::any res = awaitValue(this->requestPrivate(
                     url, this->extend(request, requestParams), channel));
                 return this->parseOrder(res, market);
               })
        .share();
  }

  /**
   * @method
   * @name gate#editOrderWs
   * @description edit a trade order, gate currently only supports the
   * modification of the price or amount fields
   * @see https://www.gate.io/docs/developers/apiv4/ws/en/#order-amend
   * @see https://www.gate.io/docs/developers/futures/ws/en/#order-amend
   * @param {string} id order id
   * @param {string} symbol unified symbol of the market to create an order in
   * @param {string} type 'market' or 'limit'
   * @param {string} side 'buy' or 'sell'
   * @param {float} amount how much of the currency you want to trade in units
   * of the base currency
   * @param {float} [price] the price at which the order is to be fulfilled, in
   * units of the quote currency, ignored in market orders
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} an [order structure]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  editOrderWs(ccxt::any id, ccxt::any symbol, ccxt::any type, ccxt::any side,
              ccxt::any amount = ccxt::any{}, ccxt::any price = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                          awaitValue(this->loadMarkets());
                        }
                        ccxt::any market = this->market(symbol);
                        ccxt::any extendedRequest = this->editOrderRequest(
                            id, symbol, type, side, amount, price, params);
                        ccxt::any messageType = this->getTypeByMarket(market);
                        ccxt::any channel =
                            add(messageType, std::string(".order_amend"));
                        ccxt::any url = this->getUrlByMarket(market);
                        awaitValue(this->authenticate(url, messageType));
                        ccxt::any rawOrder = awaitValue(this->requestPrivate(
                            url, extendedRequest, channel));
                        return this->parseOrder(rawOrder, market);
                      })
        .share();
  }

  /**
   * @method
   * @name gate#fetchOrderWs
   * @description Retrieves information on an order
   * @see https://www.gate.io/docs/developers/apiv4/ws/en/#order-status
   * @see https://www.gate.io/docs/developers/futures/ws/en/#order-status
   * @param {string} id Order id
   * @param {string} symbol Unified market symbol, *required for spot and
   * margin*
   * @param {object} [params] Parameters specified by the exchange api
   * @param {bool} [params.trigger] True if the order being fetched is a trigger
   * order
   * @param {string} [params.marginMode] 'cross' or 'isolated' - marginMode for
   * margin trading if not provided ::getValue(this,
   * std::string("options"))['defaultMarginMode'] is used
   * @param {string} [params.type] 'spot', 'swap', or 'future', if not provided
   * ::getValue(this, std::string("options"))['defaultMarginMode'] is used
   * @param {string} [params.settle] 'btc' or 'usdt' - settle currency for
   * perpetual swap and future - market settle currency is used if symbol !==
   * undefined, default="usdt" for swap and "btc" for future
   * @returns An [order structure]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  fetchOrderWs(ccxt::any id, ccxt::any symbol = ccxt::any{},
               ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = (isTrue((isEqual(symbol, ccxt::any{})))
                                        ? ccxt::any(ccxt::any{})
                                        : ccxt::any(this->market(symbol)));
                 ccxt::any requestrequestParamsVariable =
                     this->fetchOrderRequest(id, symbol, params);
                 ccxt::any request = ::getValue(requestrequestParamsVariable, 0);
                 ccxt::any requestParams =
                     ::getValue(requestrequestParamsVariable, 1);
                 ccxt::any messageType = this->getTypeByMarket(market);
                 ccxt::any channel =
                     add(messageType, std::string(".order_status"));
                 ccxt::any url = this->getUrlByMarket(market);
                 awaitValue(this->authenticate(url, messageType));
                 ccxt::any rawOrder = awaitValue(this->requestPrivate(
                     url, this->extend(request, requestParams), channel));
                 return this->parseOrder(rawOrder, market);
               })
        .share();
  }

  /**
   * @method
   * @name gate#fetchOpenOrdersWs
   * @description fetch all unfilled currently open orders
   * @see https://www.gate.io/docs/developers/futures/ws/en/#order-list
   * @param {string} symbol unified market symbol
   * @param {int} [since] the earliest time in ms to fetch open orders for
   * @param {int} [limit] the maximum number of  open orders structures to
   * retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {Order[]} a list of [order structures]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  fetchOpenOrdersWs(ccxt::any symbol = ccxt::any{}, ccxt::any since = ccxt::any{},
                    ccxt::any limit = ccxt::any{},
                    ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        return awaitValue(this->fetchOrdersByStatusWs(
                            std::string("open"), symbol, since, limit, params));
                      })
        .share();
  }

  /**
   * @method
   * @name gate#fetchClosedOrdersWs
   * @description fetches information on multiple closed orders made by the user
   * @see https://www.gate.io/docs/developers/futures/ws/en/#order-list
   * @param {string} symbol unified market symbol of the market orders were made
   * in
   * @param {int} [since] the earliest time in ms to fetch orders for
   * @param {int} [limit] the maximum number of order structures to retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {Order[]} a list of [order structures]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  fetchClosedOrdersWs(ccxt::any symbol = ccxt::any{}, ccxt::any since = ccxt::any{},
                      ccxt::any limit = ccxt::any{},
                      ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 return awaitValue(this->fetchOrdersByStatusWs(
                     std::string("finished"), symbol, since, limit, params));
               })
        .share();
  }

  /**
   * @method
   * @name gate#fetchOrdersWs
   * @see https://www.gate.io/docs/developers/futures/ws/en/#order-list
   * @description fetches information on multiple orders made by the user by
   * status
   * @param {string} status requested order status
   * @param {string} symbol unified market symbol of the market orders were made
   * in
   * @param {int|undefined} [since] the earliest time in ms to fetch orders for
   * @param {int|undefined} [limit] the maximum number of order structures to
   * retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {int} [params.orderId] order id to begin at
   * @param {int} [params.limit] the maximum number of order structures to
   * retrieve
   * @returns {object[]} a list of [order structures]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  fetchOrdersByStatusWs(ccxt::any status, ccxt::any symbol = ccxt::any{},
                        ccxt::any since = ccxt::any{},
                        ccxt::any limit = ccxt::any{},
                        ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = ccxt::any{};
                 if (isTrue(!isEqual(symbol, ccxt::any{}))) {
                   market = this->market(symbol);
                   symbol = ::getValue(market, std::string("symbol"));
                   if (isTrue(!isEqual(::getValue(market, std::string("swap")),
                                       true))) {
                     throw NotSupported(toString(add(
                         this->id,
                         std::string(
                             " fetchOrdersByStatusWs is only supported by swap "
                             "markets. Use rest API for other markets"))));
                   }
                 }
                 ccxt::any requestrequestParamsVariable =
                     this->prepareOrdersByStatusRequest(status, symbol, since,
                                                        limit, params);
                 ccxt::any request = ::getValue(requestrequestParamsVariable, 0);
                 ccxt::any requestParams =
                     ::getValue(requestrequestParamsVariable, 1);
                 ccxt::any newRequest =
                     this->omit(request, ccxt::list{std::string("settle")});
                 ccxt::any messageType = this->getTypeByMarket(market);
                 ccxt::any channel =
                     add(messageType, std::string(".order_list"));
                 ccxt::any url = this->getUrlByMarket(market);
                 awaitValue(this->authenticate(url, messageType));
                 ccxt::any rawOrders = awaitValue(this->requestPrivate(
                     url, this->extend(newRequest, requestParams), channel));
                 ccxt::any orders = this->parseOrders(rawOrders, market);
                 return this->filterBySymbolSinceLimit(orders, symbol, since,
                                                       limit);
               })
        .share();
  }

  /**
   * @method
   * @name gate#watchOrderBook
   * @description watches information on open orders with bid (buy) and ask
   * (sell) prices, volumes and other data
   * @see https://www.gate.com/docs/developers/apiv4/ws/en/#order-book-channel
   * @see https://www.gate.com/docs/developers/apiv4/ws/en/#order-book-v2-api
   * @see https://www.gate.com/docs/developers/futures/ws/en/#order-book-api
   * @see https://www.gate.com/docs/developers/futures/ws/en/#order-book-v2-api
   * @see https://www.gate.com/docs/developers/delivery/ws/en/#order-book-api
   * @see https://www.gate.com/docs/developers/options/ws/en/#order-book-channel
   * @param {string} symbol unified symbol of the market to fetch the order book
   * for
   * @param {int} [limit] the maximum amount of order book entries to return
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} an [order book structure]{@link
   * https://docs.ccxt.com/?id=order-book-structure}
   */
  std::shared_future<ccxt::any>
  watchOrderBook(ccxt::any symbol, ccxt::any limit = ccxt::any{},
                 ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = this->market(symbol);
                 symbol = ::getValue(market, std::string("symbol"));
                 ccxt::any marketId = ::getValue(market, std::string("id"));
                 ccxt::any url = this->getUrlByMarket(market);
                 ccxt::any isEuUrl = isGreaterThanOrEqual(
                     getIndexOf(url, std::string("gateeu")), 0);
                 ccxt::any isNonEuSpot =
                     isTrue((isEqual(::getValue(market, std::string("spot")),
                                     true))) &&
                     !isTrue(isEuUrl);
                 ccxt::any intervalDefault =
                     (isTrue(isNonEuSpot) ? ccxt::any(std::string("50"))
                                          : ccxt::any(std::string("100ms")));
                 ccxt::any intervalqueryVariable = this->handleOptionAndParams(
                     params, std::string("watchOrderBook"),
                     std::string("interval"), intervalDefault);
                 ccxt::any interval = ::getValue(intervalqueryVariable, 0);
                 ccxt::any query = ::getValue(intervalqueryVariable, 1);
                 ccxt::any messageType = this->getTypeByMarket(market);
                 ccxt::any messageHash = add(
                     add(std::string("orderbook"), std::string(":")), symbol);
                 if (isTrue(isEqual(limit, ccxt::any{}))) {
                   limit = (isTrue((isEqual(
                                ::getValue(market, std::string("spot")), true)))
                                ? ccxt::any(50)
                                : ccxt::any(100)); // max 100 atm
                   if (isTrue(isEqual(messageType, std::string("options")))) {
                     limit = 50; // max 50 for options
                   }
                 }
                 ccxt::any payload = ccxt::list{};
                 ccxt::any channel = std::string("");
                 if (isTrue(isEuUrl)) {
                   channel = std::string("spot.order_book_update");
                   payload = ccxt::list{marketId, interval};
                 } else if (isTrue(
                                isEqual(::getValue(market, std::string("spot")),
                                        true))) {
                   channel = std::string("spot.obu");
                   ccxt::any finalInterval = interval;
                   if (isTrue(isEqual(limit, 400))) {
                     finalInterval = std::string("400");
                   }
                   payload = ccxt::list{
                       add(add(add(std::string("ob."),
                                   ::getValue(market, std::string("id"))),
                               std::string(".")),
                           finalInterval)};
                 } else {
                   channel =
                       add(messageType, std::string(".order_book_update"));
                   payload = ccxt::list{marketId, interval};
                   ccxt::any stringLimit = toString(limit);
                   arrayPush(payload, stringLimit);
                 }
                 ccxt::any subscription = ccxt::dict{
                     {std::string("symbol"), symbol},
                     {std::string("limit"), limit},
                 };
                 ccxt::any orderbook = awaitValue(this->subscribePublic(
                     url, messageHash, payload, channel, query, subscription));
                 return ::wsLimit(orderbook);
               })
        .share();
  }

  /**
   * @method
   * @name gate#unWatchOrderBook
   * @description unWatches information on open orders with bid (buy) and ask
   * (sell) prices, volumes and other data
   * @param {string} symbol unified symbol of the market to fetch the order book
   * for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} A dictionary of [order book structures]{@link
   * https://docs.ccxt.com/?id=order-book-structure}
   */
  std::shared_future<ccxt::any>
  unWatchOrderBook(ccxt::any symbol, ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = this->market(symbol);
                 ccxt::any url = this->getUrlByMarket(market);
                 symbol = ::getValue(market, std::string("symbol"));
                 ccxt::any marketId = ::getValue(market, std::string("id"));
                 ccxt::any isEuUrl = isGreaterThanOrEqual(
                     getIndexOf(url, std::string("gateeu")), 0);
                 ccxt::any isNonEuSpot =
                     isTrue((isEqual(::getValue(market, std::string("spot")),
                                     true))) &&
                     !isTrue(isEuUrl);
                 ccxt::any intervalDefault =
                     (isTrue(isNonEuSpot) ? ccxt::any(std::string("50"))
                                          : ccxt::any(std::string("100ms")));
                 ccxt::any interval = intervalDefault;
                 ccxt::any intervalparamsVariable = this->handleOptionAndParams(
                     params, std::string("watchOrderBook"),
                     std::string("interval"), interval);
                 interval = ::getValue(intervalparamsVariable, 0);
                 params = ::getValue(intervalparamsVariable, 1);
                 ccxt::any messageType = this->getTypeByMarket(market);
                 ccxt::any limit =
                     this->safeInteger(params, std::string("limit"));
                 if (isTrue(isEqual(limit, ccxt::any{}))) {
                   limit = (isTrue((isEqual(
                                ::getValue(market, std::string("spot")), true)))
                                ? ccxt::any(50)
                                : ccxt::any(100)); // max 100 atm
                   if (isTrue(isEqual(messageType, std::string("options")))) {
                     limit = 50; // max 50 for options
                   }
                 }
                 ccxt::any payload = ccxt::list{};
                 ccxt::any channel = std::string("");
                 if (isTrue(isEuUrl)) {
                   channel = std::string("spot.order_book_update");
                   payload = ccxt::list{marketId, interval};
                 } else if (isTrue(
                                isEqual(::getValue(market, std::string("spot")),
                                        true))) {
                   channel = std::string("spot.obu");
                   ccxt::any finalInterval = interval;
                   if (isTrue(isEqual(limit, 400))) {
                     finalInterval = std::string("400");
                   }
                   payload = ccxt::list{
                       add(add(add(std::string("ob."),
                                   ::getValue(market, std::string("id"))),
                               std::string(".")),
                           finalInterval)};
                 } else {
                   channel =
                       add(messageType, std::string(".order_book_update"));
                   payload = ccxt::list{marketId, interval};
                   ccxt::any stringLimit = toString(limit);
                   arrayPush(payload, stringLimit);
                 }
                 ccxt::any subMessageHash = add(
                     add(std::string("orderbook"), std::string(":")), symbol);
                 ccxt::any messageHash =
                     add(add(std::string("unsubscribe:orderbook"),
                             std::string(":")),
                         symbol);
                 return awaitValue(this->unSubscribePublicMultiple(
                     url, std::string("orderbook"), ccxt::list{symbol},
                     ccxt::list{messageHash}, ccxt::list{subMessageHash},
                     payload, channel, params));
               })
        .share();
  }

  virtual void handleOrderBookSubscription(ccxt::any client, ccxt::any message,
                                           ccxt::any subscription) {
    ccxt::any symbol = this->safeString(subscription, std::string("symbol"));
    ccxt::any limit = this->safeInteger(subscription, std::string("limit"));
    if (isTrue(!isEqual(symbol, ccxt::any{}))) {
      ::setValue(this->orderbooks, symbol,
                 this->orderBook(ccxt::dict{}, limit));
    }
  }

  virtual void handleNewSpotOrderBook(ccxt::any client, ccxt::any message) {
    //
    //   {
    //      "channel":"spot.obu",
    //      "result":{
    //         "t":1777275365213,
    //         "full":true,
    //         "s":"ob.XRP_USDT.50",
    //         "u":9649549324,
    //         "b":[
    //            [
    //               "1.414",
    //               "1397.899"
    //            ]
    //         ],
    //         "a":[
    //            [
    //               "1.415",
    //               "17344.926"
    //            ]
    //         ]
    //      },
    //      "time_ms":1777275365214,
    //      "event":"update"
    //   }
    ccxt::any result =
        this->safeDict(message, std::string("result"), ccxt::dict{});
    ccxt::any full = this->safeBool(result, std::string("full"), false);
    ccxt::any marketIdWithPrefix = this->safeString(result, std::string("s"));
    if (isTrue(isEqual(marketIdWithPrefix, ccxt::any{}))) {
      return;
    }
    ccxt::any marketIdParts = split(marketIdWithPrefix, std::string("."));
    ccxt::any marketId = this->safeString(marketIdParts, 1);
    ccxt::any symbol = this->safeSymbol(marketId, ccxt::any{}, std::string("_"),
                                       std::string("spot"));
    ccxt::any messageHash = add(std::string("orderbook:"), symbol);
    if (isTrue(
            isEqual(this->safeValue(this->orderbooks, symbol), ccxt::any{}))) {
      ::setValue(this->orderbooks, symbol, this->orderBook(ccxt::dict{}, 1000));
    }
    ccxt::any orderbook = ::getValue(this->orderbooks, symbol);
    if (isTrue(isEqual(full, true))) {
      ccxt::any snapshopt = this->parseOrderBook(
          result, symbol, ccxt::any{}, std::string("b"), std::string("a"));
      ::setValue(snapshopt, std::string("nonce"),
                 this->safeInteger(result, std::string("u")));
      ::setValue(snapshopt, std::string("timestamp"),
                 this->safeInteger(result, std::string("t")));
      ::resetOrderBook(orderbook, snapshopt);
    } else {
      ccxt::any nonce = this->safeInteger(orderbook, std::string("nonce"));
      ccxt::any deltaStart = this->safeInteger(result, std::string("u"));
      if (isTrue(isTrue((isEqual(nonce, ccxt::any{}))) ||
                 isTrue((isTrue((!isEqual(deltaStart, ccxt::any{}))) &&
                         isTrue((isGreaterThanOrEqual(nonce, deltaStart))))))) {
        return;
      }
      this->handleDelta(orderbook, result);
    }
    this->resolve(orderbook, messageHash);
  }

  virtual void handleOrderBook(ccxt::any client, ccxt::any message) {
    //
    // spot
    //
    //     {
    //         "time": 1650189272,
    //         "channel": "spot.order_book_update",
    //         "event": "update",
    //         "result": {
    //             "t": 1650189272515,
    //             "e": "depthUpdate",
    //             "E": 1650189272,
    //             "s": "GMT_USDT",
    //             "U": 140595902,
    //             "u": 140595902,
    //             "b": [
    //                 [ '2.51518', "228.119" ],
    //                 [ '2.50587', "1510.11" ],
    //                 [ '2.49944', "67.6" ],
    //             ],
    //             "a": [
    //                 [ '2.5182', "4.199" ],
    //                 [ "2.51926", "1874" ],
    //                 [ '2.53528', "96.529" ],
    //             ]
    //         }
    //     }
    //
    // swap
    //
    //     {
    //         "id": null,
    //         "time": 1650188898,
    //         "channel": "futures.order_book_update",
    //         "event": "update",
    //         "error": null,
    //         "result": {
    //             "t": 1650188898938,
    //             "s": "GMT_USDT",
    //             "U": 1577718307,
    //             "u": 1577719254,
    //             "b": [
    //                 { p: "2.5178", s: 0 },
    //                 { p: "2.5179", s: 0 },
    //                 { p: "2.518", s: 0 },
    //             ],
    //             "a": [
    //                 { p: "2.52", s: 0 },
    //                 { p: "2.5201", s: 0 },
    //                 { p: "2.5203", s: 0 },
    //             ]
    //         }
    //     }
    //
    ccxt::any channel = this->safeString(message, std::string("channel"));
    if (isTrue(isEqual(channel, std::string("spot.obu")))) {
      this->handleNewSpotOrderBook(client, message);
      return;
    }
    ccxt::any channelParts = split(channel, std::string("."));
    ccxt::any rawMarketType = this->safeString(channelParts, 0);
    ccxt::any isSpot = isEqual(rawMarketType, std::string("spot"));
    ccxt::any marketType = (isTrue(isSpot) ? ccxt::any(std::string("spot"))
                                          : ccxt::any(std::string("contract")));
    ccxt::any delta = this->safeValue(message, std::string("result"));
    ccxt::any deltaStart = this->safeInteger(delta, std::string("U"));
    ccxt::any deltaEnd = this->safeInteger(delta, std::string("u"));
    ccxt::any marketId = this->safeString(delta, std::string("s"));
    ccxt::any symbol =
        this->safeSymbol(marketId, ccxt::any{}, std::string("_"), marketType);
    ccxt::any messageHash = add(std::string("orderbook:"), symbol);
    ccxt::any storedOrderBook = this->safeValue(this->orderbooks, symbol,
                                               this->orderBook(ccxt::dict{}));
    ccxt::any nonce = this->safeInteger(storedOrderBook, std::string("nonce"));
    if (isTrue(isEqual(nonce, ccxt::any{}))) {
      ccxt::any cacheLength = 0;
      if (isTrue(!isEqual(storedOrderBook, ccxt::any{}))) {
        cacheLength =
            getArrayLength(::getValue(storedOrderBook, std::string("cache")));
      }
      ccxt::any snapshotDelay = this->handleOption(
          std::string("watchOrderBook"), std::string("snapshotDelay"), 10);
      ccxt::any waitAmount =
          (isTrue(isSpot) ? ccxt::any(snapshotDelay) : ccxt::any(0));
      if (isTrue(isEqual(cacheLength, waitAmount))) {
        // max limit is 100
        ccxt::any subscription = ::getValue(
            ::getValue(client, std::string("subscriptions")), messageHash);
        ccxt::any limit = this->safeInteger(subscription, std::string("limit"));
        this->spawn(
            std::string("loadOrderBook"), client, messageHash, symbol, limit,
            ccxt::dict{}); // needed for c#, number of args needs to match
      }
      arrayPush(::getValue(storedOrderBook, std::string("cache")), delta);
      return;
    } else if (isTrue(isTrue((!isEqual(deltaEnd, ccxt::any{}))) &&
                      isTrue((isGreaterThanOrEqual(nonce, deltaEnd))))) {
      return;
    } else if (isTrue(isTrue((!isEqual(deltaStart, ccxt::any{}))) &&
                      isTrue((isGreaterThanOrEqual(
                          nonce, subtract(deltaStart, 1)))))) {
      this->handleDelta(storedOrderBook, delta);
    } else {
      deleteKey(::getValue(client, std::string("subscriptions")), messageHash);
      deleteKey(this->orderbooks, symbol);
      ccxt::any checksum = this->handleOption(std::string("watchOrderBook"),
                                             std::string("checksum"), true);
      if (isTrue(isEqual(checksum, true))) {
        ChecksumError error =
            ChecksumError(add(add(this->id, std::string(" ")),
                              this->orderbookChecksumMessage(symbol)));
        this->reject(error, messageHash);
      }
    }
    this->resolve(storedOrderBook, messageHash);
  }

  ccxt::any getCacheIndex(ccxt::any orderBook, ccxt::any cache) override {
    ccxt::any nonce = this->safeInteger(orderBook, std::string("nonce"));
    ccxt::any firstDelta = ::getValue(cache, 0);
    ccxt::any firstDeltaStart = this->safeInteger(firstDelta, std::string("U"));
    if (isTrue(isTrue(isTrue((!isEqual(nonce, ccxt::any{}))) &&
                      isTrue((!isEqual(firstDeltaStart, ccxt::any{})))) &&
               isTrue((isLessThan(nonce, firstDeltaStart))))) {
      return -1;
    }
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(cache));
         postFixIncrement(i)) {
      ccxt::any delta = ::getValue(cache, i);
      ccxt::any deltaStart = this->safeInteger(delta, std::string("U"));
      ccxt::any deltaEnd = this->safeInteger(delta, std::string("u"));
      if (isTrue(
              isTrue(
                  isTrue(isTrue(isTrue((!isEqual(nonce, ccxt::any{}))) &&
                                isTrue((!isEqual(deltaStart, ccxt::any{})))) &&
                         isTrue((!isEqual(deltaEnd, ccxt::any{})))) &&
                  isTrue((
                      isGreaterThanOrEqual(nonce, subtract(deltaStart, 1))))) &&
              isTrue((isLessThan(nonce, deltaEnd))))) {
        return i;
      }
    }
    return getArrayLength(cache);
  }

  virtual void handleBidAsks(ccxt::any bookSide, ccxt::any bidAsks) {
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(bidAsks));
         postFixIncrement(i)) {
      ccxt::any bidAsk = ::getValue(bidAsks, i);
      if (isTrue(isArray(bidAsk))) {
        ::wsStoreArray(bookSide, this->parseOrderBookBidAsk(bidAsk));
      } else {
        ccxt::any price = this->safeFloat(bidAsk, std::string("p"));
        ccxt::any amount = this->safeFloat(bidAsk, std::string("s"));
        ::wsStore(bookSide, price, amount);
      }
    }
  }

  void handleDelta(ccxt::any orderbook, ccxt::any delta) override {
    ccxt::any timestamp = this->safeInteger(delta, std::string("t"));
    ::setValue(orderbook, std::string("timestamp"), timestamp);
    ::setValue(orderbook, std::string("datetime"), this->iso8601(timestamp));
    ::setValue(orderbook, std::string("nonce"),
               this->safeInteger(delta, std::string("u")));
    ccxt::any bids = this->safeValue(delta, std::string("b"), ccxt::list{});
    ccxt::any asks = this->safeValue(delta, std::string("a"), ccxt::list{});
    ccxt::any storedBids = ::getValue(orderbook, std::string("bids"));
    ccxt::any storedAsks = ::getValue(orderbook, std::string("asks"));
    this->handleBidAsks(storedBids, bids);
    this->handleBidAsks(storedAsks, asks);
  }

  /**
   * @method
   * @name gate#watchTicker
   * @see https://www.gate.io/docs/developers/apiv4/ws/en/#tickers-channel
   * @see https://www.gate.com/docs/developers/futures/ws/en/#tickers-api
   * @see https://www.gate.com/docs/developers/delivery/ws/en/#tickers-api
   * @description watches a price ticker, a statistical calculation with the
   * information calculated over the past 24 hours for a specific market
   * @param {string} symbol unified symbol of the market to fetch the ticker for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [ticker structure]{@link
   * https://docs.ccxt.com/?id=ticker-structure}
   */
  std::shared_future<ccxt::any>
  watchTicker(ccxt::any symbol, ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                          awaitValue(this->loadMarkets());
                        }
                        ccxt::any market = this->market(symbol);
                        symbol = ::getValue(market, std::string("symbol"));
                        ::setValue(params, std::string("callerMethodName"),
                                   std::string("watchTicker"));
                        ccxt::any result = awaitValue(
                            this->watchTickers(ccxt::list{symbol}, params));
                        return this->safeValue(result, symbol);
                      })
        .share();
  }

  /**
   * @method
   * @name gate#watchTickers
   * @see https://www.gate.io/docs/developers/apiv4/ws/en/#tickers-channel
   * @see https://www.gate.com/docs/developers/futures/ws/en/#tickers-api
   * @see https://www.gate.com/docs/developers/delivery/ws/en/#tickers-api
   * @description watches a price ticker, a statistical calculation with the
   * information calculated over the past 24 hours for all markets of a specific
   * list
   * @param {string[]} symbols unified symbol of the market to fetch the ticker
   * for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [ticker structure]{@link
   * https://docs.ccxt.com/?id=ticker-structure}
   */
  std::shared_future<ccxt::any>
  watchTickers(ccxt::any symbols = ccxt::any{},
               ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 return awaitValue(this->subscribeWatchTickersAndBidsAsks(
                     symbols, std::string("watchTickers"),
                     this->extend(
                         ccxt::dict{
                             {std::string("method"), std::string("tickers")},
                         },
                         params)));
               })
        .share();
  }

  virtual void handleTicker(ccxt::any client, ccxt::any message) {
    //
    //    {
    //        "time": 1649326221,
    //        "channel": "spot.tickers",
    //        "event": "update",
    //        "result": {
    //          "currency_pair": "BTC_USDT",
    //          "last": "43444.82",
    //          "lowest_ask": "43444.82",
    //          "highest_bid": "43444.81",
    //          "change_percentage": "-4.0036",
    //          "base_volume": "5182.5412425462",
    //          "quote_volume": "227267634.93123952",
    //          "high_24h": "47698",
    //          "low_24h": "42721.03"
    //        }
    //    }
    //
    this->handleTickerAndBidAsk(std::string("ticker"), client, message);
  }

  /**
   * @method
   * @name gate#watchBidsAsks
   * @see https://www.gate.io/docs/developers/apiv4/ws/en/#best-bid-or-ask-price
   * @see https://www.gate.io/docs/developers/apiv4/ws/en/#order-book-channel
   * @see
   * https://www.gate.com/docs/developers/options/ws/en/#best-bid-or-ask-price
   * @description watches best bid & ask for symbols
   * @param {string[]} symbols unified symbol of the market to fetch the ticker
   * for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [ticker structure]{@link
   * https://docs.ccxt.com/?id=ticker-structure}
   */
  std::shared_future<ccxt::any>
  watchBidsAsks(ccxt::any symbols = ccxt::any{},
                ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        return awaitValue(
                            this->subscribeWatchTickersAndBidsAsks(
                                symbols, std::string("watchBidsAsks"),
                                this->extend(
                                    ccxt::dict{
                                        {std::string("method"),
                                         std::string("book_ticker")},
                                    },
                                    params)));
                      })
        .share();
  }

  virtual void handleBidAsk(ccxt::any client, ccxt::any message) {
    //
    //    {
    //        "time": 1671363004,
    //        "time_ms": 1671363004235,
    //        "channel": "spot.book_ticker",
    //        "event": "update",
    //        "result": {
    //          "t": 1671363004228,
    //          "u": 9793320464,
    //          "s": "BTC_USDT",
    //          "b": "16716.8",
    //          "B": "0.0134",
    //          "a": "16716.9",
    //          "A": "0.0353"
    //        }
    //    }
    //
    this->handleTickerAndBidAsk(std::string("bidask"), client, message);
  }

  virtual std::shared_future<ccxt::any>
  subscribeWatchTickersAndBidsAsks(ccxt::any symbols = ccxt::any{},
                                   ccxt::any callerMethodName = ccxt::any{},
                                   ccxt::any params = ccxt::dict{}) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any callerMethodNameparamsVariable =
                     this->handleParamString(params,
                                             std::string("callerMethodName"),
                                             callerMethodName);
                 callerMethodName =
                     ::getValue(callerMethodNameparamsVariable, 0);
                 params = ::getValue(callerMethodNameparamsVariable, 1);
                 symbols = this->marketSymbols(symbols, ccxt::any{}, false);
                 ccxt::any market = this->market(::getValue(symbols, 0));
                 ccxt::any messageType = this->getTypeByMarket(market);
                 ccxt::any marketIds = this->marketIds(symbols);
                 ccxt::any channelName = ccxt::any{};
                 ccxt::any channelNameparamsVariable =
                     this->handleOptionAndParams(params, callerMethodName,
                                                 std::string("method"));
                 channelName = ::getValue(channelNameparamsVariable, 0);
                 params = ::getValue(channelNameparamsVariable, 1);
                 ccxt::any url = this->getUrlByMarket(market);
                 ccxt::any channel =
                     add(add(messageType, std::string(".")), channelName);
                 if (isTrue(isEqual(callerMethodName, ccxt::any{}))) {
                   throw ArgumentsRequired(toString(add(
                       this->id,
                       std::string(" requires a callerMethodName argument"))));
                 }
                 ccxt::any isWatchTickers = isGreaterThanOrEqual(
                     getIndexOf(callerMethodName, std::string("watchTicker")),
                     0);
                 ccxt::any prefix =
                     (isTrue(isWatchTickers) ? ccxt::any(std::string("ticker"))
                                             : ccxt::any(std::string("bidask")));
                 ccxt::any messageHashes = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(symbols));
                      postFixIncrement(i)) {
                   ccxt::any symbol = ::getValue(symbols, i);
                   arrayPush(messageHashes,
                             add(add(prefix, std::string(":")), symbol));
                 }
                 ccxt::any tickerOrBidAsk =
                     awaitValue(this->subscribePublicMultiple(
                         url, messageHashes, marketIds, channel, params));
                 if (isTrue(this->newUpdates)) {
                   ccxt::any items = ccxt::dict{};
                   ::setValue(items,
                              ::getValue(tickerOrBidAsk, std::string("symbol")),
                              tickerOrBidAsk);
                   return items;
                 }
                 ccxt::any result =
                     (isTrue(isWatchTickers) ? ccxt::any(this->tickers)
                                             : ccxt::any(this->bidsasks));
                 return this->filterByArray(result, std::string("symbol"),
                                            symbols, true);
               })
        .share();
  }

  virtual void handleTickerAndBidAsk(ccxt::any objectName, ccxt::any client,
                                     ccxt::any message) {
    ccxt::any channel = this->safeString(message, std::string("channel"));
    ccxt::any parts = split(channel, std::string("."));
    ccxt::any rawMarketType = this->safeString(parts, 0);
    ccxt::any marketType =
        (isTrue((isEqual(rawMarketType, std::string("futures"))))
             ? ccxt::any(std::string("contract"))
             : ccxt::any(std::string("spot")));
    ccxt::any result = this->safeValue(message, std::string("result"));
    ccxt::any results = ccxt::list{};
    if (isTrue(isArray(result))) {
      results = this->safeList(message, std::string("result"), ccxt::list{});
    } else {
      ccxt::any rawTicker =
          this->safeDict(message, std::string("result"), ccxt::dict{});
      results = ccxt::list{rawTicker};
    }
    ccxt::any isTicker = (isEqual(
        objectName, std::string("ticker"))); // whether ticker or bid-ask
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(results));
         postFixIncrement(i)) {
      ccxt::any rawTicker = ::getValue(results, i);
      ccxt::any marketId = this->safeString(rawTicker, std::string("s"));
      ccxt::any market =
          this->safeMarket(marketId, ccxt::any{}, std::string("_"), marketType);
      ccxt::any parsedItem = this->parseTicker(rawTicker, market);
      ccxt::any symbol = ::getValue(parsedItem, std::string("symbol"));
      if (isTrue(isTicker)) {
        if (isTrue(!isEqual(symbol, ccxt::any{}))) {
          ::setValue(this->tickers, symbol, parsedItem);
        }
      } else {
        if (isTrue(!isEqual(symbol, ccxt::any{}))) {
          ::setValue(this->bidsasks, symbol, parsedItem);
        }
      }
      ccxt::any messageHash = add(add(objectName, std::string(":")), symbol);
      this->resolve(parsedItem, messageHash);
    }
  }

  /**
   * @method
   * @name gate#watchTrades
   * @see
   * https://www.gate.com/docs/developers/apiv4/ws/en/#public-trades-channel
   * @see https://www.gate.com/docs/developers/futures/ws/en/#trades-api
   * @see https://www.gate.com/docs/developers/delivery/ws/en/#trades-api
   * @see
   * https://www.gate.com/docs/developers/options/ws/en/#public-contract-trades-channel
   * @description get the list of most recent trades for a particular symbol
   * @param {string} symbol unified symbol of the market to fetch trades for
   * @param {int} [since] timestamp in ms of the earliest trade to fetch
   * @param {int} [limit] the maximum amount of trades to fetch
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [trade structures]{@link
   * https://docs.ccxt.com/?id=public-trades}
   */
  std::shared_future<ccxt::any>
  watchTrades(ccxt::any symbol, ccxt::any since = ccxt::any{},
              ccxt::any limit = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        return awaitValue(this->watchTradesForSymbols(
                            ccxt::list{symbol}, since, limit, params));
                      })
        .share();
  }

  /**
   * @method
   * @name gate#watchTradesForSymbols
   * @see
   * https://www.gate.com/docs/developers/apiv4/ws/en/#public-trades-channel
   * @see https://www.gate.com/docs/developers/futures/ws/en/#trades-api
   * @see https://www.gate.com/docs/developers/delivery/ws/en/#trades-api
   * @see
   * https://www.gate.com/docs/developers/options/ws/en/#public-contract-trades-channel
   * @description get the list of most recent trades for a particular symbol
   * @param {string[]} symbols unified symbol of the market to fetch trades for
   * @param {int} [since] timestamp in ms of the earliest trade to fetch
   * @param {int} [limit] the maximum amount of trades to fetch
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [trade structures]{@link
   * https://docs.ccxt.com/?id=public-trades}
   */
  std::shared_future<ccxt::any>
  watchTradesForSymbols(ccxt::any symbols, ccxt::any since = ccxt::any{},
                        ccxt::any limit = ccxt::any{},
                        ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 symbols = this->marketSymbols(symbols);
                 ccxt::any marketIds = this->marketIds(symbols);
                 ccxt::any market = this->market(::getValue(symbols, 0));
                 ccxt::any messageType = this->getTypeByMarket(market);
                 ccxt::any channel = add(messageType, std::string(".trades"));
                 ccxt::any messageHashes = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(symbols));
                      postFixIncrement(i)) {
                   ccxt::any symbol = ::getValue(symbols, i);
                   arrayPush(messageHashes,
                             add(std::string("trades:"), symbol));
                 }
                 ccxt::any url = this->getUrlByMarket(market);
                 ccxt::any trades = awaitValue(this->subscribePublicMultiple(
                     url, messageHashes, marketIds, channel, params));
                 if (isTrue(this->newUpdates)) {
                   ccxt::any first = this->safeValue(trades, 0);
                   ccxt::any tradeSymbol =
                       this->safeString(first, std::string("symbol"));
                   limit = ::wsGetLimit(trades, tradeSymbol, limit);
                 }
                 return this->filterBySinceLimit(
                     trades, since, limit, std::string("timestamp"), true);
               })
        .share();
  }

  /**
   * @method
   * @name gate#unWatchTradesForSymbols
   * @description get the list of most recent trades for a particular symbol
   * @param {string[]} symbols unified symbol of the market to fetch trades for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [trade structures]{@link
   * https://docs.ccxt.com/?id=public-trades}
   */
  std::shared_future<ccxt::any>
  unWatchTradesForSymbols(ccxt::any symbols,
                          ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 symbols = this->marketSymbols(symbols);
                 ccxt::any marketIds = this->marketIds(symbols);
                 ccxt::any market = this->market(::getValue(symbols, 0));
                 ccxt::any messageType = this->getTypeByMarket(market);
                 ccxt::any channel = add(messageType, std::string(".trades"));
                 ccxt::any subMessageHashes = ccxt::list{};
                 ccxt::any messageHashes = ccxt::list{};
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(symbols));
                      postFixIncrement(i)) {
                   ccxt::any symbol = ::getValue(symbols, i);
                   arrayPush(subMessageHashes,
                             add(std::string("trades:"), symbol));
                   arrayPush(messageHashes,
                             add(std::string("unsubscribe:trades:"), symbol));
                 }
                 ccxt::any url = this->getUrlByMarket(market);
                 return awaitValue(this->unSubscribePublicMultiple(
                     url, std::string("trades"), symbols, messageHashes,
                     subMessageHashes, marketIds, channel, params));
               })
        .share();
  }

  /**
   * @method
   * @name gate#unWatchTrades
   * @description get the list of most recent trades for a particular symbol
   * @param {string} symbol unified symbol of the market to fetch trades for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [trade structures]{@link
   * https://docs.ccxt.com/?id=public-trades}
   */
  std::shared_future<ccxt::any>
  unWatchTrades(ccxt::any symbol, ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        return awaitValue(this->unWatchTradesForSymbols(
                            ccxt::list{symbol}, params));
                      })
        .share();
  }

  virtual void handleTrades(ccxt::any client, ccxt::any message) {
    //
    // {
    //     "time": 1648725035,
    //     "channel": "spot.trades",
    //     "event": "update",
    //     "result": [{
    //       "id": 3130257995,
    //       "create_time": 1648725035,
    //       "create_time_ms": "1648725035923.0",
    //       "side": "sell",
    //       "currency_pair": "LTC_USDT",
    //       "amount": "0.0116",
    //       "price": "130.11"
    //     }]
    // }
    //
    ccxt::any result = this->safeValue(message, std::string("result"));
    if (!isTrue(isArray(result))) {
      result = ccxt::list{result};
    }
    ccxt::any parsedTrades = this->parseTrades(result);
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(parsedTrades));
         postFixIncrement(i)) {
      ccxt::any trade = ::getValue(parsedTrades, i);
      ccxt::any symbol = ::getValue(trade, std::string("symbol"));
      ccxt::any cachedTrades = this->safeValue(this->trades, symbol);
      if (isTrue(isEqual(cachedTrades, ccxt::any{}))) {
        ccxt::any limit =
            this->safeInteger(this->options, std::string("tradesLimit"), 1000);
        cachedTrades = ccxt::ws::ArrayCache(limit);
        if (isTrue(!isEqual(symbol, ccxt::any{}))) {
          ::setValue(this->trades, symbol, cachedTrades);
        }
      }
      ::wsAppend(cachedTrades, trade);
      ccxt::any hash = add(std::string("trades:"), symbol);
      this->resolve(cachedTrades, hash);
    }
  }

  /**
   * @method
   * @name gate#watchOHLCV
   * @see https://www.gate.com/docs/developers/apiv4/ws/en/#candlesticks-channel
   * @see https://www.gate.com/docs/developers/futures/ws/en/#candlesticks-api
   * @see https://www.gate.com/docs/developers/delivery/ws/en/#candlesticks-api
   * @description watches historical candlestick data containing the open, high,
   * low, and close price, and the volume of a market
   * @param {string} symbol unified symbol of the market to fetch OHLCV data for
   * @param {string} timeframe the length of time each candle represents
   * @param {int} [since] timestamp in ms of the earliest candle to fetch
   * @param {int} [limit] the maximum amount of candles to fetch
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {int[][]} A list of candles ordered as timestamp, open, high, low,
   * close, volume
   */
  std::shared_future<ccxt::any>
  watchOHLCV(ccxt::any symbol, ccxt::any timeframe = std::string("1m"),
             ccxt::any since = ccxt::any{}, ccxt::any limit = ccxt::any{},
             ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 // todo add options support
                 ccxt::any market = this->market(symbol);
                 symbol = ::getValue(market, std::string("symbol"));
                 ccxt::any marketId = ::getValue(market, std::string("id"));
                 ccxt::any interval =
                     this->safeString(this->timeframes, timeframe, timeframe);
                 ccxt::any messageType = this->getTypeByMarket(market);
                 ccxt::any channel =
                     add(messageType, std::string(".candlesticks"));
                 ccxt::any messageHash =
                     add(add(add(std::string("candles:"), interval),
                             std::string(":")),
                         ::getValue(market, std::string("symbol")));
                 ccxt::any url = this->getUrlByMarket(market);
                 ccxt::any payload = ccxt::list{interval, marketId};
                 ccxt::any ohlcv = awaitValue(this->subscribePublic(
                     url, messageHash, payload, channel, params));
                 if (isTrue(this->newUpdates)) {
                   limit = ::wsGetLimit(ohlcv, symbol, limit);
                 }
                 return this->filterBySinceLimit(ohlcv, since, limit, 0, true);
               })
        .share();
  }

  virtual void handleOHLCV(ccxt::any client, ccxt::any message) {
    //
    // {
    //     "time": 1606292600,
    //     "channel": "spot.candlesticks",
    //     "event": "update",
    //     "result": {
    //       "t": "1606292580", // total volume
    //       "v": "2362.32035", // volume
    //       "c": "19128.1", // close
    //       "h": "19128.1", // high
    //       "l": "19128.1", // low
    //       "o": "19128.1", // open
    //       "n": "1m_BTC_USDT" // sub
    //     }
    //   }
    //
    ccxt::any channel = this->safeString(message, std::string("channel"));
    ccxt::any channelParts = split(channel, std::string("."));
    ccxt::any rawMarketType = this->safeString(channelParts, 0);
    ccxt::any marketType = (isTrue((isEqual(rawMarketType, std::string("spot"))))
                               ? ccxt::any(std::string("spot"))
                               : ccxt::any(std::string("contract")));
    ccxt::any result = this->safeValue(message, std::string("result"));
    if (!isTrue(isArray(result))) {
      result = ccxt::list{result};
    }
    ccxt::any marketIds = ccxt::dict{};
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(result));
         postFixIncrement(i)) {
      ccxt::any ohlcv = ::getValue(result, i);
      ccxt::any subscription =
          this->safeString(ohlcv, std::string("n"), std::string(""));
      ccxt::any parts = split(subscription, std::string("_"));
      ccxt::any timeframeId = this->safeString(parts, 0);
      ccxt::any timeframe = this->findTimeframe(timeframeId);
      ccxt::any prefix = add(timeframe, std::string("_"));
      ccxt::any marketId = replace(subscription, prefix, std::string(""));
      ccxt::any symbol =
          this->safeSymbol(marketId, ccxt::any{}, std::string("_"), marketType);
      ccxt::any parsed = this->parseOHLCV(ohlcv);
      ::setValue(this->ohlcvs, symbol,
                 this->safeValue(this->ohlcvs, symbol, ccxt::dict{}));
      ccxt::any stored =
          this->safeValue(this->safeValue(this->ohlcvs, symbol), timeframe);
      if (isTrue(isEqual(stored, ccxt::any{}))) {
        ccxt::any limit =
            this->safeInteger(this->options, std::string("OHLCVLimit"), 1000);
        stored = ccxt::ws::ArrayCacheByTimestamp(limit);
        if (isTrue(isTrue(!isEqual(symbol, ccxt::any{})) &&
                   isTrue(!isEqual(timeframe, ccxt::any{})))) {
          ::setValue(::getValue(this->ohlcvs, symbol), timeframe, stored);
        }
      }
      ::wsAppend(stored, parsed);
      ::setValue(marketIds, symbol, timeframe);
    }
    ccxt::any keys = getObjectKeys(marketIds);
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(keys));
         postFixIncrement(i)) {
      ccxt::any symbol = ::getValue(keys, i);
      ccxt::any timeframe = ::getValue(marketIds, symbol);
      ccxt::any interval = this->findTimeframe(timeframe);
      ccxt::any hash =
          add(add(add(add(std::string("candles"), std::string(":")), interval),
                  std::string(":")),
              symbol);
      ccxt::any stored =
          this->safeValue(::getValue(this->ohlcvs, symbol), interval);
      this->resolve(stored, hash);
    }
  }

  /**
   * @method
   * @name gate#watchMyTrades
   * @see https://www.gate.com/docs/developers/apiv4/ws/en/#user-trades-channel
   * @see https://www.gate.com/docs/developers/futures/ws/en/#user-trades-api
   * @see https://www.gate.com/docs/developers/delivery/ws/en/#user-trades-api
   * @see
   * https://www.gate.com/docs/developers/options/ws/en/#user-trades-channel
   * @description watches information on multiple trades made by the user
   * @param {string} symbol unified market symbol of the market trades were made
   * in
   * @param {int} [since] the earliest time in ms to fetch trades for
   * @param {int} [limit] the maximum number of trade structures to retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [trade structures]{@link
   * https://docs.ccxt.com/?id=trade-structure}
   */
  std::shared_future<ccxt::any>
  watchMyTrades(ccxt::any symbol = ccxt::any{}, ccxt::any since = ccxt::any{},
                ccxt::any limit = ccxt::any{},
                ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any subType = ccxt::any{};
                 ccxt::any type = ccxt::any{};
                 ccxt::any marketId = add(std::string("!"), std::string("all"));
                 ccxt::any market = ccxt::any{};
                 if (isTrue(!isEqual(symbol, ccxt::any{}))) {
                   market = this->market(symbol);
                   marketId = ::getValue(market, std::string("id"));
                 }
                 ccxt::any typeparamsVariable = this->handleMarketTypeAndParams(
                     std::string("watchMyTrades"), market, params);
                 type = ::getValue(typeparamsVariable, 0);
                 params = ::getValue(typeparamsVariable, 1);
                 ccxt::any subTypeparamsVariable = this->handleSubTypeAndParams(
                     std::string("watchMyTrades"), market, params);
                 subType = ::getValue(subTypeparamsVariable, 0);
                 params = ::getValue(subTypeparamsVariable, 1);
                 ccxt::any messageType = this->getSupportedMapping(
                     type, ccxt::dict{
                               {std::string("spot"), std::string("spot")},
                               {std::string("margin"), std::string("spot")},
                               {std::string("future"), std::string("futures")},
                               {std::string("swap"), std::string("futures")},
                               {std::string("option"), std::string("options")},
                           });
                 ccxt::any channel =
                     add(messageType, std::string(".usertrades"));
                 ccxt::any messageHash = std::string("myTrades");
                 if (isTrue(!isEqual(symbol, ccxt::any{}))) {
                   messageHash =
                       add(messageHash, add(std::string(":"), symbol));
                 }
                 ccxt::any isInverse =
                     (isEqual(subType, std::string("inverse")));
                 ccxt::any url = this->getUrlByMarketType(type, isInverse);
                 ccxt::any payload = ccxt::list{marketId};
                 // uid required for non spot markets
                 ccxt::any requiresUid = (!isEqual(type, std::string("spot")));
                 ccxt::any trades = awaitValue(this->subscribePrivate(
                     url, messageHash, payload, channel, params, requiresUid));
                 if (isTrue(this->newUpdates)) {
                   limit = ::wsGetLimit(trades, symbol, limit);
                 }
                 return this->filterBySymbolSinceLimit(trades, symbol, since,
                                                       limit, true);
               })
        .share();
  }

  virtual void handleMyTrades(ccxt::any client, ccxt::any message) {
    //
    // {
    //     "time": 1543205083,
    //     "channel": "futures.usertrades",
    //     "event": "update",
    //     "error": null,
    //     "result": [
    //       {
    //         "id": "3335259",
    //         "create_time": 1628736848,
    //         "create_time_ms": 1628736848321,
    //         "contract": "BTC_USD",
    //         "order_id": "4872460",
    //         "size": 1,
    //         "price": "40000.4",
    //         "role": "maker"
    //       }
    //     ]
    // }
    //
    ccxt::any result =
        this->safeValue(message, std::string("result"), ccxt::list{});
    ccxt::any tradesLength = getArrayLength(result);
    if (isTrue(isEqual(tradesLength, 0))) {
      return;
    }
    ccxt::any cachedTrades = this->myTrades;
    if (isTrue(isEqual(cachedTrades, ccxt::any{}))) {
      ccxt::any limit =
          this->safeInteger(this->options, std::string("tradesLimit"), 1000);
      cachedTrades = ccxt::ws::ArrayCacheBySymbolById(limit);
      this->myTrades = cachedTrades;
    }
    ccxt::any parsed = this->parseTrades(result);
    ccxt::any marketIds = ccxt::dict{};
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(parsed));
         postFixIncrement(i)) {
      ccxt::any trade = ::getValue(parsed, i);
      ::wsAppend(cachedTrades, trade);
      ccxt::any symbol = ::getValue(trade, std::string("symbol"));
      if (isTrue(!isEqual(symbol, ccxt::any{}))) {
        ::setValue(marketIds, symbol, true);
      }
    }
    ccxt::any keys = getObjectKeys(marketIds);
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(keys));
         postFixIncrement(i)) {
      ccxt::any market = ::getValue(keys, i);
      ccxt::any hash = add(std::string("myTrades:"), market);
      this->resolve(cachedTrades, hash);
    }
    this->resolve(cachedTrades, std::string("myTrades"));
  }

  /**
   * @method
   * @name gate#watchBalance
   * @description watch balance and get the amount of funds available for
   * trading or funds locked in orders
   * @see https://www.gate.com/docs/developers/apiv4/ws/en/#spot-balance-channel
   * @see https://www.gate.com/docs/developers/futures/ws/en/#balances-api
   * @see https://www.gate.com/docs/developers/delivery/ws/en/#balances-api
   * @see https://www.gate.com/docs/developers/options/ws/en/#balances-channel
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [balance structure]{@link
   * https://docs.ccxt.com/?id=balance-structure}
   */
  std::shared_future<ccxt::any>
  watchBalance(ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any type = ccxt::any{};
                 ccxt::any subType = ccxt::any{};
                 ccxt::any typeparamsVariable = this->handleMarketTypeAndParams(
                     std::string("watchBalance"), ccxt::any{}, params);
                 type = ::getValue(typeparamsVariable, 0);
                 params = ::getValue(typeparamsVariable, 1);
                 ccxt::any subTypeparamsVariable = this->handleSubTypeAndParams(
                     std::string("watchBalance"), ccxt::any{}, params);
                 subType = ::getValue(subTypeparamsVariable, 0);
                 params = ::getValue(subTypeparamsVariable, 1);
                 ccxt::any isInverse =
                     (isEqual(subType, std::string("inverse")));
                 ccxt::any url = this->getUrlByMarketType(type, isInverse);
                 ccxt::any requiresUid = (!isEqual(type, std::string("spot")));
                 ccxt::any channelType = this->getSupportedMapping(
                     type, ccxt::dict{
                               {std::string("spot"), std::string("spot")},
                               {std::string("margin"), std::string("spot")},
                               {std::string("future"), std::string("futures")},
                               {std::string("swap"), std::string("futures")},
                               {std::string("option"), std::string("options")},
                           });
                 // todo: add correct margin support
                 ccxt::any channel = add(channelType, std::string(".balances"));
                 ccxt::any messageHash = add(type, std::string(".balance"));
                 return awaitValue(this->subscribePrivate(url, messageHash,
                                                          ccxt::any{}, channel,
                                                          params, requiresUid));
               })
        .share();
  }

  virtual void handleBalance(ccxt::any client, ccxt::any message) {
    //
    // spot order fill
    //     {
    //         "time": 1653664351,
    //         "time_ms": 1605248616763,
    //         "channel": "spot.balances",
    //         "event": "update",
    //         "result": [
    //             {
    //                 "timestamp": "1667556323",
    //                 "timestamp_ms": "1667556323730",
    //                 "user": "1000001",
    //                 "currency": "USDT",
    //                 "change": "0",
    //                 "total": "222244.3827652",
    //                 "available": "222244.3827",
    //                 "freeze": "5",
    //                 "freeze_change": "5.000000",
    //                 "change_type": "order-create"
    //             }
    //         ]
    //     }
    //
    // account transfer
    //
    //    {
    //        "id": null,
    //        "time": 1653665088,
    //        "channel": "futures.balances",
    //        "event": "update",
    //        "error": null,
    //        "result": [
    //          {
    //            "balance": 25.035008537,
    //            "change": 25,
    //            "text": "-",
    //            "time": 1653665088,
    //            "time_ms": 1653665088286,
    //            "type": "dnw",
    //            "user": "10406147"
    //          }
    //        ]
    //   }
    //
    // swap order fill
    //   {
    //       "id": null,
    //       "time": 1653665311,
    //       "channel": "futures.balances",
    //       "event": "update",
    //       "error": null,
    //       "result": [
    //         {
    //           "balance": 20.031873037,
    //           "change": -0.0031355,
    //           "text": "LTC_USDT:165551103273",
    //           "time": 1653665311,
    //           "time_ms": 1653665311437,
    //           "type": "fee",
    //           "user": "10406147"
    //         }
    //       ]
    //   }
    //
    ccxt::any result =
        this->safeValue(message, std::string("result"), ccxt::list{});
    ::setValue(this->balance, std::string("info"), result);
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(result));
         postFixIncrement(i)) {
      ccxt::any rawBalance = ::getValue(result, i);
      ccxt::any account = this->account();
      ccxt::any currencyId =
          this->safeString(rawBalance, std::string("currency"),
                           std::string("USDT")); // when not present it is USDT
      ccxt::any code = this->safeCurrencyCode(currencyId);
      ccxt::any timestamp = this->safeInteger2(
          rawBalance, std::string("time_ms"), std::string("timestamp_ms"));
      ::setValue(this->balance, std::string("timestamp"), timestamp);
      ::setValue(this->balance, std::string("datetime"),
                 this->iso8601(timestamp));
      ::setValue(account, std::string("used"),
                 this->safeString(rawBalance, std::string("freeze")));
      ::setValue(account, std::string("free"),
                 this->safeString(rawBalance, std::string("available")));
      ::setValue(account, std::string("total"),
                 this->safeString2(rawBalance, std::string("total"),
                                   std::string("balance")));
      if (isTrue(!isEqual(code, ccxt::any{}))) {
        ::setValue(this->balance, code, account);
      }
    }
    ccxt::any channel = this->safeString(message, std::string("channel"));
    ccxt::any parts = split(channel, std::string("."));
    ccxt::any rawType = this->safeString(parts, 0);
    ccxt::any channelType = this->getSupportedMapping(
        rawType, ccxt::dict{
                     {std::string("spot"), std::string("spot")},
                     {std::string("futures"), std::string("swap")},
                     {std::string("options"), std::string("option")},
                 });
    ccxt::any messageHash = add(channelType, std::string(".balance"));
    this->balance = this->safeBalance(this->balance);
    this->resolve(this->balance, messageHash);
  }

  /**
   * @method
   * @name gate#watchPositions
   * @see
   * https://www.gate.io/docs/developers/futures/ws/en/#positions-subscription
   * @see
   * https://www.gate.io/docs/developers/delivery/ws/en/#positions-subscription
   * @see https://www.gate.io/docs/developers/options/ws/en/#positions-channel
   * @description watch all open positions
   * @param {string[]} [symbols] list of unified market symbols to watch
   * positions for
   * @param {int} [since] the earliest time in ms to fetch positions for
   * @param {int} [limit] the maximum number of positions to retrieve
   * @param {object} params extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [position structure]{@link
   * https://docs.ccxt.com/en/latest/manual.html#position-structure}
   */
  std::shared_future<ccxt::any>
  watchPositions(ccxt::any symbols = ccxt::any{}, ccxt::any since = ccxt::any{},
                 ccxt::any limit = ccxt::any{},
                 ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = ccxt::any{};
                 symbols = this->marketSymbols(symbols);
                 ccxt::any payload =
                     ccxt::list{add(std::string("!"), std::string("all"))};
                 if (!isTrue(this->isEmpty(symbols))) {
                   market = this->getMarketFromSymbols(symbols);
                 }
                 ccxt::any type = ccxt::any{};
                 ccxt::any query = ccxt::any{};
                 ccxt::any typequeryVariable = this->handleMarketTypeAndParams(
                     std::string("watchPositions"), market, params);
                 type = ::getValue(typequeryVariable, 0);
                 query = ::getValue(typequeryVariable, 1);
                 if (isTrue(isEqual(type, std::string("spot")))) {
                   type = std::string("swap");
                 }
                 ccxt::any typeId = this->getSupportedMapping(
                     type, ccxt::dict{
                               {std::string("future"), std::string("futures")},
                               {std::string("swap"), std::string("futures")},
                               {std::string("option"), std::string("options")},
                           });
                 ccxt::any messageHash = add(type, std::string(":positions"));
                 if (!isTrue(this->isEmpty(symbols))) {
                   if (isTrue(isEqual(symbols, ccxt::any{}))) {
                     throw ArgumentsRequired(toString(
                         add(this->id,
                             std::string(
                                 " watchPositions() symbols is required"))));
                   }
                   messageHash =
                       add(messageHash, add(std::string("::"),
                                            join(symbols, std::string(","))));
                 }
                 ccxt::any channel = add(typeId, std::string(".positions"));
                 ccxt::any subType = ccxt::any{};
                 ccxt::any subTypequeryVariable = this->handleSubTypeAndParams(
                     std::string("watchPositions"), market, query);
                 subType = ::getValue(subTypequeryVariable, 0);
                 query = ::getValue(subTypequeryVariable, 1);
                 ccxt::any isInverse =
                     (isEqual(subType, std::string("inverse")));
                 ccxt::any url = this->getUrlByMarketType(type, isInverse);
                 ccxt::any client = this->client(url);
                 this->setPositionsCache(client, type, symbols);
                 ccxt::any fetchPositionsSnapshot = this->handleOption(
                     std::string("watchPositions"),
                     std::string("fetchPositionsSnapshot"), true);
                 ccxt::any awaitPositionsSnapshot = this->handleOption(
                     std::string("watchPositions"),
                     std::string("awaitPositionsSnapshot"), true);
                 ccxt::any cache = this->safeValue(this->positions, type);
                 if (isTrue(
                         isTrue(
                             isTrue((isEqual(fetchPositionsSnapshot, true))) &&
                             isTrue((isEqual(awaitPositionsSnapshot, true)))) &&
                         isTrue((isEqual(cache, ccxt::any{}))))) {
                   return awaitValue(::wsClientFuture(
                       client,
                       add(type, std::string(":fetchPositionsSnapshot"))));
                 }
                 ccxt::any positions = awaitValue(this->subscribePrivate(
                     url, messageHash, payload, channel, query, true));
                 if (isTrue(this->newUpdates)) {
                   return positions;
                 }
                 return this->filterBySymbolsSinceLimit(
                     this->safeValue(this->positions, type), symbols, since,
                     limit, true);
               })
        .share();
  }

  virtual void setPositionsCache(ccxt::any client, ccxt::any type,
                                 ccxt::any symbols = ccxt::any{}) {
    if (isTrue(isEqual(this->positions, ccxt::any{}))) {
      this->positions = ccxt::dict{};
    }
    if (isTrue(inOp(this->positions, type))) {
      return;
    }
    ccxt::any fetchPositionsSnapshot =
        this->handleOption(std::string("watchPositions"),
                           std::string("fetchPositionsSnapshot"), false);
    if (isTrue(isEqual(fetchPositionsSnapshot, true))) {
      ccxt::any messageHash = add(type, std::string(":fetchPositionsSnapshot"));
      if (!isTrue((
              inOp(::getValue(client, std::string("futures")), messageHash)))) {
        ::wsClientFuture(client, messageHash);
        this->spawn(std::string("loadPositionsSnapshot"), client, messageHash,
                    type);
      }
    } else {
      ::setValue(this->positions, type, ccxt::ws::ArrayCacheBySymbolBySide());
    }
  }

  virtual std::shared_future<ccxt::any>
  loadPositionsSnapshot(ccxt::any client, ccxt::any messageHash, ccxt::any type) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 ccxt::any positions = awaitValue(this->fetchPositions(
                     ccxt::any{}, ccxt::dict{
                                     {std::string("type"), type},
                                 }));
                 ::setValue(this->positions, type,
                            ccxt::ws::ArrayCacheBySymbolBySide());
                 ccxt::any cache = ::getValue(this->positions, type);
                 for (ccxt::any i = 0; isLessThan(i, getArrayLength(positions));
                      postFixIncrement(i)) {
                   ccxt::any position = ::getValue(positions, i);
                   ccxt::any contracts =
                       this->safeNumber(position, std::string("contracts"), 0);
                   if (isTrue(isTrue((!isEqual(contracts, ccxt::any{}))) &&
                              isTrue((isGreaterThan(contracts, 0))))) {
                     ::wsAppend(cache, position);
                   }
                 }
                 // don't remove the future from the .futures cache
                 if (isTrue(inOp(::getValue(client, std::string("futures")),
                                 messageHash))) {
                   ccxt::any future = ::getValue(
                       ::getValue(client, std::string("futures")), messageHash);
                   ::wsFutureResolve(future, cache);
                   this->resolve(cache, add(type, std::string(":position")));
                 }
                 return ccxt::any{};
               })
        .share();
  }

  virtual void handlePositions(ccxt::any client, ccxt::any message) {
    //
    //    {
    //        time: 1693158497,
    //        time_ms: 1693158497204,
    //        channel: 'futures.positions',
    //        event: 'update',
    //        result: [{
    //            contract: 'XRP_USDT',
    //            cross_leverage_limit: 0,
    //            entry_price: 0.5253,
    //            history_pnl: 0,
    //            history_point: 0,
    //            last_close_pnl: 0,
    //            leverage: 0,
    //            leverage_max: 50,
    //            liq_price: 0.0361,
    //            maintenance_rate: 0.01,
    //            margin: 4.89609962852,
    //            mode: 'single',
    //            realised_pnl: -0.0026265,
    //            realised_point: 0,
    //            risk_limit: 500000,
    //            size: 1,
    //            time: 1693158497,
    //            time_ms: 1693158497195,
    //            update_id: 1,
    //            user: '10444586'
    //        }]
    //    }
    //
    ccxt::any type =
        this->getMarketTypeByUrl(::getValue(client, std::string("url")));
    ccxt::any data =
        this->safeValue(message, std::string("result"), ccxt::list{});
    ccxt::any cache = ::getValue(this->positions, type);
    ccxt::any newPositions = ccxt::list{};
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(data));
         postFixIncrement(i)) {
      ccxt::any rawPosition = ::getValue(data, i);
      ccxt::any position = this->parsePosition(rawPosition);
      ccxt::any symbol = this->safeString(position, std::string("symbol"));
      ccxt::any side = this->safeString(position, std::string("side"));
      // Control when position is closed no side is returned
      if (isTrue(isEqual(side, ccxt::any{}))) {
        ccxt::any prevLongPosition =
            this->safeDict(cache, add(symbol, std::string("long")));
        if (isTrue(!isEqual(prevLongPosition, ccxt::any{}))) {
          ::setValue(position, std::string("side"),
                     ::getValue(prevLongPosition, std::string("side")));
          arrayPush(newPositions, position);
          ::wsAppend(cache, position);
        }
        ccxt::any prevShortPosition =
            this->safeDict(cache, add(symbol, std::string("short")));
        if (isTrue(!isEqual(prevShortPosition, ccxt::any{}))) {
          ::setValue(position, std::string("side"),
                     ::getValue(prevShortPosition, std::string("side")));
          arrayPush(newPositions, position);
          ::wsAppend(cache, position);
        }
        // if no prev position is found, default to long
        if (isTrue(isTrue(isEqual(prevLongPosition, ccxt::any{})) &&
                   isTrue(isEqual(prevShortPosition, ccxt::any{})))) {
          ::setValue(position, std::string("side"), std::string("long"));
          arrayPush(newPositions, position);
          ::wsAppend(cache, position);
        }
      } else {
        arrayPush(newPositions, position);
        ::wsAppend(cache, position);
      }
    }
    ccxt::any messageHashes =
        this->findMessageHashes(client, add(type, std::string(":positions::")));
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(messageHashes));
         postFixIncrement(i)) {
      ccxt::any messageHash = ::getValue(messageHashes, i);
      ccxt::any parts = split(messageHash, std::string("::"));
      ccxt::any symbolsString = ::getValue(parts, 1);
      ccxt::any symbols = split(symbolsString, std::string(","));
      ccxt::any positions = this->filterByArray(
          newPositions, std::string("symbol"), symbols, false);
      if (!isTrue(this->isEmpty(positions))) {
        this->resolve(positions, messageHash);
      }
    }
    this->resolve(newPositions, add(type, std::string(":positions")));
  }

  /**
   * @method
   * @name gate#watchOrders
   * @description watches information on multiple orders made by the user
   * @see https://www.gate.com/docs/developers/apiv4/ws/en/#orders-channel
   * @see https://www.gate.com/docs/developers/futures/ws/en/#orders-api
   * @see https://www.gate.com/docs/developers/delivery/ws/en/#orders-api
   * @see https://www.gate.com/docs/developers/options/ws/en/#orders-channel
   * @param {string} symbol unified market symbol of the market orders were made
   * in
   * @param {int} [since] the earliest time in ms to fetch orders for
   * @param {int} [limit] the maximum number of order structures to retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.type] spot, margin, swap, future, or option.
   * Required if listening to all symbols.
   * @param {boolean} [params.isInverse] if future, listen to inverse or linear
   * contracts
   * @param {boolean} [params.trigger] set to true to watch trigger orders,
   * spot.priceorders and futures.autoorders channels, see
   * https://github.com/ccxt/ccxt/issues/27202
   * @param {boolean} [params.stop] alias of params.trigger
   * @returns {object[]} a list of [order structures]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<ccxt::any>
  watchOrders(ccxt::any symbol = ccxt::any{}, ccxt::any since = ccxt::any{},
              ccxt::any limit = ccxt::any{},
              ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 ccxt::any market = ccxt::any{};
                 if (isTrue(!isEqual(symbol, ccxt::any{}))) {
                   ccxt::any marketResolved = this->market(symbol);
                   market = marketResolved;
                   symbol = ::getValue(market, std::string("symbol"));
                 }
                 ccxt::any type = ccxt::any{};
                 ccxt::any query = ccxt::any{};
                 ccxt::any typequeryVariable = this->handleMarketTypeAndParams(
                     std::string("watchOrders"), market, params);
                 type = ::getValue(typequeryVariable, 0);
                 query = ::getValue(typequeryVariable, 1);
                 ccxt::any typeId = this->getSupportedMapping(
                     type, ccxt::dict{
                               {std::string("spot"), std::string("spot")},
                               {std::string("margin"), std::string("spot")},
                               {std::string("future"), std::string("futures")},
                               {std::string("swap"), std::string("futures")},
                               {std::string("option"), std::string("options")},
                           });
                 ccxt::any isTrigger = false;
                 ccxt::any isTriggerqueryVariable = this->handleParamBool2(
                     query, std::string("trigger"), std::string("stop"), false);
                 isTrigger = ::getValue(isTriggerqueryVariable, 0);
                 query = ::getValue(isTriggerqueryVariable, 1);
                 if (isTrue(
                         isTrue((isEqual(isTrigger, true))) &&
                         isTrue((isEqual(typeId, std::string("options")))))) {
                   throw NotSupported(toString(
                       add(this->id,
                           std::string(
                               " watchOrders() does not support trigger orders "
                               "for options, see "
                               "https://github.com/ccxt/ccxt/issues/27202"))));
                 }
                 // gate pushes trigger orders on dedicated channels,
                 // spot.priceorders and futures.autoorders, see
                 // https://github.com/ccxt/ccxt/issues/27202
                 ccxt::any suffix = std::string(".orders");
                 if (isTrue(isEqual(isTrigger, true))) {
                   suffix = (isTrue((isEqual(typeId, std::string("spot"))))
                                 ? ccxt::any(std::string(".priceorders"))
                                 : ccxt::any(std::string(".autoorders")));
                 }
                 ccxt::any channel = add(typeId, suffix);
                 ccxt::any messageHash =
                     (isTrue((isEqual(isTrigger, true)))
                          ? ccxt::any(std::string("triggerOrders"))
                          : ccxt::any(std::string("orders")));
                 ccxt::any payload =
                     ccxt::list{add(std::string("!"), std::string("all"))};
                 if (isTrue(!isEqual(market, ccxt::any{}))) {
                   messageHash = add(
                       messageHash, add(std::string(":"),
                                        ::getValue(market, std::string("id"))));
                   ccxt::any mid = ::getValue(market, std::string("id"));
                   if (isTrue(!isEqual(mid, ccxt::any{}))) {
                     payload = ccxt::list{mid};
                   }
                 }
                 ccxt::any subType = ccxt::any{};
                 ccxt::any subTypequeryVariable = this->handleSubTypeAndParams(
                     std::string("watchOrders"), market, query);
                 subType = ::getValue(subTypequeryVariable, 0);
                 query = ::getValue(subTypequeryVariable, 1);
                 ccxt::any isInverse =
                     (isEqual(subType, std::string("inverse")));
                 ccxt::any url = this->getUrlByMarketType(type, isInverse);
                 // uid required for non spot markets
                 ccxt::any requiresUid = (!isEqual(type, std::string("spot")));
                 ccxt::any orders = awaitValue(this->subscribePrivate(
                     url, messageHash, payload, channel, query, requiresUid));
                 if (isTrue(this->newUpdates)) {
                   limit = ::wsGetLimit(orders, symbol, limit);
                 }
                 return this->filterBySinceLimit(
                     orders, since, limit, std::string("timestamp"), true);
               })
        .share();
  }

  virtual void handleOrder(ccxt::any client, ccxt::any message) {
    //
    //     {
    //         "time": 1774613210,
    //         "time_ms": 1774613210392,
    //         "channel": "spot.orders",
    //         "event": "update",
    //         "result": [
    //             {
    //                 "id": "1036717689726",
    //                 "text": "apiv4",
    //                 "create_time": "1774613210",
    //                 "update_time": "1774613210",
    //                 "currency_pair": "BTC_USDT",
    //                 "type": "limit",
    //                 "account": "unified",
    //                 "side": "buy",
    //                 "amount": "0.1",
    //                 "price": "200",
    //                 "time_in_force": "gtc",
    //                 "left": "0.1",
    //                 "filled_amount": "0",
    //                 "filled_total": "0",
    //                 "avg_deal_price": "0",
    //                 "fee": "0",
    //                 "fee_currency": "BTC",
    //                 "point_fee": "0",
    //                 "gt_fee": "0",
    //                 "rebated_fee": "0",
    //                 "rebated_fee_currency": "BTC",
    //                 "create_time_ms": "1774613210391",
    //                 "update_time_ms": "1774613210391",
    //                 "user": 10406147,
    //                 "event": "put",
    //                 "stp_id": 0,
    //                 "stp_act": "-",
    //                 "finish_as": "open",
    //                 "biz_info": "ch:ccxt",
    //                 "amend_text": "-"
    //             }
    //         ]
    //     }
    //
    ccxt::any orders =
        this->safeValue(message, std::string("result"), ccxt::list{});
    ccxt::any channel =
        this->safeString(message, std::string("channel"), std::string(""));
    ccxt::any isTrigger =
        isTrue((isGreaterThanOrEqual(
            getIndexOf(channel, std::string("autoorders")), 0))) ||
        isTrue((isGreaterThanOrEqual(
            getIndexOf(channel, std::string("priceorders")), 0)));
    ccxt::any hashPrefix =
        (isTrue(isTrigger) ? ccxt::any(std::string("triggerOrders"))
                           : ccxt::any(std::string("orders")));
    ccxt::any limit =
        this->safeInteger(this->options, std::string("ordersLimit"), 1000);
    if (isTrue(isEqual(this->orders, ccxt::any{}))) {
      this->orders = ccxt::ws::ArrayCacheBySymbolById(limit);
      this->triggerOrders = ccxt::ws::ArrayCacheBySymbolById(limit);
    }
    ccxt::any stored = (isTrue(isTrigger) ? ccxt::any(this->triggerOrders)
                                         : ccxt::any(this->orders));
    ccxt::any marketIds = ccxt::dict{};
    ccxt::any parsedOrders = this->parseOrders(orders);
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(parsedOrders));
         postFixIncrement(i)) {
      ccxt::any parsed = ::getValue(parsedOrders, i);
      // inject order status
      ccxt::any info = this->safeValue(parsed, std::string("info"));
      ccxt::any event = this->safeString(info, std::string("event"));
      if (isTrue(isTrue(isEqual(event, std::string("put"))) ||
                 isTrue(isEqual(event, std::string("update"))))) {
        ::setValue(parsed, std::string("status"), std::string("open"));
      } else if (isTrue(isEqual(event, std::string("finish")))) {
        ccxt::any status = this->safeString(parsed, std::string("status"));
        if (isTrue(isEqual(status, ccxt::any{}))) {
          ccxt::any left = this->safeInteger(info, std::string("left"));
          ::setValue(parsed, std::string("status"),
                     (isTrue((isEqual(left, 0)))
                          ? ccxt::any(std::string("closed"))
                          : ccxt::any(std::string("canceled"))));
        }
      }
      ::wsAppend(stored, parsed);
      ccxt::any symbol = ::getValue(parsed, std::string("symbol"));
      ccxt::any market = this->market(symbol);
      if (isTrue(!isEqual(::getValue(market, std::string("id")), ccxt::any{}))) {
        ::setValue(marketIds, ::getValue(market, std::string("id")), true);
      }
    }
    ccxt::any keys = getObjectKeys(marketIds);
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(keys));
         postFixIncrement(i)) {
      ccxt::any messageHash =
          add(add(hashPrefix, std::string(":")), ::getValue(keys, i));
      this->resolve(stored, messageHash);
    }
    this->resolve(stored, hashPrefix);
  }

  /**
   * @method
   * @name gate#watchMyLiquidations
   * @description watch the public liquidations of a trading pair
   * @see https://www.gate.io/docs/developers/futures/ws/en/#liquidates-api
   * @see https://www.gate.io/docs/developers/delivery/ws/en/#liquidates-api
   * @see https://www.gate.io/docs/developers/options/ws/en/#liquidates-channel
   * @param {string} symbol unified CCXT market symbol
   * @param {int} [since] the earliest time in ms to fetch liquidations for
   * @param {int} [limit] the maximum number of liquidation structures to
   * retrieve
   * @param {object} [params] exchange specific parameters for the bitmex api
   * endpoint
   * @returns {object} an array of [liquidation structures]{@link
   * https://github.com/ccxt/ccxt/wiki/Manual#liquidation-structure}
   */
  std::shared_future<ccxt::any>
  watchMyLiquidations(ccxt::any symbol, ccxt::any since = ccxt::any{},
                      ccxt::any limit = ccxt::any{},
                      ccxt::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        return awaitValue(this->watchMyLiquidationsForSymbols(
                            ccxt::list{symbol}, since, limit, params));
                      })
        .share();
  }

  /**
   * @method
   * @name gate#watchMyLiquidationsForSymbols
   * @description watch the private liquidations of a trading pair
   * @see https://www.gate.io/docs/developers/futures/ws/en/#liquidates-api
   * @see https://www.gate.io/docs/developers/delivery/ws/en/#liquidates-api
   * @see https://www.gate.io/docs/developers/options/ws/en/#liquidates-channel
   * @param {string[]} symbols unified CCXT market symbols
   * @param {int} [since] the earliest time in ms to fetch liquidations for
   * @param {int} [limit] the maximum number of liquidation structures to
   * retrieve
   * @param {object} [params] exchange specific parameters for the gate api
   * endpoint
   * @returns {object} an array of [liquidation structures]{@link
   * https://github.com/ccxt/ccxt/wiki/Manual#liquidation-structure}
   */
  std::shared_future<ccxt::any>
  watchMyLiquidationsForSymbols(ccxt::any symbols, ccxt::any since = ccxt::any{},
                                ccxt::any limit = ccxt::any{},
                                ccxt::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 if (isTrue(isEqual(this->markets, ccxt::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 symbols = this->marketSymbols(symbols, ccxt::any{}, true, true);
                 ccxt::any market = this->getMarketFromSymbols(symbols);
                 ccxt::any type = ccxt::any{};
                 ccxt::any query = ccxt::any{};
                 ccxt::any typequeryVariable = this->handleMarketTypeAndParams(
                     std::string("watchMyLiquidationsForSymbols"), market,
                     params);
                 type = ::getValue(typequeryVariable, 0);
                 query = ::getValue(typequeryVariable, 1);
                 ccxt::any typeId = this->getSupportedMapping(
                     type, ccxt::dict{
                               {std::string("future"), std::string("futures")},
                               {std::string("swap"), std::string("futures")},
                               {std::string("option"), std::string("options")},
                           });
                 ccxt::any subType = ccxt::any{};
                 ccxt::any subTypequeryVariable = this->handleSubTypeAndParams(
                     std::string("watchMyLiquidationsForSymbols"), market,
                     query);
                 subType = ::getValue(subTypequeryVariable, 0);
                 query = ::getValue(subTypequeryVariable, 1);
                 ccxt::any isInverse =
                     (isEqual(subType, std::string("inverse")));
                 ccxt::any url = this->getUrlByMarketType(type, isInverse);
                 ccxt::any payload = ccxt::list{};
                 ccxt::any messageHash = std::string("");
                 if (isTrue(this->isEmpty(symbols))) {
                   if (isTrue(
                           isTrue(!isEqual(typeId, std::string("futures"))) &&
                           !isTrue(isInverse))) {
                     throw BadRequest(toString(
                         add(this->id,
                             std::string(
                                 " watchMyLiquidationsForSymbols() does not "
                                 "support listening to all symbols, you must "
                                 "call watchMyLiquidations() instead for each "
                                 "symbol you wish to watch."))));
                   }
                   messageHash = std::string("myLiquidations");
                   arrayPush(payload, std::string("!all"));
                 } else {
                   ccxt::any symbolsLength = getArrayLength(symbols);
                   if (isTrue(!isEqual(symbolsLength, 1))) {
                     throw BadRequest(toString(
                         add(this->id,
                             std::string(
                                 " watchMyLiquidationsForSymbols() only allows "
                                 "one symbol at a time. To listen to several "
                                 "symbols call watchMyLiquidationsForSymbols() "
                                 "several times."))));
                   }
                   messageHash = add(std::string("myLiquidations::"),
                                     ::getValue(symbols, 0));
                   arrayPush(payload, ::getValue(market, std::string("id")));
                 }
                 ccxt::any channel = add(typeId, std::string(".liquidates"));
                 ccxt::any newLiquidations = awaitValue(this->subscribePrivate(
                     url, messageHash, payload, channel, query, true));
                 if (isTrue(this->newUpdates)) {
                   return newLiquidations;
                 }
                 return this->filterBySymbolsSinceLimit(
                     this->liquidations, symbols, since, limit, true);
               })
        .share();
  }

  virtual void handleLiquidation(ccxt::any client, ccxt::any message) {
    //
    // future / delivery
    //     {
    //         "channel":"futures.liquidates",
    //         "event":"update",
    //         "time":1541505434,
    //         "time_ms":1541505434123,
    //         "result":[
    //            {
    //               "entry_price":209,
    //               "fill_price":215.1,
    //               "left":0,
    //               "leverage":0.0,
    //               "liq_price":213,
    //               "margin":0.007816722941,
    //               "mark_price":213,
    //               "order_id":4093362,
    //               "order_price":215.1,
    //               "size":-124,
    //               "time":1541486601,
    //               "time_ms":1541486601123,
    //               "contract":"BTC_USD",
    //               "user":"1040xxxx"
    //            }
    //         ]
    //     }
    // option
    //    {
    //        "channel":"options.liquidates",
    //        "event":"update",
    //        "time":1630654851,
    //        "result":[
    //           {
    //              "user":"1xxxx",
    //              "init_margin":1190,
    //              "maint_margin":1042.5,
    //              "order_margin":0,
    //              "time":1639051907,
    //              "time_ms":1639051907000
    //           }
    //        ]
    //    }
    //
    ccxt::any rawLiquidations =
        this->safeList(message, std::string("result"), ccxt::list{});
    ccxt::any newLiquidations = ccxt::list{};
    if (isTrue(isEqual(this->liquidations, ccxt::any{}))) {
      ccxt::any limit = this->safeInteger(
          this->options, std::string("liquidationsLimit"), 1000);
      this->liquidations = ccxt::ws::ArrayCache(limit);
    }
    ccxt::any cache = this->liquidations;
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(rawLiquidations));
         postFixIncrement(i)) {
      ccxt::any rawLiquidation = ::getValue(rawLiquidations, i);
      ccxt::any liquidation = this->parseWsLiquidation(rawLiquidation);
      ::wsAppend(cache, liquidation);
      ccxt::any symbol = this->safeString(liquidation, std::string("symbol"));
      ccxt::any symbolLiquidations =
          this->safeValue(cache, symbol, ccxt::list{});
      this->resolve(symbolLiquidations,
                    add(std::string("myLiquidations::"), symbol));
    }
    this->resolve(newLiquidations, std::string("myLiquidations"));
  }

  virtual ccxt::any parseWsLiquidation(ccxt::any liquidation,
                                      ccxt::any market = ccxt::any{}) {
    //
    // future / delivery
    //    {
    //        "entry_price": 209,
    //        "fill_price": 215.1,
    //        "left": 0,
    //        "leverage": 0.0,
    //        "liq_price": 213,
    //        "margin": 0.007816722941,
    //        "mark_price": 213,
    //        "order_id": 4093362,
    //        "order_price": 215.1,
    //        "size": -124,
    //        "time": 1541486601,
    //        "time_ms": 1541486601123,
    //        "contract": "BTC_USD",
    //        "user": "1040xxxx"
    //    }
    // option
    //    {
    //        "user": "1xxxx",
    //        "init_margin": 1190,
    //        "maint_margin": 1042.5,
    //        "order_margin": 0,
    //        "time": 1639051907,
    //        "time_ms": 1639051907000
    //    }
    //
    ccxt::any marketId = this->safeString(liquidation, std::string("contract"));
    market = this->safeMarket(marketId, market);
    ccxt::any timestamp = this->safeInteger(liquidation, std::string("time_ms"));
    ccxt::any originalSize = this->safeString(liquidation, std::string("size"));
    ccxt::any left = this->safeString(liquidation, std::string("left"));
    ccxt::any amount =
        ccxt::Precise::stringAbs(ccxt::Precise::stringSub(originalSize, left));
    return this->safeLiquidation(ccxt::dict{
        {std::string("info"), liquidation},
        {std::string("symbol"), this->safeSymbol(marketId, market)},
        {std::string("contracts"), this->parseNumber(amount)},
        {std::string("contractSize"),
         this->safeNumber(market, std::string("contractSize"))},
        {std::string("price"),
         this->safeNumber(liquidation, std::string("fill_price"))},
        {std::string("baseValue"), ccxt::any{}},
        {std::string("quoteValue"), ccxt::any{}},
        {std::string("timestamp"), timestamp},
        {std::string("datetime"), this->iso8601(timestamp)},
    });
  }

  virtual ccxt::any handleErrorMessage(ccxt::any client, ccxt::any message) {
    //
    //    {
    //        "time": 1647274664,
    //        "channel": "futures.orders",
    //        "event": "subscribe",
    //        "error": { code: 2, message: "unknown contract BTC_USDT_20220318"
    //        },
    //    }
    //    {
    //      "time": 1647276473,
    //      "channel": "futures.orders",
    //      "event": "subscribe",
    //      "error": {
    //        "code": 4,
    //        "message": "{"label":"INVALID_KEY","message":"Invalid key
    //        provided"}\n"
    //      },
    //      "result": null
    //    }
    //    {
    //       header: {
    //         response_time: '1718551891329',
    //         status: '400',
    //         channel: 'spot.order_place',
    //         event: 'api',
    //         client_id: '81.34.68.6-0xc16375e2c0',
    //         conn_id: '9539116e0e09678f'
    //       },
    //       data: { errs: { label: 'AUTHENTICATION_FAILED', message: 'Not
    //       login' } }, request_id: '10406147'
    //     }
    //     {
    //         "time": 1739853211,
    //         "time_ms": 1739853211201,
    //         "id": 1,
    //         "conn_id": "62f2c1dabbe186d7",
    //         "trace_id": "cdb02a8c0b61086b2fe6f8fad2f98c54",
    //         "channel": "spot.trades",
    //         "event": "subscribe",
    //         "payload": [
    //             "LUNARLENS_USDT",
    //             "ETH_USDT"
    //         ],
    //         "error": {
    //             "code": 2,
    //             "message": "unknown currency pair: LUNARLENS_USDT"
    //         },
    //         "result": {
    //             "status": "fail"
    //         },
    //         "requestId": "cdb02a8c0b61086b2fe6f8fad2f98c54"
    //     }
    //
    ccxt::any data = this->safeDict(message, std::string("data"));
    ccxt::any errs = this->safeDict(data, std::string("errs"));
    ccxt::any error = this->safeDict(message, std::string("error"), errs);
    ccxt::any code =
        this->safeString2(error, std::string("code"), std::string("label"));
    ccxt::any id = this->safeStringN(
        message, ccxt::list{std::string("id"), std::string("requestId"),
                            std::string("request_id")});
    if (isTrue(!isEqual(error, ccxt::any{}))) {
      ccxt::any messageHash = this->safeString(
          ::getValue(client, std::string("subscriptions")), id);
      try {
        this->throwExactlyMatchedException(
            ::getValue(::getValue(this->exceptions, std::string("ws")),
                       std::string("exact")),
            code, this->json(message));
        this->throwExactlyMatchedException(
            ::getValue(this->exceptions, std::string("exact")), code,
            this->json(errs));
        ccxt::any errorMessage =
            this->safeString(error, std::string("message"),
                             this->safeString(errs, std::string("message")));
        this->throwBroadlyMatchedException(
            ::getValue(::getValue(this->exceptions, std::string("ws")),
                       std::string("broad")),
            errorMessage, this->json(message));
        throw ExchangeError(toString(this->json(message)));
      } catch (const std::exception &e) {
        this->reject(e, messageHash);
        if (isTrue(
                isTrue((!isEqual(messageHash, ccxt::any{}))) &&
                isTrue((inOp(::getValue(client, std::string("subscriptions")),
                             messageHash))))) {
          deleteKey(::getValue(client, std::string("subscriptions")),
                    messageHash);
        }
        // remove subscriptions for watchSymbols
        ccxt::any channel = this->safeString(message, std::string("channel"));
        if (isTrue(isTrue((!isEqual(channel, ccxt::any{}))) &&
                   isTrue((isGreaterThan(getIndexOf(channel, std::string(".")),
                                         0))))) {
          ccxt::any parsedChannel = split(channel, std::string("."));
          ccxt::any payload =
              this->safeList(message, std::string("payload"), ccxt::list{});
          for (ccxt::any i = 0; isLessThan(i, getArrayLength(payload));
               postFixIncrement(i)) {
            ccxt::any marketType =
                (isTrue(isEqual(::getValue(parsedChannel, 0),
                                std::string("futures")))
                     ? ccxt::any(std::string("swap"))
                     : ccxt::any(::getValue(parsedChannel, 0)));
            ccxt::any symbol =
                this->safeSymbol(::getValue(payload, i), ccxt::any{},
                                 std::string("_"), marketType);
            ccxt::any messageHashSymbol = add(
                add(::getValue(parsedChannel, 1), std::string(":")), symbol);
            if (isTrue(isTrue((!isEqual(messageHashSymbol, ccxt::any{}))) &&
                       isTrue((inOp(
                           ::getValue(client, std::string("subscriptions")),
                           messageHashSymbol))))) {
              deleteKey(::getValue(client, std::string("subscriptions")),
                        messageHashSymbol);
            }
          }
        }
      }
      if (isTrue(isTrue((!isEqual(id, ccxt::any{}))) &&
                 isTrue((inOp(::getValue(client, std::string("subscriptions")),
                              id))))) {
        deleteKey(::getValue(client, std::string("subscriptions")), id);
      }
      return true;
    }
    return false;
  }

  virtual void handleBalanceSubscription(ccxt::any client, ccxt::any message,
                                         ccxt::any subscription = ccxt::any{}) {
    this->balance = ccxt::dict{};
  }

  virtual void handleSubscriptionStatus(ccxt::any client, ccxt::any message) {
    ccxt::any channel = this->safeString(message, std::string("channel"));
    ccxt::any methods = ccxt::dict{
        {std::string("balance"), std::string("handleBalanceSubscription")},
        {std::string("spot.order_book_update"),
         std::string("handleOrderBookSubscription")},
        {std::string("futures.order_book_update"),
         std::string("handleOrderBookSubscription")},
        {std::string("options.order_book_update"),
         std::string("handleOrderBookSubscription")},
    };
    ccxt::any id = this->safeString(message, std::string("id"));
    if (isTrue(isEqual(id, ccxt::any{}))) {
      return;
    }
    if (isTrue(inOp(methods, channel))) {
      ccxt::any subscriptionHash = this->safeString(
          ::getValue(client, std::string("subscriptions")), id);
      ccxt::any subscription = this->safeValue(
          ::getValue(client, std::string("subscriptions")), subscriptionHash);
      ccxt::any method = ::getValue(methods, channel);
      this->dispatchMethodName(method,
                               ccxt::list{client, message, subscription});
    }
    if (isTrue(inOp(::getValue(client, std::string("subscriptions")), id))) {
      if (isTrue(!isEqual(id, ccxt::any{}))) {
        deleteKey(::getValue(client, std::string("subscriptions")), id);
      }
    }
  }

  virtual void handleUnSubscribe(ccxt::any client, ccxt::any message) {
    //
    // {
    //     "time":1725534679,
    //     "time_ms":1725534679786,
    //     "id":2,
    //     "conn_id":"fac539b443fd7002",
    //     "trace_id":"efe1d282b630b4aa266b84bee177791a",
    //     "channel":"spot.trades",
    //     "event":"unsubscribe",
    //     "payload":[
    //        "LTC_USDT"
    //     ],
    //     "result":{
    //        "status":"success"
    //     },
    //     "requestId":"efe1d282b630b4aa266b84bee177791a"
    // }
    //
    ccxt::any id = this->safeString(message, std::string("id"));
    ccxt::any keys =
        getObjectKeys(::getValue(client, std::string("subscriptions")));
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(keys));
         postFixIncrement(i)) {
      ccxt::any messageHash = ::getValue(keys, i);
      if (!isTrue((inOp(::getValue(client, std::string("subscriptions")),
                        messageHash)))) {
        continue;
      }
      if (isTrue(startsWith(messageHash, std::string("unsubscribe")))) {
        ccxt::any subscription = ::getValue(
            ::getValue(client, std::string("subscriptions")), messageHash);
        ccxt::any subId = this->safeString(subscription, std::string("id"));
        if (isTrue(!isEqual(id, subId))) {
          continue;
        }
        ccxt::any messageHashes = this->safeList(
            subscription, std::string("messageHashes"), ccxt::list{});
        ccxt::any subMessageHashes = this->safeList(
            subscription, std::string("subMessageHashes"), ccxt::list{});
        for (ccxt::any j = 0; isLessThan(j, getArrayLength(messageHashes));
             postFixIncrement(j)) {
          ccxt::any unsubHash = ::getValue(messageHashes, j);
          ccxt::any subHash = ::getValue(subMessageHashes, j);
          this->cleanUnsubscription(client, subHash, unsubHash);
        }
        this->cleanCache(subscription);
      }
    }
  }

  void handleMessage(ccxt::any client, ccxt::any message) override {
    //
    // subscribe
    //    {
    //        "time": 1649062304,
    //        "id": 1649062303,
    //        "channel": "spot.candlesticks",
    //        "event": "subscribe",
    //        "result": { status: "success" }
    //    }
    //
    // candlestick
    //    {
    //        "time": 1649063328,
    //        "channel": "spot.candlesticks",
    //        "event": "update",
    //        "result": {
    //          "t": "1649063280",
    //          "v": "58932.23174896",
    //          "c": "45966.47",
    //          "h": "45997.24",
    //          "l": "45966.47",
    //          "o": "45975.18",
    //          "n": "1m_BTC_USDT",
    //          "a": "1.281699"
    //        }
    //     }
    //
    //  orders
    //   {
    //       "time": 1630654851,
    //       "channel": "options.orders", or futures.orders or spot.orders
    //       "event": "update",
    //       "result": [
    //          {
    //             "contract": "BTC_USDT-20211130-65000-C",
    //             "create_time": 1637897000,
    //               (...)
    //       ]
    //   }
    // orderbook
    //   {
    //       "time": 1649770525,
    //       "channel": "spot.order_book_update",
    //       "event": "update",
    //       "result": {
    //         "t": 1649770525653,
    //         "e": "depthUpdate",
    //         "E": 1649770525,
    //         "s": "LTC_USDT",
    //         "U": 2622525645,
    //         "u": 2622525665,
    //         "b": [
    //           [Array], [Array],
    //           [Array], [Array],
    //           [Array], [Array],
    //           [Array], [Array],
    //           [Array], [Array],
    //           [Array]
    //         ],
    //         "a": [
    //           [Array], [Array],
    //           [Array], [Array],
    //           [Array], [Array],
    //           [Array], [Array],
    //           [Array], [Array],
    //           [Array]
    //         ]
    //       }
    //     }
    //
    // balance update
    //
    //    {
    //        "time": 1653664351,
    //        "channel": "spot.balances",
    //        "event": "update",
    //        "result": [
    //          {
    //            "timestamp": "1653664351",
    //            "timestamp_ms": "1653664351017",
    //            "user": "10406147",
    //            "currency": "LTC",
    //            "change": "-0.0002000000000000",
    //            "total": "0.09986000000000000000",
    //            "available": "0.09986000000000000000"
    //          }
    //        ]
    //    }
    //
    if (isTrue(isEqual(this->handleErrorMessage(client, message), true))) {
      return;
    }
    ccxt::any event = this->safeString(message, std::string("event"));
    if (isTrue(isEqual(event, std::string("subscribe")))) {
      this->handleSubscriptionStatus(client, message);
      return;
    }
    if (isTrue(isEqual(event, std::string("unsubscribe")))) {
      this->handleUnSubscribe(client, message);
      return;
    }
    ccxt::any channel =
        this->safeString(message, std::string("channel"), std::string(""));
    // after supporting more method we can create a mapping for this
    if (isTrue(isEqual(channel, std::string("spot.obu")))) {
      this->handleOrderBook(client, message);
      return;
    }
    ccxt::any channelParts = split(channel, std::string("."));
    ccxt::any channelType = this->safeValue(channelParts, 1);
    ccxt::any v4Methods = ccxt::dict{
        {std::string("usertrades"), std::string("handleMyTrades")},
        {std::string("candlesticks"), std::string("handleOHLCV")},
        {std::string("orders"), std::string("handleOrder")},
        {std::string("autoorders"), std::string("handleOrder")},
        {std::string("priceorders"), std::string("handleOrder")},
        {std::string("positions"), std::string("handlePositions")},
        {std::string("tickers"), std::string("handleTicker")},
        {std::string("book_ticker"), std::string("handleBidAsk")},
        {std::string("trades"), std::string("handleTrades")},
        {std::string("order_book_update"), std::string("handleOrderBook")},
        {std::string("balances"), std::string("handleBalance")},
        {std::string("liquidates"), std::string("handleLiquidation")},
    };
    ccxt::any method = this->safeValue(v4Methods, channelType);
    if (isTrue(!isEqual(method, ccxt::any{}))) {
      this->dispatchMethodName(method, ccxt::list{client, message});
    }
    ccxt::any requestId = this->safeString(message, std::string("request_id"));
    if (isTrue(isEqual(requestId, std::string("authenticated")))) {
      this->handleAuthenticationMessage(client, message);
      return;
    }
    if (isTrue(!isEqual(requestId, ccxt::any{}))) {
      ccxt::any data = this->safeDict(message, std::string("data"));
      // use safeValue as result may be Array or an Object
      ccxt::any result = this->safeValue(data, std::string("result"));
      ccxt::any ack = this->safeBool(message, std::string("ack"));
      if (isTrue(!isEqual(ack, true))) {
        this->resolve(result, requestId);
      }
    }
  }

  virtual ccxt::any getUrlByMarket(ccxt::any market) {
    ccxt::any baseUrl = ::getValue(::getValue(this->urls, std::string("api")),
                                  ::getValue(market, std::string("type")));
    if (isTrue(isEqual(::getValue(market, std::string("contract")), true))) {
      return (isTrue((isEqual(::getValue(market, std::string("linear")), true)))
                  ? ccxt::any(::getValue(baseUrl, std::string("usdt")))
                  : ccxt::any(::getValue(baseUrl, std::string("btc"))));
    } else {
      return baseUrl;
    }
  }

  virtual ccxt::any getTypeByMarket(ccxt::any market) {
    if (isTrue(isEqual(market, ccxt::any{}))) {
      return ccxt::any{};
    }
    if (isTrue(isEqual(::getValue(market, std::string("spot")), true))) {
      return std::string("spot");
    } else if (isTrue(
                   isEqual(::getValue(market, std::string("option")), true))) {
      return std::string("options");
    } else {
      return std::string("futures");
    }
  }

  virtual ccxt::any getUrlByMarketType(ccxt::any type,
                                      ccxt::any isInverse = false) {
    ccxt::any api = ::getValue(this->urls, std::string("api"));
    ccxt::any url = this->safeValue(api, type);
    if (isTrue(isTrue((isEqual(type, std::string("swap")))) ||
               isTrue((isEqual(type, std::string("future")))))) {
      return (isTrue(isInverse)
                  ? ccxt::any(::getValue(url, std::string("btc")))
                  : ccxt::any(::getValue(url, std::string("usdt"))));
    } else {
      return url;
    }
  }

  virtual ccxt::any getMarketTypeByUrl(ccxt::any url) {
    ccxt::any findBy = ccxt::dict{
        {std::string("op-"), std::string("option")},
        {std::string("delivery"), std::string("future")},
        {std::string("fx"), std::string("swap")},
    };
    ccxt::any keys = getObjectKeys(findBy);
    for (ccxt::any i = 0; isLessThan(i, getArrayLength(keys));
         postFixIncrement(i)) {
      ccxt::any key = ::getValue(keys, i);
      ccxt::any value = ::getValue(findBy, key);
      if (isTrue(isGreaterThanOrEqual(getIndexOf(url, key), 0))) {
        return value;
      }
    }
    return std::string("spot");
  }

  virtual ccxt::any requestId() {
    // their support said that reqid must be an int32, not documented
    this->lockId();
    ccxt::any reqid =
        this->sum(this->safeInteger(this->options, std::string("reqid"), 0), 1);
    ::setValue(this->options, std::string("reqid"), reqid);
    this->unlockId();
    return reqid;
  }

  virtual std::shared_future<ccxt::any>
  subscribePublic(ccxt::any url, ccxt::any messageHash, ccxt::any payload,
                  ccxt::any channel, ccxt::any params = ccxt::dict{},
                  ccxt::any subscription = ccxt::any{}) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 ccxt::any requestId = this->requestId();
                 ccxt::any time = this->seconds();
                 ccxt::any request = ccxt::dict{
                     {std::string("id"), requestId},
                     {std::string("time"), time},
                     {std::string("channel"), channel},
                     {std::string("event"), std::string("subscribe")},
                     {std::string("payload"), payload},
                 };
                 if (isTrue(!isEqual(subscription, ccxt::any{}))) {
                   ccxt::any client = this->client(url);
                   if (!isTrue((inOp(
                           ::getValue(client, std::string("subscriptions")),
                           messageHash)))) {
                     ccxt::any tempSubscriptionHash = toString(requestId);
                     ::setValue(
                         ::getValue(client, std::string("subscriptions")),
                         tempSubscriptionHash, messageHash);
                   }
                 }
                 ccxt::any message = this->extend(request, params);
                 return awaitValue(this->watch(url, messageHash, message,
                                               messageHash, subscription));
               })
        .share();
  }

  virtual std::shared_future<ccxt::any>
  subscribePublicMultiple(ccxt::any url, ccxt::any messageHashes,
                          ccxt::any payload, ccxt::any channel,
                          ccxt::any params = ccxt::dict{}) {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        ccxt::any requestId = this->requestId();
                        ccxt::any time = this->seconds();
                        ccxt::any request = ccxt::dict{
                            {std::string("id"), requestId},
                            {std::string("time"), time},
                            {std::string("channel"), channel},
                            {std::string("event"), std::string("subscribe")},
                            {std::string("payload"), payload},
                        };
                        ccxt::any message = this->extend(request, params);
                        return awaitValue(this->watchMultiple(
                            url, messageHashes, message, messageHashes));
                      })
        .share();
  }

  virtual std::shared_future<ccxt::any>
  unSubscribePublicMultiple(ccxt::any url, ccxt::any topic, ccxt::any symbols,
                            ccxt::any messageHashes, ccxt::any subMessageHashes,
                            ccxt::any payload, ccxt::any channel,
                            ccxt::any params = ccxt::dict{}) {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        ccxt::any requestId = this->requestId();
                        ccxt::any time = this->seconds();
                        ccxt::any request = ccxt::dict{
                            {std::string("id"), requestId},
                            {std::string("time"), time},
                            {std::string("channel"), channel},
                            {std::string("event"), std::string("unsubscribe")},
                            {std::string("payload"), payload},
                        };
                        ccxt::any sub = ccxt::dict{
                            {std::string("id"), toString(requestId)},
                            {std::string("topic"), topic},
                            {std::string("unsubscribe"), true},
                            {std::string("messageHashes"), messageHashes},
                            {std::string("subMessageHashes"), subMessageHashes},
                            {std::string("symbols"), symbols},
                        };
                        ccxt::any message = this->extend(request, params);
                        return awaitValue(this->watchMultiple(
                            url, messageHashes, message, messageHashes, sub));
                      })
        .share();
  }

  virtual std::shared_future<ccxt::any> authenticate(ccxt::any url,
                                                    ccxt::any messageType) {
    return std::async(std::launch::deferred,
                      [=]() mutable -> ccxt::any {
                        ccxt::any channel =
                            add(messageType, std::string(".login"));
                        ccxt::any client = this->client(url);
                        ccxt::any messageHash = std::string("authenticated");
                        ccxt::any future =
                            ::wsClientReusableFuture(client, messageHash);
                        ccxt::any authenticated = this->safeValue(
                            ::getValue(client, std::string("subscriptions")),
                            messageHash);
                        if (isTrue(isEqual(authenticated, ccxt::any{}))) {
                          return awaitValue(this->requestPrivate(
                              url, ccxt::dict{}, channel, messageHash));
                        }
                        return future;
                      })
        .share();
  }

  virtual void handleAuthenticationMessage(ccxt::any client, ccxt::any message) {
    ccxt::any messageHash = std::string("authenticated");
    ccxt::any future = this->safeValue(
        ::getValue(client, std::string("futures")), messageHash);
    ::wsFutureResolve(future, true);
  }

  virtual std::shared_future<ccxt::any>
  requestPrivate(ccxt::any url, ccxt::any reqParams, ccxt::any channel,
                 ccxt::any requestId = ccxt::any{}) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 this->checkRequiredCredentials();
                 // uid is required for some subscriptions only so it's not a
                 // part of required credentials
                 ccxt::any event = std::string("api");
                 if (isTrue(isEqual(requestId, ccxt::any{}))) {
                   ccxt::any reqId = this->requestId();
                   requestId = toString(reqId);
                 }
                 ccxt::any messageHash = requestId;
                 ccxt::any time = this->seconds();
                 // unfortunately, PHP demands double quotes for the escaped
                 // newline symbol
                 ccxt::any signatureString =
                     join(ccxt::list{event, channel, this->json(reqParams),
                                     toString(time)},
                          std::string("\n")); // eslint-disable-line quotes
                 ccxt::any signature = this->hmac(this->encode(signatureString),
                                                 this->encode(this->secret),
                                                 sha512, std::string("hex"));
                 ccxt::any payload = ccxt::dict{
                     {std::string("req_id"), requestId},
                     {std::string("timestamp"), toString(time)},
                     {std::string("api_key"), this->apiKey},
                     {std::string("signature"), signature},
                     {std::string("req_param"), reqParams},
                 };
                 if (isTrue(
                         isTrue((isEqual(channel,
                                         std::string("spot.order_place")))) ||
                         isTrue((isEqual(
                             channel, std::string("futures.order_place")))))) {
                   ::setValue(payload, std::string("req_header"),
                              ccxt::dict{
                                  {std::string("X-Gate-Channel-Id"),
                                   std::string("ccxt")},
                              });
                 }
                 ccxt::any request = ccxt::dict{
                     {std::string("id"), requestId},
                     {std::string("time"), time},
                     {std::string("channel"), channel},
                     {std::string("event"), event},
                     {std::string("payload"), payload},
                 };
                 return awaitValue(this->watch(url, messageHash, request,
                                               messageHash, requestId));
               })
        .share();
  }

  virtual std::shared_future<ccxt::any>
  subscribePrivate(ccxt::any url, ccxt::any messageHash, ccxt::any payload,
                   ccxt::any channel, ccxt::any params,
                   ccxt::any requiresUid = false) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> ccxt::any {
                 this->checkRequiredCredentials();
                 // uid is required for some subscriptions only so it's not a
                 // part of required credentials
                 if (isTrue(requiresUid)) {
                   if (isTrue(isTrue(isEqual(this->uid, ccxt::any{})) ||
                              isTrue(isEqual(getStringLength(this->uid), 0)))) {
                     throw ArgumentsRequired(toString(add(
                         this->id, std::string(" requires uid to subscribe"))));
                   }
                   ccxt::any idArray = ccxt::list{this->uid};
                   if (isTrue(isEqual(payload, ccxt::any{}))) {
                     payload = idArray;
                   } else {
                     payload = this->arrayConcat(idArray, payload);
                   }
                 }
                 ccxt::any time = this->seconds();
                 ccxt::any event = std::string("subscribe");
                 ccxt::any signaturePayload = add(
                     add(add(add(add(add(add(std::string("channel="), channel),
                                         std::string("&")),
                                     std::string("event=")),
                                 event),
                             std::string("&")),
                         std::string("time=")),
                     toString(time));
                 ccxt::any signature = this->hmac(this->encode(signaturePayload),
                                                 this->encode(this->secret),
                                                 sha512, std::string("hex"));
                 ccxt::any auth = ccxt::dict{
                     {std::string("method"), std::string("api_key")},
                     {std::string("KEY"), this->apiKey},
                     {std::string("SIGN"), signature},
                 };
                 ccxt::any requestId = this->requestId();
                 ccxt::any request = ccxt::dict{
                     {std::string("id"), requestId},
                     {std::string("time"), time},
                     {std::string("channel"), channel},
                     {std::string("event"), event},
                     {std::string("auth"), auth},
                 };
                 if (isTrue(!isEqual(payload, ccxt::any{}))) {
                   ::setValue(request, std::string("payload"), payload);
                 }
                 ccxt::any client = this->client(url);
                 if (!isTrue(
                         (inOp(::getValue(client, std::string("subscriptions")),
                               messageHash)))) {
                   ccxt::any tempSubscriptionHash = toString(requestId);
                   // in case of authenticationError we will throw
                   ::setValue(::getValue(client, std::string("subscriptions")),
                              tempSubscriptionHash, messageHash);
                 }
                 ccxt::any message = this->extend(request, params);
                 return awaitValue(this->watch(url, messageHash, message,
                                               messageHash, messageHash));
               })
        .share();
  }
  // GENERATED dispatch table - see createDispatchTable in
  // build/cppTranspiler.ts
  virtual ccxt::any callMethod(ccxt::any name, ccxt::any args) override {
    const std::string which = ::toString(name).has_value()
                                  ? ccxt::any_cast<std::string>(::toString(name))
                                  : std::string();
    const long count =
        ccxt::isList(args)
            ? static_cast<long>(ccxt::any_cast<ccxt::list>(args).size())
            : 0;
    if (which == "describe") {
      if (true)
        return this->describe();
    }
    if (which == "describeData") {
      if (true)
        return this->describeData();
    }
    if (which == "createOrderWs") {
      if (count <= 4)
        return awaitValue(
            this->createOrderWs(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2), ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->createOrderWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count >= 6)
        return awaitValue(this->createOrderWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
    }
    if (which == "createOrdersWs") {
      if (count <= 1)
        return awaitValue(this->createOrdersWs(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->createOrdersWs(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "cancelAllOrdersWs") {
      if (count <= 0)
        return awaitValue(this->cancelAllOrdersWs());
      if (count == 1)
        return awaitValue(this->cancelAllOrdersWs(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->cancelAllOrdersWs(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "cancelOrderWs") {
      if (count <= 1)
        return awaitValue(this->cancelOrderWs(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->cancelOrderWs(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->cancelOrderWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "editOrderWs") {
      if (count <= 4)
        return awaitValue(
            this->editOrderWs(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2), ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->editOrderWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->editOrderWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count >= 7)
        return awaitValue(this->editOrderWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
    }
    if (which == "fetchOrderWs") {
      if (count <= 1)
        return awaitValue(this->fetchOrderWs(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOrderWs(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->fetchOrderWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "fetchOpenOrdersWs") {
      if (count <= 0)
        return awaitValue(this->fetchOpenOrdersWs());
      if (count == 1)
        return awaitValue(this->fetchOpenOrdersWs(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->fetchOpenOrdersWs(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchOpenOrdersWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->fetchOpenOrdersWs(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "fetchClosedOrdersWs") {
      if (count <= 0)
        return awaitValue(this->fetchClosedOrdersWs());
      if (count == 1)
        return awaitValue(this->fetchClosedOrdersWs(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchClosedOrdersWs(::getValue(args, 0),
                                                    ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchClosedOrdersWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->fetchClosedOrdersWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "fetchOrdersByStatusWs") {
      if (count <= 1)
        return awaitValue(this->fetchOrdersByStatusWs(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchOrdersByStatusWs(::getValue(args, 0),
                                                      ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchOrdersByStatusWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchOrdersByStatusWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->fetchOrdersByStatusWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "watchOrderBook") {
      if (count <= 1)
        return awaitValue(this->watchOrderBook(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->watchOrderBook(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->watchOrderBook(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "unWatchOrderBook") {
      if (count <= 1)
        return awaitValue(this->unWatchOrderBook(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->unWatchOrderBook(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "handleOrderBookSubscription") {
      if (true) {
        this->handleOrderBookSubscription(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "handleNewSpotOrderBook") {
      if (true) {
        this->handleNewSpotOrderBook(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "handleOrderBook") {
      if (true) {
        this->handleOrderBook(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "getCacheIndex") {
      if (true)
        return this->getCacheIndex(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "handleBidAsks") {
      if (true) {
        this->handleBidAsks(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "handleDelta") {
      if (true) {
        this->handleDelta(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "watchTicker") {
      if (count <= 1)
        return awaitValue(this->watchTicker(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->watchTicker(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "watchTickers") {
      if (count <= 0)
        return awaitValue(this->watchTickers());
      if (count == 1)
        return awaitValue(this->watchTickers(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->watchTickers(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "handleTicker") {
      if (true) {
        this->handleTicker(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "watchBidsAsks") {
      if (count <= 0)
        return awaitValue(this->watchBidsAsks());
      if (count == 1)
        return awaitValue(this->watchBidsAsks(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->watchBidsAsks(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "handleBidAsk") {
      if (true) {
        this->handleBidAsk(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "subscribeWatchTickersAndBidsAsks") {
      if (count <= 0)
        return awaitValue(this->subscribeWatchTickersAndBidsAsks());
      if (count == 1)
        return awaitValue(
            this->subscribeWatchTickersAndBidsAsks(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->subscribeWatchTickersAndBidsAsks(
            ::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->subscribeWatchTickersAndBidsAsks(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "handleTickerAndBidAsk") {
      if (true) {
        this->handleTickerAndBidAsk(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "watchTrades") {
      if (count <= 1)
        return awaitValue(this->watchTrades(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->watchTrades(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchTrades(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->watchTrades(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "watchTradesForSymbols") {
      if (count <= 1)
        return awaitValue(this->watchTradesForSymbols(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->watchTradesForSymbols(::getValue(args, 0),
                                                      ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchTradesForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->watchTradesForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "unWatchTradesForSymbols") {
      if (count <= 1)
        return awaitValue(this->unWatchTradesForSymbols(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(this->unWatchTradesForSymbols(::getValue(args, 0),
                                                        ::getValue(args, 1)));
    }
    if (which == "unWatchTrades") {
      if (count <= 1)
        return awaitValue(this->unWatchTrades(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->unWatchTrades(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "handleTrades") {
      if (true) {
        this->handleTrades(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "watchOHLCV") {
      if (count <= 1)
        return awaitValue(this->watchOHLCV(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->watchOHLCV(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(
            this->watchOHLCV(::getValue(args, 0), ::getValue(args, 1),
                             ::getValue(args, 2), ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->watchOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "handleOHLCV") {
      if (true) {
        this->handleOHLCV(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "watchMyTrades") {
      if (count <= 0)
        return awaitValue(this->watchMyTrades());
      if (count == 1)
        return awaitValue(this->watchMyTrades(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->watchMyTrades(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchMyTrades(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->watchMyTrades(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "handleMyTrades") {
      if (true) {
        this->handleMyTrades(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "watchBalance") {
      if (count <= 0)
        return awaitValue(this->watchBalance());
      if (count >= 1)
        return awaitValue(this->watchBalance(::getValue(args, 0)));
    }
    if (which == "handleBalance") {
      if (true) {
        this->handleBalance(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "watchPositions") {
      if (count <= 0)
        return awaitValue(this->watchPositions());
      if (count == 1)
        return awaitValue(this->watchPositions(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->watchPositions(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchPositions(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->watchPositions(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "setPositionsCache") {
      if (count <= 2) {
        this->setPositionsCache(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
      if (count >= 3) {
        this->setPositionsCache(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "loadPositionsSnapshot") {
      if (true)
        return awaitValue(this->loadPositionsSnapshot(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "handlePositions") {
      if (true) {
        this->handlePositions(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "watchOrders") {
      if (count <= 0)
        return awaitValue(this->watchOrders());
      if (count == 1)
        return awaitValue(this->watchOrders(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->watchOrders(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->watchOrders(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "handleOrder") {
      if (true) {
        this->handleOrder(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "watchMyLiquidations") {
      if (count <= 1)
        return awaitValue(this->watchMyLiquidations(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->watchMyLiquidations(::getValue(args, 0),
                                                    ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchMyLiquidations(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->watchMyLiquidations(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "watchMyLiquidationsForSymbols") {
      if (count <= 1)
        return awaitValue(
            this->watchMyLiquidationsForSymbols(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->watchMyLiquidationsForSymbols(
            ::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchMyLiquidationsForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->watchMyLiquidationsForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "handleLiquidation") {
      if (true) {
        this->handleLiquidation(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "parseWsLiquidation") {
      if (count <= 1)
        return this->parseWsLiquidation(::getValue(args, 0));
      if (count >= 2)
        return this->parseWsLiquidation(::getValue(args, 0),
                                        ::getValue(args, 1));
    }
    if (which == "handleErrorMessage") {
      if (true)
        return this->handleErrorMessage(::getValue(args, 0),
                                        ::getValue(args, 1));
    }
    if (which == "handleBalanceSubscription") {
      if (count <= 2) {
        this->handleBalanceSubscription(::getValue(args, 0),
                                        ::getValue(args, 1));
        return ccxt::any{};
      }
      if (count >= 3) {
        this->handleBalanceSubscription(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "handleSubscriptionStatus") {
      if (true) {
        this->handleSubscriptionStatus(::getValue(args, 0),
                                       ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "handleUnSubscribe") {
      if (true) {
        this->handleUnSubscribe(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "handleMessage") {
      if (true) {
        this->handleMessage(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "getUrlByMarket") {
      if (true)
        return this->getUrlByMarket(::getValue(args, 0));
    }
    if (which == "getTypeByMarket") {
      if (true)
        return this->getTypeByMarket(::getValue(args, 0));
    }
    if (which == "getUrlByMarketType") {
      if (count <= 1)
        return this->getUrlByMarketType(::getValue(args, 0));
      if (count >= 2)
        return this->getUrlByMarketType(::getValue(args, 0),
                                        ::getValue(args, 1));
    }
    if (which == "getMarketTypeByUrl") {
      if (true)
        return this->getMarketTypeByUrl(::getValue(args, 0));
    }
    if (which == "requestId") {
      if (true)
        return this->requestId();
    }
    if (which == "subscribePublic") {
      if (count <= 4)
        return awaitValue(
            this->subscribePublic(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->subscribePublic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count >= 6)
        return awaitValue(this->subscribePublic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
    }
    if (which == "subscribePublicMultiple") {
      if (count <= 4)
        return awaitValue(this->subscribePublicMultiple(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->subscribePublicMultiple(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "unSubscribePublicMultiple") {
      if (count <= 7)
        return awaitValue(this->unSubscribePublicMultiple(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
      if (count >= 8)
        return awaitValue(this->unSubscribePublicMultiple(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7)));
    }
    if (which == "authenticate") {
      if (true)
        return awaitValue(
            this->authenticate(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "handleAuthenticationMessage") {
      if (true) {
        this->handleAuthenticationMessage(::getValue(args, 0),
                                          ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "requestPrivate") {
      if (count <= 3)
        return awaitValue(this->requestPrivate(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->requestPrivate(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "subscribePrivate") {
      if (count <= 5)
        return awaitValue(this->subscribePrivate(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count >= 6)
        return awaitValue(this->subscribePrivate(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
    }
    if (which == "cleanRestData") {
      if (true) {
        this->cleanRestData();
        return ccxt::any{};
      }
    }
    if (which == "cleanWsData") {
      if (true) {
        this->cleanWsData();
        return ccxt::any{};
      }
    }
    if (which == "safeBoolN") {
      if (count <= 2)
        return this->safeBoolN(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeBoolN(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2));
    }
    if (which == "safeBool2") {
      if (count <= 3)
        return this->safeBool2(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2));
      if (count >= 4)
        return this->safeBool2(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "safeBool") {
      if (count <= 2)
        return this->safeBool(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeBool(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2));
    }
    if (which == "safeDictN") {
      if (count <= 2)
        return this->safeDictN(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeDictN(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2));
    }
    if (which == "safeDict") {
      if (count <= 2)
        return this->safeDict(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeDict(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2));
    }
    if (which == "safeDict2") {
      if (count <= 3)
        return this->safeDict2(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2));
      if (count >= 4)
        return this->safeDict2(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "safeListN") {
      if (count <= 2)
        return this->safeListN(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeListN(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2));
    }
    if (which == "safeList2") {
      if (count <= 3)
        return this->safeList2(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2));
      if (count >= 4)
        return this->safeList2(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "safeList") {
      if (count <= 2)
        return this->safeList(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeList(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2));
    }
    if (which == "storeByKey") {
      if (true) {
        this->storeByKey(::getValue(args, 0), ::getValue(args, 1),
                         ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "handleDeltas") {
      if (true) {
        this->handleDeltas(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "handleDeltasWithKeys") {
      if (count <= 2) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
      if (count == 3) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
        return ccxt::any{};
      }
      if (count == 4) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
        return ccxt::any{};
      }
      if (count >= 5) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3),
                                   ::getValue(args, 4));
        return ccxt::any{};
      }
    }
    if (which == "arraysConcat") {
      if (true)
        return this->arraysConcat(::getValue(args, 0));
    }
    if (which == "findTimeframe") {
      if (count <= 1)
        return this->findTimeframe(::getValue(args, 0));
      if (count >= 2)
        return this->findTimeframe(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "checkProxyUrlSettings") {
      if (count <= 0)
        return this->checkProxyUrlSettings();
      if (count == 1)
        return this->checkProxyUrlSettings(::getValue(args, 0));
      if (count == 2)
        return this->checkProxyUrlSettings(::getValue(args, 0),
                                           ::getValue(args, 1));
      if (count == 3)
        return this->checkProxyUrlSettings(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->checkProxyUrlSettings(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "urlEncoderForProxyUrl") {
      if (true)
        return this->urlEncoderForProxyUrl(::getValue(args, 0));
    }
    if (which == "checkProxySettings") {
      if (count <= 0)
        return this->checkProxySettings();
      if (count == 1)
        return this->checkProxySettings(::getValue(args, 0));
      if (count == 2)
        return this->checkProxySettings(::getValue(args, 0),
                                        ::getValue(args, 1));
      if (count == 3)
        return this->checkProxySettings(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->checkProxySettings(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "checkWsProxySettings") {
      if (true)
        return this->checkWsProxySettings();
    }
    if (which == "checkConflictingProxies") {
      if (true) {
        this->checkConflictingProxies(::getValue(args, 0), ::getValue(args, 1));
        return ccxt::any{};
      }
    }
    if (which == "checkAddress") {
      if (count <= 0)
        return this->checkAddress();
      if (count >= 1)
        return this->checkAddress(::getValue(args, 0));
    }
    if (which == "findMessageHashes") {
      if (true)
        return this->findMessageHashes(::getValue(args, 0),
                                       ::getValue(args, 1));
    }
    if (which == "filterByLimit") {
      if (count <= 1)
        return this->filterByLimit(::getValue(args, 0));
      if (count == 2)
        return this->filterByLimit(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->filterByLimit(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
      if (count >= 4)
        return this->filterByLimit(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "filterBySinceLimit") {
      if (count <= 1)
        return this->filterBySinceLimit(::getValue(args, 0));
      if (count == 2)
        return this->filterBySinceLimit(::getValue(args, 0),
                                        ::getValue(args, 1));
      if (count == 3)
        return this->filterBySinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->filterBySinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->filterBySinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "filterByValueSinceLimit") {
      if (count <= 2)
        return this->filterByValueSinceLimit(::getValue(args, 0),
                                             ::getValue(args, 1));
      if (count == 3)
        return this->filterByValueSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->filterByValueSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count == 5)
        return this->filterByValueSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
      if (count == 6)
        return this->filterByValueSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5));
      if (count >= 7)
        return this->filterByValueSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6));
    }
    if (which == "setSandboxMode") {
      if (true) {
        this->setSandboxMode(::getValue(args, 0));
        return ccxt::any{};
      }
    }
    if (which == "enableDemoTrading") {
      if (true) {
        this->enableDemoTrading(::getValue(args, 0));
        return ccxt::any{};
      }
    }
    if (which == "sign") {
      if (count <= 1)
        return this->sign(::getValue(args, 0));
      if (count == 2)
        return this->sign(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2));
      if (count == 4)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3));
      if (count == 5)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3),
                          ::getValue(args, 4));
      if (count >= 6)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3),
                          ::getValue(args, 4), ::getValue(args, 5));
    }
    if (which == "fetchTime") {
      if (count <= 0)
        return awaitValue(this->fetchTime());
      if (count >= 1)
        return awaitValue(this->fetchTime(::getValue(args, 0)));
    }
    if (which == "parseCurrency") {
      if (true)
        return this->parseCurrency(::getValue(args, 0));
    }
    if (which == "parseCurrencies") {
      if (true)
        return this->parseCurrencies(::getValue(args, 0));
    }
    if (which == "parseMarket") {
      if (true)
        return this->parseMarket(::getValue(args, 0));
    }
    if (which == "parseMarkets") {
      if (true)
        return this->parseMarkets(::getValue(args, 0));
    }
    if (which == "parseTicker") {
      if (count <= 1)
        return this->parseTicker(::getValue(args, 0));
      if (count >= 2)
        return this->parseTicker(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseDepositAddress") {
      if (count <= 1)
        return this->parseDepositAddress(::getValue(args, 0));
      if (count >= 2)
        return this->parseDepositAddress(::getValue(args, 0),
                                         ::getValue(args, 1));
    }
    if (which == "parseTrade") {
      if (count <= 1)
        return this->parseTrade(::getValue(args, 0));
      if (count >= 2)
        return this->parseTrade(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseTransaction") {
      if (count <= 1)
        return this->parseTransaction(::getValue(args, 0));
      if (count == 2)
        return this->parseTransaction(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseTransaction(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2));
      if (count >= 4)
        return this->parseTransaction(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "parseTransfer") {
      if (count <= 1)
        return this->parseTransfer(::getValue(args, 0));
      if (count >= 2)
        return this->parseTransfer(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseAccount") {
      if (true)
        return this->parseAccount(::getValue(args, 0));
    }
    if (which == "parseLedgerEntry") {
      if (count <= 1)
        return this->parseLedgerEntry(::getValue(args, 0));
      if (count >= 2)
        return this->parseLedgerEntry(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseOrder") {
      if (count <= 1)
        return this->parseOrder(::getValue(args, 0));
      if (count >= 2)
        return this->parseOrder(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseMarketLeverageTiers") {
      if (count <= 1)
        return this->parseMarketLeverageTiers(::getValue(args, 0));
      if (count >= 2)
        return this->parseMarketLeverageTiers(::getValue(args, 0),
                                              ::getValue(args, 1));
    }
    if (which == "parsePosition") {
      if (count <= 1)
        return this->parsePosition(::getValue(args, 0));
      if (count >= 2)
        return this->parsePosition(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseFundingRateHistory") {
      if (count <= 1)
        return this->parseFundingRateHistory(::getValue(args, 0));
      if (count >= 2)
        return this->parseFundingRateHistory(::getValue(args, 0),
                                             ::getValue(args, 1));
    }
    if (which == "parseBorrowInterest") {
      if (count <= 1)
        return this->parseBorrowInterest(::getValue(args, 0));
      if (count >= 2)
        return this->parseBorrowInterest(::getValue(args, 0),
                                         ::getValue(args, 1));
    }
    if (which == "parseIsolatedBorrowRate") {
      if (count <= 1)
        return this->parseIsolatedBorrowRate(::getValue(args, 0));
      if (count >= 2)
        return this->parseIsolatedBorrowRate(::getValue(args, 0),
                                             ::getValue(args, 1));
    }
    if (which == "parseWsTrade") {
      if (count <= 1)
        return this->parseWsTrade(::getValue(args, 0));
      if (count >= 2)
        return this->parseWsTrade(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseWsOrder") {
      if (count <= 1)
        return this->parseWsOrder(::getValue(args, 0));
      if (count >= 2)
        return this->parseWsOrder(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseWsOrderTrade") {
      if (count <= 1)
        return this->parseWsOrderTrade(::getValue(args, 0));
      if (count >= 2)
        return this->parseWsOrderTrade(::getValue(args, 0),
                                       ::getValue(args, 1));
    }
    if (which == "parseWsOHLCV") {
      if (count <= 1)
        return this->parseWsOHLCV(::getValue(args, 0));
      if (count >= 2)
        return this->parseWsOHLCV(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "transfer") {
      if (count <= 4)
        return awaitValue(
            this->transfer(::getValue(args, 0), ::getValue(args, 1),
                           ::getValue(args, 2), ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->transfer(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "withdraw") {
      if (count <= 3)
        return awaitValue(this->withdraw(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(
            this->withdraw(::getValue(args, 0), ::getValue(args, 1),
                           ::getValue(args, 2), ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->withdraw(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "addMargin") {
      if (count <= 2)
        return awaitValue(
            this->addMargin(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->addMargin(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "setMargin") {
      if (count <= 2)
        return awaitValue(
            this->setMargin(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->setMargin(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "fetchLongShortRatioHistory") {
      if (count <= 0)
        return awaitValue(this->fetchLongShortRatioHistory());
      if (count == 1)
        return awaitValue(
            this->fetchLongShortRatioHistory(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchLongShortRatioHistory(
            ::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchLongShortRatioHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchLongShortRatioHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->fetchLongShortRatioHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "fetchMarginAdjustmentHistory") {
      if (count <= 0)
        return awaitValue(this->fetchMarginAdjustmentHistory());
      if (count == 1)
        return awaitValue(
            this->fetchMarginAdjustmentHistory(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchMarginAdjustmentHistory(
            ::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchMarginAdjustmentHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchMarginAdjustmentHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->fetchMarginAdjustmentHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "fetchOpenInterestHistory") {
      if (count <= 1)
        return awaitValue(this->fetchOpenInterestHistory(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchOpenInterestHistory(::getValue(args, 0),
                                                         ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchOpenInterestHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchOpenInterestHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->fetchOpenInterestHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "signIn") {
      if (count <= 0)
        return awaitValue(this->signIn());
      if (count >= 1)
        return awaitValue(this->signIn(::getValue(args, 0)));
    }
    if (which == "parseToInt") {
      if (true)
        return this->parseToInt(::getValue(args, 0));
    }
    if (which == "parseToNumeric") {
      if (true)
        return this->parseToNumeric(::getValue(args, 0));
    }
    if (which == "isRoundNumber") {
      if (true)
        return this->isRoundNumber(::getValue(args, 0));
    }
    if (which == "isEmptyString") {
      if (true)
        return this->isEmptyString(::getValue(args, 0));
    }
    if (which == "safeNumberOmitZero") {
      if (count <= 2)
        return this->safeNumberOmitZero(::getValue(args, 0),
                                        ::getValue(args, 1));
      if (count >= 3)
        return this->safeNumberOmitZero(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "safeIntegerOmitZero") {
      if (count <= 2)
        return this->safeIntegerOmitZero(::getValue(args, 0),
                                         ::getValue(args, 1));
      if (count >= 3)
        return this->safeIntegerOmitZero(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "afterConstruct") {
      if (true) {
        this->afterConstruct();
        return ccxt::any{};
      }
    }
    if (which == "initRestRateLimiter") {
      if (true) {
        this->initRestRateLimiter();
        return ccxt::any{};
      }
    }
    if (which == "featuresGenerator") {
      if (true) {
        this->featuresGenerator();
        return ccxt::any{};
      }
    }
    if (which == "featuresMapper") {
      if (count <= 2)
        return this->featuresMapper(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->featuresMapper(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
    }
    if (which == "featureValue") {
      if (count <= 1)
        return this->featureValue(::getValue(args, 0));
      if (count == 2)
        return this->featureValue(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->featureValue(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2));
      if (count >= 4)
        return this->featureValue(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "featureValueByType") {
      if (count <= 2)
        return this->featureValueByType(::getValue(args, 0),
                                        ::getValue(args, 1));
      if (count == 3)
        return this->featureValueByType(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->featureValueByType(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->featureValueByType(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "orderbookChecksumMessage") {
      if (true)
        return this->orderbookChecksumMessage(::getValue(args, 0));
    }
    if (which == "createNetworksByIdObject") {
      if (true) {
        this->createNetworksByIdObject();
        return ccxt::any{};
      }
    }
    if (which == "getDefaultOptions") {
      if (true)
        return this->getDefaultOptions();
    }
    if (which == "safeLedgerEntry") {
      if (count <= 1)
        return this->safeLedgerEntry(::getValue(args, 0));
      if (count >= 2)
        return this->safeLedgerEntry(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "safeCurrencyStructure") {
      if (true)
        return this->safeCurrencyStructure(::getValue(args, 0));
    }
    if (which == "safeMarketStructure") {
      if (count <= 0)
        return this->safeMarketStructure();
      if (count >= 1)
        return this->safeMarketStructure(::getValue(args, 0));
    }
    if (which == "setMarkets") {
      if (count <= 1)
        return this->setMarkets(::getValue(args, 0));
      if (count >= 2)
        return this->setMarkets(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "setMarketsFromExchange") {
      if (true)
        return this->setMarketsFromExchange(::getValue(args, 0));
    }
    if (which == "getDescribeForExtendedWsExchange") {
      if (true)
        return this->getDescribeForExtendedWsExchange(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "safeBalance") {
      if (true)
        return this->safeBalance(::getValue(args, 0));
    }
    if (which == "safeOrder") {
      if (count <= 1)
        return this->safeOrder(::getValue(args, 0));
      if (count >= 2)
        return this->safeOrder(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseOrders") {
      if (count <= 1)
        return this->parseOrders(::getValue(args, 0));
      if (count == 2)
        return this->parseOrders(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseOrders(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
      if (count == 4)
        return this->parseOrders(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3));
      if (count >= 5)
        return this->parseOrders(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3),
                                 ::getValue(args, 4));
    }
    if (which == "calculateFeeWithRate") {
      if (count <= 5)
        return this->calculateFeeWithRate(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
      if (count == 6)
        return this->calculateFeeWithRate(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5));
      if (count == 7)
        return this->calculateFeeWithRate(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6));
      if (count >= 8)
        return this->calculateFeeWithRate(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7));
    }
    if (which == "calculateFee") {
      if (count <= 5)
        return this->calculateFee(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3),
                                  ::getValue(args, 4));
      if (count == 6)
        return this->calculateFee(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3),
                                  ::getValue(args, 4), ::getValue(args, 5));
      if (count >= 7)
        return this->calculateFee(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3),
                                  ::getValue(args, 4), ::getValue(args, 5),
                                  ::getValue(args, 6));
    }
    if (which == "safeLiquidation") {
      if (count <= 1)
        return this->safeLiquidation(::getValue(args, 0));
      if (count >= 2)
        return this->safeLiquidation(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "safeTrade") {
      if (count <= 1)
        return this->safeTrade(::getValue(args, 0));
      if (count >= 2)
        return this->safeTrade(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "createCcxtTradeId") {
      if (count <= 0)
        return this->createCcxtTradeId();
      if (count == 1)
        return this->createCcxtTradeId(::getValue(args, 0));
      if (count == 2)
        return this->createCcxtTradeId(::getValue(args, 0),
                                       ::getValue(args, 1));
      if (count == 3)
        return this->createCcxtTradeId(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2));
      if (count == 4)
        return this->createCcxtTradeId(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2),
                                       ::getValue(args, 3));
      if (count >= 5)
        return this->createCcxtTradeId(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2), ::getValue(args, 3),
                                       ::getValue(args, 4));
    }
    if (which == "parsedFeeAndFees") {
      if (true)
        return this->parsedFeeAndFees(::getValue(args, 0));
    }
    if (which == "parseFeeNumeric") {
      if (true)
        return this->parseFeeNumeric(::getValue(args, 0));
    }
    if (which == "findNearestCeiling") {
      if (true)
        return this->findNearestCeiling(::getValue(args, 0),
                                        ::getValue(args, 1));
    }
    if (which == "addKeyInArrayItems") {
      if (true)
        return this->addKeyInArrayItems(::getValue(args, 0),
                                        ::getValue(args, 1));
    }
    if (which == "invertFlatStringDictionary") {
      if (true)
        return this->invertFlatStringDictionary(::getValue(args, 0));
    }
    if (which == "stringToBase16") {
      if (true)
        return this->stringToBase16(::getValue(args, 0));
    }
    if (which == "reduceFeesByCurrency") {
      if (true)
        return this->reduceFeesByCurrency(::getValue(args, 0));
    }
    if (which == "safeTicker") {
      if (count <= 1)
        return this->safeTicker(::getValue(args, 0));
      if (count >= 2)
        return this->safeTicker(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "convertTradingViewToOHLCV") {
      if (count <= 1)
        return this->convertTradingViewToOHLCV(::getValue(args, 0));
      if (count == 2)
        return this->convertTradingViewToOHLCV(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count == 3)
        return this->convertTradingViewToOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->convertTradingViewToOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count == 5)
        return this->convertTradingViewToOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
      if (count == 6)
        return this->convertTradingViewToOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5));
      if (count == 7)
        return this->convertTradingViewToOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6));
      if (count >= 8)
        return this->convertTradingViewToOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7));
    }
    if (which == "convertOHLCVToTradingView") {
      if (count <= 1)
        return this->convertOHLCVToTradingView(::getValue(args, 0));
      if (count == 2)
        return this->convertOHLCVToTradingView(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count == 3)
        return this->convertOHLCVToTradingView(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->convertOHLCVToTradingView(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count == 5)
        return this->convertOHLCVToTradingView(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
      if (count == 6)
        return this->convertOHLCVToTradingView(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5));
      if (count == 7)
        return this->convertOHLCVToTradingView(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6));
      if (count >= 8)
        return this->convertOHLCVToTradingView(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7));
    }
    if (which == "marketIds") {
      if (count <= 0)
        return this->marketIds();
      if (count >= 1)
        return this->marketIds(::getValue(args, 0));
    }
    if (which == "currencyIds") {
      if (count <= 0)
        return this->currencyIds();
      if (count >= 1)
        return this->currencyIds(::getValue(args, 0));
    }
    if (which == "marketsForSymbols") {
      if (count <= 0)
        return this->marketsForSymbols();
      if (count >= 1)
        return this->marketsForSymbols(::getValue(args, 0));
    }
    if (which == "marketSymbols") {
      if (count <= 0)
        return this->marketSymbols();
      if (count == 1)
        return this->marketSymbols(::getValue(args, 0));
      if (count == 2)
        return this->marketSymbols(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->marketSymbols(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
      if (count == 4)
        return this->marketSymbols(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
      if (count >= 5)
        return this->marketSymbols(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3),
                                   ::getValue(args, 4));
    }
    if (which == "marketCodes") {
      if (count <= 0)
        return this->marketCodes();
      if (count >= 1)
        return this->marketCodes(::getValue(args, 0));
    }
    if (which == "parseOrderBookBidsAsks") {
      if (count <= 1)
        return this->parseOrderBookBidsAsks(::getValue(args, 0));
      if (count == 2)
        return this->parseOrderBookBidsAsks(::getValue(args, 0),
                                            ::getValue(args, 1));
      if (count == 3)
        return this->parseOrderBookBidsAsks(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->parseOrderBookBidsAsks(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "filterByKey") {
      if (count <= 2)
        return this->filterByKey(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->filterByKey(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
    }
    if (which == "filterBySymbol") {
      if (count <= 1)
        return this->filterBySymbol(::getValue(args, 0));
      if (count >= 2)
        return this->filterBySymbol(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseOHLCV") {
      if (count <= 1)
        return this->parseOHLCV(::getValue(args, 0));
      if (count >= 2)
        return this->parseOHLCV(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "safeNetwork") {
      if (true)
        return this->safeNetwork(::getValue(args, 0));
    }
    if (which == "prioritizedNetworkAliases") {
      if (count <= 0)
        return this->prioritizedNetworkAliases();
      if (count == 1)
        return this->prioritizedNetworkAliases(::getValue(args, 0));
      if (count == 2)
        return this->prioritizedNetworkAliases(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count >= 3)
        return this->prioritizedNetworkAliases(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "networkCodeToId") {
      if (count <= 1)
        return this->networkCodeToId(::getValue(args, 0));
      if (count >= 2)
        return this->networkCodeToId(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "networkIdToCode") {
      if (count <= 0)
        return this->networkIdToCode();
      if (count == 1)
        return this->networkIdToCode(::getValue(args, 0));
      if (count >= 2)
        return this->networkIdToCode(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "handleNetworkCodeAndParams") {
      if (true)
        return this->handleNetworkCodeAndParams(::getValue(args, 0));
    }
    if (which == "defaultNetworkCode") {
      if (true)
        return this->defaultNetworkCode(::getValue(args, 0));
    }
    if (which == "safeNumber2") {
      if (count <= 3)
        return this->safeNumber2(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
      if (count >= 4)
        return this->safeNumber2(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "parseOrderBook") {
      if (count <= 2)
        return this->parseOrderBook(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseOrderBook(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
      if (count == 4)
        return this->parseOrderBook(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3));
      if (count == 5)
        return this->parseOrderBook(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3),
                                    ::getValue(args, 4));
      if (count == 6)
        return this->parseOrderBook(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3),
                                    ::getValue(args, 4), ::getValue(args, 5));
      if (count == 7)
        return this->parseOrderBook(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3),
                                    ::getValue(args, 4), ::getValue(args, 5),
                                    ::getValue(args, 6));
      if (count >= 8)
        return this->parseOrderBook(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3),
                                    ::getValue(args, 4), ::getValue(args, 5),
                                    ::getValue(args, 6), ::getValue(args, 7));
    }
    if (which == "parseOHLCVs") {
      if (count <= 1)
        return this->parseOHLCVs(::getValue(args, 0));
      if (count == 2)
        return this->parseOHLCVs(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseOHLCVs(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
      if (count == 4)
        return this->parseOHLCVs(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3));
      if (count == 5)
        return this->parseOHLCVs(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3),
                                 ::getValue(args, 4));
      if (count >= 6)
        return this->parseOHLCVs(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3),
                                 ::getValue(args, 4), ::getValue(args, 5));
    }
    if (which == "parseLeverageTiers") {
      if (count <= 1)
        return this->parseLeverageTiers(::getValue(args, 0));
      if (count == 2)
        return this->parseLeverageTiers(::getValue(args, 0),
                                        ::getValue(args, 1));
      if (count >= 3)
        return this->parseLeverageTiers(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "safePosition") {
      if (true)
        return this->safePosition(::getValue(args, 0));
    }
    if (which == "parsePositions") {
      if (count <= 1)
        return this->parsePositions(::getValue(args, 0));
      if (count == 2)
        return this->parsePositions(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parsePositions(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
    }
    if (which == "parseADLRank") {
      if (count <= 1)
        return this->parseADLRank(::getValue(args, 0));
      if (count >= 2)
        return this->parseADLRank(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseADLRanks") {
      if (count <= 1)
        return this->parseADLRanks(::getValue(args, 0));
      if (count == 2)
        return this->parseADLRanks(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parseADLRanks(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
    }
    if (which == "parseAccounts") {
      if (count <= 1)
        return this->parseAccounts(::getValue(args, 0));
      if (count >= 2)
        return this->parseAccounts(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseTradesHelper") {
      if (count <= 2)
        return this->parseTradesHelper(::getValue(args, 0),
                                       ::getValue(args, 1));
      if (count == 3)
        return this->parseTradesHelper(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2));
      if (count == 4)
        return this->parseTradesHelper(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2),
                                       ::getValue(args, 3));
      if (count == 5)
        return this->parseTradesHelper(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2), ::getValue(args, 3),
                                       ::getValue(args, 4));
      if (count >= 6)
        return this->parseTradesHelper(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5));
    }
    if (which == "parseTrades") {
      if (count <= 1)
        return this->parseTrades(::getValue(args, 0));
      if (count == 2)
        return this->parseTrades(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseTrades(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
      if (count == 4)
        return this->parseTrades(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3));
      if (count >= 5)
        return this->parseTrades(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3),
                                 ::getValue(args, 4));
    }
    if (which == "parseWsTrades") {
      if (count <= 1)
        return this->parseWsTrades(::getValue(args, 0));
      if (count == 2)
        return this->parseWsTrades(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseWsTrades(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
      if (count == 4)
        return this->parseWsTrades(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
      if (count >= 5)
        return this->parseWsTrades(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3),
                                   ::getValue(args, 4));
    }
    if (which == "parseTransactions") {
      if (count <= 1)
        return this->parseTransactions(::getValue(args, 0));
      if (count == 2)
        return this->parseTransactions(::getValue(args, 0),
                                       ::getValue(args, 1));
      if (count == 3)
        return this->parseTransactions(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2));
      if (count == 4)
        return this->parseTransactions(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2),
                                       ::getValue(args, 3));
      if (count >= 5)
        return this->parseTransactions(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2), ::getValue(args, 3),
                                       ::getValue(args, 4));
    }
    if (which == "parseTransfers") {
      if (count <= 1)
        return this->parseTransfers(::getValue(args, 0));
      if (count == 2)
        return this->parseTransfers(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseTransfers(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
      if (count == 4)
        return this->parseTransfers(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3));
      if (count >= 5)
        return this->parseTransfers(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3),
                                    ::getValue(args, 4));
    }
    if (which == "parseLedger") {
      if (count <= 1)
        return this->parseLedger(::getValue(args, 0));
      if (count == 2)
        return this->parseLedger(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseLedger(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
      if (count == 4)
        return this->parseLedger(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3));
      if (count >= 5)
        return this->parseLedger(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3),
                                 ::getValue(args, 4));
    }
    if (which == "nonce") {
      if (true)
        return this->nonce();
    }
    if (which == "setHeaders") {
      if (true)
        return this->setHeaders(::getValue(args, 0));
    }
    if (which == "currencyId") {
      if (true)
        return this->currencyId(::getValue(args, 0));
    }
    if (which == "marketId") {
      if (true)
        return this->marketId(::getValue(args, 0));
    }
    if (which == "symbol") {
      if (true)
        return this->symbol(::getValue(args, 0));
    }
    if (which == "handleParamString") {
      if (count <= 2)
        return this->handleParamString(::getValue(args, 0),
                                       ::getValue(args, 1));
      if (count >= 3)
        return this->handleParamString(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2));
    }
    if (which == "handleParamString2") {
      if (count <= 3)
        return this->handleParamString2(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->handleParamString2(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "handleParamInteger") {
      if (count <= 2)
        return this->handleParamInteger(::getValue(args, 0),
                                        ::getValue(args, 1));
      if (count >= 3)
        return this->handleParamInteger(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "handleParamInteger2") {
      if (count <= 3)
        return this->handleParamInteger2(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->handleParamInteger2(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "handleParamBool") {
      if (count <= 2)
        return this->handleParamBool(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->handleParamBool(::getValue(args, 0), ::getValue(args, 1),
                                     ::getValue(args, 2));
    }
    if (which == "handleParamBool2") {
      if (count <= 3)
        return this->handleParamBool2(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2));
      if (count >= 4)
        return this->handleParamBool2(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "handleRequestNetwork") {
      if (count <= 3)
        return this->handleRequestNetwork(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->handleRequestNetwork(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->handleRequestNetwork(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "resolvePath") {
      if (true)
        return this->resolvePath(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "getListFromObjectValues") {
      if (true)
        return this->getListFromObjectValues(::getValue(args, 0),
                                             ::getValue(args, 1));
    }
    if (which == "filterByArray") {
      if (count <= 2)
        return this->filterByArray(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->filterByArray(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
      if (count >= 4)
        return this->filterByArray(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "filterOutByArray") {
      if (count <= 2)
        return this->filterOutByArray(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->filterOutByArray(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2));
      if (count >= 4)
        return this->filterOutByArray(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "buildOHLCVC") {
      if (count <= 1)
        return this->buildOHLCVC(::getValue(args, 0));
      if (count == 2)
        return this->buildOHLCVC(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->buildOHLCVC(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
      if (count >= 4)
        return this->buildOHLCVC(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "parseTradingViewOHLCV") {
      if (count <= 1)
        return this->parseTradingViewOHLCV(::getValue(args, 0));
      if (count == 2)
        return this->parseTradingViewOHLCV(::getValue(args, 0),
                                           ::getValue(args, 1));
      if (count == 3)
        return this->parseTradingViewOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->parseTradingViewOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->parseTradingViewOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "parseOrderBookBidAsk") {
      if (count <= 1)
        return this->parseOrderBookBidAsk(::getValue(args, 0));
      if (count == 2)
        return this->parseOrderBookBidAsk(::getValue(args, 0),
                                          ::getValue(args, 1));
      if (count == 3)
        return this->parseOrderBookBidAsk(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->parseOrderBookBidAsk(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "safeCurrency") {
      if (count <= 1)
        return this->safeCurrency(::getValue(args, 0));
      if (count >= 2)
        return this->safeCurrency(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "safeMarket") {
      if (count <= 0)
        return this->safeMarket();
      if (count == 1)
        return this->safeMarket(::getValue(args, 0));
      if (count == 2)
        return this->safeMarket(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->safeMarket(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2));
      if (count >= 4)
        return this->safeMarket(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "marketOrNull") {
      if (count <= 0)
        return this->marketOrNull();
      if (count >= 1)
        return this->marketOrNull(::getValue(args, 0));
    }
    if (which == "checkRequiredCredentials") {
      if (count <= 0)
        return this->checkRequiredCredentials();
      if (count >= 1)
        return this->checkRequiredCredentials(::getValue(args, 0));
    }
    if (which == "oath") {
      if (true)
        return this->oath();
    }
    if (which == "parseBalance") {
      if (true)
        return this->parseBalance(::getValue(args, 0));
    }
    if (which == "getSupportedMapping") {
      if (count <= 1)
        return this->getSupportedMapping(::getValue(args, 0));
      if (count >= 2)
        return this->getSupportedMapping(::getValue(args, 0),
                                         ::getValue(args, 1));
    }
    if (which == "requireValue") {
      if (count <= 1)
        return this->requireValue(::getValue(args, 0));
      if (count >= 2)
        return this->requireValue(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "handleOptionAndParams") {
      if (count <= 3)
        return this->handleOptionAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->handleOptionAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "handleOptionAndParams2") {
      if (count <= 4)
        return this->handleOptionAndParams2(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->handleOptionAndParams2(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "handleOption") {
      if (count <= 2)
        return this->handleOption(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->handleOption(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2));
    }
    if (which == "handleMarketTypeAndParams") {
      if (count <= 1)
        return this->handleMarketTypeAndParams(::getValue(args, 0));
      if (count == 2)
        return this->handleMarketTypeAndParams(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count == 3)
        return this->handleMarketTypeAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->handleMarketTypeAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "handleSubTypeAndParams") {
      if (count <= 1)
        return this->handleSubTypeAndParams(::getValue(args, 0));
      if (count == 2)
        return this->handleSubTypeAndParams(::getValue(args, 0),
                                            ::getValue(args, 1));
      if (count == 3)
        return this->handleSubTypeAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->handleSubTypeAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "handleMarginModeAndParams") {
      if (count <= 1)
        return this->handleMarginModeAndParams(::getValue(args, 0));
      if (count == 2)
        return this->handleMarginModeAndParams(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count >= 3)
        return this->handleMarginModeAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "throwExactlyMatchedException") {
      if (true) {
        this->throwExactlyMatchedException(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "throwBroadlyMatchedException") {
      if (true) {
        this->throwBroadlyMatchedException(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "findBroadlyMatchedKey") {
      if (true)
        return this->findBroadlyMatchedKey(::getValue(args, 0),
                                           ::getValue(args, 1));
    }
    if (which == "handleErrors") {
      if (true)
        return this->handleErrors(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7), ::getValue(args, 8));
    }
    if (which == "calculateRateLimiterCost") {
      if (count <= 4)
        return this->calculateRateLimiterCost(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->calculateRateLimiterCost(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "fetchConvertTradeHistory") {
      if (count <= 0)
        return awaitValue(this->fetchConvertTradeHistory());
      if (count == 1)
        return awaitValue(this->fetchConvertTradeHistory(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchConvertTradeHistory(::getValue(args, 0),
                                                         ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchConvertTradeHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->fetchConvertTradeHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "setTakeProfitAndStopLossParams") {
      if (count <= 4)
        return this->setTakeProfitAndStopLossParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count == 5)
        return this->setTakeProfitAndStopLossParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
      if (count == 6)
        return this->setTakeProfitAndStopLossParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5));
      if (count == 7)
        return this->setTakeProfitAndStopLossParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6));
      if (count >= 8)
        return this->setTakeProfitAndStopLossParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7));
    }
    if (which == "fetchDepositsWithdrawals") {
      if (count <= 0)
        return awaitValue(this->fetchDepositsWithdrawals());
      if (count == 1)
        return awaitValue(this->fetchDepositsWithdrawals(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchDepositsWithdrawals(::getValue(args, 0),
                                                         ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchDepositsWithdrawals(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->fetchDepositsWithdrawals(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "fetchFundingRateHistory") {
      if (count <= 0)
        return awaitValue(this->fetchFundingRateHistory());
      if (count == 1)
        return awaitValue(this->fetchFundingRateHistory(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchFundingRateHistory(::getValue(args, 0),
                                                        ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchFundingRateHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->fetchFundingRateHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "parseLastPrice") {
      if (count <= 1)
        return this->parseLastPrice(::getValue(args, 0));
      if (count >= 2)
        return this->parseLastPrice(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "account") {
      if (true)
        return this->account();
    }
    if (which == "commonCurrencyCode") {
      if (true)
        return this->commonCurrencyCode(::getValue(args, 0));
    }
    if (which == "currency") {
      if (true)
        return this->currency(::getValue(args, 0));
    }
    if (which == "market") {
      if (true)
        return this->market(::getValue(args, 0));
    }
    if (which == "createExpiredOptionMarket") {
      if (true)
        return this->createExpiredOptionMarket(::getValue(args, 0));
    }
    if (which == "isLeveragedCurrency") {
      if (count <= 1)
        return this->isLeveragedCurrency(::getValue(args, 0));
      if (count == 2)
        return this->isLeveragedCurrency(::getValue(args, 0),
                                         ::getValue(args, 1));
      if (count >= 3)
        return this->isLeveragedCurrency(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "handleWithdrawTagAndParams") {
      if (true)
        return this->handleWithdrawTagAndParams(::getValue(args, 0),
                                                ::getValue(args, 1));
    }
    if (which == "costToPrecision") {
      if (true)
        return this->costToPrecision(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "priceToPrecision") {
      if (true)
        return this->priceToPrecision(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "amountToPrecision") {
      if (true)
        return this->amountToPrecision(::getValue(args, 0),
                                       ::getValue(args, 1));
    }
    if (which == "feeToPrecision") {
      if (true)
        return this->feeToPrecision(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "currencyToPrecision") {
      if (count <= 2)
        return this->currencyToPrecision(::getValue(args, 0),
                                         ::getValue(args, 1));
      if (count >= 3)
        return this->currencyToPrecision(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "forceString") {
      if (true)
        return this->forceString(::getValue(args, 0));
    }
    if (which == "isTickPrecision") {
      if (true)
        return this->isTickPrecision();
    }
    if (which == "isDecimalPrecision") {
      if (true)
        return this->isDecimalPrecision();
    }
    if (which == "isSignificantPrecision") {
      if (true)
        return this->isSignificantPrecision();
    }
    if (which == "safeNumber") {
      if (count <= 2)
        return this->safeNumber(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeNumber(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2));
    }
    if (which == "safeNumberN") {
      if (count <= 2)
        return this->safeNumberN(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeNumberN(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
    }
    if (which == "parsePrecision") {
      if (true)
        return this->parsePrecision(::getValue(args, 0));
    }
    if (which == "integerPrecisionToAmount") {
      if (true)
        return this->integerPrecisionToAmount(::getValue(args, 0));
    }
    if (which == "implodeHostname") {
      if (true)
        return this->implodeHostname(::getValue(args, 0));
    }
    if (which == "safeCurrencyCode") {
      if (count <= 1)
        return this->safeCurrencyCode(::getValue(args, 0));
      if (count >= 2)
        return this->safeCurrencyCode(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "filterBySymbolSinceLimit") {
      if (count <= 1)
        return this->filterBySymbolSinceLimit(::getValue(args, 0));
      if (count == 2)
        return this->filterBySymbolSinceLimit(::getValue(args, 0),
                                              ::getValue(args, 1));
      if (count == 3)
        return this->filterBySymbolSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->filterBySymbolSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->filterBySymbolSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "filterByCurrencySinceLimit") {
      if (count <= 1)
        return this->filterByCurrencySinceLimit(::getValue(args, 0));
      if (count == 2)
        return this->filterByCurrencySinceLimit(::getValue(args, 0),
                                                ::getValue(args, 1));
      if (count == 3)
        return this->filterByCurrencySinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->filterByCurrencySinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->filterByCurrencySinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "filterBySymbolsSinceLimit") {
      if (count <= 1)
        return this->filterBySymbolsSinceLimit(::getValue(args, 0));
      if (count == 2)
        return this->filterBySymbolsSinceLimit(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count == 3)
        return this->filterBySymbolsSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->filterBySymbolsSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->filterBySymbolsSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "parseLastPrices") {
      if (count <= 1)
        return this->parseLastPrices(::getValue(args, 0));
      if (count == 2)
        return this->parseLastPrices(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parseLastPrices(::getValue(args, 0), ::getValue(args, 1),
                                     ::getValue(args, 2));
    }
    if (which == "parseTickers") {
      if (count <= 1)
        return this->parseTickers(::getValue(args, 0));
      if (count == 2)
        return this->parseTickers(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parseTickers(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2));
    }
    if (which == "parseDepositAddresses") {
      if (count <= 1)
        return this->parseDepositAddresses(::getValue(args, 0));
      if (count == 2)
        return this->parseDepositAddresses(::getValue(args, 0),
                                           ::getValue(args, 1));
      if (count == 3)
        return this->parseDepositAddresses(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->parseDepositAddresses(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "parseBorrowInterests") {
      if (count <= 1)
        return this->parseBorrowInterests(::getValue(args, 0));
      if (count >= 2)
        return this->parseBorrowInterests(::getValue(args, 0),
                                          ::getValue(args, 1));
    }
    if (which == "parseBorrowRate") {
      if (count <= 1)
        return this->parseBorrowRate(::getValue(args, 0));
      if (count >= 2)
        return this->parseBorrowRate(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseBorrowRateHistory") {
      if (true)
        return this->parseBorrowRateHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "parseIsolatedBorrowRates") {
      if (true)
        return this->parseIsolatedBorrowRates(::getValue(args, 0));
    }
    if (which == "parseFundingRateHistories") {
      if (count <= 1)
        return this->parseFundingRateHistories(::getValue(args, 0));
      if (count == 2)
        return this->parseFundingRateHistories(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count == 3)
        return this->parseFundingRateHistories(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->parseFundingRateHistories(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "safeSymbol") {
      if (count <= 1)
        return this->safeSymbol(::getValue(args, 0));
      if (count == 2)
        return this->safeSymbol(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->safeSymbol(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2));
      if (count >= 4)
        return this->safeSymbol(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "parseFundingRate") {
      if (count <= 1)
        return this->parseFundingRate(::getValue(args, 0));
      if (count >= 2)
        return this->parseFundingRate(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseFundingRates") {
      if (count <= 1)
        return this->parseFundingRates(::getValue(args, 0));
      if (count >= 2)
        return this->parseFundingRates(::getValue(args, 0),
                                       ::getValue(args, 1));
    }
    if (which == "parseLongShortRatio") {
      if (count <= 1)
        return this->parseLongShortRatio(::getValue(args, 0));
      if (count >= 2)
        return this->parseLongShortRatio(::getValue(args, 0),
                                         ::getValue(args, 1));
    }
    if (which == "parseLongShortRatioHistory") {
      if (count <= 1)
        return this->parseLongShortRatioHistory(::getValue(args, 0));
      if (count == 2)
        return this->parseLongShortRatioHistory(::getValue(args, 0),
                                                ::getValue(args, 1));
      if (count == 3)
        return this->parseLongShortRatioHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->parseLongShortRatioHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "handleTriggerPricesAndParams") {
      if (count <= 2)
        return this->handleTriggerPricesAndParams(::getValue(args, 0),
                                                  ::getValue(args, 1));
      if (count >= 3)
        return this->handleTriggerPricesAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "handleTriggerAndParams") {
      if (true)
        return this->handleTriggerAndParams(::getValue(args, 0));
    }
    if (which == "isTriggerOrder") {
      if (true)
        return this->isTriggerOrder(::getValue(args, 0));
    }
    if (which == "isPostOnly") {
      if (count <= 2)
        return this->isPostOnly(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->isPostOnly(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2));
    }
    if (which == "handlePostOnly") {
      if (count <= 2)
        return this->handlePostOnly(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->handlePostOnly(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
    }
    if (which == "parseOpenInterest") {
      if (count <= 1)
        return this->parseOpenInterest(::getValue(args, 0));
      if (count >= 2)
        return this->parseOpenInterest(::getValue(args, 0),
                                       ::getValue(args, 1));
    }
    if (which == "parseOpenInterests") {
      if (count <= 1)
        return this->parseOpenInterests(::getValue(args, 0));
      if (count >= 2)
        return this->parseOpenInterests(::getValue(args, 0),
                                        ::getValue(args, 1));
    }
    if (which == "parseOpenInterestsHistory") {
      if (count <= 1)
        return this->parseOpenInterestsHistory(::getValue(args, 0));
      if (count == 2)
        return this->parseOpenInterestsHistory(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count == 3)
        return this->parseOpenInterestsHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->parseOpenInterestsHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "handleTimeInForce") {
      if (count <= 0)
        return this->handleTimeInForce();
      if (count >= 1)
        return this->handleTimeInForce(::getValue(args, 0));
    }
    if (which == "convertTypeToAccount") {
      if (true)
        return this->convertTypeToAccount(::getValue(args, 0));
    }
    if (which == "checkRequiredArgument") {
      if (count <= 3) {
        this->checkRequiredArgument(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
        return ccxt::any{};
      }
      if (count >= 4) {
        this->checkRequiredArgument(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3));
        return ccxt::any{};
      }
    }
    if (which == "checkRequiredMarginArgument") {
      if (true) {
        this->checkRequiredMarginArgument(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return ccxt::any{};
      }
    }
    if (which == "parseDepositWithdrawFees") {
      if (count <= 1)
        return this->parseDepositWithdrawFees(::getValue(args, 0));
      if (count == 2)
        return this->parseDepositWithdrawFees(::getValue(args, 0),
                                              ::getValue(args, 1));
      if (count >= 3)
        return this->parseDepositWithdrawFees(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "parseDepositWithdrawFee") {
      if (count <= 1)
        return this->parseDepositWithdrawFee(::getValue(args, 0));
      if (count >= 2)
        return this->parseDepositWithdrawFee(::getValue(args, 0),
                                             ::getValue(args, 1));
    }
    if (which == "depositWithdrawFee") {
      if (true)
        return this->depositWithdrawFee(::getValue(args, 0));
    }
    if (which == "parseIncome") {
      if (count <= 1)
        return this->parseIncome(::getValue(args, 0));
      if (count >= 2)
        return this->parseIncome(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseIncomes") {
      if (count <= 1)
        return this->parseIncomes(::getValue(args, 0));
      if (count == 2)
        return this->parseIncomes(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseIncomes(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2));
      if (count >= 4)
        return this->parseIncomes(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "getMarketFromSymbols") {
      if (count <= 0)
        return this->getMarketFromSymbols();
      if (count >= 1)
        return this->getMarketFromSymbols(::getValue(args, 0));
    }
    if (which == "parseWsOHLCVs") {
      if (count <= 1)
        return this->parseWsOHLCVs(::getValue(args, 0));
      if (count == 2)
        return this->parseWsOHLCVs(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseWsOHLCVs(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
      if (count == 4)
        return this->parseWsOHLCVs(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
      if (count >= 5)
        return this->parseWsOHLCVs(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3),
                                   ::getValue(args, 4));
    }
    if (which == "filterByArrayPositions") {
      if (count <= 2)
        return this->filterByArrayPositions(::getValue(args, 0),
                                            ::getValue(args, 1));
      if (count == 3)
        return this->filterByArrayPositions(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->filterByArrayPositions(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "filterByArrayTickers") {
      if (count <= 2)
        return this->filterByArrayTickers(::getValue(args, 0),
                                          ::getValue(args, 1));
      if (count == 3)
        return this->filterByArrayTickers(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->filterByArrayTickers(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "filterByArrayADLRanks") {
      if (count <= 2)
        return this->filterByArrayADLRanks(::getValue(args, 0),
                                           ::getValue(args, 1));
      if (count == 3)
        return this->filterByArrayADLRanks(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->filterByArrayADLRanks(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "createOHLCVObject") {
      if (true)
        return this->createOHLCVObject(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2));
    }
    if (which == "fetchPaginatedCallDynamic") {
      if (count <= 1)
        return awaitValue(this->fetchPaginatedCallDynamic(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchPaginatedCallDynamic(::getValue(args, 0),
                                                          ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchPaginatedCallDynamic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchPaginatedCallDynamic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->fetchPaginatedCallDynamic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->fetchPaginatedCallDynamic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count >= 7)
        return awaitValue(this->fetchPaginatedCallDynamic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
    }
    if (which == "fetchPaginatedCallDeterministic") {
      if (count <= 1)
        return awaitValue(
            this->fetchPaginatedCallDeterministic(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchPaginatedCallDeterministic(
            ::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchPaginatedCallDeterministic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchPaginatedCallDeterministic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->fetchPaginatedCallDeterministic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->fetchPaginatedCallDeterministic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count >= 7)
        return awaitValue(this->fetchPaginatedCallDeterministic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
    }
    if (which == "fetchPaginatedCallCursor") {
      if (count <= 1)
        return awaitValue(this->fetchPaginatedCallCursor(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchPaginatedCallCursor(::getValue(args, 0),
                                                         ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchPaginatedCallCursor(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchPaginatedCallCursor(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->fetchPaginatedCallCursor(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->fetchPaginatedCallCursor(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count == 7)
        return awaitValue(this->fetchPaginatedCallCursor(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
      if (count == 8)
        return awaitValue(this->fetchPaginatedCallCursor(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7)));
      if (count >= 9)
        return awaitValue(this->fetchPaginatedCallCursor(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7), ::getValue(args, 8)));
    }
    if (which == "fetchPaginatedCallIncremental") {
      if (count <= 1)
        return awaitValue(
            this->fetchPaginatedCallIncremental(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchPaginatedCallIncremental(
            ::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchPaginatedCallIncremental(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchPaginatedCallIncremental(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->fetchPaginatedCallIncremental(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->fetchPaginatedCallIncremental(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count >= 7)
        return awaitValue(this->fetchPaginatedCallIncremental(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
    }
    if (which == "sortCursorPaginatedResult") {
      if (true)
        return this->sortCursorPaginatedResult(::getValue(args, 0));
    }
    if (which == "removeRepeatedTradesFromArray") {
      if (true)
        return this->removeRepeatedTradesFromArray(::getValue(args, 0));
    }
    if (which == "removeKeysFromDict") {
      if (true)
        return this->removeKeysFromDict(::getValue(args, 0),
                                        ::getValue(args, 1));
    }
    if (which == "handleUntilOption") {
      if (count <= 3)
        return this->handleUntilOption(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2));
      if (count >= 4)
        return this->handleUntilOption(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2),
                                       ::getValue(args, 3));
    }
    if (which == "safeOpenInterest") {
      if (count <= 1)
        return this->safeOpenInterest(::getValue(args, 0));
      if (count >= 2)
        return this->safeOpenInterest(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseLiquidation") {
      if (count <= 1)
        return this->parseLiquidation(::getValue(args, 0));
      if (count >= 2)
        return this->parseLiquidation(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseLiquidations") {
      if (count <= 1)
        return this->parseLiquidations(::getValue(args, 0));
      if (count == 2)
        return this->parseLiquidations(::getValue(args, 0),
                                       ::getValue(args, 1));
      if (count == 3)
        return this->parseLiquidations(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2));
      if (count >= 4)
        return this->parseLiquidations(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2),
                                       ::getValue(args, 3));
    }
    if (which == "parseGreeks") {
      if (count <= 1)
        return this->parseGreeks(::getValue(args, 0));
      if (count >= 2)
        return this->parseGreeks(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseAllGreeks") {
      if (count <= 1)
        return this->parseAllGreeks(::getValue(args, 0));
      if (count == 2)
        return this->parseAllGreeks(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parseAllGreeks(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
    }
    if (which == "parseOption") {
      if (count <= 1)
        return this->parseOption(::getValue(args, 0));
      if (count == 2)
        return this->parseOption(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parseOption(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
    }
    if (which == "parseOptionChain") {
      if (count <= 1)
        return this->parseOptionChain(::getValue(args, 0));
      if (count == 2)
        return this->parseOptionChain(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parseOptionChain(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2));
    }
    if (which == "parseMarginModes") {
      if (count <= 1)
        return this->parseMarginModes(::getValue(args, 0));
      if (count == 2)
        return this->parseMarginModes(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseMarginModes(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2));
      if (count >= 4)
        return this->parseMarginModes(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "parseMarginMode") {
      if (count <= 1)
        return this->parseMarginMode(::getValue(args, 0));
      if (count >= 2)
        return this->parseMarginMode(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseLeverages") {
      if (count <= 1)
        return this->parseLeverages(::getValue(args, 0));
      if (count == 2)
        return this->parseLeverages(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseLeverages(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
      if (count >= 4)
        return this->parseLeverages(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "parseLeverage") {
      if (count <= 1)
        return this->parseLeverage(::getValue(args, 0));
      if (count >= 2)
        return this->parseLeverage(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseConversions") {
      if (count <= 1)
        return this->parseConversions(::getValue(args, 0));
      if (count == 2)
        return this->parseConversions(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseConversions(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2));
      if (count == 4)
        return this->parseConversions(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3));
      if (count == 5)
        return this->parseConversions(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3),
                                      ::getValue(args, 4));
      if (count == 6)
        return this->parseConversions(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3),
                                      ::getValue(args, 4), ::getValue(args, 5));
      if (count >= 7)
        return this->parseConversions(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3),
                                      ::getValue(args, 4), ::getValue(args, 5),
                                      ::getValue(args, 6));
    }
    if (which == "parseConversion") {
      if (count <= 1)
        return this->parseConversion(::getValue(args, 0));
      if (count == 2)
        return this->parseConversion(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parseConversion(::getValue(args, 0), ::getValue(args, 1),
                                     ::getValue(args, 2));
    }
    if (which == "convertExpireDate") {
      if (true)
        return this->convertExpireDate(::getValue(args, 0));
    }
    if (which == "convertExpireDateToMarketIdDate") {
      if (true)
        return this->convertExpireDateToMarketIdDate(::getValue(args, 0));
    }
    if (which == "convertMarketIdExpireDate") {
      if (true)
        return this->convertMarketIdExpireDate(::getValue(args, 0));
    }
    if (which == "loadMarketsAndSignIn") {
      if (true)
        return awaitValue(this->loadMarketsAndSignIn());
    }
    if (which == "parseMarginModification") {
      if (count <= 1)
        return this->parseMarginModification(::getValue(args, 0));
      if (count >= 2)
        return this->parseMarginModification(::getValue(args, 0),
                                             ::getValue(args, 1));
    }
    if (which == "parseMarginModifications") {
      if (count <= 1)
        return this->parseMarginModifications(::getValue(args, 0));
      if (count == 2)
        return this->parseMarginModifications(::getValue(args, 0),
                                              ::getValue(args, 1));
      if (count == 3)
        return this->parseMarginModifications(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->parseMarginModifications(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "cleanUnsubscription") {
      if (count <= 3) {
        this->cleanUnsubscription(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2));
        return ccxt::any{};
      }
      if (count >= 4) {
        this->cleanUnsubscription(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3));
        return ccxt::any{};
      }
    }
    if (which == "cleanCache") {
      if (true) {
        this->cleanCache(::getValue(args, 0));
        return ccxt::any{};
      }
    }
    if (which == "timeframeFromMilliseconds") {
      if (true)
        return this->timeframeFromMilliseconds(::getValue(args, 0));
    }
    if (which == "fetchCanceledAndClosedOrders") {
      if (count <= 0)
        return awaitValue(this->fetchCanceledAndClosedOrders());
      if (count == 1)
        return awaitValue(
            this->fetchCanceledAndClosedOrders(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchCanceledAndClosedOrders(
            ::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchCanceledAndClosedOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->fetchCanceledAndClosedOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "createOrderWithTakeProfitAndStopLossWs") {
      if (count <= 4)
        return awaitValue(this->createOrderWithTakeProfitAndStopLossWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->createOrderWithTakeProfitAndStopLossWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->createOrderWithTakeProfitAndStopLossWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count == 7)
        return awaitValue(this->createOrderWithTakeProfitAndStopLossWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
      if (count >= 8)
        return awaitValue(this->createOrderWithTakeProfitAndStopLossWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7)));
    }
    if (which == "editOrder") {
      if (count <= 4)
        return awaitValue(
            this->editOrder(::getValue(args, 0), ::getValue(args, 1),
                            ::getValue(args, 2), ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->editOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->editOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count >= 7)
        return awaitValue(this->editOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
    }
    if (which == "editOrderWithClientOrderId") {
      if (count <= 4)
        return awaitValue(this->editOrderWithClientOrderId(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->editOrderWithClientOrderId(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->editOrderWithClientOrderId(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count >= 7)
        return awaitValue(this->editOrderWithClientOrderId(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
    }
    if (which == "watchPositionForSymbols") {
      if (count <= 0)
        return awaitValue(this->watchPositionForSymbols());
      if (count == 1)
        return awaitValue(this->watchPositionForSymbols(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->watchPositionForSymbols(::getValue(args, 0),
                                                        ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchPositionForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->watchPositionForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "createOrderWithTakeProfitAndStopLoss") {
      if (count <= 4)
        return awaitValue(this->createOrderWithTakeProfitAndStopLoss(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->createOrderWithTakeProfitAndStopLoss(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->createOrderWithTakeProfitAndStopLoss(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count == 7)
        return awaitValue(this->createOrderWithTakeProfitAndStopLoss(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
      if (count >= 8)
        return awaitValue(this->createOrderWithTakeProfitAndStopLoss(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7)));
    }
    // not defined on this exchange: fall back to the TS parent class
    return ccxt::gate::callMethod(name, args);
  }
};

} // namespace pro
} // namespace ccxt
