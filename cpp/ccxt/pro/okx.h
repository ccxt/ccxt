#pragma once

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#include "../base/Exchange.h"
#include "../exchanges/okx.h"
#include "ProExchangeFactory.h"

namespace ccxt {
namespace pro {

class okx : public ccxt::okx {
public:
  using ccxt::okx::okx;
  std::any describe() override {
    return this->deepExtend(
        ccxt::okx::describe(),
        ccxt::dict{
            {std::string("has"),
             ccxt::dict{
                 {std::string("ws"), true},
                 {std::string("watchTicker"), true},
                 {std::string("watchMarkPrice"), true},
                 {std::string("watchMarkPrices"), true},
                 {std::string("watchTickers"), true},
                 {std::string("watchBidsAsks"), true},
                 {std::string("watchOrderBook"), true},
                 {std::string("watchTrades"), true},
                 {std::string("watchTradesForSymbols"), true},
                 {std::string("watchOrderBookForSymbols"), true},
                 {std::string("watchBalance"), true},
                 {std::string("watchLiquidations"), std::string("emulated")},
                 {std::string("watchLiquidationsForSymbols"), true},
                 {std::string("watchMyLiquidations"), std::string("emulated")},
                 {std::string("watchMyLiquidationsForSymbols"), true},
                 {std::string("watchOHLCV"), true},
                 {std::string("watchOHLCVForSymbols"), true},
                 {std::string("watchOrders"), true},
                 {std::string("watchMyTrades"), true},
                 {std::string("watchPositions"), true},
                 {std::string("watchFundingRate"), true},
                 {std::string("watchFundingRates"), true},
                 {std::string("createOrderWs"), true},
                 {std::string("editOrderWs"), true},
                 {std::string("cancelOrderWs"), true},
                 {std::string("cancelOrdersWs"), true},
                 {std::string("cancelAllOrdersWs"), true},
             }},
            {std::string("urls"),
             ccxt::dict{
                 {std::string("api"),
                  ccxt::dict{
                      {std::string("ws"),
                       std::string("wss://ws.okx.com:8443/ws/v5")},
                  }},
                 {std::string("test"),
                  ccxt::dict{
                      {std::string("ws"),
                       std::string("wss://wspap.okx.com:8443/ws/v5")},
                  }},
             }},
            {std::string("options"),
             ccxt::dict{
                 {std::string("watchOrderBook"),
                  ccxt::dict{
                      {std::string("checksum"), true},
                      {std::string("depth"), std::string("books")},
                  }},
                 {std::string("watchBalance"), std::string("spot")},
                 {std::string("watchTicker"),
                  ccxt::dict{
                      {std::string("channel"), std::string("tickers")},
                  }},
                 {std::string("watchTickers"),
                  ccxt::dict{
                      {std::string("channel"), std::string("tickers")},
                  }},
                 {std::string("watchOrders"),
                  ccxt::dict{
                      {std::string("type"), std::string("ANY")},
                  }},
                 {std::string("watchMyTrades"),
                  ccxt::dict{
                      {std::string("type"), std::string("ANY")},
                  }},
                 {std::string("createOrderWs"),
                  ccxt::dict{
                      {std::string("op"), std::string("batch-orders")},
                  }},
                 {std::string("editOrderWs"),
                  ccxt::dict{
                      {std::string("op"), std::string("amend-order")},
                  }},
                 {std::string("ws"), ccxt::dict{}},
             }},
            {std::string("streaming"),
             ccxt::dict{
                 {std::string("ping"), std::string("ping")},
                 {std::string("keepAlive"), 18000},
             }},
        });
  }

  virtual std::any getUrl(std::any channel,
                          std::any access = std::string("public")) {
    // for context:
    // https://www.okx.com/help-center/changes-to-v5-api-websocket-subscription-parameter-and-url
    if (isTrue(isEqual(channel, std::any{}))) {
      throw ArgumentsRequired(toString(
          add(this->id, std::string(" getUrl() requires a channel argument"))));
    }
    std::any isSandbox = ::getValue(this->options, std::string("sandboxMode"));
    std::any sandboxSuffix = (isTrue((isEqual(isSandbox, true)))
                                  ? std::any(std::string("?brokerId=9999"))
                                  : std::any(std::string("")));
    std::any isBusiness = (isEqual(access, std::string("business")));
    std::any isPublic = (isEqual(access, std::string("public")));
    std::any url = ::getValue(::getValue(this->urls, std::string("api")),
                              std::string("ws"));
    if (isTrue(isTrue(isTrue(isBusiness) ||
                      isTrue((isGreaterThan(
                          getIndexOf(channel, std::string("candle")), -1)))) ||
               isTrue((isEqual(channel, std::string("orders-algo")))))) {
      return add(add(url, std::string("/business")), sandboxSuffix);
    } else if (isTrue(isPublic)) {
      return add(add(url, std::string("/public")), sandboxSuffix);
    }
    return add(add(url, std::string("/private")), sandboxSuffix);
  }

  virtual std::shared_future<std::any>
  subscribeMultiple(std::any access, std::any channel,
                    std::any symbols = std::any{},
                    std::any params = ccxt::dict{}) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 if (isTrue(isEqual(symbols, std::any{}))) {
                   symbols = this->symbols;
                 }
                 symbols = this->marketSymbols(symbols);
                 std::any url = this->getUrl(channel, access);
                 std::any messageHashes = ccxt::list{};
                 std::any args = ccxt::list{};
                 if (isTrue(isEqual(symbols, std::any{}))) {
                   throw ArgumentsRequired(toString(
                       add(this->id,
                           std::string(
                               " subscribeMultiple() symbols is required"))));
                 }
                 for (std::any i = 0; isLessThan(i, getArrayLength(symbols));
                      postFixIncrement(i)) {
                   if (isTrue(isEqual(symbols, std::any{}))) {
                     throw ArgumentsRequired(toString(
                         add(this->id,
                             std::string(
                                 " subscribeMultiple() symbols is required"))));
                   }
                   std::any marketId = this->marketId(::getValue(symbols, i));
                   std::any arg = ccxt::dict{
                       {std::string("channel"), channel},
                       {std::string("instId"), marketId},
                   };
                   arrayPush(args, this->extend(arg, params));
                   if (isTrue(isEqual(symbols, std::any{}))) {
                     throw ArgumentsRequired(toString(
                         add(this->id,
                             std::string(
                                 " subscribeMultiple() symbols is required"))));
                   }
                   arrayPush(messageHashes, add(add(channel, std::string("::")),
                                                ::getValue(symbols, i)));
                 }
                 std::any request = ccxt::dict{
                     {std::string("op"), std::string("subscribe")},
                     {std::string("args"), args},
                 };
                 return awaitValue(this->watchMultiple(url, messageHashes,
                                                       request, messageHashes));
               })
        .share();
  }

  virtual std::shared_future<std::any>
  subscribe(std::any access, std::any messageHash, std::any channel,
            std::any symbol, std::any params = ccxt::dict{}) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 std::any url = this->getUrl(channel, access);
                 std::any firstArgument = ccxt::dict{
                     {std::string("channel"), channel},
                 };
                 if (isTrue(!isEqual(symbol, std::any{}))) {
                   std::any market = this->market(symbol);
                   messageHash = add(
                       messageHash, add(std::string(":"),
                                        ::getValue(market, std::string("id"))));
                   ::setValue(firstArgument, std::string("instId"),
                              ::getValue(market, std::string("id")));
                 }
                 std::any request = ccxt::dict{
                     {std::string("op"), std::string("subscribe")},
                     {std::string("args"),
                      ccxt::list{this->deepExtend(firstArgument, params)}},
                 };
                 return awaitValue(
                     this->watch(url, messageHash, request, messageHash));
               })
        .share();
  }

  /**
   * @method
   * @name okx#watchTrades
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-trades-channel
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-all-trades-channel
   * @description get the list of most recent trades for a particular symbol
   * @param {string} symbol unified symbol of the market to fetch trades for
   * @param {int} [since] timestamp in ms of the earliest trade to fetch
   * @param {int} [limit] the maximum amount of trades to fetch
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [trade structures]{@link
   * https://docs.ccxt.com/?id=public-trades}
   */
  std::shared_future<std::any>
  watchTrades(std::any symbol, std::any since = std::any{},
              std::any limit = std::any{},
              std::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> std::any {
                        return awaitValue(this->watchTradesForSymbols(
                            ccxt::list{symbol}, since, limit, params));
                      })
        .share();
  }

  /**
   * @method
   * @name okx#watchTradesForSymbols
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-trades-channel
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-all-trades-channel
   * @description get the list of most recent trades for a particular symbol
   * @param {string} symbols
   * @param {int} [since] timestamp in ms of the earliest trade to fetch
   * @param {int} [limit] the maximum amount of trades to fetch
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.channel] the channel to subscribe to, trades by
   * default. Can be 'trades' and 'trades-all'
   * @returns {object[]} a list of [trade structures]{@link
   * https://docs.ccxt.com/?id=public-trades}
   */
  std::shared_future<std::any>
  watchTradesForSymbols(std::any symbols, std::any since = std::any{},
                        std::any limit = std::any{},
                        std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 std::any symbolsLength = getArrayLength(symbols);
                 if (isTrue(isEqual(symbolsLength, 0))) {
                   throw ArgumentsRequired(toString(
                       add(this->id,
                           std::string(" watchTradesForSymbols() requires a "
                                       "non-empty array of symbols"))));
                 }
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 symbols = this->marketSymbols(symbols);
                 std::any channel = std::any{};
                 std::any channelparamsVariable = this->handleOptionAndParams(
                     params, std::string("watchTrades"), std::string("channel"),
                     std::string("trades"));
                 channel = ::getValue(channelparamsVariable, 0);
                 params = ::getValue(channelparamsVariable, 1);
                 std::any topics = ccxt::list{};
                 std::any messageHashes = ccxt::list{};
                 for (std::any i = 0; isLessThan(i, getArrayLength(symbols));
                      postFixIncrement(i)) {
                   std::any symbol = ::getValue(symbols, i);
                   arrayPush(messageHashes,
                             add(add(channel, std::string(":")), symbol));
                   std::any marketId = this->marketId(symbol);
                   std::any topic = ccxt::dict{
                       {std::string("channel"), channel},
                       {std::string("instId"), marketId},
                   };
                   arrayPush(topics, topic);
                 }
                 std::any request = ccxt::dict{
                     {std::string("op"), std::string("subscribe")},
                     {std::string("args"), topics},
                 };
                 std::any access = std::string("public");
                 if (isTrue(isEqual(channel, std::string("trades-all")))) {
                   access = std::string("business");
                   awaitValue(this->authenticate(ccxt::dict{
                       {std::string("access"), access},
                   }));
                 }
                 std::any url = this->getUrl(channel, access);
                 std::any trades = awaitValue(this->watchMultiple(
                     url, messageHashes, request, messageHashes));
                 if (isTrue(this->newUpdates)) {
                   std::any first = this->safeValue(trades, 0);
                   std::any tradeSymbol =
                       this->safeString(first, std::string("symbol"));
                   limit = ::wsGetLimit(trades, tradeSymbol, limit);
                 }
                 return this->filterBySinceLimit(
                     trades, since, limit, std::string("timestamp"), true);
               })
        .share();
  }

  /**
   * @method
   * @name okx#unWatchTradesForSymbols
   * @description unWatches from the stream channel
   * @param {string[]} symbols
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.channel] the channel to subscribe to, trades by
   * default. Can be trades, trades-all
   * @returns {object[]} a list of [trade structures]{@link
   * https://docs.ccxt.com/?id=public-trades}
   */
  std::shared_future<std::any>
  unWatchTradesForSymbols(std::any symbols,
                          std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 symbols = this->marketSymbols(symbols, std::any{}, false);
                 std::any channel = std::any{};
                 std::any channelparamsVariable = this->handleOptionAndParams(
                     params, std::string("watchTrades"), std::string("channel"),
                     std::string("trades"));
                 channel = ::getValue(channelparamsVariable, 0);
                 params = ::getValue(channelparamsVariable, 1);
                 std::any topics = ccxt::list{};
                 std::any messageHashes = ccxt::list{};
                 for (std::any i = 0; isLessThan(i, getArrayLength(symbols));
                      postFixIncrement(i)) {
                   std::any symbol = ::getValue(symbols, i);
                   arrayPush(messageHashes,
                             add(add(add(std::string("unsubscribe:"), channel),
                                     std::string(":")),
                                 symbol));
                   std::any marketId = this->marketId(symbol);
                   std::any topic = ccxt::dict{
                       {std::string("channel"), channel},
                       {std::string("instId"), marketId},
                   };
                   arrayPush(topics, topic);
                 }
                 std::any request = ccxt::dict{
                     {std::string("op"), std::string("unsubscribe")},
                     {std::string("args"), topics},
                 };
                 std::any access = std::string("public");
                 if (isTrue(isEqual(channel, std::string("trades-all")))) {
                   access = std::string("business");
                   awaitValue(this->authenticate(ccxt::dict{
                       {std::string("access"), access},
                   }));
                 }
                 std::any url = this->getUrl(channel, access);
                 return awaitValue(this->watchMultiple(url, messageHashes,
                                                       request, messageHashes));
               })
        .share();
  }

  /**
   * @method
   * @name okx#unWatchTrades
   * @description unWatches from the stream channel
   * @param {string} symbol unified symbol of the market to fetch trades for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [trade structures]{@link
   * https://docs.ccxt.com/?id=public-trades}
   */
  std::shared_future<std::any>
  unWatchTrades(std::any symbol, std::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> std::any {
                        return awaitValue(this->unWatchTradesForSymbols(
                            ccxt::list{symbol}, params));
                      })
        .share();
  }

  virtual void handleTrades(std::any client, std::any message) {
    //
    //     {
    //         "arg": { channel: "trades", instId: "BTC-USDT" },
    //         "data": [
    //             {
    //                 "instId": "BTC-USDT",
    //                 "tradeId": "216970876",
    //                 "px": "31684.5",
    //                 "sz": "0.00001186",
    //                 "side": "buy",
    //                 "ts": "1626531038288"
    //             }
    //         ]
    //     }
    //     {
    //         "arg": {
    //             "channel": "trades-all",
    //             "instId": "BTC-USDT"
    //         },
    //         "data": [
    //             {
    //                 "instId": "BTC-USDT",
    //                 "tradeId": "130639474",
    //                 "px": "42219.9",
    //                 "sz": "0.12060306",
    //                 "side": "buy",
    //                 "source": "0",
    //                 "ts": "1630048897897"
    //             }
    //         ]
    //     }
    //
    std::any arg = this->safeValue(message, std::string("arg"), ccxt::dict{});
    std::any channel = this->safeString(arg, std::string("channel"));
    std::any marketId = this->safeString(arg, std::string("instId"));
    std::any symbol = this->safeSymbol(marketId);
    std::any data = this->safeValue(message, std::string("data"), ccxt::list{});
    std::any tradesLimit =
        this->safeInteger(this->options, std::string("tradesLimit"), 1000);
    for (std::any i = 0; isLessThan(i, getArrayLength(data));
         postFixIncrement(i)) {
      std::any trade = this->parseTrade(::getValue(data, i));
      std::any messageHash = add(add(channel, std::string(":")), symbol);
      std::any stored = this->safeValue(this->trades, symbol);
      if (isTrue(isEqual(stored, std::any{}))) {
        stored = ccxt::ws::ArrayCache(tradesLimit);
        ::setValue(this->trades, symbol, stored);
      }
      ::wsAppend(stored, trade);
      this->resolve(stored, messageHash);
    }
  }

  /**
   * @method
   * @name okx#watchFundingRate
   * @description watch the current funding rate
   * @see
   * https://www.okx.com/docs-v5/en/#public-data-websocket-funding-rate-channel
   * @param {string} symbol unified market symbol
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [funding rate structure]{@link
   * https://docs.ccxt.com/?id=funding-rate-structure}
   */
  std::shared_future<std::any>
  watchFundingRate(std::any symbol, std::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> std::any {
                        symbol = this->symbol(symbol);
                        std::any fr = awaitValue(this->watchFundingRates(
                            ccxt::list{symbol}, params));
                        return ::getValue(fr, symbol);
                      })
        .share();
  }

  /**
   * @method
   * @name okx#watchFundingRates
   * @description watch the funding rate for multiple markets
   * @see
   * https://www.okx.com/docs-v5/en/#public-data-websocket-funding-rate-channel
   * @param {string[]} symbols a list of unified market symbols
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a dictionary of [funding rates structures]{@link
   * https://docs.ccxt.com/?id=funding-rate-structure}, indexed by market
   * symbols
   */
  std::shared_future<std::any>
  watchFundingRates(std::any symbols = std::any{},
                    std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(symbols, std::any{}))) {
                   throw ArgumentsRequired(toString(add(
                       this->id, std::string(" watchFundingRates() requires an "
                                             "array of symbols"))));
                 }
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 symbols = this->marketSymbols(symbols);
                 std::any channel = std::string("funding-rate");
                 std::any topics = ccxt::list{};
                 std::any messageHashes = ccxt::list{};
                 for (std::any i = 0; isLessThan(i, getArrayLength(symbols));
                      postFixIncrement(i)) {
                   std::any symbol = ::getValue(symbols, i);
                   arrayPush(messageHashes,
                             add(add(channel, std::string(":")), symbol));
                   std::any marketId = this->marketId(symbol);
                   std::any topic = ccxt::dict{
                       {std::string("channel"), channel},
                       {std::string("instId"), marketId},
                   };
                   arrayPush(topics, topic);
                 }
                 std::any request = ccxt::dict{
                     {std::string("op"), std::string("subscribe")},
                     {std::string("args"), topics},
                 };
                 std::any url = this->getUrl(channel, std::string("public"));
                 std::any fundingRate = awaitValue(this->watchMultiple(
                     url, messageHashes, request, messageHashes));
                 if (isTrue(this->newUpdates)) {
                   std::any symbol =
                       this->safeString(fundingRate, std::string("symbol"));
                   std::any result = ccxt::dict{};
                   if (isTrue(!isEqual(symbol, std::any{}))) {
                     ::setValue(result, symbol, fundingRate);
                   }
                   return result;
                 }
                 return this->filterByArray(this->fundingRates,
                                            std::string("symbol"), symbols);
               })
        .share();
  }

  virtual void handleFundingRate(std::any client, std::any message) {
    //
    // "data":[
    //     {
    //        "fundingRate":"0.0001875391284828",
    //        "fundingTime":"1700726400000",
    //        "instId":"BTC-USD-SWAP",
    //        "instType":"SWAP",
    //        "method": "next_period",
    //        "maxFundingRate":"0.00375",
    //        "minFundingRate":"-0.00375",
    //        "nextFundingRate":"0.0002608059239328",
    //        "nextFundingTime":"1700755200000",
    //        "premium": "0.0001233824646391",
    //        "settFundingRate":"0.0001699799259033",
    //        "settState":"settled",
    //        "ts":"1700724675402"
    //     }
    // ]
    //
    std::any data = this->safeList(message, std::string("data"), ccxt::list{});
    for (std::any i = 0; isLessThan(i, getArrayLength(data));
         postFixIncrement(i)) {
      std::any rawfr = ::getValue(data, i);
      std::any fundingRate = this->parseFundingRate(rawfr);
      std::any symbol = ::getValue(fundingRate, std::string("symbol"));
      if (isTrue(!isEqual(symbol, std::any{}))) {
        ::setValue(this->fundingRates, symbol, fundingRate);
      }
      this->resolve(fundingRate,
                    add(add(std::string("funding-rate"), std::string(":")),
                        ::getValue(fundingRate, std::string("symbol"))));
    }
  }

  /**
   * @method
   * @name okx#watchTicker
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-tickers-channel
   * @description watches a price ticker, a statistical calculation with the
   * information calculated over the past 24 hours for a specific market
   * @param {string} symbol unified symbol of the market to fetch the ticker for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.channel] the channel to subscribe to, tickers by
   * default. Can be tickers, sprd-tickers, index-tickers, block-tickers
   * @returns {object} a [ticker structure]{@link
   * https://docs.ccxt.com/?id=ticker-structure}
   */
  std::shared_future<std::any>
  watchTicker(std::any symbol, std::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> std::any {
                        std::any channel = std::any{};
                        std::any channelparamsVariable =
                            this->handleOptionAndParams(
                                params, std::string("watchTicker"),
                                std::string("channel"), std::string("tickers"));
                        channel = ::getValue(channelparamsVariable, 0);
                        params = ::getValue(channelparamsVariable, 1);
                        ::setValue(params, std::string("channel"), channel);
                        std::any market = this->market(symbol);
                        symbol = ::getValue(market, std::string("symbol"));
                        std::any ticker = awaitValue(
                            this->watchTickers(ccxt::list{symbol}, params));
                        return this->safeValue(ticker, symbol);
                      })
        .share();
  }

  /**
   * @method
   * @name okx#unWatchTicker
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-tickers-channel
   * @description unWatches a price ticker, a statistical calculation with the
   * information calculated over the past 24 hours for a specific market
   * @param {string} symbol unified symbol of the market to fetch the ticker for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.channel] the channel to subscribe to, tickers by
   * default. Can be tickers, sprd-tickers, index-tickers, block-tickers
   * @returns {object} a [ticker structure]{@link
   * https://docs.ccxt.com/?id=ticker-structure}
   */
  std::shared_future<std::any>
  unWatchTicker(std::any symbol, std::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> std::any {
                        return awaitValue(
                            this->unWatchTickers(ccxt::list{symbol}, params));
                      })
        .share();
  }

  /**
   * @method
   * @name okx#watchTickers
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-tickers-channel
   * @description watches a price ticker, a statistical calculation with the
   * information calculated over the past 24 hours for all markets of a specific
   * list
   * @param {string[]} [symbols] unified symbol of the market to fetch the
   * ticker for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.channel] the channel to subscribe to, tickers by
   * default. Can be tickers, sprd-tickers, index-tickers, block-tickers
   * @returns {object} a [ticker structure]{@link
   * https://docs.ccxt.com/?id=ticker-structure}
   */
  std::shared_future<std::any>
  watchTickers(std::any symbols = std::any{},
               std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 symbols = this->marketSymbols(symbols, std::any{}, false);
                 std::any channel = std::any{};
                 std::any channelparamsVariable = this->handleOptionAndParams(
                     params, std::string("watchTickers"),
                     std::string("channel"), std::string("tickers"));
                 channel = ::getValue(channelparamsVariable, 0);
                 params = ::getValue(channelparamsVariable, 1);
                 std::any newTickers = awaitValue(this->subscribeMultiple(
                     std::string("public"), channel, symbols, params));
                 if (isTrue(this->newUpdates)) {
                   return newTickers;
                 }
                 return this->filterByArray(this->tickers,
                                            std::string("symbol"), symbols);
               })
        .share();
  }

  /**
   * @method
   * @name okx#watchMarkPrice
   * @see
   * https://www.okx.com/docs-v5/en/#public-data-websocket-mark-price-channel
   * @description watches a mark price
   * @param {string} symbol unified symbol of the market to fetch the ticker for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.channel] the channel to subscribe to, tickers by
   * default. Can be tickers, sprd-tickers, index-tickers, block-tickers
   * @returns {object} a [ticker structure]{@link
   * https://docs.ccxt.com/?id=ticker-structure}
   */
  std::shared_future<std::any>
  watchMarkPrice(std::any symbol, std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 std::any channel = std::any{};
                 std::any channelparamsVariable = this->handleOptionAndParams(
                     params, std::string("watchMarkPrice"),
                     std::string("channel"), std::string("mark-price"));
                 channel = ::getValue(channelparamsVariable, 0);
                 params = ::getValue(channelparamsVariable, 1);
                 ::setValue(params, std::string("channel"), channel);
                 std::any market = this->market(symbol);
                 symbol = ::getValue(market, std::string("symbol"));
                 std::any ticker = awaitValue(
                     this->watchMarkPrices(ccxt::list{symbol}, params));
                 return ::getValue(ticker, symbol);
               })
        .share();
  }

  /**
   * @method
   * @name okx#watchMarkPrices
   * @see
   * https://www.okx.com/docs-v5/en/#public-data-websocket-mark-price-channel
   * @description watches mark prices
   * @param {string[]} [symbols] unified symbol of the market to fetch the
   * ticker for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.channel] the channel to subscribe to, tickers by
   * default. Can be tickers, sprd-tickers, index-tickers, block-tickers
   * @returns {object} a [ticker structure]{@link
   * https://docs.ccxt.com/?id=ticker-structure}
   */
  std::shared_future<std::any>
  watchMarkPrices(std::any symbols = std::any{},
                  std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 symbols = this->marketSymbols(symbols, std::any{}, false);
                 std::any channel = std::any{};
                 std::any channelparamsVariable = this->handleOptionAndParams(
                     params, std::string("watchMarkPrices"),
                     std::string("channel"), std::string("mark-price"));
                 channel = ::getValue(channelparamsVariable, 0);
                 params = ::getValue(channelparamsVariable, 1);
                 std::any newTickers = awaitValue(this->subscribeMultiple(
                     std::string("public"), channel, symbols, params));
                 if (isTrue(this->newUpdates)) {
                   return newTickers;
                 }
                 return this->filterByArray(this->tickers,
                                            std::string("symbol"), symbols);
               })
        .share();
  }

  /**
   * @method
   * @name okx#unWatchTickers
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-tickers-channel
   * @description unWatches a price ticker, a statistical calculation with the
   * information calculated over the past 24 hours for all markets of a specific
   * list
   * @param {string[]} [symbols] unified symbol of the market to fetch the
   * ticker for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.channel] the channel to subscribe to, tickers by
   * default. Can be tickers, sprd-tickers, index-tickers, block-tickers
   * @returns {object} a [ticker structure]{@link
   * https://docs.ccxt.com/?id=ticker-structure}
   */
  std::shared_future<std::any>
  unWatchTickers(std::any symbols = std::any{},
                 std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 symbols = this->marketSymbols(symbols, std::any{}, false);
                 std::any channel = std::any{};
                 std::any channelparamsVariable = this->handleOptionAndParams(
                     params, std::string("watchTickers"),
                     std::string("channel"), std::string("tickers"));
                 channel = ::getValue(channelparamsVariable, 0);
                 params = ::getValue(channelparamsVariable, 1);
                 std::any topics = ccxt::list{};
                 std::any messageHashes = ccxt::list{};
                 for (std::any i = 0; isLessThan(i, getArrayLength(symbols));
                      postFixIncrement(i)) {
                   std::any symbol = ::getValue(symbols, i);
                   arrayPush(messageHashes,
                             add(std::string("unsubscribe:ticker:"), symbol));
                   std::any marketId = this->marketId(symbol);
                   std::any topic = ccxt::dict{
                       {std::string("channel"), channel},
                       {std::string("instId"), marketId},
                   };
                   arrayPush(topics, topic);
                 }
                 std::any request = ccxt::dict{
                     {std::string("op"), std::string("unsubscribe")},
                     {std::string("args"), topics},
                 };
                 std::any url = this->getUrl(channel, std::string("public"));
                 return awaitValue(this->watchMultiple(url, messageHashes,
                                                       request, messageHashes));
               })
        .share();
  }

  virtual void handleTicker(std::any client, std::any message) {
    //
    //     {
    //         "arg": { channel: "tickers", instId: "BTC-USDT" },
    //         "data": [
    //             {
    //                 "instType": "SPOT",
    //                 "instId": "BTC-USDT",
    //                 "last": "31500.1",
    //                 "lastSz": "0.00001754",
    //                 "askPx": "31500.1",
    //                 "askSz": "0.00998144",
    //                 "bidPx": "31500",
    //                 "bidSz": "3.05652439",
    //                 "open24h": "31697",
    //                 "high24h": "32248",
    //                 "low24h": "31165.6",
    //                 "sodUtc0": "31385.5",
    //                 "sodUtc8": "32134.9",
    //                 "volCcy24h": "503403597.38138519",
    //                 "vol24h": "15937.10781721",
    //                 "ts": "1626526618762"
    //             }
    //         ]
    //     }
    //
    this->handleBidAsk(client, message);
    std::any arg = this->safeValue(message, std::string("arg"), ccxt::dict{});
    std::any marketId = this->safeString(arg, std::string("instId"));
    std::any market = this->safeMarket(marketId, std::any{}, std::string("-"));
    std::any symbol = ::getValue(market, std::string("symbol"));
    std::any channel = this->safeString(arg, std::string("channel"));
    std::any data = this->safeValue(message, std::string("data"), ccxt::list{});
    std::any newTickers = ccxt::dict{};
    for (std::any i = 0; isLessThan(i, getArrayLength(data));
         postFixIncrement(i)) {
      std::any ticker = this->parseTicker(::getValue(data, i));
      ::setValue(this->tickers, symbol, ticker);
      ::setValue(newTickers, symbol, ticker);
    }
    std::any messageHash = add(add(channel, std::string("::")), symbol);
    this->resolve(newTickers, messageHash);
  }

  /**
   * @method
   * @name okx#watchBidsAsks
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-tickers-channel
   * @description watches best bid & ask for symbols
   * @param {string[]} symbols unified symbol of the market to fetch the ticker
   * for
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [ticker structure]{@link
   * https://docs.ccxt.com/?id=ticker-structure}
   */
  std::shared_future<std::any>
  watchBidsAsks(std::any symbols = std::any{},
                std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 symbols = this->marketSymbols(symbols, std::any{}, false);
                 std::any channel = std::any{};
                 std::any channelparamsVariable = this->handleOptionAndParams(
                     params, std::string("watchBidsAsks"),
                     std::string("channel"), std::string("tickers"));
                 channel = ::getValue(channelparamsVariable, 0);
                 params = ::getValue(channelparamsVariable, 1);
                 std::any url = this->getUrl(channel, std::string("public"));
                 std::any messageHashes = ccxt::list{};
                 std::any args = ccxt::list{};
                 for (std::any i = 0; isLessThan(i, getArrayLength(symbols));
                      postFixIncrement(i)) {
                   std::any marketId = this->marketId(::getValue(symbols, i));
                   std::any arg = ccxt::dict{
                       {std::string("channel"), channel},
                       {std::string("instId"), marketId},
                   };
                   arrayPush(args, this->extend(arg, params));
                   arrayPush(messageHashes, add(std::string("bidask::"),
                                                ::getValue(symbols, i)));
                 }
                 std::any request = ccxt::dict{
                     {std::string("op"), std::string("subscribe")},
                     {std::string("args"), args},
                 };
                 std::any newTickers = awaitValue(this->watchMultiple(
                     url, messageHashes, request, messageHashes));
                 if (isTrue(this->newUpdates)) {
                   std::any tickers = ccxt::dict{};
                   ::setValue(tickers,
                              ::getValue(newTickers, std::string("symbol")),
                              newTickers);
                   return tickers;
                 }
                 return this->filterByArray(this->bidsasks,
                                            std::string("symbol"), symbols);
               })
        .share();
  }

  virtual void handleBidAsk(std::any client, std::any message) {
    //
    //     {
    //         "arg": { channel: "tickers", instId: "BTC-USDT" },
    //         "data": [
    //             {
    //                 "instType": "SPOT",
    //                 "instId": "BTC-USDT",
    //                 "last": "31500.1",
    //                 "lastSz": "0.00001754",
    //                 "askPx": "31500.1",
    //                 "askSz": "0.00998144",
    //                 "bidPx": "31500",
    //                 "bidSz": "3.05652439",
    //                 "open24h": "31697",
    //                 "high24h": "32248",
    //                 "low24h": "31165.6",
    //                 "sodUtc0": "31385.5",
    //                 "sodUtc8": "32134.9",
    //                 "volCcy24h": "503403597.38138519",
    //                 "vol24h": "15937.10781721",
    //                 "ts": "1626526618762"
    //             }
    //         ]
    //     }
    //
    std::any data = this->safeList(message, std::string("data"), ccxt::list{});
    std::any ticker = this->safeDict(data, 0, ccxt::dict{});
    std::any parsedTicker = this->parseWsBidAsk(ticker);
    std::any symbol = ::getValue(parsedTicker, std::string("symbol"));
    if (isTrue(!isEqual(symbol, std::any{}))) {
      ::setValue(this->bidsasks, symbol, parsedTicker);
    }
    std::any messageHash = add(std::string("bidask::"), symbol);
    this->resolve(parsedTicker, messageHash);
  }

  virtual std::any parseWsBidAsk(std::any ticker,
                                 std::any market = std::any{}) {
    std::any marketId = this->safeString(ticker, std::string("instId"));
    market = this->safeMarket(marketId, market);
    std::any symbol = this->safeString(market, std::string("symbol"));
    std::any timestamp = this->safeInteger(ticker, std::string("ts"));
    return this->safeTicker(
        ccxt::dict{
            {std::string("symbol"), symbol},
            {std::string("timestamp"), timestamp},
            {std::string("datetime"), this->iso8601(timestamp)},
            {std::string("ask"),
             this->safeString(ticker, std::string("askPx"))},
            {std::string("askVolume"),
             this->safeString(ticker, std::string("askSz"))},
            {std::string("bid"),
             this->safeString(ticker, std::string("bidPx"))},
            {std::string("bidVolume"),
             this->safeString(ticker, std::string("bidSz"))},
            {std::string("info"), ticker},
        },
        market);
  }

  /**
   * @method
   * @name okx#watchLiquidationsForSymbols
   * @description watch the public liquidations of a trading pair
   * @see
   * https://www.okx.com/docs-v5/en/#public-data-websocket-liquidation-orders-channel
   * @param {string} symbols
   * @param {int} [since] the earliest time in ms to fetch liquidations for
   * @param {int} [limit] the maximum number of liquidation structures to
   * retrieve
   * @param {object} [params] exchange specific parameters for the okx api
   * endpoint
   * @returns {object} an array of [liquidation structures]{@link
   * https://github.com/ccxt/ccxt/wiki/Manual#liquidation-structure}
   */
  std::shared_future<std::any>
  watchLiquidationsForSymbols(std::any symbols, std::any since = std::any{},
                              std::any limit = std::any{},
                              std::any params = ccxt::dict{}) override {
    return std::
        async(std::launch::deferred,
              [=]() mutable -> std::any {
                if (isTrue(isEqual(this->markets, std::any{}))) {
                  awaitValue(this->loadMarkets());
                }
                symbols = this->marketSymbols(symbols, std::any{}, true, true);
                std::any messageHash = std::string("liquidations");
                std::any messageHashes = ccxt::list{};
                if (isTrue(!isEqual(symbols, std::any{}))) {
                  for (std::any i = 0; isLessThan(i, getArrayLength(symbols));
                       postFixIncrement(i)) {
                    std::any symbol = ::getValue(symbols, i);
                    arrayPush(messageHashes,
                              add(add(messageHash, std::string("::")), symbol));
                  }
                } else {
                  arrayPush(messageHashes, messageHash);
                }
                std::any market = this->getMarketFromSymbols(symbols);
                std::any type = std::any{};
                std::any typeparamsVariable = this->handleMarketTypeAndParams(
                    std::string("watchliquidationsForSymbols"), market, params);
                type = ::getValue(typeparamsVariable, 0);
                params = ::getValue(typeparamsVariable, 1);
                std::any channel = std::string("liquidation-orders");
                if (isTrue(isEqual(type, std::string("spot")))) {
                  type = std::string("SWAP");
                } else if (isTrue(isEqual(type, std::string("future")))) {
                  type = std::string("futures");
                }
                if (isTrue(isEqual(type, std::any{}))) {
                  throw ArgumentsRequired(toString(add(
                      this->id,
                      std::string(
                          " watchLiquidationsForSymbols() type is required"))));
                }
                std::any uppercaseType = toUpperCase(type);
                std::any request = ccxt::dict{
                    {std::string("op"), std::string("subscribe")},
                    {std::string("args"),
                     ccxt::list{ccxt::dict{
                         {std::string("channel"), channel},
                         {std::string("instType"), uppercaseType},
                     }}},
                };
                std::any url = this->getUrl(channel, std::string("public"));
                std::any newLiquidations = awaitValue(this->watchMultiple(
                    url, messageHashes, request, messageHashes));
                if (isTrue(this->newUpdates)) {
                  return newLiquidations;
                }
                return this->filterBySymbolsSinceLimit(
                    this->liquidations, symbols, since, limit, true);
              })
            .share();
  }

  virtual void handleLiquidation(std::any client, std::any message) {
    //
    //    {
    //        "arg": {
    //            "channel": "liquidation-orders",
    //            "instType": "SWAP"
    //        },
    //        "data": [
    //            {
    //                "details": [
    //                    {
    //                        "bkLoss": "0",
    //                        "bkPx": "0.007831",
    //                        "ccy": "",
    //                        "posSide": "short",
    //                        "side": "buy",
    //                        "sz": "13",
    //                        "ts": "1692266434010"
    //                    }
    //                ],
    //                "instFamily": "IOST-USDT",
    //                "instId": "IOST-USDT-SWAP",
    //                "instType": "SWAP",
    //                "uly": "IOST-USDT"
    //            }
    //        ]
    //    }
    //
    std::any rawLiquidations =
        this->safeList(message, std::string("data"), ccxt::list{});
    for (std::any i = 0; isLessThan(i, getArrayLength(rawLiquidations));
         postFixIncrement(i)) {
      std::any rawLiquidation = ::getValue(rawLiquidations, i);
      std::any liquidation = this->parseWsLiquidation(rawLiquidation);
      std::any symbol = this->safeString(liquidation, std::string("symbol"));
      if (isTrue(isEqual(this->liquidations, std::any{}))) {
        std::any limit = this->safeInteger(
            this->options, std::string("liquidationsLimit"), 1000);
        this->liquidations = ccxt::ws::ArrayCache(limit);
      }
      std::any cache = this->liquidations;
      ::wsAppend(cache, liquidation);
      this->resolve(ccxt::list{liquidation}, std::string("liquidations"));
      this->resolve(ccxt::list{liquidation},
                    add(std::string("liquidations::"), symbol));
    }
  }

  /**
   * @method
   * @name okx#watchMyLiquidationsForSymbols
   * @description watch the private liquidations of a trading pair
   * @see
   * https://www.okx.com/docs-v5/en/#trading-account-websocket-balance-and-position-channel
   * @param {string[]} symbols
   * @param {int} [since] the earliest time in ms to fetch liquidations for
   * @param {int} [limit] the maximum number of liquidation structures to
   * retrieve
   * @param {object} [params] exchange specific parameters for the okx api
   * endpoint
   * @returns {object} an array of [liquidation structures]{@link
   * https://github.com/ccxt/ccxt/wiki/Manual#liquidation-structure}
   */
  std::shared_future<std::any>
  watchMyLiquidationsForSymbols(std::any symbols, std::any since = std::any{},
                                std::any limit = std::any{},
                                std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 std::any isTrigger =
                     this->safeValue2(params, std::string("stop"),
                                      std::string("trigger"), false);
                 params =
                     this->omit(params, ccxt::list{std::string("stop"),
                                                   std::string("trigger")});
                 std::any accessType = (isTrue((isEqual(isTrigger, true)))
                                            ? std::any(std::string("business"))
                                            : std::any(std::string("private")));
                 awaitValue(this->authenticate(ccxt::dict{
                     {std::string("access"), accessType},
                 }));
                 symbols = this->marketSymbols(symbols, std::any{}, true, true);
                 std::any messageHash = std::string("myLiquidations");
                 std::any messageHashes = ccxt::list{};
                 if (isTrue(!isEqual(symbols, std::any{}))) {
                   for (std::any i = 0; isLessThan(i, getArrayLength(symbols));
                        postFixIncrement(i)) {
                     std::any symbol = ::getValue(symbols, i);
                     arrayPush(
                         messageHashes,
                         add(add(messageHash, std::string("::")), symbol));
                   }
                 } else {
                   arrayPush(messageHashes, messageHash);
                 }
                 std::any channel = std::string("balance_and_position");
                 std::any request = ccxt::dict{
                     {std::string("op"), std::string("subscribe")},
                     {std::string("args"),
                      ccxt::list{ccxt::dict{
                          {std::string("channel"), channel},
                      }}},
                 };
                 std::any url = this->getUrl(channel, std::string("private"));
                 std::any newLiquidations = awaitValue(this->watchMultiple(
                     url, messageHashes, this->deepExtend(request, params),
                     messageHashes));
                 if (isTrue(this->newUpdates)) {
                   return newLiquidations;
                 }
                 return this->filterBySymbolsSinceLimit(
                     this->liquidations, symbols, since, limit, true);
               })
        .share();
  }

  virtual void handleMyLiquidation(std::any client, std::any message) {
    //
    //    {
    //        "arg": {
    //            "channel": "balance_and_position",
    //            "uid": "77982378738415879"
    //        },
    //        "data": [{
    //            "pTime": "1597026383085",
    //            "eventType": "snapshot",
    //            "balData": [{
    //                "ccy": "BTC",
    //                "cashBal": "1",
    //                "uTime": "1597026383085"
    //            }],
    //            "posData": [{
    //                "posId": "1111111111",
    //                "tradeId": "2",
    //                "instId": "BTC-USD-191018",
    //                "instType": "FUTURES",
    //                "mgnMode": "cross",
    //                "posSide": "long",
    //                "pos": "10",
    //                "ccy": "BTC",
    //                "posCcy": "",
    //                "avgPx": "3320",
    //                "uTIme": "1597026383085"
    //            }],
    //            "trades": [{
    //                "instId": "BTC-USD-191018",
    //                "tradeId": "2",
    //            }]
    //        }]
    //    }
    //
    std::any rawLiquidations =
        this->safeList(message, std::string("data"), ccxt::list{});
    for (std::any i = 0; isLessThan(i, getArrayLength(rawLiquidations));
         postFixIncrement(i)) {
      std::any rawLiquidation = ::getValue(rawLiquidations, i);
      std::any eventType =
          this->safeString(rawLiquidation, std::string("eventType"));
      if (isTrue(!isEqual(eventType, std::string("liquidation")))) {
        return;
      }
      std::any liquidation = this->parseWsMyLiquidation(rawLiquidation);
      std::any symbol = this->safeString(liquidation, std::string("symbol"));
      if (isTrue(isEqual(this->liquidations, std::any{}))) {
        std::any limit = this->safeInteger(
            this->options, std::string("liquidationsLimit"), 1000);
        this->liquidations = ccxt::ws::ArrayCache(limit);
      }
      std::any cache = this->liquidations;
      ::wsAppend(cache, liquidation);
      this->resolve(ccxt::list{liquidation}, std::string("myLiquidations"));
      this->resolve(ccxt::list{liquidation},
                    add(std::string("myLiquidations::"), symbol));
    }
  }

  virtual std::any parseWsMyLiquidation(std::any liquidation,
                                        std::any market = std::any{}) {
    //
    //    {
    //        "pTime": "1597026383085",
    //        "eventType": "snapshot",
    //        "balData": [{
    //            "ccy": "BTC",
    //            "cashBal": "1",
    //            "uTime": "1597026383085"
    //        }],
    //        "posData": [{
    //            "posId": "1111111111",
    //            "tradeId": "2",
    //            "instId": "BTC-USD-191018",
    //            "instType": "FUTURES",
    //            "mgnMode": "cross",
    //            "posSide": "long",
    //            "pos": "10",
    //            "ccy": "BTC",
    //            "posCcy": "",
    //            "avgPx": "3320",
    //            "uTIme": "1597026383085"
    //        }],
    //        "trades": [{
    //            "instId": "BTC-USD-191018",
    //            "tradeId": "2",
    //        }]
    //    }
    //
    std::any posData =
        this->safeList(liquidation, std::string("posData"), ccxt::list{});
    std::any firstPosData = this->safeDict(posData, 0, ccxt::dict{});
    std::any marketId = this->safeString(firstPosData, std::string("instId"));
    market = this->safeMarket(marketId, market);
    std::any timestamp = this->safeInteger(firstPosData, std::string("uTIme"));
    return this->safeLiquidation(ccxt::dict{
        {std::string("info"), liquidation},
        {std::string("symbol"), this->safeSymbol(marketId, market)},
        {std::string("contracts"),
         this->safeNumber(firstPosData, std::string("pos"))},
        {std::string("contractSize"),
         this->safeNumber(market, std::string("contractSize"))},
        {std::string("price"),
         this->safeNumber(liquidation, std::string("avgPx"))},
        {std::string("baseValue"), std::any{}},
        {std::string("quoteValue"), std::any{}},
        {std::string("timestamp"), timestamp},
        {std::string("datetime"), this->iso8601(timestamp)},
    });
  }

  virtual std::any parseWsLiquidation(std::any liquidation,
                                      std::any market = std::any{}) {
    //
    // public liquidation
    //    {
    //        "details": [
    //            {
    //                "bkLoss": "0",
    //                "bkPx": "0.007831",
    //                "ccy": "",
    //                "posSide": "short",
    //                "side": "buy",
    //                "sz": "13",
    //                "ts": "1692266434010"
    //            }
    //        ],
    //        "instFamily": "IOST-USDT",
    //        "instId": "IOST-USDT-SWAP",
    //        "instType": "SWAP",
    //        "uly": "IOST-USDT"
    //    }
    //
    std::any details =
        this->safeList(liquidation, std::string("details"), ccxt::list{});
    std::any liquidationDetails = this->safeDict(details, 0, ccxt::dict{});
    std::any marketId = this->safeString(liquidation, std::string("instId"));
    market = this->safeMarket(marketId, market);
    std::any timestamp =
        this->safeInteger(liquidationDetails, std::string("ts"));
    return this->safeLiquidation(ccxt::dict{
        {std::string("info"), liquidation},
        {std::string("symbol"), this->safeSymbol(marketId, market)},
        {std::string("contracts"),
         this->safeNumber(liquidationDetails, std::string("sz"))},
        {std::string("contractSize"),
         this->safeNumber(market, std::string("contractSize"))},
        {std::string("price"),
         this->safeNumber(liquidationDetails, std::string("bkPx"))},
        {std::string("side"),
         this->safeString(liquidationDetails, std::string("side"))},
        {std::string("baseValue"), std::any{}},
        {std::string("quoteValue"), std::any{}},
        {std::string("timestamp"), timestamp},
        {std::string("datetime"), this->iso8601(timestamp)},
    });
  }

  /**
   * @method
   * @name okx#watchOHLCV
   * @description watches historical candlestick data containing the open, high,
   * low, and close price, and the volume of a market
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-candlesticks-channel
   * @param {string} symbol unified symbol of the market to fetch OHLCV data for
   * @param {string} timeframe the length of time each candle represents
   * @param {int} [since] timestamp in ms of the earliest candle to fetch
   * @param {int} [limit] the maximum amount of candles to fetch
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {int[][]} A list of candles ordered as timestamp, open, high, low,
   * close, volume
   */
  std::shared_future<std::any>
  watchOHLCV(std::any symbol, std::any timeframe = std::string("1m"),
             std::any since = std::any{}, std::any limit = std::any{},
             std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 symbol = this->symbol(symbol);
                 std::any interval =
                     this->safeString(this->timeframes, timeframe, timeframe);
                 std::any name = add(std::string("candle"), interval);
                 std::any ohlcv = awaitValue(this->subscribe(
                     std::string("public"), name, name, symbol, params));
                 if (isTrue(this->newUpdates)) {
                   limit = ::wsGetLimit(ohlcv, symbol, limit);
                 }
                 return this->filterBySinceLimit(ohlcv, since, limit, 0, true);
               })
        .share();
  }

  /**
   * @method
   * @name okx#unWatchOHLCV
   * @description watches historical candlestick data containing the open, high,
   * low, and close price, and the volume of a market
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-candlesticks-channel
   * @param {string} symbol unified symbol of the market to fetch OHLCV data for
   * @param {string} timeframe the length of time each candle represents
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {int[][]} A list of candles ordered as timestamp, open, high, low,
   * close, volume
   */
  std::shared_future<std::any>
  unWatchOHLCV(std::any symbol, std::any timeframe = std::string("1m"),
               std::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> std::any {
                        return awaitValue(this->unWatchOHLCVForSymbols(
                            ccxt::list{ccxt::list{symbol, timeframe}}, params));
                      })
        .share();
  }

  /**
   * @method
   * @name okx#watchOHLCVForSymbols
   * @description watches historical candlestick data containing the open, high,
   * low, and close price, and the volume of a market
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-candlesticks-channel
   * @param {string[][]} symbolsAndTimeframes array of arrays containing unified
   * symbols and timeframes to fetch OHLCV data for, example [['BTC/USDT',
   * '1m'], ['LTC/USDT', '5m']]
   * @param {int} [since] timestamp in ms of the earliest candle to fetch
   * @param {int} [limit] the maximum amount of candles to fetch
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {int[][]} A list of candles ordered as timestamp, open, high, low,
   * close, volume
   */
  std::shared_future<std::any>
  watchOHLCVForSymbols(std::any symbolsAndTimeframes,
                       std::any since = std::any{}, std::any limit = std::any{},
                       std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 std::any symbolsLength = getArrayLength(symbolsAndTimeframes);
                 if (isTrue(isTrue(isEqual(symbolsLength, 0)) ||
                            !isTrue(isArray(
                                ::getValue(symbolsAndTimeframes, 0))))) {
                   throw ArgumentsRequired(toString(
                       add(this->id,
                           std::string(
                               " watchOHLCVForSymbols() requires a an array of "
                               "symbols and timeframes, like  [['BTC/USDT', "
                               "'1m'], ['LTC/USDT', '5m']]"))));
                 }
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 std::any topics = ccxt::list{};
                 std::any messageHashes = ccxt::list{};
                 for (std::any i = 0;
                      isLessThan(i, getArrayLength(symbolsAndTimeframes));
                      postFixIncrement(i)) {
                   std::any symbolAndTimeframe =
                       ::getValue(symbolsAndTimeframes, i);
                   std::any sym = ::getValue(symbolAndTimeframe, 0);
                   std::any tf = ::getValue(symbolAndTimeframe, 1);
                   std::any marketId = this->marketId(sym);
                   std::any interval =
                       this->safeString(this->timeframes, tf, tf);
                   std::any channel = add(std::string("candle"), interval);
                   std::any topic = ccxt::dict{
                       {std::string("channel"), channel},
                       {std::string("instId"), marketId},
                   };
                   arrayPush(topics, topic);
                   arrayPush(messageHashes,
                             add(add(add(std::string("multi:"), channel),
                                     std::string(":")),
                                 sym));
                 }
                 std::any request = ccxt::dict{
                     {std::string("op"), std::string("subscribe")},
                     {std::string("args"), topics},
                 };
                 std::any url =
                     this->getUrl(std::string("candle"), std::string("public"));
                 std::any symboltimeframecandlesVariable =
                     awaitValue(this->watchMultiple(url, messageHashes, request,
                                                    messageHashes));
                 std::any symbol =
                     ::getValue(symboltimeframecandlesVariable, 0);
                 std::any timeframe =
                     ::getValue(symboltimeframecandlesVariable, 1);
                 std::any candles =
                     ::getValue(symboltimeframecandlesVariable, 2);
                 if (isTrue(this->newUpdates)) {
                   limit = ::wsGetLimit(candles, symbol, limit);
                 }
                 std::any filtered =
                     this->filterBySinceLimit(candles, since, limit, 0, true);
                 return this->createOHLCVObject(symbol, timeframe, filtered);
               })
        .share();
  }

  /**
   * @method
   * @name okx#unWatchOHLCVForSymbols
   * @description unWatches historical candlestick data containing the open,
   * high, low, and close price, and the volume of a market
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-candlesticks-channel
   * @param {string[][]} symbolsAndTimeframes array of arrays containing unified
   * symbols and timeframes to fetch OHLCV data for, example [['BTC/USDT',
   * '1m'], ['LTC/USDT', '5m']]
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {int[][]} A list of candles ordered as timestamp, open, high, low,
   * close, volume
   */
  std::shared_future<std::any>
  unWatchOHLCVForSymbols(std::any symbolsAndTimeframes,
                         std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 std::any symbolsLength = getArrayLength(symbolsAndTimeframes);
                 if (isTrue(isTrue(isEqual(symbolsLength, 0)) ||
                            !isTrue(isArray(
                                ::getValue(symbolsAndTimeframes, 0))))) {
                   throw ArgumentsRequired(toString(
                       add(this->id,
                           std::string(
                               " watchOHLCVForSymbols() requires a an array of "
                               "symbols and timeframes, like  [['BTC/USDT', "
                               "'1m'], ['LTC/USDT', '5m']]"))));
                 }
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 std::any topics = ccxt::list{};
                 std::any messageHashes = ccxt::list{};
                 for (std::any i = 0;
                      isLessThan(i, getArrayLength(symbolsAndTimeframes));
                      postFixIncrement(i)) {
                   std::any symbolAndTimeframe =
                       ::getValue(symbolsAndTimeframes, i);
                   std::any sym = ::getValue(symbolAndTimeframe, 0);
                   std::any tf = ::getValue(symbolAndTimeframe, 1);
                   std::any marketId = this->marketId(sym);
                   std::any interval =
                       this->safeString(this->timeframes, tf, tf);
                   std::any channel = add(std::string("candle"), interval);
                   std::any topic = ccxt::dict{
                       {std::string("channel"), channel},
                       {std::string("instId"), marketId},
                   };
                   arrayPush(topics, topic);
                   arrayPush(
                       messageHashes,
                       add(add(add(std::string("unsubscribe:multi:"), channel),
                               std::string(":")),
                           sym));
                 }
                 std::any request = ccxt::dict{
                     {std::string("op"), std::string("unsubscribe")},
                     {std::string("args"), topics},
                 };
                 std::any url =
                     this->getUrl(std::string("candle"), std::string("public"));
                 return awaitValue(this->watchMultiple(url, messageHashes,
                                                       request, messageHashes));
               })
        .share();
  }

  virtual void handleOHLCV(std::any client, std::any message) {
    //
    //     {
    //         "arg": { channel: "candle1m", instId: "BTC-USDT" },
    //         "data": [
    //             [
    //                 "1626690720000",
    //                 "31334",
    //                 "31334",
    //                 "31334",
    //                 "31334",
    //                 "0.0077",
    //                 "241.2718"
    //             ]
    //         ]
    //     }
    //
    std::any arg = this->safeValue(message, std::string("arg"), ccxt::dict{});
    std::any channel = this->safeString(arg, std::string("channel"));
    if (isTrue(isEqual(channel, std::any{}))) {
      return;
    }
    std::any data = this->safeValue(message, std::string("data"), ccxt::list{});
    std::any marketId = this->safeString(arg, std::string("instId"));
    std::any market = this->safeMarket(marketId);
    std::any symbol = ::getValue(market, std::string("symbol"));
    std::any interval =
        replace(channel, std::string("candle"), std::string(""));
    // use a reverse lookup in a static map instead
    std::any timeframe = this->findTimeframe(interval);
    for (std::any i = 0; isLessThan(i, getArrayLength(data));
         postFixIncrement(i)) {
      std::any parsed = this->parseOHLCV(::getValue(data, i), market);
      ::setValue(this->ohlcvs, symbol,
                 this->safeValue(this->ohlcvs, symbol, ccxt::dict{}));
      std::any stored =
          this->safeValue(this->safeValue(this->ohlcvs, symbol), timeframe);
      if (isTrue(isEqual(stored, std::any{}))) {
        std::any limit =
            this->safeInteger(this->options, std::string("OHLCVLimit"), 1000);
        stored = ccxt::ws::ArrayCacheByTimestamp(limit);
        if (isTrue(isTrue(!isEqual(symbol, std::any{})) &&
                   isTrue(!isEqual(timeframe, std::any{})))) {
          ::setValue(::getValue(this->ohlcvs, symbol), timeframe, stored);
        }
      }
      ::wsAppend(stored, parsed);
      std::any messageHash = add(add(channel, std::string(":")),
                                 ::getValue(market, std::string("id")));
      this->resolve(stored, messageHash);
      // for multiOHLCV we need special object, as opposed to other "multi"
      // methods, because OHLCV response item does not contain symbol
      // or timeframe, thus otherwise it would be unrecognizable
      std::any messageHashForMulti = add(
          add(add(std::string("multi:"), channel), std::string(":")), symbol);
      this->resolve(ccxt::list{symbol, timeframe, stored}, messageHashForMulti);
    }
  }

  /**
   * @method
   * @name okx#watchOrderBook
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-order-book-channel
   * @description watches information on open orders with bid (buy) and ask
   * (sell) prices, volumes and other data
   * @param {string} symbol unified symbol of the market to fetch the order book
   * for
   * @param {int} [limit] the maximum amount of order book entries to return
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.depth] okx order book depth, can be books, books5,
   * books-rpi, books-l2-tbt, books50-l2-tbt, bbo-tbt
   * @returns {object} A dictionary of [order book structures]{@link
   * https://docs.ccxt.com/?id=order-book-structure}
   */
  std::shared_future<std::any>
  watchOrderBook(std::any symbol, std::any limit = std::any{},
                 std::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> std::any {
                        //
                        // bbo-tbt
                        // 1. Newly added channel that sends tick-by-tick Level
                        // 1 data
                        // 2. All API users can subscribe
                        // 3. Public depth channel, verification not required
                        //
                        // books-l2-tbt
                        // 1. Only users who're VIP5 and above can subscribe
                        // 2. Identity verification required before subscription
                        //
                        // books50-l2-tbt
                        // 1. Only users who're VIP4 and above can subscribe
                        // 2. Identity verification required before subscription
                        //
                        // books
                        // 1. All API users can subscribe
                        // 2. Public depth channel, verification not required
                        //
                        // books5
                        // 1. All API users can subscribe
                        // 2. Public depth channel, verification not required
                        // 3. Data feeds will be delivered every 100ms (vs.
                        // every 200ms now)
                        //
                        // books-rpi
                        // 1. All API users can subscribe
                        // 2. Public depth channel, verification not required
                        // 3. 400 depth levels, data feeds will be delivered
                        // every 100ms
                        //
                        return awaitValue(this->watchOrderBookForSymbols(
                            ccxt::list{symbol}, limit, params));
                      })
        .share();
  }

  /**
   * @method
   * @name okx#watchOrderBookForSymbols
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-order-book-channel
   * @description watches information on open orders with bid (buy) and ask
   * (sell) prices, volumes and other data
   * @param {string[]} symbols unified array of symbols
   * @param {int} [limit] 1,5, 400, 50 (l2-tbt, vip4+) or 40000 (vip5+) the
   * maximum amount of order book entries to return
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.depth] okx order book depth, can be books, books5,
   * books-rpi, books-l2-tbt, books50-l2-tbt, bbo-tbt
   * @returns {object} an [order book structure]{@link
   * https://docs.ccxt.com/?id=order-book-structure}
   */
  std::shared_future<std::any>
  watchOrderBookForSymbols(std::any symbols, std::any limit = std::any{},
                           std::any params = ccxt::dict{}) override {
    return std::
        async(std::launch::deferred,
              [=]() mutable -> std::any {
                if (isTrue(isEqual(this->markets, std::any{}))) {
                  awaitValue(this->loadMarkets());
                }
                symbols = this->marketSymbols(symbols);
                std::any depth = std::any{};
                std::any depthparamsVariable = this->handleOptionAndParams(
                    params, std::string("watchOrderBook"), std::string("depth"),
                    std::string("books"));
                depth = ::getValue(depthparamsVariable, 0);
                params = ::getValue(depthparamsVariable, 1);
                if (isTrue(!isEqual(limit, std::any{}))) {
                  if (isTrue(isEqual(limit, 1))) {
                    depth = std::string("bbo-tbt");
                  } else if (isTrue(isTrue(isGreaterThan(limit, 1)) &&
                                    isTrue(isLessThanOrEqual(limit, 5)))) {
                    depth = std::string("books5");
                  } else if (isTrue(isEqual(limit, 50))) {
                    depth = std::string(
                        "books50-l2-tbt"); // Make sure you have VIP4 and above
                  } else if (isTrue(isEqual(limit, 400))) {
                    depth = std::string("books");
                  }
                }
                if (isTrue(
                        isTrue((isEqual(depth, std::string("books-l2-tbt")))) ||
                        isTrue(
                            (isEqual(depth, std::string("books50-l2-tbt")))))) {
                  if (!isTrue(this->checkRequiredCredentials(false))) {
                    throw AuthenticationError(toString(add(
                        this->id,
                        std::string(
                            " watchOrderBook/watchOrderBookForSymbols requires "
                            "authentication for this depth. Add credentials or "
                            "change the depth option to books or books5"))));
                  }
                  awaitValue(this->authenticate(ccxt::dict{
                      {std::string("access"), std::string("public")},
                  }));
                }
                std::any topics = ccxt::list{};
                std::any messageHashes = ccxt::list{};
                for (std::any i = 0; isLessThan(i, getArrayLength(symbols));
                     postFixIncrement(i)) {
                  std::any symbol = ::getValue(symbols, i);
                  arrayPush(messageHashes,
                            add(add(depth, std::string(":")), symbol));
                  std::any marketId = this->marketId(symbol);
                  std::any topic = ccxt::dict{
                      {std::string("channel"), depth},
                      {std::string("instId"), marketId},
                  };
                  arrayPush(topics, topic);
                }
                std::any request = ccxt::dict{
                    {std::string("op"), std::string("subscribe")},
                    {std::string("args"), topics},
                };
                std::any url = this->getUrl(depth, std::string("public"));
                std::any orderbook = awaitValue(this->watchMultiple(
                    url, messageHashes, request, messageHashes));
                return ::wsLimit(orderbook);
              })
            .share();
  }

  /**
   * @method
   * @name okx#unWatchOrderBookForSymbols
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-order-book-channel
   * @description unWatches information on open orders with bid (buy) and ask
   * (sell) prices, volumes and other data
   * @param {string[]} symbols unified array of symbols
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {int} [params.limit] the maximum amount of order book entries to
   * return
   * @param {string} [params.depth] okx order book depth, can be books, books5,
   * books-rpi, books-l2-tbt, books50-l2-tbt, bbo-tbt
   * @returns {object} A dictionary of [order book structures]{@link
   * https://docs.ccxt.com/?id=order-book-structure}
   */
  std::shared_future<std::any>
  unWatchOrderBookForSymbols(std::any symbols,
                             std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 symbols = this->marketSymbols(symbols, std::any{}, false);
                 std::any depth = std::any{};
                 std::any depthparamsVariable = this->handleOptionAndParams(
                     params, std::string("watchOrderBook"),
                     std::string("depth"), std::string("books"));
                 depth = ::getValue(depthparamsVariable, 0);
                 params = ::getValue(depthparamsVariable, 1);
                 std::any limit =
                     this->safeInteger(params, std::string("limit"));
                 if (isTrue(!isEqual(limit, std::any{}))) {
                   if (isTrue(isEqual(limit, 1))) {
                     depth = std::string("bbo-tbt");
                   } else if (isTrue(isTrue(isGreaterThan(limit, 1)) &&
                                     isTrue(isLessThanOrEqual(limit, 5)))) {
                     depth = std::string("books5");
                   } else if (isTrue(isEqual(limit, 50))) {
                     depth = std::string(
                         "books50-l2-tbt"); // Make sure you have VIP4 and above
                   } else if (isTrue(isEqual(limit, 400))) {
                     depth = std::string("books");
                   }
                 }
                 std::any topics = ccxt::list{};
                 std::any subMessageHashes = ccxt::list{};
                 std::any messageHashes = ccxt::list{};
                 for (std::any i = 0; isLessThan(i, getArrayLength(symbols));
                      postFixIncrement(i)) {
                   std::any symbol = ::getValue(symbols, i);
                   arrayPush(subMessageHashes,
                             add(add(depth, std::string(":")), symbol));
                   arrayPush(
                       messageHashes,
                       add(std::string("unsubscribe:orderbook:"), symbol));
                   std::any marketId = this->marketId(symbol);
                   std::any topic = ccxt::dict{
                       {std::string("channel"), depth},
                       {std::string("instId"), marketId},
                   };
                   arrayPush(topics, topic);
                 }
                 std::any request = ccxt::dict{
                     {std::string("op"), std::string("unsubscribe")},
                     {std::string("args"), topics},
                 };
                 std::any url = this->getUrl(depth, std::string("public"));
                 return awaitValue(this->watchMultiple(url, messageHashes,
                                                       request, messageHashes));
               })
        .share();
  }

  /**
   * @method
   * @name okx#unWatchOrderBook
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-market-data-ws-order-book-channel
   * @description unWatches information on open orders with bid (buy) and ask
   * (sell) prices, volumes and other data
   * @param {string} symbol unified array of symbols
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {int} [params.limit] the maximum amount of order book entries to
   * return
   * @param {string} [params.depth] okx order book depth, can be books, books5,
   * books-rpi, books-l2-tbt, books50-l2-tbt, bbo-tbt
   * @returns {object} A dictionary of [order book structures]{@link
   * https://docs.ccxt.com/?id=order-book-structure}
   */
  std::shared_future<std::any>
  unWatchOrderBook(std::any symbol, std::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> std::any {
                        return awaitValue(this->unWatchOrderBookForSymbols(
                            ccxt::list{symbol}, params));
                      })
        .share();
  }

  void handleDelta(std::any bookside, std::any delta) override {
    //
    //     [
    //         "31685", // price
    //         "0.78069158", // amount
    //         "0", // liquidated orders
    //         "17" // orders
    //     ]
    //
    std::any price = this->safeFloat(delta, 0);
    std::any amount = this->safeFloat(delta, 1);
    ::wsStore(bookside, price, amount);
  }

  void handleDeltas(std::any bookside, std::any deltas) override {
    for (std::any i = 0; isLessThan(i, getArrayLength(deltas));
         postFixIncrement(i)) {
      this->handleDelta(bookside, ::getValue(deltas, i));
    }
  }

  virtual std::any handleOrderBookMessage(std::any client, std::any message,
                                          std::any orderbook,
                                          std::any messageHash,
                                          std::any market = std::any{}) {
    //
    //     {
    //         "asks": [
    //             [ '31738.3', '0.05973179', "0", "3" ],
    //             [ '31738.5', '0.11035404', "0", "2" ],
    //             [ '31739.6', '0.01', "0", "1" ],
    //         ],
    //         "bids": [
    //             [ '31738.2', '0.67557666', "0", "9" ],
    //             [ '31738', '0.02466947', "0", "2" ],
    //             [ '31736.3', '0.01705046', "0", "2" ],
    //         ],
    //         "instId": "BTC-USDT",
    //         "ts": "1626537446491"
    //         "checksum": -855196043,
    //         "prevSeqId": 123456,
    //         "seqId": 123457
    //     }
    //
    std::any asks = this->safeValue(message, std::string("asks"), ccxt::list{});
    std::any bids = this->safeValue(message, std::string("bids"), ccxt::list{});
    std::any storedAsks = ::getValue(orderbook, std::string("asks"));
    std::any storedBids = ::getValue(orderbook, std::string("bids"));
    this->handleDeltas(storedAsks, asks);
    this->handleDeltas(storedBids, bids);
    std::any marketId = this->safeString(message, std::string("instId"));
    std::any symbol = this->safeSymbol(marketId, market);
    std::any seqId = this->safeInteger(message, std::string("seqId"));
    std::any prevSeqId = this->safeInteger(message, std::string("prevSeqId"));
    std::any nonce = ::getValue(orderbook, std::string("nonce"));
    std::any error = std::any{};
    if (isTrue(isTrue(isTrue(!isEqual(prevSeqId, std::any{})) &&
                      isTrue(!isEqual(prevSeqId, -1))) &&
               isTrue(!isEqual(nonce, prevSeqId)))) {
      error = InvalidNonce(
          add(this->id, std::string(" watchOrderBook received invalid nonce")));
    }
    if (isTrue(!isEqual(error, std::any{}))) {
      deleteKey(::getValue(client, std::string("subscriptions")), messageHash);
      if (isTrue(!isEqual(symbol, std::any{}))) {
        deleteKey(this->orderbooks, symbol);
      }
      this->reject(error, messageHash);
    }
    std::any timestamp = this->safeInteger(message, std::string("ts"));
    ::setValue(orderbook, std::string("nonce"), seqId);
    ::setValue(orderbook, std::string("timestamp"), timestamp);
    ::setValue(orderbook, std::string("datetime"), this->iso8601(timestamp));
    return orderbook;
  }

  virtual std::any handleOrderBook(std::any client, std::any message) {
    //
    // snapshot
    //
    //     {
    //         "arg": { channel: 'books-l2-tbt', instId: "BTC-USDT" },
    //         "action": "snapshot",
    //         "data": [
    //             {
    //                 "asks": [
    //                     [ '31685', '0.78069158', "0", "17" ],
    //                     [ '31685.1', '0.0001', "0", "1" ],
    //                     [ '31685.6', '0.04543165', "0", "1" ],
    //                 ],
    //                 "bids": [
    //                     [ '31684.9', '0.01', "0", "1" ],
    //                     [ '31682.9', '0.0001', "0", "1" ],
    //                     [ '31680.7', '0.01', "0", "1" ],
    //                 ],
    //                 "ts": "1626532416403",
    //                 "checksum": -1023440116
    //             }
    //         ]
    //     }
    //
    // update
    //
    //     {
    //         "arg": { channel: 'books-l2-tbt', instId: "BTC-USDT" },
    //         "action": "update",
    //         "data": [
    //             {
    //                 "asks": [
    //                     [ '31657.7', '0', "0", "0" ],
    //                     [ '31659.7', '0.01', "0", "1" ],
    //                     [ '31987.3', '0.01', "0", "1" ]
    //                 ],
    //                 "bids": [
    //                     [ '31642.9', '0.50296385', "0", "4" ],
    //                     [ '31639.9', '0', "0", "0" ],
    //                     [ '31638.7', '0.01', "0", "1" ],
    //                 ],
    //                 "ts": "1626535709008",
    //                 "checksum": 830931827
    //             }
    //         ]
    //     }
    //
    // books5
    //
    //     {
    //         "arg": { channel: "books5", instId: "BTC-USDT" },
    //         "data": [
    //             {
    //                 "asks": [
    //                     [ '31738.3', '0.05973179', "0", "3" ],
    //                     [ '31738.5', '0.11035404', "0", "2" ],
    //                     [ '31739.6', '0.01', "0", "1" ],
    //                 ],
    //                 "bids": [
    //                     [ '31738.2', '0.67557666', "0", "9" ],
    //                     [ '31738', '0.02466947', "0", "2" ],
    //                     [ '31736.3', '0.01705046', "0", "2" ],
    //                 ],
    //                 "instId": "BTC-USDT",
    //                 "ts": "1626537446491"
    //             }
    //         ]
    //     }
    //
    // bbo-tbt
    //
    //     {
    //         "arg":{
    //             "channel":"bbo-tbt",
    //             "instId":"BTC-USDT"
    //         },
    //         "data":[
    //             {
    //                 "asks":[["36232.2","1.8826134","0","17"]],
    //                 "bids":[["36232.1","0.00572212","0","2"]],
    //                 "ts":"1651826598363"
    //             }
    //         ]
    //     }
    //
    std::any arg = this->safeDict(message, std::string("arg"), ccxt::dict{});
    std::any channel = this->safeString(arg, std::string("channel"));
    std::any action = this->safeString(message, std::string("action"));
    std::any data = this->safeList(message, std::string("data"), ccxt::list{});
    std::any marketId = this->safeString(arg, std::string("instId"));
    std::any market = this->safeMarket(marketId);
    std::any symbol = ::getValue(market, std::string("symbol"));
    std::any depths = ccxt::dict{
        {std::string("bbo-tbt"), 1},        {std::string("books"), 400},
        {std::string("books5"), 5},         {std::string("books-rpi"), 400},
        {std::string("books-l2-tbt"), 400}, {std::string("books50-l2-tbt"), 50},
    };
    std::any limit = this->safeInteger(depths, channel);
    std::any messageHash = add(add(channel, std::string(":")), symbol);
    if (isTrue(isEqual(action, std::string("snapshot")))) {
      for (std::any i = 0; isLessThan(i, getArrayLength(data));
           postFixIncrement(i)) {
        std::any update = ::getValue(data, i);
        std::any orderbook = this->orderBook(ccxt::dict{}, limit);
        ::setValue(this->orderbooks, symbol, orderbook);
        ::setValue(orderbook, std::string("symbol"), symbol);
        this->handleOrderBookMessage(client, update, orderbook, messageHash);
        this->resolve(orderbook, messageHash);
      }
    } else if (isTrue(isEqual(action, std::string("update")))) {
      if (isTrue(inOp(this->orderbooks, symbol))) {
        std::any orderbook = ::getValue(this->orderbooks, symbol);
        for (std::any i = 0; isLessThan(i, getArrayLength(data));
             postFixIncrement(i)) {
          std::any update = ::getValue(data, i);
          this->handleOrderBookMessage(client, update, orderbook, messageHash,
                                       market);
          this->resolve(orderbook, messageHash);
        }
      }
    } else if (isTrue(isTrue((isEqual(channel, std::string("books5")))) ||
                      isTrue((isEqual(channel, std::string("bbo-tbt")))))) {
      if (!isTrue((inOp(this->orderbooks, symbol)))) {
        ::setValue(this->orderbooks, symbol,
                   this->orderBook(ccxt::dict{}, limit));
      }
      std::any orderbook = ::getValue(this->orderbooks, symbol);
      for (std::any i = 0; isLessThan(i, getArrayLength(data));
           postFixIncrement(i)) {
        std::any update = ::getValue(data, i);
        std::any timestamp = this->safeInteger(update, std::string("ts"));
        std::any snapshot =
            this->parseOrderBook(update, symbol, timestamp, std::string("bids"),
                                 std::string("asks"), 0, 1);
        ::resetOrderBook(orderbook, snapshot);
        this->resolve(orderbook, messageHash);
      }
    }
    return message;
  }

  virtual std::shared_future<std::any>
  authenticate(std::any params = ccxt::dict{}) {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 this->checkRequiredCredentials();
                 std::any access = this->safeString(
                     params, std::string("access"), std::string("private"));
                 params = this->omit(params, ccxt::list{std::string("access")});
                 std::any url = this->getUrl(std::string("users"), access);
                 std::any messageHash = std::string("authenticated");
                 std::any client = this->client(url);
                 std::any future =
                     ::wsClientReusableFuture(client, messageHash);
                 std::any authenticated = this->safeValue(
                     ::getValue(client, std::string("subscriptions")),
                     messageHash);
                 if (isTrue(isEqual(authenticated, std::any{}))) {
                   std::any timestamp = toString(this->seconds());
                   std::any method = std::string("GET");
                   std::any path = std::string("/users/self/verify");
                   std::any auth = add(add(timestamp, method), path);
                   std::any signature = this->hmac(
                       this->encode(auth), this->encode(this->secret), sha256,
                       std::string("base64"));
                   std::any operation = std::string("login");
                   std::any request = ccxt::dict{
                       {std::string("op"), operation},
                       {std::string("args"),
                        ccxt::list{ccxt::dict{
                            {std::string("apiKey"), this->apiKey},
                            {std::string("passphrase"), this->password},
                            {std::string("timestamp"), timestamp},
                            {std::string("sign"), signature},
                        }}},
                   };
                   // Only add params['access'] to prevent sending custom
                   // parameters, such as extraParams.
                   if (isTrue(inOp(params, std::string("access")))) {
                     ::setValue(request, std::string("access"),
                                ::getValue(params, std::string("access")));
                   }
                   this->watch(url, messageHash, request, messageHash);
                 }
                 return awaitValue(future);
               })
        .share();
  }

  /**
   * @method
   * @name okx#watchBalance
   * @see
   * https://www.okx.com/docs-v5/en/#trading-account-websocket-account-channel
   * @description watch balance and get the amount of funds available for
   * trading or funds locked in orders
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} a [balance structure]{@link
   * https://docs.ccxt.com/?id=balance-structure}
   */
  std::shared_future<std::any>
  watchBalance(std::any params = ccxt::dict{}) override {
    return std::async(std::launch::deferred,
                      [=]() mutable -> std::any {
                        if (isTrue(isEqual(this->markets, std::any{}))) {
                          awaitValue(this->loadMarkets());
                        }
                        awaitValue(this->authenticate());
                        return awaitValue(this->subscribe(
                            std::string("private"), std::string("account"),
                            std::string("account"), std::any{}, params));
                      })
        .share();
  }

  virtual void handleBalanceAndPosition(std::any client, std::any message) {
    this->handleMyLiquidation(client, message);
  }

  virtual void handleBalance(std::any client, std::any message) {
    //
    //     {
    //         arg: {
    //             channel: 'account',
    //             uid: '158635852459810816'
    //         },
    //         eventType: 'snapshot',
    //         curPage: 1,
    //         lastPage: true,
    //         data: [
    //             {
    //                 adjEq: '100942.13298468884',
    //                 availEq: '98461.99074635761',
    //                 borrowFroz: '409.3702076072169',
    //                 delta: '',
    //                 deltaLever: '',
    //                 deltaNeutralStatus: '',
    //                 details: [
    //                     {
    //                         accAvgPx: '',
    //                         autoLendAmt: '0',
    //                         autoLendMtAmt: '0',
    //                         autoLendStatus: 'unsupported',
    //                         autoStakingStatus: 'unsupported',
    //                         availBal: '2900',
    //                         availEq: '2900',
    //                         borrowFroz: '0',
    //                         cashBal: '2900',
    //                         ccy: 'TUSD',
    //                         clSpotInUseAmt: '',
    //                         coinUsdPrice: '200.026',
    //                         colBorrAutoConversion: '0',
    //                         colRes: '0',
    //                         collateralEnabled: false,
    //                         collateralRestrict: false,
    //                         crossLiab: '0',
    //                         disEq: '0',
    //                         eq: '2900',
    //                         eqUsd: '580075.4',
    //                         fixedBal: '0',
    //                         frozenBal: '0',
    //                         frpType: '0',
    //                         imr: '',
    //                         interest: '0',
    //                         isoEq: '0',
    //                         isoLiab: '0',
    //                         isoUpl: '0',
    //                         liab: '0',
    //                         maxLoan: '0',
    //                         maxSpotInUseAmt: '',
    //                         mgnRatio: '',
    //                         mmr: '',
    //                         notionalLever: '',
    //                         openAvgPx: '',
    //                         ordFrozen: '0',
    //                         rewardBal: '',
    //                         smtSyncEq: '0',
    //                         spotBal: '',
    //                         spotCopyTradingEq: '0',
    //                         spotInUseAmt: '',
    //                         spotIsoBal: '0',
    //                         spotUpl: '',
    //                         spotUplRatio: '',
    //                         stgyEq: '0',
    //                         totalPnl: '',
    //                         totalPnlRatio: '',
    //                         twap: '0',
    //                         uTime: '1752243427083',
    //                         upl: '0',
    //                         uplLiab: '0'
    //                     }
    //                 ],
    //                 imr: '2480.1422383312265',
    //                 isoEq: '0',
    //                 mgnRatio: '1814.5924297007082',
    //                 mmr: '52.196024182958055',
    //                 notionalUsd: '4634.0971302081125',
    //                 notionalUsdForBorrow: '2046.8510380360844',
    //                 notionalUsdForFutures: '101.11322817202809',
    //                 notionalUsdForOption: '0',
    //                 notionalUsdForSwap: '2486.1328640000006',
    //                 ordFroz: '1208.3566666666668',
    //                 totalEq: '711018.8951315446',
    //                 uTime: '1773837554158',
    //                 upl: '9.244336372701904'
    //             }
    //         ]
    //     }
    //
    std::any arg = this->safeValue(message, std::string("arg"), ccxt::dict{});
    std::any channel = this->safeString(arg, std::string("channel"));
    std::any balance = this->parseTradingBalance(message);
    std::any newBalance = this->deepExtend(this->balance, balance);
    this->balance = this->safeBalance(newBalance);
    this->resolve(this->balance, channel);
  }

  virtual std::any orderToTrade(std::any order, std::any market = std::any{}) {
    std::any info = this->safeValue(order, std::string("info"), ccxt::dict{});
    std::any timestamp = this->safeInteger(info, std::string("fillTime"));
    std::any feeMarketId = this->safeString(info, std::string("fillFeeCcy"));
    std::any isTaker = isEqual(
        this->safeString(info, std::string("execType"), std::string("")),
        std::string("T"));
    return this->safeTrade(
        ccxt::dict{
            {std::string("info"), info},
            {std::string("timestamp"), timestamp},
            {std::string("datetime"), this->iso8601(timestamp)},
            {std::string("symbol"),
             this->safeString(order, std::string("symbol"))},
            {std::string("id"), this->safeString(info, std::string("tradeId"))},
            {std::string("order"), this->safeString(order, std::string("id"))},
            {std::string("type"), this->safeString(order, std::string("type"))},
            {std::string("takerOrMaker"),
             (isTrue((isTaker)) ? std::any(std::string("taker"))
                                : std::any(std::string("maker")))},
            {std::string("side"), this->safeString(order, std::string("side"))},
            {std::string("price"),
             this->safeNumber(info, std::string("fillPx"))},
            {std::string("amount"),
             this->safeNumber(info, std::string("fillSz"))},
            {std::string("cost"), this->safeNumber(order, std::string("cost"))},
            {std::string("fee"),
             ccxt::dict{
                 {std::string("cost"),
                  this->safeNumber(info, std::string("fillFee"))},
                 {std::string("currency"), this->safeCurrencyCode(feeMarketId)},
             }},
        },
        market);
  }

  /**
   * @method
   * @name okx#watchMyTrades
   * @description watches information on multiple trades made by the user
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-trade-ws-order-channel
   * @param {string} [symbol] unified market symbol of the market trades were
   * made in
   * @param {int} [since] the earliest time in ms to fetch trades for
   * @param {int} [limit] the maximum number of trade structures to retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {bool} [params.trigger] true if fetching trigger or conditional
   * trades
   * @param {string} [params.type] 'spot', 'swap', 'future', 'option', 'ANY',
   * 'SPOT', 'MARGIN', 'SWAP', 'FUTURES' or 'OPTION'
   * @param {string} [params.marginMode] 'cross' or 'isolated', for
   * automatically setting the type to spot margin
   * @returns {object[]} a list of [trade structures]{@link
   * https://docs.ccxt.com/?id=trade-structure}
   */
  std::shared_future<std::any>
  watchMyTrades(std::any symbol = std::any{}, std::any since = std::any{},
                std::any limit = std::any{},
                std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 // By default, receive order updates from any instrument type
                 std::any type = std::any{};
                 std::any typeparamsVariable = this->handleOptionAndParams(
                     params, std::string("watchMyTrades"), std::string("type"),
                     std::string("ANY"));
                 type = ::getValue(typeparamsVariable, 0);
                 params = ::getValue(typeparamsVariable, 1);
                 std::any isTrigger =
                     this->safeBool2(params, std::string("trigger"),
                                     std::string("stop"), false);
                 params = this->omit(params, ccxt::list{std::string("trigger"),
                                                        std::string("stop")});
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 std::any access = (isTrue((isEqual(isTrigger, true)))
                                        ? std::any(std::string("business"))
                                        : std::any(std::string("private")));
                 awaitValue(this->authenticate(ccxt::dict{
                     {std::string("access"), access},
                 }));
                 std::any channel = (isTrue((isEqual(isTrigger, true)))
                                         ? std::any(std::string("orders-algo"))
                                         : std::any(std::string("orders")));
                 std::any messageHash = add(channel, std::string("::myTrades"));
                 std::any market = std::any{};
                 if (isTrue(!isEqual(symbol, std::any{}))) {
                   market = this->market(symbol);
                   symbol = ::getValue(market, std::string("symbol"));
                   type = ::getValue(market, std::string("type"));
                   messageHash =
                       add(add(messageHash, std::string("::")), symbol);
                 }
                 if (isTrue(isEqual(type, std::string("future")))) {
                   type = std::string("futures");
                 }
                 if (isTrue(isEqual(type, std::any{}))) {
                   throw ArgumentsRequired(toString(
                       add(this->id,
                           std::string(" watchMyTrades() type is required"))));
                 }
                 std::any uppercaseType = toUpperCase(type);
                 std::any marginMode = std::any{};
                 std::any marginModeparamsVariable =
                     this->handleMarginModeAndParams(
                         std::string("watchMyTrades"), params);
                 marginMode = ::getValue(marginModeparamsVariable, 0);
                 params = ::getValue(marginModeparamsVariable, 1);
                 if (isTrue(isEqual(uppercaseType, std::string("SPOT")))) {
                   if (isTrue(!isEqual(marginMode, std::any{}))) {
                     uppercaseType = std::string("MARGIN");
                   }
                 }
                 std::any request = ccxt::dict{
                     {std::string("instType"), uppercaseType},
                 };
                 std::any orders = awaitValue(this->subscribe(
                     std::string("private"), messageHash, channel, std::any{},
                     this->extend(request, params)));
                 if (isTrue(this->newUpdates)) {
                   limit = ::wsGetLimit(orders, symbol, limit);
                 }
                 return this->filterBySymbolSinceLimit(orders, symbol, since,
                                                       limit, true);
               })
        .share();
  }

  /**
   * @method
   * @name okx#watchPositions
   * @see
   * https://www.okx.com/docs-v5/en/#trading-account-websocket-positions-channel
   * @description watch all open positions
   * @param {string[]} [symbols] list of unified market symbols
   * @param {int} [since] timestamp in ms of the earliest position to fetch
   * @param {int} [limit] the maximum number of positions to fetch
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [position structure]{@link
   * https://docs.ccxt.com/en/latest/manual.html#position-structure}
   */
  std::shared_future<std::any>
  watchPositions(std::any symbols = std::any{}, std::any since = std::any{},
                 std::any limit = std::any{},
                 std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 awaitValue(this->authenticate(params));
                 symbols = this->marketSymbols(symbols);
                 std::any request = ccxt::dict{
                     {std::string("instType"), std::string("ANY")},
                 };
                 std::any channel = std::string("positions");
                 std::any newPositions = std::any{};
                 if (isTrue(isEqual(symbols, std::any{}))) {
                   std::any arg = ccxt::dict{
                       {std::string("channel"), std::string("positions")},
                       {std::string("instType"), std::string("ANY")},
                   };
                   std::any args = ccxt::list{this->extend(arg, params)};
                   std::any nonSymbolRequest = ccxt::dict{
                       {std::string("op"), std::string("subscribe")},
                       {std::string("args"), args},
                   };
                   std::any url = this->getUrl(channel, std::string("private"));
                   newPositions = awaitValue(
                       this->watch(url, channel, nonSymbolRequest, channel));
                 } else {
                   newPositions = awaitValue(this->subscribeMultiple(
                       std::string("private"), channel, symbols,
                       this->extend(request, params)));
                 }
                 if (isTrue(this->newUpdates)) {
                   return (isTrue((isEqual(newPositions, std::any{})))
                               ? std::any(ccxt::list{})
                               : std::any(newPositions));
                 }
                 return this->filterBySymbolsSinceLimit(
                     this->positions, symbols, since, limit, true);
               })
        .share();
  }

  virtual void handlePositions(std::any client, std::any message) {
    //
    //    {
    //        arg: {
    //            channel: 'positions',
    //            instType: 'ANY',
    //            instId: 'XRP-USDT-SWAP',
    //            uid: '464737184507959869'
    //        },
    //        data: [{
    //            adl: '1',
    //            availPos: '',
    //            avgPx: '0.52668',
    //            baseBal: '',
    //            baseBorrowed: '',
    //            baseInterest: '',
    //            bizRefId: '',
    //            bizRefType: '',
    //            cTime: '1693151444408',
    //            ccy: 'USDT',
    //            closeOrderAlgo: [],
    //            deltaBS: '',
    //            deltaPA: '',
    //            gammaBS: '',
    //            gammaPA: '',
    //            idxPx: '0.52683',
    //            imr: '17.564000000000004',
    //            instId: 'XRP-USDT-SWAP',
    //            instType: 'SWAP',
    //            interest: '',
    //            last: '0.52691',
    //            lever: '3',
    //            liab: '',
    //            liabCcy: '',
    //            liqPx: '0.3287514731020614',
    //            margin: '',
    //            markPx: '0.52692',
    //            mgnMode: 'cross',
    //            mgnRatio: '69.00363001456147',
    //            mmr: '0.26346',
    //            notionalUsd: '52.68620388000001',
    //            optVal: '',
    //            pTime: '1693151906023',
    //            pendingCloseOrdLiabVal: '',
    //            pos: '1',
    //            posCcy: '',
    //            posId: '616057041198907393',
    //            posSide: 'net',
    //            quoteBal: '',
    //            quoteBorrowed: '',
    //            quoteInterest: '',
    //            spotInUseAmt: '',
    //            spotInUseCcy: '',
    //            thetaBS: '',
    //            thetaPA: '',
    //            tradeId: '138745402',
    //            uTime: '1693151444408',
    //            upl: '0.0240000000000018',
    //            uplLastPx: '0.0229999999999952',
    //            uplRatio: '0.0013670539986328',
    //            uplRatioLastPx: '0.001310093415356',
    //            usdPx: '',
    //            vegaBS: '',
    //            vegaPA: ''
    //        }]
    //    }
    //
    std::any arg = this->safeValue(message, std::string("arg"), ccxt::dict{});
    std::any marketId = this->safeString(arg, std::string("instId"));
    std::any market = this->safeMarket(marketId, std::any{}, std::string("-"));
    std::any symbol = ::getValue(market, std::string("symbol"));
    std::any channel =
        this->safeString(arg, std::string("channel"), std::string(""));
    std::any data = this->safeValue(message, std::string("data"), ccxt::list{});
    if (isTrue(isEqual(this->positions, std::any{}))) {
      this->positions = ccxt::ws::ArrayCacheBySymbolBySide();
    }
    std::any cache = this->positions;
    std::any newPositions = ccxt::list{};
    for (std::any i = 0; isLessThan(i, getArrayLength(data));
         postFixIncrement(i)) {
      std::any rawPosition = ::getValue(data, i);
      std::any position = this->parsePosition(rawPosition);
      if (isTrue(isTrue(isEqual(::getValue(position, std::string("contracts")),
                                0)) &&
                 isTrue(isEqual(::getValue(rawPosition, std::string("posSide")),
                                std::string("net"))))) {
        ::setValue(position, std::string("side"), std::string("long"));
        std::any shortPosition = this->clone(position);
        ::setValue(shortPosition, std::string("side"), std::string("short"));
        ::wsAppend(cache, shortPosition);
        arrayPush(newPositions, shortPosition);
      }
      arrayPush(newPositions, position);
      ::wsAppend(cache, position);
    }
    std::any messageHash = channel;
    if (isTrue(!isEqual(symbol, std::any{}))) {
      messageHash = add(add(channel, std::string("::")), symbol);
    }
    this->resolve(newPositions, messageHash);
  }

  /**
   * @method
   * @name okx#watchOrders
   * @description watches information on multiple orders made by the user
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-trade-ws-order-channel
   * @param {string} [symbol] unified market symbol of the market the orders
   * were made in
   * @param {int} [since] the earliest time in ms to fetch orders for
   * @param {int} [limit] the maximum number of order structures to retrieve
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {bool} [params.trigger] true if fetching trigger or conditional
   * orders
   * @param {string} [params.type] 'spot', 'swap', 'future', 'option', 'ANY',
   * 'SPOT', 'MARGIN', 'SWAP', 'FUTURES' or 'OPTION'
   * @param {string} [params.marginMode] 'cross' or 'isolated', for
   * automatically setting the type to spot margin
   * @returns {object[]} a list of [order structures]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<std::any>
  watchOrders(std::any symbol = std::any{}, std::any since = std::any{},
              std::any limit = std::any{},
              std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 std::any type = std::any{};
                 // By default, receive order updates from any instrument type
                 std::any typeparamsVariable = this->handleOptionAndParams(
                     params, std::string("watchOrders"), std::string("type"),
                     std::string("ANY"));
                 type = ::getValue(typeparamsVariable, 0);
                 params = ::getValue(typeparamsVariable, 1);
                 std::any isTrigger =
                     this->safeValue2(params, std::string("stop"),
                                      std::string("trigger"), false);
                 params =
                     this->omit(params, ccxt::list{std::string("stop"),
                                                   std::string("trigger")});
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 std::any accessType = (isTrue((isEqual(isTrigger, true)))
                                            ? std::any(std::string("business"))
                                            : std::any(std::string("private")));
                 awaitValue(this->authenticate(ccxt::dict{
                     {std::string("access"), accessType},
                 }));
                 std::any market = std::any{};
                 if (isTrue(!isEqual(symbol, std::any{}))) {
                   market = this->market(symbol);
                   symbol = ::getValue(market, std::string("symbol"));
                   type = ::getValue(market, std::string("type"));
                 }
                 if (isTrue(isEqual(type, std::string("future")))) {
                   type = std::string("futures");
                 }
                 if (isTrue(isEqual(type, std::any{}))) {
                   throw ArgumentsRequired(toString(
                       add(this->id,
                           std::string(" watchOrders() type is required"))));
                 }
                 std::any uppercaseType = toUpperCase(type);
                 std::any marginMode = std::any{};
                 std::any marginModeparamsVariable =
                     this->handleMarginModeAndParams(std::string("watchOrders"),
                                                     params);
                 marginMode = ::getValue(marginModeparamsVariable, 0);
                 params = ::getValue(marginModeparamsVariable, 1);
                 if (isTrue(isEqual(uppercaseType, std::string("SPOT")))) {
                   if (isTrue(!isEqual(marginMode, std::any{}))) {
                     uppercaseType = std::string("MARGIN");
                   }
                 }
                 std::any request = ccxt::dict{
                     {std::string("instType"), uppercaseType},
                 };
                 std::any channel = (isTrue((isEqual(isTrigger, true)))
                                         ? std::any(std::string("orders-algo"))
                                         : std::any(std::string("orders")));
                 std::any orders = awaitValue(
                     this->subscribe(std::string("private"), channel, channel,
                                     symbol, this->extend(request, params)));
                 if (isTrue(this->newUpdates)) {
                   limit = ::wsGetLimit(orders, symbol, limit);
                 }
                 return this->filterBySymbolSinceLimit(orders, symbol, since,
                                                       limit, true);
               })
        .share();
  }

  virtual void handleOrders(std::any client, std::any message) {
    //
    //     {
    //         "arg":{
    //             "channel":"orders",
    //             "instType":"SPOT"
    //         },
    //         "data":[
    //             {
    //                 "accFillSz":"0",
    //                 "amendResult":"",
    //                 "avgPx":"",
    //                 "cTime":"1634548275191",
    //                 "category":"normal",
    //                 "ccy":"",
    //                 "clOrdId":"e847386590ce4dBC330547db94a08ba0",
    //                 "code":"0",
    //                 "execType":"",
    //                 "fee":"0",
    //                 "feeCcy":"USDT",
    //                 "fillFee":"0",
    //                 "fillFeeCcy":"",
    //                 "fillNotionalUsd":"",
    //                 "fillPx":"",
    //                 "fillSz":"0",
    //                 "fillTime":"",
    //                 "instId":"ETH-USDT",
    //                 "instType":"SPOT",
    //                 "lever":"",
    //                 "msg":"",
    //                 "notionalUsd":"451.4516256",
    //                 "ordId":"370257534141235201",
    //                 "ordType":"limit",
    //                 "pnl":"0",
    //                 "posSide":"",
    //                 "px":"60000",
    //                 "rebate":"0",
    //                 "rebateCcy":"ETH",
    //                 "reqId":"",
    //                 "side":"sell",
    //                 "slOrdPx":"",
    //                 "slTriggerPx":"",
    //                 "state":"live",
    //                 "sz":"0.007526",
    //                 "tag":"",
    //                 "tdMode":"cash",
    //                 "tgtCcy":"",
    //                 "tpOrdPx":"",
    //                 "tpTriggerPx":"",
    //                 "tradeId":"",
    //                 "uTime":"1634548275191"
    //             }
    //         ]
    //     }
    //
    this->handleMyTrades(client, message);
    std::any arg = this->safeValue(message, std::string("arg"), ccxt::dict{});
    std::any channel = this->safeString(arg, std::string("channel"));
    std::any orders =
        this->safeValue(message, std::string("data"), ccxt::list{});
    std::any ordersLength = getArrayLength(orders);
    if (isTrue(isGreaterThan(ordersLength, 0))) {
      std::any limit =
          this->safeInteger(this->options, std::string("ordersLimit"), 1000);
      if (isTrue(isEqual(this->orders, std::any{}))) {
        this->orders = ccxt::ws::ArrayCacheBySymbolById(limit);
        this->triggerOrders = ccxt::ws::ArrayCacheBySymbolById(limit);
      }
      std::any stored = (isTrue((isEqual(channel, std::string("orders-algo"))))
                             ? std::any(this->triggerOrders)
                             : std::any(this->orders));
      std::any marketIds = ccxt::list{};
      std::any parsed = this->parseOrders(orders);
      for (std::any i = 0; isLessThan(i, getArrayLength(parsed));
           postFixIncrement(i)) {
        std::any order = ::getValue(parsed, i);
        ::wsAppend(stored, order);
        std::any symbol = ::getValue(order, std::string("symbol"));
        std::any market = this->market(symbol);
        arrayPush(marketIds, ::getValue(market, std::string("id")));
      }
      this->resolve(stored, channel);
      for (std::any i = 0; isLessThan(i, getArrayLength(marketIds));
           postFixIncrement(i)) {
        std::any messageHash =
            add(add(channel, std::string(":")), ::getValue(marketIds, i));
        this->resolve(stored, messageHash);
      }
    }
  }

  virtual void handleMyTrades(std::any client, std::any message) {
    //
    //     {
    //         "arg":{
    //             "channel":"orders",
    //             "instType":"SPOT"
    //         },
    //         "data":[
    //             {
    //                 "accFillSz":"0",
    //                 "amendResult":"",
    //                 "avgPx":"",
    //                 "cTime":"1634548275191",
    //                 "category":"normal",
    //                 "ccy":"",
    //                 "clOrdId":"e847386590ce4dBC330547db94a08ba0",
    //                 "code":"0",
    //                 "execType":"",
    //                 "fee":"0",
    //                 "feeCcy":"USDT",
    //                 "fillFee":"0",
    //                 "fillFeeCcy":"",
    //                 "fillNotionalUsd":"",
    //                 "fillPx":"",
    //                 "fillSz":"0",
    //                 "fillTime":"",
    //                 "instId":"ETH-USDT",
    //                 "instType":"SPOT",
    //                 "lever":"",
    //                 "msg":"",
    //                 "notionalUsd":"451.4516256",
    //                 "ordId":"370257534141235201",
    //                 "ordType":"limit",
    //                 "pnl":"0",
    //                 "posSide":"",
    //                 "px":"60000",
    //                 "rebate":"0",
    //                 "rebateCcy":"ETH",
    //                 "reqId":"",
    //                 "side":"sell",
    //                 "slOrdPx":"",
    //                 "slTriggerPx":"",
    //                 "state":"live",
    //                 "sz":"0.007526",
    //                 "tag":"",
    //                 "tdMode":"cash",
    //                 "tgtCcy":"",
    //                 "tpOrdPx":"",
    //                 "tpTriggerPx":"",
    //                 "tradeId":"",
    //                 "uTime":"1634548275191"
    //             }
    //         ]
    //     }
    //
    std::any arg = this->safeValue(message, std::string("arg"), ccxt::dict{});
    std::any channel = this->safeString(arg, std::string("channel"));
    std::any rawOrders =
        this->safeValue(message, std::string("data"), ccxt::list{});
    std::any filteredOrders = ccxt::list{};
    // filter orders with no last trade id
    for (std::any i = 0; isLessThan(i, getArrayLength(rawOrders));
         postFixIncrement(i)) {
      std::any rawOrder = ::getValue(rawOrders, i);
      std::any tradeId =
          this->safeString(rawOrder, std::string("tradeId"), std::string(""));
      if (isTrue(isGreaterThan(getStringLength(tradeId), 0))) {
        std::any order = this->parseOrder(rawOrder);
        arrayPush(filteredOrders, order);
      }
    }
    std::any tradesLength = getArrayLength(filteredOrders);
    if (isTrue(isEqual(tradesLength, 0))) {
      return;
    }
    if (isTrue(isEqual(this->myTrades, std::any{}))) {
      std::any limit =
          this->safeInteger(this->options, std::string("tradesLimit"), 1000);
      this->myTrades = ccxt::ws::ArrayCacheBySymbolById(limit);
    }
    std::any myTrades = this->myTrades;
    std::any symbols = ccxt::dict{};
    for (std::any i = 0; isLessThan(i, getArrayLength(filteredOrders));
         postFixIncrement(i)) {
      std::any rawTrade = ::getValue(filteredOrders, i);
      std::any trade = this->orderToTrade(rawTrade);
      ::wsAppend(myTrades, trade);
      std::any symbol = ::getValue(trade, std::string("symbol"));
      if (isTrue(!isEqual(symbol, std::any{}))) {
        ::setValue(symbols, symbol, true);
      }
    }
    std::any messageHash = add(channel, std::string("::myTrades"));
    this->resolve(this->myTrades, messageHash);
    std::any tradeSymbols = getObjectKeys(symbols);
    for (std::any i = 0; isLessThan(i, getArrayLength(tradeSymbols));
         postFixIncrement(i)) {
      std::any symbolMessageHash =
          add(add(messageHash, std::string("::")), ::getValue(tradeSymbols, i));
      this->resolve(this->myTrades, symbolMessageHash);
    }
  }

  virtual std::any requestId() {
    std::any ts = toString(this->milliseconds());
    std::any randomNumber = this->randNumber(4);
    std::any randomPart = toString(randomNumber);
    return add(ts, randomPart);
  }

  /**
   * @method
   * @name okx#createOrderWs
   * @see https://www.okx.com/docs-v5/en/#websocket-api-trade-place-order
   * @description create a trade order
   * @param {string} symbol unified symbol of the market to create an order in
   * @param {string} type 'market' or 'limit'
   * @param {string} side 'buy' or 'sell'
   * @param {float} amount how much of currency you want to trade in units of
   * base currency
   * @param {float|undefined} [price] the price at which the order is to be
   * fulfilled, in units of the quote currency, ignored in market orders
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {boolean} params.test test order, default false
   * @returns {object} an [order structure]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<std::any>
  createOrderWs(std::any symbol, std::any type, std::any side, std::any amount,
                std::any price = std::any{},
                std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 awaitValue(this->authenticate());
                 std::any url = this->getUrl(std::string("private"),
                                             std::string("private"));
                 std::any messageHash = this->requestId();
                 std::any op = std::any{};
                 std::any opparamsVariable = this->handleOptionAndParams(
                     params, std::string("createOrderWs"), std::string("op"),
                     std::string("batch-orders"));
                 op = ::getValue(opparamsVariable, 0);
                 params = ::getValue(opparamsVariable, 1);
                 std::any args = this->createOrderRequest(
                     symbol, type, side, amount, price, params);
                 std::any market = this->market(symbol);
                 std::any instIdCode =
                     this->safeInteger(market, std::string("instIdCode"));
                 if (isTrue(!isEqual(instIdCode, std::any{}))) {
                   deleteKey(args, std::string("instId"));
                   ::setValue(args, std::string("instIdCode"), instIdCode);
                 }
                 std::any ordType =
                     this->safeString(args, std::string("ordType"));
                 if (isTrue(
                         isTrue(
                             isTrue(
                                 isTrue(isTrue(isTrue((isEqual(
                                                   ordType,
                                                   std::string("trigger")))) ||
                                               isTrue((isEqual(
                                                   ordType,
                                                   std::string(
                                                       "conditional"))))) ||
                                        isTrue((isEqual(
                                            type, std::string("oco"))))) ||
                                 isTrue((isEqual(
                                     type, std::string("move_order_stop"))))) ||
                             isTrue((isEqual(type, std::string("iceberg"))))) ||
                         isTrue((isEqual(type, std::string("twap")))))) {
                   throw BadRequest(toString(add(
                       this->id,
                       std::string(
                           " createOrderWs() does not support algo trading. "
                           "this.options[\"createOrderWs\"][\"op\"] must be "
                           "either order or batch-order"))));
                 }
                 if (isTrue(
                         isTrue((!isEqual(op, std::string("order")))) &&
                         isTrue((!isEqual(op, std::string("batch-orders")))))) {
                   throw BadRequest(toString(add(
                       this->id,
                       std::string(
                           " createOrderWs() does not support algo trading. "
                           "this.options[\"createOrderWs\"][\"op\"] must be "
                           "either order or privatePostTradeOrder or "
                           "privatePostTradeOrderAlgo"))));
                 }
                 std::any request = ccxt::dict{
                     {std::string("id"), messageHash},
                     {std::string("op"), op},
                     {std::string("args"), ccxt::list{args}},
                 };
                 return awaitValue(
                     this->watch(url, messageHash, request, messageHash));
               })
        .share();
  }

  virtual void handlePlaceOrders(std::any client, std::any message) {
    //
    //  batch-orders/order/cancel-order
    //    {
    //        "id": "1689281055",
    //        "op": "batch-orders",
    //        "code": "0",
    //        "msg": '',
    //        "data": [{
    //            "tag": "e847386590ce4dBC",
    //            "ordId": "599823446566084608",
    //            "clOrdId": "e847386590ce4dBCb939511604f394b0",
    //            "sCode": "0",
    //            "sMsg": "Order successfully placed."
    //        },
    //        ...
    //        ]
    //    }
    //
    std::any messageHash = this->safeString(message, std::string("id"));
    std::any args = this->safeValue(message, std::string("data"), ccxt::list{});
    // filter out partial errors
    args = this->filterBy(args, std::string("sCode"), std::string("0"));
    // if empty means request failed and handle error
    if (isTrue(this->isEmpty(args))) {
      std::any method = this->safeString(message, std::string("op"));
      std::any stringMsg = this->json(message);
      this->handleErrors(
          1, std::string(""), ::getValue(client, std::string("url")), method,
          ccxt::dict{}, stringMsg, message, ccxt::dict{}, ccxt::dict{});
    }
    std::any orders =
        this->parseOrders(args, std::any{}, std::any{}, std::any{});
    std::any first = this->safeDict(orders, 0, ccxt::dict{});
    this->resolve(first, messageHash);
  }

  /**
   * @method
   * @name okx#editOrderWs
   * @description edit a trade order
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-trade-ws-amend-order
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-trade-ws-amend-multiple-orders
   * @param {string} id order id
   * @param {string} symbol unified symbol of the market to create an order in
   * @param {string} type 'market' or 'limit'
   * @param {string} side 'buy' or 'sell'
   * @param {float} amount how much of the currency you want to trade in units
   * of the base currency
   * @param {float|undefined} [price] the price at which the order is to be
   * fulfilled, in units of the quote currency, ignored in market orders
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} an [order structure]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<std::any>
  editOrderWs(std::any id, std::any symbol, std::any type, std::any side,
              std::any amount = std::any{}, std::any price = std::any{},
              std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 awaitValue(this->authenticate());
                 std::any url = this->getUrl(std::string("private"),
                                             std::string("private"));
                 std::any messageHash = this->requestId();
                 std::any op = std::any{};
                 std::any opparamsVariable = this->handleOptionAndParams(
                     params, std::string("editOrderWs"), std::string("op"),
                     std::string("amend-order"));
                 op = ::getValue(opparamsVariable, 0);
                 params = ::getValue(opparamsVariable, 1);
                 std::any args = this->editOrderRequest(id, symbol, type, side,
                                                        amount, price, params);
                 std::any market = this->market(symbol);
                 std::any instIdCode =
                     this->safeInteger(market, std::string("instIdCode"));
                 if (isTrue(!isEqual(instIdCode, std::any{}))) {
                   deleteKey(args, std::string("instId"));
                   ::setValue(args, std::string("instIdCode"), instIdCode);
                 }
                 std::any request = ccxt::dict{
                     {std::string("id"), messageHash},
                     {std::string("op"), op},
                     {std::string("args"), ccxt::list{args}},
                 };
                 return awaitValue(this->watch(url, messageHash,
                                               this->extend(request, params),
                                               messageHash));
               })
        .share();
  }

  /**
   * @method
   * @name okx#cancelOrderWs
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-trade-ws-cancel-order
   * @description cancel multiple orders
   * @param {string} id order id
   * @param {string} symbol unified market symbol, default is undefined
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @param {string} [params.clOrdId] client order id
   * @returns {object} an list of [order structures]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<std::any>
  cancelOrderWs(std::any id, std::any symbol = std::any{},
                std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 if (isTrue(isEqual(symbol, std::any{}))) {
                   throw BadRequest(toString(add(
                       this->id,
                       std::string(
                           " cancelOrderWs() requires a symbol argument"))));
                 }
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 awaitValue(this->authenticate());
                 std::any url = this->getUrl(std::string("private"),
                                             std::string("private"));
                 std::any messageHash = this->requestId();
                 std::any clientOrderId =
                     this->safeString2(params, std::string("clOrdId"),
                                       std::string("clientOrderId"));
                 params =
                     this->omit(params, ccxt::list{std::string("clientOrderId"),
                                                   std::string("clOrdId")});
                 std::any market = this->market(symbol);
                 std::any instIdCode =
                     this->safeInteger(market, std::string("instIdCode"));
                 std::any arg = ccxt::dict{
                     {std::string("instIdCode"), instIdCode},
                 };
                 if (isTrue(!isEqual(clientOrderId, std::any{}))) {
                   ::setValue(arg, std::string("clOrdId"), clientOrderId);
                 } else {
                   ::setValue(arg, std::string("ordId"), id);
                 }
                 std::any request = ccxt::dict{
                     {std::string("id"), messageHash},
                     {std::string("op"), std::string("cancel-order")},
                     {std::string("args"),
                      ccxt::list{this->extend(arg, params)}},
                 };
                 return awaitValue(
                     this->watch(url, messageHash, request, messageHash));
               })
        .share();
  }

  /**
   * @method
   * @name okx#cancelOrdersWs
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-trade-ws-cancel-multiple-orders
   * @description cancel multiple orders
   * @param {string[]} ids order ids
   * @param {string} symbol unified market symbol, default is undefined
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object} an list of [order structures]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<std::any>
  cancelOrdersWs(std::any ids, std::any symbol = std::any{},
                 std::any params = ccxt::dict{}) override {
    return std::async(
               std::launch::deferred,
               [=]() mutable -> std::any {
                 std::any idsLength = getArrayLength(ids);
                 if (isTrue(isGreaterThan(idsLength, 20))) {
                   throw BadRequest(toString(
                       add(this->id, std::string(" cancelOrdersWs() accepts up "
                                                 "to 20 ids at a time"))));
                 }
                 if (isTrue(isEqual(symbol, std::any{}))) {
                   throw BadRequest(toString(add(
                       this->id,
                       std::string(
                           " cancelOrdersWs() requires a symbol argument"))));
                 }
                 if (isTrue(isEqual(this->markets, std::any{}))) {
                   awaitValue(this->loadMarkets());
                 }
                 awaitValue(this->authenticate());
                 std::any url = this->getUrl(std::string("private"),
                                             std::string("private"));
                 std::any messageHash = this->requestId();
                 std::any args = ccxt::list{};
                 std::any market = this->market(symbol);
                 std::any instIdCode =
                     this->safeInteger(market, std::string("instIdCode"));
                 std::any instParams = ccxt::dict{
                     {std::string("instIdCode"), instIdCode},
                 };
                 for (std::any i = 0; isLessThan(i, idsLength);
                      postFixIncrement(i)) {
                   std::any arg = this->extend(
                       instParams,
                       ccxt::dict{
                           {std::string("ordId"), ::getValue(ids, i)},
                       });
                   arrayPush(args, arg);
                 }
                 std::any request = ccxt::dict{
                     {std::string("id"), messageHash},
                     {std::string("op"), std::string("batch-cancel-orders")},
                     {std::string("args"), args},
                 };
                 return awaitValue(this->watch(
                     url, messageHash, this->deepExtend(request, params),
                     messageHash));
               })
        .share();
  }

  /**
   * @method
   * @name okx#cancelAllOrdersWs
   * @see
   * https://www.okx.com/docs-v5/en/#order-book-trading-trade-ws-mass-cancel-order
   * @description cancel all open orders of a type. Only applicable to Option in
   * Portfolio Margin mode, and MMP privilege is required.
   * @param {string} symbol unified market symbol, only orders in the market of
   * this symbol are cancelled when symbol is not undefined
   * @param {object} [params] extra parameters specific to the exchange API
   * endpoint
   * @returns {object[]} a list of [order structures]{@link
   * https://docs.ccxt.com/?id=order-structure}
   */
  std::shared_future<std::any>
  cancelAllOrdersWs(std::any symbol = std::any{},
                    std::any params = ccxt::dict{}) override {
    return std::
        async(std::launch::deferred,
              [=]() mutable -> std::any {
                if (isTrue(isEqual(symbol, std::any{}))) {
                  throw BadRequest(toString(add(
                      this->id,
                      std::string(
                          " cancelAllOrdersWs() requires a symbol argument"))));
                }
                if (isTrue(isEqual(this->markets, std::any{}))) {
                  awaitValue(this->loadMarkets());
                }
                awaitValue(this->authenticate());
                std::any market = this->market(symbol);
                if (isTrue(!isEqual(::getValue(market, std::string("type")),
                                    std::string("option")))) {
                  throw BadRequest(toString(
                      add(this->id,
                          std::string(" cancelAllOrdersWs is only applicable "
                                      "to Option in Portfolio Margin mode, and "
                                      "MMP privilege is required."))));
                }
                std::any url = this->getUrl(std::string("private"),
                                            std::string("private"));
                std::any messageHash = this->requestId();
                std::any request = ccxt::dict{
                    {std::string("id"), messageHash},
                    {std::string("op"), std::string("mass-cancel")},
                    {std::string("args"),
                     ccxt::list{this->extend(
                         ccxt::dict{
                             {std::string("instType"), std::string("OPTION")},
                             {std::string("instFamily"),
                              ::getValue(market, std::string("id"))},
                         },
                         params)}},
                };
                return awaitValue(
                    this->watch(url, messageHash, request, messageHash));
              })
            .share();
  }

  virtual void handleCancelAllOrders(std::any client, std::any message) {
    //
    //    {
    //        "id": "1512",
    //        "op": "mass-cancel",
    //        "data": [
    //            {
    //                "result": true
    //            }
    //        ],
    //        "code": "0",
    //        "msg": ""
    //    }
    //
    std::any messageHash = this->safeString(message, std::string("id"));
    std::any data = this->safeValue(message, std::string("data"), ccxt::list{});
    this->resolve(data, messageHash);
  }

  virtual std::any handleSubscriptionStatus(std::any client, std::any message) {
    //
    //     { event: 'subscribe', arg: { channel: "tickers", instId: "BTC-USDT" }
    //     }
    //
    // const channel = this.safeString (message, "channel");
    // ::getValue(client, std::string("subscriptions"))[channel] = message;
    return message;
  }

  virtual void handleAuthenticate(std::any client, std::any message) {
    //
    //     { event: "login", success: true }
    //
    std::any future =
        this->safeValue(::getValue(client, std::string("futures")),
                        std::string("authenticated"));
    ::wsFutureResolve(future, true);
  }

  std::any ping(std::any client) override {
    // OKX does not support the built-in WebSocket protocol-level ping-pong.
    // Instead, it requires a custom text-based ping-pong mechanism.
    return std::string("ping");
  }

  virtual std::any handlePong(std::any client, std::any message) {
    ::getValue(client, std::string("lastPong")) = this->milliseconds();
    return message;
  }

  virtual std::any handleErrorMessage(std::any client, std::any message) {
    //
    //     { event: 'error', msg: "Illegal request:
    //     {"op":"subscribe","args":["spot/ticker:BTC-USDT"]}", code: "60012" }
    //     { event: 'error", msg: "channel:ticker,instId:BTC-USDT doesn"t
    //     exist", code: "60018" }
    //     {"event":"error","msg":"Illegal request:
    //     {\\"id\\":\\"17321173472466905\\",\\"op\\":\\"amend-order\\",\\"args\\":[{\\"instId\\":\\"ETH-USDC\\",\\"ordId\\":\\"2000345622407479296\\",\\"newSz\\":\\"0.050857\\",\\"newPx\\":\\"2949.4\\",\\"postOnly\\":true}],\\"postOnly\\":true}","code":"60012","connId":"0808af6c"}
    //
    std::any errorCode = this->safeString(message, std::string("code"));
    try {
      if (isTrue(isTrue((isTrue(!isEqual(errorCode, std::any{})) &&
                         isTrue(!isEqual(errorCode, std::string(""))))) &&
                 isTrue(!isEqual(errorCode, std::string("0"))))) {
        std::any feedback =
            add(add(this->id, std::string(" ")), this->json(message));
        if (isTrue(!isEqual(errorCode, std::string("1")))) {
          this->throwExactlyMatchedException(
              ::getValue(this->exceptions, std::string("exact")), errorCode,
              feedback);
        }
        std::any messageString = this->safeValue(message, std::string("msg"));
        if (isTrue(!isEqual(messageString, std::any{}))) {
          this->throwBroadlyMatchedException(
              ::getValue(this->exceptions, std::string("broad")), messageString,
              feedback);
        } else {
          std::any data =
              this->safeList(message, std::string("data"), ccxt::list{});
          for (std::any i = 0; isLessThan(i, getArrayLength(data));
               postFixIncrement(i)) {
            std::any d = ::getValue(data, i);
            errorCode = this->safeString(d, std::string("sCode"));
            if (isTrue(!isEqual(errorCode, std::any{}))) {
              this->throwExactlyMatchedException(
                  ::getValue(this->exceptions, std::string("exact")), errorCode,
                  feedback);
            }
            messageString = this->safeValue(d, std::string("sMsg"));
            if (isTrue(!isEqual(messageString, std::any{}))) {
              this->throwBroadlyMatchedException(
                  ::getValue(this->exceptions, std::string("broad")),
                  messageString, feedback);
            }
          }
        }
        throw ExchangeError(toString(feedback));
      }
    } catch (const std::exception &e) {
      // if the message contains an id, it means it is a response to a request
      // so we only reject that promise, instead of deleting all futures,
      // destroying the authentication future
      std::any id = this->safeString(message, std::string("id"));
      if (isTrue(isEqual(id, std::any{}))) {
        // try to parse it from the stringified json inside msg
        std::any msg = this->safeString(message, std::string("msg"));
        if (isTrue(
                isTrue(!isEqual(msg, std::any{})) &&
                isTrue(startsWith(msg, std::string("Illegal request: {"))))) {
          std::any stringifiedJson =
              replace(msg, std::string("Illegal request: "), std::string(""));
          std::any parsedJson = this->parseJson(stringifiedJson);
          id = this->safeString(parsedJson, std::string("id"));
        }
      }
      if (isTrue(!isEqual(id, std::any{}))) {
        this->reject(e, id);
        return false;
      }
      this->reject(e);
      return false;
    }
    return true;
  }

  void handleMessage(std::any client, std::any message) override {
    if (isTrue(!isEqual(this->handleErrorMessage(client, message), true))) {
      return;
    }
    //
    //     { event: 'subscribe', arg: { channel: "tickers", instId: "BTC-USDT" }
    //     } { event: 'login", msg: '", code: "0" }
    //
    //     {
    //         "arg": { channel: "tickers", instId: "BTC-USDT" },
    //         "data": [
    //             {
    //                 "instType": "SPOT",
    //                 "instId": "BTC-USDT",
    //                 "last": "31500.1",
    //                 "lastSz": "0.00001754",
    //                 "askPx": "31500.1",
    //                 "askSz": "0.00998144",
    //                 "bidPx": "31500",
    //                 "bidSz": "3.05652439",
    //                 "open24h": "31697",
    //                 "high24h": "32248",
    //                 "low24h": "31165.6",
    //                 "sodUtc0": "31385.5",
    //                 "sodUtc8": "32134.9",
    //                 "volCcy24h": "503403597.38138519",
    //                 "vol24h": "15937.10781721",
    //                 "ts": "1626526618762"
    //             }
    //         ]
    //     }
    //
    //     { event: 'error', msg: "Illegal request:
    //     {"op":"subscribe","args":["spot/ticker:BTC-USDT"]}", code: "60012" }
    //     { event: 'error", msg: "channel:ticker,instId:BTC-USDT doesn"t
    //     exist", code: "60018" } { event: 'error', msg: "Invalid
    //     OK_ACCESS_KEY", code: "60005" }
    //     {
    //         "event": "error",
    //         "msg": "Illegal request:
    //         {"op":"login","args":["de89b035-b233-44b2-9a13-0ccdd00bda0e","7KUcc8YzQhnxBE3K","1626691289","H57N99mBt5NvW8U19FITrPdOxycAERFMaapQWRqLaSE="]}",
    //         "code": "60012"
    //     }
    //
    //
    //
    if (isTrue(isEqual(message, std::string("pong")))) {
      this->handlePong(client, message);
      return;
    }
    // const table = this.safeString (message, 'table');
    // if (table === undefined) {
    std::any event =
        this->safeString2(message, std::string("event"), std::string("op"));
    if (isTrue(!isEqual(event, std::any{}))) {
      std::any methods = ccxt::dict{
          {std::string("login"), std::string("handleAuthenticate")},
          {std::string("subscribe"), std::string("handleSubscriptionStatus")},
          {std::string("unsubscribe"), std::string("handleUnsubscription")},
          {std::string("order"), std::string("handlePlaceOrders")},
          {std::string("batch-orders"), std::string("handlePlaceOrders")},
          {std::string("amend-order"), std::string("handlePlaceOrders")},
          {std::string("batch-amend-orders"), std::string("handlePlaceOrders")},
          {std::string("cancel-order"), std::string("handlePlaceOrders")},
          {std::string("mass-cancel"), std::string("handleCancelAllOrders")},
      };
      std::any method = this->safeValue(methods, event);
      if (isTrue(!isEqual(method, std::any{}))) {
        this->dispatchMethodName(method, ccxt::list{client, message});
      }
    } else {
      std::any arg = this->safeValue(message, std::string("arg"), ccxt::dict{});
      std::any channel = this->safeString(arg, std::string("channel"));
      if (isTrue(isEqual(channel, std::any{}))) {
        return;
      }
      std::any methods = ccxt::dict{
          {std::string("bbo-tbt"), std::string("handleOrderBook")},
          {std::string("books"), std::string("handleOrderBook")},
          {std::string("books5"), std::string("handleOrderBook")},
          {std::string("books-rpi"), std::string("handleOrderBook")},
          {std::string("books50-l2-tbt"), std::string("handleOrderBook")},
          {std::string("books-l2-tbt"), std::string("handleOrderBook")},
          {std::string("tickers"), std::string("handleTicker")},
          {std::string("mark-price"), std::string("handleTicker")},
          {std::string("positions"), std::string("handlePositions")},
          {std::string("index-tickers"), std::string("handleTicker")},
          {std::string("sprd-tickers"), std::string("handleTicker")},
          {std::string("block-tickers"), std::string("handleTicker")},
          {std::string("trades"), std::string("handleTrades")},
          {std::string("trades-all"), std::string("handleTrades")},
          {std::string("account"), std::string("handleBalance")},
          {std::string("funding-rate"), std::string("handleFundingRate")},
          {std::string("orders"), std::string("handleOrders")},
          {std::string("orders-algo"), std::string("handleOrders")},
          {std::string("liquidation-orders"), std::string("handleLiquidation")},
          {std::string("balance_and_position"),
           std::string("handleBalanceAndPosition")},
      };
      std::any method = this->safeValue(methods, channel);
      if (isTrue(isEqual(method, std::any{}))) {
        if (isTrue(isEqual(getIndexOf(channel, std::string("candle")), 0))) {
          this->handleOHLCV(client, message);
        }
      } else {
        this->dispatchMethodName(method, ccxt::list{client, message});
      }
    }
  }

  virtual void handleUnSubscriptionTrades(std::any client, std::any symbol,
                                          std::any channel) {
    std::any subMessageHash = add(add(channel, std::string(":")), symbol);
    std::any messageHash = add(std::string("unsubscribe:"), subMessageHash);
    this->cleanUnsubscription(client, subMessageHash, messageHash);
    if (isTrue(inOp(this->trades, symbol))) {
      deleteKey(this->trades, symbol);
    }
  }

  virtual void handleUnsubscriptionOrderBook(std::any client, std::any symbol,
                                             std::any channel) {
    std::any subMessageHash = add(add(channel, std::string(":")), symbol);
    std::any messageHash = add(std::string("unsubscribe:orderbook:"), symbol);
    this->cleanUnsubscription(client, subMessageHash, messageHash);
    if (isTrue(inOp(this->orderbooks, symbol))) {
      deleteKey(this->orderbooks, symbol);
    }
  }

  virtual void handleUnsubscriptionOHLCV(std::any client, std::any symbol,
                                         std::any channel) {
    std::any tf = replace(channel, std::string("candle"), std::string(""));
    std::any timeframe = this->findTimeframe(tf);
    if (isTrue(isEqual(timeframe, std::any{}))) {
      return;
    }
    std::any subMessageHash =
        add(add(add(std::string("multi:"), channel), std::string(":")), symbol);
    std::any messageHash = add(std::string("unsubscribe:"), subMessageHash);
    this->cleanUnsubscription(client, subMessageHash, messageHash);
    if (isTrue(isTrue(isTrue((!isEqual(symbol, std::any{}))) &&
                      isTrue((!isEqual(timeframe, std::any{})))) &&
               isTrue((inOp(::getValue(this->ohlcvs, symbol), timeframe))))) {
      deleteKey(::getValue(this->ohlcvs, symbol), timeframe);
    }
  }

  virtual void handleUnsubscriptionTicker(std::any client, std::any symbol,
                                          std::any channel) {
    std::any subMessageHash = add(add(channel, std::string("::")), symbol);
    std::any messageHash = add(std::string("unsubscribe:ticker:"), symbol);
    this->cleanUnsubscription(client, subMessageHash, messageHash);
    if (isTrue(inOp(this->tickers, symbol))) {
      deleteKey(this->tickers, symbol);
    }
  }

  virtual void handleUnsubscription(std::any client, std::any message) {
    //
    // {
    //     "event": "unsubscribe",
    //     "arg": {
    //       "channel": "tickers",
    //       "instId": "LTC-USD-200327"
    //     },
    //     "connId": "a4d3ae55"
    // }
    // arg might be an array or list
    std::any arg = this->safeDict(message, std::string("arg"), ccxt::dict{});
    std::any channel =
        this->safeString(arg, std::string("channel"), std::string(""));
    std::any marketId = this->safeString(arg, std::string("instId"));
    std::any symbol = this->safeSymbol(marketId);
    if (isTrue(isTrue(isEqual(channel, std::string("trades"))) ||
               isTrue(isEqual(channel, std::string("trades-all"))))) {
      this->handleUnSubscriptionTrades(client, symbol, channel);
    } else if (isTrue(isTrue(startsWith(channel, std::string("bbo"))) ||
                      isTrue(startsWith(channel, std::string("book"))))) {
      this->handleUnsubscriptionOrderBook(client, symbol, channel);
    } else if (isTrue(isGreaterThan(getIndexOf(channel, std::string("tickers")),
                                    -1))) {
      this->handleUnsubscriptionTicker(client, symbol, channel);
    } else if (isTrue(startsWith(channel, std::string("candle")))) {
      this->handleUnsubscriptionOHLCV(client, symbol, channel);
    }
  }
  // GENERATED dispatch table - see createDispatchTable in
  // build/cppTranspiler.ts
  virtual std::any callMethod(std::any name, std::any args) override {
    const std::string which = ::toString(name).has_value()
                                  ? std::any_cast<std::string>(::toString(name))
                                  : std::string();
    const long count =
        ccxt::isList(args)
            ? static_cast<long>(std::any_cast<ccxt::list>(args).size())
            : 0;
    if (which == "describe") {
      if (true)
        return this->describe();
    }
    if (which == "getUrl") {
      if (count <= 1)
        return this->getUrl(::getValue(args, 0));
      if (count >= 2)
        return this->getUrl(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "subscribeMultiple") {
      if (count <= 2)
        return awaitValue(
            this->subscribeMultiple(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->subscribeMultiple(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->subscribeMultiple(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "subscribe") {
      if (count <= 4)
        return awaitValue(
            this->subscribe(::getValue(args, 0), ::getValue(args, 1),
                            ::getValue(args, 2), ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->subscribe(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "watchTrades") {
      if (count <= 1)
        return awaitValue(this->watchTrades(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->watchTrades(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchTrades(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->watchTrades(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "watchTradesForSymbols") {
      if (count <= 1)
        return awaitValue(this->watchTradesForSymbols(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->watchTradesForSymbols(::getValue(args, 0),
                                                      ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchTradesForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->watchTradesForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "unWatchTradesForSymbols") {
      if (count <= 1)
        return awaitValue(this->unWatchTradesForSymbols(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(this->unWatchTradesForSymbols(::getValue(args, 0),
                                                        ::getValue(args, 1)));
    }
    if (which == "unWatchTrades") {
      if (count <= 1)
        return awaitValue(this->unWatchTrades(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->unWatchTrades(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "handleTrades") {
      if (true) {
        this->handleTrades(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "watchFundingRate") {
      if (count <= 1)
        return awaitValue(this->watchFundingRate(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->watchFundingRate(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "watchFundingRates") {
      if (count <= 0)
        return awaitValue(this->watchFundingRates());
      if (count == 1)
        return awaitValue(this->watchFundingRates(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->watchFundingRates(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "handleFundingRate") {
      if (true) {
        this->handleFundingRate(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "watchTicker") {
      if (count <= 1)
        return awaitValue(this->watchTicker(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->watchTicker(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "unWatchTicker") {
      if (count <= 1)
        return awaitValue(this->unWatchTicker(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->unWatchTicker(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "watchTickers") {
      if (count <= 0)
        return awaitValue(this->watchTickers());
      if (count == 1)
        return awaitValue(this->watchTickers(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->watchTickers(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "watchMarkPrice") {
      if (count <= 1)
        return awaitValue(this->watchMarkPrice(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->watchMarkPrice(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "watchMarkPrices") {
      if (count <= 0)
        return awaitValue(this->watchMarkPrices());
      if (count == 1)
        return awaitValue(this->watchMarkPrices(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->watchMarkPrices(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "unWatchTickers") {
      if (count <= 0)
        return awaitValue(this->unWatchTickers());
      if (count == 1)
        return awaitValue(this->unWatchTickers(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->unWatchTickers(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "handleTicker") {
      if (true) {
        this->handleTicker(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "watchBidsAsks") {
      if (count <= 0)
        return awaitValue(this->watchBidsAsks());
      if (count == 1)
        return awaitValue(this->watchBidsAsks(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->watchBidsAsks(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "handleBidAsk") {
      if (true) {
        this->handleBidAsk(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "parseWsBidAsk") {
      if (count <= 1)
        return this->parseWsBidAsk(::getValue(args, 0));
      if (count >= 2)
        return this->parseWsBidAsk(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "watchLiquidationsForSymbols") {
      if (count <= 1)
        return awaitValue(
            this->watchLiquidationsForSymbols(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->watchLiquidationsForSymbols(
            ::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchLiquidationsForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->watchLiquidationsForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "handleLiquidation") {
      if (true) {
        this->handleLiquidation(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "watchMyLiquidationsForSymbols") {
      if (count <= 1)
        return awaitValue(
            this->watchMyLiquidationsForSymbols(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->watchMyLiquidationsForSymbols(
            ::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchMyLiquidationsForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->watchMyLiquidationsForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "handleMyLiquidation") {
      if (true) {
        this->handleMyLiquidation(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "parseWsMyLiquidation") {
      if (count <= 1)
        return this->parseWsMyLiquidation(::getValue(args, 0));
      if (count >= 2)
        return this->parseWsMyLiquidation(::getValue(args, 0),
                                          ::getValue(args, 1));
    }
    if (which == "parseWsLiquidation") {
      if (count <= 1)
        return this->parseWsLiquidation(::getValue(args, 0));
      if (count >= 2)
        return this->parseWsLiquidation(::getValue(args, 0),
                                        ::getValue(args, 1));
    }
    if (which == "watchOHLCV") {
      if (count <= 1)
        return awaitValue(this->watchOHLCV(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->watchOHLCV(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(
            this->watchOHLCV(::getValue(args, 0), ::getValue(args, 1),
                             ::getValue(args, 2), ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->watchOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "unWatchOHLCV") {
      if (count <= 1)
        return awaitValue(this->unWatchOHLCV(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->unWatchOHLCV(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->unWatchOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "watchOHLCVForSymbols") {
      if (count <= 1)
        return awaitValue(this->watchOHLCVForSymbols(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->watchOHLCVForSymbols(::getValue(args, 0),
                                                     ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchOHLCVForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->watchOHLCVForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "unWatchOHLCVForSymbols") {
      if (count <= 1)
        return awaitValue(this->unWatchOHLCVForSymbols(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(this->unWatchOHLCVForSymbols(::getValue(args, 0),
                                                       ::getValue(args, 1)));
    }
    if (which == "handleOHLCV") {
      if (true) {
        this->handleOHLCV(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "watchOrderBook") {
      if (count <= 1)
        return awaitValue(this->watchOrderBook(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->watchOrderBook(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->watchOrderBook(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "watchOrderBookForSymbols") {
      if (count <= 1)
        return awaitValue(this->watchOrderBookForSymbols(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->watchOrderBookForSymbols(::getValue(args, 0),
                                                         ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->watchOrderBookForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "unWatchOrderBookForSymbols") {
      if (count <= 1)
        return awaitValue(
            this->unWatchOrderBookForSymbols(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(this->unWatchOrderBookForSymbols(
            ::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "unWatchOrderBook") {
      if (count <= 1)
        return awaitValue(this->unWatchOrderBook(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->unWatchOrderBook(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "handleDelta") {
      if (true) {
        this->handleDelta(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "handleDeltas") {
      if (true) {
        this->handleDeltas(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "handleOrderBookMessage") {
      if (count <= 4)
        return this->handleOrderBookMessage(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->handleOrderBookMessage(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "handleOrderBook") {
      if (true)
        return this->handleOrderBook(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "authenticate") {
      if (count <= 0)
        return awaitValue(this->authenticate());
      if (count >= 1)
        return awaitValue(this->authenticate(::getValue(args, 0)));
    }
    if (which == "watchBalance") {
      if (count <= 0)
        return awaitValue(this->watchBalance());
      if (count >= 1)
        return awaitValue(this->watchBalance(::getValue(args, 0)));
    }
    if (which == "handleBalanceAndPosition") {
      if (true) {
        this->handleBalanceAndPosition(::getValue(args, 0),
                                       ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "handleBalance") {
      if (true) {
        this->handleBalance(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "orderToTrade") {
      if (count <= 1)
        return this->orderToTrade(::getValue(args, 0));
      if (count >= 2)
        return this->orderToTrade(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "watchMyTrades") {
      if (count <= 0)
        return awaitValue(this->watchMyTrades());
      if (count == 1)
        return awaitValue(this->watchMyTrades(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->watchMyTrades(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchMyTrades(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->watchMyTrades(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "watchPositions") {
      if (count <= 0)
        return awaitValue(this->watchPositions());
      if (count == 1)
        return awaitValue(this->watchPositions(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->watchPositions(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchPositions(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->watchPositions(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "handlePositions") {
      if (true) {
        this->handlePositions(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "watchOrders") {
      if (count <= 0)
        return awaitValue(this->watchOrders());
      if (count == 1)
        return awaitValue(this->watchOrders(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->watchOrders(::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(
            this->watchOrders(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2), ::getValue(args, 3)));
    }
    if (which == "handleOrders") {
      if (true) {
        this->handleOrders(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "handleMyTrades") {
      if (true) {
        this->handleMyTrades(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "requestId") {
      if (true)
        return this->requestId();
    }
    if (which == "createOrderWs") {
      if (count <= 4)
        return awaitValue(
            this->createOrderWs(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2), ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->createOrderWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count >= 6)
        return awaitValue(this->createOrderWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
    }
    if (which == "handlePlaceOrders") {
      if (true) {
        this->handlePlaceOrders(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "editOrderWs") {
      if (count <= 4)
        return awaitValue(
            this->editOrderWs(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2), ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->editOrderWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->editOrderWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count >= 7)
        return awaitValue(this->editOrderWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
    }
    if (which == "cancelOrderWs") {
      if (count <= 1)
        return awaitValue(this->cancelOrderWs(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->cancelOrderWs(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->cancelOrderWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "cancelOrdersWs") {
      if (count <= 1)
        return awaitValue(this->cancelOrdersWs(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(
            this->cancelOrdersWs(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->cancelOrdersWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "cancelAllOrdersWs") {
      if (count <= 0)
        return awaitValue(this->cancelAllOrdersWs());
      if (count == 1)
        return awaitValue(this->cancelAllOrdersWs(::getValue(args, 0)));
      if (count >= 2)
        return awaitValue(
            this->cancelAllOrdersWs(::getValue(args, 0), ::getValue(args, 1)));
    }
    if (which == "handleCancelAllOrders") {
      if (true) {
        this->handleCancelAllOrders(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "handleSubscriptionStatus") {
      if (true)
        return this->handleSubscriptionStatus(::getValue(args, 0),
                                              ::getValue(args, 1));
    }
    if (which == "handleAuthenticate") {
      if (true) {
        this->handleAuthenticate(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "ping") {
      if (true)
        return this->ping(::getValue(args, 0));
    }
    if (which == "handlePong") {
      if (true)
        return this->handlePong(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "handleErrorMessage") {
      if (true)
        return this->handleErrorMessage(::getValue(args, 0),
                                        ::getValue(args, 1));
    }
    if (which == "handleMessage") {
      if (true) {
        this->handleMessage(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "handleUnSubscriptionTrades") {
      if (true) {
        this->handleUnSubscriptionTrades(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return std::any{};
      }
    }
    if (which == "handleUnsubscriptionOrderBook") {
      if (true) {
        this->handleUnsubscriptionOrderBook(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return std::any{};
      }
    }
    if (which == "handleUnsubscriptionOHLCV") {
      if (true) {
        this->handleUnsubscriptionOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return std::any{};
      }
    }
    if (which == "handleUnsubscriptionTicker") {
      if (true) {
        this->handleUnsubscriptionTicker(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return std::any{};
      }
    }
    if (which == "handleUnsubscription") {
      if (true) {
        this->handleUnsubscription(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "cleanRestData") {
      if (true) {
        this->cleanRestData();
        return std::any{};
      }
    }
    if (which == "cleanWsData") {
      if (true) {
        this->cleanWsData();
        return std::any{};
      }
    }
    if (which == "safeBoolN") {
      if (count <= 2)
        return this->safeBoolN(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeBoolN(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2));
    }
    if (which == "safeBool2") {
      if (count <= 3)
        return this->safeBool2(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2));
      if (count >= 4)
        return this->safeBool2(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "safeBool") {
      if (count <= 2)
        return this->safeBool(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeBool(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2));
    }
    if (which == "safeDictN") {
      if (count <= 2)
        return this->safeDictN(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeDictN(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2));
    }
    if (which == "safeDict") {
      if (count <= 2)
        return this->safeDict(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeDict(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2));
    }
    if (which == "safeDict2") {
      if (count <= 3)
        return this->safeDict2(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2));
      if (count >= 4)
        return this->safeDict2(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "safeListN") {
      if (count <= 2)
        return this->safeListN(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeListN(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2));
    }
    if (which == "safeList2") {
      if (count <= 3)
        return this->safeList2(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2));
      if (count >= 4)
        return this->safeList2(::getValue(args, 0), ::getValue(args, 1),
                               ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "safeList") {
      if (count <= 2)
        return this->safeList(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeList(::getValue(args, 0), ::getValue(args, 1),
                              ::getValue(args, 2));
    }
    if (which == "storeByKey") {
      if (true) {
        this->storeByKey(::getValue(args, 0), ::getValue(args, 1),
                         ::getValue(args, 2));
        return std::any{};
      }
    }
    if (which == "handleDeltasWithKeys") {
      if (count <= 2) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
      if (count == 3) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
        return std::any{};
      }
      if (count == 4) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
        return std::any{};
      }
      if (count >= 5) {
        this->handleDeltasWithKeys(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3),
                                   ::getValue(args, 4));
        return std::any{};
      }
    }
    if (which == "getCacheIndex") {
      if (true)
        return this->getCacheIndex(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "arraysConcat") {
      if (true)
        return this->arraysConcat(::getValue(args, 0));
    }
    if (which == "findTimeframe") {
      if (count <= 1)
        return this->findTimeframe(::getValue(args, 0));
      if (count >= 2)
        return this->findTimeframe(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "checkProxyUrlSettings") {
      if (count <= 0)
        return this->checkProxyUrlSettings();
      if (count == 1)
        return this->checkProxyUrlSettings(::getValue(args, 0));
      if (count == 2)
        return this->checkProxyUrlSettings(::getValue(args, 0),
                                           ::getValue(args, 1));
      if (count == 3)
        return this->checkProxyUrlSettings(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->checkProxyUrlSettings(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "urlEncoderForProxyUrl") {
      if (true)
        return this->urlEncoderForProxyUrl(::getValue(args, 0));
    }
    if (which == "checkProxySettings") {
      if (count <= 0)
        return this->checkProxySettings();
      if (count == 1)
        return this->checkProxySettings(::getValue(args, 0));
      if (count == 2)
        return this->checkProxySettings(::getValue(args, 0),
                                        ::getValue(args, 1));
      if (count == 3)
        return this->checkProxySettings(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->checkProxySettings(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "checkWsProxySettings") {
      if (true)
        return this->checkWsProxySettings();
    }
    if (which == "checkConflictingProxies") {
      if (true) {
        this->checkConflictingProxies(::getValue(args, 0), ::getValue(args, 1));
        return std::any{};
      }
    }
    if (which == "checkAddress") {
      if (count <= 0)
        return this->checkAddress();
      if (count >= 1)
        return this->checkAddress(::getValue(args, 0));
    }
    if (which == "findMessageHashes") {
      if (true)
        return this->findMessageHashes(::getValue(args, 0),
                                       ::getValue(args, 1));
    }
    if (which == "filterByLimit") {
      if (count <= 1)
        return this->filterByLimit(::getValue(args, 0));
      if (count == 2)
        return this->filterByLimit(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->filterByLimit(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
      if (count >= 4)
        return this->filterByLimit(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "filterBySinceLimit") {
      if (count <= 1)
        return this->filterBySinceLimit(::getValue(args, 0));
      if (count == 2)
        return this->filterBySinceLimit(::getValue(args, 0),
                                        ::getValue(args, 1));
      if (count == 3)
        return this->filterBySinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->filterBySinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->filterBySinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "filterByValueSinceLimit") {
      if (count <= 2)
        return this->filterByValueSinceLimit(::getValue(args, 0),
                                             ::getValue(args, 1));
      if (count == 3)
        return this->filterByValueSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->filterByValueSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count == 5)
        return this->filterByValueSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
      if (count == 6)
        return this->filterByValueSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5));
      if (count >= 7)
        return this->filterByValueSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6));
    }
    if (which == "setSandboxMode") {
      if (true) {
        this->setSandboxMode(::getValue(args, 0));
        return std::any{};
      }
    }
    if (which == "enableDemoTrading") {
      if (true) {
        this->enableDemoTrading(::getValue(args, 0));
        return std::any{};
      }
    }
    if (which == "sign") {
      if (count <= 1)
        return this->sign(::getValue(args, 0));
      if (count == 2)
        return this->sign(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2));
      if (count == 4)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3));
      if (count == 5)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3),
                          ::getValue(args, 4));
      if (count >= 6)
        return this->sign(::getValue(args, 0), ::getValue(args, 1),
                          ::getValue(args, 2), ::getValue(args, 3),
                          ::getValue(args, 4), ::getValue(args, 5));
    }
    if (which == "fetchTime") {
      if (count <= 0)
        return awaitValue(this->fetchTime());
      if (count >= 1)
        return awaitValue(this->fetchTime(::getValue(args, 0)));
    }
    if (which == "parseCurrency") {
      if (true)
        return this->parseCurrency(::getValue(args, 0));
    }
    if (which == "parseCurrencies") {
      if (true)
        return this->parseCurrencies(::getValue(args, 0));
    }
    if (which == "parseMarket") {
      if (true)
        return this->parseMarket(::getValue(args, 0));
    }
    if (which == "parseMarkets") {
      if (true)
        return this->parseMarkets(::getValue(args, 0));
    }
    if (which == "parseTicker") {
      if (count <= 1)
        return this->parseTicker(::getValue(args, 0));
      if (count >= 2)
        return this->parseTicker(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseDepositAddress") {
      if (count <= 1)
        return this->parseDepositAddress(::getValue(args, 0));
      if (count >= 2)
        return this->parseDepositAddress(::getValue(args, 0),
                                         ::getValue(args, 1));
    }
    if (which == "parseTrade") {
      if (count <= 1)
        return this->parseTrade(::getValue(args, 0));
      if (count >= 2)
        return this->parseTrade(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseTransaction") {
      if (count <= 1)
        return this->parseTransaction(::getValue(args, 0));
      if (count == 2)
        return this->parseTransaction(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseTransaction(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2));
      if (count >= 4)
        return this->parseTransaction(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "parseTransfer") {
      if (count <= 1)
        return this->parseTransfer(::getValue(args, 0));
      if (count >= 2)
        return this->parseTransfer(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseAccount") {
      if (true)
        return this->parseAccount(::getValue(args, 0));
    }
    if (which == "parseLedgerEntry") {
      if (count <= 1)
        return this->parseLedgerEntry(::getValue(args, 0));
      if (count >= 2)
        return this->parseLedgerEntry(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseOrder") {
      if (count <= 1)
        return this->parseOrder(::getValue(args, 0));
      if (count >= 2)
        return this->parseOrder(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseMarketLeverageTiers") {
      if (count <= 1)
        return this->parseMarketLeverageTiers(::getValue(args, 0));
      if (count >= 2)
        return this->parseMarketLeverageTiers(::getValue(args, 0),
                                              ::getValue(args, 1));
    }
    if (which == "parsePosition") {
      if (count <= 1)
        return this->parsePosition(::getValue(args, 0));
      if (count >= 2)
        return this->parsePosition(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseFundingRateHistory") {
      if (count <= 1)
        return this->parseFundingRateHistory(::getValue(args, 0));
      if (count >= 2)
        return this->parseFundingRateHistory(::getValue(args, 0),
                                             ::getValue(args, 1));
    }
    if (which == "parseBorrowInterest") {
      if (count <= 1)
        return this->parseBorrowInterest(::getValue(args, 0));
      if (count >= 2)
        return this->parseBorrowInterest(::getValue(args, 0),
                                         ::getValue(args, 1));
    }
    if (which == "parseIsolatedBorrowRate") {
      if (count <= 1)
        return this->parseIsolatedBorrowRate(::getValue(args, 0));
      if (count >= 2)
        return this->parseIsolatedBorrowRate(::getValue(args, 0),
                                             ::getValue(args, 1));
    }
    if (which == "parseWsTrade") {
      if (count <= 1)
        return this->parseWsTrade(::getValue(args, 0));
      if (count >= 2)
        return this->parseWsTrade(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseWsOrder") {
      if (count <= 1)
        return this->parseWsOrder(::getValue(args, 0));
      if (count >= 2)
        return this->parseWsOrder(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseWsOrderTrade") {
      if (count <= 1)
        return this->parseWsOrderTrade(::getValue(args, 0));
      if (count >= 2)
        return this->parseWsOrderTrade(::getValue(args, 0),
                                       ::getValue(args, 1));
    }
    if (which == "parseWsOHLCV") {
      if (count <= 1)
        return this->parseWsOHLCV(::getValue(args, 0));
      if (count >= 2)
        return this->parseWsOHLCV(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "transfer") {
      if (count <= 4)
        return awaitValue(
            this->transfer(::getValue(args, 0), ::getValue(args, 1),
                           ::getValue(args, 2), ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->transfer(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "withdraw") {
      if (count <= 3)
        return awaitValue(this->withdraw(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(
            this->withdraw(::getValue(args, 0), ::getValue(args, 1),
                           ::getValue(args, 2), ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->withdraw(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "addMargin") {
      if (count <= 2)
        return awaitValue(
            this->addMargin(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->addMargin(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "setMargin") {
      if (count <= 2)
        return awaitValue(
            this->setMargin(::getValue(args, 0), ::getValue(args, 1)));
      if (count >= 3)
        return awaitValue(this->setMargin(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
    }
    if (which == "fetchLongShortRatioHistory") {
      if (count <= 0)
        return awaitValue(this->fetchLongShortRatioHistory());
      if (count == 1)
        return awaitValue(
            this->fetchLongShortRatioHistory(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchLongShortRatioHistory(
            ::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchLongShortRatioHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchLongShortRatioHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->fetchLongShortRatioHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "fetchMarginAdjustmentHistory") {
      if (count <= 0)
        return awaitValue(this->fetchMarginAdjustmentHistory());
      if (count == 1)
        return awaitValue(
            this->fetchMarginAdjustmentHistory(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchMarginAdjustmentHistory(
            ::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchMarginAdjustmentHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchMarginAdjustmentHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->fetchMarginAdjustmentHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "fetchOpenInterestHistory") {
      if (count <= 1)
        return awaitValue(this->fetchOpenInterestHistory(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchOpenInterestHistory(::getValue(args, 0),
                                                         ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchOpenInterestHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchOpenInterestHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count >= 5)
        return awaitValue(this->fetchOpenInterestHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
    }
    if (which == "signIn") {
      if (count <= 0)
        return awaitValue(this->signIn());
      if (count >= 1)
        return awaitValue(this->signIn(::getValue(args, 0)));
    }
    if (which == "parseToInt") {
      if (true)
        return this->parseToInt(::getValue(args, 0));
    }
    if (which == "parseToNumeric") {
      if (true)
        return this->parseToNumeric(::getValue(args, 0));
    }
    if (which == "isRoundNumber") {
      if (true)
        return this->isRoundNumber(::getValue(args, 0));
    }
    if (which == "isEmptyString") {
      if (true)
        return this->isEmptyString(::getValue(args, 0));
    }
    if (which == "safeNumberOmitZero") {
      if (count <= 2)
        return this->safeNumberOmitZero(::getValue(args, 0),
                                        ::getValue(args, 1));
      if (count >= 3)
        return this->safeNumberOmitZero(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "safeIntegerOmitZero") {
      if (count <= 2)
        return this->safeIntegerOmitZero(::getValue(args, 0),
                                         ::getValue(args, 1));
      if (count >= 3)
        return this->safeIntegerOmitZero(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "afterConstruct") {
      if (true) {
        this->afterConstruct();
        return std::any{};
      }
    }
    if (which == "initRestRateLimiter") {
      if (true) {
        this->initRestRateLimiter();
        return std::any{};
      }
    }
    if (which == "featuresGenerator") {
      if (true) {
        this->featuresGenerator();
        return std::any{};
      }
    }
    if (which == "featuresMapper") {
      if (count <= 2)
        return this->featuresMapper(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->featuresMapper(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
    }
    if (which == "featureValue") {
      if (count <= 1)
        return this->featureValue(::getValue(args, 0));
      if (count == 2)
        return this->featureValue(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->featureValue(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2));
      if (count >= 4)
        return this->featureValue(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "featureValueByType") {
      if (count <= 2)
        return this->featureValueByType(::getValue(args, 0),
                                        ::getValue(args, 1));
      if (count == 3)
        return this->featureValueByType(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->featureValueByType(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->featureValueByType(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "orderbookChecksumMessage") {
      if (true)
        return this->orderbookChecksumMessage(::getValue(args, 0));
    }
    if (which == "createNetworksByIdObject") {
      if (true) {
        this->createNetworksByIdObject();
        return std::any{};
      }
    }
    if (which == "getDefaultOptions") {
      if (true)
        return this->getDefaultOptions();
    }
    if (which == "safeLedgerEntry") {
      if (count <= 1)
        return this->safeLedgerEntry(::getValue(args, 0));
      if (count >= 2)
        return this->safeLedgerEntry(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "safeCurrencyStructure") {
      if (true)
        return this->safeCurrencyStructure(::getValue(args, 0));
    }
    if (which == "safeMarketStructure") {
      if (count <= 0)
        return this->safeMarketStructure();
      if (count >= 1)
        return this->safeMarketStructure(::getValue(args, 0));
    }
    if (which == "setMarkets") {
      if (count <= 1)
        return this->setMarkets(::getValue(args, 0));
      if (count >= 2)
        return this->setMarkets(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "setMarketsFromExchange") {
      if (true)
        return this->setMarketsFromExchange(::getValue(args, 0));
    }
    if (which == "getDescribeForExtendedWsExchange") {
      if (true)
        return this->getDescribeForExtendedWsExchange(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "safeBalance") {
      if (true)
        return this->safeBalance(::getValue(args, 0));
    }
    if (which == "safeOrder") {
      if (count <= 1)
        return this->safeOrder(::getValue(args, 0));
      if (count >= 2)
        return this->safeOrder(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseOrders") {
      if (count <= 1)
        return this->parseOrders(::getValue(args, 0));
      if (count == 2)
        return this->parseOrders(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseOrders(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
      if (count == 4)
        return this->parseOrders(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3));
      if (count >= 5)
        return this->parseOrders(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3),
                                 ::getValue(args, 4));
    }
    if (which == "calculateFeeWithRate") {
      if (count <= 5)
        return this->calculateFeeWithRate(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
      if (count == 6)
        return this->calculateFeeWithRate(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5));
      if (count == 7)
        return this->calculateFeeWithRate(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6));
      if (count >= 8)
        return this->calculateFeeWithRate(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7));
    }
    if (which == "calculateFee") {
      if (count <= 5)
        return this->calculateFee(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3),
                                  ::getValue(args, 4));
      if (count == 6)
        return this->calculateFee(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3),
                                  ::getValue(args, 4), ::getValue(args, 5));
      if (count >= 7)
        return this->calculateFee(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3),
                                  ::getValue(args, 4), ::getValue(args, 5),
                                  ::getValue(args, 6));
    }
    if (which == "safeLiquidation") {
      if (count <= 1)
        return this->safeLiquidation(::getValue(args, 0));
      if (count >= 2)
        return this->safeLiquidation(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "safeTrade") {
      if (count <= 1)
        return this->safeTrade(::getValue(args, 0));
      if (count >= 2)
        return this->safeTrade(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "createCcxtTradeId") {
      if (count <= 0)
        return this->createCcxtTradeId();
      if (count == 1)
        return this->createCcxtTradeId(::getValue(args, 0));
      if (count == 2)
        return this->createCcxtTradeId(::getValue(args, 0),
                                       ::getValue(args, 1));
      if (count == 3)
        return this->createCcxtTradeId(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2));
      if (count == 4)
        return this->createCcxtTradeId(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2),
                                       ::getValue(args, 3));
      if (count >= 5)
        return this->createCcxtTradeId(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2), ::getValue(args, 3),
                                       ::getValue(args, 4));
    }
    if (which == "parsedFeeAndFees") {
      if (true)
        return this->parsedFeeAndFees(::getValue(args, 0));
    }
    if (which == "parseFeeNumeric") {
      if (true)
        return this->parseFeeNumeric(::getValue(args, 0));
    }
    if (which == "findNearestCeiling") {
      if (true)
        return this->findNearestCeiling(::getValue(args, 0),
                                        ::getValue(args, 1));
    }
    if (which == "addKeyInArrayItems") {
      if (true)
        return this->addKeyInArrayItems(::getValue(args, 0),
                                        ::getValue(args, 1));
    }
    if (which == "invertFlatStringDictionary") {
      if (true)
        return this->invertFlatStringDictionary(::getValue(args, 0));
    }
    if (which == "stringToBase16") {
      if (true)
        return this->stringToBase16(::getValue(args, 0));
    }
    if (which == "reduceFeesByCurrency") {
      if (true)
        return this->reduceFeesByCurrency(::getValue(args, 0));
    }
    if (which == "safeTicker") {
      if (count <= 1)
        return this->safeTicker(::getValue(args, 0));
      if (count >= 2)
        return this->safeTicker(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "convertTradingViewToOHLCV") {
      if (count <= 1)
        return this->convertTradingViewToOHLCV(::getValue(args, 0));
      if (count == 2)
        return this->convertTradingViewToOHLCV(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count == 3)
        return this->convertTradingViewToOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->convertTradingViewToOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count == 5)
        return this->convertTradingViewToOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
      if (count == 6)
        return this->convertTradingViewToOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5));
      if (count == 7)
        return this->convertTradingViewToOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6));
      if (count >= 8)
        return this->convertTradingViewToOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7));
    }
    if (which == "convertOHLCVToTradingView") {
      if (count <= 1)
        return this->convertOHLCVToTradingView(::getValue(args, 0));
      if (count == 2)
        return this->convertOHLCVToTradingView(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count == 3)
        return this->convertOHLCVToTradingView(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->convertOHLCVToTradingView(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count == 5)
        return this->convertOHLCVToTradingView(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
      if (count == 6)
        return this->convertOHLCVToTradingView(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5));
      if (count == 7)
        return this->convertOHLCVToTradingView(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6));
      if (count >= 8)
        return this->convertOHLCVToTradingView(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7));
    }
    if (which == "marketIds") {
      if (count <= 0)
        return this->marketIds();
      if (count >= 1)
        return this->marketIds(::getValue(args, 0));
    }
    if (which == "currencyIds") {
      if (count <= 0)
        return this->currencyIds();
      if (count >= 1)
        return this->currencyIds(::getValue(args, 0));
    }
    if (which == "marketsForSymbols") {
      if (count <= 0)
        return this->marketsForSymbols();
      if (count >= 1)
        return this->marketsForSymbols(::getValue(args, 0));
    }
    if (which == "marketSymbols") {
      if (count <= 0)
        return this->marketSymbols();
      if (count == 1)
        return this->marketSymbols(::getValue(args, 0));
      if (count == 2)
        return this->marketSymbols(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->marketSymbols(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
      if (count == 4)
        return this->marketSymbols(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
      if (count >= 5)
        return this->marketSymbols(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3),
                                   ::getValue(args, 4));
    }
    if (which == "marketCodes") {
      if (count <= 0)
        return this->marketCodes();
      if (count >= 1)
        return this->marketCodes(::getValue(args, 0));
    }
    if (which == "parseOrderBookBidsAsks") {
      if (count <= 1)
        return this->parseOrderBookBidsAsks(::getValue(args, 0));
      if (count == 2)
        return this->parseOrderBookBidsAsks(::getValue(args, 0),
                                            ::getValue(args, 1));
      if (count == 3)
        return this->parseOrderBookBidsAsks(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->parseOrderBookBidsAsks(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "filterByKey") {
      if (count <= 2)
        return this->filterByKey(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->filterByKey(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
    }
    if (which == "filterBySymbol") {
      if (count <= 1)
        return this->filterBySymbol(::getValue(args, 0));
      if (count >= 2)
        return this->filterBySymbol(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseOHLCV") {
      if (count <= 1)
        return this->parseOHLCV(::getValue(args, 0));
      if (count >= 2)
        return this->parseOHLCV(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "safeNetwork") {
      if (true)
        return this->safeNetwork(::getValue(args, 0));
    }
    if (which == "prioritizedNetworkAliases") {
      if (count <= 0)
        return this->prioritizedNetworkAliases();
      if (count == 1)
        return this->prioritizedNetworkAliases(::getValue(args, 0));
      if (count == 2)
        return this->prioritizedNetworkAliases(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count >= 3)
        return this->prioritizedNetworkAliases(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "networkCodeToId") {
      if (count <= 1)
        return this->networkCodeToId(::getValue(args, 0));
      if (count >= 2)
        return this->networkCodeToId(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "networkIdToCode") {
      if (count <= 0)
        return this->networkIdToCode();
      if (count == 1)
        return this->networkIdToCode(::getValue(args, 0));
      if (count >= 2)
        return this->networkIdToCode(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "handleNetworkCodeAndParams") {
      if (true)
        return this->handleNetworkCodeAndParams(::getValue(args, 0));
    }
    if (which == "defaultNetworkCode") {
      if (true)
        return this->defaultNetworkCode(::getValue(args, 0));
    }
    if (which == "safeNumber2") {
      if (count <= 3)
        return this->safeNumber2(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
      if (count >= 4)
        return this->safeNumber2(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "parseOrderBook") {
      if (count <= 2)
        return this->parseOrderBook(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseOrderBook(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
      if (count == 4)
        return this->parseOrderBook(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3));
      if (count == 5)
        return this->parseOrderBook(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3),
                                    ::getValue(args, 4));
      if (count == 6)
        return this->parseOrderBook(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3),
                                    ::getValue(args, 4), ::getValue(args, 5));
      if (count == 7)
        return this->parseOrderBook(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3),
                                    ::getValue(args, 4), ::getValue(args, 5),
                                    ::getValue(args, 6));
      if (count >= 8)
        return this->parseOrderBook(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3),
                                    ::getValue(args, 4), ::getValue(args, 5),
                                    ::getValue(args, 6), ::getValue(args, 7));
    }
    if (which == "parseOHLCVs") {
      if (count <= 1)
        return this->parseOHLCVs(::getValue(args, 0));
      if (count == 2)
        return this->parseOHLCVs(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseOHLCVs(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
      if (count == 4)
        return this->parseOHLCVs(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3));
      if (count == 5)
        return this->parseOHLCVs(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3),
                                 ::getValue(args, 4));
      if (count >= 6)
        return this->parseOHLCVs(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3),
                                 ::getValue(args, 4), ::getValue(args, 5));
    }
    if (which == "parseLeverageTiers") {
      if (count <= 1)
        return this->parseLeverageTiers(::getValue(args, 0));
      if (count == 2)
        return this->parseLeverageTiers(::getValue(args, 0),
                                        ::getValue(args, 1));
      if (count >= 3)
        return this->parseLeverageTiers(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "safePosition") {
      if (true)
        return this->safePosition(::getValue(args, 0));
    }
    if (which == "parsePositions") {
      if (count <= 1)
        return this->parsePositions(::getValue(args, 0));
      if (count == 2)
        return this->parsePositions(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parsePositions(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
    }
    if (which == "parseADLRank") {
      if (count <= 1)
        return this->parseADLRank(::getValue(args, 0));
      if (count >= 2)
        return this->parseADLRank(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseADLRanks") {
      if (count <= 1)
        return this->parseADLRanks(::getValue(args, 0));
      if (count == 2)
        return this->parseADLRanks(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parseADLRanks(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
    }
    if (which == "parseAccounts") {
      if (count <= 1)
        return this->parseAccounts(::getValue(args, 0));
      if (count >= 2)
        return this->parseAccounts(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseTradesHelper") {
      if (count <= 2)
        return this->parseTradesHelper(::getValue(args, 0),
                                       ::getValue(args, 1));
      if (count == 3)
        return this->parseTradesHelper(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2));
      if (count == 4)
        return this->parseTradesHelper(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2),
                                       ::getValue(args, 3));
      if (count == 5)
        return this->parseTradesHelper(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2), ::getValue(args, 3),
                                       ::getValue(args, 4));
      if (count >= 6)
        return this->parseTradesHelper(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5));
    }
    if (which == "parseTrades") {
      if (count <= 1)
        return this->parseTrades(::getValue(args, 0));
      if (count == 2)
        return this->parseTrades(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseTrades(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
      if (count == 4)
        return this->parseTrades(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3));
      if (count >= 5)
        return this->parseTrades(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3),
                                 ::getValue(args, 4));
    }
    if (which == "parseWsTrades") {
      if (count <= 1)
        return this->parseWsTrades(::getValue(args, 0));
      if (count == 2)
        return this->parseWsTrades(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseWsTrades(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
      if (count == 4)
        return this->parseWsTrades(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
      if (count >= 5)
        return this->parseWsTrades(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3),
                                   ::getValue(args, 4));
    }
    if (which == "parseTransactions") {
      if (count <= 1)
        return this->parseTransactions(::getValue(args, 0));
      if (count == 2)
        return this->parseTransactions(::getValue(args, 0),
                                       ::getValue(args, 1));
      if (count == 3)
        return this->parseTransactions(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2));
      if (count == 4)
        return this->parseTransactions(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2),
                                       ::getValue(args, 3));
      if (count >= 5)
        return this->parseTransactions(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2), ::getValue(args, 3),
                                       ::getValue(args, 4));
    }
    if (which == "parseTransfers") {
      if (count <= 1)
        return this->parseTransfers(::getValue(args, 0));
      if (count == 2)
        return this->parseTransfers(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseTransfers(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
      if (count == 4)
        return this->parseTransfers(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3));
      if (count >= 5)
        return this->parseTransfers(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3),
                                    ::getValue(args, 4));
    }
    if (which == "parseLedger") {
      if (count <= 1)
        return this->parseLedger(::getValue(args, 0));
      if (count == 2)
        return this->parseLedger(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseLedger(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
      if (count == 4)
        return this->parseLedger(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3));
      if (count >= 5)
        return this->parseLedger(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3),
                                 ::getValue(args, 4));
    }
    if (which == "nonce") {
      if (true)
        return this->nonce();
    }
    if (which == "setHeaders") {
      if (true)
        return this->setHeaders(::getValue(args, 0));
    }
    if (which == "currencyId") {
      if (true)
        return this->currencyId(::getValue(args, 0));
    }
    if (which == "marketId") {
      if (true)
        return this->marketId(::getValue(args, 0));
    }
    if (which == "symbol") {
      if (true)
        return this->symbol(::getValue(args, 0));
    }
    if (which == "handleParamString") {
      if (count <= 2)
        return this->handleParamString(::getValue(args, 0),
                                       ::getValue(args, 1));
      if (count >= 3)
        return this->handleParamString(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2));
    }
    if (which == "handleParamString2") {
      if (count <= 3)
        return this->handleParamString2(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->handleParamString2(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "handleParamInteger") {
      if (count <= 2)
        return this->handleParamInteger(::getValue(args, 0),
                                        ::getValue(args, 1));
      if (count >= 3)
        return this->handleParamInteger(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "handleParamInteger2") {
      if (count <= 3)
        return this->handleParamInteger2(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->handleParamInteger2(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "handleParamBool") {
      if (count <= 2)
        return this->handleParamBool(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->handleParamBool(::getValue(args, 0), ::getValue(args, 1),
                                     ::getValue(args, 2));
    }
    if (which == "handleParamBool2") {
      if (count <= 3)
        return this->handleParamBool2(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2));
      if (count >= 4)
        return this->handleParamBool2(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "handleRequestNetwork") {
      if (count <= 3)
        return this->handleRequestNetwork(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->handleRequestNetwork(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->handleRequestNetwork(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "resolvePath") {
      if (true)
        return this->resolvePath(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "getListFromObjectValues") {
      if (true)
        return this->getListFromObjectValues(::getValue(args, 0),
                                             ::getValue(args, 1));
    }
    if (which == "filterByArray") {
      if (count <= 2)
        return this->filterByArray(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->filterByArray(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
      if (count >= 4)
        return this->filterByArray(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "filterOutByArray") {
      if (count <= 2)
        return this->filterOutByArray(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->filterOutByArray(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2));
      if (count >= 4)
        return this->filterOutByArray(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "buildOHLCVC") {
      if (count <= 1)
        return this->buildOHLCVC(::getValue(args, 0));
      if (count == 2)
        return this->buildOHLCVC(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->buildOHLCVC(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
      if (count >= 4)
        return this->buildOHLCVC(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "parseTradingViewOHLCV") {
      if (count <= 1)
        return this->parseTradingViewOHLCV(::getValue(args, 0));
      if (count == 2)
        return this->parseTradingViewOHLCV(::getValue(args, 0),
                                           ::getValue(args, 1));
      if (count == 3)
        return this->parseTradingViewOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->parseTradingViewOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->parseTradingViewOHLCV(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "parseOrderBookBidAsk") {
      if (count <= 1)
        return this->parseOrderBookBidAsk(::getValue(args, 0));
      if (count == 2)
        return this->parseOrderBookBidAsk(::getValue(args, 0),
                                          ::getValue(args, 1));
      if (count == 3)
        return this->parseOrderBookBidAsk(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->parseOrderBookBidAsk(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "safeCurrency") {
      if (count <= 1)
        return this->safeCurrency(::getValue(args, 0));
      if (count >= 2)
        return this->safeCurrency(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "safeMarket") {
      if (count <= 0)
        return this->safeMarket();
      if (count == 1)
        return this->safeMarket(::getValue(args, 0));
      if (count == 2)
        return this->safeMarket(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->safeMarket(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2));
      if (count >= 4)
        return this->safeMarket(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "marketOrNull") {
      if (count <= 0)
        return this->marketOrNull();
      if (count >= 1)
        return this->marketOrNull(::getValue(args, 0));
    }
    if (which == "checkRequiredCredentials") {
      if (count <= 0)
        return this->checkRequiredCredentials();
      if (count >= 1)
        return this->checkRequiredCredentials(::getValue(args, 0));
    }
    if (which == "oath") {
      if (true)
        return this->oath();
    }
    if (which == "parseBalance") {
      if (true)
        return this->parseBalance(::getValue(args, 0));
    }
    if (which == "getSupportedMapping") {
      if (count <= 1)
        return this->getSupportedMapping(::getValue(args, 0));
      if (count >= 2)
        return this->getSupportedMapping(::getValue(args, 0),
                                         ::getValue(args, 1));
    }
    if (which == "requireValue") {
      if (count <= 1)
        return this->requireValue(::getValue(args, 0));
      if (count >= 2)
        return this->requireValue(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "handleOptionAndParams") {
      if (count <= 3)
        return this->handleOptionAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->handleOptionAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "handleOptionAndParams2") {
      if (count <= 4)
        return this->handleOptionAndParams2(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->handleOptionAndParams2(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "handleOption") {
      if (count <= 2)
        return this->handleOption(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->handleOption(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2));
    }
    if (which == "handleMarketTypeAndParams") {
      if (count <= 1)
        return this->handleMarketTypeAndParams(::getValue(args, 0));
      if (count == 2)
        return this->handleMarketTypeAndParams(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count == 3)
        return this->handleMarketTypeAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->handleMarketTypeAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "handleSubTypeAndParams") {
      if (count <= 1)
        return this->handleSubTypeAndParams(::getValue(args, 0));
      if (count == 2)
        return this->handleSubTypeAndParams(::getValue(args, 0),
                                            ::getValue(args, 1));
      if (count == 3)
        return this->handleSubTypeAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->handleSubTypeAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "handleMarginModeAndParams") {
      if (count <= 1)
        return this->handleMarginModeAndParams(::getValue(args, 0));
      if (count == 2)
        return this->handleMarginModeAndParams(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count >= 3)
        return this->handleMarginModeAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "throwExactlyMatchedException") {
      if (true) {
        this->throwExactlyMatchedException(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return std::any{};
      }
    }
    if (which == "throwBroadlyMatchedException") {
      if (true) {
        this->throwBroadlyMatchedException(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return std::any{};
      }
    }
    if (which == "findBroadlyMatchedKey") {
      if (true)
        return this->findBroadlyMatchedKey(::getValue(args, 0),
                                           ::getValue(args, 1));
    }
    if (which == "handleErrors") {
      if (true)
        return this->handleErrors(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7), ::getValue(args, 8));
    }
    if (which == "calculateRateLimiterCost") {
      if (count <= 4)
        return this->calculateRateLimiterCost(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->calculateRateLimiterCost(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "fetchConvertTradeHistory") {
      if (count <= 0)
        return awaitValue(this->fetchConvertTradeHistory());
      if (count == 1)
        return awaitValue(this->fetchConvertTradeHistory(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchConvertTradeHistory(::getValue(args, 0),
                                                         ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchConvertTradeHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->fetchConvertTradeHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "setTakeProfitAndStopLossParams") {
      if (count <= 4)
        return this->setTakeProfitAndStopLossParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count == 5)
        return this->setTakeProfitAndStopLossParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
      if (count == 6)
        return this->setTakeProfitAndStopLossParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5));
      if (count == 7)
        return this->setTakeProfitAndStopLossParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6));
      if (count >= 8)
        return this->setTakeProfitAndStopLossParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7));
    }
    if (which == "fetchDepositsWithdrawals") {
      if (count <= 0)
        return awaitValue(this->fetchDepositsWithdrawals());
      if (count == 1)
        return awaitValue(this->fetchDepositsWithdrawals(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchDepositsWithdrawals(::getValue(args, 0),
                                                         ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchDepositsWithdrawals(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->fetchDepositsWithdrawals(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "fetchFundingRateHistory") {
      if (count <= 0)
        return awaitValue(this->fetchFundingRateHistory());
      if (count == 1)
        return awaitValue(this->fetchFundingRateHistory(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchFundingRateHistory(::getValue(args, 0),
                                                        ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchFundingRateHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->fetchFundingRateHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "parseLastPrice") {
      if (count <= 1)
        return this->parseLastPrice(::getValue(args, 0));
      if (count >= 2)
        return this->parseLastPrice(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "account") {
      if (true)
        return this->account();
    }
    if (which == "commonCurrencyCode") {
      if (true)
        return this->commonCurrencyCode(::getValue(args, 0));
    }
    if (which == "currency") {
      if (true)
        return this->currency(::getValue(args, 0));
    }
    if (which == "market") {
      if (true)
        return this->market(::getValue(args, 0));
    }
    if (which == "createExpiredOptionMarket") {
      if (true)
        return this->createExpiredOptionMarket(::getValue(args, 0));
    }
    if (which == "isLeveragedCurrency") {
      if (count <= 1)
        return this->isLeveragedCurrency(::getValue(args, 0));
      if (count == 2)
        return this->isLeveragedCurrency(::getValue(args, 0),
                                         ::getValue(args, 1));
      if (count >= 3)
        return this->isLeveragedCurrency(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "handleWithdrawTagAndParams") {
      if (true)
        return this->handleWithdrawTagAndParams(::getValue(args, 0),
                                                ::getValue(args, 1));
    }
    if (which == "costToPrecision") {
      if (true)
        return this->costToPrecision(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "priceToPrecision") {
      if (true)
        return this->priceToPrecision(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "amountToPrecision") {
      if (true)
        return this->amountToPrecision(::getValue(args, 0),
                                       ::getValue(args, 1));
    }
    if (which == "feeToPrecision") {
      if (true)
        return this->feeToPrecision(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "currencyToPrecision") {
      if (count <= 2)
        return this->currencyToPrecision(::getValue(args, 0),
                                         ::getValue(args, 1));
      if (count >= 3)
        return this->currencyToPrecision(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "forceString") {
      if (true)
        return this->forceString(::getValue(args, 0));
    }
    if (which == "isTickPrecision") {
      if (true)
        return this->isTickPrecision();
    }
    if (which == "isDecimalPrecision") {
      if (true)
        return this->isDecimalPrecision();
    }
    if (which == "isSignificantPrecision") {
      if (true)
        return this->isSignificantPrecision();
    }
    if (which == "safeNumber") {
      if (count <= 2)
        return this->safeNumber(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeNumber(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2));
    }
    if (which == "safeNumberN") {
      if (count <= 2)
        return this->safeNumberN(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->safeNumberN(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
    }
    if (which == "parsePrecision") {
      if (true)
        return this->parsePrecision(::getValue(args, 0));
    }
    if (which == "integerPrecisionToAmount") {
      if (true)
        return this->integerPrecisionToAmount(::getValue(args, 0));
    }
    if (which == "implodeHostname") {
      if (true)
        return this->implodeHostname(::getValue(args, 0));
    }
    if (which == "safeCurrencyCode") {
      if (count <= 1)
        return this->safeCurrencyCode(::getValue(args, 0));
      if (count >= 2)
        return this->safeCurrencyCode(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "filterBySymbolSinceLimit") {
      if (count <= 1)
        return this->filterBySymbolSinceLimit(::getValue(args, 0));
      if (count == 2)
        return this->filterBySymbolSinceLimit(::getValue(args, 0),
                                              ::getValue(args, 1));
      if (count == 3)
        return this->filterBySymbolSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->filterBySymbolSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->filterBySymbolSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "filterByCurrencySinceLimit") {
      if (count <= 1)
        return this->filterByCurrencySinceLimit(::getValue(args, 0));
      if (count == 2)
        return this->filterByCurrencySinceLimit(::getValue(args, 0),
                                                ::getValue(args, 1));
      if (count == 3)
        return this->filterByCurrencySinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->filterByCurrencySinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->filterByCurrencySinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "filterBySymbolsSinceLimit") {
      if (count <= 1)
        return this->filterBySymbolsSinceLimit(::getValue(args, 0));
      if (count == 2)
        return this->filterBySymbolsSinceLimit(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count == 3)
        return this->filterBySymbolsSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count == 4)
        return this->filterBySymbolsSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
      if (count >= 5)
        return this->filterBySymbolsSinceLimit(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4));
    }
    if (which == "parseLastPrices") {
      if (count <= 1)
        return this->parseLastPrices(::getValue(args, 0));
      if (count == 2)
        return this->parseLastPrices(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parseLastPrices(::getValue(args, 0), ::getValue(args, 1),
                                     ::getValue(args, 2));
    }
    if (which == "parseTickers") {
      if (count <= 1)
        return this->parseTickers(::getValue(args, 0));
      if (count == 2)
        return this->parseTickers(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parseTickers(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2));
    }
    if (which == "parseDepositAddresses") {
      if (count <= 1)
        return this->parseDepositAddresses(::getValue(args, 0));
      if (count == 2)
        return this->parseDepositAddresses(::getValue(args, 0),
                                           ::getValue(args, 1));
      if (count == 3)
        return this->parseDepositAddresses(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->parseDepositAddresses(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "parseBorrowInterests") {
      if (count <= 1)
        return this->parseBorrowInterests(::getValue(args, 0));
      if (count >= 2)
        return this->parseBorrowInterests(::getValue(args, 0),
                                          ::getValue(args, 1));
    }
    if (which == "parseBorrowRate") {
      if (count <= 1)
        return this->parseBorrowRate(::getValue(args, 0));
      if (count >= 2)
        return this->parseBorrowRate(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseBorrowRateHistory") {
      if (true)
        return this->parseBorrowRateHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "parseIsolatedBorrowRates") {
      if (true)
        return this->parseIsolatedBorrowRates(::getValue(args, 0));
    }
    if (which == "parseFundingRateHistories") {
      if (count <= 1)
        return this->parseFundingRateHistories(::getValue(args, 0));
      if (count == 2)
        return this->parseFundingRateHistories(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count == 3)
        return this->parseFundingRateHistories(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->parseFundingRateHistories(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "safeSymbol") {
      if (count <= 1)
        return this->safeSymbol(::getValue(args, 0));
      if (count == 2)
        return this->safeSymbol(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->safeSymbol(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2));
      if (count >= 4)
        return this->safeSymbol(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "parseFundingRate") {
      if (count <= 1)
        return this->parseFundingRate(::getValue(args, 0));
      if (count >= 2)
        return this->parseFundingRate(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseFundingRates") {
      if (count <= 1)
        return this->parseFundingRates(::getValue(args, 0));
      if (count >= 2)
        return this->parseFundingRates(::getValue(args, 0),
                                       ::getValue(args, 1));
    }
    if (which == "parseLongShortRatio") {
      if (count <= 1)
        return this->parseLongShortRatio(::getValue(args, 0));
      if (count >= 2)
        return this->parseLongShortRatio(::getValue(args, 0),
                                         ::getValue(args, 1));
    }
    if (which == "parseLongShortRatioHistory") {
      if (count <= 1)
        return this->parseLongShortRatioHistory(::getValue(args, 0));
      if (count == 2)
        return this->parseLongShortRatioHistory(::getValue(args, 0),
                                                ::getValue(args, 1));
      if (count == 3)
        return this->parseLongShortRatioHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->parseLongShortRatioHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "handleTriggerPricesAndParams") {
      if (count <= 2)
        return this->handleTriggerPricesAndParams(::getValue(args, 0),
                                                  ::getValue(args, 1));
      if (count >= 3)
        return this->handleTriggerPricesAndParams(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "handleTriggerAndParams") {
      if (true)
        return this->handleTriggerAndParams(::getValue(args, 0));
    }
    if (which == "isTriggerOrder") {
      if (true)
        return this->isTriggerOrder(::getValue(args, 0));
    }
    if (which == "isPostOnly") {
      if (count <= 2)
        return this->isPostOnly(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->isPostOnly(::getValue(args, 0), ::getValue(args, 1),
                                ::getValue(args, 2));
    }
    if (which == "handlePostOnly") {
      if (count <= 2)
        return this->handlePostOnly(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->handlePostOnly(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
    }
    if (which == "parseOpenInterest") {
      if (count <= 1)
        return this->parseOpenInterest(::getValue(args, 0));
      if (count >= 2)
        return this->parseOpenInterest(::getValue(args, 0),
                                       ::getValue(args, 1));
    }
    if (which == "parseOpenInterests") {
      if (count <= 1)
        return this->parseOpenInterests(::getValue(args, 0));
      if (count >= 2)
        return this->parseOpenInterests(::getValue(args, 0),
                                        ::getValue(args, 1));
    }
    if (which == "parseOpenInterestsHistory") {
      if (count <= 1)
        return this->parseOpenInterestsHistory(::getValue(args, 0));
      if (count == 2)
        return this->parseOpenInterestsHistory(::getValue(args, 0),
                                               ::getValue(args, 1));
      if (count == 3)
        return this->parseOpenInterestsHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->parseOpenInterestsHistory(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "handleTimeInForce") {
      if (count <= 0)
        return this->handleTimeInForce();
      if (count >= 1)
        return this->handleTimeInForce(::getValue(args, 0));
    }
    if (which == "convertTypeToAccount") {
      if (true)
        return this->convertTypeToAccount(::getValue(args, 0));
    }
    if (which == "checkRequiredArgument") {
      if (count <= 3) {
        this->checkRequiredArgument(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
        return std::any{};
      }
      if (count >= 4) {
        this->checkRequiredArgument(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3));
        return std::any{};
      }
    }
    if (which == "checkRequiredMarginArgument") {
      if (true) {
        this->checkRequiredMarginArgument(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
        return std::any{};
      }
    }
    if (which == "parseDepositWithdrawFees") {
      if (count <= 1)
        return this->parseDepositWithdrawFees(::getValue(args, 0));
      if (count == 2)
        return this->parseDepositWithdrawFees(::getValue(args, 0),
                                              ::getValue(args, 1));
      if (count >= 3)
        return this->parseDepositWithdrawFees(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
    }
    if (which == "parseDepositWithdrawFee") {
      if (count <= 1)
        return this->parseDepositWithdrawFee(::getValue(args, 0));
      if (count >= 2)
        return this->parseDepositWithdrawFee(::getValue(args, 0),
                                             ::getValue(args, 1));
    }
    if (which == "depositWithdrawFee") {
      if (true)
        return this->depositWithdrawFee(::getValue(args, 0));
    }
    if (which == "parseIncome") {
      if (count <= 1)
        return this->parseIncome(::getValue(args, 0));
      if (count >= 2)
        return this->parseIncome(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseIncomes") {
      if (count <= 1)
        return this->parseIncomes(::getValue(args, 0));
      if (count == 2)
        return this->parseIncomes(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseIncomes(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2));
      if (count >= 4)
        return this->parseIncomes(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "getMarketFromSymbols") {
      if (count <= 0)
        return this->getMarketFromSymbols();
      if (count >= 1)
        return this->getMarketFromSymbols(::getValue(args, 0));
    }
    if (which == "parseWsOHLCVs") {
      if (count <= 1)
        return this->parseWsOHLCVs(::getValue(args, 0));
      if (count == 2)
        return this->parseWsOHLCVs(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseWsOHLCVs(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2));
      if (count == 4)
        return this->parseWsOHLCVs(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3));
      if (count >= 5)
        return this->parseWsOHLCVs(::getValue(args, 0), ::getValue(args, 1),
                                   ::getValue(args, 2), ::getValue(args, 3),
                                   ::getValue(args, 4));
    }
    if (which == "filterByArrayPositions") {
      if (count <= 2)
        return this->filterByArrayPositions(::getValue(args, 0),
                                            ::getValue(args, 1));
      if (count == 3)
        return this->filterByArrayPositions(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->filterByArrayPositions(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "filterByArrayTickers") {
      if (count <= 2)
        return this->filterByArrayTickers(::getValue(args, 0),
                                          ::getValue(args, 1));
      if (count == 3)
        return this->filterByArrayTickers(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->filterByArrayTickers(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "filterByArrayADLRanks") {
      if (count <= 2)
        return this->filterByArrayADLRanks(::getValue(args, 0),
                                           ::getValue(args, 1));
      if (count == 3)
        return this->filterByArrayADLRanks(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->filterByArrayADLRanks(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "createOHLCVObject") {
      if (true)
        return this->createOHLCVObject(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2));
    }
    if (which == "fetchPaginatedCallDynamic") {
      if (count <= 1)
        return awaitValue(this->fetchPaginatedCallDynamic(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchPaginatedCallDynamic(::getValue(args, 0),
                                                          ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchPaginatedCallDynamic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchPaginatedCallDynamic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->fetchPaginatedCallDynamic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->fetchPaginatedCallDynamic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count >= 7)
        return awaitValue(this->fetchPaginatedCallDynamic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
    }
    if (which == "fetchPaginatedCallDeterministic") {
      if (count <= 1)
        return awaitValue(
            this->fetchPaginatedCallDeterministic(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchPaginatedCallDeterministic(
            ::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchPaginatedCallDeterministic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchPaginatedCallDeterministic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->fetchPaginatedCallDeterministic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->fetchPaginatedCallDeterministic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count >= 7)
        return awaitValue(this->fetchPaginatedCallDeterministic(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
    }
    if (which == "fetchPaginatedCallCursor") {
      if (count <= 1)
        return awaitValue(this->fetchPaginatedCallCursor(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchPaginatedCallCursor(::getValue(args, 0),
                                                         ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchPaginatedCallCursor(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchPaginatedCallCursor(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->fetchPaginatedCallCursor(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->fetchPaginatedCallCursor(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count == 7)
        return awaitValue(this->fetchPaginatedCallCursor(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
      if (count == 8)
        return awaitValue(this->fetchPaginatedCallCursor(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7)));
      if (count >= 9)
        return awaitValue(this->fetchPaginatedCallCursor(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7), ::getValue(args, 8)));
    }
    if (which == "fetchPaginatedCallIncremental") {
      if (count <= 1)
        return awaitValue(
            this->fetchPaginatedCallIncremental(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchPaginatedCallIncremental(
            ::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchPaginatedCallIncremental(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count == 4)
        return awaitValue(this->fetchPaginatedCallIncremental(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->fetchPaginatedCallIncremental(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->fetchPaginatedCallIncremental(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count >= 7)
        return awaitValue(this->fetchPaginatedCallIncremental(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
    }
    if (which == "sortCursorPaginatedResult") {
      if (true)
        return this->sortCursorPaginatedResult(::getValue(args, 0));
    }
    if (which == "removeRepeatedTradesFromArray") {
      if (true)
        return this->removeRepeatedTradesFromArray(::getValue(args, 0));
    }
    if (which == "removeKeysFromDict") {
      if (true)
        return this->removeKeysFromDict(::getValue(args, 0),
                                        ::getValue(args, 1));
    }
    if (which == "handleUntilOption") {
      if (count <= 3)
        return this->handleUntilOption(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2));
      if (count >= 4)
        return this->handleUntilOption(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2),
                                       ::getValue(args, 3));
    }
    if (which == "safeOpenInterest") {
      if (count <= 1)
        return this->safeOpenInterest(::getValue(args, 0));
      if (count >= 2)
        return this->safeOpenInterest(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseLiquidation") {
      if (count <= 1)
        return this->parseLiquidation(::getValue(args, 0));
      if (count >= 2)
        return this->parseLiquidation(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseLiquidations") {
      if (count <= 1)
        return this->parseLiquidations(::getValue(args, 0));
      if (count == 2)
        return this->parseLiquidations(::getValue(args, 0),
                                       ::getValue(args, 1));
      if (count == 3)
        return this->parseLiquidations(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2));
      if (count >= 4)
        return this->parseLiquidations(::getValue(args, 0), ::getValue(args, 1),
                                       ::getValue(args, 2),
                                       ::getValue(args, 3));
    }
    if (which == "parseGreeks") {
      if (count <= 1)
        return this->parseGreeks(::getValue(args, 0));
      if (count >= 2)
        return this->parseGreeks(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseAllGreeks") {
      if (count <= 1)
        return this->parseAllGreeks(::getValue(args, 0));
      if (count == 2)
        return this->parseAllGreeks(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parseAllGreeks(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
    }
    if (which == "parseOption") {
      if (count <= 1)
        return this->parseOption(::getValue(args, 0));
      if (count == 2)
        return this->parseOption(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parseOption(::getValue(args, 0), ::getValue(args, 1),
                                 ::getValue(args, 2));
    }
    if (which == "parseOptionChain") {
      if (count <= 1)
        return this->parseOptionChain(::getValue(args, 0));
      if (count == 2)
        return this->parseOptionChain(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parseOptionChain(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2));
    }
    if (which == "parseMarginModes") {
      if (count <= 1)
        return this->parseMarginModes(::getValue(args, 0));
      if (count == 2)
        return this->parseMarginModes(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseMarginModes(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2));
      if (count >= 4)
        return this->parseMarginModes(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "parseMarginMode") {
      if (count <= 1)
        return this->parseMarginMode(::getValue(args, 0));
      if (count >= 2)
        return this->parseMarginMode(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseLeverages") {
      if (count <= 1)
        return this->parseLeverages(::getValue(args, 0));
      if (count == 2)
        return this->parseLeverages(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseLeverages(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2));
      if (count >= 4)
        return this->parseLeverages(::getValue(args, 0), ::getValue(args, 1),
                                    ::getValue(args, 2), ::getValue(args, 3));
    }
    if (which == "parseLeverage") {
      if (count <= 1)
        return this->parseLeverage(::getValue(args, 0));
      if (count >= 2)
        return this->parseLeverage(::getValue(args, 0), ::getValue(args, 1));
    }
    if (which == "parseConversions") {
      if (count <= 1)
        return this->parseConversions(::getValue(args, 0));
      if (count == 2)
        return this->parseConversions(::getValue(args, 0), ::getValue(args, 1));
      if (count == 3)
        return this->parseConversions(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2));
      if (count == 4)
        return this->parseConversions(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3));
      if (count == 5)
        return this->parseConversions(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3),
                                      ::getValue(args, 4));
      if (count == 6)
        return this->parseConversions(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3),
                                      ::getValue(args, 4), ::getValue(args, 5));
      if (count >= 7)
        return this->parseConversions(::getValue(args, 0), ::getValue(args, 1),
                                      ::getValue(args, 2), ::getValue(args, 3),
                                      ::getValue(args, 4), ::getValue(args, 5),
                                      ::getValue(args, 6));
    }
    if (which == "parseConversion") {
      if (count <= 1)
        return this->parseConversion(::getValue(args, 0));
      if (count == 2)
        return this->parseConversion(::getValue(args, 0), ::getValue(args, 1));
      if (count >= 3)
        return this->parseConversion(::getValue(args, 0), ::getValue(args, 1),
                                     ::getValue(args, 2));
    }
    if (which == "convertExpireDate") {
      if (true)
        return this->convertExpireDate(::getValue(args, 0));
    }
    if (which == "convertExpireDateToMarketIdDate") {
      if (true)
        return this->convertExpireDateToMarketIdDate(::getValue(args, 0));
    }
    if (which == "convertMarketIdExpireDate") {
      if (true)
        return this->convertMarketIdExpireDate(::getValue(args, 0));
    }
    if (which == "loadMarketsAndSignIn") {
      if (true)
        return awaitValue(this->loadMarketsAndSignIn());
    }
    if (which == "parseMarginModification") {
      if (count <= 1)
        return this->parseMarginModification(::getValue(args, 0));
      if (count >= 2)
        return this->parseMarginModification(::getValue(args, 0),
                                             ::getValue(args, 1));
    }
    if (which == "parseMarginModifications") {
      if (count <= 1)
        return this->parseMarginModifications(::getValue(args, 0));
      if (count == 2)
        return this->parseMarginModifications(::getValue(args, 0),
                                              ::getValue(args, 1));
      if (count == 3)
        return this->parseMarginModifications(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2));
      if (count >= 4)
        return this->parseMarginModifications(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3));
    }
    if (which == "cleanUnsubscription") {
      if (count <= 3) {
        this->cleanUnsubscription(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2));
        return std::any{};
      }
      if (count >= 4) {
        this->cleanUnsubscription(::getValue(args, 0), ::getValue(args, 1),
                                  ::getValue(args, 2), ::getValue(args, 3));
        return std::any{};
      }
    }
    if (which == "cleanCache") {
      if (true) {
        this->cleanCache(::getValue(args, 0));
        return std::any{};
      }
    }
    if (which == "timeframeFromMilliseconds") {
      if (true)
        return this->timeframeFromMilliseconds(::getValue(args, 0));
    }
    if (which == "fetchCanceledAndClosedOrders") {
      if (count <= 0)
        return awaitValue(this->fetchCanceledAndClosedOrders());
      if (count == 1)
        return awaitValue(
            this->fetchCanceledAndClosedOrders(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->fetchCanceledAndClosedOrders(
            ::getValue(args, 0), ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->fetchCanceledAndClosedOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->fetchCanceledAndClosedOrders(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "createOrderWithTakeProfitAndStopLossWs") {
      if (count <= 4)
        return awaitValue(this->createOrderWithTakeProfitAndStopLossWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->createOrderWithTakeProfitAndStopLossWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->createOrderWithTakeProfitAndStopLossWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count == 7)
        return awaitValue(this->createOrderWithTakeProfitAndStopLossWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
      if (count >= 8)
        return awaitValue(this->createOrderWithTakeProfitAndStopLossWs(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7)));
    }
    if (which == "editOrder") {
      if (count <= 4)
        return awaitValue(
            this->editOrder(::getValue(args, 0), ::getValue(args, 1),
                            ::getValue(args, 2), ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->editOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->editOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count >= 7)
        return awaitValue(this->editOrder(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
    }
    if (which == "editOrderWithClientOrderId") {
      if (count <= 4)
        return awaitValue(this->editOrderWithClientOrderId(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->editOrderWithClientOrderId(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->editOrderWithClientOrderId(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count >= 7)
        return awaitValue(this->editOrderWithClientOrderId(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
    }
    if (which == "watchPositionForSymbols") {
      if (count <= 0)
        return awaitValue(this->watchPositionForSymbols());
      if (count == 1)
        return awaitValue(this->watchPositionForSymbols(::getValue(args, 0)));
      if (count == 2)
        return awaitValue(this->watchPositionForSymbols(::getValue(args, 0),
                                                        ::getValue(args, 1)));
      if (count == 3)
        return awaitValue(this->watchPositionForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2)));
      if (count >= 4)
        return awaitValue(this->watchPositionForSymbols(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
    }
    if (which == "createOrderWithTakeProfitAndStopLoss") {
      if (count <= 4)
        return awaitValue(this->createOrderWithTakeProfitAndStopLoss(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3)));
      if (count == 5)
        return awaitValue(this->createOrderWithTakeProfitAndStopLoss(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4)));
      if (count == 6)
        return awaitValue(this->createOrderWithTakeProfitAndStopLoss(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5)));
      if (count == 7)
        return awaitValue(this->createOrderWithTakeProfitAndStopLoss(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6)));
      if (count >= 8)
        return awaitValue(this->createOrderWithTakeProfitAndStopLoss(
            ::getValue(args, 0), ::getValue(args, 1), ::getValue(args, 2),
            ::getValue(args, 3), ::getValue(args, 4), ::getValue(args, 5),
            ::getValue(args, 6), ::getValue(args, 7)));
    }
    // not defined on this exchange: fall back to the TS parent class
    return ccxt::okx::callMethod(name, args);
  }
};

} // namespace pro
} // namespace ccxt
